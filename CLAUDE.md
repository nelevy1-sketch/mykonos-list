## חוק עבודה קבוע
לעולם אל תבצע git commit או git push בלי לעצור קודם ולהראות לי בדיוק מה השתנה (diff) ולחכות לאישור מפורש ממני. זה חל תמיד - גם כשמשימה נראית פשוטה, וגם כשסוכן משנה (coder, qa-tester, או כל סוכן עתידי) מבצע את העבודה.

## צפיפות ויזואלית

האפליקציה היא מובייל-first. כל רכיב חדש צריך להיות הגרסה
הקטנה ביותר שעדיין קריאה.

- ברירת המחדל היא לא להוסיף. כל שורה, אייקון, padding
  ו-gap צריכים להצדיק את הגובה שהם גוזלים.
- העדף מידע בשורה אחת על פני הערמה אנכית.
- מידע שאפשר להסיק מההקשר — אל תציג. (למשל: תאריך
  מספרי ליד יום בשבוע בשבוע הקרוב.)
- "עדין" ו"לא לתפוס מקום" הן דרישות מחייבות, לא המלצות.
  אם אין אילוץ מספרי במשימה — תשאל לפני שאתה בונה.
- בסיום כל משימת UI: הצג צילום מסך של הרכיב בהקשר
  העמוד המלא, לא במנותק. אם הרכיב מגדיל את הגלילה
  בעמוד — הוא כנראה גדול מדי.

## ניהול גרסה

מקור האמת היחיד הוא version.js. אף פעם לא לקודד מספר
גרסה בשום קובץ אחר.

בכל commit שמשנה התנהגות שהמשתמש רואה, עדכן את version.js
ואת CHANGELOG.md כחלק מאותו commit:
- תיקון באג → העלאת patch
- תכונה חדשה או שינוי סכמה → העלאת minor
- שינוי שובר תאימות → major

לא מעדכנים גרסה עבור: שינויי תיעוד בלבד, הערות, או
שינויים פנימיים שלא משנים שום דבר שהמשתמש חווה.

**חריג**: כל קובץ שנטען לדפדפן (אחד מ-6 העמודים, או קובץ
`<script src>` כמו `datetime.js`/`legs.js`) מקבל version bump
בכל שינוי - גם אם אף קוד עדיין לא קורא לתוספת. `scripts/`
(כלי פיתוח, לא נטען לדפדפן) לא. הסיבה מעשית: הגרסה משמשת
למעקב אחרי מה בפרודקשן ואיזה commit הכניס מה - commit בלי
bump נעלם מהרצף.

הצג את עדכון הגרסה כחלק מה-diff לאישור, לא כ-commit נפרד.

## Cloud Functions (functions/) והחשיפה ב-GitHub Pages

הריפו הזה משרת גם את האתר הסטטי (GitHub Pages) וגם מכיל
את קוד ה-Cloud Functions (functions/, מ-2026-09-06, הועבר
מפרויקט gittrip-ai נפרד). המשמעות: כל קובץ תחת functions/
(כמו functions/index.js) נגיש טכנית כקובץ טקסט גולמי דרך
כתובת ה-Pages, בדיוק כמו packing.html או כל קובץ אחר בריפו.

נכון לעכשיו זה תקין - אין שם מפתחות API (הם ב-Secret
Manager, לא בקוד) ואין לוגיקה עסקית רגישה. אבל אם בעתיד
נכנס לפונקציה משהו שלא רוצים לחשוף (פרומפט מורכב, לוגיקה
עסקית) - צריך לטפל בזה (למשל: להוציא את functions/ לריפו
נפרד, או להגביל מה ה-Pages בפועל מגיש).

## מבנה סקריפטים בין 6 הדפים — אותה חלוקה חוזרת

index.html הוא היוצא מן הכלל היחיד מבין 6 העמודים, בשני
דברים שקשורים זה לזה (לא צירוף מקרים - אותה סיבה):

- **SDK**: index.html על Firebase compat v8
  (`db.ref(path).on(...)`). wizard/places/packing/shopping/
  itinerary כולם על מודולרי v9 (`ref(db,...)`, `onValue(...)`).
- **סוג הסקריפט**: בגלל זה, הסקריפט הראשי של index.html הוא
  script קלאסי (לא `type="module"`). ב-5 העמודים האחרים
  הסקריפט הראשי הוא `type="module"` (import דורש module).

ההשלכה שממשיכה להפתיע: הצהרת `function` בשורה עליונה
בסקריפט קלאסי נדבקת אוטומטית ל-`window` (למשל `window.toast`).
אותה הצהרה בתוך `type="module"` נשארת מקומית למודול ולעולם
לא נוגעת ב-`window` - **גם אם לשתי הפונקציות אותו שם בדיוק**.
גילינו את זה בפועל כשהוספנו את toast.js (ר' הסעיף הבא):
`window.toast` נדרס רק ב-index.html (הסקריפט המקומי שלו רץ
אחרי toast.js ומחליף אותו) - לא נדרס באף אחד מ-5 העמודים
האחרים, למרות שלכולם חוץ מ-wizard יש פונקציה מקומית בשם
דומה. לפני שמניחים "כל הדפים מתנהגים אותו דבר" בגלל שהם
"נראים" סימטריים - לבדוק בפועל בדפדפן (`window.toast.toString()`
למשל), לא רק לקרוא את הקוד.

## מסמכי docs/ — מי אחראי על מה

`docs/roadmap.md` הוא רשימת העבודה העדכנית (מה נסגר, מה הבא
בתור, סתירות שצריך להכריע). `docs/schema-legs.md` הוא תיעוד -
ההחלטות שהתקבלו על מבנה הנתונים של יעדים מרובים, לא משימות
פתוחות. §8 שם (סדר המימוש) הושלם במלואו ומפנה בעצמו ל-roadmap.md.

## יעדים מרובים, אופי טיול ו-vibes

**לפני כל משימה שנוגעת ב-`legs`, `tripCharacter`, `vibes` או
`getLegs()` — קרא קודם את `docs/schema-legs.md`.**

המסמך מכריע את כל שאלות המבנה: מה נשמר איפה ומה קורה לטיולים
קיימים. אל תפתח מחדש שאלות שכבר הוכרעו שם. אם משהו במסמך נראה
שגוי — אמור זאת ועצור. אל תסטה בשקט.

### עובדות על מבנה הנתונים שקל לטעות בהן

שתיהן נתפסו רק בהרצה מול RTDB אמיתי, אחרי שעברו סבבי בדיקות:

- **שם השדה הוא `lon`, לא `lng`.** Open-Meteo מחזיר `longitude`
  ורוב הקוד בעולם משתמש ב-`lng`. כאן זה `lon`. קוד שמנחש `lng`
  יקבל `undefined` בשקט.
- **`startAt`/`endAt` הם מחרוזות ISO, לא timestamps מספריים.**
  ערך אמיתי: `"2026-10-06T20:00:00.000Z"`. תמיד לעטוף ב-`new Date()`
  לפני כל חישוב או השוואה.

**הקוד הקיים הוא מקור האמת על מבנה הנתונים, לא התיעוד.**

**`datetime.js` מרכזת `zonedParts`/`dateKeyInZone`/`todayKeyInZone`/
`nowHHMMInZone`** (מ-2026-09-07) - נטענת בכל 6 העמודים באותה תבנית
כמו `legs.js` (script קלאסי, מוצמדת ל-`window`). נוצר אחרי ש-`zonedParts`
נתפסה כעותק זהה בשני מקומות עצמאיים (`itinerary.html`, `index.html`)
ועמד להיווצר עותק שלישי בתיקון של `places.html`. **אל תשכפל את
הפונקציות האלה שוב** - אם צריך לוגיקת זמן/timezone חדשה, זה המקום
הטבעי להוסיף אליה, לא קובץ שלישי.

**`--glow-color` משוכפל ב-4 מקומות עצמאיים** (shared.css + שלושה
בלוקי `<style>` inline נפרדים ב-index/shopping/wizard) ולא מוגדר
במקום אחד - כי index/shopping/wizard לא רשומים ל-`[data-tokens="core"]`
ויש להם כבר ערך מקומי משלהם ל-`--primary`/`--glow-color` שכלל גורף
היה דורס. **`--accent-gradient` (§3.4, מומש ב-v4.17.0) נמנע מהשכפול
הזה** - הוא משתנה חדש בלי ערך קודם בשום עמוד, אז כלל top-level יחיד
ב-shared.css (לא מגודר ב-`[data-tokens="core"]`) מספיק לכל 6 העמודים.

**אותו נימוק חל על `.forecast-leg-separator` (index.html) מול
`.day-leg-separator` (itinerary.html, שלב 7)** - שתי מחלקות נפרדות
בכוונה לאותו רעיון עיצובי (מפריד בין רגליים ברצועה גוללת), לא כפילות
שנשכחה: index.html על גבי כרטיס תמיד-כהה (`color:#fff` קשיח, נכון),
itinerary.html על גבי `[data-tokens="core"]` עם `var(--muted)`. איחוד
למחלקה משותפת ישבור מצב בהיר באחד משני העמודים.

**אותה מלכודת קיימת גם במצבים ריקים (`.empty`/`.empty-state`) - שלוש
הגדרות נפרדות, לא אחת.** `packing.html` (`.empty` + `<span class="emoji">`),
`places.html`/`itinerary.html` (`.empty` + `<b>`, כמעט זהות בין השתיים
אבל לא לגמרי - padding שונה), ו-`shopping.html` (`.empty-state` +
`<strong>`/`<span>` לכותרת/תת-כותרת). שום אחת מהן לא ב-shared.css.
`packing.html` ו-`places.html`/`itinerary.html` חולקות את השם `.empty`
בלי לחלוק CSS בכלל - שתי הגדרות `<style>` מקומיות, עצמאיות. מי שיערוך
אחת לא ישפיע על השנייה, ולא כדאי לאחד אותן בלי משימה ייעודית לזה
(ראה docs/roadmap.md part 3 item 0, commit ה-empty-states - במפורש
לא אוחד שם, כדי לא לערבב תיקון עם רה-פקטור).

**מחלקות `character-X` (מבטא לפי `tripCharacter`) תמיד על `documentElement`,
בכל 6 העמודים - כולל ב-index.html, למרות ש-`theme-X` שם דווקא על `document.body`.**
עקביות מכוונת בין העמודים למחלקות *חדשות* - כדי שלא יתווסף עוד פיצול
כמו זה של `theme-X` (`documentElement` בחלק, `body` באחרים).

**אותו פיצול חל גם על `[data-theme="dark"]` עצמו, לא רק על מחלקות `theme-X`
- וב-`wizard.html` הוא קיים גם בלי שום `theme-X` בכלל.** `index.html` שם את
שניהם (`theme-X` וגם `[data-theme]`) על `document.body`. `wizard.html` אין לו
`theme-X`/`tripCharacter` בכלל (אין עדיין טיול בשלב הזה), אבל שם `[data-theme]`
על `body` בדיוק כמו index.html. שאר 4 העמודים שמים את שניהם על
`documentElement`. **ההשלכה המעשית שנתפסה בפועל (v4.28.2)**: כלל CSS גרידא
כמו `html{background:var(--bg-color)}` לא יכול לעקוב אחרי מצב כהה/סוג טיול
ב-index.html/wizard.html - custom properties לא עוברים בירושה כלפי מעלה
מ-`body` להורה שלו (`html`), אז `html` פשוט לא רואה את הערך שהוגדר על `body`.
בשני העמודים האלה נדרש `window.syncHtmlBackground()` (מעתיק את הרקע
המחושב בפועל מ-`body` ל-`html`, בכל נקודה שמצב `body` משתנה) - לא רק תוספת
CSS כמו בשאר העמודים.

**`background: <gradient>` בלבד לא קובע `background-color` - הוא נשאר
שקוף.** לא אינטואיטיבי, ועלה לנו שני סבבי תיקון עוקבים (v4.28.2 → v4.28.3)
על אותו באג בפועל ב-`shopping.html`: קיצור ה-`background` קובע רק את תת-
המאפיינים שהערך שלו בפועל תואם להם - צבע מלא (`#fff`, `var(--bg)`) כן נופל
תחת `background-color`, אבל `linear-gradient(...)` הוא אך ורק ערך
`background-image`, ולא נוגע ב-`background-color` בכלל. `background-color`
נשאר בברירת המחדל שלו - שקוף - גם כשה-`background` "נראה" כאילו הוא קבע
הכל. **אומת מדוד, לא משוער**: `getComputedStyle(documentElement)
.backgroundColor` בעמוד החי החזיר `rgba(0,0,0,0)`. על אלמנט שאמור לצבוע
שטח שלם (בעיקר `html`, שמצייר את כל הקנבס כולל רצועות safe-area שמעבר
לתיבה של `body` עצמו) - צריך גם `background-color` מפורש, לא רק
`background-image`. אלמנט עם `background` שהוא gradient ויושב על גבי הורה
כבר אטום (כרטיס, badge, פס התקדמות) לא נפגע מזה - זה משפיע רק על אלמנט
שאמור להיות השכבה האטומה בעצמו.

**`wizard.html` כותבת `tripData.schemaVersion: 2` בכל טיול חדש, אבל שום קוד באפליקציה לא קורא את השדה הזה** - לא `getLegs()`, לא שום מקום אחר. אל תניחו שהוא משפיע על משהו.

**באג ידוע ב-`starterLists()` (packing.html): `vibes` דורס את
`clothing.items` במקום למזג עם מה ש-`tripType` כבר שם שם** -
`clothingList.items = [...merged]` מחליף לגמרי, לא מוסיף. לא
תוקן (מחוץ להיקף), תועד כשנוסף `tripCharacter` שכן ממזג נכון.

**סיווג שגוי ידוע בטבלת `vibeClothing`** (אותה פונקציה): לא כל
פריט שם הוא ביגוד - `קרם הגנה`/`Sunscreen` (beach) ו-`תרמיל גב`/
`Backpack` (nature) הם ציוד, לא ביגוד. זה השורש להתנגשות עם
פריטי `tripCharacter` שמסווגים נכון לפי קטגוריה (`gear` מול
`clothing`) - ה-dedup הבין-קטגורי רואה "קרם הגנה" פעמיים ומעדיף
להשאיר אותו במקום שבו `vibes` כבר שם אותו. לא תוקן (מחוץ להיקף).

**`.forecast-strip` (index.html) סובלת משני הפערים שתוקנו
ב-`.quick-picks` (packing.html)**, לא רק ריפוד: **א.** אין
`padding` אופקי בכלל, רק `padding-top`. **ב.** באותה מידה
חמור יותר - סרגל הגלילה מוסתר (`scrollbar-width:none`) בלי
שום מנגנון חלופי במחשב (בלי JS, בלי סרגל חלופי), אז במחשב
(עכבר, בלי מגע) אין שום דרך להגיע לתוכן שגלש - אומת בפועל
ב-`.quick-picks` לפני התיקון: גלגלת אנכית רגילה לא מזיזה
`scrollLeft` בכלל. לא נגעו ב-`.forecast-strip` - מחוץ להיקף.

## שתי מערכות טוסט במקביל (זמני, עד commit נפרד)

נכון לעכשיו קיימות שתי מערכות טוסט נפרדות, בו-זמנית:

- **מקומית** - `function toast()`/`showToast()` בתוך כל עמוד
  (או בתוך ה-module שלו). עדיין משרתת את כל הקריאות הקיימות
  באפליקציה. לא נגעו בה.
- **גלובלית** - `window.toast(message, type)` מ-toast.js +
  מחלקת CSS `.gt-toast` ב-shared.css (סוף הקובץ). תשתית חדשה
  בלבד - נכון לעכשיו שום קוד קיים לא קורא לה בפועל.

איחוד (מחיקת המימושים המקומיים והעברת כל הקריאות הקיימות
ל-`window.toast`) הוא commit נפרד, עדיין לא בוצע. אל תניחו
שיש מערכת טוסט אחת בקוד - תלוי איזה קובץ.

## lastStateBackup ב-shopping.html - הגנה חלקית

`lastStateBackup` (משתנה יחיד ברמת המודול, לא היסטוריה)
משותף ל-5 נקודות כתיבה: `updateBaseQty`, הענף שמעדכן כמות
במוצר קיים ב-`addModalForm`, `savePersonalQtyBtn`,
`removePersonalQtyBtn`, ו-`deleteItem`. כל אחת מהן דורסת
אותו באמצעות `saveUndoBackup()` רגע לפני שהיא יורה כתיבה
משלה.

`undoBtn` (v4.11.1) מגן על `lastStateBackup` מפני איפוס
מוקדם מדי - אבל ההגנה הזו חלקית מטבעה: היא לא מונעת
מפעולה *אחרת* מבין ה-5 לדרוס את הגיבוי הקיים באמצע, אם
המשתמש מבצע פעולה שנייה לפני שהראשונה נפתרה. זה מצב קיים
מאז ומתמיד (לא רגרסיה מה-fix), פשוט לא טופל - כרגע יש
undo אחד בלבד, לא היסטוריה, וזה מספיק כדי לדעת שהוא לא
תמיד יגן על מה שהמשתמש חושב שהוא מגן עליו.

## בדיקת UI בדפדפן — שרת מקומי

`.claude/launch.json` מגדיר שרת קבצים סטטי מקומי
(`npx http-server . -p 8080`, בלי caching) - כדי לבדוק שינויי
UI בפועל בדפדפן (screenshot, קריאת DOM, `getAttribute`) בלי
לשחזר את ההגדרה בכל משימה. שים לב: אין גישה לאינטרנט/Firebase
אמיתיים בסביבת הבדיקה - כל מה שדורש חיבור אמיתי (auth, כתיבה
ל-RTDB) לא ניתן לבדיקה מלאה כך; לרוב פותר את זה hook זמני
(`window.__test`, ר' גם `scripts/i18n-audit.js` לבדיקת כיסוי
תרגום) שמוסר ומאומת שהוסר לפני push.

**מגבלה אמיתית של השיטה הזו, אין מה לעשות בה**: כתיבה ל-RTDB
תמיד נכשלת ב-`PERMISSION_DENIED` בסביבה הזו - כל בדיקה שלוכדת
את האובייקט רגע לפני `update(ref(db), {...})` מוכיחה **מה נשלח
לכתיבה**, לא **מה Firebase בפועל שמר**. לרוב זה אותו דבר, אבל
זו הנחה שלא מאומתת - אם יש ספק אמיתי (לא רק "לרוב עובד"), הבדיקה
הבאה היא בפרודקשן, לא עוד סבב מקומי.

**בדיקת תיקון שנוגע ביצירת טיול (`wizard.html`) - תמיד בטאב חדש
או incognito, גם ב-production, לא רק מקומית.** `version.js` בודק
גרסה חדשה רק באירוע `visibilitychange` (ר' "ניהול גרסה" למעלה) -
טאב שכבר היה פתוח על הוויזרד לפני שגרסה חדשה עלתה ימשיך להריץ
את ה-JS הישן שכבר נטען, ולא ידע שיש עדכון. נתפס בפועל: דיווח על
באג ב-`trip.timezone` שהתברר כטאב ישן במטמון - התיקון כבר היה
נכון ב-production, הבדיקה פשוט רצה נגד קוד ישן. סבב שלם התבזבז
לפני שהתברר.

## צ׳ק ליסט לפני commit

לא כל סעיף חל על כל משימה. עבור על הרשימה ואמור במפורש
מה נבדק ומה לא רלוונטי.

### נתונים
- [ ] **כל פונקציה שקוראת מ-RTDB הורצה לפחות פעם אחת מול נתון
      אמיתי, לא מול אובייקט שבנית ידנית.**
      אובייקט שבנית מאמת את ההנחות שלך, לא את הנתונים.
      אם אין לך גישה לרשת - תן למשתמש שורה אחת להדבקה בקונסול.
- [ ] נבדק על טיול קיים, לא רק על טיול חדש או מזויף
- [ ] נבדק על יותר מסוג טיול אחד כשרלוונטי (חו״ל / בארץ /
      קמפינג / מילואים) - לטיולים ישנים חסרים שדות
- [ ] תיקון שנוגע ביצירת טיול (`wizard.html`) נבדק בטאב חדש
      או incognito - לא בטאב שכבר היה פתוח. ר' "בדיקת UI
      בדפדפן" למעלה
- [ ] בדיקת יצירת טיול עוברת דרך הקליקים האמיתיים (ניווט
      בין שלבי הוויזרד, לחיצה על "צור טיול") - לא קריאה ישירה
      ל-`generateTripAndSave()` או פונקציה דומה, שמדלגת בדיוק
      על מה שמשתמש אמיתי עובר

### תאימות
- [ ] טיול בלי `legs` מתנהג בדיוק כמו לפני השינוי
- [ ] טיול בלי `tripCharacter` מתנהג בדיוק כמו לפני השינוי
- [ ] שדה חסר לא שובר - יש fallback או `console.warn`, לא קריסה

### שני ההקשרים
- [ ] קוד משותף בין העמודים (script/פונקציה) נבדק בפועל בשני
      ההקשרים - `index.html` (קלאסי) וגם אחד מ-5 המודולים.
      ר' "מבנה סקריפטים בין 6 הדפים" למעלה.

### מרוצים ואסינכרוניות
- [ ] אם הפונקציה יכולה לרוץ פעמיים במקביל - נבדק מה קורה
- [ ] כתיבה ל-RTDB בלי חיבור נכנסת לתור ולא נכשלת.
      אל תניח שהיא תיפתר או תידחה במהירות.
- [ ] אין הכרזת הצלחה לפני שהכתיבה נפתרה, אלא אם זו החלטה מודעת

### כשלונות שקטים
- [ ] אין `then` בלי `catch` ואין `await` בלי `try` בקוד חדש
- [ ] כשלון משאיר את ה-UI במצב שאפשר להתאושש ממנו -
      לא מודאל תקוע ולא מסך ריק בלי הסבר
- [ ] כל כשלון שהמשתמש לא רואה מייצר `console.warn`.
      **כשלון שקט הוא הדפוס שכבר עלה חודשים בפרויקט הזה.**

### UI
(מעבר לדרישות ב"צפיפות ויזואלית" למעלה)
- [ ] נבדק בעברית ובאנגלית
- [ ] נבדק במצב כהה ובהיר
- [ ] נבדק בכל 4 ערכות סוגי הטיול אם נגעת בצבע
- [ ] נבדק במסך צר (375px) - לא רק ברוחב ברירת המחדל
- [ ] כל אלמנט חדש עם טקסט למשתמש (textContent/placeholder/title/
      aria-label) - יש לו שורה בפונקציית השפה של העמוד? הרץ
      `node scripts/i18n-audit.js` לפני push - זה בדיוק הדפוס שחזר
      על עצמו ב-6/6 העמודים (ר' CHANGELOG v4.13.3)

### לפני push
- [ ] כל קוד בדיקה זמני הוסר, ואומת שהוסר
- [ ] שינויים לא קשורים לא נכנסו ל-staging
