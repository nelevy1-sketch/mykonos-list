# GitTrip — CHANGELOG

## v4.33.0 — organizerName + פרטי הזמנה בקישור המשותף (commit 2א מתוך 3)

### ✨ תכונה חדשה, שינוי סכמה
- **`organizerName` חדש ב-`tripData`, נכתב פעם אחת ב-wizard.html ברגע יצירת הטיול** (`organizer.displayName || organizer.email || ''`) - השם הראשון שנשמר על טיול בכלל. `ownerId` הקיים משמש רק להרשאות, שום קוד לא פתר אותו לשם. לא ב-`memberProfiles` בכוונה: מסך ההזמנה (2ב) מוצג *לפני* התחברות, ואז אי אפשר לקרוא מ-RTDB בכלל (`.read: auth != null`) - ומי שיצר טיול ומיד שלח קישור עדיין אין לו `memberProfiles` בכלל
- **שני מקומות נפרדים בונים את הקישור המשותף - שניהם עודכנו**: `updateAdminShareLink()` (index.html, פאנל המנהל) ו-`generateTripAndSave()`'s success handler (wizard.html, `#finalTripLink`/`#openTripBtn` - **הקישור שמוצג מיד אחרי יצירת הטיול, המסלול הנפוץ ביותר בפועל**). שניהם מוסיפים `d` (יעד) ו-`h` (שם מארגן) - `?tripId=X&d=ברלין&h=נתנאל` - עם `URLSearchParams.set()`, לא הרכבת מחרוזת (מקודד נכון אוטומטית, בלי `encodeURIComponent` ידני מעל - זה היה מקודד כפול). כל אחד מהשניים מתווסף רק אם קיים - טיול ישן בלי `organizerName` פשוט משתף קישור בלי `h`
- **`organizerName` נטען ב-`hydrateTripData()`**, אותה נקודה בדיוק שבה `destinationName` כבר נטען מ-`primaryLeg.name` - עקבי עם התבנית הקיימת
- **נסרקו כל 6 העמודים לבנייה נוספת של קישורי שיתוף** - נמצאו 3 מימושים עצמאיים ל-URL עם `tripId` (לא אחד): `scopedTripUrl` (index.html), `tripUrl` (wizard.html), `scopedUrl` (shopping.html). `wizard.html`'s `renderRecentTrips()` (גם משתמשת ב-`tripUrl`) **לא נגעה** - אלה קישורים לטיולים *של המשתמש עצמו*, `d`/`h` לא רלוונטיים שם. `shopping.html`'s `updateShareLink()` **לא נגעה בכוונה, מתועד בהערת קוד**: זו שיתוף מחדש של טאב רשימת קניות ספציפי בין מי שכבר בקבוצה (לא הזמנה ראשונית), והיא מובילה ל-`shopping.html` ממילא - מסך ההזמנה קיים רק ב-`authGate` של `index.html`, אז `d`/`h` שם לא היו נקראים בכל מקרה. **שלוש הפונקציות לא אוחדו** - נוסף לכך CLAUDE.md, כדי שהכפילות לא תיסגר בטעות ("מימוש רביעי") ולא תיפתח בטעות ("בואו נאחד עכשיו") בלי משימה ייעודית

### ⚠️ הערת אבטחה - לא רק תיעוד
- **`d`/`h` הם קלט מקישור, לא מהימנים** - כשמסך ההזמנה (2ב) יציג אותם, זה *חייב* להיות דרך `textContent`, לא `innerHTML`/הרכבת מחרוזת. קישור עם `d=<script>...` הוא XSS רפלקטיבי אמיתי אם זה לא נאכף. זיוף התוכן עצמו (שם/יעד שקריים) מקובל ולא מסוכן - `d`/`h` הם רק להצגה, לא משפיעים על מה שנקרא בפועל מ-RTDB אחרי התחברות

נבדק בפועל: גם `updateAdminShareLink()` (index.html) וגם בניית `shareUrl` ב-wizard.html הורצו בדפדפן חי עם שמות עברית - `URLSearchParams.get()` על הקישור שנבנה פענח בדיוק את אותם ערכים שנכנסו, בלי קידוד כפול. wizard.html נבדק דרך hook זמני `window.__test` (כי `tripUrl` מקומית ל-module, לא נגישה מהקונסול) - הוזרק, אומת, הוסר ואומת שהוסר. קונסול נקי בשלושת העמודים (index.html, wizard.html, shopping.html). `node scripts/i18n-audit.js` - 0 ממצאים חדשים (אין טקסט משתמש חדש, רק שדה נתונים ותיקוני קישור). **לא אומת מקצה-לקצה מול RTDB אמיתי** (מגבלת סביבה מתועדת) - `organizerName` בפועל ייכתב רק ביצירת טיול אמיתי דרך הוויזרד בפרודקשן.

## v4.32.2 — שינוי שם תצוגה (commit ג, אחרון בשרשרת ההרשמה האוטומטית)

### ✨ תכונה חדשה
- **עיפרון ליד השם ב-index.html פותח שדה inline לשינוי שם התצוגה** - הכתיבה מוגבלת ל-`name` בלבד ב-`memberProfiles/{tripRoot}/{uid}` (לא נוגעת ב-`participantKey` - הכלל דורש שהוא יישאר כפי שנקבע ב-auto-register), מוגבלת ל-40 תווים עם `trim` לפני שמירה. Enter ו-blur שומרים אם השם השתנה ולא ריק; Escape מאפס את הערך ואז מטשטש (blur) - כך "ביטול" ו"נלחץ הצידה בלי שינוי" חולקים נתיב קוד אחד. שם ריק אחרי trim: ב-Enter (ניסיון שמירה מפורש) - טוסט + השדה נשאר פתוח וממוקד; ב-blur (התחרטות) - נסגר בשקט בלי הודעה
- **כשלון כתיבה**: לא אופטימי - לא מציג את השם החדש עד שה-`update()` נפתר בפועל. בכשלון: טוסט + חזרה לשם הישן + סגירת השדה

### 🏗️ תיקון ארכיטקטורה - מקור הנתונים של #sessionUserLabel
- **`#sessionUserLabel` הציג עד היום `user.displayName` (Google), לא `memberProfiles.name`** - שני מקורות נתונים נפרדים לגמרי. שינוי שם לא היה משפיע עליו בכלל אם היה נכתב רק ל-`memberProfiles`. תוקן: `currentProfile?.name || user.displayName || user.email` - מעדיף את השם הניתן-לעריכה של הטיול הנוכחי, נופל לשם מ-Google לפני שטיול נטען. **הבהרה של מה שהיה קיים תמיד, לא סיבוך חדש**: אין "שם גלובלי" במודל הנתונים - `memberProfiles` הוא per-trip מאז ומתמיד, פשוט לא היה עורך עד עכשיו
- **`syncSessionUserLabel()` חדשה, נקודת קריאה אחת** לעדכון התווית + נראות העיפרון - `currentProfile` ב-`index.html` נטען עם `.once("value")`, לא מאזין חי (בשונה מ-`renderLinkedMembers()`, שמתעדכן לבד מהמאזין החי של הטיול). נקראת מ-4 מקומות: `handleAuthStateChanged`, סוף `loadMemberProfile()`, סוף `claimMember()` (מסלול הבחירה הידני החלופי), ואחרי שינוי שם מוצלח

### 🎨 ellipsis בכל מקום שמציג שם (למעט אחד, בכוונה)
- **תוקן**: `#sessionUserLabel` (index, לא היה שם כלל CSS ייעודי קודם), `renderLinkedMembers()` (index, פאנל מנהל - נמצא בסריקה, לא הוזכר בפרומפט המקורי), `.group-title` (packing, כותרת "פריטי X" בתצוגה משותפת), `.comment-owner` (places, תגובות+תשובות), `.photo-info`/figcaption (places, מי העלה תמונה - הכי מסוכן, יושב מעל thumbnail מרובע קטן), `.activity-owner-name` (itinerary - **היה `flex:0 0 auto` בלי שום `overflow`/`max-width`, לא נגלש/נחתך, פשוט דוחף את הכרטיס לרוחב - אומת בפועל: קלף התרחב במקום להישבר. תוקן `flex:0 1 auto;min-width:0` + ellipsis, אותו דפוס כמו `.activity-note` הסמוכה שכבר טופלה בעבר**
- **`.logout-link` (index.html, משותף ל-`darkModeToggle` ו-`logoutLink`) קיבל `flex-shrink:0;white-space:nowrap`** - בלעדיו, ellipsis על `#sessionUserLabel` לא הספיק: שם ארוך עדיין דחק את שני הכפתורים האלה לכיווץ, הטקסט שלהם עטף שורה, וה-`.session-row` גדל לשתי שורות (32px → 52px, נמדד בפועל). התיקון האמיתי היה לתת לשני הכפתורים גודל קבוע כדי ש-`#sessionUserLabel` יספוג את כל לחץ הרוחב לבדו - ellipsis לבדו לא היה מספיק
- **הושאר בכוונה, בלי ellipsis**: `.item-by`/`.item-buyer` ב-shopping.html - `.item-sub-info` הוא `flex-wrap:wrap` כבר, אז שם ארוך עוטף שורה במקום לשבור פריסה, והמידע נשאר קריא במלואו. ellipsis שם היה מסתיר מידע (מי הוסיף/מי קונה) בלי תועלת פריסתית שה-wrap כבר לא נותן. תועד בהערת קוד כדי שלא "יתוקן" בטעות לעקביות
- **RSVP באיתוד (itinerary.html) לא רלוונטי לסעיף הזה** - ה-UI מציג רק מספר מגיעים ("3 מגיעים ✓"), לא שמות. `joinerNames` (שדה שקיים ב-RTDB) לא נקרא בשום מקום באפליקציה

נבדק בפועל: זרימת העריכה המלאה (שינוי, ללא-שינוי, ריק+Enter, ריק+blur, כשלון כתיבה) הורצה ישירות מול הפונקציות בדפדפן חי - `PERMISSION_DENIED` אומת כצפוי (אותה מגבלת סביבה מתועדת). `.session-row` נמדד בפועל לפני ואחרי תיקון ה-`flex-shrink` (52px→32px עם שם ארוך). כל 5 מיקומי ה-ellipsis אומתו במדידת `scrollWidth`/`clientWidth` וחזותית (צילום מסך) עם שם ארוך מכוון. עברית ואנגלית, בהיר וכהה, 375px. `node scripts/i18n-audit.js` על 5 הקבצים - `#editNameBtn` נמצא כממצא חדש (aria-label/title מקודדים בלי תרגום), תוקן דרך פונקציית השפה הקיימת (אותו דפוס כמו `adminGearBtn`), אומת נקי אחרי התיקון. קונסול נקי מלבד שגיאות הרשת הצפויות. **לא אומת מקצה-לקצה מול RTDB אמיתי** (אותה מגבלת SDK) - מומלץ לבדוק בפרודקשן.

## v4.32.1 — מצבים ריקים: שיהיה ברור שזה כפתור

### 🎨 שיפור ויזואלי
- **6 מצבי הריק מ-v4.31.0 היו כפתורים אמיתיים, אבל בלי שום סימן ויזואלי שזו לחיצה** - נראו כמו טקסט במרכז מסך ריק. שלושה תיקונים: **א.** מסגרת מקווקוות עדינה סביב כל אזור (דפוס מוכר מהעלאת קבצים - "משהו יכול להיכנס לכאן"). **ב.** שורת CTA בתוך הכפתור ("+ הוסף פריט" וכו', משתנה לפי עמוד) שנראית כמו כפתור אמיתי - אבל היא `<span>`, לא `<button>` מקונן (קינון כפתורים בתוך כפתור לא תקין ב-HTML ומבלבל קוראי מסך; האלמנט האמיתי הלחיץ הוא כל הקובייה). **ג.** אדווה - `animation-iteration-count:3`, בלי JS, בלי localStorage. עוצרת מעצמה ואינה חוזרת בלי re-render של האלמנט (בניגוד ל-`adminGearPulse` ב-index.html, שיש לו זיכרון בין ביקורים דרך `localStorage`/`.admin-gear-seen` - כאן אין זיכרון בכלל, וזה מכוון: מצב ריק יכול לחיות ימים, ואדווה אינסופית שרואים שוב למחרת הופכת מהזמנה לרעש)
- **`prefers-reduced-motion: reduce` מכבה את האדווה לגמרי** בכל 4 הקבצים - המסגרת ושורת ה-CTA נשארות

### 🔀 shopping.html שונה בכוונה - בלי שורת CTA
- **`#openModalBtn` ("➕ הוסף פריט") אינו FAB צף - הוא כפתור אמיתי ב-`.main-actions-row`, ממש מעל `#categoriesContainer`** שבו מוצג המצב הריק. שורת CTA זהה ("+ הוסף פריט") ממש מתחתיו הייתה נראית ככפתור כפול/שבור, לא כרמז. shopping.html מקבל רק מסגרת מקווקוות (כבר הייתה שם, `.empty-state`) ואדווה - בלי שורת CTA. **תועד בהערת קוד ב-shopping.html במפורש** כדי שהחוסר-עקביות הזה לא "יתוקן" בטעות בעתיד

נבדק בפועל: מבנה/CSS מחושב אומת בכל 6 המקומות (`animation-name`/`animation-iteration-count`/`border-style` נכונים דרך `getComputedStyle`), קליק עכבר על פינת האלמנט (לא על שורת ה-CTA) הפעיל את ה-handler - מאמת שהקובייה כולה לחיצה, לא רק השורה. עברית+אנגלית, בהיר+כהה, 375px, שלושת הטאבים ב-places.html. **מגבלת בדיקה חדשה שנתפסה כאן, מתועדת ב-CLAUDE.md**: לטאב ב-Browser pane אין לולאת רינדור רציפה בזמן שהוא לא נצפה ישירות (`requestAnimationFrame` לא יורה, `getAnimations().currentTime` קפוא) - אי אפשר לאמת מקצה-לקצה בזמן אמת שהאדווה באמת נעצרת אחרי 3 חזרות ולא רק דרך `getComputedStyle` הסטטי. `animation-iteration-count` היא תכונת CSS בסיסית ותקנית לגמרי - אין סיבה אמיתית לחשוד בהתנהגותה בדפדפן אמיתי, אבל זו אינה אימות מדוד כמו שהצ'ק ליסט דורש.

## v4.32.0 — shopping.html: buyer ו-personalQtyMap עוברים משם ל-uid (commit ব2)

### ⚠️ שינוי סכמה, שבירת תאימות מכוונת
- **`buyer` (מי אחראי לקנות פריט) ו-`personalQtyMap` (מפתח הבקשה האישית) היו שמורים לפי שם, לא לפי `uid`** - בדיוק התרחיש שזוהה אחרי commit ব1: רינוי שם משאיר פריטים ישנים עם השם הקודם, שנראה כמו שני אנשים שונים. שני השדות עכשיו לפי `uid` - עקבי עם `joiners` ב-`itinerary.html` ועם הנתיב הפרטי ב-`packing.html` (`packing/users/{uid}`), שכבר בנויים ככה
- **לא נשמרה מיגרציה לנתונים קיימים - החלטה מודעת.** כל הטיולים כרגע הם טיולי בדיקה. פריט ישן עם `buyer`/`personalQtyMap` לפי שם לא נשבר: `memberDisplayName(uid)` (חדש) נופל ל-`tr('חבר/ה לקבוצה', 'a group member')` עבור כל מפתח שלא תואם `memberProfiles` חי - כולל `uid` אמיתי שכבר לא קיים וגם מחרוזת שם ישנה מלפני המעבר, אותו נתיב בדיוק. הפריט מוצג כלא-שלי לכל הצופים (`isUserBuyer`/`isUserWanting` לעולם לא יתאימו למחרוזת שם ישנה), לא כתקוע או שבור

### 🔧 מה זז
- **`toggleClaimItem`/`openPersonalQtyModal`/`savePersonalQtyBtn`/`removePersonalQtyBtn`**: כותבים/משווים לפי `currentUser.uid` במקום השם שמחזיר `requireUserName()`. `requireUserName()` עצמו לא השתנה - עדיין שער הטעינה (`currentProfile?.name`), עדיין המקור לתווית `item.by` ("הוסיף: X") שנשארת לפי שם בכוונה (תמונת מצב תיאורית, לא בדיקת זהות - אותה החלטה כמו `ownerName` בעמודים האחרים)
- **`render()`**: המשתנה המקומי `myName` הוחלף ב-`myUid` (`currentUser?.uid`) בשלוש הבדיקות (`isUserWanting`/`isUserBuyer`/חיפוש הכמות האישית שלי)
- **`memberDisplayName(uid)` חדש** - פותר `uid` לשם תצוגה חי מ-`tripData.memberProfiles`, אותו דפוס בדיוק כמו ה-fallback הקיים כבר ב-`packing.html`'s `importItem()` (`trip.memberProfiles?.[uid]?.name || ...`). נקרא משלוש נקודות: תווית "🛒 אחראי" ברינדור, וייצוא הרשימה לטקסט (`generateExportText`) - שתיהן הציגו את `buyer` הגולמי קודם, מה שהיה מדפיס `uid` גולמי לטקסט משותף לוואטסאפ בלי התיקון הזה
- **נסרק כל הקובץ לשדות אחרים ששומרים שם** - נמצא רק `item.by` (תווית "הוסיף: X"), ואומת שאף קוד לא משווה אותו ל-`===` לזיהוי - תווית תצוגה בלבד, נשאר כפי שהוא

נבדק בפועל: `memberDisplayName` - 4 מקרים (uid ידוע, uid לא ידוע/"עזב", מחרוזת שם ישנה מלפני המעבר, מחרוזת ריקה) הורצו מול הפונקציה החיה בדפדפן (הוזרק hook זמני `window.__test`, אומת ידנית, הוסר ואומת שהוסר) - כל הארבעה נופלים בדיוק כמתוכנן. קונסול נקי מלבד שגיאות הרשת הצפויות. `node scripts/i18n-audit.js shopping.html` - אותם 3 ממצאים ידועים, 0 חדשים. **לא אומת מקצה-לקצה מול RTDB אמיתי** (אותה מגבלת סביבה מתועדת מ-commit ব) - מומלץ לבדוק בפרודקשן.

## v4.31.0 — מצבים ריקים: מהודעה להזמנה

### ✨ תכונה חדשה
- **6 מצבים ריקים הופכים להזמנה לחיצה, במקום הודעת טקסט פסיבית**: `packing.html` (ריקה לכל קטגוריה), `shopping.html`, `places.html` (מקומות, גלריה, מוזיקה), `itinerary.html` (יום ריק). כל אחד עכשיו `<button>` אמיתי (לא `div` עם `onclick`) שקורא לאותו handler בדיוק שה-FAB/כפתור ההוספה של אותו עמוד פותח - לא מסלול מקביל. `packing.html`: `emptyActionBtn.onclick` מפעיל `$("addItemFab").click()` (שומר על הלוגיקה הנלווית - בניית select הקטגוריות כש-`activeList==="all"`, רמז תיוג רגל). `shopping.html`: `openModalBtn.click()`. `places.html`: `fabAction()` (מנתב לפי הטאב הפעיל - מקומות/גלריה/מוזיקה). `itinerary.html`: `$('fab').click()`
- **הנוסח עצמו: שאלה במקום הצהרה, ואז מה עושים, עם רמז שהפעולה משותפת** - למשל "מה לוקחים לברלין?" במקום "הרשימה עדיין ריקה". ברגע שיש פריט אחד, המצב הריק נעלם לגמרי - אין שני כפתורים מתחרים (הכפתור עצמו + ה-FAB)
- **`getCurrentLegName(trip)` חדש ב-`legs.js`** - איזה יעד רלוונטי "עכשיו" לצורך נוסח: הרגל הפעילה, נופלת לרגל הראשונה לפני שהטיול מתחיל או לאחרונה אחריו (אותה שרשרת נפילה בדיוק כמו `getCurrentLegCoords` הקיימת - לא הומצאה מחדש). `packing.html`/`places.html` משתמשות בה לנוסח הדינמי; טיול בלי שם יעד בכלל מקבל נוסח חלופי בלי השם ("מה לוקחים לטיול?")

### 🔒 נשאר טקסט, במפורש
- **"עדיין לא שותפו פריטים" (packing.html, `renderGroup()`) נשאר `div` פסיבי, לא כפתור** - שיתוף הוא toggle על פריט קיים (🌐/🔒 בשורת הפריט), אין פעולה יחידה טבעית להצביע עליה ממסך ריק. יש `makeListPublicBtn` (שיתוף כל הקטגוריה בבת אחת) אבל הוא פעולה גורפת בתפריט משני - לא נכון לקשר אליו מכאן
- **"אין תוצאות לפילטר" (places.html/shopping.html/itinerary.html) לא נגעו בהם** - זרימה נפרדת לגמרי, עם כפתור "הצג הכל" משלה; המשתמש יודע בדיוק מה קרה, זה לא "ריק" במובן שהפרומפט הזה מטפל בו

נבדק בפועל: `getCurrentLegName` - 4 מקרים (טיול רגל-אחת, בלי יעד, רגל פעילה באמצע טיול מרובה, לפני תחילת טיול מרובה) הורצו מול הפונקציה החיה בדפדפן, כולם נכונים. מרקאפ כל 6 הכפתורים אומת חזותית (עברית+אנגלית, בהיר+כהה, 375px) - `button` אמיתי, לא `div`, גובה מגע 168px בפועל (מעל 44px המינימום). `node scripts/i18n-audit.js` על 4 הקבצים - אותם 3 ממצאים ידועים מראש, 0 חדשים. קונסול נקי בכל 4 העמודים מלבד שגיאות הרשת הצפויות בסביבה הזו.

## v4.30.2 — תיקון shopping.html: זהות הייתה תלויה ב-<select> שלא התעדכן (commit ב1)

### 🐛 תיקון
- **`#myName` היה `<select>`, לא שדה טקסט - וזה מה ששבר את auto-register מ-commit ב.** `requireUserName()` ו-`render()` קראו את הזהות מ-`myNameInput.value` (ערך ה-DOM שנבחר), לא מ-`currentProfile`. האופציות של הבורר נבנו אך ורק מ-`tripData.participants` (הרשימה הקבועה שהמארגן הקליד ב-wizard.html) - ושם שנקבע ע"י auto-register (מה-Google identity) כמעט תמיד לא נמצא באותה רשימה. הצבת `.value` על `<select>` לערך בלי `<option>` תואם היא **no-op שקט** ב-DOM: הבורר נשאר על הפלייסהולדר הריק, ו-`requireUserName()` חסם כל פעולה לצמיתות - נמצא בבדיקה עם חברים אמיתיים בפרודקשן (packing/places/itinerary עבדו, shopping לא)
- **`#myName` הפך ל-`<span>` לקריאה בלבד**, לא select מנוטרל - אין יותר "בחירה מרשימה", הזהות מגיעה כולה מ-`currentProfile.name`. `requireUserName()`/`render()` (3 שימושים: `isUserWanting`/`isUserBuyer`/`personalMap` lookup) קוראים עכשיו `currentProfile?.name` ישירות
- **`requireUserName()` נשאר, בתפקיד מצומצם**: לא יותר "תבחר שם", אלא רשת ביטחון להבדל התזמון הצר בין טעינת הדף לרגע ש-auto-register מסיים (`currentProfile` עדיין `null`) - טוסט מעודכן ("עדיין טוען את הפרופיל שלך") במקום ההודעה הישנה שהפנתה לבורר שכבר לא קיים

### 🗑️ הסרת קוד legacy (לא רק תיקון)
- **הוסר ענף `!tripRoot` שלם ב-`shopping.html`**: הודעת "מצב תאימות" + בורר עם 9 שמות מקודדים בקוד (שריד מלפני שהאפליקציה תמכה בטיולים מרובים - חבורת חברים ספציפית, לא רלוונטית לאף טיול פעיל). כל טיול היום נוצר עם `tripId`, ו-`index.html` בלי `tripId` מפנה למסך בחירת טיול - הענף בלתי־נגיש, ואם כן היה נגיש, בורר עם שמות של חבורה אחרת גרוע מכלום
- **`renderParticipants()` נמחקה כולה** - כל שלושת הקריאות אליה (משתי נקודות בזרימת ה-auto-register, ומהענף שהוסר) התייתרו יחד עם ה-`<select>` שהיא מילאה
- **נסרקו כל שאר העמודים לשאריות `!tripRoot`/`!root` דומות** - לא נמצא דבר להסרה: `packing.html`'s "Missing tripId" toast ו-`index.html`'s הפניה למסך בחירת טיול הם התנהגות נוכחית ומכוונת (לא שרידי legacy), לא נגעתי בהם. `itinerary.html`/`places.html` נבנו אחרי שהמערכת הרב-טיולית כבר הייתה קיימת - אין להם ענף מקביל בכלל

נבדק בפועל: מרקאפ ה-`<select>` נעלם מה-DOM (אומת), ה-`<span>` מציג שם כשמוזרק ידנית (אומת חזותית, זהה בגובה/מיקום ל-select הישן - אין תוספת שורה). קונסול נקי מלבד שגיאות רשת הצפויות בסביבה הזו. `node scripts/i18n-audit.js shopping.html` - אותם 3 ממצאים קיימים מראש (כפתורי ייצוא, כבר מתועדים כדחייה מכוונת ב-v4.13.3), 0 חדשים. **לא אומת מקצה-לקצה מול RTDB אמיתי** (אותה מגבלת SDK מודולרי כמו commit ב) - ממתין לבדיקת המשתמש בפרודקשן.

## v4.30.1 — הרשמה אוטומטית בארבעת עמודי המשנה (commit ב מתוך 3)

### 🐛 תיקון
- **`packing.html`/`places.html`/`itinerary.html`/`shopping.html`**: אותה חסימה מוחלטת ש-commit א תיקן ב-`index.html` תוקנה עכשיו גם בארבעת העמודים האחרים - כל אחד החזיק `#memberOverlay`/`#memberGate` משלו עם אותה רשימה סגורה חוסמת. `loadProfile()`/`loadMemberProfile()` בכל עמוד קוראות עכשיו ל-`window.autoRegisterMember()` (מ-`member.js`, לא שוכפל) כשאין פרופיל קיים
- **`#memberOverlay`/`#memberGate` נמחקו פיזית מהמרקאפ, לא הוקפאו** - כולל כל הפונקציות שהתייתמו: `participantKey` המקומי (הוחלף ב-`window.participantKey`, ואז גם זה התייתר - `member.js` מחשב את מפתח התביעה פנימית, אף עמוד לא צריך לקרוא לזה יותר), `profileOptions`/`renderParticipants`'s חצי ה-`memberSelect`, ו-`saveProfile`/`claimMember` - הפכו לקוד מת (אין UI שקורא להם) בכל עמוד חוץ מ-`packing.html`, שם `#participantSelect` (אלמנט שונה, תמיד גלוי, לא הגייט) עדיין משתמש ב-`saveProfile` כנתיב חלופי אם מישהו בוחר שם ידנית - **לא נמחק, לא נדרש למחיקה**
- **כשלון בהרשמה האוטומטית מציג טוסט במפורש, לא נשאר שקט** - עם הגייט הישן שהיה חוזר בטיימאאוט (8 שניות) כרשת ביטחון עצמה נעלמת, כשלון (טיימאאוט, או ההרשמה עצמה נכשלת) חייב הודעה כלשהי במקום מסך שנשאר תקוע בלי הסבר - זה בדיוק הדפוס ש-CLAUDE.md כבר מתעד כחוזר בפרויקט הזה
- **הכתיבה ל-`users/{uid}/trips/{tripId}`** (כדי שהטיול יופיע בבורר הטיולים של המצטרף ב-index.html - נכתבה עד היום רק מ-`saveProfile`/`claimMember` הישנים) **נשמרה במלואה בנתיב ההרשמה האוטומטית החדש** בכל 4 העמודים - best-effort, לא חוסמת, בדיוק כמו במקור

### ⚠️ לא בהיקף - נמצא תוך כדי, מתועד ולא תוקן
- **`index.html` (commit א) לא כותבת `users/{uid}/trips/{tripId}` בנתיב ההרשמה האוטומטית שלה** - ה-`claimMember` הישן של `index.html` מעולם לא עשה את זה גם הוא (רק 4 עמודי המשנה עשו, מ-v4.8.0). כלומר מצטרף שנוחת ישירות על `index.html` (בדיוק המסלול שההרשמה האוטומטית נבנתה בשבילו) לא יראה את הטיול בבורר הטיולים שלו. פער אמיתי, לא תוקן כאן - `index.html` לא בהיקף של commit ב

נבדק בפועל: מרקאפ הגייט נעלם פיזית משני ה-4 עמודים (`#memberOverlay`/`#memberSelect`/`#saveMemberBtn` ב-3, `#memberGate`/`#memberGateSelect`/`#claimMemberBtn` ב-shopping) - אומת ב-DOM בדפדפן, לא רק בקוד. קונסול נקי (מלבד PERMISSION_DENIED הידוע) בכל 4 העמודים. **מגבלת בדיקה אמיתית**: בניגוד ל-index.html (Firebase compat, `firebase.database.Reference.prototype` ניתן לדריסה זמנית מהקונסול ואומת קצה-לקצה מול קריאות Firebase מדומות) - 4 העמודים האלה על ה-SDK המודולרי (v9), שאין לו גלובל שניתן לדרוס באותה צורה; `ref`/`get`/`set`/`runTransaction` הם imports מקומיים למודול, לא נגישים מבחוץ. אומת חלקית: 14 בדיקות היחידה על `member.js` עצמו (מ-commit א, SDK-אגנוסטי), התאמה מדויקת בין תחביר קריאות ה-Firebase שנכתב לכאן לתחביר הקיים שכבר עבד ב-`saveProfile`/`claimMember` המקוריים (אותם דפוסי `runTransaction`/`set` בדיוק, לא הומצא תחביר חדש), והרצה אמיתית מול הכלים החיים (נכשלת ב-Permission denied כצפוי בסביבה הזו - מוודאת שהנתיב מגיע לקוד הנכון, לא שהכתיבה מצליחה). **לא אומת מקצה-לקצה מול RTDB אמיתי** - מומלץ לבדוק בפרודקשן לפני שממשיכים ל-commit ג

## v4.30.0 — הרשמה אוטומטית במקום בחירת שם (שלב חוויית המשתמש הראשון, commit א מתוך 3)

### ✨ תכונה חדשה
- **`member.js` חדש** - שכבה משותפת באותה תבנית כמו `legs.js`/`datetime.js`: script קלאסי, לא module, נטען עכשיו ב-`index.html` (5 העמודים המגודרים בהרשאות יטענו אותו ב-commit ב). מכיל `participantKey()` (הועברה, לא שוכפלה) ו-`autoRegisterMember()` חדשה - לוגיקת ההחלטה בלבד (חישוב שם, תביעה, כתיבת פרופיל, נסיגה בכשלון), בלי קריאות Firebase ישירות משלה - `index.html` הוא compat v8 (`db.ref(path).once/transaction`), 4 מתוך 5 העמודים האחרים הם modular v9 (`get`/`runTransaction` כפונקציות עצמאיות) - אותו הבדל שכבר מתועד ב-CLAUDE.md. כל עמוד מזריק את פעולות ה-Firebase שלו (`tryClaim`/`writeProfile`/`rollbackClaim`) לפי הטעם שלו; הקובץ המשותף שולט רק ברצף
- **`index.html`**: מי שנכנס לטיול בפעם הראשונה כבר לא רואה חלונית בחירת שם מרשימה סגורה. `loadMemberProfile()` קוראת ל-`autoRegisterMember()` כשאין פרופיל קיים - שם מ-`displayName`/`email`/נפילה ל'משתתף', בלי מחסום. `#memberGate` נשאר במרקאפ כרשת ביטחון בלבד - אם ההרשמה האוטומטית עצמה נכשלת (שגיאת רשת, אי-התאמת כלל בלתי צפויה), הוא עדיין מוצג במקום להשאיר את המשתמש תקוע בלי שום אינדיקציה
- **התביעות (`participantClaims`) נקבעות עכשיו לפי `participantKey(uid)`, לא לפי `participantKey(name)`** - ההכרעה המרכזית של השינוי. הרשימה של המארגן הייתה המשאב הנדיר בעבר; בלעדיה, ייחודיות שם היא חסימה מלאכותית. שני משתמשים אמיתיים בשם "דני כהן" נרשמים שניהם בהצלחה, בלי התנגשות, כי ה-uid שלהם תמיד שונה - אומת ב-14 בדיקות יחידה (Node, בלי דפדפן - הקובץ לא תלוי ב-DOM/Firebase בכלל)
- **נבדק שזה לא שובר שלושה צרכנים במורד הזרם** ש-`participantClaims`/`participantKey` כבר משמשים: `renderAdminTags()` קוראת רק מ-`groupMembers` (הרשימה, לא נוגעת); `renderLinkedMembers()` (רשימת "מי הצטרף" בפאנל האדמין) קוראת ישירות מ-`memberProfiles` לפי uid, לא חוצה-מפנה ל-`participantClaims` בכלל; `releaseMember()` קורא את `profile.participantKey` **המאוחסן** כדי לדעת מה לנתק, לא מחשב אותו מחדש מהשם - שלושתם כבר מתייחסים ל-`participantKey` כמפתח אטום, לא כמשהו שמניח שהוא נגזר משם

נבדק בפועל: 14 בדיקות יחידה על `member.js` בבידוד (Node, ללא דפדפן) - שם מ-displayName, מפתח תביעה נגזר מ-uid ולא משם, שני uid-ים שונים עם אותו displayName נרשמים שניהם בלי התנגשות, נפילה ל-email כשאין displayName, נפילה ל-fallbackName כששניהם חסרים, שגיאת writeProfile מפעילה rollback ומועברת הלאה, מפתח בטוח ל-RTDB גם לשם עברי. **הזרימה המלאה ב-`index.html` נבדקה מקצה לקצה** - לא רק הקובץ המשותף בבידוד: `firebase.database.Reference.prototype` (once/transaction/set) נדרסו זמנית כדי לדמות תגובת Firebase אמיתית בלי חיבור אמיתי (שאינו זמין בסביבת הבדיקה המקומית) - משתמש חדש עם displayName אמיתי עבר את `handleAuthStateChanged`→`loadMemberProfile`→`autoRegisterMember` בפועל, נכתב `participantClaims/{base64url(uid)}` ואז `memberProfiles/{uid}` עם הערכים הנכונים, והשער התכסה; משתמש עם פרופיל קיים - `autoRegisterMember` לא נקרא כלל (בדיקת הרגרסיה), השער התכסה מהפרופיל הקיים בלבד. `node --check member.js` תקין. `node scripts/i18n-audit.js index.html member.js` - 0 ממצאים (אין טקסט משתמש חדש - `tr('משתתף','Traveler')` כבר קיים בפרויקט). **לא נבדק מול הכללים האמיתיים** - `ops.tryClaim`/`ops.writeProfile` בקוד האמיתי (לא בבדיקה) עדיין לא רצו מול RTDB חי; הכלל ל-`memberProfiles` תואם למה שנבדק בעבר (v4.8.0, מפתח מסוג `participantClaims/{key}` תואם) אבל לא אומת ספציפית מול השינוי הזה

## v4.29.0 — הצעות AI לכל רגל (שלב 12, commit ג מתוך 3 - האחרון)

### ✨ תכונה חדשה
- **`packing.html`/`functions/index.js`**: כשמסנן היעד על "הכל" בטיול מרובה-רגליים, לחיצה על "הצע רשימה עם AI" מייצרת עכשיו קריאה לכל רגל (עם היעד/תאריכים/vibes שלה, והוראה מפורשת לא להציע פריטים כלליים) + קריאה נפרדת אחת לפריטים משותפים לכל הטיול (`scope:"shared"` חדש, פריט שרות אופציונלי בבקשה - השרת מתעלם ממנו לגמרי אם לא נשלח). מסנן על רגל ספציפית, וטיול רגל אחת, נשארים על נתיב הקריאה הבודדת מ-commit ב - ללא שינוי, לא נגעו
- **`functions/index.js`**: פרומפט חדש למקרה `scope:"shared"` - מבקש 8 עד 14 פריטים כלליים שרלוונטיים לאורך כל הטיול (לא תלויי אקלים/יעד ספציפי), עם `listName` נשמר בפרומפט כדי שה"כלליות" תישאר בתוך הקטגוריה הנוכחית (למשל ביגוד כללי, לא מסמכי נסיעה, כשמבקשים הצעות לרשימת ביגוד). `scope:"leg"` משתמש באותו פרומפט הקיים בדיוק + משפט אחד נוסף שאוסר פריטים כלליים. **פרומפט ברירת המחדל (`scope` לא נשלח) נשאר זהה תו-בתו** לזה שהיה - אומת בקוד השוואה ישיר (לא רק נומק) לפני הפריסה
- **`legId` נקבע בקוד לפי מקור ההצעה, לא לפי מסנן העמוד** - כל פריט שחוזר מקריאת רגל מסומן ב-`legId` שלה, פריטים מהקריאה המשותפת נשארים בלי שיוך. עד עכשיו ההצעות עברו כמחרוזות שטוחות בלי שום מטא-דאטה - שינוי מבני ל-`{name,legId}[]` דרך כל הצינור (render, דדופ, קבלה), לא רק בשכבת ה-fetch
- **התנגשות בין רגליים - שיוך במקום ניחוש**: כשאותו פריט מוצע באופן עצמאי משני מקורות שונים (שתי רגליים, או רגל והקריאה המשותפת), המיזוג יוצא בלי שיוך במקום לבחור מקור שרירותי - תועד בהערת קוד במפורש (ר' §7ב, `renderAiSuggestions`) כי זו לא בחירה מובנת מאליה. **אומת מול הפונקציה החיה, לא מדומה**: טיול בנגקוק+קנקון (שני יעדי חוף) - דרכון/בגד ים/קרם הגנה/משקפי שמש/כובע שמש/מטען נייד/מתאם שקע/מעיל גשם התנגשו בין שתי הרגליים בפועל, וגם מטרייה מתקפלת התנגשה בין רגל לקריאה המשותפת - כל המקרים יצאו בלי שיוך
- **`Promise.allSettled`** - כשלון בקריאה אחת לא מפיל את השאר. נבדק מכסת Gemini (Tier 1: RPM/TPM/RPD בסביבות 0.3% ניצול) לפני הבחירה במקבילי מלא, בלי השהיות או batching
- **כשלון חלקי - הודעה נפרדת, לא שקט**: ההצעות שהצליחו מוצגות תמיד, ובנוסף טוסט שאומר מה חסר - שם היעד כשרגל אחת נכשלה, מספר יעדים כשיותר. כל הקריאות נכשלות → אותו טוסט כללי שהיה קיים תמיד
- **טקסט הכפתור בזמן ריבוי קריאות שונה מהקריאה הבודדת** - "אוסף הצעות לכל היעדים..." / "Gathering suggestions for every destination..." במקום "חושב..." - נמדד בפועל שריבוי קריאות מקביליות לוקח פי 1.4-1.5 מקריאה בודדת (7-10 שניות, לא 5-7), לא זהה כמו שהונח לפני המדידה

נבדק בפועל: טיול רגל אחת - קריאה אחת, payload זהה בית-לבית (ללא `scope`); מסנן על רגל ספציפית בטיול מרובה - קריאה אחת, ללא שינוי מ-commit ב; מסנן "הכל" בטיול מרובה - 3 קריאות בפועל (2 רגליים + משותף) עם `scope` נכון בכל אחת; **הבדיקה המרכזית** - קבלת הצעה שמקורה ברגל ספציפית בזמן שמסנן העמוד עדיין על "הכל" כותבת את ה-`legId` של הרגל שייצרה (`leg_rome`), לא את מסנן העמוד - נלכד payload בפועל לפני הכתיבה; כשלון חלקי מדומה (רגל אחת נכשלת) - שאר ההצעות מוצגות, טוסט נפרד עם שם היעד; שתי רגליים נכשלות - טוסט עם מספר; כל הקריאות נכשלות - טוסט כללי. **הרצה חיה מול הפונקציה שנפרסה, לא מדומה, כמו שנדרש**: רומא (עירוני, קריר) מול בנגקוק (טרופי, חם) החזירו רשימות ברורות ושונות (רומא: ז'קט ג'ינס, צעיף כתפיים, חולצה מכופתרת; בנגקוק: בגד ים, כפכפי ים, מעיל גשם, כובע שמש) והפריטים המשותפים היו כלליים בתוך קטגוריית הביגוד (טי-שירטים, ג'ינס, פיג'מה, סוודר) לא ספציפיים ליעד - בדיוק מה שהתבקש. `console` נקי (מלבד PERMISSION_DENIED הידוע). `window.__test` הזמני (כולל לוגר זמני להוכחת מנגנון ההתנגשות) הוסר ואומת שהוסר. `node scripts/i18n-audit.js packing.html` - 0 ממצאים. `node --check functions/index.js` - תקין

## v4.28.5 — הצעות AI מתעלמות ממסנן היעד (שלב 12, commit ב מתוך 3)

### 🐛 תיקון
- **`requestAiSuggestions()` (`packing.html`) שלחה תמיד `window.getPrimaryLeg(trip).name` - הרגל הראשונה - ללא קשר למסנן היעד הפעיל.** מסנן על בנגקוק ולחיצה על "הצע רשימה" שלחה בקשה על ניו יורק (הרגל הראשונה בטיול) בלי שום סימן שמשהו לא בסדר - הצעות ליעד הלא נכון שנראות תקינות לגמרי. תוקן: `aiSuggestLeg()` חדשה פותרת את הרגל שהמסנן מצביע עליה בפועל, עם נפילה לרגל הראשונה כשהמסנן על "הכל" - אותו דפוס בדיוק שכל צרכן אחר של `activeLegFilter` בקובץ הזה כבר משתמש בו
- **`vibes`/`startDate`/`endDate` נלקחים עכשיו מאותה רגל, לא מ-`trip.vibes`/`trip.startAt`/`getLastLeg(trip).endAt`** - בקשה על בנגקוק צריכה לתאר את ה-vibes וטווח התאריכים של בנגקוק, לא של הטיול כולו. `leg.vibes` זהה כרגע בין כל הרגליים (הוויזרד עדיין כותב אותו מערך משותף לכולן - §3.3 במסמך), אז זה לא-שינוי בפועל להיום, אבל יקרא נכון כשלוויזרד יהיה קלט vibes per-leg בלי שיידרש תיקון נוסף כאן
- **טיול רגל אחת - אפס שינוי**: `aiSuggestLeg()` נופלת לרגל הראשונה בדיוק כמו היום (המסנן תמיד "הכל" בטיול כזה), וה-`leg` היחידה שמחזירה `getLegs()` כבר מכילה בדיוק את אותם `startAt`/`endAt`/`vibes` ש-`trip` עצמו מחזיק (המבנה הסינתטי הקיים ב-`legs.js`)
- **כפתור ההצעות נשאר פעיל כשהמסנן על "הכל" בטיול מרובה-רגליים - לא הושבת**, בניגוד להשבתה הקיימת כש-`activeList` (קטגוריה) על "הכל". החלטה מכוונת: ל"הכל" של הקטגוריות אין ברירת מחדל הגיונית (אי אפשר להציע לאיזו קטגוריה), אבל ל"הכל" של היעד יש - הרגל הראשונה - וההשבתה הייתה מרגיזה בלי סיבה אמיתית

### לא בהיקף (commit ג)
ריבוי קריאות (קריאה לכל רגל + קריאה משותפת), שיוך `legId` בקוד להצעות שהתקבלו, ומסנן "הכל" שמפעיל בקשה לכל רגל - כל אלה נשארים כמו שהם: קריאה בודדת אחת, בדיוק כמו היום.

נבדק בפועל: טיול רגל אחת - ה-payload שנלכד זהה בית-לבית לזה שהקוד הישן היה שולח לאותו אובייקט טיול (הושוו במפורש, לא רק נומק); מסנן על בנגקוק (טיול שתי-רגליים בדוי: ניו יורק → בנגקוק) - `destination` יוצא "Bangkok" עם התאריכים והvibes של הרגל ההיא; מסנן על "הכל" - `destination` יוצא "New York" (הרגל הראשונה); מסנן מפורש על ניו יורק - אותה תוצאה, לוודא שהפתרון עובד בשני הכיוונים. **בדיקה 4 - הרצה אמיתית מול הפונקציה שנפרסה (לא מדומה)**: בנגקוק החזירה "תרסיס נגד יתושים", "מעיל גשם", "סנדלי הליכה" (עונת הגשמים, טרופי); ניו יורק החזירה "נעלי הליכה", "ז'קט", "משקפי שמש", "אוזניות" - שתי רשימות שונות בבירור, כל אחת סבירה ליעד שלה. הוסר `window.__test` הזמני ואומת שהוסר. `node scripts/i18n-audit.js packing.html` - 0 ממצאים (אין טקסט משתמש חדש בשינוי הזה - רק זרימת נתונים)

## v4.28.4 — זיהוי חיתוך בהצעות AI (שלב 12, commit א מתוך 3)

### 🐛 תיקון
- **`functions/index.js`**: תגובת Gemini שנחתכה (`finishReason === "MAX_TOKENS"`) מזוהה עכשיו במפורש, לפני ניסיון `JSON.parse` ובנפרד ממנו - docs/schema-legs.md §7ב. עד כה חיתוך היה נופל לענף השגיאה הכללי (JSON לא תקין), נרשם ומדווח בדיוק כמו כל כשלון אחר - בדיוק הכשלון השקט שכבר עלה חודשים כש-`maxOutputTokens` היה 500. `logger.warn` נפרד (לא `logger.error` הכללי), והתגובה ללקוח כוללת `reason:"truncated"` כדי שהלקוח יוכל להבדיל
- **`packing.html`**: `requestAiSuggestions()` קוראת את גוף השגיאה לפני שהיא זורקת, ומציגה הודעה שונה כשהסיבה `truncated` - "התשובה נקטעה, נסו שוב" במקום "לא ניתן לקבל הצעות AI כרגע" הכללית. כל כשלון אחר (שגיאת רשת, JSON לא תקין מסיבה אחרת, תגובה ריקה) ממשיך להציג את ההודעה הכללית כפי שהיה
- **תיקון, לא תכונה** - שלב 12 (הצעות AI לכל רגל) עדיין לא התחיל. commit זה סוגר פער קיים שעלה בחקירה: תשתית זיהוי החיתוך שהמסמך דרש מעולם לא נבנתה, אף שהסיכון תועד. נסגר עכשיו, לפני שריבוי הקריאות (commit ג) יכפיל את מספר ההזדמנויות לאותו כשלון

נבדק: לוגיקת הענף החדש בשרת אומתה מבודדת ב-Node (4 מקרים - MAX_TOKENS, הצלחה רגילה, JSON פגום עם finishReason שאינו MAX_TOKENS שנשאר על הענף הישן, ו-MAX_TOKENS שבמקרה כן היה מתפרסר - נשאר מסומן כחיתוך בכל זאת כי הבדיקה על finishReason קודמת לניסיון הפרסור) - כל 4 עברו. `node --check functions/index.js` - תקין. בצד הלקוח, `fetch` דומה (mock) הרץ נגד `requestAiSuggestions()` דרך `window.__test` זמני: תגובת חיתוך מציגה את ההודעה הספציפית, 502 לא-חיתוך ושגיאת רשת ממשיכות להציג את ההודעה הכללית, ומסלול ההצלחה (הצעות מוצגות, ניתן לבחור) לא נפגע. `window.__test` הוסר ואומת שהוסר. `node scripts/i18n-audit.js packing.html` - 0 ממצאים. **לא נבדק מול Gemini עצמו** - זה חוזה חיצוני; דורש `firebase deploy --only functions` שלא בוצע, ממתין לאישור נפרד

## v4.28.3 — תיקון אמיתי: הפס הלבן ב-shopping.html (v4.28.2 לא הספיק)

### 🐛 תיקון
- **האבחנה ב-v4.28.2 (html בלי background) הייתה נכונה אבל לא מספקת** - המשתמש דיווח שהפס עדיין שם אחרי שהגרסה עלתה לפרודקשן, ותיאר אותו כ**לבן ממש**, לא כחול (מה ש-`theme-color` היה מייצר) ולא כהה. חקירה חוזרת, הפעם מול הדף החי (לא רק מקומי): `getComputedStyle(document.documentElement).backgroundColor` על shopping.html בפרודקשן החזיר `rgba(0,0,0,0)` - שקוף לגמרי, למרות ש-`background-image` (ה-gradient) כן היה שם
- **הסיבה: `background:<gradient>` בקיצור קובע רק `background-image`, אף פעם לא `background-color`.** `html{background:var(--page-bg-gradient)}` מ-v4.28.2 קבע gradient אבל השאיר את `background-color` בברירת המחדל שלו - שקוף. איפה שה-gradient (image layer) לא הגיע לצייר - נשאר שקוף לגמרי, לא צבע כלשהו, ולכן לבן (ברירת המחדל של הדפדפן). `packing.html`/`places.html`/`itinerary.html` לא נתקלו בזה כי ה-`html{background:var(--bg)}` שלהם הוא ערך צבע מלא, שכן נופל תחת `background-color` בקיצור - בדיוק ההבדל בין gradient לצבע מלא בהתנהגות הקיצור, לא ב"איך זה נמתח על הקנבס"
- **`shopping.html`**: `html` מקבל עכשיו גם `background-color:var(--bg-gradient-end)` מפורש, לצד ה-`background` הקיים - צבע אטום מתחת ל-gradient, לא רק תחתיו. `--bg-gradient-end` נבחר כי זה הצבע שה-gradient עצמו יציב ממנו והלאה (מ-40% ועד הסוף) - התאמה מדויקת לרוב הקנבס, וקרוב מאוד גם בקצה העליון
- **סקר את כל 6 העמודים** לאיתור מקום נוסף שבו `background` עם gradient בלבד יושב על אלמנט שאמור להיות שכבה אטומה (לא רק דקורטיבי מעל הורה אטום כבר) - `html`/`body`/מעטפת עמוד מלאה. נמצא רק המקום הזה. כל שאר ה-gradient-ים בכל 6 העמודים (כרטיסי hero, badges, פס התקדמות, אפקטי fade על תמונות/רצועות גוללות) יושבים על הורה כבר אטום, ולא נפגעים מהבאג הזה
- **`index.html`/`wizard.html`**: `window.syncHtmlBackground()` מ-v4.28.2 מעתיק `getComputedStyle(body).backgroundColor` - נבדק במפורש שזה לא אותו באג במסווה: ל-`body` בשני העמודים האלה אין gradient בכלל (`--bg-color`/`--background` הם ערכי צבע מלא בכל 8 השילובים שנבדקו), אז ה-`backgroundColor` שמועתק תמיד אטום. לא נדרש תיקון שם - נבדק, לא רק נומק

נבדק בפועל: כל 8 השילובים (4 סוגי טיול × בהיר/כהה) ב-shopping.html - `background-color` על `html` אטום ותואם בדיוק ל-`--bg-gradient-end` הנוכחי בכל אחד; index.html/wizard.html - `background-image` נשאר `none` בכל 8 השילובים, `background-color` אטום כפי שהיה; packing.html - עדיין אטום, ללא שינוי. אומת גם מול הדף החי בפרודקשן (github pages), לא רק מול שרת מקומי - כך בדיוק נמצא הפער בין v4.28.2 המקומי (שנראה תקין) לבין מה שהמשתמש ראה בפועל. קונסול נקי (מלבד PERMISSION_DENIED הידוע).

## v4.28.2 — תיקון: רקע לא מגיע לקצוות בנייד (safe-area)

### 🐛 תיקון
- **`shopping.html`/`index.html`/`wizard.html`**: ברקע `<html>` פינה שני קצות המסך (מעל התוכן ומתחת לתפריט/כפתורים) בנייד - האזורים שנחשפים ע"י `viewport-fit=cover` מאחורי אזור השעון/הבית ב-iOS. הרקע היה מוגדר רק על `body`, ו-`<html>` (שצובע את כל הקנבס, כולל האזורים האלה, לפי ספק ה-CSS) נשאר בברירת המחדל של הדפדפן. `packing.html`/`places.html`/`itinerary.html` כבר הגדירו את זה נכון על שני האלמנטים - **נבדקו במפורש ואומת שהם לא צריכים תיקון**
- **`shopping.html`**: `html{background:var(--page-bg-gradient)}` (משתנה חדש שמאחד את ה-gradient הקיים, לא שכפול) לצד ההגדרה הקיימת על `body` - כאן `[data-theme]`/`.theme-X` כבר על `documentElement`, אז CSS גרידא מספיק
- **`index.html`/`wizard.html` - תיקון שונה, לא רק אותה הוספת CSS**: בשני הדפים האלה `[data-theme="dark"]` (ו-ב-index גם `.theme-X`) יושבים על `document.body`, לא על `documentElement` - הפער היחיד מבין 6 הדפים בנקודה הזו (תועד עכשיו ב-CLAUDE.md). כלל CSS גרידא על `html` לא יכול לעקוב אחרי מצב שמוגדר רק על הילד שלו (custom properties לא עוברים בירושה כלפי מעלה). הפתרון: `window.syncHtmlBackground()` חדש - מעתיק את הרקע המחושב בפועל מ-`body` ל-`html`, בכל נקודה שבה מצב הדארק/סוג הטיול של `body` משתנה (הסקריפט הראשוני לפני הצביעה הראשונה, `applyTripPresentation()` ב-index, וכפתור החלפת מצב כהה בשניהם) - לא שכפול של הצבעים עצמם ב-JS, רק העתקה של מה ש-CSS כבר חישב. ל-`html` עצמו נשאר ערך ברירת מחדל סטטי (הצבע הבהיר של `:root`) כ-fallback לרגע שלפני שה-JS רץ

נבדק בפועל (קונסול, לא רק נומק): שמונה השילובים (4 סוגי טיול × בהיר/כהה) ב-shopping.html - `html`/`body` תואמים בכל אחד; index.html - כנ"ל דרך `applyTripPresentation()` המדומה וגם דרך כפתור מצב כהה האמיתי; wizard.html - בהיר/כהה וגם דרך כפתור מצב כהה האמיתי; packing/places/itinerary.html - נבדקו ונשארו תואמים ללא שינוי. `console` נקי משגיאות בשלוש הדפים ששונו (מלבד PERMISSION_DENIED הידוע בסביבת הבדיקה המקומית). **לא אומת במכשיר אמיתי** - הבדיקה בדפדפן מחשב לא מדמה safe-area-inset אמיתי (notch/home indicator), אז הפס עצמו לא נראה כאן; מה שכן נבדק ואומת הוא שה-`html`/`body` background מחושבים לזהות בפועל בכל שילוב, שזה בדיוק המנגנון שאמור לתקן את הפס במכשיר אמיתי

## v4.28.1 — בורר יעד בטופס המקום (השלמת שלב 9)

### ✨ תכונה חדשה
- **`places.html`**: בטופס הוספה/עריכה של מקום נוסף בורר יעד (`#placeLeg`), ליד בורר הקטגוריה - סוגר שני פערים: אין דרך לשייך מקום כשמסנן העמוד על "הכל", ואין דרך לשנות שיוך של מקום קיים. מודל על `editListSelect` מ-packing.html - `select` פשוט בתוך הטופס, לא פקד חדש ברמת העמוד
- **ביצירה: ברירת המחדל היא מסנן העמוד הפעיל; אם המסנן על "הכל" - ברירת המחדל "ללא שיוך". בעריכה: מציג את השיוך הנוכחי, ניתן לשנות לכל יעד או בחזרה ל"ללא שיוך"** - "ללא שיוך" הוא אפשרות מפורשת ברשימה (`value=""`), לא היעדר בחירה, כי מקום כזה מופיע בכל מסנן (התנהגות `renderPlaces()` הקיימת, ללא שינוי)
- **טיול רגל אחת - הבורר לא מוצג בכלל** - אותה בדיקה (`window.isMultiLeg`) כמו הבורר ברמת העמוד. אפס שינוי מבני
- **הרמז הקודם ("יתווסף ל-X") הוסר** - עם בורר מפורש בטופס עצמו, הרמז הפך למידע כפול שעלול לצאת מסנכרון עם הבורר שהוא מתאר. הקוד המת נמחק, לא הושאר מוסתר
- **`GEO_RULES` עוקב עכשיו אחרי בורר היעד של הטופס, לא אחרי מסנן העמוד** - `geoRefLeg()`/`categories()` מקבלות `legIdOverride` אופציונלי: קריאה בלי ארגומנט (כמו ב-`applyLanguage()`) ממשיכה לקרוא את מסנן העמוד בדיוק כמו היום; קריאה עם ערך מפורש (גם `""`) עוקפת אותו לגמרי. בחירת "רומא" בבורר הטופס מציגה "חוף 🏖️" כקטגוריה גם כשמסנן העמוד על "הכל" או על יעד אחר
- **קטגוריה שכבר נבחרת לא נעלמת בשקט כששינוי הבורר מסנן אותה גיאוגרפית** - שינוי בורר הטופס ליעד יבשתי בזמן שקטגוריית "חוף" כבר נבחרה משמר אותה כ"extra" ברשימת האפשרויות, באותו דפוס canon+extra שכבר קיים ב-`distinctPlaceCategories()` באותו קובץ - לא המצאה, הרחבה. **אומת בפועל**: מקום עם קטגוריית "חוף", עריכה ושינוי יעד לרגל יבשתית בלי לגעת בקטגוריה, שמירה - ה-payload שנלכד מראה `category:"חוף 🏖️"` ללא שינוי

### 🐛 תיקון
- **`savePlace()` כתבה `legId` רק ביצירה, ורק ממסנן העמוד** - ההערה הישנה תיעדה את זה כמצב קבוע ומכוון. עם בורר הטופס, ההגבלה בטלה: `legId` נכתב גם ביצירה וגם בעריכה, מהבורר בטופס. עריכה שמחזירה לבורר ל"ללא שיוך" כותבת `legId:null` במפורש (לא משמיטה את המפתח) - `update()` נוגע רק במפתחות שהוא מקבל, אותו לקח בדיוק מתיקון `legs:null` בפאנל הניהול

נבדק בפועל: טיול רגל אחת - הבורר מוסתר, אפס שינוי; יצירה במצב מסנן "הכל" - הבורר מאפשר שיוך, ה-payload שנלכד מראה `legId` שנבחר; יצירה כשמסנן פעיל - ברירת המחדל של הבורר תואמת את מסנן העמוד; עריכה - שינוי שיוך בשני הכיוונים (`leg1→leg2`, ואז `leg2→""` שנכתב כ-`legId:null`) - שני ה-payloads נלכדו בפועל; "ללא שיוך" - מקום בלי `legId` מופיע תחת כל מסנן עמוד; `GEO_RULES` עוקב אחרי בורר הטופס ולא אחרי מסנן העמוד - קטגוריית "חוף" מופיעה/נעלמת לפי בחירת הבורר בלבד, לא לפי מסנן העמוד; עברית ואנגלית, מצב כהה ובהיר, מסך נייד 375px - צולם מסך: שם היעד הנבחר קריא בבורר גם בלי הרמז שהוסר. הוסר `window.__test` הזמני ואומת שהוסר
`node scripts/i18n-audit.js places.html` - 0 ממצאים

## v4.28.0 — סינון מקומות לפי יעד (שלב 9)

### ✨ תכונה חדשה
- **`places.html`**: מקומות יכולים עכשיו להיות משויכים ליעד ספציפי (`legId`) ולהיות מסוננים לפיו - §5.4 במסמך. מסנן, לא רשימות נפרדות: מקום בלי `legId` ממשיך להופיע בכל מסנן
- **רק בטאב "מקומות" - לא תמונות, לא מוזיקה** - נבדק לפני כתיבה: למוזיקה כבר יש ציר סינון משלה (מקור - Spotify/YouTube וכו׳), ולשיר אין מושג מיקום בכלל. לתמונות אין שום UI סינון קיים היום ואין להן מטא-דאטה של מיקום - זה היה תוסף חדש, לא הרחבה. גם תואם את §5.4 עצמו, שמזכיר רק "מקומות"
- **`select` קומפקטי בתוך `.toolbar` הקיימת, לא שורה נוספת** - שדה החיפוש (`flex:1`) מצטמצם, הבורר (`flex:0 0 auto`) לצידו. **נמדד בפועל ב-375px לפני שהוחלט, לא רק הונח**: עם 4 יעדים והשם הארוך ביותר ("קואלה לומפור") נבחר, לשדה החיפוש נשארו 231px - נבדק חזותית (צילום מסך) שזה עדיין שדה קריא ונוח להקלדה, לא רק מדידת פיקסלים יבשה
- **מחלקות CSS נפרדות מ-packing.html (לא משותפות), עם הפניה הדדית** - `.place-leg-filter-select` לא `.leg-filter-select`: `min-height` שונה (46px כאן מול 42px שם) היה גורם להזזה של 4px אילו שותפו. `.leg-tag-hint` (האינדיקציה החזותית) כן זהה בערכים בין שני הקבצים (שני העמודים רשומים ל-`[data-tokens="core"]`) אבל נשאר כלל CSS נפרד בכל זאת - שלוש שורות משותפות בשני קבצים לא מצדיקות תלות בין-עמודית
- **מקום חדש עם מסנן יעד פעיל מקבל `legId` אוטומטית + אינדיקציה חזותית ("יתווסף ל-X")** - רק ביצירה, לא בעריכה, אותו היקף בדיוק כמו שלב 6. **תועד בהערת קוד**: אין דרך לשנות שיוך של מקום קיים - מקום שנוצר לפני שהיו רגליים, או כשהמסנן היה על "הכל", יישאר בלי שיוך לצמיתות. זה תקין לפי §5.4 (מופיע בכל מסנן), אבל מצב קבוע שכדאי להיות מודעים אליו, לא הפתעה
- **`openDayPicker` (הוספת מקום ליום) מסנן את הימים המוצעים לפי היעד של המקום** - מקום המשויך לבנגקוק כבר לא מציע ימי רומא. מספרי הימים ("יום N") נשארים הספירה המקורית של כל הטיול, לא ממוספרים מחדש בתוך קבוצת המשנה המסוננת - נבדק בפועל: מקום בנגקוק הציג "יום 5/6/7", לא "יום 1/2/3". רגל שנמחקה מאז (legId לא תקין) נופלת לכל הימים במקום בורר ריק - מקום בלי `legId` לא מושפע כלל

### 🐛 תיקון
- **`GEO_RULES` (מסתיר "חוף 🏖️"/"ספורט ים 🏄" ביעדים לא-חופיים) הייתה תמיד עוגנת לרגל הראשונה, ללא קשר לאיזה יעד פעיל בפועל - באג אמיתי, לא היפותטי, שנתפס תוך כדי הוספת המסנן.** בטיול שהרגל הראשונה יבשתית והבאה חופית, טופס הוספת מקום לא הציע "חוף" כקטגוריה גם כשמוסיפים מקום ליעד החופי בפועל. תוקן: `geoRefLeg()` חדשה משתמשת ברגל שתואמת את מסנן היעד הפעיל כשיש כזה, ונופלת לרגל הראשונה בדיוק כמו היום כשהמסנן על "הכל" - אפס שינוי לטיול רגל אחת או לתצוגה לא-מסוננת. **אומת בפועל**: טיול ציריך (יבשתית, ראשונה) ← רומא (חופית, שנייה, `vibes:["beach"]`) - מסנן "הכל"/ציריך לא מציע "חוף", מסנן רומא כן מציע - זה בדיוק התיקון
- **`window.hasValidCoords` (legs.js, v4.25.3) מגנה על רגל בלי קואורדינטות אמיתיות (geocoding כושל) בתוך `geoRefLeg()`** - לא כי `isNearCoast()` הייתה קורסת (היא כבר מטפלת ב-lat/lon לא-תקינים ומניחה "קרוב לחוף" כברירת מחדל בטוחה), אלא כי נפילה לקואורדינטות האמיתיות של הרגל הראשונה היא תשובה משמעותית יותר מברירת מחדל עיוורת. **אומת בפועל, לא רק נומק**: רגל עם `lat:null,lon:null` כמסנן פעיל - אין קריסה (`threw:null`), והתוצאה נופלת נכון לרגל הראשונה האמיתית (ציריך, יבשתית - "חוף" נשאר מוסתר, לא מוצג בטעות)

נבדק בפועל: טיול רגל אחת - הבורר מוסתר וריק, אפס שינוי מבני; טיול מרובה - הבורר מופיע; סינון - מקום משויך לרומא ומקום בלי `legId` שניהם מופיעים במסנן "רומא", מקום בנגקוק נעלם (ואותו דבר בכיוון ההפוך); מקום חדש - נלכד ה-payload בפועל לפני השליחה (`{legId:"leg_bkk"}`), לא רק נומק; `GEO_RULES` עדיין עובד ומתוקן לרגל הנכונה; רגל בלי קואורדינטות כמסנן פעיל - אין קריסה; `openDayPicker` מסנן נכון עם מספור ימים מקורי, ונופל לכל הימים על `legId` לא תקין; עברית ואנגלית; מסך נייד 375px. הוסר `window.__test` הזמני ואומת שהוסר
`node scripts/i18n-audit.js places.html` - 0 ממצאים

## v4.27.0 — סינון רשימת הציוד לפי יעד (שלב 6)

### ✨ תכונה חדשה
- **`packing.html`**: פריטי ציוד יכולים עכשיו להיות משויכים ליעד ספציפי (`legId`) ולהיות מסוננים לפיו - §5.1 במסמך. מסנן, לא רשימות נפרדות: מד ההתקדמות נשאר אחד לכל מה שמוצג, ופריט בלי `legId` ממשיך להופיע בכל מסנן
- **`select` קומפקטי, לא רצועת צ׳יפים שנייה** - נבדק לפני כתיבה מול רצועת הקטגוריות הקיימת: קטגוריות הן בלתי-מוגבלות ונוצרות ע"י המשתמש (בדיוק בשביל זה קיימת רצועה גוללת), רגליים חסומות ב-6 (`MAX_ADMIN_LEGS`) וברוב הטיולים 2-4. שורה שנייה קבועה הייתה עולה בצפיפות ויזואלית בדיוק במקום שהמשתמש ציין שהמסך כבר צפוף. הפתרון: `<select>` בתוך `.list-head` הקיימת - אפס גובה חדש, ותואם את המוסכמה הקיימת בקובץ הזה (`participantSelect`/`addItemCategorySelect`/`importCategorySelect`/`editListSelect` כבר משתמשים ב-`select` רגיל בדיוק לצורך הזה - "לבחור אחד מתוך רשימה קצרה, כשדה טופס" - לא ממציא דפוס אינטראקציה חדש (פופאובר מותאם) עבור הצורך הזה
- **נבדק בפועל ב-375px, כולל תרחיש הגרוע ביותר**: 4 יעדים (המקסימום המעשי) עם השם הארוך ביותר שנבדק ("קואלה לומפור") נבחר - השורה לא נשברת, לא גולשת, לא עוטפת. צפופה יותר מ-4 פקדים ל-5, אבל זו תחושה מדודה מפורשות ולא באג: נבדק `scrollWidth`/`clientWidth` (אין חריגה) לצד צילום מסך שהוצג למשתמש לאישור לפני שהומשך - כי רוב הטיולים הם רגל אחת, ושם ה-`select` כלל לא מוצג (ר' למטה)
- **טיול רגל אחת - הבורר לא מוצג בכלל, לא רק מנוטרל** - `window.isMultiLeg(trip)` (אותה בדיקה כבר בשימוש ב-index.html/itinerary.html, לא מומשה מקומית מחדש). אפס שינוי מבני
- **פריט חדש עם מסנן יעד פעיל מקבל `legId` אוטומטית + אינדיקציה חזותית** - כל שלוש נקודות ההוספה (הקלדה ידנית, אישור הצעת AI, אישור quick-pick) עוברות כולן דרך `addItem()` יחיד - התיוג נוסף שם פעם אחת ומכסה את כולן, בלי חיווט נפרד לכל אחת. האינדיקציה ("יתווסף ל{שם היעד}") מוצגת בגיליון ההוספה הידנית, לפני ההקלדה - לא נבנה רכיב חדש, נעשה שימוש באותו תפקיד טקסט-מעומעם-קטן שכבר ממלא `quick-picks-label` באותו גיליון
- **הצעות AI - תנאי הנטרול לא השתנה** (עדיין `activeList === "all"`, לא קשור ליעד) - וכיוון שהצעה מאושרת עוברת דרך `addItem()` הקיים, היא מקבלת את תיוג היעד באופן אוטומטי, בלי קוד נוסף

### 🐛 תיקון
- **פריט מיובא מחבר לא יורש את ה-`legId` של המקור - בכוונה, ומתועד למה** (§5.1 + חידוד מפורש): קטגוריה עוברת בייבוא כי היא סמנטית - "מסמכים" זה מסמכים לכולם. שיוך ליעד הוא הקשר אישי של מי שארז, לא עובדה משותפת - חבר שתייג פריט לרומא אולי נשאר שם יומיים נוספים; זה לא אומר שהמשתמש המייבא צריך את זה שם. הכלל: פריט מיובא מתויג לפי מסנן היעד הפעיל של **המשתמש המייבא**, אותו כלל בדיוק כמו פריט חדש שהוקלד - `importItem()` לא עוברת דרך `addItem()` (יש לה `push()` משלה מאז ומתמיד), אז זה חוט חיווט נפרד, לא קיבל את זה בחינם
- **נתפס באודיט ה-i18n, לפני push**: `aria-label` הסטטי של `#legFilterSelect` היה בעברית בלבד. תוקן באותו דפוס בדיוק שכל שאר כפתורי `.list-head` כבר משתמשים בו - עדכון ב-`applyLanguage()` דרך `tr()`, לא מחרוזת סטטית ב-HTML

נבדק בפועל: טיול רגל אחת - הבורר מוסתר וריק, 4 פקדים ב-`.list-head-actions` בדיוק כמו לפני; טיול מרובה - הבורר מופיע; סינון לפי יעד - פריט משויך לרומא ופריט בלי `legId` שניהם מופיעים במסנן "רומא", פריט בנגקוק נעלם; אותה בדיקה בכיוון ההפוך (מסנן בנגקוק); שני המסננים יחד (קטגוריה="ביגוד" + יעד="רומא") - התוצאה היא החיתוך של שניהם, כולל מד ההתקדמות שמשקף רק את מה שמוצג בפועל; פריט חדש עם מסנן יעד פעיל - נלכד ה-payload בפועל לפני השליחה (`{targetCategory:"main",legId:"leg_bkk"}`), לא רק נומק; עברית ואנגלית (כולל היפוך סדר ה-`select`/האייקונים ב-LTR); מסך נייד 375px. הוסר `window.__test` הזמני (הוצג פעמיים - לבדיקות הליבה ולבדיקת השפה הנוספת) ואומת שהוסר בשני הסבבים
`node scripts/i18n-audit.js packing.html` - 0 ממצאים (אחרי התיקון; 1 ממצא נתפס ותוקן לפני כן)

## v4.26.2 — מפריד בין רגליים בטאבי הימים (שלב 7, כולל 4ב שנדחה משלב 4)

### ✨ תכונה חדשה
- **`itinerary.html`**: רצועת טאבי הימים מסמנת עכשיו מעבר בין רגליים - חץ + שם היעד הבא, בין הטאב האחרון של רגל אחת לטאב הראשון של הבאה. §5.2 במסמך, מוזג לכאן מ-4ב שנדחה בשלב 4
- **`.day-leg-separator` חדשה - לא שכפול של `.forecast-leg-separator` (index.html, שלב 8 commit ג), למרות רעיון עיצובי זהה.** נבדק לפני כתיבה: הפריסה זהה (רצועת flex אופקית גוללת), אבל מערכת הצבע שונה מהותית - `.forecast-strip` יושבת על כרטיס תמיד-כהה (`color:#fff` קשיח, נכון שם), `.day-tabs` יושבת על המשטח של האתר עצמו (`[data-tokens="core"]`, `var(--muted)`). **אומת חזותית, לא רק נומק**: צילום מסך במצב בהיר עם הצבעים הקשיחים של index.html היה מציג טקסט לבן כמעט בלתי-נראה על רקע בהיר - זו ההוכחה הקונקרטית לכך שהחלטה לשכפל ולא לחלץ הייתה נכונה, לא רק עקרונית. `--glow-color` (CLAUDE.md) הוא התקדים המתועד למקרה הזה - שתי מערכות צבע אמיתיות, לא אותו דבר שנשכח בשני מקומות
- **חץ הכיוון גם הוא לא חולץ** - `legRouteArrow()` (index.html) פונקציה בת שורה אחת בסקריפט הקלאסי של אותו עמוד, לא נגישה מ-`itinerary.html` (עמוד HTML נפרד לגמרי) גם אם הייתה מיוצאת. וגם: זו טרנרי יציב-לנצח (שתי שפות בלבד, לא ישתנה) - לא באותה קטגוריית סיכון כמו `zonedParts` (15 שורות, מקרה קצה אמיתי) שהצדיק את יצירת `datetime.js`. `itinerary.html` כבר כותב `trip.language==='en'` inline ממש ליד (לפורמט התאריך) - אותו דפוס מקומי, לא קובץ משותף חדש בשביל טרנרי אחד
- **תיעוד הפיצול בשני הכיוונים** - הערת קוד ב-`index.html` וב-`itinerary.html`, כל אחת מפנה לשנייה ומסבירה שזה פיצול מכוון. **גם ב-`CLAUDE.md`, ליד ההערה הקיימת על `--glow-color`** - בעוד חצי שנה מישהו שרואה שתי מחלקות כמעט-זהות עלול לחשוב שזו כפילות לניקוי; איחוד ישבור את המצב הבהיר באחד מהעמודים
- **המפריד אחרי יום המעבר, לא לפניו (§5.2, בדיקה 4)** - `tripDayDates()` (legs.js, כבר משלב 4א) כבר משייכת את יום המעבר לרגל **היוצאת** ("יום המעבר שייך לרגל היוצאת"), אז תנאי ההשוואה הפשוט (`days[i].leg.id !== days[i-1].leg.id`) ממקם את המפריד נכון מעצמו - לא נדרש טיפול מיוחד, זו תוצאה של החלטה שכבר התקבלה. **אומת בפועל, לא רק בהיגיון**: טיול 3 רגליים עם גבולות בימים מדויקים - יום המעבר (13.9) הופיע כיום הרביעי והאחרון של רומא, והמפריד "← בנגקוק" רק אחריו
- **טיול רגל אחת - אפס מפרידים, מבחינה מבנית** - מפריד מוכנס רק כש-`days[i].leg.id !== days[i-1].leg.id`; טיול רגל אחת אף פעם לא מגיע לתנאי הזה
- **הגלילה לטאב "היום" (v4.23.2) נבדקה מחדש עם מפרידים ברצועה** - עדיין נוחתת נכון (`scrollIntoView` פועל על האלמנט עצמו, לא על אינדקס/היסט מחושב, כך שמפרידים נוספים לא משפיעים על הדיוק)

נבדק בפועל: טיול רגל אחת - 0 מפרידים; טיול 3 רגליים עם גבולות ימים מדויקים - 2 מפרידים במקום הנכון (כולל בדיקת יום-מעבר הדקה שתוארה למעלה); גלילה ל"היום" עם 2 מפרידים ברצועה - הטאב נכנס לתחום הנראה של המכולה; עברית ואנגלית (כיוון חץ הפוך, פורמט תאריך שונה); מצב בהיר וכהה - אומת חזותית שהטקסט קריא בשניהם; מסך נייד (375px) - הרצועה גוללת חלק. הוסר `window.__test` הזמני ואומת שהוסר (`grep` נקי בשני הקבצים) לפני push
`node scripts/i18n-audit.js itinerary.html` - 0 ממצאים

## v4.26.1 — מפריד בין רגליים ברצועת מזג האוויר (שלב 8, commit ג)

### ✨ תכונה חדשה
- **המעבר בין יעדים ברצועת מזג האוויר כבר לא מסומן רק ע"י קפיצת טמפרטורה** - נבדק בפרודקשן: `18°` ואז `32°` ברצף, בלי שום סימון שהיעד השתנה. עכשיו כל מעבר בין רגל לרגל מסומן בפריט מפריד עם חץ ושם היעד הבא
- **עיצוב חדש, לא שכפול** - §5.3 מפנה ל"אותו דפוס ויזואלי כמו התוכנית היומית", אבל הדפוס הזה (§5.2: קו מלא ברוחב + טקסט במרכז) לא קיים עדיין בשום מקום בקוד - שלב 7 (המפריד בטאבי הימים) עוד לא מומש. גם ההקשר שונה מבנית: שם רשימה אנכית ברוחב מלא, כאן רצועת צ׳יפים אופקית גוללת. המפריד כאן הוא איבר `flex` צר בגודל תוכן, בלי רקע ובלי padding כמו `.forecast-day` - עיצוב מכוון, לא סתם השמטה: מפריד בצורת צ׳יפ היה מתחרה חזותית בנתונים במקום להיות כפוף להם (עקרון הצפיפות)
- **`legRouteArrow()` הקיימת משמשת לכיוון החץ** (לא נגזרת מחדש) - אותה פונקציה שכבר משמשת את טקסט המסלול בכותרת דף הבית (§5.8) - RTL/LTR נכונים בלי לוגיקה כפולה
- **מוצג גם לפני רגל בלי נתונים** (הצ׳יפ המעומעם, geocoding כושל או מעבר ל-16 יום) - המפריד מסמן שהיעד השתנה, וזה נכון גם כשליעד הבא אין תחזית. בלי המפריד, הצ׳יפ המעומעם היה נראה כמו פער לא-מוסבר באמצע הרצועה
- **טיול רגל אחת - אפס מפרידים, מבחינה מבנית לא רק ויזואלית** - מפריד מוכנס רק בין הפריטים של שתי רגליים *שונות*; טיול רגל אחת אף פעם לא מגיע לתנאי הזה

נבדק בפועל: טיול רגל אחת - 0 מפרידים, אותה כמות צ׳יפים כמו לפני commit ג; טיול 3 רגליים - בדיוק 2 מפרידים, במקום הנכון בין הרגליים (לא לפני הרגל הראשונה, לא אחרי האחרונה); עברית ואנגלית עם שמות יעד ארוכים ("קואלה לומפור", "סן פרנסיסקו") - רוחב המפריד נשאר בערך כמו צ׳יפ בודד, לא שני צ׳יפים; גודל הטקסט (`.62rem`, נבדק בפועל `9.92px` ברזולוציית מכשיר אמיתית) גדול במעט מטקסט שם היום בצ׳יפ עצמו (`9.6px`) - קריא במסך 375px, לא רק בצילום מוקטן; גלילה בכמה נקודות אמצע (`scrollLeft` שונים) - המפריד לא נחתך בצורה שמסתירה חלקית רק את החץ או רק את השם, אותה התנהגות קצה כמו צ׳יפ נתונים רגיל; אין מנגנון הצללת-קצה (`has-more`) לרצועה הזו בקוד בכלל - נבדק ולא נמצא, אז אין מה לוודא התאמה אליו
`node scripts/i18n-audit.js index.html` - 0 ממצאים

## v4.26.0 — רצועת מזג האוויר הופכת מרובת-רגליים (שלב 8, commit ב)

### ✨ תכונה חדשה
- **`fetchForecast()` כבר לא מציגה תמיד את הרגל הראשונה** - הליבה של שלב 8 (docs/schema-legs.md §5.3). בטיול ניו יורק→בנגקוק→הונולולו, הרצועה הציגה תמיד את מזג האוויר של ניו יורק, בלי קשר לאיפה הטיול נמצא בפועל. עכשיו כל רגל תורמת את הימים שלה, כל אחד עם מזג האוויר של המקום הנכון - ברצף אחד, בלי מפריד ויזואלי בין רגליים (זה commit ג - דפוס עיצוב חדש לגמרי, לא שכפול של משהו קיים, כמו שכבר תועד כשנחקר). פחות ברור מבחינה ויזואלית, אבל נכון - ועדיף על תחזית של המקום הלא נכון
- **קריאה רב-מיקומית יחידה, לא N מקביליות** - **אומת מול ה-API האמיתי לפני הכתיבה**, לא הונח: Open-Meteo תומך בקואורדינטות מופרדות בפסיק. ממצא לא-מובן-מאליו: **צורת התשובה תלויה במספר הקואורדינטות** - קואורדינטה אחת מחזירה אובייקט בודד (הצורה המוכרת), 2+ מחזירות **מערך**, אובייקט אחד לכל מיקום, **באותו סדר שבו הקואורדינטות נשלחו**. הקוד בודק `Array.isArray(data)` ומנרמל ל-`[data]` כשזו לא רשימה - כך טיול רגל אחת ממשיך לשלוח קואורדינטה בודדת ולקבל את אותה צורה בדיוק כמו היום, ולא נשלחת קואורדינטה מיותרת רק כדי לאחד את הצורה. **הקורלציה בין תשובה לרגל היא לפי מיקום במערך בלבד** - אין `id`/שם בתשובה שאפשר להתאים לפיו (יש `location_id` שמופיע החל מאינדקס 1 בלבד, נעדר באינדקס 0 - קוריוזיטה של ה-API, לא בסיס לקורלציה) - מתועד בהערת קוד כאזהרה: אם Open-Meteo אי-פעם יפסיק לשמור על סדר הקלט, זה יצמיד בשקט מזג אוויר לרגל הלא נכונה בלי שום דרך לזהות זאת מהתשובה עצמה
- **תגלית נוספת שמומשה בחינם**: לכל מיקום בתשובה יש `timezone` משלו (מ-`timezone=auto`), ו-`daily.time[0]` שלו מעוגן ל"היום" **של אותו מיקום עצמו** - לא לתאריך משותף אחד לכל הבקשה. זה בדיוק מה שסעיף 5 (todayKey per-leg) היה צריך, בלי חישוב לקוח נוסף - כל רגל משווה מול ה-`dateKey` שלה עצמה, בשעון שלה עצמה
- **רגל בלי קואורדינטות (geocoding כושל, מקרה "קליפורניה") ורגל מעבר ל-16 הימים** - שתיהן מקבלות אותה התנהגות: צ׳יפ יחיד עם שם היעד, בלי נתונים, מעומעם - אותה תבנית ויזואלית בדיוק כמו העמעום הקיים לימים 7+ (`.unreliable`), לא CSS חדש. **צ׳יפ אחד לרגל, לא אחד לכל יום חסר** - רגל ארוכה שחורגת מ-16 הימים מקבלת קצת ימים אמיתיים ואז צ׳יפ נגררת יחיד לשארית, לא שרשרת של צ׳יפים זהים. `window.hasValidCoords` (legs.js, v4.25.3) קובעת אילו רגליים בכלל נכנסות לבקשת ה-API - לא `hasRealCoords` הגלובלי הישן, שנבדק רק מול הרגל הראשונה ומסתיר את **כל** הרצועה אם היא נכשלה, גם כשרגליים אחרות תקינות
- **`todayKey`/הטווח הרלוונטי - לכל רגל בנפרד, בשעון שלה עצמה** - זה בדיוק מה שנעל את commit ג של שלב 5 ("הקריאה עוד ממוקדת לרגל ראשונה בלבד"): עכשיו כשהקריאה עצמה מפוצלת, כל רגל משווה מול ה-`dateKey` שלה. רגל שכבר הסתיימה (ה"היום" שלה כבר עבר את ה-`endAt` שלה) לא תורמת שום דבר - אותה השפעה כמו הבדיקה הישנה "rangeStartKey > tripEndKey" הגלובלית, רק מוערכת פר-רגל

### 🐛 תיקון
- **מפתח המטמון תוקן משני כיוונים, לא רק "נוסף legId"**: הבקשה המקורית הייתה `legId + שפה`, אבל שני מקרים אמיתיים מראים ש-`legId` לבדו לא מספיק. **(1)** טיול רגל אחת: הרגל הסינתטית מ-`getLegs()` (legs.js) נושאת תמיד `id: "main"` - מפתח לפי id בלבד היה **אף פעם לא משתנה** בשום עריכה של רוב הטיולים באפליקציה (כל טיול רגל אחת). **(2)** שינוי שם רגל קיימת בפאנל אדמין שומר את ה-`id` שלה (v4.25.1, בכוונה - יציבות שיוך) אבל **כן** משנה `lat`/`lon` (geocoding מחדש); ועריכת תאריך מעבר בלי שינוי שם לא מסמנת אף רגל כ-`dirty` בכלל. **הפתרון**: `id:lat:lon:startAt:endAt` לכל רגל רלוונטית + השפה - אותו תפקיד בדיוק שמילא `tripEndKey` במפתח הישן (לתפוס עריכת תאריך), רק פר-רגל במקום גלובלי לטיול
- **הוספת השפה למפתח מתקנת גם באג שכבר תועד**: הרצועה הייתה נשארת בשמות ימים בשפה הקודמת אחרי החלפת שפה, כי `lastForecastKey` הישן לא כלל שפה בכלל - שינוי שפה לא היה גורם ל-refetch, ורינדור מחדש קורה רק בתוך `fetchForecast()`. אומת בפועל: החלפת שפה (בלי לגעת ברגליים) מפעילה כעת קריאת רשת נוספת ומעדכנת את שמות הימים; שמירה על שדה לא-קשור (למשל `showAccent`) **לא** מפעילה קריאה נוספת - המטמון מחזיק
- **נתפס בבדיקות, לא בעיון**: הגרסה הראשונה של הקוד קראה ל-`window.dateKeyInZone(leg.startAt, tz)`/`(leg.endAt, tz)` עם מחרוזת ISO גולמית במקום עטופה ב-`new Date(...)` - זרקה `RangeError: Invalid time value` מיד עם טעינת כל טיול. בדיוק האזהרה התיעודית ב-`legs.js`'s הראש ("any future code that reads leg.startAt/leg.endAt directly... without wrapping in new Date() first will silently misbehave") - כאן זה לא נכשל בשקט, זה זרק מיד, מה שאיפשר לתפוס ולתקן לפני commit

נבדק בפועל (לא רק על אובייקטים שנבנו לפי ההנחות - `fetch` הוחלף ב-spy שלוכד את ה-URL האמיתי שהקוד בונה, ובחלק מהבדיקות הושוו ל-HTML שהקוד הישן (`git stash`) הפיק על אותו קלט בדיוק):
- טיול רגל אחת - **HTML זהה בית-לבית** לקוד הישן (השוואה ישירה, לא רק "נראה דומה"), ואותו URL בדיוק (`latitude=X&longitude=Y`, בלי פסיק)
- טיול 3 רגליים (רומא פעילה, בנגקוק+הונולולו עתידיות) - קריאה רב-מיקומית יחידה אחת (`latitude=41.9,13.75,21.3&longitude=...`), טמפרטורות ייחודיות לכל רגל (100/200/300 במבחן) אימתו שאין ערבוב בין המיקומים; "היום" מסומן נכון על היום הראשון של הרגל הפעילה
- רגל בלי קואורדינטות ("קליפורניה") - צ׳יפ מעומעם עם השם, `--°`, אין קריאת רשת עבורה (רק הרגל התקינה נכנסת ל-URL); רגל ארוכה שחוצה את 16 הימים - ימים אמיתיים לחלק המכוסה ואז צ׳יפ נגררת יחיד לשארית
- החלפת שפה - שמות הימים מתעדכנים בפועל, ורק אז; שמירה על שדה לא-קשור - אין קריאת רשת מיותרת (זו הבדיקה שמוכיחה שהמטמון עובד)
- עברית ואנגלית, מסך נייד (375px) - שני צילומי מסך, כולל הצ׳יפ המעומעם והרצועה הגוללת

`node scripts/i18n-audit.js index.html` - 0 ממצאים

## v4.25.4 — תיקון: קריסה חוזרת כשלרגל הפעילה אין timezone (רגרסיה מ-v4.25.0)

### 🐛 תיקון
- **רגרסיה מ-v4.25.0, נתפסה תוך כדי בדיקת commit ב של שלב 8 (v4.25.3), לא ממצא צדדי**: כשהרגל הפעילה בטיול מרובה-רגליים כשלה ב-geocoding (`timezone: null` - אותו מקרה "קליפורניה"), `multiLegDuringStatusLine()` זרקה `RangeError: Invalid time zone specified: null` מתוך `new Intl.DateTimeFormat({timeZone: activeTz})` - `activeTz` נקרא ישירות מ-`activeLeg.timezone`, בלי הגנה. `updateClockAndPhases()` (הקוראת לפונקציה) רצה כל שנייה - **קריסה חוזרת בקונסול, לא אירוע חד-פעמי**: אומת בפועל שהטאב המשיך לזרוק את אותה שגיאה כל טיק במשך כל הזמן שנשאר טעון
- **שתי שכבות הגנה, לא אחת**: (1) `activeTz` עברה מ-`activeLeg.timezone` הגולמי ל-`window.getCurrentLegTimezone({legs: currentLegs})` - נופלת לרגל האחרונה כשלרגל הפעילה אין timezone משלה, אותה שרשרת נפילה שכבר קיימת ב-§4. (2) קריאת ה-`Intl.DateTimeFormat` עצמה שולחת `activeTz || undefined` במקום `activeTz` - `undefined` גורם ל-Intl.DateTimeFormat להשתמש בשעון הריצה (המכשיר) כברירת מחדל; `null` מפורש זורק. הגנה שנייה כי `getCurrentLegTimezone` עצמה עדיין יכולה להחזיר `null` (גם הרגל הפעילה וגם האחרונה בלי timezone) - לא כפילות, קו הגנה אחרון בנקודת הקריאה עצמה
- **למה `getCurrentLegTimezone` לא תפסה את זה קודם**: היא פשוט לא הייתה בשימוש כאן בכלל - `multiLegDuringStatusLine` קראה `activeLeg.timezone` ישירות, לא דרך הפונקציה המשותפת
- **חיפוש רחב, בכל 6 העמודים ובקבצים המשותפים (`legs.js`, `datetime.js`): 11 מקומות עם `Intl.DateTimeFormat`/`toLocale*`, 10 מהם כבר מוגנים כראוי, מקום אחד יחיד לא - וזה בדיוק זה שקרס.** 7 לא מקבלים `timeZone` בכלל (בטוחים מבנית - `wizard.html`×3, `index.html`×2, `itinerary.html`×1, `places.html`×1). מתוך 3 שמקבלים `timeZone`: `zonedParts` (`datetime.js`) בודקת `!timeZone` פנימית לפני כל קריאה; `localInputToUtcInZone` (`datetime.js`) ו-`toZonedInputValue` (`index.html`) לא מוגנות פנימית, אבל **כל אחת** מ-4 הקריאות הקיימות אליהן (`legBuilder.js`×2, `index.html`×2) עוטפת בבדיקת אמת לפני הקריאה. **10 מתוך 11 - זו לא רק סטטיסטיקה: זה אומר שאין כאן משפחת-באגים נסתרת, ושהעבודה הקודמת על אזורי זמן (§4, §8ব סעיף 7, v4.23.1) הייתה יסודית - המקום היחיד שנשבר הוא זה שנכתב הבוקר ועדיין לא עבר את אותו משטר הגנה.** נרשם כאן במפורש בשביל מי שיוסיף `Intl.DateTimeFormat` חדש בעתיד: זה הדפוס לשמור עליו - אם `timeZone` יכול להיות `null`, או לבדוק `!timeZone` לפני הקריאה (כמו `zonedParts`) או לוודא שכל קריאה מוגנת בנקודת הקריאה (כמו `localInputToUtcInZone`/`toZonedInputValue`) - לא להעביר ערך גולמי ישירות
- נבדק בפועל: קריסה שוחזרה במלואה לפני התיקון (`git stash` זמני לחזרה לקוד הישן, `RangeError` נלכד עם ה-stack המדויק, כולל שהטאב המשיך לקרוס כל שנייה ברקע); אחרי התיקון - אותו fixture בדיוק (טיול 3 רגליים, הרגל האמצעית הפעילה בלי timezone) בטאב נקי לחלוטין: אין `RangeError` בקונסול אחרי כמה טיקים של ה-interval החי, השורה מציגה `✈️ ב-10.9 טסים לHonolulu` (תאריך המעבר בשעון המכשיר, סביר)
- `node scripts/i18n-audit.js index.html legs.js` - 0 ממצאים (אין טקסט משתמש חדש)

## v4.25.3 — תיקון: geocoding כושל נשלח ל-Open-Meteo כ-(0,0) במקום להיחסם

### 🐛 תיקון
- **נתפס תוך כדי בדיקת commit א של שלב 8 (רגל בלי קואורדינטות), לא בעיון**: `hasRealCoords`/`latCoord`/`lonCoord` (`index.html`, ליד שורה 3510) בדקו `Number.isFinite(Number(primaryLeg.lat))` בלי לבדוק `!= null` קודם. `Number(null)` הוא `0`, לא `NaN` - אז רגל שנכשלה ב-geocoding (`lat: null, lon: null`, הערך שכשל `legBuilder.js` כותב בפועל) עברה כאילו יש לה קואורדינטות תקינות. **חמור משנראה**: לא רק ש-`hasRealCoords` היה שגוי (`true` במקום `false`) - `latCoord`/`lonCoord` עצמן נדרסו ל-`0`/`0` (Null Island, נקודה באוקיינוס האטלנטי), לא נשארו בערך הקודם כפי שההערה הישנה בקוד הניחה
- **הוכח בפועל, לא רק נומק**: טיול מרובה-רגליים עם הרגל הראשונה (רומא) כושלת geocoding והרגל הפעילה (בנגקוק) תקינה - לפני התיקון, `fetchForecast()` שלחה בפועל `https://api.open-meteo.com/v1/forecast?latitude=0&longitude=0&...` ל-Open-Meteo (נלכד עם fetch spy על הקוד האמיתי, לא אובייקט שנבנה לפי הנחות). אחרי התיקון: `hasRealCoords=false`, `latCoord`/`lonCoord` נשארים בערך הקודם, וה-guard הקיים של `fetchForecast()` (`if (!hasRealCoords || ...)`) עוצר את הקריאה לגמרי - בלי לשנות את ה-guard עצמו, רק את הערך שהוא בודק
- **`window.hasValidCoords(obj)` חדשה ב-`legs.js`** - `lat`/`lon` `!= null` בדיקה מפורשת לפני ההמרה למספר. חולצה כפונקציה משותפת אחרי שאותה טעות בדיוק קרתה פעמיים בישיבה אחת: פעם ב-`getCurrentLegCoords()` (v4.25.2, נתפסה בבדיקות שלה) ופעם כאן, כבר בפרודקשן. `getCurrentLegCoords()` עודכנה להשתמש בפונקציה המשותפת במקום בעותק הפנימי שלה
- **`index.html`'s שני ה-`if` הנפרדים (בדיקת lat ובדיקת lon בנפרד) אוחדו לבדיקה אחת**: בפועל lat/lon תמיד מגיעים יחד מאותה קריאת geocoding (`legBuilder.js`) - אף פעם לא תקינים בנפרד. `hasRealCoords` (המחושב נכון עכשיו) שולט על עדכון שניהם יחד, לא שני checks כפולים שיכולים לדרוש תחזוקה נפרדת
- **ממצא נוסף, לא תוקן כאן - מחוץ להיקף**: באותה בדיקה, טיול מרובה-רגליים שבו ה**רגל הפעילה עצמה** (לא רק רגל אחרת בטיול) כושלת geocoding (`timezone: null`) גרם ל-`multiLegDuringStatusLine()` (v4.25.0, שורת המצב בכרטיס הבית) לזרוק `RangeError: Invalid time zone specified: null` מתוך `new Intl.DateTimeFormat(locale, {timeZone: activeTz})` - קריסה לא-תפוסה בתוך `updateClockAndPhases()`. תועד, לא תוקן - commit נפרד
- נבדק בפועל: טיול רגל אחת עם קואורדינטות תקינות - אפס שינוי (`hasRealCoords=true`, `latCoord`/`lonCoord` נכונים, שתי הקריאות ל-Open-Meteo יוצאות עם הקואורדינטות הנכונות); טיול מרובה-רגליים עם רגל ראשונה כושלת ורגל פעילה תקינה - `fetchWeather()` (מ-v4.25.2) כבר משתמשת ברגל הפעילה כך שלא הושפעה; `fetchForecast()` (שעדיין רגל-ראשונה-בלבד, מחוץ להיקף) עוברת מקריאה שגויה ל-(0,0) לחסימה נכונה
- `node scripts/i18n-audit.js index.html legs.js` - 0 ממצאים (אין טקסט משתמש חדש)

## v4.25.2 — תיקון: שורת "עכשיו" מציגה מזג אוויר של הרגל הראשונה תמיד

### 🐛 תיקון
- **docs/schema-legs.md §8, commit א**: בטיול מרובה-רגליים, שורת "עכשיו" (טמפ׳/רוח/UV/מצב) הציגה תמיד את מזג האוויר של הרגל **הראשונה** - `fetchWeather()` קראה קואורדינטות מ-`latCoord`/`lonCoord`, שתי גלובליות שמוזנות מ-`primaryLeg` בלבד. משתמש שכבר בבנגקוק (רגל 2 מתוך 3) ראה את מזג האוויר של ניו יורק - מידע שגוי, לא חסר
- **`window.getCurrentLegCoords(trip)` חדשה ב-`legs.js`** - האנלוגיה הקואורדינטית ל-`getCurrentLegTimezone()` הקיימת, לא באותה צורה בדיוק: ל-timezone יש "שעון המכשיר" כתשובה סופית תמיד-תקינה; לקואורדינטה אין מקבילה. במקום זאת, לפני/אחרי הטיול (`getLegForDate` מחזירה `null`) המשתנה משקף את הפיצול הקיים ב-§4 עצמו - `trip.startAt` נקרא מהרגל הראשונה, `trip.endAt` מהאחרונה: לפני הטיול = רגל ראשונה, אחריו = רגל אחרונה, ובמהלכו = הרגל הפעילה, כמו `getCurrentLegTimezone`
- **רגל עם קואורדינטות חסרות לא מסתירה את השורה**: אם הרגל שנבחרה (פעילה, ראשונה או אחרונה) חסרת `lat`/`lon` תקינים (geocoding כושל/מעורפל - למשל "קליפורניה"), מחפשת רגל אחרת שכן יש לה - רגל אחרונה, ואז ראשונה. מחזירה `null` רק כשאף רגל בטיול כולו לא נושאת קואורדינטות אמיתיות
- **נתפס בבדיקה, לא בעיון**: הבדיקה הראשונה של הבדיקה 5 (רגל בלי קואורדינטות) החזירה `{lat:0, lon:0}` במקום ליפול לרגל הבאה - `Number(null)` הוא `0`, לא `NaN`, אז `Number.isFinite(Number(leg.lat))` לבדו מקבל `lat:null` (הערך שכשל geocoding כותב בפועל, `legBuilder.js`) כאילו זו קואורדינטה אמיתית ב-Null Island. תוקן: בדיקת `!= null` מפורשת לפני ההמרה. **אותו דפוס בדיוק (`Number.isFinite(Number(leg.lat))` בלי בדיקת `null` קודם) קיים כבר ב-`hasRealCoords` (`index.html`, שורה קרובה ל-3510) - לא תוקן כאן, מחוץ להיקף ה-commit הזה, אבל זה ממצא אמיתי שכדאי לטפל בו בנפרד**: `fetchForecast()`'s guard עלול לא לתפוס בפועל רגל עם `lat:null`/`lon:null` מוצהר
- **`fetchWeather()` (`index.html`)** קוראת ל-`getCurrentLegCoords({legs: currentLegs})` רק כש-`tripId` אמיתי; במצב "אין טיול טעון" (עמוד נחיתה בלי `tripId`, שם `currentLegs` עדיין `[]` הראשוני) ממשיכה להשתמש ב-`latCoord`/`lonCoord` הגלובליים כמו היום - אפס שינוי לאותו מצב, לא נבדק בבקשה המקורית אבל נמצא כנקודת קריאה אמיתית ל-`fetchWeather()` ולא הושאר ללא טיפול
- **התוצאה נשארת מקומית**, לא נכתבת חזרה ל-`latCoord`/`lonCoord` הגלובליים - אלה עדיין מזינות את `fetchForecast()` (עדיין רגל-ראשונה-בלבד במפורש, עד commit נפרד בהמשך §8) ואת האיפוס בפאנל האדמין; דריסה כאן הייתה מזליגה את מיקום הרגל הפעילה לקוד שעדיין לא אמור להכיר אותו
- נבדק בפועל מול `fetchWeather()` האמיתית (fetch מוחלף ב-spy שלוכד את ה-URL, לא אובייקט שנבנה לפי ההנחות): (1) טיול רגל אחת - קואורדינטות זהות בית-לבית למקור; (2) מרובה-רגליים, הרגל האמצעית פעילה - קואורדינטות הרגל האמצעית, לא הראשונה; (3) לפני הטיול - רגל ראשונה; (4) אחרי הטיול - רגל אחרונה; (5א) הרגל הפעילה בלי קואורדינטות, האחרונה כן - נופל לאחרונה; (5ב) גם האחרונה בלי קואורדינטות, הראשונה כן - נופל לראשונה; (5ג) אף רגל בטיול לא נושאת קואורדינטות - "לא זמין" מוצג, אין קריאת רשת בכלל; מצב "אין טיול" - זהה בית-לבית למקור
- אין שינוי טקסט משתמש חדש ("לא זמין" כבר קיים ב-catch block הקיים של אותה פונקציה) - לא נדרש `i18n-audit.js`, הורץ בכל זאת ללא ממצאים

## v4.25.1 — תיקון: legId לא היה יציב בין שמירות בפאנל אדמין

### 🐛 תיקון
- **נתפס בחקירת שלב 8 (docs/schema-legs.md, רצועת מזג האוויר), לפני שהוא הפך לבאג בפרודקשן**: `buildLegs()` (`legBuilder.js`) המציאה `id` אקראי חדש לכל רגל בכל קריאה - כולל רגליים שלא נגעו בהן (`dirty=false`, קואורדינטות מועתקות מ-`originalLeg`). זרימת השמירה בפאנל אדמין (`index.html`) קוראת ל-`buildLegs()` על כל הרגליים יחד בכל שמירה שנוגעת בטיול מרובה-רגליים - לא רק ברגליים ששונו. §2.2 קובעת במפורש `id` יציב "כי פריטי ציוד ופעילויות מצביעים על רגל" - שמירה שמאפסת את כל ה-id-ים הייתה מנתקת שיוכים כאלה בשקט, בכל שמירה, לא רק בעריכת מבנה הרגליים עצמו
- **אף קוד קיים לא קרא `leg.id`/`legId` עד כה** (אומת בגרפ) - שלבים 6/9 (שיוך ציוד/מקומות לפי רגל) יהיו הצרכן הראשון, וה-instability הייתה מתגלה רק אז, מאוחר מדי לאבחן בקלות
- **התיקון**: `buildLegs()` מקבלת פרמטר אופציונלי חדש, `legIds` - כשיש ערך לאינדקס נתון, הוא נשמר כ-`id` של הרגל במקום למינות חדש. הוויזרד לא מעביר `legIds` בכלל (כל הרגליים שם חדשות מיסודן) - שם ההתנהגות זהה לפני ואחרי. הפאנל אדמין מעביר `legDescriptors.map(r => r.originalLeg?.id || null)` - שורה חדשה יחידה לפני הקריאה הקיימת ל-`buildLegs()`
- **שינוי סדר רגליים ממשיך לעבוד נכון**: `moveAdminLegUp()` כבר מחליפה את `originalLeg` (כולל ה-`id` שבתוכו) יחד עם `input.value`/`dirty` בין שתי השורות - נבדק ישירות שהמיפוי `legDescriptors → legIds` אחרי סימולציה של אותה החלפה מחזיר את ה-id-ים בסדר החדש, לא הישן
- נבדק בפועל (קריאה ישירה ל-`window.buildLegs()` בקונסול, לא רק אובייקט שנבנה לפי ההנחות): (1) כל הרגליים עם `legIds` קיים - ה-id-ים חוזרים זהים בית-לבית; (2) בלי `legIds` (מקרה הוויזרד) - כל הרגליים מקבלות id טרי וייחודי; (3) `legIds` מעורב (רגל אמצעית `null`, שתי הקצוות עם id קיים) - הקצוות נשמרים, האמצעית מקבלת id טרי; (4) סימולציית שינוי סדר על מערך descriptors - ה-id-ים זזים עם הרגל, לא נשארים במיקום
- אין שינוי ל-UI/טקסט משתמש - לא נדרש `i18n-audit.js`

## v4.25.0 — שורת המצב בכרטיס הבית ביעדים מרובים (index.html בלבד)

### ✨ תכונה חדשה
- **שורת המצב (`#timerTitle`) בשלב "במהלך הטיול" מתעדכנת ליעד הפעיל, לא נשארת תקועה על היעד הראשון** - עד עכשיו טיול מרובה-יעדים המשיך להציג "הטיול בניו יורק התחיל" גם אחרי שהמשתמש עבר לבנגקוק, בזמן שהכותרת והרמז (§5.8) כבר הראו את היעד הנוכחי. שלוש השורות בכרטיס נועדו למסור שלושה דברים שונים - כותרת=איפה אנחנו, רמז=מיקום ברצף, שורת מצב=מה הלאה - וזו הייתה היחידה שנשארה בעבר
- **`multiLegDuringStatusLine()` חדשה** - 4 מצבים, לפי זהות הרגל הפעילה (`idx === currentLegs.length - 1`) ולא לפי תאריך: יש רגל הבאה ואין מעבר היום → `✈️ ב-13.9 טסים לבנגקוק`; המעבר הוא היום → `✈️ היום טסים לבנגקוק`; ברגל האחרונה עם ימים שנותרו → `✈️ נשארו 4 ימים`; ברגל האחרונה ביום האחרון → `✈️ היום האחרון`. פיצול לפי זהות רגל ולא תאריך נבחר כי שתי השאלות ("יש מעבר הבא" מול "זו הרגל האחרונה") הן אותה שאלה בשני ניסוחים - לא יכולות להתנגש גם ברגליים לא-רציפות, מתועד בהערת קוד
- **טיול רגל אחת - אפס שינוי, מבחינה מבנית לא רק ויזואלית**: `multiLegDuringStatusLine()` אפילו לא נקראת כש-`currentLegs.length <= 1` - הענף הקיים (`הטיול ב-X התחיל`) נשאר המסלול היחיד
- **`reserve` לא נוגע כלל** - הענף הקיים ("הפעילות בעיצומה", בלי שם יעד) בודק `tripType === 'reserve'` לפני שהוא בכלל שואל אם יש כמה רגליים
- **פועל תלוי-סוג טיול**: `abroad` → "טסים"/"Flying", `local`/`camping` → "נוסעים"/"Heading" - "טסים" לא מתאים לטיול גליל→נגב
- **אזורי זמן, לפי המסמך**: "היום"/תאריך המעבר נקראים בשעון הרגל היוצאת (`activeLeg.timezone`) דרך `window.getLegForDate`/`dateKeyInZone` הקיימות - לא שעון המכשיר, לא שעון הרגל האחרונה (בדיוק הבאג שתוקן ב-v4.23.1). "נשארו X ימים" נספר עד `legs[last].endAt` בשעון הרגל האחרונה, בימי-לוח (`dateKeyInZone` diff) לא שעות גולמיות - כמו `tripDayDates`/רצועת התחזית
- תאריך המעבר מוצג עם `Intl.DateTimeFormat` (`he-IL`/`en-GB`, `day/month` בלבד, עם `timeZone` מפורש כי הקלט הוא ISO instant גולמי) - אותה תבנית תצוגה כמו טאבי הימים
- נבדק בפועל: טיול רגל אחת במהלך - טקסט זהה ל-byte למקור (הקריטי); מרובה-רגליים באמצע רגל 1 מתוך 3 - "ב-X טסים ל-Y"; יום המעבר - "היום טסים ל-Y"; ברגל האחרונה עם ימים שנותרו - "נשארו X ימים"; ביום האחרון - "היום האחרון"; לפני/אחרי טיול מרובה-רגליים - אפס שינוי; עברית ואנגלית כולל החלפת הפועל לפי סוג טיול; צילום מסך 375px
- `node scripts/i18n-audit.js index.html` - 0 ממצאים

## v4.24.0 — כותרת דף הבית ביעדים מרובים (§5.8, index.html בלבד)

### ✨ תכונה חדשה
- **הכותרת עונה על שאלה שונה בכל שלב**: לפני הטיול - "לאן נוסעים" (מסלול חצים); במהלכו - "איפה אנחנו עכשיו" (היעד הפעיל + דגלו + רמז "יעד N מתוך M"); אחריו - שוב המסלול המלא ("לאן נסענו", לא רק היעד האחרון)
- **דגל רק ביעד יחיד** - במסלול-חצים (2+ יעדים, לפני/אחרי) אין דגל, רק שמות ומחברים; חוזר ברגע שהכותרת מציגה יעד אחד (במהלך הטיול, או תמיד בטיול חד-יעד)
- **טיול רגל אחת - אפס שינוי, מבחינה מבנית לא רק ויזואלית**: הענף הקיים ב-`applyTripPresentation()` לא נגע כלל; הלוגיקה החדשה (`refreshDestinationTitle()`) אפילו לא נקראת כש-`currentLegs.length <= 1`
- **הרמז ("יעד 2 מתוך 3") ב-markup חדש, מוסתר לגמרי (`display:none`) כשאין אותו** - לא רק טקסט ריק, כדי שלא יתפוס גובה בטיול חד-יעד או לפני/אחרי הטיול
- **מדידה בזמן ריצה במקום סף קבוע**: מסלול מלא → אם לא נכנס בשורה אחת, "ראשון ← … ← אחרון" → אם גם זה לא נכנס, "N יעדים". נבדק בפועל שגם השלב האמצעי מגיע (לא רק הקצוות) - ראשון קצר + אחרון קצר + אמצעי ארוך במיוחד. ellipsis-פר-שם (שנשקל ונדחה קודם) היה חותך כל שם בנפרד לבלתי-קריא גם באורך טיפוסי, ובעיקר מאבד את היעד האחרון - בדיוק מה שהקיצור נועד לשמר
- **הגנות על מדידה שבירה, לפי בקשה מפורשת**: לא סומכת על `scrollWidth`/`clientWidth` כשהפונטים לא נטענו (`document.fonts.status !== 'loaded'`) או כשהאלמנט ברוחב 0 - נופלת ישר ל-"N יעדים" הבטוח, עם ריצה חוזרת חד-פעמית דרך `document.fonts.ready`; רצה מחדש ב-resize (עם debounce) וב-שינוי שפה (כבר קורה כחלק מ-`applyTripPresentation()`); רצה מחדש בכל מעבר שלב אמיתי (לפני→במהלך וכו') דרך ה-guard הקיים כבר ב-`applyCardsForPhase()`, לא polling נפרד
- `lastCardsPhase` **לא** שימש למקור האמת על השלב בתוך `refreshDestinationTitle()` - נמצא בבדיקה ש-`applyTripPresentation()` (בתוך `hydrateTripData()`) רץ **לפני** `updateClockAndPhases()` באותו מעבר הידרציה, אז `lastCardsPhase` היה עדיין משקף את השלב הקודם (או `null` בטעינה ראשונה). השלב מחושב מקומית מ-`flightOut`/`tripEnd` ישירות, כמו `updateClockAndPhases()` עצמה
- נבדק בפועל: טיול רגל אחת בשלושת השלבים - כותרת זהה בכל שלב (`getBoundingClientRect`/טקסט מדויק, לא רק "נראה בסדר"); 2 יעדים לפני - חצים בלי דגל בלי רמז; 3 יעדים טיפוסיים - נכנס במלואו; 3 יעדים ארוכים - נופל ל-"3 יעדים" (גם "ראשון…אחרון" לא נכנס, אומת במדידה ישירה); מקרה מכוון שבו רק "ראשון…אחרון" נכנס (קצוות קצרים, אמצעי ארוך) - אומת שהשלב האמצעי בקסקדה נבחר בפועל; במהלך - יעד פעיל+דגל+רמז נכון; אחרי - מסלול מלא; עברית ואנגלית (כיוון חץ, טקסט רמז/ספירה); resize מפעיל מדידה מחדש (אומת עם קונטיינר מוצר בכוח); מעבר שלב מדומה (`applyCardsForPhase`) מרענן את הכותרת; `document.fonts.status='loading'` מדומה - נופל בבטחה ל-"N יעדים" בלי לנסות למדוד; צילומי מסך 375px בשלושת השלבים
- `node scripts/i18n-audit.js index.html` - 0 ממצאים (מחרוזות דינמיות עם `tr()` inline, אותו דפוס כמו "N פעילויות מתוכננות להיום" הקיים)

## v4.23.2 — תיקון: טאב "היום" לא נגלל לתצוגה ברצועת הימים (itinerary.html)

### 🐛 תיקון
- **הטאב של "היום" סומן נכון (מסגרת מודגשת), אבל הרצועה לא נגללה אליו** - בטיול ארוך, מי שנוחת על העמוד רואה את יום 1 ולא את היום עצמו, כי הרצועה מתחילה תמיד ללא גלילה
- **תוקן**: אחרי `renderDayTabs()`, קריאה אחת ל-`.day-tab.today`'s `scrollIntoView({block:'nearest', inline:'center'})` - רק כשהטאב הזה בכלל קיים (כלומר היום בטווח הטיול; לפני/אחרי הטיול הרצועה נשארת בהתחלה, לא מרכזת את יום 1)
- **רק בטעינה הראשונית**, לא בכל render - דגל מודול חדש (`dayTabsScrolledOnce`) שמונע קפיצה גם בבחירת טאב ידנית וגם בכל עדכון realtime שמגיע מ-`listen()`'s `onValue` (שמפעיל `applyLanguage()`←`renderDayTabs()` בכל שינוי בטיול, לא רק בטעינה) - נבדק בהמשך שהוא לא נדרס בטעות
- נבדק בפועל: טיול ארוך (21 ימים) עם "היום" באמצע - הרצועה נגללת כך שהטאב שלו גלוי (אומת עם `getBoundingClientRect`, לא רק ויזואלית); טיול עתידי (היום לא בטווח) - `scrollLeft` נשאר `0`, אין קפיצה; בחירת טאב ידנית אחרי גלילה ידנית של המשתמש - הרצועה **לא** קופצת בחזרה (אומת: `scrollLeft` זהה לפני ואחרי הקליק); עברית (RTL, `scrollLeft` שלילי - תואם לממצא הקיים מ-`.quick-picks`) ואנגלית (LTR, `scrollLeft` חיובי) - שתיהן עובדות נכון עם אותה קריאת `scrollIntoView` בלי טיפול מיוחד לכיוון; מסך 375px
- `node scripts/i18n-audit.js itinerary.html` - 0 ממצאים (לא נוספו מחרוזות משתמש)

## v4.23.1 — תיקון: "היום" חושב לפי אזור הזמן של הרגל האחרונה, לא הפעילה (שלב 5, commit ג)

### 🐛 תיקון
- **קטגוריה שלישית של באג אזור-זמן (§8ব סעיף 7), נדחתה עד שאפשר היה לבדוק על טיול מרובה-רגליים אמיתי - עכשיו אפשר.** ארבעה מקומות חישבו "מה התאריך/השעה עכשיו" לפי `getLastLeg(trip).timezone` - נכון רק כשהרגל הפעילה היא גם האחרונה. בטיול 3 רגליים, כשהמשתמש ברגל האמצעית, "היום" חושב לפי אזור הזמן של יעד עתידי שעוד לא הגיעו אליו
- **`window.getCurrentLegTimezone(trip)` חדש ב-`legs.js`** - `getLegForDate(trip, new Date())?.timezone`, נופל ל-`getLastLeg(trip).timezone` ואז `null` (הרחבת שרשרת ה-fallback הקיימת ב-§4 "רגל בלי timezone → timezone של הטיול → שעון המכשיר", לא כלל חדש). פונקציה, לא משתנה גלובלי - הרגל הפעילה משתנה תוך כדי הטיול, מחושבת מחדש בכל קריאה
- **תוקנו 3 מתוך 4 המקומות**: `index.html`'s `todaysPlanContext()`, `itinerary.html`'s `renderDayTabs()` ו-`computeLiveActivityId()`. `index.html` אין לו אובייקט `trip` (הכל משוטח ב-`hydrateTripData()`) - נקרא עם עטיפה `{legs: currentLegs}`, לא טיול אמיתי; `currentLegs` כבר `getLegs()`'s output כך שזה תקין לרגל אחת ומרובה כאחד
- **`fetchForecast()`'s `todayKey` (index.html) - במפורש לא תוקן.** נבדק ונדחה: הקריאה ל-Open-Meteo עדיין שולחת קואורדינטה יחידה (הרגל הראשונה, `timezone=auto` - מגבלה מתועדת בשלב 8), כך שכל `d.dateKey` שחוזר כבר מעוגן לרגל הראשונה. שינוי `todayKey` לרגל הפעילה בלי לתקן גם את הקריאה עצמה היה שובר את ההתאמה ביניהם - רגרסיה בתחפושת של תיקון. נשאר `tripTimezone`, מתועד ב-`docs/schema-legs.md` §8 (שלב 8) כחלק ממה שצריך להיסגר שם
- נבדק בפועל, מול הזמן האמיתי כרגע (לא רק תאריכים מזויפים): טיול 3 רגליים עם פערי אזורי-זמן גדולים (ניו יורק/בנגקוק/הונולולו) - ב-2026-09-08 בפועל "היום" בבנגקוק (הרגל הפעילה) ו"היום" בהונולולו (הרגל האחרונה, המקור הישן) **נבדלו בפועל ביום שלם** (`2026-09-08` מול `2026-09-07`) - `todaysPlanContext()` ו-`renderDayTabs()` בחרו נכון לפי בנגקוק; לפני הטיול ואחריו - הערך זהה **בית-לבית** למה ש-`getLastLeg` כבר החזירה היום, לא רק "לא קורס"; טיול רגל אחת - זהה ל-`getLastLeg` תמיד, אפס שינוי; `computeLiveActivityId` סימנה נכון את הפעילות האחרונה שכבר עברה, לפי שעון בנגקוק בזמן אמת
- `node scripts/i18n-audit.js index.html itinerary.html` - 0 ממצאים (לא נוספו מחרוזות משתמש)

## v4.23.0 — `countryCode` עובר לשדה per-leg (שלב 11ব.3, תיקון עוקב)

### ✨ שינוי סכמה
- **`legs[i].countryCode` חדש** - `buildLegs` (`legBuilder.js`) שומרת אותו לכל רגל מ-geocoding, בדיוק כמו `timezone`. `deriveTripFields` גוזרת את `trip.countryCode` מ-`legs[0].countryCode` במקום מ-`legLocations[0]` הזמני
- **פותר את הממצא שדווח בבדיקות 11ব.2**: שינוי סדר שהזיז רגל לא-`dirty` (לא נגעו בשם שלה) למיקום 0 לא ריענן את `trip.countryCode` - עכשיו כל רגל נושאת את הדגל שלה, אז אחרי שינוי סדר `deriveTripFields` קוראת מהרגל הנכונה. הקוד המיוחד באדמין (`i === 0 ? countryCode : ""`) הוסר - מיותר
- **fallback הכרחי לתאימות אחורה**: רגליים שנוצרו לפני v4.23.0 לא נושאות את השדה - `deriveTripFields` הייתה מחזירה '' עבורן. בלי נפילה חזרה, השמירה הראשונה של רגל ישנה כזו (גם בלי לגעת בשם שלה) הייתה מאפסת דגל שכבר היה נכון. נוסף: `tripFields.countryCode = tripFields.countryCode || countryCode` (הערך הגלובלי הקיים) מיד אחרי `deriveTripFields` בפאנל האדמין
- נבדק בפועל: טיול חדש דרך הוויזרד עם 2 רגליים במדינות שונות (רומא/בנגקוק) - כל רגל עם `countryCode` משלה (`IT`/`TH`), נכון; **שינוי סדר בפאנל - בדיוק הבדיקה שהתחילה את זה** - הדגל ברמת הטיול עבר מ-`IT` ל-`TH` בהתאמה לרגל שהפכה ראשונה; טיול "ישן" עם רגליים בלי `countryCode` בכלל - שמירה בלי לגעת בכלום השאירה `trip.countryCode` בדיוק כפי שהיה (fallback עבד), הרגליים עצמן נשארו בלי השדה (יקבלו אותו רק כששמן ישתנה בפעם הבאה - צפוי, לא תוקן רטרואקטיבית); רגל אחת - אפס שינוי, `legs` לא נכתב, זהה להיום
- `node scripts/i18n-audit.js index.html wizard.html` - 0 ממצאים (לא נוספו מחרוזות משתמש)

## v4.22.0 — פאנל אדמין: שמירה בפועל של רגליים (שלב 11ব.2)

### ✨ תכונה חדשה
- **`saveFlightBtn` כותב עכשיו רגליים בפועל** - לא כפתור חדש, אותו כפתור, תנאי ההסתעפות עבר מ-`currentLegs.length > 1` (מצב שמור) ל-`adminLegRowCount > 1` (מצב העורך החי) - זה מה שבאמת קובע מה יישמר. `tripType`/`checkinLink`/`showAccent` ממשיכים להישמר תמיד
- **geocoding רק לרגליים ששמן השתנה או שנוספו** (`dirty`, מ-11ব.1) - רגל שלא נגעו בה משתמשת ב-`lat`/`lon`/`timezone` המקוריים שלה (מ-`originalLeg`, אובייקט הרגל המלא שהשורה נזרעה ממנו - לא רק שם), אפס קריאת רשת
- **`buildLegs` ואז `deriveTripFields`** (שתיהן מ-`legBuilder.js`) - **לא שכפול inline**. זה בדיוק הסכנה שסומנה לפני הכתיבה - `deriveTripFields` כבר תיקנה פעם אחת באג ש-inline-קוד יצר (v4.20.2), וזה בדיוק למה היא קיימת כפונקציה משותפת אחת
- **`vibes` פר-רגל נשמרים** - שינוי שם רגל לא מוחק את ה-`vibes` הקיימים שלה (נשמרים מ-`originalLeg.vibes`, בלי קשר ל-`dirty`); רגל חדשה מקבלת `[]` (אין UI לעריכת vibes בפאנל עדיין - לא ניחוש)
- **ולידציית סדר-כרונולוגי לפני שמירה** - כל רגל נבדקת ש-`endAt` שלה מאוחר מ-`startAt` שלה (מספיק כדי להבטיח שכל שרשרת ה-boundaries עולה מונוטונית, כי הרציפות בין רגליים כבר מובטחת מבנית). כשל **חוסם את השמירה** ומציג הודעה שמזהה איזה יעד — לא "שגיאה" כללית. תוקן אחרי שהוכר שזה תיקון לחור אמיתי, לא רק הגנה - בניגוד לוויזרד (שם הגבולות תמיד מחושבים אוטומטית), כאן המנהל מקליד תאריכי מעבר ידנית, אז משך שלילי דורש טעות הקלדה אחת
- **מחיקה חזרה לרגל אחת כותבת `legs: null` במפורש** כש-`currentLegs.length > 1` (היה מרובה) - **תיקון לבאג שקט אמיתי**, לא רק ניקוי: `update()` לא מוחק מפתחות שלא מצוינים, אז בלי זה `legs` הישן היה נשאר ב-RTDB ו-`getLegs()` ממשיכה להעדיף אותו על פני השדות הסקלריים החדשים (§6) - העריכה "מצליחה" ולא קורית בפועל
- נבדק בפועל (console fixtures + לכידת `db.ref().update`, ספירת קריאות `fetch` לגיאוקוד): רגל אחת + שינוי שם - `legs` לא נכתב, זהה להיום; הוספת רגל שנייה - `legs` נכתב עם 2, רגל 0 חדשה נגזרת (`destination`/`lat`/`lon`), אפס geocoding לרגל שלא נגעו בה, vibes שלה נשמרים; מחיקת הרגל האחרונה מתוך 3 - `timezone`/`endAt` מתעדכנים לרגל שהפכה אחרונה; שינוי סדר - `destination`/`timezone` מתחלפים; שינוי שם רגל בלבד - geocoding רץ בדיוק פעם אחת (נספר בפועל), console.warn נבדק בכשל geocoding; שינוי תאריך מעבר בלבד - אפס קריאות geocoding; תאריך מעבר לא תקין (לפני תחילת הטיול) - השמירה נחסמה, הודעה מזהה את היעד הבעייתי; רגל אחת → מרובה → אחת - `legs: null` נכתב בפועל, ואומת ש-`getLegs()` על האובייקט שאחרי הכתיבה מחזירה נכון רגל סינתטית מהשדות הסקלריים; רציפות (`legs[n].endAt === legs[n+1].startAt`) מאומתת בכל אחת מהבדיקות למעלה; startAt זהה בית-לבית לפני/אחרי בטיול מרובה-רגליים (השדה חסום, לא נערך) - לא רק "נראה נכון"
- **ממצא שנתפס בבדיקה, לא תוקן**: שינוי **סדר** שמזיז רגל לא-משתנה-שם למיקום 0 לא מרענן את `countryCode` ברמת הטיול (עדיין הערך הישן, מהרגל שהייתה במיקום 0 לפני השינוי) - כלל ה-geocoding-רק-לרגליים-ששונו לא כולל מנגנון "רענן countryCode כשמיקום 0 מתחלף". משפיע רק על תצוגת דגל-מדינה - lat/lon/timezone/תאריכים כולם נכונים. לא תוקן, מחוץ להיקף שהוגדר
- `node scripts/i18n-audit.js index.html` - 0 ממצאים (לא נוספו מחרוזות משתמש חדשות - הודעת השגיאה נבנית עם `tr()` inline, אותו דפוס כמו שאר ה-`customAlert`-ים בקובץ)

## v4.21.0 — פאנל אדמין: עורך רגליים בזיכרון (שלב 11ב.1)

### ✨ תכונה חדשה
- **רשימת יעדים ניתנת לעריכה בפאנל האדמין** - הוספה, הסרה, שינוי סדר, שינוי שם - **בזיכרון בלבד, בלי geocoding ובלי שמירה** (commit הבא, 11ব.2, יסגור את זה). `#adminDestInput` הופך לשורה הראשונה של אותה רשימה - בדיוק כמו `#tripDestination` בוויזרד (שלב 10, commit א) - לא שדה נפרד. אותו איחוד קוד: `saveFlightBtn`'s handler ממשיך לקרוא מאותו אלמנט בדיוק, בלי שינוי
- **זריעה מ-`currentLegs`** (כבר `window.getLegs()`'s output, מתעדכן ב-`hydrateTripData()`) - לא קריאה חוזרת ל-`getLegs()` עם אובייקט טיול מאולתר (אין ל-`index.html` כזה, §8ব.1). זה מה שהופך טיפול ברגל-אחת ומרובה-רגליים לאותו קוד בדיוק - `currentLegs.length` הוא 1 או N, בלי `if` נפרד
- **הרשימה מתקפלת לשדה בודד ברגל אחת** - בדיוק אותו היגיון שכבר הוכרע בוויזרד (§5.6): בלי תג סידורי, בלי חץ, בלי כפתור הסרה, כשנשארת רגל אחת (בין אם זה מצב הטיול המקורי או שהמנהל מחק חזרה לאחת באותה עריכה)
- **תאריכי מעבר בין רגליים** - שורה בין-רגלית לכל גבול, בדיוק כמו בוויזרד. **בפתיחת הפאנל** נזרעים מהגבול האמיתי השמור (`legs[i].endAt`, מפורש בשעון הרגל היוצאת - `toZonedInputValue`/`window.dateKeyInZone`) - לא ברירת מחדל. **בכל הוספה/הסרה** מחושבים מחדש לברירת מחדל בחלוקה שווה (`window.splitEqualBoundaries`, מ-`legBuilder.js` - לא שוכפל) על כל התאריכים הנראים, **כולל דריסת גבולות אמיתיים שכבר היו שמורים** - אותו כלל בדיוק שכבר אושר בוויזרד ("תאריך מעבר שנערך ידנית לא שורד הוספה/הסרה מאוחרת יותר"), מוחל עכשיו גם על גבולות אמיתיים, לא רק טיוטות. שום שמירה אוטומטית לא קורית - המנהל רואה את הניחוש ומתקן לפני ש-11ব.2 בכלל קיים
- **דגל "השתנה" (`dirty`) לכל רגל** - לא לגיאוקוד, נועד ל-11ব.2: מסמן אם שם הרגל השתנה מאז שנטען (או שהיא חדשה), כדי שהשמירה העתידית תבצע geocoding רק לרגליים ששונו/נוספו - לא לכל רגל בכל שמירה (קריאת רשת מיותרת, סיכון שרירותי לקואורדינטה שונה במקצת לאותה עיר)
- **שדות התאריך הכוללים (`adminFlightInput`/`adminEndInput`) נשארים חסומים** לטיול מרובה-רגליים - לא בהיקף כאן; טקסט ה-hint צומצם בהתאם (רק תאריכים, לא יעדים)
- **`legBuilder.js` נטען עכשיו גם ב-`index.html`** (אחרי `datetime.js`, לפני `auth-gate.js`)
- נבדק בפועל: טיול רגל אחת - נראה בדיוק כמו היום, הוספה חושפת שרשרת (מספור, חץ, הסרה) + שורת מעבר עם ברירת מחדל מחושבת נכון; טיול 3 רגליים אמיתי (רומא/בנגקוק/טוקיו, timezone שונה) - נזרע עם שלושת השמות והתאריכים האמיתיים (אומת נומרית: `06:00 10/10/2026`=`04:00Z` בשעון רומא/CEST, `14:00 12/10/2026`=`07:00Z` בשעון בנגקוק/UTC+7); הסרת רגל אמצעית - שינוי סדר נכון + מעברים מחושבים מחדש; שינוי סדר (הזזה למעלה); קיפול חזרה לרגל אחת אחרי הוספה+הסרה; **הגגה המתועדת מאומתת בפועל** - הוספת רגל + שמירה בטיול (עדיין) רגל-אחת כותבת רק את `destination` המעודכן של רגל 0, מתעלמת בשקט מהרגל השנייה שלא נשמרה, בלי שגיאה; עברית/אנגלית (כולל תיקון: תווית "מעבר"/"Transition" מתעדכנת מחדש בכל פתיחת פאנל, לא רק בבנייה הראשונה); מצב כהה; מסך 375px
- `node scripts/i18n-audit.js index.html` - 0 ממצאים

## v4.20.3 — חילוץ `legBuilder.js` מ-wizard.html (שלב 11, commit ב - רה-פקטור)

### ♻️ רה-פקטור (אפס שינוי התנהגות)
- **קובץ חדש `legBuilder.js`** - פונקציות טהורות לבניית `legs[]` מקלט גולמי (בלי DOM, בלי מצב גלובלי): `splitEqualBoundaries` (חלוקה לינארית טהורה), `buildLegs` (בניית `boundaries[]` והנגזרת ל-`legs[]`, כולל אכיפת הרציפות המבנית וכלל שעון-הרגל-היוצאת), `deriveTripFields` (גזירת `destination`/`lat`/`lon`/`countryCode`/`timezone`/`startAt`/`endAt` מ-`legs[0]`/`legs[legs.length-1]`). נטען כרגע רק ב-`wizard.html` - לא ב-4 העמודים שלעולם לא בונים `legs` (packing/places/shopping/itinerary), ולא עדיין ב-`index.html` (יתווסף ב-11ב/ג כשהפאנל בונה רגליים בפועל)
- **לא ב-`legs.js`, במכוון** - `legs.js` הוא קריאה/נרמול בלבד ("בהינתן טיול שכבר יש לו legs, תחזיר תצוגה"); הפונקציות החדשות הן בנייה ("בהינתן קלט גולמי, תבנה legs חדש"). אותו נימוק בדיוק שהוציא את `datetime.js` מ-`legs.js` (ר' הערת הכותרת של `datetime.js`) - ציר שונה (זמן-ביום מול מבנה-טיול), אותה מסקנה: לא לערבב שתי אחריות בקובץ אחד
- **`wizard.html`'s `generateTripAndSave()` ו-`recomputeTransitionDefaults()` עוברות לקרוא ל-`window.buildLegs`/`window.deriveTripFields`/`window.splitEqualBoundaries`** - הלוגיקה המקומית נמחקה פיזית, לא הוקפאה. קריאות DOM (ערכי טופס, כתיבה ל-`<input>`) נשארות בוויזרד; החישוב עצמו זז
- **`localInputToUtcInZone` הועברה ל-`datetime.js`** - הייתה משוכפלת בית-לבית בין `wizard.html` ו-`index.html` (אומת ב-diff לפני ההעברה - רק סגנון מרכאות/הזחה שונה, שתי הפעמים העתק זהה בהתנהגות). שני העותקים נמחקו פיזית; `index.html`'s קריאה נשארה `localInputToUtcInZone(...)` ללא שינוי (script קלאסי - הזיהוי הגלובלי עובד אוטומטית מ-`datetime.js` שכבר נטען שם)
- **ולידציית רציפות (`legsAreContiguous`) - נדחתה ל-11ב במכוון**, לא חולצה עכשיו. אין קוד קיים לה היום - הרציפות מובטחת מבנית ב-`buildLegs`, לא נבדקת בנפרד, ובוויזרד אין דרך למשתמש לשבור אותה. תוסיף ערך רק כשהפאנל (11ב) בונה רגליים מעריכת משתמש בפועל
- נבדק בפועל: **בדיקה 2 (ההוכחה)** - טיול רומא/בנגקוק/טוקיו, אותו קלט בדיוק לפני ואחרי החילוץ - payload זהה בית-לבית (חוץ מ-`id`/`createdAt` האקראיים): `destination`/`lat`/`lon`/`countryCode`/`timezone`/`startAt`/`endAt` ברמת הטיול, ושלוש הרגליים (שם/קואורדינטות/timezone/`startAt`/`endAt`) זהים; טיול ברלין יעד יחיד - payload זהה בית-לבית להיום, `legs` לא נכתב; `splitEqualBoundaries` בקונסול - טיול 3 ימים/4 רגליים מייצר 3 גבולות שונים (לא באורך אפס, לא כפולים); רציפות `legs[n].endAt === legs[n+1].startAt` מאומתת בבדיקה 2. אין שגיאות קונסולה חדשות בטעינת `wizard.html`/`index.html` (רק רעש `PERMISSION_DENIED` הרגיל של הסביבה)
- `node scripts/i18n-audit.js wizard.html index.html` - 0 ממצאים (לא נוספו מחרוזות משתמש)

## v4.20.2 — תיקון: trip.timezone נכתב מהרגל הראשונה במקום האחרונה (שלב 11, commit א.5)

### 🐛 תיקון
- **כל טיול מרובה-רגליים שנוצר מאז v4.20.0 (2026-09-07) נשמר עם `timezone` שגוי.** `generateTripAndSave()` ([wizard.html](wizard.html)) כתבה `timezone: legs[0].timezone` - הרגל **הראשונה** - בעוד `destination`/`lat`/`lon` נכונים מהראשונה (§2.1) ו-`endAt` נכון מהאחרונה (§4). `trip.timezone` צריך לשקף את **אותה רגל** כמו `endAt` (שתיהן "הרגל האחרונה", אותו נימוק) - `index.html`'s `hydrateTripData()` כבר קורא לפי ההנחה הזו (`tripTimezone = lastLeg.timezone`), אז כל טיול מרובה-רגליים היה עם `timezone` שלא תואם לאזור הזמן שבו `endAt` בפועל מחושב
- נבדק את שאר השדות ברמת הטיול (`destination`/`lat`/`lon`/`countryCode`/`startAt` מהרגל הראשונה, `endAt` מהאחרונה) - כולם נכונים, `timezone` היה החריג היחיד
- **תוקן**: `timezone: legs[legs.length - 1].timezone`
- `scripts/find-desynced-legs.js` (נוסף ב-v4.20.1) כבר בודק בדיוק את זה - `trip.timezone !== legs[last].timezone` - לא נדרש שינוי בסקריפט
- נבדק בפועל (console fixture, לכידת ה-payload לפני הכתיבה): טיול 3 רגליים (רומא/בנגקוק/טוקיו) - `trip.timezone` כעת `"Asia/Tokyo"`, תואם ל-`legs[2].timezone` ול-`trip.endAt`; טיול רגל אחת (ברלין) - רגרסיה, ללא שינוי

## v4.20.1 — תיקון: פאנל האדמין דרס legs בשינוי יעד/תאריכים (שלב 11, commit דחוף)

### 🐛 תיקון
- **באג מאושר בפרודקשן**: שינוי יעד בטיול מרובה-רגליים מהפאנל כתב `destination`/`lat`/`lon`/`countryCode`/`timezone` ברמת הטיול בלי לגעת ב-`legs` בכלל - `trip.destination` ו-`legs[0].name` נותרו סותרים (`getLegs()` ממשיכה להחזיר `legs`, אז התוכנית היומית ומזג האוויר ממשיכים לפי הרגל הישנה בעוד הכותרת מציגה את החדשה). **וקטור שני, פחות מיידי**: אותה שמירה כתבה מחדש `startAt`/`endAt` ברמת הטיול **בכל שמירה**, גם בלי לגעת ביעד - סותר את `legs[0].startAt`/`legs[last].endAt` (§4) בלי קשר לשינוי יעד. עלה בחקירת שלב 11 (עריכת רגליים בפאנל האדמין)
- **תוקן בחסימה, לא בסנכרון** - הוחלט במפורש נגד סנכרון חלקי: עדכון `legs[0]` רק בשם/קואורדינטות/timezone בלי לגעת ב-`startAt`/`endAt` היה משאיר את הרגל **פנימית לא עקבית** (שדה `timezone` מצביע על יעד אחד, בעוד האינסטנט ב-`startAt`/`endAt` חושב עבור אחר) - קורפציה חדשה, גרועה מהבאג הנוכחי. `getLegs()`/§6 כבר קובעים ש-`legs` הוא מקור האמת בטיול מרובה-רגליים - הפאנל הישן לא בנוי לערוך רגל בודדת מתוך רשימה, אז חוסמים עד שעריכת רגליים אמיתית (§5.7) קיימת
- שדה היעד, מועד ההתחלה ומועד הסיום בפאנל **מנוטרלים (לא מוסתרים) עם הסבר** כש-`currentLegs.length > 1` - `disabled` על שלושת השדות + שורת `admin-hint` ("עריכת יעדים ותאריכים בטיול מרובה יעדים תתאפשר בקרוב"). שאר שדות הפאנל (סוג טיול, צ'ק-אין, מבטא צבעוני, מודולים) נשארים פעילים ללא שינוי
- **החסימה גם בצד הכתיבה, לא רק ב-UI**: `saveFlightBtn`'s handler מדלג לגמרי על geocoding, ולידציית תאריך/יעד, ועדכון המשתנים הגלובליים (`destinationName`/`latCoord`/`lonCoord`/`countryCode`/`tripTimezone`/`flightOut`/`tripEnd`) כש-`multiLeg` אמת - לא רק על נטרול השדה. שדה `disabled` בלבד לא היה מספיק (קריאה ל-`.value` דרך JS עדיין עובדת על שדה מנוטרל) - האובייקט שנכתב ל-RTDB כולל רק `tripType`/`checkinLink`/`showAccent` בטיול מרובה-רגליים, לא `destination`/`lat`/`lon`/`countryCode`/`timezone`/`startAt`/`endAt`
- **`scripts/find-desynced-legs.js`** (כלי חדש, לא נטען לדפדפן - בלי version bump) - סורק את `/trips` ב-RTDB דרך ה-REST API, ומזהה טיולים מרובי-רגליים שבהם `trip.destination`/`lat`/`lon` לא תואמים ל-`legs[0]`, או `trip.timezone`/`endAt` לא תואמים ל-`legs[אחרון]` (ומדווח גם, כמידע משני, אי-התאמת `startAt` ל-`legs[0].startAt` - §4 מגדיר את אלה כשני שדות שונים סמנטית, אבל אין כיום מנגנון לגיטימי שגורם להם לסטות חוץ מהבאג הזה)
- נבדק בפועל (console fixtures, `db.ref().update` מוחלף זמנית בלכידה): טיול רגל אחת - שלושת השדות פעילים, השמירה כותבת `destination`/`lat`/`lon`/`countryCode`/`timezone`/`startAt`/`endAt` בדיוק כמו לפני התיקון (רגרסיה); טיול 3 רגליים (רומא/בנגקוק/טוקיו) - שלושת השדות מנוטרלים, ההסבר מוצג, השמירה כותבת אך ורק `{tripType, checkinLink, showAccent}` - `currentLegs` ומשתני רמת-הטיול (`destinationName`/`latCoord`/`lonCoord`/`tripTimezone`/`flightOut`/`tripEnd`) נשארים בדיוק כפי שהיו; עברית/אנגלית; מסך 375px, מצב בהיר
- `node scripts/i18n-audit.js index.html` - 0 ממצאים

## v4.20.0 — ויזרד: תאריכי מעבר, geocoding מקבילי, שמירת legs (שלב 10, commit ב מתוך 2)

### ✨ תכונה חדשה
- **תאריכי מעבר בין רגליים** - N-1 שדות, לא 2N: כל שדה הוא בו-זמנית `legs[n].endAt` וגם `legs[n+1].startAt`, אותו ערך בדיוק. נערכים לאורך כל הוויזרד כמחרוזת נאיבית (`datetime-local`/`date`, בדיוק כמו `#tripStart`/`#tripEnd` היום) - אין timezone פר-רגל ידוע לפני geocoding, אז אין מה לזון בזמן עריכה
- **חלוקת ברירת מחדל שווה** של הטווח הכולל (`#tripStart`→`#tripEnd`) בין הרגליים, ניתנת לתיקון ידני. מחושבת מחדש רק בהוספה/הסרה של רגל (`recomputeTransitionDefaults()`) - לא על כל הקלדה, ולא בהזזה (סדר בלבד, לא משנה טווח). חלוקה לינארית של טווח חיובי לעולם לא מייצרת גבול כפול או באורך אפס - רק קצר (טיול 3 ימים/4 רגליים ⇐ רגליים של כ-18 שעות) - מקובל, בהתאם לעיקרון "ניתנות לתיקון ידני"; לא נוספה ולידציית אורך-מינימלי
- **geocoding מקבילי** לכל הרגליים (`Promise.all`, אותה `geocodeDestination()` הקיימת ללא שינוי). כשל ברגל בודדת לא עוצר את השמירה - אותה רגל נשמרת בלי `lat`/`lon`/`timezone` (null) + `console.warn` עם שם הרגל, שאר הרגליים והטיול נשמרים תקין, בלי הודעת UI - אותו דפוס הידרדרות שקיים היום כשגם המסלול הבודד נכשל ב-geocoding
- **`generateTripAndSave()` בנוי מחדש לנתיב אחיד**: תמיד בונה `legs[]` פנימית (גם עבור יעד יחיד), ושדות רמת-הטיול (`destination`/`lat`/`lon`/`timezone`/`countryCode`/`startAt`/`endAt`) נגזרים אחידה מ-`legs[0]`/`legs[אחרון]`. `legs` **מצורף ל-payload רק כש-`legs.length > 1`** - טיול יעד יחיד נשמר זהה בדיוק להיום (§2.1/§10, לא רגרסיה)
- **רציפות מובטחת מבנית, לא בבדיקה**: כל גבולות הרגליים (התחלת טיול, כל מעבר, סוף טיול) נבנים פעם אחת למערך `boundaries[]` משותף; `legs[i].startAt`/`legs[i-1].endAt` קוראים את **אותו איבר מערך** פעמיים - לא שני חישובים נפרדים שעלולים לסטות. אומת בפועל: `legs[n].endAt === legs[n+1].startAt` כמחרוזת ISO זהה, לא ערך קרוב
- **תאריך מעבר מתפרש בשעון הרגל היוצאת** (לא הנכנסת) - הכללה של שני כללים שכבר הוכרעו, לא החלטה חדשה: §5.2 ("יום המעבר שייך לרגל היוצאת", מיום ליחידת-זמן), וההגדרה הקיימת ש-`endAt` תמיד בשעון הרגל שלו עצמו (§4, שכבר חל על `trip.endAt`, כאן מוכלל לכל גבול פנימי)
- **`legs[0].startAt = trip.startAt` בדיוק, בלי re-zoning** - הערת קוד מפנה במפורש ל-§4 ("שני דברים סמנטית שונים שקורים להיות זהים היום") כדי שזה לא "יתוקן" בעתיד למשהו שנראה עקבי יותר
- **`vibes`** - הקלט נשאר בחירה יחידה ברמת הטיול בוויזרד (כמו שהוכרע); בשמירה, אותם vibes שנבחרו נכתבים למערך `vibes` של **כל** רגל
- **שלב הסיכום (4) מציג יעדים מרובים** - "יעד"/"יעדים" (יחיד/רבים) עם שרשרת חצים תלוית-כיוון (`←` בעברית RTL, `→` באנגלית LTR)
- נבדק בפועל מול Open-Meteo אמיתי: טיול רומא/בנגקוק/טוקיו (3 רגליים, אזורי זמן אמיתיים כולל DST שונה) - `legs` נכתב, רציפות מלאה, `destination`/`lat`/`lon` מהרגל הראשונה, `endAt`/`timezone` מהאחרונה; טיול ברלין יעד יחיד - **רגרסיה**: `legs` לא נכתב כלל, payload זהה לפני השינוי; כשל geocoding מודמה ברגל 2 - הרגל נשמרת עם שדות null + warning, השאר תקין; חלוקת תאריכים כפונקציה טהורה בקונסול; **הקבלה האמיתית** - הרצת `getLegs()`/`tripDayDates()` (מ-`legs.js`) ישירות מול הטיול שזה עתה נשמר: 3 רגליים נכונות, ימים משויכים נכון, אפס אזהרות פער
- `node scripts/i18n-audit.js wizard.html` - 0 ממצאים

## v4.19.0 — קלט רשימת יעדים בוויזרד (שלב 10, commit א מתוך 3)

### ✨ תכונה חדשה
- **רשימת יעדים דינמית בוויזרד** - הוספה, הסרה, הזזה למעלה, עד 6 יעדים. `#tripDestination` הקיים הופך לשורה הראשונה של אותה רשימה - **לא שדה נפרד** - "מסלול אחד" לאותו נתון, בדיוק הנימוק שכבר תיקן את הכפילות ב-`tripDayDates`/`zonedParts`
- **מאגר שורות קבוע (עד 6), לא DOM שנוצר/נהרס.** הוספה/הסרה/הזזה מזיזות **ערכים** בין שורות קיימות, לא צמתים ב-DOM - כך `#tripDestination` תמיד נשאר אותו רכיב פיזי (כל שאר הקוד בקובץ - `validateStep`, `generateTripAndSave`, `saveWizardDraft`/`restoreWizardDraft`, `resetWizard`, `renderSummary` - ממשיך לקרוא ממנו בלי שום שינוי)
- **תצוגה מקדימה (דגל) עצמאית לכל שורה** - טיימר דיבאונס נפרד per-row, לא משתנה מודול יחיד כמו קודם. הדגל **מחוץ** לשדה (לא overlay כמו הגרסה הישנה) - נבדק בפועל: עם overlay, טקסט יעד ארוך הגיע ל-5px משוליים מהדגל; בגרסה החדשה השדה חותך ב-ellipsis במקום לחפוף
- **חצי סידור-מחדש: חץ יחיד "הזז למעלה", לא זוג חצים מוערמים.** נבדק בפועל בנייד: זוג מוערם ב-32px רוחב יוצא כ-22px גובה לחץ - מתחת לסף מגע (48px, לפי הקונבנציה הקיימת בקובץ). חץ יחיד מלא = 44-48px אמיתי. אפשר להגיע לכל סדר עם כיוון אחד בלבד
- **מחיקה בחזרה ליעד אחד מקפלת את הרשימה** - חוזרת בדיוק למראה שדה בודד (התג הסידורי, החץ וה-✕ נעלמים, "נוסעים ליותר ממקום אחד?" חוזר) - לא נשארת פתוחה עם שורה יתומה
- `saveWizardDraft`/`restoreWizardDraft`/`resetWizard` מעודכנים - `draft.legs` הוא הפורמט הנוכחי, `draft.destination` נשאר רק כ-fallback לטיוטה ישנה מגרסה קודמת
- **בהיקף במפורש**: הקלט בזיכרון בלבד. **לא בהיקף**: תאריכי מעבר, geocoding בשמירה, כתיבת `legs` ל-RTDB, `vibes` - הכל commit ב. ה-payload הנשמר זהה לגמרי להיום; טיול עם יעד אחד נשמר בדיוק כמו קודם
- נבדק בפועל: הוספה עד 6 (הכפתור נעלם), הסרת שורה אמצעית (המספרים הסידוריים מתעדכנים, `+ הוספת יעד` חוזר), הזזה למעלה כולל שורה 1 (החץ שם מנוטרל, הקליק לא עושה כלום), תצוגה מקדימה על 2 שורות בו-זמנית (דגלים נכונים, בלי החלפה), draft (3 יעדים, שמירה, רענון עמוד אמיתי, שחזור אוטומטי), `resetWizard()` (הכל חוזר ל-1, כולל ניקוי שורות מוסתרות), **רגרסיה** (יעד בודד בלי לגעת ברשימה - `legRowCount` נשאר 1, `#tripDestination.value` בדיוק כמו קודם), החלפת שפה (מחרוזות + aria-label מתעדכנים בכל השורות), עברית/אנגלית, צילום נייד 375px עם 4 שורות
- `node scripts/i18n-audit.js wizard.html` - 0 ממצאים

## v4.18.2 — תיקון: fetchForecast פירשה flightOut לפי אזור הזמן הלא נכון (שלב 5, commit ב מתוך 3)

### 🐛 תיקון
- **`tripStartKey` ב-`fetchForecast()` פירשה `flightOut` (=`trip.startAt`) לפי `tripTimezone` (רגל אחרונה) במקום רגל ראשונה** - אותה משפחת באג שתוקנה ב-`tripDayDates()` בשלב 4א. שורה אחת בלבד שונתה - `todayKey` ו-`tripEndKey`, השורות מעליה ומתחתיה, נשארו כפי שהיו (שתיהן תקינות)

### ✨ תכונה חדשה
- **`currentLegs` - מערך רגליים גלובלי חדש ב-index.html**, מוגדר ב-`hydrateTripData()` ליד `tripTimezone` (`window.getLegs(legInput, tripId)` - לא מ-`data` הגולמי, כדי להתאים ל-`primaryLeg`/`lastLeg` שכבר מחושבים מ-`legInput` באותה פונקציה, כולל נפילת התאריכים הישנים ל-`flightOut`/`tripEnd`). לא `primaryLegTimezone` בודד - מערך שלם, כי הקטגוריה הבאה (§8ב סעיף 7, "הרגל הפעילה עכשיו") לא ניתנת לפתרון עם משתנה קבוע יחיד, ואין טעם להוסיף משתנה גלובלי חדש לכל צורך עתידי
- **`tripStartKey` משתמשת ב-`currentLegs[0].timezone`** - לא ב-`getPrimaryLeg(trip)` ישיר כפי שהוצע קודם ב-§8: אומת ש-`index.html` לא מחזיקה אובייקט `trip` בשום מקום (הכל משוטח למשתנים ב-`hydrateTripData` ונזרק) - קריאה ישירה ל-`getPrimaryLeg(trip)` הייתה דורשת בניית אובייקט מזויף, שהיה חסר את ה-`legs` האמיתיים ומחזיר בדיוק את אותו ערך שגוי מחדש
- נבדק בפועל: ברלין (רגל אחת) - הרצועה נטענת עם נתונים אמיתיים מ-Open-Meteo, `tripStartKey` תואם ליום הראשון, אפס שינוי. טיול מזויף רומא/בנגקוק (2 רגליים, timezone שונה) - נוסחה ישנה (`tripTimezone`) נותנת `2026-10-06` (שגוי), `currentLegs[0].timezone` נותן `2026-10-05` (נכון) - אותם ערכים בדיוק כמו בדיקה 10 של שלב 4א. אין שגיאות קונסולה חדשות
- `node scripts/i18n-audit.js index.html` - 0 ממצאים

## v4.18.1 — תיקון: פאנל האדמין כתב endAt לפי אזור הזמן הישן (שלב 5, commit א מתוך 3)

### 🐛 תיקון
- **`tripEnd` חושבה לפני שהחיפוש הגאוגרפי עדכן את `tripTimezone`.** בשמירת פאנל האדמין, אם אדמין שינה יעד **וגם** את מועד הסיום באותה שמירה - `tripEnd` נכתבה לפי אזור הזמן של היעד **הישן**, בעוד `trip.timezone` עצמו נכתב עם הזמן **החדש**. לא קשור למעבר ל-legs בכלל - קיים גם על טיול רגל אחת, קיים היום
- **תוקן**: חישוב `tripEnd` הועבר לאחר בלוק ה-geocoding, לא לפניו. נבדק שזו הנקודה היחידה בתוך ה-handler הזה עם התלות הזו - `latCoord`/`lonCoord`/`countryCode` נכתבים רק על ידי ה-geocoding ונקראים פעם אחת אחריו (סדר תקין); `tripType`/`checkinLink`/`destinationName`/`showAccent` לא תלויים בו בכלל
- נבדק בפועל: החלפת יעד ל"טוקיו" (מדומה, `fetch` הוחלף זמנית כדי לא תלוי ברשת אמיתית) + שינוי שעת סיום ל-10:00 באותה שמירה → `tripEnd` נכתב כ-01:00 UTC (10:00 Asia/Tokyo, נכון) ולא 08:00 UTC (10:00 בברלין הישנה, מה שהיה קורה לפני התיקון). רגרסיה: `tripType:"reserve"` (מדלג על geocoding) - `tripTimezone` נשאר ללא שינוי, `tripEnd` עדיין מחושב נכון
- `node scripts/i18n-audit.js index.html` - 0 ממצאים

## v4.18.0 — `tripDayDates` עובר ל-`legs.js` + תיקון startAt (שלב 4, commit א מתוך 2)

### 🐛 תיקון
- **`tripDayDates()` פירשה גם `startAt` וגם `endAt` לפי `getLastLeg().timezone`.** `startAt` שייך ליום הראשון של הטיול וצריך להתפרש בשעון **הרגל הראשונה** (`getPrimaryLeg`), לא האחרונה. בטיול רגל אחת השתיים זהות - בדיוק למה זה שרד בלי שאף בדיקה תפסה. אומת עם בדיקה שנכשלת לפני התיקון ועוברת אחריו (רומא/בנגקוק, `startAt` קרוב לחצות בשתי הערכות הזמן)

### ✨ תכונה חדשה
- **`window.tripDayDates(trip, tripId)` עברה ל-`legs.js`, ומחזירה `{date, leg}[]`** במקום `Date[]` - כל יום מתויג ברגל שלו, לא רק בתאריך. `itinerary.html` ו-`places.html` **החזיקו עותק זהה מילה-במילה** של הפונקציה (אומת ב-diff מפורש לפני המחיקה) - זו הפעם השלישית שאותו דפוס כפילות חוזר בקוד הזה. הפתרון: לא "לזכור לתקן שניים" אלא שלא יהיה שניים - שתי ההגדרות המקומיות **נמחקו פיזית**, שני הקבצים קוראים עכשיו ל-`window.tripDayDates`
- **יום שנופל בפער בין רגליים נופל לרגל הקודמת ברצף; לפני הרגל הראשונה - הרגל הראשונה.** פער הוא נתון פגום (רגליים תקינות ורציפות לא מייצרות אותו) - `console.warn` + `window.__gtLegGap={tripId,date,at}` בכל פעם שזה קורה, כדי שזה לא יישאר שקט
- **`renderDayTabs()` (itinerary.html) ו-`openDayPicker()` (places.html) עודכנו** לצרוך `.date`/`.leg` במקום `Date` ישיר - `tripId` מועבר כפרמטר מפורש (כמו ב-`getLegs`), לא נקרא ממשתנה גלובלי
- **הכרעה נדחית, לא שכחה**: `index.html`'s `hydrateTripData()` לא בהיקף - הבדיקה גילתה שהיא כבר נכונה (`tripTimezone=lastLeg.timezone` תואם את המפרט). הבאג האמיתי, מאותה משפחה, הוא ב-`fetchForecast()` (רצועת מזג האוויר) שמפרשת `flightOut` לפי `tripTimezone` - זה שלב 8, לא שלב 4. `primaryLegTimezone` גלובלי לא נוסף עכשיו כהכנה - הוחלט לא ליצור משתנה שאף קוד לא קורא לו; ההכרעה תתקבל בשלב 8 עצמו (מתועדת ב-`docs/schema-legs.md` §8)

### באג אמיתי נוסף שנתפס תוך כדי מימוש, ותוקן באותו commit
- **הטמעה ראשונה של תיוג הימים ברגל השתמשה ב-`getLegForDate(trip, date)`** (השוואת instant מוחלט מול `leg.startAt/endAt`) - נכון לפעילות אמיתית עם timestamp, אבל שגוי כאן: `date` נבנה עם `new Date(year,month,day)`, שמתפרש ב-timezone **של הדפדפן**, לא של הרגל. נבדק בפועל מדפדפן ב-`Asia/Jerusalem` מול טיול רומא/בנגקוק - יצר אזהרות "פער" כוזבות בימי הגבול של שתי הרגליים. **תוקן**: השוואת מחרוזות `"YYYY-MM-DD"` שמחושבות דרך ה-timezone של כל רגל בנפרד - אותה גישה שכבר קיימת ב-`fetchForecast()` (index.html) בדיוק מהסיבה הזו (ר' ההערה שם: "sidesteps device-vs-destination instant confusion entirely")
- נבדק בפועל אחרי התיקון, מאותו דפדפן (`Asia/Jerusalem`): טיול רומא/בנגקוק ללא פער - אפס אזהרות כוזבות, כל יום ברגל הנכונה כולל יום המעבר (רגל יוצאת). טיול עם פער מכוון - אזהרה אחת בדיוק, ביום הנכון, נופלת לרגל הקודמת. טיול רגל אחת - זהה לגמרי להיום, אפס אזהרות
- `node scripts/i18n-audit.js itinerary.html places.html` - 0 ממצאים

## v4.17.1 — תיקון: פס המבטא של ski נבלע בטורקיז של חו״ל

### 🐛 תיקון
- **פלטת `character-ski` בלבד שונתה** - `#4FC3F7 → #B3E5FC` (שני גוונים בהירים, קרובים בגוון ל-`--glow-color` של theme-abroad, `#2DD4C8`) הוחלף ב-`#0B3D91 → #4FC3F7 → #FFFFFF`. הצורה, המיקום ו-`character-pride` (שעובד היטב) לא נגעו בהם
- **כחול עמוק כעוגן, לא כמעט-שחור.** נוסה גם `#0F172A` (כמעט שחור-כחול) כקצה כהה - נבלע ב-`--bg` הכהה (`#0B1015`) באותה מידה שהגרסה המקורית נבלעה בטורקיז, רק בכיוון ההפוך. `#0B3D91` כהה/רווי מספיק כדי להיבדל מטורקיז (משפחת גוון שונה לגמרי), אבל לא כהה מספיק כדי להיעלם ברקע
- נבדק בפועל, בטלפון (375px): 4 וריאציות הושוו בתוך theme-abroad כהה (הקונפליקט המקורי) לפני הבחירה. הנבחרת נבדקה גם ב-theme-abroad בהיר, וב-theme-camping כהה (לוודא שלא נוצר קונפליקט חדש עם ירוק). `character-pride` צולם מחדש לאישור שלא השתנה
- `node scripts/i18n-audit.js` - לא רלוונטי, אין מחרוזות בשינוי

## v4.17.0 — שכבת מבטא צבעוני לפי אופי הטיול (שלב 3ב מתוך §8 ב-docs/schema-legs.md)

### ✨ תכונה חדשה
- **פס 3px מתחת לכותרת העמוד, לפי `tripCharacter`.** `tripType` ממשיך לשלוט בכל הצבע הקיים — `--glow-color`/`--primary`/וכו׳ לא משתנים לעולם לפי אופי. רק 2 מתוך 7 אופי נושאים מבטא: `pride` (גרדיאנט קשת), `ski` (גרדיאנט קר). שאר האופי — אין פס, אפס שינוי
- **`--accent-gradient` מוגדר פעם אחת ב-shared.css, בלי שכפול פר-עמוד.** בשונה מ-`--glow-color` (מוגדר מאחורי `[data-tokens="core"]` כי ל-index/shopping/wizard כבר יש ערך מקומי משלהם) - `--accent-gradient` הוא משתנה חדש לגמרי בלי ערך קודם בשום עמוד, אז כלל top-level אחד מספיק לכל 6 העמודים. `docs/schema-legs.md` §3.4 ו-`CLAUDE.md` עודכנו עם ההנמקה, כדי שזה לא "יתוקן" בעתיד לחקות את השכפול של `--glow-color`
- **הפס ממומש כ-`::after` עם `flex-basis:100%`, לא `position:absolute`.** שלושת יעדי הכותרת (`.page-head` המשותף ל-packing/places/itinerary, `.hero-top-row` ב-index, `<header>` ב-shopping) הם כולם `display:flex` - `::after` רגיל היה נהפך לפריט פלקס נוסף באותה שורה, לא לפס מתחתיה. `flex-basis:100%` + `flex-wrap:wrap` (חדש על שלושתם) מכריח אותו לשורה נפרדת, בלי לשנות איך התוכן האמיתי מתנהג
- **מחלקות `character-pride`/`character-ski` תמיד על `documentElement`, בכל 6 העמודים - כולל index.html**, למרות ש-`theme-X` שם דווקא על `document.body`. עקביות מכוונת למחלקות חדשות, לא חיקוי של הפיצול הקיים ב-`theme-X` (`documentElement` בחלק מהעמודים, `body` באחרים) - מתועד ב-CLAUDE.md כדי שהפיצול לא יתפשט הלאה
- **`showAccent` (בוליאני, ברירת מחדל `true`) - מתג חדש בפאנל האדמין**, בתוך "🌍 סוג טיול ויעד". חל אחיד על כל המבטאים, לא רק גאווה - נועד למי שלא רוצה פס קשת גלוי בשימוש ציבורי באפליקציה
- **wizard.html לא נגע** - אין שם טיול טעון ואין כותרת עמוד; לא רלוונטי
- נבדק בפועל: טיול עם `tripCharacter:"pride"` - פס קשת. `"ski"` - פס קר. שאר האופי וטיול בלי `tripCharacter` - אין פס, אפס שינוי. `showAccent:false` - אין פס גם עם אופי תואם. נבדק בכל 4 ערכות `tripType` - הפס לא מתנגש עם `--glow-color`. מצב כהה ובהיר. `flex-wrap:wrap` החדש על שלושת יעדי הכותרת לא גרם לתוכן האמיתי (כפתורי אייקון, כותרת) לגלוש לשורה נוספת בנייד (375px) - רק ה-`::after` עצמו עוטף. צילום נייד: הפס לא מגדיל את הגלילה בעמוד
- `node scripts/i18n-audit.js index.html shopping.html packing.html places.html itinerary.html` - 3 ממצאים קיימים מראש בשלושת כפתורי הייצוא ב-shopping.html (אומת מול ה-baseline לפני השינוי - לא רגרסיה, מחוץ להיקף), 0 חדשים

## v4.16.0 — ייבוא פריט מחבר נוחת בקטגוריה התואמת, לא ב-activeList (שלב 6ב מתוך 6, commit ב מתוך 2)

### ✨ תכונה חדשה
- **`importItem()` כבר לא כותב תמיד ל-`activeList`** - עכשיו היא מחפשת קטגוריה **בשם זהה** (מנורמל, כמו `addItem()`) לקטגוריה שממנה הפריט הגיע אצל החבר, בין הקטגוריות של המשתמש הנוכחי. אם נמצאה התאמה - הייבוא נוחת שם ישירות, בלי חלונית. תיקון אגב: מצב "הכל" היה גורם לכתיבה לקטגוריה שקוראים לה פשוט "all" (אף פעם לא טופל בשלב 5, כי `importItem` לא היה בהיקף) - עכשיו לא רלוונטי, כי היעד תמיד נגזר מהתאמת שם, לא מ-`activeList`
- **השם של קטגוריית המקור אצל החבר נקרא דרך `listLabelFor(uid, id)` חדשה** - חילוץ של `listLabel()` הקיימת (שנשארה זהה לגמרי בהתנהגות, מבצעת `listLabelFor(user.uid, id)`) עם `uid` כפרמטר, כי `listLabel()` המקורית הייתה נעולה ל-`user.uid` שלי ולא יכלה לקרוא metadata של משתמש אחר
- **אם אין התאמה בשם - חלונית חדשה (`importCategoryOverlay`)**: ברירת המחדל **מסומנת מראש** (`selected` + נאכף גם ב-`.value`, לא רק בהינתן HTML) היא "צור קטגוריה בשם X"; שאר האפשרויות הן הקטגוריות הקיימות של המשתמש. בחירה ידנית של קטגוריה קיימת אחרת - תקינה, בלי אזהרה נוספת
- **`createListReturningId(name)` חדשה** - חילוץ של הליבה מ-`createList()` (אותה יצירת id, אותה כתיבה ל-`listMeta`) בלי תופעות הלוואי הספציפיות ל-`newListOverlay` (סגירה, ניקוי שדה, טוסט) - `createList()` עצמה נשארה זהה בהתנהגות, רק עכשיו קוראת לפונקציית הליבה החדשה
- **אומת בפועל (לא רק הונח): אחרי שנוצרה קטגוריה פעם אחת דרך החלונית, ייבוא שני מאותה קטגוריית-מקור אצל החבר מתאים בשם ומדלג על החלונית לגמרי** - הבעיה של "החלונית תקפוץ בכל ייבוא מאותה קטגוריה לא-מותאמת" אכן נפתרת מעצמה אחרי הפריט הראשון, בדיוק כמו שהניחו
- נבדק בפועל (hook זמני, הוסר ואומת): (א) ייבוא לקטגוריה בשם זהה אצלי → נוחת שם ישירות, החלונית לא נפתחת. (ב) ייבוא מקטגוריה שאין לי → חלונית נפתחת, "צור קטגוריה" מסומן מראש, אחרי יצירה מדומה הפריט נוחת בקטגוריה הנכונה (אומת דרך נתיב הכתיבה המדויק שהופיע באזהרת ה-`permission_denied`). (ג) ייבוא שני מאותה קטגוריית-מקור אחרי היצירה → בלי חלונית, נוחת ישירות באותה קטגוריה חדשה. (ד) שם עם רווחים מיותרים ואותיות שונות → הנרמול תפס את ההתאמה. בנוסף: בחירה ידנית של קטגוריה קיימת אחרת מתוך החלונית → נוחתת שם, בלי חלונית נוספת. סגירה בלחיצה על הרקע (backdrop) עובדת כמו בכל שאר החלוניות. `renderMine`/`renderGroup` לא נשברו. נבדק גם בנייד (375px) - החלונית לא מגדילה את הגלילה בעמוד
- `node scripts/i18n-audit.js packing.html` - 0 ממצאים

## v4.15.0 — "main" מוסתרת מרצועת הצ׳יפים כשהיא ריקה (שלב 6א מתוך 6, commit א מתוך 2)

### ✨ תכונה חדשה
- **"main" - רשימת הרפאים (נוצרת תמיד ב-`allListIds()`, אף פעם לא נוצרת בפועל ב-RTDB, אי אפשר לשנות לה שם או למחוק אותה) - מוסתרת מרצועת הצ׳יפים כשאין לה לא פריטים ולא `listMeta` מפורש.** לא נמחקת, לא ממוזגת - טיול שכן צבר בה פריטים ימשיך לראות אותה בדיוק כמו קודם. ההסתרה מוגבלת לרצועת הצ׳יפים בלבד - `editListSelect`, `bulkMoveTarget` ו-`addItemCategorySelect` (בחירת קטגוריה במודאל ההוספה, מצב "הכל") ממשיכים להציג אותה תמיד - אלה הקשרי "בחר יעד", ששם יעד ריק-אבל-אמיתי עדיין לגיטימי להציע
- **בדיקת ה-`listMeta` המפורש נבדקת גם כשהיא לא אמורה להתממש היום** (אין דרך ליצור `listMeta.main` דרך שום מסך קיים - שינוי שם חסום ל-main) - נבדקה בכל זאת, לא הונחה כבלתי אפשרית, למקרה שקוד עתידי כן יכתוב אליה
- **ההגנה על ערך `activeList` לא תקין ב-`renderLists()` נופלת עכשיו ל-"הכל", לא ל-"main"** - שינוי הכרחי, לא רק ניקוי: `allListIds()` **תמיד** מחזירה "main" (רשימת רפאים, לא פער אמיתי), כך שההגנה הישנה (`!ids.includes(activeList)`) מעולם לא הייתה תופסת את המקרה "המסנן הפעיל היה main, ו-main כרגע מתחבאת" - בלי התיקון, משתמש שנשאר על main הריקה היה רואה מסך ריק בלי אף צ׳יפ מסומן ובלי הסבר. הבדיקה הורחבה לכסות גם את המקרה הזה במפורש, לא רק "הקטגוריה לא קיימת בכלל"
- **לא נגעו ב-`deleteList()`'s ה-`activeList = "main"` המפורש שלה** - `renderMine()` קוראת ל-`renderLists()` מיד אחרי כל שינוי שמקצה מחדש את `activeList`, כך שהבדיקה המרכזית הזו מתקנת בכל מקרה, בלי קשר לאיזו משבע נקודות ההקצאה קבעה את הערך הביניים - אותו נימוק שכבר קבע למה נקודת השמירה ל-localStorage יושבת כאן לבד ולא משוכפלת בשבע הנקודות
- נבדק בפועל (hook זמני, הוסר ואומת): main ריקה בלי `listMeta` - מוסתרת. main עם פריט - מוצגת. main עם `listMeta` מפורש בלי פריטים - עדיין מוצגת (המקרה ה"בלתי אפשרי" שנבדק בכל זאת). `activeList="main"` כשיש בה תוכן, ואז התוכן מתרוקן - נופל ל"הכל" עם צ׳יפ מסומן, לא נשאר תקוע. קטגוריה שנמחקה לגמרי (לא רק main) - גם היא נופלת ל"הכל" עכשיו, לא ל"main" - רגרסיה מכוונת, לטובה. אין שגיאות קונסולה בטעינה נקייה
- `node scripts/i18n-audit.js packing.html` - 0 ממצאים

## v4.14.1 — תיקון: רצועת הצ׳יפים קטומה בנייד, מאחורי כפתורי ⚙️/בחירה מרובה

### 🐛 תיקון
- **הרצועה קיבלה שורה משלה, ברוחב מלא**, במקום לחלוק שורה עם 3 כפתורי האייקון (⚙️/☑️/➕). ⚙️ ובחירה מרובה (וגם ➕ "רשימה חדשה", שלא הוזכר במפורש אך שייך לאותה קבוצה) עברו לשורת הכותרת/מונה הקיימת (`.list-head`), שהתפנתה ברגע שהרצועה יצאה ממנה
- **נבדק לפני שינוי הפריסה שיש מקום**: ב-375px, אחרי הפרשי הריפוד, נשארים כ-339px לשורת הכותרת. שלושת הכפתורים (3×42px + רווחים) + המונה (עד כ-100px גם במצב "הכל" עם מספרים גדולים) - כ-240px, מקום נוח, לא צפוף
- **לא נדרש שינוי במנגנון ההצללה/גלילה עצמו** - הוא כבר היה יחסי לרוחב הקונטיינר (`calc(100% - 24px)`, כמו ב-`.quick-picks`), לא קשיח. הבעיה הייתה אך ורק ברוחב שהוקצה לרצועה (כ-137px מתוך 375px כשחלקה שורה עם 3 כפתורים), לא בקוד ההצללה - נסיון קודם לחזק את ההצללה עצמה (v4.14.0, לא שוחרר) לא היה יכול לפתור את זה: אין הצללה שפותרת רוחב שמראה בקושי שניים וחצי צ׳יפים
- נבדק בפועל, נייד (375px), לפני ואחרי - צילום מסך: המונה קריא במלואו ("50% · 1/2"), הכפתורים לא צפופים, הרצועה מציגה 3+ צ׳יפים במקום כ-1.5 קטומים. `has-more`/הצללה אומתו שעדיין עובדים נכון עם יותר קטגוריות ממה שנכנס ברוחב (6 צ׳יפים, `scrollWidth:426 > clientWidth:339`)
- `node scripts/i18n-audit.js packing.html` - 0 ממצאים

## v4.14.0 — רצועת צ׳יפים במקום בורר הרשימות ב-packing.html (שלב 5 מתוך 6)

### ✨ תכונה חדשה
- **`#listSelect` (בורר `<select>`) הוחלף ברצועת צ׳יפים גוללת אופקית** - צ׳יפ ראשון "הכל", אחריו קטגוריה לכל רשימה. לחיצה מסננת; הצ׳יפ הפעיל מסומן במילוי (כמו `.day-tab` ב-itinerary.html - בחירה יחידה, נשארת מסומנת - לא כמו `.pick` בהצעות המהירות, שהן כפתורי פעולה חד-פעמיים). מנגנון הגלילה (הצללה שנעלמת בסוף, כיוון תלוי RTL/LTR, סרגל דק במחשב בלבד, spacer בסוף) מועתק ישירות מ-`.quick-picks` - אותה תבנית שכבר תוקנה ונבדקה
- **"הכל" הוא ערך `activeList` תקין**, נשמר ב-`localStorage` בדיוק כמו כל קטגוריה אמיתית - מחרוזת פשוטה, בלי קידוד מיוחד (עקבי עם איך ש"main" וכל שאר ה-id-ים כבר מטופלים)
- **מד ההתקדמות ומספר הפריטים ב"הכל" מציגים את כל הפריטים מכל הקטגוריות** - לא נדרש קוד חדש: `renderMine()` כבר קראה מ-`itemsForCategoryFilter(activeList)` משלב 2, ופונקציה הזו כבר יודעת לאחד הכל כש-`filter==="all"` משלב 1
- **כפתור הצעות ה-AI מושבת ב"הכל"**, עם הטקסט עצמו (לא רק tooltip) משתנה להסבר ("בחרו קטגוריה לקבלת הצעות AI") - כפתור עם טקסט גלוי, לא אייקון בלבד, אז ההסבר צריך להיות קריא בלי hover (שלא עובד במגע ממילא)
- **כפתור ⚙️ ניהול הרשימה מושבת ב"הכל"**, עם `title`/`aria-label` מוסברים - "הכל" אינה רשימה אמיתית, אין `listMeta`/`items` משלה לנהל
- **הוספת פריט ב"הכל": בורר קטגוריה בתוך אותו מודאל הוספה**, לא השבתה. `addItem()` מקבלת פרמטר `categoryId` מפורש אופציונלי (ברירת המחדל `activeList`, כל שאר הקריאות הקיימות - הצעות מהירות, הצעות AI - לא השתנו). קופץ רק כש-`activeList==="all"`; מדלג עליו ומוסיף ישירות בכל מצב אחר, בדיוק כמו קודם. הבורר עצמו הוא עותק של `editListSelect` הקיים - אותה תבנית `.field`+`<select>`, לא רכיב חדש
- **תיבת ההצעה "זה נשמע כמו X - להוסיף כתת-פריט?" (`updateCategorySuggestion`) לא מוצגת ב"הכל"** - `addToSuggestedCategory()` עצמה כותבת ל-`activeList` בלי שינוי, ולא הפכה מודעת-קטגוריה (בניית בורר שני בשבילה הייתה חורגת מהיקף הצעד הזה) - במקום זאת, הדרך היחידה להוסיף ב"הכל" היא תמיד דרך הבורר, שכן מפיק קטגוריה מפורשת
- **דה-דופ בהוספה עובר לבדוק מול כל הקטגוריות**, לא רק היעד - עקבי עם איך שהצעות AI כבר בודקות כפילות (הכרעה קודמת, מיושמת עכשיו)

### 🐛 תיקון
- **`renderLists()`'s ההגנה על ערך לא תקין (`if (!ids.includes(activeList)) activeList = "main"`) הייתה מאפסת "all" בשקט לכל טעינה** - "all" לעולם לא רשימה אמיתית ולכן אף פעם לא ב-`ids`, כך שההגנה הקיימת הייתה תופסת אותה כלא-תקינה ומחזירה תמיד ל-"main". זה לא רק חלק מהשכתוב לצ׳יפים - זה באג ששבר את השמירה מ-commit קודם באופן שקט לחלוטין (הבחירה "הכל" הייתה נעלמת בכל רענון). תוקן: "all" מטופלת כערך תקין תמיד, לפני בניית הצ׳יפים (לא אחרי, כמו שההגנה הישנה עשתה על ה-`<select>`)
- **`$("listSelect").onchange` נשאר מקושר לאלמנט שכבר לא קיים** - נתפס תוך כדי בדיקה (לא רק בקריאת קוד): שגיאת `TypeError: Cannot set properties of null` עצרה את כל טעינת המודול, כך שאף פעם אין תלות בהנחה - כל שינוי כזה עובר בדיקת טעינה נקייה בפועל

### 🔧 בדיקה
- **נבדקו לראשונה, בתצוגה מאוחדת אמיתית, לא מדומה**: פעולות (סימון, תת-פריט) על פריטים משתי קטגוריות שונות **מוצגים יחד באותו מסך** - כל אחת נחתה בקטגוריה הנכונה (`items/catB/b1`, `items/catC/c1/subItems/s1`, אומת דרך נתיבי אזהרת Firebase). מודאל עריכה שנפתח מהתצוגה המאוחדת לכד את הקטגוריה האמיתית (`catC`), לא "all". **בחירה מרובה חוצת-קטגוריות בוצעה ישירות מתוך התצוגה המאוחדת** (לא בהדמיה של החלפת activeList כמו בשלב 4) - שני פריטים משתי קטגוריות נבחרו על אותו מסך, `selectedIds` תיעד נכון
- נבדק: מעבר "הכל" ⇄ קטגוריה בודדת - תוכן, התקדמות, מצב כפתורים מתעדכנים נכון בשתי הכיוונים. `applyLanguage()` נבדק תוך כדי מצב מושבת - הטקסט/מצב ההשבתה נשמר נכון (לא נדרס בחזרה למצב "פעיל" על ידי החלפת שפה)
- נבדק: הוספת פריט ב"הכל" עם בחירת קטגוריה מהבורר - נחת בקטגוריה שנבחרה, לא ב-`activeList`. דה-דופ חוצה-קטגוריות נבדק - פריט בשם קיים בקטגוריה אחרת נחסם גם כשהיעד הנבחר שונה
- נבדק: התמדה של "all" דרך רענון עמוד אמיתי (לא רק קריאה/כתיבה ל-localStorage) - `activeList` חוזר "all" אחרי `navigate` מלא, בדיוק התרחיש שההגנה הישנה הייתה שוברת
- נבדק חזותית בעברית/RTL ובאנגלית/LTR - כיוון ההצללה בקצה הרצועה נבדק דרך `getComputedStyle` (`to left` ב-RTL, `to right` ב-LTR), לא רק חזותית. `scrollWidth`/`clientWidth`/`.has-more` אומתו ישירות. עיצוב ה-CSS החדש (`.chip`/`.category-chips`) משתמש אך ורק בטוקנים קיימים (`--primary`, `--line`, `--surface-2`, `--glow-color`) שכבר מתחלפים נכון בין מצב כהה/בהיר וארבע ערכות סוג הטיול - לא נבדק חזותית בנפרד בכל שילוב, מוסק מהתבנית הזהה ל-`.day-tab`/`.quick-picks` שכבר הוכחו
- `node scripts/i18n-audit.js packing.html` אחרי כל השינויים - 0 ממצאים

## v4.13.13 — בחירה מרובה חוצת-קטגוריות (שלב 4 מתוך 6)

### ✨ תכונה חדשה
- **`selectedIds` הפך מ-`Set` שטוח של `itemId` ל-`Map<itemId, categoryId>`** - בחירה יכולה עכשיו לחצות קטגוריות, וכל פריט נבחר נושא את הקטגוריה האמיתית שלו, לא רק את `activeList`. נבחרה `Map` על פני `Set` של `"categoryId:itemId"` (התבנית הקיימת ב-`data-sub-check` וכו') כי כל אחת משלוש הפעולות (`bulkDelete`/`bulkToggleDone`/`confirmBulkMove`) צריכה את שני הערכים יחד לכל פריט - `Map.forEach((categoryId, id) => ...)` נותן את זה ישירות, בלי לפרק מחרוזת בכל אתר קריאה
- **`bulkDelete`/`bulkToggleDone` כותבות לקטגוריה של כל פריט בנפרד** - לא ל-`activeList` אחיד לכולם
- **`openBulkMove()` מסננת דינמית**: אם כל הפריטים שנבחרו מגיעים מקטגוריה אחת, היא מוסתרת מרשימת היעדים (בדיוק ההתנהגות הקודמת מבוססת-`activeList`). בבחירה חוצת-קטגוריות שום דבר לא מוסתר - אין "קטגוריה נוכחית" יחידה להסתיר, וכל קטגוריה אמיתית היא יעד תקף לפחות לחלק מהפריטים
- **`confirmBulkMove()` מדלגת על פריט שכבר ביעד** (אפשרי גם כשההסתרה הדינמית לא תפסה את זה - למשל 2 מתוך 3 פריטים כבר ביעד) - בלי כתיבה מיותרת. **הטוסט מציג את המספר האמיתי שהועבר** (`"3 הועברו"`), לא מספר הנבחרים - אחרת דילוג שקט על חלק מהפריטים ייראה כהצלחה מלאה. אם כל הנבחרים כבר ביעד, טוסט נפרד ("הפריטים כבר ברשימה הזו") בלי לגעת ב-RTDB בכלל
- **בכוונה: `activeList` לא קופץ ליעד אחרי העברה מרובה**, בניגוד ל-`saveEdit()`'s ההעברה הבודדת שכן קופצת. תועד בקוד כדי שלא "יתוקן" בעתיד: פריט בודד "לקפוץ אחרי" הגיוני; קבוצה יכולה להגיע ממספר קטגוריות מקור בבת אחת, ומשתמש שרק סיים בחירה מרובה מאבד את ההקשר של מה שנשאר אם התצוגה קופצת
- **כלל 1 (אין fallback) מיושם גם כאן**: `requireCategory` פוצל ל-`assertCategory(categoryId, context)` (בדיקה גנרית) ו-`requireCategory(el, context)` שעוטפת אותה לאלמנטי DOM - שלוש פעולות ה-bulk קוראות ל-`assertCategory` ישירות על כל ערך ב-`selectedIds`

### 🐛 תיקון
- **`bulkToggleDone` קראה `currentItems()[id]` (מוגבל ל-`activeList`) כדי לבדוק אם כל הנבחרים כבר מסומנים** - לא רק רה-פקטור נלווה: בבחירה חוצת-קטגוריות זה היה מחזיר `undefined` בשקט לכל פריט מחוץ ל-`activeList`, וקובע "לא כולם בוצעו" גם כשכולם באמת בוצעו. תוקן לקרוא `items[categoryId][id]` - הבאג אומת בפועל: 3 פריטים ב-2 קטגוריות, כולם `isDone:true`, `activeList` מוצב על קטגוריה שלישית - הקוד המתוקן חישב `allDone:true` נכון

### 🔧 בדיקה
- נבדק בפועל (hook זמני, הוסר ואומת): בחירה חוצת-קטגוריות אמיתית (2 פריטים מקטגוריה אחת + 1 משנייה) - מונה "3 נבחרו" נכון. `openBulkMove` נבדק בשני המקרים - בחירה הומוגנית (קטגוריה משותפת מוסתרת, כמו קודם) ובחירה חוצת-קטגוריות (כלום לא מוסתר). `confirmBulkMove` נבדק עם דילוג חלקי (2 מתוך 3 כבר ביעד - `movedCount:1`, ה-`updates` בפועל הכיל רק את הפריט השלישי) ודילוג מלא (כל הנבחרים כבר ביעד - `movedCount:0`, `updates` ריק, אין קריאת RTDB). `bulkDelete`/`bulkToggleDone` נבדקו עם בחירה חוצת-קטגוריות - הנתיבים בפועל אומתו (לא רק שלא קרסו). כלל 1 נבדק ישירות - `Map` עם ערך קטגוריה חסר זורק לפני כל כתיבה

## v4.13.12 — 7 נקודות כתיבה קוראות `data-category` במקום `activeList` (שלב 3 מתוך 6, השלב המסוכן)

### ✨ תכונה חדשה
- **7 מתוך 14 נקודות הכתיבה על פריט קיים עברו מ-`activeList` ל-`data-category` של האלמנט שהפעיל אותן**: סימון בוצע (`data-check`), נראות (`data-toggle-visibility`), סימון תת-פריט (`data-sub-check`), מחיקת תת-פריט (`data-sub-delete`), הוספת תת-פריט - **שני המסלולים**, קליק על ➕ ו-Enter בשדה (`data-sub-add`/`data-sub-add-input`), ועריכה/העברה/מחיקה (`saveEdit`/`deleteItem`, דרך `data-edit`)
- **כלל מרכזי, בלי חריגים: אין fallback ל-`activeList`.** `requireCategory(el, context)` חדשה - אם `data-category` חסר על האלמנט שהפעיל את הפעולה, `console.error` + `throw` לפני שנוגעים ב-RTDB. כישלון רועש, לא כתיבה שקטה לקטגוריה הלא נכונה
- **`openEdit(id, categoryId)` לוכדת את הקטגוריה בזמן הקליק על `data-edit`**, לא קוראת מ-`activeList` כשהמודאל נפתח - נשמרת ב-`editingCategoryId` (משתנה מודול חדש, מתאפס יחד עם `editingId` בכל נקודת סגירה: שמירה, ביטול, מחיקה). `saveEdit()` מבדילה במפורש: המקור הוא `editingCategoryId`, היעד הוא מה שנבחר ב-`editListSelect`
- **תת-פריטים יורשים את קטגוריית ההורה** - אין להם קטגוריה עצמאית, מתויגים ב-`renderMine()` (שלב 2) בקטגוריה של הפריט המכיל
- **`currentItems()` נשארת ללא שינוי** - עדיין בשימוש בכל מקום שלא הוסב (5 קבוצות כתיבה מחוץ להיקף: `setListVisibility`, שלוש פעולות bulk, `importItem`, `addToSuggestedCategory`, `addItem`)
- **מחוץ להיקף בכוונה, ללא שינוי**: הפעולות שאין להן שורת-פריט קיימת לקרוא ממנה - פעולה על כל הרשימה, בחירה מרובה (שלב 4), ייבוא מקבוצה ופריט חדש (שלב 6)
- **נבדק בפועל, כל 9 נקודות הכתיבה שהשתנו, בתרחיש קבוע**: פריט בקטגוריה `catB` מרונדר בזמן ש-`activeList` על `catB`, ואז `activeList` מוחלף ל-`catA` **בלי רינדור מחדש** - מדמה בדיוק את מצב "פריט ב-B, הקשר על A" שהמודל הממוזג ייצור. כל אחת מ-9 הפעולות הופעלה על האלמנט (עדיין מתויג `catB`), והנתיב שנשלח בפועל אומת דרך אזהרות `permission_denied` של Firebase עצמה (שמראות את הנתיב המלא) - **כל התשע נחתו תחת `items/catB/...`, אף אחת לא תחת `catA`**. `editingCategoryId` אומת ישירות (`"catB"`) גם כשמעבר-בין-רשימות (multi-path `update`) לא הראה נתיבים בודדים באזהרת Firebase המקובצת
- **כלל 1 (אין fallback) אומת ישירות**: אלמנט `data-check` בלי `data-category` (מדמה באג רינדור עתידי) - `console.error` נורה, `Error` נזרק, **ואין אזהרת Firebase חדשה בכלל** - הכתיבה מעולם לא יצאה
- אין שגיאות קונסולה חדשות בטעינה רגילה (נבדק בטאב נקי)

## v4.13.11 — `renderMine` מתייגת `data-category` (שלב 2 מתוך 6, רשימת ציוד מסוננת)

### 🔧 תשתית
- **`renderMine()` צורכת `itemsForCategoryFilter(activeList)` במקום `currentItems()`** - אותה קבוצת פריטים בדיוק (המסנן הוא `activeList`, זהה למה שהיה מוצג), אבל כל item בתוצאה נושא עכשיו `categoryId`
- **כל אלמנט אינטראקטיבי בשורת פריט מתויג ב-`data-category`**: `data-select`, `data-check`, `data-expand`, `data-toggle-visibility`, `data-edit`, `data-sub-check`, `data-sub-delete`, `data-sub-add-input`, `data-sub-add` - 9 סוגי אלמנטים, `data-check` על תת-פריט מקבל את הקטגוריה **של ההורה** (תת-פריטים לא נושאים קטגוריה עצמאית)
- **אפס שינוי התנהגות**: שום קוד לא קורא ל-`data-category` עדיין - שלב 3 יקרא. כל הפעולות ממשיכות לכתוב דרך `activeList`, בדיוק כמו קודם
- **מיפוי מלא של 14 נקודות הכתיבה** מול השינוי: 9 מהן מתויגות (ישירות או דרך `data-select`/`data-edit` שמזינות אותן בשלבים הבאים). 5 לא רלוונטיות לשלב הזה כי אין להן שורת-פריט קיימת לתייג - `setListVisibility` (פעולה על כל הרשימה), `importItem` (ב-`renderGroup()`, פונקציה אחרת לגמרי), `addToSuggestedCategory` ושתי הענפים של הוספת פריט חדש (`addItem`) - כולן פועלות לפני שיש item שרונדר, הקטגוריה שלהן תיפתר אחרת (שלב 6)
- נבדק בפועל (hook זמני, הוסר ואומת): 3 פריטים (אחד עם 2 תת-פריטים, מצב בחירה מרובה + תת-פריטים פתוחים) - 20 אלמנטים אינטראקטיביים בשורות, **20** נושאים `data-category` תקין (לא ריק, לא `undefined`). צילום מסך מאמת תצוגה זהה למה ש-`currentItems()` הייתה מציגה. אין שגיאות קונסולה

## v4.13.10 — `itemsForCategoryFilter()` ב-packing.html (שלב 1 מתוך 6, רשימת ציוד מסוננת במקום בורר רשימות)

### 🔧 תשתית
- **פונקציה חדשה, לא מחוברת לשום דבר עדיין**: `itemsForCategoryFilter(filter)` - תחליף עתידי ל-`currentItems()` (שנשארת ללא שינוי ובשימוש מלא כרגיל). `filter === "all"` מאחדת פריטים מכל הקטגוריות (`items/{categoryId}`) למפה שטוחה אחת, כשכל פריט מתויג ב-`categoryId` שהוא הגיע ממנו; `filter` ספציפי מחזיר קטגוריה בודדת, באותה צורה
- **הסיבה לתיוג**: 14 נקודות הכתיבה הקיימות (סימון, מחיקה, עריכה, תת-פריטים, העברה) מניחות כרגע ש-`activeList` **הוא** הקטגוריה של הפריט - נכון היום כי כל תצוגה מציגה קטגוריה אחת. ברגע שתצוגה אחת תציג כמה קטגוריות יחד, ההנחה נשברת בכל מקום בו-זמנית - התיוג הזה הוא מה שיאפשר לכל נקודת כתיבה לדעת את הקטגוריה האמיתית של הפריט, לא לנחש מה-state הגלובלי
- **אפס שינוי התנהגות**: נבדק בפועל (hook זמני, הוסר ואומת) - מיזוג נכון על פני 3 קטגוריות, תיוג נכון, `filter` שלא קיים מחזיר `{}` בלי קריסה, ו-`currentItems()` הקיימת ממשיכה להתעלם לגמרי מהתוספת. אין שגיאות קונסולה

## v4.13.9 — תיקון: הרשימה הפעילה ב-packing.html מתאפסת ל"רשימה ראשית" בכל טעינת עמוד

### 🐛 תיקון
- **הבאג**: `activeList` היה משתנה בזיכרון בלבד (`let activeList = "main"`) - לא נשמר בשום מקום. כל טעינת עמוד (כולל יציאה וחזרה) איפסה אותו לברירת המחדל הקשיחה `"main"`, בלי קשר לאיזו רשימה המשתמש היה בה קודם
- **התיקון, אותה תבנית בדיוק כמו `lastTripId` הקיים**: `activeListKey = \`trip-${tripId||"legacy"}-activeList\`` - מפתח **לכל טיול בנפרד** (לא גלובלי), נקרא פעם אחת בהצהרת `activeList` (עם `try/catch`, כמו שאר קריאות ה-localStorage בקובץ), ונשמר בנקודה יחידה - בתוך `renderLists()`, מיד אחרי שהיא כבר קבעה את הערך הסופי
- **למה נקודת שמירה יחידה ולא 7 נקודות**: `activeList` מוקצה מחדש ב-7 מקומות שונים בקוד (מעבר ידני, יצירה/שכפול/מחיקה של רשימה, `ensureStarterLists`, ההגנה הקיימת על רשימה שנעלמה, העברת פריט לרשימה אחרת) - **כל אחד מהם מגיע בסופו של דבר ל-`renderMine()`→`renderLists()`**, ישירות או דרך הד ה-listener של RTDB. שמירה שם אחת בלבד מכסה את כל השבעה בלי לגעת בהם, ומצמצמת את השטח שהשתנה
- **ההגנה הקיימת על רשימה שנמחקה/לא קיימת - לא שונתה בכלל**: `if (!ids.includes(activeList)) activeList = "main";` נשארה מילה במילה. אומת בפועל: רשימה משוחזרת שכבר לא קיימת נופלת ל-"main" **וגם מתקנת את עצמה ב-localStorage** (השמירה שמופעלת מיד אחרי ההגנה, באותה הרצה)
- **הקשר בין `ensureStarterLists()` לערך המשוחזר - לא דורש קוד נוסף, רק אומת**: למשתמש קיים (יש `listMeta`/`items`) - ה-early-return הקיים של הפונקציה מונע ממנה לגעת ב-`activeList` בכלל, כך שהערך המשוחזר עובר בלי הפרעה. למשתמש חדש (אין כלום) - ההקצאה הקיימת `activeList = lists[0].id` דורסת בלי תנאי, כולל אם משהו הזדמן להישמר בעבר - שתי הכיוונים כבר מטופלים על ידי ההסתעפות הקיימת בקוד, לא נוספה הגנה חדשה
- נבדק בפועל (hook זמני `window.__test`, נוסף/נבדק/הוסר, אומת שהוסר): **1.** טעינה ראשונה בלי ערך שמור - `"main"`, כמו קודם. **2.** מעבר לרשימה + `renderLists()` - נשמר מיד. **3.** רענון עמוד - משוחזר נכון, **לפני** שהגיע נתון כלשהו מ-RTDB. **4.** רשימה משוחזרת שלא קיימת יותר - נופלת ל-"main" **ומתקנת** את הערך השמור. **5.** משתמש קיים (יש `listMeta`) - `ensureStarterLists()` לא נוגעת בערך המשוחזר (early-return מאומת). **6.** שני `tripId` שונים - מפתחות נפרדים לגמרי, אין דליפה בין טיולים. (המסלול של משתמש חדש-לגמרי לא נבדק חי - דורש `user` אמיתי מחובר ל-`ensureStarterLists()`, לא זמין בסביבת הבדיקה; מאומת בקריאת קוד בלבד: הפונקציה לא שונתה כלל, וההקצאה שלה ל-`activeList` תמיד ללא תנאי כשהיא מגיעה לשם)

## v4.13.8 — תיקון: places.html מציגה יום פנטום שנעלם ל-itinerary.html (commit ב מתוך 2)

### 🐛 תיקון
- **הבאג**: `places.html`'s `tripDayDates()` חישבה את היום הראשון/אחרון של הטיול לפי אזור הזמן של **המכשיר הצופה**, לא של היעד - בעוד `itinerary.html`'s הגרסה (מתוקנת מאז 2026-09-05) עושה זאת נכון דרך `zonedParts()`. הועתקה מ-itinerary.html ב-2026-09-03, יומיים **לפני** התיקון של 09-05 - והתיקון ההוא מעולם לא הועתק חזרה
- **לא רק תצוגה שגויה - תוכן שנעלם בפועל**: `openDayPicker()` כותבת דרך `addPlaceToDay(dayKey)` ל-`itinerary/days/{dayKey}/activities`, עם `dayKey` שמחושב מהיום הפנטום. `itinerary.html` לעולם לא מייצר טאב עם אותו מפתח (הרשימה שלה מתחילה יום מאוחר יותר) - הפעילות נשמרת, אבל לא מוצגת באף מקום
- **התיקון**: places.html מקבל את אותה שורה בדיוק שכבר ב-itinerary.html - `tz`/`startP`/`endP` דרך `window.zonedParts()` (מ-`datetime.js`, ר' v4.13.7) במקום `start.getFullYear()/getMonth()/getDate()` הגולמי. `tripDayDates()` בשני הקבצים זהה עכשיו מילה במילה (מלבד קידומת `window.`)
- **תיעוד מתוקן (v4.13.7) הוחלף בשימוש בפועל**: `itinerary.html` ו-`index.html` הפסיקו להשתמש בעותקים המקומיים של `zonedParts`/`todayKeyInZone`/`nowHHMMInZone`/`dateKeyInZone` וקוראים ל-`window.*` מ-`datetime.js` בכל נקודת קריאה. **העותקים המקומיים נמחקו** (לא רק "לא נקראים יותר") - כדי שאף אחד לא יערוך בטעות את הגרסה הלא נכונה. `dateKey()` נשאר מקומי בשני הקבצים בכוונה - לא נגעו בו ב-09-05 ולא ברשימת ארבע הפונקציות שרוכזו
- נבדק בפועל: **1.** השוואת שוויון - טיול סינתטי לבנגקוק (`startAt:"2026-10-06T20:00:00.000Z"`, `timezone:"Asia/Bangkok"`) הורץ בשני הקבצים (hook זמני `window.__test`, נוסף/נבדק/הוסר, אומת שהוסר). `itinerary.html` ו-`places.html` מחזירות **בדיוק** אותו מערך של 8 מפתחות יום (`2026-10-07` עד `2026-10-14`) - זו ההוכחה שהתיקון נכון, לא רק שהוא לא קורס. **2.** לשם ההשוואה בלבד (לא מהקוד עצמו) - חושב מה הגרסה הישנה הייתה מחזירה על אותו טיול: 9 מפתחות במקום 8, כולל `2026-10-06` פנטום - בדיוק התרחיש שתואר בחקירה. **3.** טיול ריק (`{}`) - `tripDayDates()` מחזירה `[]` בלי שגיאה, כמו קודם. **4.** כל 6 העמודים נטענו מחדש - אין שגיאות קונסולה חדשות; `window.todaysPlanContext()` (index.html, צרכן של `window.dateKeyInZone`) נבדק ישירות ולא זרק
- **לא בהיקף**: הנתונים היתומים שכבר קיימים ב-RTDB אמיתי (אם יש) - הסקריפט לבדיקתם עדיין ממתין לבדיקה שלך, לא הורץ

## v4.13.7 — `datetime.js`: ריכוז `zonedParts`/`todayKeyInZone`/`nowHHMMInZone`/`dateKeyInZone` (commit א מתוך 2)

### ✨ תשתית
- **קובץ חדש ברמת השורש, `datetime.js`, אותה תבנית טעינה בדיוק כמו `legs.js`** - IIFE, script קלאסי בכל 6 העמודים (`<script src="datetime.js">` מיד אחרי `legs.js`), מצמיד ל-`window` במפורש כדי שיהיה נגיש גם מ-index.html (script קלאסי) וגם מחמשת המודולים
- **הסיבה שזה לא נכנס ל-legs.js**: הפונקציות האלה לא קשורות לרגליים - הן עונות "מה השעה/היום ב-timezone X", בלי שום ידע על מבנה טיול. לערבב את השניים היה הופך את legs.js לקובץ אוסף
- **למה עכשיו, לא רק "עוד ניקוי"**: `zonedParts` היה כבר קיים כעותק זהה ביט-ביט בשני קבצים (`itinerary.html`, `index.html` - אומת ב-git log: שניהם עצמאיים, לא אחד הועתק מהשני). היה עומד להיווצר עותק שלישי בתיקון של `tripDayDates()` ב-places.html (commit ב). שלושה עותקים של פונקציה עם קצה אמיתי (`hour===24`→`0`) זה כבר לא תקדים כמו `dateKey`/`--glow-color` (חד-שורתי/CSS) - זה דפוס שיחזור. רוכז לפני שהעותק השלישי נכתב, לא אחריו
- **אומת לפני הכתיבה, לא רק אחריה**: השוואה בין שלוש המימושים (itinerary.html, index.html, והגרסה המתוכננת ל-datetime.js) על 5 תאריכים × 3 timezones (כולל שתי נקודות DST של ארה"ב, אזור עם היסט חצי-שעה - Asia/Kolkata, ומופע מדויק של קצה ה-hour===24 ב-Asia/Bangkok) - 20/20 השוואות זהות. אפס הבדל, כולל הקצה
- **אפס שינוי התנהגות**: שום קוד קיים לא קורא ל-`datetime.js` עדיין - העותקים המקומיים ב-itinerary.html/index.html עדיין קיימים ומוצללים (shadowing) על ידי הצהרות הפונקציה המקומיות שלהם, בדיוק כמו ש-legs.js לא שינה כלום ביום שנוסף. הוחלפה בפועל רק ב-commit ב
- נבדק בפועל: כל 6 העמודים נטענו מחדש, `window.zonedParts`/`window.dateKeyInZone`/`window.todayKeyInZone`/`window.nowHHMMInZone` הם `function` בכולם (גם ב-index.html הקלאסי, גם במודול כמו packing.html), קצה ה-`hour===24` נבדק חי דרך `window.zonedParts` (לא רק בבדיקת ה-Node) - `Asia/Bangkok` בחצות מקומי בדיוק מחזיר `hour:0`. אין שגיאות קונסולה חדשות באף עמוד

## v4.13.6 — תיקון: aria-label "הסר X" לא מתעדכן, ולא מתעדכן חי (commit ג2 מתוך 3, `scripts/i18n-audit.js`)

### 🐛 תיקון
- **מ-12 מועמדים דינמיים שהסורק דיווח, 11 היו חיוביים שגויים** (כבר עברו דרך `tr()`/ternary עובד - ר' תיקון הסורק למטה) ורק אחד היה באג אמיתי: `wizard.html`'s `renderParticipants()` - ה-`aria-label` "הסר {שם}" על כפתור ה-× ליד כל תג משתתף היה מחרוזת עברית קשיחה, בלי שום תלות בשפה
- **שני שינויים, לא אחד**: **א.** ה-`aria-label` עכשיו לפי `currentLanguage === 'he' ? 'הסר' : 'Remove'` - בתבנית ה-ternary של `wizard.html` עצמו (לעמוד הזה אין `tr()` בכלל). **ב.** נוספה קריאה ל-`renderParticipants()` בתוך `changeGlobalLanguage()` - **שינוי התנהגות, לא רק תרגום, מאושר במפורש**: בלעדיה, תגי משתתפים שכבר נוספו היו נשארים עם ה-aria-label הישן עד ההוספה/הסרה הבאה, לא עד החלפת השפה עצמה - כי `renderParticipants()` לא היה נקרא כלל מ-`changeGlobalLanguage()` (אומת בקריאת הפונקציה כולה, שורות 862–946 לפני התיקון)
- **אומת שהקריאה הנוספת לא מאפסת מצב**: `participants` הוא מערך שמות בלבד (`let participants = []`), ו-`renderParticipants()` בונה מחדש `innerHTML` אך ורק ממנו - אין קלט/עריכה בתוך כל תג (רק שם + כפתור הסרה), ואין מצב אחר שיכול ללכת לאיבוד בהחלפת שפה
- נבדק בפועל: **1.** הוספת 3 משתתפים, החלפה לאנגלית - השמות (Dana/Roi/Maya) לא נעלמו, כל ה-`aria-label` עדכנו ל-"Remove X" (נבדק דרך `getAttribute` בקונסול, לא בעין) - וחזרה לעברית מחזירה "הסר X" עם אותם שמות. **2.** דף טרי בלי משתתפים, שתי החלפות שפה - לא קרס, `innerHTML` ריק כצפוי, אין שגיאות קונסולה. **3.** החלפה לאנגלית ואז הוספת משתתף - "Yael" נוספה עם `aria-label="Remove Yael"` מיד, לא בעברית
- `node scripts/i18n-audit.js wizard.html` אחרי התיקון - 0 ממצאים

### 🔧 כלי פיתוח (`scripts/i18n-audit.js`, בלי version bump - קומיט נפרד)
- **תוקן חיובי שגוי שלישי בתור** (אחרי `previewBtn` ב-commit א ו-`langHeBtn`): scan 2 סימן עברית בתוך template string ב-`render*()` בלי לבדוק אם היא כבר ענף של תרגום עובד. נוספה `isTranslationBranch()` - מדלגת על עברית שהיא ארגומנט ל-`tr(` **או** ענף בתנאי `? :` על `currentLanguage`/`lang` (התבנית של `wizard.html`, שאין בו `tr()` בכלל). אומת עם קובץ סינתטי: שני הדפוסים מדולגים, עברית קשיחה אמיתית עדיין נתפסת
- לפני התיקון: 12 מועמדים (8 ב-`shopping.html`, 2 ב-`index.html`, 1 ב-`places.html`, 1 ב-`wizard.html`). אחרי: 1 בלבד (`renderParticipants`) - שתוקן למעלה. `packing.html` נשאר 0

## v4.13.5 — תיקון: 4 aria-label/title שלא תורגמו (commit ב מתוך 3, `scripts/i18n-audit.js`)

### 🐛 תיקון
- **`index.html` (3)**: `#appTitle` (`title`, "לחץ 3 פעמים לכניסת מנהל"), `#adminGearBtn` (`aria-label`, "הגדרות מנהל" - תורגם "Organizer settings", עקבי עם `adminModalTitle` הקיים ולא "Admin settings"), `#forecastStrip` (`aria-label` על הרצועה עצמה - התוכן הדינמי בפנים כבר היה מתורגם, רק התווית של הקונטיינר לא). כל השלוש נוספו כשורות ישירות ב-`applyTripLanguage()`, אותו דפוס כמו `weatherRow`/`.bottom-nav` הקיימים
- **`itinerary.html` (1)**: `#fab` (`aria-label`, "הוספת פעילות") - שורה אחת ב-`applyLanguage()`, משתמשת באותו זוג `tr()` שכבר קיים לכותרת המודאל
- **אלה תיקונים שלא נראים למשתמש** (אף אחד מהארבעה לא טקסט גלוי) - **הבדיקה בוצעה אך ורק דרך `getAttribute` בקונסול, לא בעין**: `index.html` דרך `currentLanguage`/`applyTripLanguage()` הגלובליים, `itinerary.html` דרך hook זמני (`window.__test`) שנוסף, שימש לבדיקה, הוסר, ואומת שהוסר (`grep` ל-`__test` - "No files found"). שתי הפעולות בשתי השפות: אנגלית ואז חזרה לעברית, שתיהן נכונות
- נבדק גם: `node scripts/i18n-audit.js index.html itinerary.html` אחרי התיקון - 0 ממצאים סטטיים (נשארו רק 2 המועמדים הדינמיים של `renderActionBadges()`, שייכים ל-commit ג)

## v4.13.4 — תיקון: 12 טקסטים גלויים שלא תורגמו, ב-3 עמודים (commit א מתוך 3, `scripts/i18n-audit.js`)

### 🐛 תיקון
- **`index.html` (7)**: כל שער "הוספת קישור להתחשבנות" (Spliit) - כותרת, טקסט, קישור, כפתורי שמירה/ביטול - ומצב ריק/כפתור יצירה ב"בחירת טיול" (`tripPicker`, כשלמשתמש כמה טיולים). נוספו כ-7 מפתחות חדשים ל-`dashboardText` (he/en) - הלולאה הגנרית ב-`applyTripLanguage()` שכבר קיימת מיישמת אותם, לא נדרש קוד נוסף
- **`wizard.html` (2)**: `#tripTypeHint` ו-`#checkinHint` - שני רמזי שדה בשלב 1. נוספו `typeHint`/`checkinHint` ל-`translations` (he/en) + שורה לכל אחד ב-`changeGlobalLanguage()`
- **`places.html` (3)**: כל מודאל "לאיזה יום להוסיף" (`dayPicker`) - כותרת, תת-כותרת, וכפתור ביטול. שלוש שורות ב-`applyLanguage()`, אותה תבנית כמו שאר הפונקציה
- **בכוונה לא תוקן, מתועד כדחייה**: 3 כפתורי שפת ייצוא ב-`shopping.html` (`exportHebrewBtn`/`exportEnglishBtn`/`exportGreekBtn`, למשל `"🇮🇱 עברית (Hebrew)"`) - בניגוד לביקורת הידנית הקודמת שסימנה אותם כפער אמיתי, בבדיקה חוזרת הם נראים מכוונים: כל אחד כבר כולל גלוסה דו-לשונית קבועה בסוגריים, בדיוק המוסכמה המקובלת לבורר שפות (כמו אתר שהמתג שלו ממשיך להציג "Français" גם באנגלית) - לא באג, ולכן לא תוקן. יחד עם `langHeBtn` (שכבר ב-`scripts/i18n-ignore.txt`), אלה 4 הדחיות המוצדקות שהתגלו בסבב הזה
- **נבדק בפועל, לפי commit**: **1.** אנגלית בכל 12 המחרוזות - הוחלף בפועל בדפדפן (`index.html`/`wizard.html` דרך הפונקציה הגלובלית שלהם, `places.html` דרך hook זמני שהוסר ואומת שהוסר). **2.** חזרה לעברית - כל 12 חוזרות. **3.** כל 6 העמודים נטענו מחדש ונבדקו לאיתור שגיאות קונסולה - אין חדשות (רק שגיאות הרשת הצפויות בסביבת הבדיקה, ללא אינטרנט אמיתי)
- **תיקון קטן ל-`scripts/i18n-audit.js` עצמו, לפני שהריצה הזו**: הסורק דיווח בטעות על 5 כפתורי "צפה בדוגמה" ב-`wizard.html` כפער - הם כן מתורגמים, רק לא לפי `id` בודד אלא דרך `document.querySelectorAll('.preview-btn').forEach(...)`. נוסף זיהוי כיסוי מבוסס-מחלקה (Pattern D) שתפס את זה; אומת מול `packing.html` (שוב, 0 ממצאים)

## v4.13.3 — תיקון: מודאל "הוספת פריט חדש" נשאר בעברית באנגלית (packing.html)

### 🐛 תיקון
- **הבעיה**: 6 אלמנטים ב-`applyLanguage()` לא היו מכוסים בכלל - לא רק בכותרת המודאל, גם במקומות אחרים בעמוד: 3 טקסטים גלויים (`categorySuggestBtn`, `addItemTitle`, `closeAddItemOverlay`) ו-3 `aria-label` (`addItemFab`, `homeTop`, `themeBtn`)
- **`#addItemFab` (הכפתור שפותח את המודאל) ו-`#addItemTitle` (הכותרת בתוך המודאל) חולקים את אותו טקסט** - "הוספת פריט"/"Add item" - נפתר עם קריאת `tr()` אחת יחידה, ששני האלמנטים משתמשים בתוצאה שלה, לא שני עותקים נפרדים של אותו זוג מחרוזות
- **נבדק במפורש אם `applyLanguage()` יכולה לרוץ לפני שהאלמנטים האלה קיימים בכלל** (חשש שעלה מחקירת ה-warn של `trip` בשלב מוקדם): לא רלוונטי כאן - הסקריפט הראשי הוא `type="module"`, שדוחה ריצה עד אחרי שכל ה-HTML נפרס, וארבעת האלמנטים (`homeTop`, `themeBtn`, `addItemFab`, `addItemTitle`) כולם מופיעים ב-markup **לפני** תג ה-`<script type="module">` - אומת לפי מספרי שורה, לא הונח. לכן לא נוספה הגנת `if (el)` - עקבי עם שאר הפונקציה (~50 שורות, אפס בדיקות הגנתיות קיימות)
- נבדק בפועל: **1.** אנגלית - כל 6 מתורגמים. **2.** חזרה לעברית - כל 6 חוזרים. **3.** שני ה-`aria-label` נבדקו ב-devtools (`getAttribute`), לא בעין. **4.** החלפת שפה בזמן שהמודאל כבר פתוח - הטקסט מתעדכן חי, בלי לסגור ולפתוח מחדש

### 📝 לתיעוד (ר' CLAUDE.md)
- **סקר את אותו דפוס בדיוק על 5 העמודים האחרים** (itinerary, places, wizard, shopping, index) - נמצא בכולם, בהיקפים שונים: itinerary (aria-label אחד), places (מודאל שלם - 3 טקסטים), wizard (2 רמזי שדה, ועוד 3 ממצאים דומים בלי `id`), shopping (3 כפתורי שפה ביצוא), index (8 טקסטים + 3 aria-label/title, בעיקר בשער הקישור ל-Spliit). לא תוקן - החלטה על סדר עדיפויות בהמשך

## v4.13.2 — תיקון: אין דרך לגלול את רצועת ההצעות במחשב (packing.html)

### 🐛 תיקון
- **מידע חדש מבדיקה במכשירים אמיתיים**: התיקון הקודם (v4.13.1) פתר את בעיית הנייד (גוללת אבל בלי סימן) - אבל במחשב הרצועה לא הייתה גוללת **בכלל**, לא רק חסרת סימון. `scrollLeft` שמדדתי בבדיקה הקודמת היה גלילה תכנותית, לא כזו שמשתמש יכול לבצע בפועל
- **אומת לפני התיקון**: גלגלת עכבר רגילה מעל הרצועה לא מזיזה `scrollLeft` בכלל (נבדק ישירות). גרירה עם העכבר לא נתמכת באופן טבעי בדפדפן. לקונטיינר אין `tabindex`, אז מקלדת לא מגיעה אליו. Shift+גלגלת הוא באמת מנגנון דפדפן קיים שממיר גלילה אנכית לאופקית - אבל לא ניתן היה לבדוק אותו אוטומטית בסביבת הבדיקה (כלי האוטומציה לא תומך במקש modifier על גלילה), וגם אם הוא עובד - זו אינטראקציה נדירה שכמעט אף משתמש לא יגלה בלי שיגידו לו
- **`.forecast-strip` (index.html) נבדקה - סובלת מאותה בעיה בדיוק**, לא רק מפער הריפוד שכבר תועד. אין שם שום מנגנון חלופי. תועד ב-CLAUDE.md, לא תוקן - מחוץ להיקף
- **הפתרון שנבחר: סרגל גלילה דק, במחשב בלבד, בלי JS** - `@media (hover:hover) and (pointer:fine)` ולא breakpoint לפי רוחב מסך, כי הסיגנל הנכון הוא סוג המצביע (יש עכבר מדויק?) לא גודל החלון - טאבלט מגע ברוחב גדול לא אמור לקבל את זה, חלון עכברי צר כן. `scrollbar-width:thin` (Firefox) + `::-webkit-scrollbar` מעוצב (Chrome/Safari/Edge), בצבע `var(--line)` - טוקן קיים שכבר משתנה לפי ערכת סוג הטיול ומצב כהה/בהיר, כך שאין צורך בהתאמות נפרדות
- **נשקלה ונדחתה בכוונה: המרת גלילה אנכית לאופקית ב-JS.** חוטפת התנהגות דפדפן מוכרת - משתמש שמגלגל כדי לגלול את העמוד והעכבר נמצא מעל הרצועה, העמוד נעצר במקום. מרגיש שבור גם כשזה מכוון. הבעיה היחידה שדרשה פתרון הייתה "אי אפשר להגיע" - הסרגל פותר אותה במלואה בלי הסיכון הזה
- **קפיצת פריסה שנבדקה במפורש**: השטח ששמור בפועל לסרגל התברר כ-10px (לא 5px כמו שהוגדר מפורש ב-`::-webkit-scrollbar{height:5px}` - הדפדפן הזה מעדיף כנראה את הגודל המובנה של `scrollbar-width:thin` על פני הגובה המפורש). כדי לצמצם את ההבדל, `padding-block-end` יורד ל-0 במחשב (היה 2px). התוצאה: `.quick-picks` גבוה בכ-8px יותר במחשב מאשר בנייד - אין חפיפה או קפיצה, כפתור ה-AI ממשיך לשבת ב-`margin-top:12px` הרגיל שלו מתחת לרצועה, בלי קשר לגובה שלה
- נבדק בפועל: **1.** הגרירה בפועל עובדת - `scrollLeft` השתנה מ-0 ל-(-291) בעקבות גרירת הסרגל עם העכבר, לא רק גלילה תכנותית. **2.** נבדק שהסרגל **נעלם לגמרי** במצב נייד/מגע (`matchMedia` על `hover:hover`/`pointer:fine` מחזיר `false`, `scrollbar-width` חוזר ל-`none`, אפס שטח שמור) - אפס השפעה על חוויית הנייד. **3.** מראה סביר נבדק חזותית במצב כהה (ברירת המחדל) ובשתי ערכות נוספות (`local`, `camping`, מצב בהיר) - הסרגל דק, בצבע התואם לערכה, לא בולט מדי. **4.** קפיצת פריסה - נמדד לפני ואחרי, כפתור ה-AI לא זז ממיקומו היחסי

## v4.13.1 — תיקון: רצועת "מה עוד חסר לך?" נגללת אבל לא מציגה את זה (packing.html)

### 🐛 תיקון
- **הבעיה**: הרצועה כבר הייתה `display:flex; overflow:auto` - גוללת בפועל, לא נחתכת - אבל `padding:10px 1px 2px` השאיר כ-1px ריפוד אופקי בלבד, כך שבסוף הגלילה הפריט האחרון נגע ממש בגבול הקונטיינר. יחד עם סרגל גלילה מוסתר בכוונה (`scrollbar-width:none`), למשתמש לא היה שום רמז שיש עוד לגלול - זה נראה כמו "נחתך", לא כמו "אפשר לגלול עוד קצת". החמיר עם `tripCharacter` כי הרשימות התארכו והחיתוך נהיה שכיח יותר
- **אומת לפני התיקון, לא רק נקרא מה-CSS**: מדידת `scrollWidth`/`clientWidth`/`scrollLeft` בפועל בדפדפן הראתה שהרצועה כן נגללת, כולל כיוון RTL נכון (הפריט הראשון מוצג בימין ב-`scrollLeft:0`, בדיוק כמו שצריך). מנוע הדפדפן הזה משתמש ב-`scrollLeft` **שלילי** לכיוון הסוף ב-RTL - לא כל המנועים זהים בזה, זו בדיוק הסיבה ל-`Math.abs()` בלוגיקת ההצללה למטה
- **שלושה תיקונים**: **א.** ריפוד אמיתי בסוף הרצועה - גם `padding-inline` לוגי (מתהפך אוטומטית RTL/LTR) וגם spacer אמיתי כ-flex child (לא ריפוד CSS בלבד - ריפוד בקצה קונטיינר גלילה לא תמיד מכובד עד הסוף בכל מנוע). **ב.** `flex-shrink:0` מפורש על `.pick`. **ג.** הצללה עדינה בקצה ה"רחוק" (`mask-image`, עם `-webkit-mask-image` ל-Safari) שמוצגת רק כשיש עוד לגלול (`.has-more`, מחושב ב-JS) ונעלמת בפועל בהגעה לסוף האמיתי - לא הצללה קבועה. כיוון ההצללה **הפוך** בין השפות בכוונה, לא זהה-עם-flip: הקצה ה"רחוק" הוא שמאל בעברית וימין באנגלית, אז `[dir="rtl"]`/`[dir="ltr"]` מגדירים כל אחד את כיוון ה-gradient שלו בנפרד
- **`-webkit-overflow-scrolling:touch` נבדק ולא נוסף בכוונה** - תכונה מיושנת שממילא לא משפיעה על שום דבר החל מ-iOS 13 (momentum scrolling אוטומטי כבר שנים). מתועד בקוד כדי שלא "יתוקן" בחזרה
- **באג אמיתי שנתפס תוך כדי בדיקה, לא רק תוכנן**: מעבר בין קטגוריות (`gear`→`clothing` וכו') לא איפס את מיקום הגלילה - רשימה קצרה שנפתחה אחרי רשימה ארוכה יכלה לרשת מיקום גלילה שגוי, וכתוצאה מכך גם מצב הצללה שגוי. `renderQuickPicks()` מאפס עכשיו `scrollLeft` בכל רינדור מחדש
- **בכוונה לא נגעו**: `.forecast-strip` (index.html) - סובלת מאותו פער ריפוד-בסוף בדיוק, מתועד ב-CLAUDE.md, לא בהיקף המשימה הזו
- נבדק בפועל: **1.** גלילה במגע - **לא נבדק על מכשיר אמיתי בפועל** (לא זמין בסביבת הבדיקה) - נבדק בקירוב הכי טוב הזמין: viewport נייד מדומה (375px) + מדידת scroll מציאותית, לא רק קריאת CSS; מומלץ לוודא במכשיר אמיתי בנוסף. **2.** ההצללה מופיעה עם רשימה גולשת (`abroad`+`ski`, 9 פריטים) ונעלמת בהגעה לסוף האמיתי - נבדק ונמדד בשתי השפות, כולל אימות כיוון ה-gradient המחושב בפועל (`to left` ב-RTL, `to right` ב-LTR). **3.** רשימה קצרה שנכנסת במלואה (`reserve`/`uniform`, 3 פריטים, `scrollWidth===clientWidth` בדיוק) - אין `has-more` בכלל. **4.** צילומי מסך בהקשר עמוד מלא, בעברית ובאנגלית, בשני מצבים (עם הצללה / בסוף גלילה בלי הצללה) - הפריט האחרון מלא ונגיש עם רווח אמיתי (32-33px) בשתי הבדיקות

## v4.13.0 — tripCharacter משפיע על קטגוריות הציוד (שלב 3ב מתוך docs/schema-legs.md, packing.html בלבד)

### ✨ תכונה חדשה
- **`starterLists()` מקבל ממד שלישי**: אחרי `tripType` (קטגוריות) ו-`vibes` (פריטים ל-`clothing`), עכשיו גם `tripCharacter` מוסיף פריטים לקטגוריות **קיימות** - לא יוצר קטגוריות חדשות. שבעה אופי, כל הפריטים ל-`gear` חוץ מ"תלבושות למסיבות" (`pride`/`bachelor`) שנכנס ל-`clothing`
- **`friends`/`couple` ריקים בכוונה, מתועד בקוד** - טיול חברים הוא ברירת המחדל של האפליקציה, וטיול זוגי לא שונה בציוד. הערה בקוד מפורשת נגד "תיקון" עתידי
- **חל על כל 4 סוגי הטיול**, לא רק `abroad`/`local` כמו `vibes` - נבדק במפורש שההגבלה של `vibes` לא הועתקה
- **מיזוג, לא דריסה** - בניגוד ל-`vibes` (ר' "לתיעוד" למטה), `tripCharacter` קורא את הפריטים הקיימים בקטגוריה ומוסיף אליהם, לא מחליף. רץ **אחרי** בלוק ה-`vibes` הקיים בכוונה, כדי ששני המנגנונים באמת יפעלו במקביל: תרומת `tripCharacter` שורדת גם כשל-`vibes` יש מה להגיד על אותה קטגוריה
- **Dedup בין-קטגורי, לא רק מול קטגוריית היעד**: פריט שכבר קיים בכל קטגוריה אחרת של אותו `tripType` (למשל "שק שינה" שכבר ב-`shelter` של קמפינג) לא נוסף שוב ל-`gear` רק כי שם הטבלה שמה אותו
- **טבלת שמות נרדפים קטנה, ל-dedup בלבד** (`DEDUP_SYNONYMS`, 5 ערכים) - "פאוור בנק"/"מטען"/"מטען נייד"/"power bank"/"charger" מזוהים כאותו פריט אמיתי, בלי לשנות את השם המוצג. **חריג מכוון**: "מטען נייד נוסף"/"Extra power bank" (`family`) לא בטבלה - עם ילדים באמת רוצים שניים, אז הוא תמיד נוסף גם כשכבר יש מטען. זוגות נוספים שנשקלו ונדחו (מתועד בקוד, לא בטבלה): "מטען לטלפון"/"Phone charger" (כבר מופיע יחד עם "פאוור בנק" כשני פריטים נפרדים ברשימת `abroad` הקיימת - הוכחה שהם לא נחשבים לאותו דבר), "תרופות אישיות" ו"כדורי כאב ראש" מול "תרופות" הגנרי (נקראים כתוספות מכוונות, לא כשכפול, אותה צורה בדיוק כמו חריג המטען)
- **`reserve` בלי `clothing`** - אומת שזה אכן קורה בפועל (היחיד מבין 4 סוגי הטיול). טיול `reserve` עם `pride`/`bachelor` מדלג בשקט על "תלבושות למסיבות"; כל שאר הפריטים ל-`gear` נוספים כרגיל
- **התנגשות אמיתית שנתגלתה בבדיקות, נפתרה לפי הכרעה מפורשת**: `ski` (יש לו "קרם הגנה" ב-`gear`) + vibe `beach` (מכניס "קרם הגנה" ל-`clothing`) - ה-dedup הבין-קטגורי מזהה שהפריט כבר קיים ומוותר על ההוספה החוזרת ל-`gear`. **זו התנהגות נכונה, לא כשל**: המשתמש רואה רשימת ציוד אחת, לא ארבע נפרדות - פריט שמופיע פעם אחת בכל קטגוריה השלים את המשימה; אותו פריט פעמיים היא בדיוק הכפילות שתוקנה בהצעות ה-AI (v4.11.7-v4.11.9)
- **בכוונה לא נגע**: `payload` ל-Cloud Function של הצעות ה-AI - `tripCharacter` יגיע לשם בשלב נפרד

### 📝 לתיעוד (ר' CLAUDE.md)
- סיווג שגוי בטבלת `vibeClothing`: "קרם הגנה"/`Sunscreen` (beach) ו-"תרמיל גב"/`Backpack` (nature) הם ציוד, לא ביגוד - זה השורש להתנגשות שתוארה למעלה. לא תוקן

### נבדק בפועל, לא רק נקרא
כל הבדיקות הורצו גם כפונקציות טהורות (Node, כולל snapshot מלא של כל 8 צירופי סוג-טיול/שפה לפני השינוי) וגם מול `starterLists()` האמיתית בדף (`window.__test` זמני - הוסר ואומת שהוסר): **1.** `abroad`+`ski` - 5 פריטים ב-`gear`, בלי כפילות. **2.** `abroad`+`pride` - 5 ב-`gear`, "תלבושות למסיבות" ב-`clothing` בלבד. **3.** `friends` - זהה בייט-לבייט לברירת המחדל. **4.** בלי `tripCharacter` בכלל - זהה לברירת המחדל בכל 8 הצירופים (סוג טיול × שפה) - **הבדיקה הקריטית**, מכסה כל טיול קיים. **5.** אנגלית - כל הפריטים מתורגמים, כולל dedup נכון ("Power bank" מופיע פעם אחת). **6.** `ski`+vibe `beach` יחד - 4 פריטי ski שלא מתנגשים ב-`gear`, "קרם הגנה" ב-`clothing` בלבד דרך ה-vibe (ר' ההכרעה למעלה). **7.** `camping`+`festival` - עובד גם שם; "שק שינה" ו"פנס" לא מוכפלים ל-`gear` (כבר ב-`shelter`)

### ✨ תכונה חדשה
- **שדה חדש `tripCharacter`** בוויזרד - בחירה יחידה מתוך 7 (`family`/`friends`/`couple`/`ski`/`pride`/`bachelor`/`festival`), `<select>` מתחת לשדה סוג הטיול, עם אופציה ריקה ראשונה "לא צוין". `<select>` נבחר בכוונה, לא צ׳יפים - הוויזרד עומד לעבור עבודה גדולה בשלב 10 (קלט רגליים), ואין טעם לבנות רכיב חדש שישונה בקרוב; `<select>` מטפל ב-7 אופציות כמו שהוא
- **המפתח באנגלית נשמר, התצוגה תמיד דרך תרגום** - בדיוק כמו `tripType`. אין מחרוזת עברית בערך שנכתב
- **השדה אופציונלי, ובאמת אופציונלי**: כשלא נבחר - `tripCharacter` לא נכתב בכלל לאובייקט שנשלח ל-Firebase, לא כמחרוזת ריקה. מימוש: `...(tripCharacter ? { tripCharacter } : {})` בתוך אובייקט היצירה
- **`resetWizard()` מאפס גם אותו** - נבדק במפורש, כי בדיוק בפונקציה הזו כבר היה באג בעבר (v4.10.x לערך): לחיצה על "טיול חדש" מהצלחה בלי רענון השאירה מצב ישן (`selectedVibes` לא מתנקה וכו')
- **`saveWizardDraft()`/`restoreWizardDraft()` (שמירת מצב הטופס לפני/אחרי redirect של Google Sign-In) כוללות גם אותן, באותה תבנית בדיוק כמו `tripType`** - זוהה תוך כדי העבודה, לא כבאג נפרד אלא כחלק מהחיים המלאים של השדה: שדה שנשמר ומתאפס אבל לא שורד redirect הוא מיושם חלקית. משתמש שבוחר אופי ואז מתחבר עם Google היה מאבד אותו בשקט אלמלא זה
- **ביקורת מלאה**: כל שדה אחר בטופס שנבדק מול `saveWizardDraft()` כבר נשמר נכון (`tripDestination`, `tripType`, `vibes`, `tripStart`/`tripEnd`, `checkinUrl`, חמשת ה-`feat*`). נמצא פער אחד נוסף, **שלא תוקן** (מחוץ להיקף): `participantName` - תיבת הטקסט של שם חבר קבוצה *לפני* לחיצה על "הוסף" - לא נשמרת; רק שמות שכבר אושרו ל-`participants` שורדים redirect. זה פער מבודד (הדפוס היחיד בטופס של "הקלדה ואז אישור בכפתור נפרד"), לא דפוס רחב - כל שאר השדות הם ערכים ישירים שכבר נשמרים
- **בכוונה מחוץ להיקף**: `starterLists()`, `packing.html`, וה-payload ל-Cloud Function של הצעות ה-AI - לא נגעו. זה שלב נפרד (3ב), אחרי שהשפעת `tripCharacter` על קטגוריות ציוד תיקבע בנפרד
- נבדק בפועל: **1.** מבנה השדה - 8 אופציות (ריקה + 7), ברירת מחדל ריקה. **2.** מעבר שפה - התווית ושמונת האופציות מתורגמות נכון בשני הכיוונים. **3.** `resetWizard()` - נבדק ישירות: `tripCharacter` נקבע ל-`'ski'`, `resetWizard()` נקרא, הערך חוזר ל-`''`. **4.** בניית האובייקט - הביטוי המדויק מ-`generateTripAndSave()` (`...(tripCharacter ? {tripCharacter} : {})`) הורץ נגד קריאת ה-DOM האמיתית: עם בחירה - המפתח קיים עם הערך האנגלי (`'ski'`); בלי בחירה - המפתח נעדר לגמרי מהאובייקט. **5.** טיוטה: `tripCharacter` נקבע ל-`'pride'`, `saveWizardDraft()` נקרא (נבדק גם שהטיוטה השמורה בפועל מכילה `tripCharacter:'pride'`), השדה נוקה ידנית ל-`''`, `restoreWizardDraft()` נקרא - הערך חוזר ל-`'pride'`. **לא נבדק קצה-לקצה מול Firebase בפועל** - `generateTripAndSave()` דורש `requireOrganizer()` (Google Sign-In אמיתי), לא זמין בסביבת הבדיקה, כמו בכל משימה קודמת הפעם

### 📝 לתיעוד (ר' CLAUDE.md)
`--glow-color` משוכפל ב-4 מקומות עצמאיים (shared.css + שלושה בלוקי `<style>` inline נפרדים) ולא מוגדר במקום אחד - שכבת המבטא העתידית (`--accent-gradient`, §3.4) תצטרך את אותו שכפול

## v4.11.9 — פרומפט הצעות AI: אין תוארי קישוט מיותרים (functions/index.js)

### 🐛 תיקון
- **סבב שלישי בבדיקת פרודקשן**: אחרי v4.11.7+v4.11.8, 0 כפילויות זוהו על ידי הסינון - אבל נשארו זוגות כמו "מטרייה מתקפלת קלה"/"מטרייה מתקפלת קומפקטית" ו"מעיל מעבר עמיד למים"/"מעיל מעבר עמיד לגשם". הפיצול במילה האחרונה - שני הפריטים חולקים בסיס ונבדלים בתואר, ואף אחד אינו תחילית של השני, כך שאין כלל טקסטואלי שיכול לתפוס את זה. השורש: המודל ניצל את התקרה של "2-4 מילים" במקום לשאוף לרצפה - "קלה"/"קומפקטית" הם קישוט, לא מידע
- **שני שינויים בפרומפט בלבד, שום דבר אחר**: **א.** "2-4 מילים" → **"2-3 מילים, עדיפות ל-2"**. **ב.** פסקה חדשה: אל תוסיף תואר אלא אם הוא באמת מבחין בין הפריט לפריט אחר ברשימה - אם השם החשוף כבר מספיק כדי לדעת מה לארוז, לעצור שם. עם דוגמאות טובות/רעות משלה (לא אותה דוגמה כמו פסקת האורך - זו ממוקדת בתוארי קישוט ספציפית, לא באורך כללי)
- **הדוגמאות "רעות" נבחרו כך שיהיו חד-משמעיות**: הניסוח הראשון ("waterproof rain jacket") היה בעצמו בתוך טווח 2-3 המילים, מה שהפך אותו למבלבל כדוגמת "רע" - הוחלף ב"lightweight compact umbrella"/"waterproof windproof jacket" (שני תוארים מוערמים, חריגה ברורה). הדוגמאות בעברית ("מטרייה מתקפלת קלה"/"מעיל מעבר עמיד למים") נשארו כמו שהוצעו
- **`generationConfig` לא נגע** - `temperature: 0.7`, `maxOutputTokens: 2000`, `responseMimeType: "application/json"` זהים. `packing.html` לא נגע בכלל - זה תיקון פרומפט בלבד
- נבדק: הפרומפט המלא הורץ ב-Node עבור עברית ואנגלית, אומת שהדוגמאות תואמות שפה בשתי הפסקאות (אורך ותוארים) ושה-`generationConfig` לא השתנה. **לא נבדק מול Gemini עצמו עד לפריסה** - חוזה חיצוני; `firebase deploy --only functions` באישור נפרד

## v4.11.8 — רשת ביטחון: סינון כפילויות-תחילית בהצעות AI (packing.html)

### 🐛 תיקון
- **commit ב' משניים** (אחרי v4.11.7). בדיקה בפרודקשן של תיקון הפרומפט הראתה: הפריטים קצרים עכשיו, אבל נשארו כפילויות מסוג חדש - תחילית מדויקת ברמת מילים: `"צעיף קל"` / `"צעיף קל לסתיו"`, `"מטרייה מתקפלת"` / `"מטרייה מתקפלת קלה"`. בשונה מהמקרה הקודם (`"מעיל מעבר"` מול `"מעיל סתווי"`) - זה כן ניתן לתפיסה טקסטואלית
- **כלל שלישי, ב-`renderAiSuggestions`**: פריט נחשב כפילות אם אחרי נירמול המילים שלו הן תחילית מדויקת של פריט אחר - ברמת **מילים שלמות**, לא תווים. `"מעיל"` לא תופס את `"מעילים"` או `"מעילון"` (מחרוזות שונות לגמרי במערך המילים, לא substring). כש-2 פריטים מזוהים ככפילות - **נשמר הקצר**, ללא תלות בסדר ההגעה מה-API (אם הארוך מגיע ראשון במערך, הוא עדיין מוחלף בקצר כשהקצר מגיע)
- **החלטה מפורשת על המקרה שביקשת שאשקול**: פריט של מילה אחת לא נכנס לבדיקת התחילית בכלל - רק להתאמה מדויקת. הנימוק: מילה בודדת גנרית (`"מעיל"`) היא סיגנל חלש מדי מול פריט ספציפי יותר (`"מעיל גשם"`) - הם עלולים להיות שני דברים שהמשתמש הוסיף בכוונה, לא אותו פריט. הכלל דורש שהצד הקצר יהיה **לפחות 2 מילים**
- **הרחבת סינון מול הרשימה הקיימת**: היה בודק רק `currentItems()` (הקטגוריה הפעילה), עכשיו בודק את **כל** הרשימות של המשתמש (`Object.values(items)` על פני כל ה-lists, לא רק `activeList`) - הצעה שכבר קיימת ברשימה *אחרת* מסוננת גם היא
- **נירמול, בסדר הנכון**: הסרת סוגריים ותוכנם → צמצום רווחים כפולים → trim → lowercase (בניגוד לניסוח הקודם שהציע סדר שגוי - `"מטען (נייד) חזק"` היה משאיר רווח כפול)
- **אבחון בקונסול, לא חסימה**: `console.log` כשמשהו סונן בפועל - כמה מול הרשימה הקיימת וכמה ככפילות בתוך המנה עצמה (מתוך כמה שהוצעו) - כדי לדעת אם הפרומפט לבד מספיק או שהרשת עושה את העבודה. `console.warn` (לא חוסם) אם אחרי הסינון נשארו פחות מ-3 הצעות - סימן שמשהו למעלה בשרשרת עלול להיות לא תקין
- נבדק בפועל, לא רק נקרא: **הזוגות האמיתיים מהפרודקשן** - `"צעיף קל"`/`"צעיף קל לסתיו"` ו-`"מטרייה מתקפלת"`/`"מטרייה מתקפלת קלה"` - מזוהים נכון ככפילות. **הזוגות שאסור שייתפסו** - `"מטען נייד"`/`"מטען חשמלי"` ו-`"צעיף קל"`/`"כפפות קלות"` - לא מזוהים ככפילות. בדיקות נוספות: `"מעיל"` מול `"מעילים"`/`"מעילון"` (לא נתפס, ברמת מילים לא תווים), סדר הגעה הפוך (הארוך קודם) - עדיין נשמר הקצר, נירמול סוגריים תקין. כל הבדיקות הורצו גם כפונקציות טהורות ב-Node וגם מול הפונקציה האמיתית בדף עצמו (`window.__test` זמני - הוסר ואומת שהוסר), כולל תרחיש מלא: מנת AI עם כפילויות + פריט שכבר קיים ברשימה לא-פעילה → הצ'יפים שנוצרו, ה-`console.log`, וה-`console.warn` כולם תואמים למצופה

## v4.11.7 — פרומפט הצעות AI לציוד: שמות קצרים, בלי כפילויות סמנטיות (functions/index.js)

### 🐛 תיקון
- **הבעיה**: הצעות ה-AI לרשימת הציוד ייצרו כפילויות סמנטיות - אותו פריט אמיתי בכמה ניסוחים שונים באותה תשובה (למשל "מעיל מעבר עמיד למים ולרוח" + "מעיל סתווי עמיד לרוח ולגשם", או שלוש גרסאות של "מטען נייד"). מדוגמה אמיתית: 9 פריטים, 8 מהם ארבעה זוגות/שלישיות של אותו פריט. סינון טקסטואלי לא יכול לתפוס את זה - אלה מחרוזות שונות לגמרי, לא שגיאות כתיב
- **השורש היה בפרומפט**, לא בסינון: הוא ביקש "specific items" עם דוגמה אחת שהיא כבר משפט ("power adapter for [region] outlets"), ולא הגביל אורך ולא אמר במפורש לא לחזור על אותו פריט בניסוח שונה
- **שלושה תיקונים לפרומפט**: **א.** אילוץ אורך מפורש - כל פריט הוא שם של 2-4 מילים, לא תיאור, עם דוגמאות טובות/רעות (כולל הדוגמה האמיתית "מטען נייד חזק ליום שלם מחוץ למלון" כדוגמה ל"רע"). **ב.** הדוגמאות תלויות-שפה - כש-`language` הוא `en` כל הדוגמאות באנגלית בלבד, וכש-`he`/ברירת מחדל כולן בעברית בלבד. **לא** דוגמאות דו-לשוניות מעורבות באותה הוראה - המודל מתבקש לענות בשפה אחת (`Respond in ${lang}`), ודוגמאות מעורבות עלולות לגרום לו לערבב שפות בפועל. **ג.** הוראה מפורשת: כל פריט הוא צורך נפרד, אל תציע את אותו פריט בניסוחים שונים
- **`generationConfig` לא נגע** - `maxOutputTokens: 2000`, `responseMimeType: "application/json"` ו-`temperature: 0.7` נשארו בדיוק כמו שהיו. פריטים קצרים יורידו את הצריכה, לא יעלו אותה
- **מכוון**: זהו commit א' משניים. commit ב' (רשת ביטחון בסינון ב-`packing.html`) מגיע אחריו בכוונה, כדי שאפשר יהיה לבדוק אם התיקון בפרומפט לבד פותר את הבעיה לפני שהרשת מסתירה את התוצאה
- נבדק: תבנית הפרומפט הורצה ב-Node עם קלט עברית ואנגלית, אומת ידנית שהדוגמאות תואמות שפה בכל מקרה ושה-`generationConfig` לא השתנה. **לא נבדק מול Gemini עצמו** - זה חוזה חיצוני; דורש `firebase deploy --only functions` שלא בוצע, ממתין לאישור נפרד

## v4.11.6 — legs.js: האזהרה על lat/lon חסר שותקת בטיול שעוד לא נטען

### 🐛 תיקון
- **הבעיה**: ה-`console.warn` שנוסף ב-legs.js (v4.11.2) ירה על **כל** טעינת עמוד ב-`itinerary.html`, `places.html` ו-`packing.html` - לא רק על טיול אמיתי עם נתון חסר. הסיבה: שלושת העמודים האלה מריצים "תיקון הבזק" קיים - אם יש שפה שמורה ב-localStorage, הם קוראים `applyLanguage()` (וב-`places`/`itinerary` גם פונקציות נוספות בשרשרת) **באופן סינכרוני, מיד בטעינה, לפני שה-Firebase עונה** - על אובייקט `trip` כמעט ריק (`{language:"he"}` בלבד). זה דפוס קיים משכבר, לא רגרסיה - `trip.destination` היה `undefined` שם גם לפני שהיה `legs.js` בכלל, פשוט בשקט. האזהרה החדשה נתנה לזה קול, ונמדד בפועל: 3 פעמים ב-`places.html`, 2 ב-`packing.html`, ו-**6** ב-`itinerary.html` (יותר מהצפוי - `applyLanguage()` שם משרשרת גם ל-`renderDayTabs()`→`tripDayDates()` וגם ל-`renderActivities()`→`computeLiveActivityId()`, וכל אחת קוראת ל-`getLastLeg()` בנפרד). אזהרה שיורה בכל טעינת עמוד היא אזהרה שמתעלמים ממנה - בדיוק ההפך ממה שהיא נועדה לעשות
- **התיקון, ב-`getLegs()` בלבד** - שום דבר לא השתנה בארבעת העמודים עצמם: האזהרה יורה עכשיו רק כש-`trip.destination` **קיים** וגם `lat`/`lon` **חסרים** - טיול אמיתי עם נתון חסר, בדיוק המקרה שרצינו לתפוס. אובייקט בלי `destination` (בין אם ריק לגמרי `{}` ובין אם `{language:"he"}` כמו במקרה שתואר למעלה) מתפרש כ"עוד לא נטען" ולא מייצר אזהרה. בכוונה **לא** נבדק לפי `trip.language` או שדה אחר - אובייקט לפני-טעינה יכול להיראות שונה בעמוד אחר; `destination` הוא המשותף לכולם
- **ערך ההחזרה של `getLegs()` לא השתנה בכלל** - רק מתי מודפסת האזהרה. טיול בלי `lat`/`lon` עדיין מחזיר רגל עם `lat`/`lon` בתור `undefined`, בדיוק כמו היום
- נבדק בפועל, לא רק נקרא: **1.** שלושת העמודים (`places`, `packing`, `itinerary`) נטענו בפועל בדפדפן עם `langKey` שמור ב-localStorage - אומת ש"תיקון ההבזק" אכן רץ (הכותרת הציגה placeholder ריק, "•", בדיוק כמו לפני התיקון) - ואפס אזהרות בקונסול, בטאב נקי לגמרי כדי לשלול שאריות מבדיקות קודמות. **2.** טיול אמיתי טעון (כל שבעת השדות) - אפס אזהרות. **3.** אובייקט מזויף עם `destination` בלי `lat`/`lon` - האזהרה יורה, פעם אחת - זה המקרה שהאזהרה קיימת בשבילו. **4.** אובייקט ריק לגמרי `{}` - אפס אזהרות, ומחזירה רגל אחת עם שדות `undefined`, זהה למה שהיה לפני התיקון. כל הבדיקות הורצו זהות בשלושת העמודים

## v4.11.5 — קריאות destination/endAt דרך getLegs ב-packing.html (אפס שינוי התנהגות)

### ✨ תכונה חדשה
- **שלב 2ב-ב מתוך docs/schema-legs.md**: `packing.html` בלבד, מבודד מהקומיט הקודם (itinerary/places/shopping) כי שני מתוך ארבעת אתרי הקריאה כאן הם ה-payload ל-Cloud Function של הצעות ה-AI (`requestAiSuggestions()`) - חוזה חיצוני, לא תצוגה פנימית. אם משהו שם נשבר, רוצים שזה יהיה מבודד לקומיט משלו
- **אותו מיפוי כמו בקומיטים הקודמים**: `destination` ← `getPrimaryLeg()`; `endAt`/`endDate` ← `getLastLeg()`; `startAt`/`startDate` נשאר נקרא ישירות מ-`trip.startAt` - כולל בתוך ה-payload ל-AI (`startDate: trip.startAt`, ללא שינוי)
- **ארבע נקודות עדכון**: `applyTheme()` (מטמון localStorage), `applyLanguage()` (`document.title` + `#destination`), `requestAiSuggestions()` (ה-payload שנשלח ל-Cloud Function), וכתיבת הסיכום ל-`users/{uid}/trips/{tripId}` (אותו דפוס כמו בשלושת העמודים הקודמים)
- נבדק בפועל, לא רק נקרא, עם אותו דפוס `window.__test` זמני (הוסר ואומת שהוסר לפני ה-commit): **א.** טיול אמיתי (ברלין) - `document.title`, `#destination`, וה-**payload המלא** ל-AI הודפסו לפני ואחרי והושוו מול השדות הגולמיים, זהים בייט-לבייט. **ב.** טיול מזויף עם 2 רגליים ו-timezone/`endAt` שונים (רומא/ניו יורק) - הכותרת הציגה את הרגל הראשונה (רומא), ה-payload ל-AI הכיל `destination:"רומא"`, `startDate` זהה ל-`trip.startAt` הגולמי (ללא שינוי), ו-`endDate` מהרגל **האחרונה** (20/10, לא 9/10 של הרגל הראשונה) - אותה בדיקה שתפסה את הטעות הפוטנציאלית ב-2א. **ג.** אין שגיאות קונסול חדשות מעבר לכשלי הרשת הצפויים בסביבת הבדיקה (ושתי שגיאות `console.error` צפויות מיירוט ה-`fetch` המכוון בבדיקה עצמה, כדי לתפוס את ה-payload בלי לפגוע ברשת אמיתית)

## v4.11.4 — קריאות destination/lat/lon/timezone/endAt דרך getLegs (itinerary/places/shopping, אפס שינוי התנהגות)

### ✨ תכונה חדשה
- **שלב 2ב-א מתוך docs/schema-legs.md**, שלושה עמודים מודולריים: `itinerary.html`, `places.html`, `shopping.html`. `packing.html` בכוונה **לא** בקומיט הזה - ה-payload שלו ל-Cloud Function מבודד בקומיט נפרד (חוזה חיצוני, לא תצוגה פנימית)
- **המיפוי, זהה לשלב 2א**: `destination`/`lat`/`lon` ← `getPrimaryLeg()`; `timezone`/`endAt` ← `getLastLeg()`; `startAt` נשאר נקרא ישירות מ-`trip.startAt`/`tripData.startAt` - לא עובר דרך `getLegs`, בדיוק כמו ב-index.html, ומאותה סיבה (§4 במסמך)
- **מימוש: קריאה מפורשת בכל נקודת שימוש** (`window.getPrimaryLeg(trip)`/`window.getLastLeg(trip)`), לא מוטציה על אובייקט ה-`trip`/`tripData` המשותף. שלוש האפשרויות שנשקלו ולמה זו נבחרה: מוטציה (אפשרות ג) הייתה מייצרת אובייקט שלא משקף את ה-snapshot מ-RTDB - סותר ישירות את הכלל ב-§8ב "הקוד הקיים הוא מקור האמת על מבנה הנתונים" - ובנוסף `onValue` יורה בכל שינוי בתת-עץ הטיול, כך שהדריסה הייתה חוזרת שוב ושוב ויוצרת invariant סמוי. משתנה מודול חדש (אפשרות א) היה דורש את אותו מספר שורות בדיוק בלי היתרון של הצהרה מקומית בכל אתר שימוש
- **כל שלושת העמודים התבררו כבעלי נקודת חנק יחידה** לטעינת ה-snapshot: `itinerary.html:115` ו-`places.html:176` (`trip=s.val()` בתוך `listen()`), ו-`shopping.html:2766` (`tripData = data || {}` בתוך `syncTripUI()`, הנקראת מ-`onValue` ב-2930) - אבל בניגוד ל-`index.html`, אין שכבת ביניים של משתנים גלובליים מנורמלים, אז ההחלפה עדיין נגעה בכל אתר קריאה בנפרד, לא רק בנקודת החנק
- **גילוי אמיתי, לא רק בדיקה**: `places.html:113` (`GEO_RULES`, `t=>!isNearCoast(t.lat,t.lon)`) מתברר להיות קריאת `trip.lat`/`trip.lon` אמיתית - `t` שם הוא אובייקט הטיול עצמו (`forTrip`), לא נקודת מקום. עודכנה בהתאם, בשתי נקודות הקריאה ב-`categories()`
- **אין fallback ל-`flightOut`/`tripEnd`/`flightBack` בשלושת העמודים האלה** (בניגוד ל-`index.html`) - כל שלושתם קוראים רק `startAt`/`endAt` ישירות, ללא שכבת תאימות לשמות ישנים. אומת בחיפוש מקיף לפני הכתיבה
- **תשומת לב מיוחדת ל-`tripDayDates()`** ב-`itinerary.html` (שורה 99) וב-`places.html` (שורה 168): `endAt` עובר ל-`getLastLeg`, `startAt` נשאר. בטיול רגל אחת אין הבדל בערך בין הישן לחדש - **בדיוק לכן קל לכתוב את זה לא נכון בלי לשים לב** - אומת במפורש בבדיקה עם טיול מזויף בעל שתי רגליים ו-timezone/endAt שונים ביניהן
- **הבאג הידוע ב-`tripDayDates()` של `places.html`** (בלי נרמול timezone, בניגוד לגרסה של `itinerary.html`) - לא נגע, מחוץ להיקף כפי שהוחלט קודם
- **`wizard.html` לא נגע בכלל** - נבדק ואומת שאין בו שום קריאה לשדות אלה מטיול קיים/טעון; כל השימושים שם הם כתיבה של טיול חדש (מהטופס + geocoding) או קריאת תצוגה מאינדקס-סיכום שאף פעם לא שמר lat/lon/timezone מלכתחילה. שייך במלואו לשלב 10
- נבדק בפועל על שלושת העמודים, לא רק נקרא: לכל עמוד הוצמד זמנית `window.__test` שחשף את `trip`/`tripData` ואת הפונקציות הרלוונטיות (הוסר ואומת שהוסר לפני ה-commit): **א.** טיול אמיתי (ברלין) - כל ערך שהוחלף (`document.title`, `#destination`/`#destinationKicker`, `tripDayDates()`) הודפס והושווה מול השדה הגולמי המקורי, זהה בייט-לבייט; ב-`places.html` גם `GEO_RULES` אומת ישירות (טיול פנים-יבשתי מסתיר "חוף", טיול חופי לא). **ב.** טיול מזויף עם 2 רגליים ו-timezone/`endAt`/קואורדינטות **שונים** בכל רגל (רומא/ניו יורק) - הכותרת ואלמנט היעד הציגו את הרגל הראשונה (רומא) ולא האחרונה, `tripDayDates()`/מספר הימים שיקפו נכון את ה-`endAt` של הרגל **האחרונה** (15 ימים, לא 4 שהיו מתקבלים אילו נשאר על הרגל הראשונה), וב-`places.html` הקואורדינטות של הרגל הראשונה (ברלין) קבעו את הסתרת "חוף" למרות שהרגל השנייה (תל אביב) חופית. **ג.** ב-`shopping.html` הודפס גם ה-payload המלא שנכתב ל-`users/{uid}/trips/{tripId}` (`destination`/`startAt`/`endAt`) לפני ואחרי - התאמה מלאה למצופה. **ד.** אין שגיאות קונסול חדשות מעבר לכשלי הרשת הצפויים בסביבת הבדיקה בכל שלושת העמודים

## v4.11.3 — hydrateTripData ניזונה מ-getLegs (index.html, אפס שינוי התנהגות)

### ✨ תכונה חדשה
- **שלב 2א מתוך docs/schema-legs.md**: `hydrateTripData()` (index.html) - נקודת החנק היחידה שמזינה 30+ פונקציות דרך משתני מודול גלובליים - עכשיו שואבת שבעה מהם (`destinationName`, `latCoord`, `lonCoord`, `hasRealCoords`, `tripTimezone`, `tripEnd`) מ-`getPrimaryLeg()`/`getLastLeg()` במקום ישירות מ-`data`. **אף צרכן לא נגע** - כל 30+ הפונקציות ממשיכות לקרוא את אותם משתנים בדיוק כמו קודם
- **מיפוי, לפי §4 במסמך הסכמה**: `destinationName`/`latCoord`/`lonCoord`/`hasRealCoords` מהרגל **הראשונה**; `tripTimezone`/`tripEnd` מהרגל **האחרונה**. `flightOut` נשאר קורא `trip.startAt` **ישירות, לא דרך `getLegs`** - זה שעון המכשיר של יוצר הטיול, לא שעון היעד, שני דברים שונים סמנטית גם אם הערך זהה בטיול רגל אחת
- **פער תאימות אמיתי שנתפס לפני כתיבת קוד, לא אחריה**: `getLegs()` מכירה רק `trip.startAt`/`trip.endAt` - לא את השמות הישנים `flightOut`/`tripEnd`/`flightBack` שה-`hydrateTripData` הקודמת נפלה אליהם. תוקן בנרמול על **עותק רדוד** לפני הקריאה ל-`getLegs` (`data` המקורי לעולם לא משתנה): אם חסר `startAt` יש נפילה ל-`flightOut`, אם חסר `endAt` יש נפילה ל-`tripEnd`/`flightBack`. כשהנפילה מופעלת בפועל - `console.warn` עם ה-`tripId`, כדי לדעת אם יש טיול אמיתי שעדיין תלוי בשמות הישנים
- **`hydrateTripData` רצה על כל שינוי בתת-עץ הטיול**, לא פעם אחת (מאזין חי על `tripRoot`) - אומת בקוד, לא רק בהנחה. `getLegs()` זולה מספיק שזה בסדר, ובכוונה **לא** נוספה memoization - זה שיפור אגב, וזה commit של אפס שינוי התנהגות
- **`renderActionBadges`/`updateExpensesCard` לא נגעו** - הן מקבלות את ה-`data` הגולמי כפרמטר משלהן, לא דרך המשתנים הגלובליים, ולא נוגעות באף אחד משבעת השדות
- נבדק בפועל, לא רק נקרא: **א.** טיול אמיתי (ברלין, בלי `legs`) - הלוגיקה הישנה והחדשה הושוו ישירות באותו קלט, תוצאה זהה בייט-לבייט על כל 7 המשתנים; אומת גם בדף עצמו (`http://localhost:8777/index.html`, קריאה ל-`hydrateTripData()` בפועל בקונסול) - `appTitle` הציג נכון "ברלין 🇩🇪". **ב.** טיול מזויף עם 2 רגליים ו-timezone **שונה** בכל רגל (רומא/ניו יורק, לא אותו ערך במקרה) - `destinationName`/קואורדינטות מהרגל הראשונה, `tripTimezone`/`tripEnd` מהרגל **האחרונה** (`America/New_York`, לא `Europe/Rome`) - מוכיח שיוך נכון, לא צירוף מקרים. **ג.** טיול מזויף עם `flightOut`/`tripEnd` בלבד, בלי `startAt`/`endAt` - התאריכים נטענו נכון, ה-`console.warn` ירה עם ה-`tripId`. **ד.** אין שגיאות קונסול חדשות מעבר לכשלי הרשת הצפויים בסביבת הבדיקה (אין אינטרנט אמיתי), והעמוד נראה זהה - נבדק דרך שער ההתחברות (Google sign-in לא זמין בסביבת הבדיקה, כמו בכל משימה קודמת הפעם)

## v4.11.2 — legs.js: פונקציות עזר ליעדים מרובים (תשתית בלבד, לא בשימוש עדיין)

### ✨ תכונה חדשה
- **קובץ חדש `legs.js`**, טעון ב-6 קבצי ה-HTML (אחרי `toast.js`). זו התשתית הראשונה למסע רב-יעדים (מסמך סכמה נפרד, `legs`) - **שום קובץ אחר לא השתנה מעבר לתג ה-`<script>`, ואף קוד קיים לא קורא לפונקציות האלה**
- חמש פונקציות, כולן על `window`: `getLegs(trip, tripId?)` (הליבה - מנרמלת כל טיול למערך רגליים; טיול בלי `trip.legs` מקבל רגל יחידה מסונתזת מהשדות הקיימים, כולל `vibes`; `tripId` אופציונלי, רק לזיהוי בלוג האזהרה - ר' סעיף הבא), `getPrimaryLeg`, `getLastLeg`, `isMultiLeg`, `getLegForDate(trip, date)`
- **תוקן לפני שנעשה בו שימוש בפועל: `lon` ולא `lng`**. מסמך הסכמה שהמימוש הזה מבוסס עליו קרא לשדה `lng`, אבל בדיקה מול קוד אמיתי (`wizard.html` כותב, `index.html`/`places.html` קוראים) מצאה שהשדה בפועל ב-RTDB הוא `lon`. הנתונים מנצחים - `legs.js` תוקן ל-`lon`, וגם מבנה הרגל שמוחזר וגם ה-JSDoc-style header מתעדים את זה במפורש (כולל למה זה מבלבל: Open-Meteo מחזיר `longitude`, ורוב קוד המיפוי בעולם משתמש ב-`lng`, אז קוד עתידי שיכתב כאן ינחש `lng` ויקבל `undefined` בשקט). **נבדק בקוד**: `lng` מופיע פעם אחת בלבד בכל הריפו - השורה שתוקנה כאן; אין ערבוב `lat/lng` מול `lat/lon` בשום מקום אחר באפליקציה
- **אזהרה מוקדמת**: אם ל-`getLegs` חסר `lat` או `lon` (בענף הסינתזה של טיול בלי `trip.legs`) - `console.warn` עם ה-`tripId` (אם נמסר) או שם היעד כגיבוי, ואובייקט הטיול המלא ללוג. לא זורק, לא שובר שום זרימה - רק מתריע. טיול עם `trip.legs` תקין לא בודק את זה (הרגליים כבר קיימות, אין סינתזה)
- **script קלאסי בכוונה** (לא `type="module"`) - כמו `toast.js` - כדי שהפונקציות יהיו נגישות דרך `window.*` גם מ-`index.html` (script קלאסי) וגם משאר 5 העמודים (`type="module"`). נבדק בפועל בשני ההקשרים
- **החלטת עיצוב ל-`getLegForDate` על תאריך מחוץ לטווח הטיול**: מחזיר `null`. זה **לא** fallback לנתון חסר (כמו timezone: רגל ← טיול ← מכשיר) - תאריך מחוץ לטווח הוא נתון קיים שבאמת לא שייך לאף רגל, וclamp שקט היה גורם ליום שלפני הטיול להיראות כאילו הוא ברגל הראשונה. קורא שרוצה בכל זאת "הרגל הקרובה ביותר" יקבל את זה מ-wrapper נפרד בעתיד (`getLegForDateOrNearest()`), לא כברירת מחדל של הפונקציה הזו
- בגבול בין שתי רגליים (`legs[n].endAt === legs[n+1].startAt`) - הרגל **היוצאת** מנצחת, כנדרש בסכמה §5.2: טווח סגור-סגור על כל רגל, והתאמה ראשונה במערך (שממילא כרונולוגי) זוכה
- נבדק בפועל, לא רק נקרא, על אובייקטי טיול מציאותיים (אין אינטרנט אמיתי בסביבת הבדיקה, אז נבנו אובייקטים תואמי-סכמה ידנית ולא נשלפו מ-Firebase בפועל): **א.** טיול קיים בלי `legs` - רגל יחידה, כל השדות וה-`vibes` נכונים, גם משתי דרכי גישה (`getLegs` ישיר ו-`window.getLegs`). **ב.** `isMultiLeg` - `false` על טיול כזה. **ג.** `getLegForDate` בתוך הטווח ובדיוק על `startAt`/`endAt` - הרגל הנכונה; לפני/אחרי הטווח - `null`. **ד.** טיול מזויף עם שתי רגליים (רומא→פירנצה, גבול משותף) - `getLegs` מחזירה את המערך המקורי (לא מסונתז), `isMultiLeg` → `true`, `getPrimaryLeg`/`getLastLeg` מצביעות על הרגל הנכונה, `getLegForDate` בגבול המדויק מחזירה את הרגל היוצאת (רומא), ולפני/אחרי הטיול כולו - `null`. נבדק גם ש-`window.*` נגיש (ללא שגיאות קונסול) בכל 6 העמודים

## v4.11.1 — undo וreset רשימה לא מכריזים הצלחה לפני שהכתיבה אושרה (shopping.html)

### 🐛 תיקון
- **undoBtn**: `lastStateBackup` היה מתאפס ל-`null` באופן סינכרוני, בלי קשר לתוצאה של כתיבת השחזור - אם הכתיבה נכשלה, הגיבוי היחיד נמחק והמצב לא ניתן היה לשחזור. עכשיו הגיבוי מתאפס **רק** אחרי שהכתיבה אכן נפתרת בהצלחה
- **clearCurrentListBtn**: שתי כתיבות נפרדות (`remove(listRef)` + `set(listStatusRef,...)`) אוחדו ל-`update()` רב-נתיבי אחד, אטומי - מחיקה חלקית (פריטים נמחקו אבל הסטטוס נתקע, או להפך) גרועה יותר מכשלון מלא
- **הטוסט המיידי לא נדחה** - בשני המקרים הוא נשאר בדיוק כמו היום, מוצג מיד. מה שבאמת השתנה זה מה שקורה **ברקע**, אחרי הטוסט:
  - **הצלחה אמיתית** → אין שינוי גלוי, רק עכשיו `lastStateBackup` באמת מתאפס רק כשמובטח שהשחזור הצליח
  - **rejection מפורש** (permission-denied וכו') → `showToast` שני עם הודעת כשלון, דרך ה-`showToast` המקומי הקיים - לא `window.toast` (איחוד הטוסטים באפליקציה הוא commit נפרד). ב-undo, הטוסט השני מציג מחדש את כפתור ה-undo (`showUndo:true`) כדי שאותו גיבוי יהיה ניתן לניסיון חוזר
  - **timeout (4 שניות, לא 8)** - המקרה של כתיבת RTDB שנכנסת לתור בלי חיבור ולעולם לא נדחית ולא מתאשרת עד שהחיבור חוזר: **בלי שום הודעה למשתמש** - הוא כבר ראה טוסט הצלחה והמשיך הלאה; טוסט שני שמדבר על בעיית חיבור בלי שיש מה לעשות רק ידאיג. רק `console.warn` + `window.__gtWriteTimeout = { op, at: Date.now() }` (`op` הוא `'undo'` או `'clearList'`) - הדרך היחידה לדעת בעתיד אם/כמה פעמים זה קורה. ב-undo, `lastStateBackup` **לא** מתאפס במצב הזה - אין ודאות שהכתיבה תצליח
  - **הסף 4 שניות, לא 8**: כפתור ה-undo חי בערך 3.5 שניות בלבד - הוא יושב בתוך הטוסט שמתפוגג (`.toast.show` מסתיים ב-3.7 שניות). סף של 8 שניות היה פג הרבה אחרי שהמשתמש כבר איבד את ההקשר
- נבדק בפועל, לא רק בקוד, עם כתיבה מדומה בת-שליטה: **א.** undo מצליח - הגיבוי מתאפס, אין שינוי מהתנהגות היום. **ב.** undo נכשל (rejection מדומה) - הגיבוי נשאר, טוסט כשלון מוצג, כפתור ה-undo חוזר להיות גלוי - ולחיצה שנייה עליו, עם כתיבה שמצליחה הפעם, אכן עובדת ומאפסת את הגיבוי. **ג.** undo בפסק זמן - ב-12+ שניות אחרי הקליק, הטוסט עדיין "בוטל בהצלחה" (אין טוסט שני), הגיבוי עדיין שלם, `window.__gtWriteTimeout` נכתב. **ד.** reset רשימה - אימות שנעשית קריאת כתיבה אחת בלבד (לא שתיים), טוסט הצלחה עם כתיבה שמצליחה, טוסט כשלון עם כתיבה שנכשלת, ושתיקה מלאה + `console.warn` בפסק זמן

### 📝 לתיעוד (לא תוקן עכשיו, ר' CLAUDE.md)
`lastStateBackup` משותף ל-5 נקודות כתיבה שונות בעמוד (לא רק undo/clear) - ההגנה שנוספה כאן חלקית מטבעה, כי כל אחת מהן דורסת את הגיבוי היחיד. ר' "lastStateBackup - הגנה חלקית" ב-CLAUDE.md

## v4.11.0 — timeout על loadProfile: אין תקיעה בלי מוצא (packing/places/itinerary)

### ✨ תכונה חדשה
- **הבעיה**: ה-`get()` שטוען את הפרופיל של המשתמש (`loadProfile`, בעיה מספר 1 מהתחקיר) יכול להיתקע לנצח. עד עכשיו זה אומר שגיליון "מי את/ה בקבוצה?" (`#memberOverlay`) לעולם לא נחשף - המשתמש תקוע בלי שום מוצא, בלי הודעה
- **התיקון**, ב-3 עמודים בלבד (`packing.html`, `places.html`, `itinerary.html` - `shopping.html` ו-`index.html` מחוץ להיקף בכוונה): `get()` עטוף בסף של 8 שניות. אם הוא לא נפתר עד אז - מתנהג **בדיוק** כמו המסלול הקיים של "אין profile": חושף את `#memberOverlay`, קורא ל-`profileOptions()`/`syncIdentity()` בהתאם לעמוד. אין UI חדש, אין הודעה, אין כפתור "נסה שוב" - המשתמש בוחר שם ידנית וממשיך, בדיוק כמו כל משתמש חדש היום
- **דגל resolved** מבטיח שהתשובה הראשונה מנצחת, בשני הכיוונים: אם ה-`get()` המקורי נפתר אחרי שהסף כבר פג, הוא מתעלם ממנו לחלוטין (לא דורס `profile`, לא נוגע ב-`#memberOverlay` שכבר נחשף וייתכן שהמשתמש כבר מקליד/בוחר בו) - וגם ההפך, אם ה-`get()` מצליח לפני הסף, ה-timeout מבוטל (`clearTimeout`) ולא עושה כלום. `catch` על שגיאה מפורשת מתנהג באותה צורה בדיוק כמו timeout
- **אבחון**: כשהסף יורה (או כשיש שגיאה), נכתב `console.warn` עם שם העמוד, ונשמר `window.__gtProfileTimeout = { page, at: Date.now() }` - הדרך היחידה כרגע לדעת בעתיד אם/כמה פעמים זה קורה בפועל
- **למה לא helper משותף**: שלושת העמודים דומים מבנית אבל השימוש הוא נקודתי (מקום אחד לכל עמוד) - helper גנרי לא היה מקצר כלום, רק מוסיף שכבת הפשטה. כל עמוד מקבל עטיפה מקומית, בדיוק בגודל שצריך
- **מרוץ שנתגלה תוך כדי בדיקה עצמית, לפני שהיה commit**: `loadProfile()` לא נקראת פעם אחת - היא נקראת בכל ירייה של מאזין הטיול (`onValue` על `tripRoot`), וזה מאזין חי שיורה מחדש על **כל** שינוי בתת-העץ של הטיול, כולל פריטי ציוד. בלי הגנה, קריאה ישנה שעדיין תקועה יכולה למחוק profile שקריאה חדשה וטרייה כבר טענה בהצלחה - ולפתוח מחדש גיליון שכבר נסגר, כולל איפוס בחירה שהמשתמש כבר התחיל להקליד
- **התיקון למרוץ**: מונה `profileLoadGen` ברמת המודול. כל כניסה ל-`loadProfile()` עושה `++profileLoadGen` ולוקחת עותק מקומי (`gen`); מסלול ההצלחה, ה-timeout וה-catch כולם בודקים `if (gen !== profileLoadGen) return` לפני שהם כותבים ל-`profile` או קוראים ל-`syncIdentity`/`profileOptions`. דגל `resolved` המקומי נשאר בדיוק כמו שהיה - הוא פותר מרוץ **בתוך** אותה קריאה (timeout מול get שנפתר בו-זמנית); המונה פותר מרוץ **בין** קריאות שונות. שניהם נחוצים, לא חלופיים זה לזה. בנוסף, `profileLoadTimer` ברמת המודול - כל כניסה חדשה מנקה (`clearTimeout`) את הטיימר של הקריאה הקודמת כניקיון best-effort, אבל ההגנה האמיתית היא בדיקת המונה, לא הניקוי
- **למה לא `if(!profile)` בנקודת הקריאה**: זה היה מונע מ-`syncIdentity()`/`profileOptions()` לרוץ מחדש כשמשתתף *אחר* תובע שם - הדרופדאון היה מתיישן. רוצים שהקריאה תמשיך לרוץ בכל פעם; רק שקריאה ישנה לא תדרוס תוצאה חדשה יותר
- נבדק בפועל (לא רק בקוד), בכל 3 העמודים, דרך קריאה ישירה לפונקציה עם `get()` מדומה בר-שליטה (אין אינטרנט אמיתי בסביבת הבדיקה): **א.** פתרון מהיר עם profile - אין שינוי, הגיליון לא נחשף. **ב.** תקיעה אמיתית - אחרי 8+ שניות הגיליון נחשף, ה-`console.warn` וה-`window.__gtProfileTimeout` נכתבים, מאומת בצילום מסך בהקשר עמוד מלא (packing.html). **ג.** תקיעה שנפתרת בשנייה 12 עם profile אמיתי, אחרי שהגיליון כבר נחשף ומשתמש מדומה כבר "בחר" שם בתפריט - ב-30+ שניות אחרי ההתחלה, `profile` נשאר `null`, הגיליון נשאר גלוי, הבחירה לא התאפסה, וחותמת הזמן לא השתנתה. **ד.** קריאה ראשונה תקועה, קריאה שנייה (דור עדכני יותר) נפתרת מהר עם profile אמיתי - אחרי שהסף של הראשונה פג: `profile` נשאר הערך האמיתי מהקריאה השנייה, הגיליון לא נפתח, `window.__gtProfileTimeout` לא נכתב כלל. **ה. הכיוון ההפוך**: קריאה ראשונה נפתרת מהר, קריאה שנייה (הדור העדכני) נתקעת - הסף שלה כן יורה כרגיל, הגיליון נחשף, `profile` חוזר ל-`null`

## v4.10.1 — רשימת הציוד לא תלויה בטעינת הזהות (packing.html)

### 🐛 תיקון
- **הבעיה**: `listenPacking()` - המאזין שטוען את כל רשימת הציוד מ-Firebase - נקרא רק משני מקומות: מזרימת ההצטרפות, ומתוך `loadProfile()` **ורק אם `profile` קיים**. כלומר למשתמש חוזר, כל הרשימה תלויה בכך ש-`get()` של הפרופיל שלו יצליח. אם היא נתקעת (הבעיה מספר 1 מהתחקיר הקודם) - הרשימה נשארת `0/0` ריקה לצמיתות, בלי שום שגיאה, וניסיון להוסיף פריט נכשל בשקט לגמרי (`addItem` בודק `!profile` ועושה `return` בלי אפילו טוסט)
- **התיקון**: `listenPacking()` נקרא עכשיו **גם** באופן עצמאי, ישירות ממאזין טעינת הטיול (אותו מקום שבו `applyTheme`/`applyLanguage`/`configureLinks`/`participantOptions` כבר רצים ללא תלות בזהות) - לא רק מתוך `loadProfile()`. `loadProfile()` עצמו נשאר אחראי רק על טעינת שם המשתמש והצגת/הסתרת גיליון "מי את/ה בקבוצה" - שום קוד לא הניח שהוא רץ אחרי הקריאה שהוסרה ממנו (היא הייתה המשפט האחרון בפונקציה)
- **בטיחות שנבדקה, לא רק נקראה**: `listenPacking()` וכל שרשרת ה-render שהוא מפעיל (`render`→`renderMine`/`renderGroup`, `ensureStarterLists`) אינם מפנים בשום מקום ל-`profile` - רק ל-`user.uid` ולנתוני הטיול/פריטים עצמם. `packingListening` (דגל קיים, `packing.html:1056`) כבר מונע קריאה כפולה - הקריאה השנייה (מזרימת ההצטרפות, אחרי שהמשתמש בוחר שם) פשוט לא עושה כלום, ו-`syncIdentity()` הצמוד אליה ממשיך לעדכן את התצוגה כרגיל
- נבדק בפועל (לא רק בקוד): שני התרחישים דרך קריאה ישירה לפונקציות הפנימיות (הזרקת מצב מדומה, כי אין אינטרנט אמיתי בסביבת הבדיקה) - **א.** משתמש עם `profile` - הרשימה נטענת, גיליון הבחירה נשאר סמוי (ללא שינוי מההתנהגות הקודמת). **ב.** משתמש בלי `profile` - הרשימה נטענת **וגם** גיליון "מי את/ה בקבוצה?" מוצג, בו-זמנית - זה החידוש, מאומת בצילום מסך בהקשר עמוד מלא. גם `listenPacking()` הופעל פעמיים ברצף ישירות - הדגל עולה פעם אחת, הקריאה השנייה לא זורקת שגיאה ולא עושה כלום, כצפוי

## v4.10.0 — תשתית משותפת: טוסט גלובלי + רג'יסטרי onForeground

### ✨ תשתית (לא משנה שום דבר שהמשתמש רואה היום)

הכנה לניטור חיבור ל-Firebase RTDB (`.info/connected`) ו-reconnect
כפוי ב-visibilitychange, בעקבות תחקיר קריאה-בלבד. שלושה חלקים,
אף אחד מהם עדיין לא מחובר להתנהגות קיימת:

- **`window.toast(message, type)`** (`toast.js`, חדש, נטען בכל 6
  העמודים אחרי version.js) - טוסט גלובלי לא-חוסם, נעלם לבד,
  `type` אופציונלי `'info'`/`'error'`. מוגדר ב-CSS חדש ב-shared.css
  (`.gt-toast`, סוף הקובץ):
  - **צבע הרקע הבסיסי** קבוע בכוונה (לא theme-token) - כדי לקרוא
    נכון על פני כל 4 ערכות הטיול ומצב כהה בלי override לכל אחת.
    **צבע ה-error** לעומת זאת משתמש ב-`var(--danger, #D14343)` -
    אותו טוקן סמנטי שכפתור המחיקה בדיאלוג הגלובלי כבר משתמש בו -
    כי צבע סמנטי (שגיאה/סכנה) צריך להישאר עקבי בכל האפליקציה,
    בניגוד לצבע הבסיס שאין לו משמעות כזו לשמר
  - **z-index 200** - זהה לטוסט הישן, בכוונה: טוסט לא אמור לצוף
    מעל דיאלוג אישור (השתמשנו קודם ב-3000 בטעות, לפני שהתבקשנו
    להשוות ל-200)
  - **מיקום**: לא קרוב ל-88-90px שבו יושבים שלושת מימושי הטוסט
    הקיימים (`.toast` הליבה, `.app-toast` של index.html) - אלא
    `calc(160px + safe-area-inset-bottom)`, כדי ששני הטוסטים לא
    ייפלו זה על זה אם שניהם מוצגים בו-זמנית, **וגם** לא ייגע
    ב-`.fab` (שהקצה העליון שלו יושב ב-148px מתחתית המסך: `bottom:
    90px` + גובה `58px`). ניסינו קודם 146px וזה השאיר מרווח של
    2px בלבד מה-FAB - לא מספיק, במיוחד עם הודעה ארוכה שמתרחבת
    כמעט לרוחב המסך. נבדק בפועל ב-viewport של 375×667 (iPhone SE)
    עם `getBoundingClientRect`: מרווח אמיתי של 12px מה-FAB, ושני
    הטוסטים בו-זמנית מוצגים בלי לחפוף זה את זה, את ה-FAB, או את
    התפריט התחתון - כולל צילום מסך
  - קריאות רצופות ל-`window.toast` משתמשות באלמנט DOM אחד
    ובטיימר אחד שמתאפס (לא נערם) - נבדק.
- **המימושים המקומיים הקיימים לא נגעו** (`toast()`/`showToast()`
  בכל עמוד) - עדיין משרתים את כל הקריאות הקיימות באפליקציה.
  ראו CLAUDE.md, "שתי מערכות טוסט במקביל" - האיחוד (מחיקת
  המימושים הישנים) הוא commit נפרד, עדיין לא קיים
- **תגלית תוך כדי בדיקה, לא כפי שתועדה בהתחלה**: `window.toast`
  נדרס בפועל (על ידי המימוש המקומי של אותו עמוד) **רק** ב-
  index.html - לא ב-4 מתוך 6 כפי שהונח לפני שנבדק בדפדפן אמיתי.
  הסיבה: index.html הוא היחיד עם script קלאסי; שאר 5 העמודים
  מריצים את ה-toast המקומי שלהם בתוך `type="module"`, שההצהרות
  שלו לעולם לא נדבקות ל-window. אותה חלוקה בדיוק כמו ה-SDK
  (compat v8 מול מודולרי v9) - ראו CLAUDE.md, "מבנה סקריפטים
  בין 6 הדפים"
- **`window.onForeground(fn)`** (ב-version.js) - רג'יסטרי מינימלי:
  רושם callback שרץ כשהעמוד חוזר להיות גלוי, דרך מאזין
  visibilitychange **יחיד** לכל הצרכנים (לא אחד לכל צרכן).
  `checkForUpdate()` הקיים (בדיקת גרסה + באנר "גרסה חדשה
  זמינה") נרשם עכשיו דרך אותו רג'יסטרי במקום מאזין נפרד משלו -
  ההתנהגות שלו זהה לחלוטין: אותו DOM, אותו z-index, אותם
  guard-ים (`shown`/`checking`), אותה הודעה. נבדק ישירות (לא
  רק נקרא): mismatch אמיתי מציג באנר, mismatch שני לא מציג
  כפול, dismiss מסיר, ה-fetch ל-version.js אכן קורה על
  visibilitychange
- נבדק: 6 העמודים טוענים בלי שגיאות קונסול חדשות; `typeof
  window.toast`/`window.onForeground` הן function בכל עמוד;
  ה-diff בכל אחד מ-6 קבצי ה-HTML הוא שורת `<script>` אחת בלבד

## v4.9.1 — כפתור הבית המוקדם ב-shopping.html

### 🐛 תיקון
- **תיקון לטעות מ-v4.9.0**: הערך שם ("ל-shopping.html אין בכלל configureLinks, קישורי הניווט שלו לא מתעדכנים עם tripId בשום שלב") היה שגוי - נבדק שוב לעומק. לעמוד יש חיווט מקביל מלא, רק בשם אחר: `scopedUrl()` וקוד שמעדכן את `.bottom-nav a` + `#homeBtn` (כפתור בית נפרד בכותרת, בנוסף ל-`#navHome` בסרגל התחתון), חי בתוך `syncTripUI()`. כלומר זה בדיוק אותו מרוץ תזמון שהיה בשלושת העמודים האחרים (packing/places/itinerary) לפני v4.9.0 - לא חיווט חסר לגמרי כפי שנכתב
- **התיקון**: הוצא הקוד שהיה חבוי בתוך `syncTripUI()` (עדכון קישורי `.bottom-nav a`, `#homeBtn`, והחלפת קישור ההוצאות ל-`tripData.spliitUrl`) לפונקציה עצמאית בשם `configureLinks()` - אותו שם בדיוק כמו בשאר העמודים. `syncTripUI()` קורא לה כרגיל אחרי טעינת הטיול, אבל היא רצה **גם** מיד עם טעינת הדף, סינכרונית מה-tripId שבכתובת - אותה תבנית כמו בשלושת העמודים האחרים
- **בטיחות לקריאה מוקדמת**: הקריאה ל-`tripData.spliitUrl` הפכה ל-`tripData?.spliitUrl` (אופציונל-צ'יינינג) כי `tripData` הוא `null` עד שהטיול נטען - קריאה מוקדמת בלי זה הייתה זורקת שגיאה
- נבדק: `#homeBtn` ו-`#navHome` נושאים tripId מיד בטעינה (גם עם וגם בלי tripId בכתובת), אין שגיאות קונסול חדשות

## v4.9.0 — מסך בחירת טיול ב-index.html כשאין tripId (שלב 2/2)

### ✨ תכונה חדשה
- **הבעיה**: משתמש מחובר שנוחת על index.html בלי tripId בכתובת (לחיצה על כפתור הבית לפני שהתעדכן, פתיחה מאייקון PWA, או tripId שכבר לא קיים) ראה עד היום דשבורד **מזויף** - כותרת, טיימר, מזג אוויר וכרטיסי פעולה עם נתוני ברירת מחדל קשיחים בקוד, בלי שום סימן שזה לא הטיול האמיתי שלו. זה כנראה בדיוק מה שדווח בעבר כ"האפליקציה החליפה לי טיול" - לא טיול מחוק שצץ מחדש, אלא placeholder שנראה כמו טיול אמיתי
- **התיקון**: כשמשתמש מחובר מגיע בלי tripId תקין, index.html טוען את `users/{uid}/trips` (אותו נתיב שנכתב גם ביצירת טיול ב-wizard.html וגם בהצטרפות בכל 4 העמודים האחרים, v4.8.0) ומציג מסך בחירה במקום הדשבורד:
  - **טיול אחד** ברשימה → ניווט אוטומטי אליו, בלי מסך ביניים
  - **כמה טיולים** → רשימה לחיצה (יעד, סוג, תאריך התחלה), אותו מסך למארגן ולמשתתף - רק הרשימה שמאחוריו שונה
  - **אין טיולים** → הודעה + קישור ליצירת טיול חדש (wizard.html)
  - **tripId שגוי/טיול שנמחק** → אותו מסך בחירה, עם הודעת "הטיול לא נמצא" מעליו
- **lastTripId**: מפתח localStorage חדש (נפרד מהמטמון הקיים `trip-{id}-*`), נכתב בכל אחד מ-5 עמודי הטיול (index/packing/places/itinerary/shopping) **רק** אחרי טעינה מוצלחת של טיול אמיתי - לא סתם כשיש tripId בכתובת, כדי לא לשמור מזהה של טיול מחוק וליצור לולאת הפניות. index.html בודק אותו לפני שהוא פונה בכלל ל-users/trips: אם קיים, מפנה ישר לטיול האחרון שנטען בהצלחה (המקרה הנפוץ - כפתור בית / אייקון PWA); אם הטיול הזה מתברר כלא תקין, המפתח נמחק ונופלים חזרה לרשימה מ-users/trips
- **מניעת חזרה של הדשבורד המזויף**: `updateClockAndPhases()` (הפונקציה שמצירת את הטיימר/מזג האוויר/הבאנרים ורצה כל שנייה ב-setInterval) הייתה מציגה מחדש את הבאנרים עם נתוני ברירת מחדל גם אחרי שהוסתרו, כי היא לא הייתה תלויה בכלל בכך שנמצא טיול אמיתי. נוסף דגל `tripPickerActive` שחוסם אותה כליל כשמסך הבחירה פעיל - אומת שהדשבורד המזויף לא חוזר גם אחרי כמה סבבי טיימר
- **כפתור הבית המוקדם**: `configureLinks()` (שמעדכן את קישורי הניווט, כולל כפתור הבית, עם tripId) עכשיו רץ גם מיד עם טעינת העמוד (סינכרונית, מה-tripId שבכתובת) - לא רק אחרי שהטיול נטען מ-Firebase - ב-packing.html, places.html, itinerary.html. **shopping.html לא כלול בסבב הזה** - תוקן בנפרד ב-v4.9.1 (התיאור כאן שהופיע במקור, שלעמוד אין בכלל חיווט מהסוג הזה, היה שגוי - תוקן שם)
- נבדק: כל 4 המצבים (0/1/2+ טיולים, טיול לא נמצא) ב-index.html, כולל שהדשבורד המזויף באמת נעלם ולא חוזר; lastTripId נכתב ונקרא נכון (כולל מחיקה כשהוא כבר לא תקף); כפתור הבית נושא tripId מיד בטעינה ב-3 העמודים; דסקטופ ומובייל; אין שגיאות קונסול חדשות
- **מגבלת בדיקה**: בסביבת האוטומציה אין חיבור Firebase אמיתי, אז מסלול ה-`users/{uid}/trips` לא נבדק מול נתונים אמיתיים - רק עם נתונים מדומים שהוזרקו ישירות לפונקציית הרינדור. מומלץ לוודא בזרימה אמיתית (משתמש עם כמה טיולים) לפני שממשיכים הלאה

## v4.8.1 — datetime-local לא נפתח בלחיצה בדסקטופ (רגרסיה)

### 🐛 תיקון
- **הרגרסיה**: בתיקון הקודם (v4.8.0 ומטה, שני הסבבים על date-only) הרחבנו את הסלקטור `.date-picker-shell input[type="date"]` והוספנו מאזין `click` שמפעיל `showPicker()` **רק** על `type="date"` - מתוך הנחה ש-`datetime-local` "כבר נפתח בלחיצה" מעצמו. בדיקה בפועל (דסקטופ, כל 4 השדות) הראתה שההנחה הזו שגויה: כמו בשדות date-only, לחיצה על datetime-local בדסקטופ פותחת את הפיקר רק אם היא נופלת בדיוק על אזור האייקון הפנימי של הדפדפן - לא בכל מקום בשדה המורחב ב-CSS. במובייל זה עבד כי שם הפיקר הטבעי נפתח כבר מ-focus, לא רק מהאייקון - אותה אסימטריה מובייל/דסקטופ שראינו בסבב הקודם על date-only
- **התיקון**: אותו מאזין `click` -> `showPicker()` שכבר קיים ל-`type="date"` הורחב גם ל-`datetime-local`, בשני הקבצים (`wizard.html`: tripStart/tripEnd, `index.html`: adminFlightInput/adminEndInput). הוסר התנאי `input.type === 'date'` - הקריאה יוצאת לשני הסוגים
- **החשש שנבדק ונמצא לא מוצדק**: בסבב הקודם נמנענו מלהחיל את זה גם על datetime-local מחשש להבהוב (סגירה-פתיחה) אם קוראים ל-`showPicker()` כשהפיקר כבר פתוח. נבדק ישירות: קריאות חוזרות ל-`showPicker()` על שדה עם פיקר פתוח לא גרמו ל-blur/focus מחדש ולא זרקו שגיאה - אותה התנהגות בדיוק כמו ב-date-only שכבר בפרודקשן. אין הבהוב בפועל
- **מגבלת בדיקה**: לא ניתן לראות חזותית את הפופאפ הנפתח בסביבת האוטומציה (חלונות פיקר טבעיים של הדפדפן לא נלכדים בצילום מסך, לא ב-date ולא ב-datetime-local) - האימות נעשה בעקיפין: `showPicker()` אכן נקרא בפועל בלחיצה אמיתית (patch על הפונקציה + מעקב אחרי הקריאות), בלי שגיאות קונסול חדשות, בכל 4 השדות ובשני סוגי הקלט
- נבדק: tripStart/tripEnd (wizard.html) ו-adminFlightInput/adminEndInput (index.html), דסקטופ, בטיול לחו"ל (datetime-local) ובטיול בארץ/קמפינג (date) - showPicker() נקרא כצפוי בשני המצבים, ללא רגרסיה בהתנהגות ה-date-only הקיימת

## v4.8.0 — הצטרפות לטיול רושמת אותו תחת users/{uid}/trips (שלב 1/2)

### ✨ שינוי סכמה
- **השורש**: `users/{uid}/trips/{tripId}` נכתב עד היום **רק** כשמישהו יוצר טיול חדש (wizard.html). הצטרפות לטיול קיים דרך קישור (saveProfile/claimMember בכל 4 העמודים) כתבה רק ל-`{tripRoot}/memberProfiles/{uid}` ו-`participantClaims` - בתוך הטיול, לא מתחת ל-uid של המצטרף. וידאנו: אין שום מנגנון אחר (לא בשרת, לא ב-localStorage) שמאפשר למשתתף רגיל לגלות מחדש אילו טיולים הוא חבר בהם. כללי Firebase לא מאפשרים סריקה גורפת של `trips/*` לחיפוש memberProfiles
- **התיקון**: הצטרפות לטיול (בכל 4 העמודים: packing/places/itinerary/shopping) כותבת עכשיו **גם** ל-`users/{uid}/trips/{tripId}` - אותה צורת נתיב וצורת ערך בדיוק כמו שה-wizard כבר כותב למארגן (`destination`,`tripType`,`language`,`startAt`,`endAt`,`createdAt`), כדי שמסך בחירת הטיול (שלב 2, נפרד) יוכל להציג טיולים של מארגן ומשתתף במאוחד בלי קוד מיוחד. כל השדות כבר טעונים ב-`trip`/`tripData`, אין קריאת Firebase נוספת
- **בכוונה לא אטומי**: הכתיבה ל-`memberProfiles` (עם ה-`.validate` המורכב שלה מול `participantClaims`) נשארה `set()` נפרד, **בדיוק כמו לפני השינוי**, ללא שום שינוי בהתנהגות שלה. הכתיבה ל-`users/{uid}/trips/{tripId}` היא קריאה נפרדת ל-`update()`, **אחרי** שההצטרפות כבר הצליחה במלואה - לא מבוטלת (`update()` רב-נתיבי) ביחד איתה. הסיבה: `update()` רב-נתיבי ב-Firebase הוא אטומי (כל-או-כלום) - אם הנתיב החדש, הפחות-מוכח, היה נכשל מול הכללים, זה היה מפיל גם את ה-`memberProfiles` שכבר עובד היום, רגרסיה גרועה יותר מהבאג שאנחנו מתקנים. אם הכתיבה השנייה נכשלת - `.catch()` שכותב ל-console בלבד, **לא** מוצג למשתמש: מבחינתו ההצטרפות הצליחה, וזו האמת
- **וידוא הרשאות**: אין קובץ rules בריפו (מנוהל רק ב-Firebase Console) - אושר ישירות מול הכלל בקונסולה: `"users": { "$uid": { ".write": "auth != null && auth.uid === $uid" } }` - אותה צורת נתיב בדיוק (`users/{auth.uid}/...`) שה-wizard כבר כותב בהצלחה בפרודקשן ליצירת טיול, מתיר את אותו דבר למשתתף
- **לא רטרואקטיבי בכוונה**: מי שכבר הצטרף לפני השינוי הזה לא ייכתב לו כלום - הכתיבה קורית רק בזרימת ההצטרפות עצמה. מילוי רטרואקטיבי (Cloud Function עם הרשאות אדמין) נשקל ונדחה כרגע - אין עדיין משתמשים אמיתיים מלבד המפתח
- נבדק: כל 4 העמודים נטענים בלי שגיאות קונסול חדשות (בדיקת syntax/scope בלבד - **לא** נבדקה הצלחת הכתיבה בפועל מול כללי Firebase האמיתיים, כי אין אינטרנט אמיתי בסביבת הבדיקה; מומלץ לוודא בהצטרפות אמיתית לפני בניית שלב 2 עליה)

## v4.7.3 — הסרת נפילת-לאחור גלובלית שהחזירה טיולים ישנים (index.html)

### 🐛 תיקון
- **השורש**: index.html שמר נתיב Firebase גלובלי ישן (`trip_settings`/`group_members`/`personal_locker` - שרד מלפני התמיכה בכמה טיולים), שהופעל בכל פעם ש-`tripId` חסר מהכתובת. הקריאה (`hydrateTripData`) הציגה את הנתונים משם **כאילו** היו הטיול הנוכחי, והכתיבה (בשמירת הגדרות ניהול / לוקר אישי / רשימת משתתפים) כתבה **לאותו נתיב גלובלי משותף לכל מי שנכנס בלי tripId** - לא קשור בכלל למערכת ה-trips/{tripId} החדשה. מחיקת טיול (`deleteRecentTrip` ב-wizard.html) אף פעם לא נגעה בנתיב הזה, כך שנתונים ישנים ששכבו שם יכלו "לצוץ מחדש" בכל כניסה בלי tripId - בלי שום קשר למה שנמחק או לא נמחק
- **איך מגיעים לשם בלי tripId**: כפתור הבית (⌂/"בית") בכל 6 העמודים הוא `href="index.html"` **גולמי בלי tripId** ב-HTML - ה-tripId מתווסף רק אחר-כך, א-סינכרונית, ברגע שנתוני הטיול נטענים (`configureLinks()`). לחיצה על הכפתור לפני שזה קרה (טעינה איטית, רגע אחרי חזרה מרקע) נוחתת בלי tripId. גם `manifest.json`'s `start_url` הוא `"index.html"` בלי tripId - פתיחה מאייקון מסך הבית (אם הותקן) נופלת לאותו מצב תמיד. **שני המסלולים האלה עדיין קיימים** - לא טופלו בסבב הזה, לפי בקשה מפורשת. עכשיו זה משנה פחות (אין יותר נתונים גלובליים לחשוף/לשבש), אבל עדיין חוויה שבורה - עמוד ריק/לא פונקציונלי בלי הודעה
- **התיקון**: הוסרו לחלוטין - בלוק ההאזנה (`db.ref("trip_settings").on(...)` וכו') וכל שלוש נקודות הכתיבה שנפלו לאחור לנתיבים הגלובליים. `saveTripCollection()`/שמירת הגדרות ניהול/לוקר אישי עכשיו פשוט **לא שומרות שום דבר** כש-`tripRoot` חסר, במקום לכתוב לנתיב משותף. אומת: המסלול הרגיל (עם tripId) לא נגוע כלל - יש populator נפרד ומתוזמן נכון ל-`groupMembers`/`personalLockerCards` דרך `trips/{tripId}/...` שלא נגעתי בו
- **הערה מהמשתמש**: שלושת הנתיבים הגלובליים האלה כבר חסומים בכללי Firebase מלפני יומיים - הקוד שהוסר ממילא לא יכול היה לעבוד, רק לייצר מצב שבור/תקוע
- נבדק: טעינה עם ובלי tripId - אין שגיאות קונסול חדשות; grep מוודא שלא נשארה שום קריאה ל-`trip_settings`/`group_members`/`personal_locker` כנתיב גולמי

## v4.7.2 — timeout על שער ה-auth (סעיף 1 מתוך התחקיר: אפליקציה נתקעת אחרי חוסר פעילות)

### 🐛 תיקון
- **הבעיה**: כל עמוד (מלבד wizard.html) חוסם את כל המסך מאחורי `.auth-loading` (spinner מלא-מסך) ומסיר אותו **רק** בתוך ה-callback הראשון של `onAuthStateChanged` - בלי שום timeout. אם ה-callback הזה נתקע (חשד: חיבור IndexedDB/WebSocket שנתקע ב-WebKit אחרי שהטאב הוקפא ברקע ב-iOS - ראו את דוח התחקיר), המשתמש נשאר תקוע על ה-spinner לנצח, בכל עמוד, בלי שום דרך החוצה חוץ מסגירת הדפדפן
- **התיקון**: קובץ JS משותף חדש (`auth-gate.js`, נטען אחרי version.js בכל 5 העמודים הרלוונטיים - **לא** wizard.html, שאין לו שער auth כזה). כל עמוד קורא ל-`window.armAuthGate(retryFn)` פעם אחת, ליד ה-listener של `onAuthStateChanged`. אם `.auth-loading` לא נעלם תוך 12 שניות, מוצגת שכבה (z-index מעל ה-spinner, לא נוגעת ב-class שלו) עם הודעה וכפתור "נסה שוב" - לא רענון דף
- **הכפתור מנסה מחדש באמת**: כל עמוד עבר ריפקטור קטן (ה-callback האנונימי של `onAuthStateChanged` הפך לפונקציה בשם `handleAuthStateChanged`), כך שהכפתור יכול לקרוא לה ישירות עם `auth.currentUser` (המצב הסינכרוני שכבר שמור בזיכרון ה-SDK) - עוקף אפשרי את הבדיקה האסינכרונית התקועה, בלי טעינה מחדש של כל הדף. אם `auth.currentUser` עדיין ריק, שום דבר לא נכפה - ה-listener המקורי (עדיין מחובר) ממשיך לחכות, וטיימר חדש מתחיל
- **התנהגות זהה כשהחיבור תקין**: `armAuthGate` בודק אם `.auth-loading` כבר מוסתר לפני שהוא בכלל מפעיל טיימר - במקרה הרגיל (resolve מהיר) הוא לא עושה כלום. `MutationObserver` על ה-class של `.auth-loading` הוא מה שמזהה הצלחה בכל מצב - גם דרך הכפתור וגם אם ה-listener המקורי נפתר לבד תוך כדי שההודעה מוצגת - ומנקה הכל אוטומטית
- **באג אמיתי שנתפס תוך כדי בדיקה עצמית**: בגרסה ראשונה, הצגת ההודעה כללה הסתרת `.auth-loading` בעצמה (כדי "להחליף" את ה-spinner) - וזה גרם ל-MutationObserver לפרש את זה כאילו ה-listener האמיתי הצליח, ומיד הסתיר את ההודעה שהוא בעצמו הרגע הציג. תוקן: ההודעה יושבת מעל ה-spinner (z-index) בלי לגעת ב-class שלו בכלל - כך שה-observer לעולם לא מבלבל בין "אנחנו הסתרנו" ל"הדף האמיתי הסתיר"
- **מה לא נכלל בסעיף הזה** (בכוונה, לפי בקשה): מאזין `.info/connected`, `goOffline`/`goOnline` בחזרה מרקע, timeout על קריאות get/update בודדות - אלה שלבים נפרדים לאחר אימות שהתיקון הזה עובד
- נבדק: סימולציה מלאה של תקיעה (spinner מוסתר אחרי timeout, הודעה מופיעה, כפתור מנסה שוב, מצב שבו הפתרון קורה עצמאי תוך כדי שההודעה מוצגת) על packing.html במלואו; שאר 4 העמודים נבדקו לפרסור/קונסול תקין וללא רגרסיה בזרימה הרגילה. tr(he/en), בהיר/כהה, RTL/LTR

## v4.7.1 — הידוק המרווחים באזור העליון של עמוד הציוד (packing.html)

### 🐛 תיקון
- **שני מרווחים אנכיים גדולים מדי בין שורות** (לא גובה השורות, לא מספרן): בין שורת המשתמש לטאבים, ובין הטאבים לבורר הרשימה. השורש - כל השורות הן ילדי flex ישירים של `.shell` (`display:flex;flex-direction:column`), ומרווחי flex-items **לא מתמזגים** (margin collapse) כמו קופסאות block רגילות - כך ש`margin-bottom` של שורה אחת ו-`margin-top` של הבאה **מצטברים** (14+16=30px, 12+16=28px) במקום להתמזג לערך המקסימלי ביניהם
- **הייחוס** (shopping.html, לפי בקשה): שם המרווחים בין שורות דומות הם רק 6px - כי שם רק הצד ה**קודם** של כל שורה מגדיר `margin-bottom`, והבא לא מגדיר `margin-top` בכלל, כך שאין הצטברות. יושר `packing.html` לאותה קונבנציה: הוסר ה-`margin-top` מ-`.tabs` ומ-`.list-head`, וצומצמו הערכים הנותרים
- נמדד (מכשיר וירטואלי, לא הערכה): מרווח 1 (משתמש→טאבים) 30px→8px, מרווח 2 (טאבים→בורר רשימה) 28px→8px. גובה האזור העליון הכולל מעל הרשימה: 296px→254px נוסף (בנוסף לצמצום הקודם מ-318px)
- נבדק: מספר השורות וגובהן לא השתנו; פעולות (☑️⚙️➕, בורר רשימה) נגישות ולוחצות; בהיר/כהה; RTL/LTR; shopping.html - ללא שינוי בפועל

## v4.7.0 — הידוק האזור העליון ברשימת קניות (shopping.html)

### ✨ צפיפות
- **איחוד סטטוס+שם לשורה אחת**: "סטטוס רשימה: בהכנה" ו"שם: יניב" עברו משתי שורות נפרדות (עם סרגל ההתקדמות ביניהן) לשורה אחת - סטטוס בצד אחד, שם בצד השני. תווית הסטטוס קוצרה ל"סטטוס:" (מ"סטטוס רשימה:") כדי להתאים לשורה הצפופה. שם ארוך נחתך בשלוש נקודות (`overflow:hidden;text-overflow:ellipsis` על ה-select) ולעולם לא דוחק את הסטטוס החוצה - הסטטוס קיבל `flex:0 0 auto` (מוגן, לא נחתך), השם `flex:1 1 auto;min-width:0` (סופג את הלחץ). מצב ה-highlight (מסגרת אדומה כשלא נבחר שם) נשאר מוגבל לתיבת השם בלבד, לא לכל השורה
- **הידוק ערכים בהשוואה ל-packing.html**: `.sticky-header-container` padding-bottom 8px→6px, margin-bottom 10px→8px; `header` margin-bottom 8px→6px; `.progress-section` padding 11px 14px→9px 12px, margin-bottom 12px→10px; `.whoami` padding 8px 12px→7px 10px; `.main-actions-row` margin-bottom 12px→10px
- נמדד (מסך מובייל 812px): האזור מעל הרשימה 379px→309px (46.7%→38%), לעומת packing.html 296px (36.5%) - קרוב משמעותית, לא זהה: shopping עדיין כולל שורת main-actions-row (הוסף פריט/הסתר שהושלמו/עוד) שאין ל-packing כשורה נפרדת, ולא הוסרו שורות מעבר לאיחוד המבוקש
- נבדק: שם ארוך (40+ תווים) לא דוחק את הסטטוס ולא חורג מרוחב המסך; מצב highlight; בהיר/כהה; RTL/LTR; פעולות קיימות (בורר סטטוס לחיץ, שדה שם ניתן לעריכה) נשארו נגישות

## v4.6.1 — תיקון חלקי 1/N: קטימת ראש העמוד חוזרת ב-wizard.html

### 🐛 תיקון באג (בבדיקה - סבב ראשון מתוך כמה)
- **הבאג חזר אחרי תיקון קודם (fd0a9da)**: אותו סבב תיקן רק את `.wizard-container` (הוחלף `calc(100dvh-58px)` ב-`flex:1`), אבל `body` עצמו נשאר עם `min-height:100dvh` בלי נגיעה - אותה מחלה בדיוק, אלמנט אחר. אומת: זה לא קשור לשינויי הצפיפות של אתמול/היום (הושוותה הצהרת ה-body ב-git diff מול `eb5b6d7` - זהה לחלוטין), והתבנית קדמה לסבב הזה בכמה ימים
- **סקר renderMine בכל 6 העמודים**: אותה תבנית בדיוק (body ו/או מכולה ראשית נעולים ישירות ל-`100dvh`, בלי `overflow:hidden`) חוזרת גם ב-index.html (`body`+`.app-container`) וגם ב-packing/places/itinerary/shopping (`body`+`.shell`, עם `overflow:hidden`). זה תיקון **חלקי, ראשון מתוך כמה** - wizard.html בלבד בסבב הזה, שאר העמודים בהמתנה לאישור אחרי בדיקה במכשיר אמיתי
- **התיקון**: משתנה CSS מחושב ב-JS (`--vh`, מ-`window.innerHeight`, מתעדכן ב-resize/orientationchange בלבד - לא בגלילה) במקום `100dvh` ב-`body`. זו הטכניקה הסטנדרטית נגד הבאג המתועד ב-WebKit שבו הערך המחושב של dvh נתקע "ישן" אחרי מחזור הצגה/הסתרה של סרגל הכתובת שמופעל מגלילה

### ⚠️ באג פתוח - לא פתור
- **התיקון לעיל לא פתר את הבאג בפועל במכשיר אמיתי**. נוסף badge אבחון חי מוסתר מאחורי `?vhdebug=1` (ב-wizard.html, לא הוסר) שמראה innerHeight/visualViewport.height/offsetTop/גובה body/גובה `.wizard-container` בזמן אמת - כדי לתפוס את המספרים ברגע שהבאג קורה, במקום לנחש תיקון נוסף. **הבאג לא ניתן לשחזור בעקביות** - ממתין לצילום מסך של ה-badge ברגע שהתוכן נחתך בפועל לפני שממשיכים
- היקף: וידאנו (v4.6.1) שהתבנית הבעייתית (body/מכולה ראשית נעולים ל-`100dvh`) קיימת בכל 6 העמודים, לא רק wizard - אבל בפועל, נכון לרגע זה, wizard.html הוא היחיד שבו הבאג נצפה. שאר העמודים לא נגעו בהם

## v4.6.0 — כיווץ האזור העליון בעמוד הציוד (packing.html)

### ✨ צפיפות
- **שורת בורר הרשימה + סרגל ההתקדמות אוחדו לשורה אחת**: הכותרת הסטטית "הרשימה שלי" הוסרה (מיותרת - הטאב "הציוד שלי" ממש מעליה כבר אומר את זה), ובמקומה בקצה השני של אותה שורה יושב המונה "1/8 · 13%". סרגל ההתקדמות עצמו נשאר כפס דק תחת השורה. תווית "התקדמות" הוסרה מהתצוגה (מוסקת מההקשר - בצמוד לפס התקדמות) אך נשמרה כ-aria-label דינמי לנגישות
- **תגית שם המשתמש הפסיקה לחזור על השם**: הייתה מציגה "דניאל · Daniel Cohen" כששם המשתתף כבר מוצג (נעול) בשדה הבחירה ממש לידה - השם הכפול הוסר, נשאר רק חשבון ה-Google המקושר ("Daniel Cohen") שהוא המידע החדש היחיד שהתגית נושאת
- **באג חפיפה שהתגלה תוך כדי**: שורת הבורר-המאוחדת חרגה מרוחב המסך ב-LTR (חרגה גם לפני השינוי, בכיוון ההפוך ב-RTL - לא נגרם על ידי השינוי הזה אבל תוקן באותה הזדמנות כי נגעתי באותה שורה) - `.list-select` לא היה `min-width:0`, כך שברירת המחדל שלו לא התכווצה מתחת לתוכן, ודחפה את שאר השורה החוצה. תוקן עם `flex:1 1 auto;min-width:0` + `text-overflow:ellipsis` על ה-select, כך שהוא זה שנחתך (עם שלוש נקודות) כשיש הרבה רשימות/שם רשימה ארוך, לא הכפתורים ולא המונה
- נמדד: גובה האזור מעל הרשימה 318px→296px (39%→36% ממסך מובייל בגובה 812px) - שיפור בעקבות איחוד השורות בלבד; המרחקים בין השורות (identity-row/tabs) לא נגעו בהם
- נבדק: תפריט ⚙️/☑️/➕ נגישים ולוחצים, בהיר/כהה, RTL/LTR, שם רשימה ארוך לא שובר את השורה

## v4.5.0 — כיווץ שורת ציוד ומקומות/מוזיקה למבנה שתי-שורות (כמו כרטיס הפעילות)

### ✨ צפיפות (packing.html item, places.html שני הטאבים)
- **packing.html**: שם הפריט ו"פרטי"/"משותף" עברו משתי שורות נפרדות לשורה אחת - שם בולט (נחתך ראשון אם ארוך מאוד, עם רצפת 30% רוחב), מטא-דאטה עמומה קטנה נשארת שלמה ברוב המקרים (נחתכת רק כשהשילוב ארוך במיוחד). "גלוי לקבוצה" קוצר ל"משותף" (מ-11 לכ-5 תווים) - הביטוי הישן נגרם לחיתוך מיותר גם בשמות פריט קצרים. גם כפתורי הפעולה (▸/🔒/•••) קיבלו bleed בשוליים שליליים כדי שלא הם יכתיבו את גובה השורה. נמדד: 60px→50px לפריט רגיל (ירידה ~17%), 7→8 פריטים במסך מובייל
- **places.html (מקומות + מוזיקה, אותו CSS לשני הטאבים)**: שם/הערה/תגית (מקומות) או שם/מוסיף/תגית (מוזיקה) עברו משלוש שורות נפרדות לשתיים - שורה 1 שם בלבד (נחתך אם ארוך), שורה 2 מידע משני בשורה אחת (הערה או שם-מוסיף · תגית). התגית לעולם לא נחתכת (`flex:0 0 auto`); שם-המוסיף (מוזיקה) מוגן ברצפת 50% רוחב - נחתך רק במקרי-קיצון, לא נבלע לגמרי ולא דוחק את התגית מחוץ לכרטיס (בדוק עם שם בדוי בן 48 תווים). כפתור "ניווט" הפך מכפתור-טקסט בשורה נפרדת (52px+) לאייקון מצפן 🧭 קבוע 40×40 ליד ⋮. תוקן גם `.cards{grid-template-columns:repeat(2,1fr)}`→`minmax(0,1fr)` בדסקטופ, כדי שכרטיס אחד לא ימתח את האחרים. נמדד: כרטיס מקום עם הערה+ניווט 173px→76px, כרטיס מוזיקה (שם ארוך) 123px→76px - במסך מובייל: מקומות 2→5-7 פריטים, מוזיקה 3-4→5-6 פריטים
- נבדק: תגית/שם-מוסיף במגוון אורכים כולל קיצון, בהיר/כהה, RTL/LTR, דסקטופ שתי-עמודות, פעולות קיימות (עריכה/מחיקה/הוספה ליום) נשארו נגישות

## v4.4.3 — באג: תת-רשימת ציוד נבלעת מאחורי הפריט הבא (packing.html)

### 🐛 תיקון באג
- **התת-רשימה נחתכת ונעלמת חלקית מאחורי הפריטים שבאים אחריה**: השורש - `.items` (מיכל הרשימה, `display:grid`) הסתמך על ברירת המחדל `grid-auto-rows:auto`. כשפריט התרחב (`.sub-panel` נוסף כילד עם `grid-column:1/-1`), שורת ה-grid החיצונית של אותו פריט לא גדלה בהתאם לתוכן הפנימי שלו - נמדד בפועל: גובה הפריט נשאר נעול על 58px (ה-min-height) גם כשהתת-רשימה בפנים דרשה כ-109px, כך שהתת-רשימה חרגה מחוץ לתיבת הפריט וטואטה מתחת לפריט הבא (שם רקע אטום צייר מעליה). התיקון: `grid-auto-rows:max-content` על `.items` - כל שורה מתכווצת/מתרחבת לפי התוכן בפועל שלה, כולל תת-רשימה שנפתחת
- **חץ ההרחבה כמעט בלתי-נראה**: ה-▸/▾ עצמו היה בפונט 0.72rem בצבע מעומעם (`--muted`) - כמעט לא ניתן להבחין בו ליד סמל המנעול וכפתור ה-•••, למרות שאזור הלחיצה עצמו כבר היה 40×40px (תקין). הוגדל ל-1.05rem, מודגש, בצבע `--text`. נוסף מצב "פתוח" ברור: רקע מגוון בצבע הטיול (`color-mix`) כשהתת-רשימה מורחבת, כדי שהמצב פתוח/סגור יהיה חד-משמעי ויזואלית
- נבדק: תת-רשימה עם פריט אחד ועם כמה; הרחבת הפריט האחרון ברשימה (לא נחתך מאחורי ה-FAB); כמה תתי-רשימות פתוחות בו-זמנית; מצב בחירה מרובה (☑️) יחד עם הרחבה - אין התנגשות; בהיר/כהה, RTL/LTR

## v4.4.2 — כיווץ נוסף של כרטיס הפעילות (מדוד: 91px→75px)

### ✨/🐛 itinerary.html
- **הצ׳יפ/כפתור התפריט הכתיבו את גובה שורה 1**: `.action-menu-btn` המשותף (shared.css) גובה 40px, ו-`.join-chip` היה 38px - הרבה יותר מגובה הטקסט (שעה+כותרת, כ-22px). שורה 1 נמדדה בפועל ב-40px במקום ~22px. הפתרון: `margin-top/bottom:-8px` על `.activity-row1-end` - הצ׳יפ ותפריט 3-הנקודות ממשיכים להיראות בגודלם המלא (36-40px, נוח ללחיצה), בלי לנפח את גובה השורה שסביבם. גם `.join-chip` צומצם קלות (38px→36px, פונט .78rem→.74rem)
- **רווח פנימי מיותר**: היה תופעת לוואי ישירה של הבעיה הקודמת - עם שורה 1 בגובה 40px אך הטקסט בה רק ~22px, נשאר שטח ריק בתוך השורה מתחת לטקסט, לפני ה-gap המפורש (4px) לשורה 2. לא נדרש שינוי נפרד - נעלם עם התיקון הראשון
- **באג: שם היוצר נעלם בהערה ארוכה** - שורה 2 הייתה טקסט משורשר אחד (`הערה · שם`) עם חיתוך יחיד, כך שחיתוך ה-ellipsis יכול היה לבלוע את השם. פוצל לשני span נפרדים: `.activity-note` (`flex:0 1 auto`, מתכווץ ונחתך) ו-`.activity-owner-name` (`flex:0 0 auto`, קבוע, לעולם לא נחתך) - השם תמיד גלוי, צמוד ישירות אחרי סוף ההערה (בין השניים 0px רווח, נמדד)
- מדידה בפועל (כרטיס עם הערה קצרה): גובה כרטיס 91px→75px, שורה 1 40px→24px, גובה צ׳יפ 38px→36px
- הרחבה/קיפול בלחיצה על שורה 2 ממשיכה לעבוד עם המבנה החדש - במצב מורחב `.activity-note` עובר ל-`white-space:normal`, השם ממשיך לזרום מיד אחריו

### 🐛 תיקוני באגים (itinerary.html)
- כרטיס עם הערה ארוכה נדחק מחוץ לרוחב המסך, והצ׳יפ נחתך יחד איתו. השורש: `.cards` הוא `display:grid`, ופריט grid (`.activity-card`) מקבל כברירת מחדל `min-width:auto` - כלומר לא יכול להתכווץ מתחת לרוחב התוכן הפנימי שלו. טקסט `white-space:nowrap` בשורה 2 דחף את הכרטיס לרוחב תוכנו המלא, וה-grid track נאלץ לגדול בהתאם - ומתח איתו **כל** כרטיס אחר באותה עמודה, כולל כרטיס קצר לגמרי ("לילה טוב")
- התיקון: `min-width:0` על `.activity-card` (ועל `.activity-row1`/`.activity-row2`), ו-`minmax(0,1fr)` במקום `1fr` בעמודות ה-grid בדסקטופ - כך שפריט grid יכול להתכווץ לרוחב המיכל בפועל, וטקסט ה-nowrap+ellipsis בשורה 2 נחתך במקום לדחוף את הכרטיס. הרווח הגדול בין השורות היה תופעת לוואי של אותו overflow ונעלם עם התיקון
- **הרחבה/קיפול הערה בלחיצה**: זו הייתה חסרה לגמרי בפועל - שורה 2 הייתה CSS-truncation סטטי בלבד, בלי שום מנגנון להרחבה. נוסף עכשיו: לחיצה על שורה 2 מוסיפה/מסירה מחלקת `expanded` שמחליפה `white-space:nowrap` ל-`normal` (עטיפה טבעית לכמה שורות שצריך), לחיצה נוספת מחזירה לשורה אחת חתוכה. מצב ההרחבה מתאפס ברינדור מחדש של הרשימה, כמו שאר מצבי ה-UI הזמניים בעמוד הזה (למשל תפריט 3-הנקודות)

### ✨ כרטיס פעילות בשתי שורות במקום שש (itinerary.html)
- כרטיס פעילות תפס שש שורות (שעה, כותרת, הערה, "נוסף ע״י", מונה מגיעים נפרד, וכפתור הצטרפות נפרד) - נכנסו רק 2-3 כרטיסים למסך. עכשיו שתי שורות: שורה 1 - שעה + כותרת, ובקצה הנגדי צ׳יפ ההצטרפות + תפריט 3 הנקודות; שורה 2 - הערה · שם היוצר (בלי מפריד כשאין הערה)
- **צ׳יפ אחד מחליף שני אלמנטים**: המונה (עם ערימת האווטארים) וכפתור "אני בא/ה" הנפרד אוחדו לכפתור-צ׳יפ יחיד. לא מסומן: "אני בא/ה" (+ "· N" אם כבר יש אחרים, בלי מספר כשאין). מסומן: "N מגיעים ✓" ברקע צבע הטיול. לחיצה מחליפה מצב כמו קודם
- כותרת ארוכה גולשת לשורה נוספת בלי לדחוף את הצ׳יפ החוצה (`flex:0 0 auto` על קבוצת הצ׳יפ+תפריט); הערה ארוכה נחתכת בשלוש נקודות בשורה 2 כדי לשמור על הגובה הקומפקטי
- "קורה עכשיו" ותפריט 3 הנקודות לא נגעו בהם

### 🐛 תיקוני באגים
- **itinerary.html**: הכותרת וטאבי הימים נגללו יחד עם רשימת הפעילויות (בניגוד ל-packing/places/shopping) - כי הקובץ מעולם לא אימץ את תבנית "מעטפת קבועה + גלילה פנימית" שלהם, אלא גלל את כל המסמך. עכשיו `.shell` הוא `height:100dvh;overflow:hidden`, `.page-head`/`.user-chip`/`.day-tabs` קבועים (`flex-shrink:0`), ורק רשימת הפעילויות (`.cards`) גוללת (`flex:1;min-height:0;overflow-y:auto`, עם `align-content:start`). החלפת יום בטאבים מאפסת עכשיו את גלילת הרשימה לראש, במקום להשאיר אותה באמצע מהיום הקודם
- **חפיפת FAB**: בכל עמוד עם FAB (packing.html, places.html, itinerary.html) הפריט האחרון ברשימה חפף לכפתור הצף - הריפוד התחתון (86px) לא כיסה את מלוא גובה ה-FAB (148px). הועלה ל-`calc(160px + env(safe-area-inset-bottom))` בשלושתם

### ✨ הבהרה למצב בחירה מרובה (packing.html)
- לחיצה על כפתור ה-☑️ הראתה checkbox נוסף ליד כל פריט בלי שום הסבר - ה-`title`/`aria-label` הקיימים לא מוצגים במגע (אין hover במובייל). נוסף טוסט קצר בכניסה למצב: "מצב בחירה מרובה: סמנו פריטים ובחרו פעולה למטה"

## v4.3.2 — ניסיון תיקון ממוקד: לא ניתן לגלול חזרה לראש הוויזרד

### 🐛 תיקון (ממוקד, טרם מאושר במכשיר אמיתי)
- **wizard.html**: אחרי גלילה למטה וחזרה למעלה ב-iOS Safari, כותרת הוויזרד נחתכת מאחורי שורת הכתובת של כרום והעמוד מסרב לגלול עוד למעלה. נבדק ונשלל: `justify-content:center` על ה-body לא הבעיה - כבר קיים override ל-`flex-start` במובייל
- הבדל מבני אמיתי מול שאר העמודים: בעמודים שעובדים (packing/places/itinerary) שום דבר שמשתתף בחישוב גובה אזור הגלילה בפועל לא תלוי ב-dvh - הגלילה קורית באלמנט פנימי נפרד עם `flex:1;min-height:0` בלבד. בוויזרד אין אלמנט גלילה פנימי - הגלילה היא גלילת המסמך (body), ול-`.wizard-container` (הילד שקובע כמה body גבוה) היה `min-height:calc(100dvh - 58px)` - כלומר גם מסגרת הגלילה וגם התוכן שבתוכה תלויים ב-dvh בו-זמנית. זו קטגוריית באג מתועדת ב-WebKit (scrollHeight לא תמיד מתעדכן נכון אחרי שתוכן הנגזר מ-dvh גדל וחוזר ומתכווץ)
- התיקון: הוחלף `min-height:calc(100dvh - 58px)` ב-`flex:1` - הכרטיס ממלא את שארית המסך מתחת ל-top-bar דרך flexbox רגיל, בלי לגזור שום דבר מ-dvh מלבד ה-body הבסיסי (משותף לכל העמודים). תוקן רק במדיה קוורי מובייל - דסקטופ לא נגע
- לא ניתן לשחזר את הבאג המקורי (תלוי ב-toolbar דינמי אמיתי של iOS Safari) ולכן גם לא לאמת ישירות שהתיקון סוגר אותו - נדרש אימות ידני במכשיר. אם זה לא מספיק, תוכנית ב' היא מעבר למבנה גלילה פנימית כמו packing.html (משימה נפרדת)

## v4.3.1 — תפריט תחתון שצף גבוה מדי במובייל עם safe-area

### 🐛 תיקוני באגים
- **index.html / shopping.html**: `.bottom-nav` חישב את `env(safe-area-inset-bottom)` פעמיים - גם כריפוד פנימי וגם דרך היסט קבוע (`bottom:8px`) שלא כלל אותו. במכשיר עם פס תחתון (iPhone עם Face ID) זה הצטבר לכ-51px של שטח מת מתחת לאייקונים, והרצועה נראתה צפה גבוה מדי. שאר שלושת העמודים (itinerary/places/packing) לא נפגעו - הם משתמשים ב-`.bottom-nav` המשותף מ-shared.css שלא סבל מהבעיה. התיקון: ה-safe-area מחושב עכשיו רק במיקום (`bottom`), לא גם בריפוד
- **index.html**: ל-`.bottom-sheet` (המודל של פאנל האדמין ועוד) היה `max-height:85vh` בלי fallback ל-`85dvh`, בניגוד לכל שאר השימושים ב-vh באפליקציה

## v4.3.0 — שעון היעד, שדה תאריך חכם לפי סוג טיול, ובדיקת גרסה אוטומטית

### 🌍 אזור הזמן של היעד
- טיולים לחו״ל ומקומיים שומרים את אזור הזמן (IANA) של היעד בזמן היצירה, כולל עדכון כששינוי היעד בפאנל האדמין מגיאוקד מחדש
- `endAt` (מועד סיום/חזרה) מתפרש עכשיו לפי שעון היעד ולא שעון המכשיר של המארגן. `startAt` (המראה מישראל) נשאר במכוון בשעון המקומי של המארגן - זו לא אותה סמנטיקה, בכוונה
- כל חישוב "עכשיו"/"היום" באפליקציה (באדג׳ "מתרחש עכשיו" וטאבי הימים ב-itinerary, הבאנר החכם ורצועת מזג האוויר ב-index) מחושב לפי שעון היעד ולא שעון הצופה - חבר בישראל שמסתכל על טיול ברומא או בבנגקוק רואה עכשיו את השלב והיום הנכונים
- טיולים ללא אזור זמן שמור (reserve, טיולים ישנים מלפני התכונה) ממשיכים ליפול חזרה לשעון המכשיר בדיוק כמו קודם - אין מיגרציה

### ☁️ רצועת תחזית מזג אוויר ב-index
- נוספה רצועת תחזית לאורך ימי הטיול (Open-Meteo, ללא מפתח או ספרייה חיצונית), עם סימון היום הנוכחי וטשטוש הדרגתי לימים הרחוקים יותר
- כווצה בהמשך לכ-50% מהגובה המקורי לפי סעיף הצפיפות הוויזואלית

### 🕐 שדה שעה רק בטיולים לחו״ל
- טיולים מסוג local/camping/reserve מציגים שדה תאריך בלבד (`type="date"`) בוויזרד ובפאנל האדמין - שעת המראה רלוונטית רק לחו״ל
- `startAt`/`endAt` נשמרים כתחילת/סוף היום (00:00/23:59) כשאין שדה שעה, כך שהיום הראשון והאחרון בטיול נכללים במלואם
- החלפת סוג טיול באמצע מילוי הטופס משמרת את התאריך שכבר הוזן, בשני הכיוונים
- תוקנה רגרסיה שנחשפה בעקבות זה: שדה מסוג `date` פתח את הבורר הילידי רק בקליק מדויק על אזור האייקון שלו, לא בכל שטח השדה (בניגוד ל-datetime-local) - נפתר עם קריאה מפורשת ל-`showPicker()` בקליק על שדות תאריך בלבד

### 🔄 בדיקת גרסה אוטומטית
- GitHub Pages שומר כל קובץ (כולל HTML) במטמון עד 10 דקות ואין שליטה על כותרות ה-caching מהריפו - טאב פתוח לאורך זמן, או PWA שחוזר מהרקע, לא מרעננים שום דבר מעצמם ועלולים לשבת על קוד ישן בלי שום סימן
- בחזרה לטאב (`visibilitychange`) - לא polling תקופתי - version.js נבדק מחדש מול השרת ללא מטמון; אם הגרסה שונה מהטעונה, מוצגת רצועה קטנה וניתנת לסגירה בראש העמוד, בכל 6 העמודים (קוד יחיד ב-version.js, בלי לגעת בעמודים בנפרד)

### 🐛 תיקוני באגים
- **index.html**: `direction:rtl` קשיח שהיה מוגדר על ה-body דרס את מצב אנגלית (LTR) - הוסר
- **wizard.html**: לחיצה על "טיול חדש" ממסך ההצלחה בלי רענון עמוד השאירה מצב ישן מהטיול הקודם (באדג׳ יעד קודם, `selectedVibes` לא מנוקה, כפתור "הבא" תקוע במצב `loading`) - `resetWizard()` מנקה עכשיו הכל
- הוסרה קריאת `navigator.serviceWorker.register("sw.js")` מ-index.html - הקובץ לא קיים בריפו, הקריאה נכשלה בשקט בכל טעינה. service worker אמיתי, אם וכשיתווסף, יתווסף בכוונה עם אסטרטגיית invalidation מתוכננת

## v4.2.0 — תפריטי 3-נקודות בכל מקום, תיקוני מובייל, צבעי טיול מעודכנים

מספר הגרסה לא עודכן ברצף בזמן שהעבודות הבאות בוצעו - הרשומה הזו מרכזת את כולן יחד בדיעבד.

### 🎵 מוזיקה: תמיכה ב-SoundCloud + כרטיסים קומפקטיים
- נוסף זיהוי קישורי SoundCloud (`musicSourceFor`/`detectMusicTitle`/`musicSourceMeta`) דרך ה-oembed הרשמי שלהם
- כרטיסי מוזיקה כווצו: שם השיר עצמו הוא הקישור הלחיץ, כפתור "פתח" נפרד הוסר, עריכה/מחיקה עברו לתפריט 3-נקודות

### 📱 תיקון באג מובייל אמיתי ב-wizard.html
- מדיה קוורי במובייל הסתירה לגמרי את כפתורי "פתח"/"העתק קישור" בכרטיסי "הטיולים שלי" - נשאר רק מחיקה. הכפתורים צומצמו לאייקונים במקום שיוסתרו, כדי שהפונקציונליות תישאר נגישה

### 🃏 תפריטי 3-נקודות בכרטיסי מקום, פעילות ותגובה
- places.html: כרטיסי מקום - "ניווט" נשאר גלוי, "הוסף ליום"/עריכה/מחיקה עברו לתפריט
- itinerary.html: כרטיסי פעילות - "אני בא/ה" נשאר גלוי, עריכה/מחיקה עברו לתפריט
- places.html (טאב תמונות): תגובות על תמונה - עריכה/מחיקה עברו לתפריט 3-נקודות

### 📏 צמצום צפיפות ב-places.html (לפי מדידות getComputedStyle בפועל)
- רווח פנימי, גובה כפתורים וגדלי גופן צומצמו במספר מקומות בשלושת הטאבים (מקומות/תמונות/מוזיקה) - הכל אומת מול יעדי נגישות סבירים למובייל (כפתורים לא מעל 44-48px, ריפוד פנימי 12-16px)
- כפתורי "•••" ברשימת הציוד (packing.html) הוגדלו מ-38×38px ל-40×40px

### 🐛 תפריט התגובה שנפתח "מוסתר" + מקלדת שמכסה את שדה התגובה
- שורש הבעיה: תפריט ה-3-נקודות של תגובה היה `position:absolute` בתוך רשימת תגובות שגוללת (`overflow:auto`), ומעל טור המסך שגם הוא גולל - כשהתגובה קרובה לתחתית הרשימה, התפריט נחתך ונעלם
- התיקון: התפריט ממוקם עכשיו לפי `position:fixed` עם קואורדינטות שמחושבות מהכפתור עצמו בזמן פתיחה, נצמד לגבולות המסך, ונפתח כלפי מעלה כשאין מקום למטה. נסגר גם בגלילה
- בנוסף: פתיחת "תגובה" הייתה מפעילה `focus()` פרוגרמטית שפותחת את המקלדת ב-iOS Safari בלי לגלול את השדה לתצוגה - נוסף גלילה מפורשת לשדה כשהמקלדת נפתחת בפועל

### 🎨 עדכון צבעי קמפינג ומילואים
- קמפינג: מירוק-מנטה (#6EE7A0/#4F772D) לירוק "אמיתי" יותר (#52B788/#2C7755)
- מילואים: מכחול-פלדה (#7FB3E8/#55637A) לסגול (#A78BFA/#6C57A7)
- חו״ל ומקומי נשארו ללא שינוי. גרסת מצב בהיר נגזרה מגרסת מצב כהה לפי אותו יחס OKLCH שקיים בין הגרסאות הבהירות/כהות של חו״ל ומקומי, ואומתה ניגודיות WCAG AA

### 🤖 subagent חדש: ux-reviewer
- נוסף `.claude/agents/ux-reviewer.md` - סוכן ביקורת UX שרק בודק ומדווח, לא מתקן קוד בעצמו

## v4.1.0 — עוד סבב הבהוב שם + מספור גרסה אחיד

### 🐛 "הטיול שלנו" עדיין הבהב - נמצא ותוקן
- הבאג האמיתי: ב-places / packing / itinerary יש טריק ותיק למניעת הבהוב כיוון (RTL/LTR) - קריאה מוקדמת ל-`applyLanguage()` עם השפה השמורה, עוד לפני שפיירבייס ענה. הבעיה: `applyLanguage()` היא פונקציית "צייר הכל מחדש" מונוליטית שגם כותבת את שם הטיול - ועם `trip.destination` עדיין ריק באותו רגע, היא כתבה את "הטיול שלנו" **מעל** השם שכבר צויר מהמטמון. אותו דפוס בדיוק כמו הבאג הקודם ב-index (`applyTripPresentation`) - קריאה מוקדמת שדורסת ערך אחר שכבר תקין
- התיקון: דגל `tripDataLoaded`, בדיוק כמו ב-index - הקריאה המוקדמת כבר לא נוגעת בשם הטיול עד שהגיעה רשומת טיול אמיתית
- אומת בדפדפן אמיתי: שוחזר הבאג במדויק (מטמון עם שם ושפה, טעינה חדשה → "הטיול שלנו"), ואומת שהתיקון סוגר אותו - בשלושת העמודים

### 🔍 סריקה שיטתית לבאגים מאותה משפחה
נבדקו כל 6 העמודים בכמה זוויות:
- **ID כפולים** - אין
- **תקינות תחביר JS** בכל בלוק `<script>` (כולל מודולים) - הכל תקין
- **איזון סוגריים CSS** בכל עמוד + shared.css - תקין
- **מחלקות CSS שה-JS מפעיל אך לא מוגדרות בשום מקום** (בדיוק הדפוס שגרם לבאג של כפתור ההגדרות) - נמצא אחד: **מפת ההושבה במטוס** (`.seat`) הוסיפה מחלקת `reveal` בהדרגה (30ms על כל מושב) ליצירת אפקט "קופצים פנימה" - אבל לא הייתה שום הגדרת CSS ל-`.reveal`, אז זו הייתה קריאה סמויה לגמרי. נוסף מצב התחלתי שקוף+מוקטן ומעבר חלק, בכיבוד להגדרת "הפחת תנועה"
- **קריאות ל-ID שלא קיים** - אין

### 🔢 מספר גרסה אחיד לכל האפליקציה
- עד עכשיו מספר הגרסה היה כתוב ידנית בכל עמוד בנפרד: תגית `<!-- GitTrip vX.X.X -->` ב-4 עמודים (שנשארה תקועה על v4.0.0), טקסט גלוי בפוטר ב-2 עמודים אחרים (גם תקוע על v4.0.0) - בזמן ש-CHANGELOG.md כבר היה ב-v4.0.3. בדיוק אותה משפחת באגים: דבר שאמור להתעדכן בכמה מקומות, ומתישהו מפסיק
- נוצר `version.js` - מקור אמת יחיד: `window.APP_VERSION`. כל 6 העמודים טוענים אותו, ושני העמודים שמציגים גרסה בפוטר (index, shopping) קוראים ממנו בזמן ריצה במקום מחרוזת קשיחה
- מכאן והלאה: לעדכן מספר אחד ב-`version.js`, ולהוסיף רשומה תואמת ב-CHANGELOG.md - שני המקומות היחידים שצריך לגעת בהם

### 🤖 subagent חדש: coder
- נוסף `.claude/agents/coder.md` - subagent ייעודי לפיתוח על GitTrip, עם העובדות הקריטיות על הפרויקט (6 עמודים ללא build, shared.css, Firebase SDK ישן מול מודולרי, tr() לתרגום, דיאלוגים מותאמים, מצב כהה) ורשימת בדיקות לפני שמסמנים משימה כגמורה

---

## v4.0.3 — סוף להבהוב שם הטיול במעבר בין עמודים

### ⚡ שם הטיול נצבע מיד, בלי "הטיול שלנו" באמצע
- כל עמוד הגיע עם שם ממלא-מקום קשיח ב-HTML ("הטיול שלנו" / "הטיול הבא"), והשם האמיתי הוחלף רק אחרי שפיירבייס ענה - כלומר בכל מעבר בין עמודים
- אותו פתרון שכבר היה בשימוש לנושא ולמצב כהה: שם הטיול נשמר ב-`localStorage` לפי `tripId`, וסקריפט זעיר מיד אחרי הכותרת צובע אותו בזמן שהפרסר עוד בתוך `<body>` - לפני הציור הראשון
- ‏index שומר את הכותרת המורכבת ("שם + דגל") ואת `countryCode`, כדי שגם הדגל לא יקפוץ

### 🎨 שני באגים שהיו מאחורי חלק מה"רפרוש"
- **shopping לא החיל את ערכת הטיול בכלל בטעינה** - הסקריפט המוקדם שלו שיחזר רק בהיר/כהה, ולא את `theme-<סוג>`. התוצאה: כל טעינה התחילה בטורקיז ברירת המחדל ורק אז עברה לצבע של הטיול. עכשיו הוא זהה לשאר העמודים
- **index דרס את סוג הטיול השמור** - `applyTripPresentation()` רץ פעם אחת באתחול, לפני שהמידע הגיע, וכתב את ברירת המחדל `"abroad"` לתוך `trip-<id>-type`. כלומר ביקור בדף הבית הרס את המטמון, וכל שאר העמודים נטענו אחריו עם הפלטה הלא נכונה. עכשיו נכתב רק אחרי שהגיעה רשומת טיול אמיתית
- מאותה סיבה index גם התחיל מברירות המחדל שלו במקום מהמטמון - עכשיו הוא מאתחל את סוג הטיול, השם והמדינה מהמטמון

### ✅ אימות
- נמדד עם `PerformanceObserver`: הסקריפט שצובע את השם רץ ב-12-22ms, ו-first-contentful-paint ב-284-388ms. השם האמיתי נמצא ב-DOM כ-270-370ms לפני שהדפדפן מצייר משהו, כך שהממלא-מקום לא נצבע כלל
- הושוו `getComputedStyle` של כל אלמנט בכל 6 העמודים מול הגרסה הקודמת, עם מטמון ריק, על פני 4 ערכות × בהיר/כהה - כולם זהים לחלוטין

### ℹ️ מה עדיין יבהב
- בכניסה ראשונה לטיול חדש במכשיר, לפני שיש מטמון. מהביקור השני והלאה - לא
- אם המארגן משנה את שם היעד, מכשיר אחר יראה לרגע את השם הישן מהמטמון עד שפיירבייס עונה, ואז יתוקן

---

## v4.0.2 — כפתור "טיול חדש" באדמין + תיקון מחלקת hidden

### ✈️ יצירת טיול נוסף מתוך טיול קיים
- נוסף בתחתית פאנל האדמין סעיף "טיול נוסף" עם כפתור "יצירת טיול חדש" שמוביל לאשף. הטיול הנוכחי לא מושפע - נפתח טיול חדש ונפרד שהלוחץ הוא המארגן שלו
- הכפתור בתוך המודאל, שכבר חסום ל-`requireOrganizerAccess()`, כך שהוא ממילא זמין רק למארגן. עיצוב מתאר (outline) בצבע הנושא, כדי להבדיל אותו מכפתורי השמירה המלאים
- מתורגם לעברית ולאנגלית

### 🐛 מחלקת `hidden` לא הייתה מוגדרת באף עמוד
- באג ותיק: כל 6 העמודים משתמשים ב-`classList.toggle("hidden", ...)` מ-JavaScript, אבל אף אחד מהם לא הגדיר את הכלל `.hidden`. כל ההסתרות האלה פשוט לא עשו כלום
- **התוצאה שדווחה:** כפתור ההגדרות (⚙️) הופיע לכל אחד, לא רק למארגן. הבדיקה עצמה הייתה תקינה כל הזמן - `isOrganizer()` ו-`requireOrganizerAccess()` פעלו נכון, ולכן לחיצה החזירה "רק למארגן יש הרשאות". רק ההסתרה החזותית לא עבדה
- אותו באג הסתיר-לכאורה גם את כפתור "יציאה מהחשבון" (הופיע גם למי שלא מחובר) ואת 5 תגי הסטטוס על כרטיסי הפעולה בדף הבית (הופיעו ריקים), וב-wizard את דגל היעד שנשאר גלוי בזמן הקלדה מחדש
- הכלל `.hidden { display: none !important; }` הוגדר עכשיו פעם אחת ב-`shared.css`, לכל העמודים

### ✅ אימות
- הושוו `getComputedStyle` של כל אלמנט בכל 6 העמודים לפני ואחרי, על פני 4 ערכות טיול × בהיר/כהה
- כל ההבדלים הם אלמנטים שנושאים `hidden` (עברו ל-`display:none`) והמכולות שהתכווצו סביבם. המאפיינים שהשתנו הם גיאומטריה, `display`, וצל הפעימה של גלגל השיניים - **אף לא שינוי צבע, גופן, רקע או גבול**
- שימו לב: index ו-wizard שמים `data-theme` על `<body>`, ושאר העמודים על `<html>`. האימות הורץ בשני המסלולים

---

## v4.0.1 — shared.css: איחוד ה-CSS המשותף

ריפקטור בלבד. אין שינוי בהתנהגות או במראה של אף עמוד.

### 📦 קובץ CSS משותף
- נוצר `shared.css` שאליו הועבר כל ה-CSS שהיה משוכפל בין 6 העמודים. כל 6 הקבצים טוענים אותו לפני ה-`<style>` שלהם
- **683 שורות נמחקו מהעמודים, 42 נוספו.** ה-CSS המוטבע ירד מ-128KB ל-104KB
- מה עבר: ה-`@keyframes` המשותפים (`dialogFadeIn`, `dialogPopIn`, `overlayFadeIn`, `sheetSlideUp`, `auth-spin`), מערכת החלונות הקופצים `.custom-dialog-*` על האנימציות והזוהר שלה, ומסך טעינת האימות
- ‏places / packing / itinerary העבירו גם את מערכת ה-design tokens ומצב כהה במלואה (4 ערכות × בהיר/כהה), את התפריט התחתון, את ה-bottom sheet, ה-toast וה-FAB

### 🎛️ איך זה מוגדר
- שלושת העמודים שחולקים אוצר מילים אחד של טוקנים (`--surface`/`--text`/`--line`) נרשמים עם `<html data-tokens="core">`. index (`--card-bg`/`--text-main`), shopping (`--sea`/`--tab-bg`) ו-wizard (`--background`/`--border`) שומרים על הפלטה שלהם
- הסגר כתוב ב-`:where()` שתורם 0 לספציפיות, כך שכל סלקטור שומר בדיוק על אותו משקל שהיה לו בתוך העמוד
- החלונות הקופצים קוראים צבעים דרך שרשראות `var()` (למשל `var(--surface, var(--card-bg))`), כך שכל עמוד ממשיך להשתמש בטוקנים שלו
- ההבדלים שנשארו הוצאו ל-knobs שכל עמוד מגדיר ב-`:root` שלו: `--nav-max-width`, `--sheet-max-width`, `--fab-inset`, `--nav-label-size`, `--field-label-weight`, `--auth-loading-z`, `--auth-spinner-size`, `--dlg-danger-glow`

### ✅ אימות
- הושוו `getComputedStyle` של כל אלמנט בכל עמוד (כולל `::before`/`::after`) לפני ואחרי, על פני 4 ערכות טיול × בהיר/כהה, בשני רוחבי מסך (420px ו-1200px, כדי לכסות גם את ה-`@media (min-width:680/700px)`)
- כל 6 העמודים יצאו זהים לחלוטין. ההפרש היחיד: הזוהר של כפתור המחיקה ב-shopping במצב כהה נגזר עכשיו מ-`color-mix()` במקום `rgba()` כתוב ידנית - אותו צבע בדיוק, רק ייצוג טקסטואלי אחר

### 🚫 מה לא אוחד
- התפריט התחתון של index ו-shopping הוא רכיב אחר ("גלולה" צפה עם `.nav-item`, לא גריד עם `.bottom-nav a`) ונשאר בעמוד. איחוד היה משנה את המראה

---

## v4.0.0 — Dark Mode, AI, and Cross-Page Overhaul

זהו שדרוג מג'ורי - נגע בכל אחד מ-6 העמודים באפליקציה. מתועד כאן לפי נושא, לא לפי סדר כרונולוגי.

### 🎨 עיצוב ומצב כהה
- מצב כהה מלא בכל 6 העמודים, עם פלטת צבעים ייחודית לכל סוג טיול (חו״ל=טורקיז, מקומי=כתום, קמפינג=ירוק, מילואים=כחול-פלדה)
- שפת "glow" סלקטיבית - זוהר עדין רק על אלמנטים פעילים/ראשיים, לא בכל מקום
- איורים תלת-ממדיים מ-Fluent Emoji (Microsoft, MIT license) לכרטיסי הפעולה בדף הבית, עם נפילה חזרה (fallback) לאייקוני קו אם הטעינה נכשלת
- תפריט תחתון אוחד - אותם 6 אייקוני SVG בדיוק בכל עמוד (במקום 3 מערכות שונות שהיו קיימות)
- ברירת מחדל חכמה למצב כהה/בהיר לפי הגדרת המכשיר, עם זיכרון בחירה ידנית
- תיקון הבהוב מעבר-בין-עמודים (סקריפט מוקדם שקורא את הנושא לפני הציור הראשון של הדף) בכל 6 העמודים
- דף הבית דינמי לפי שלב הטיול (לפני/במהלך/אחרי) - כרטיסים מתחלפים ומוסתרים אוטומטית
- אייקון גישה למנהל (⚙️) גלוי עם פעימת תשומת-לב, במקום לחיצה משולשת נסתרת

### 🤖 בינה מלאכותית
- פונקציית ענן (Cloud Function) עם Gemini API להצעות ציוד חכמות, נפרסה בהצלחה
- תוקנו: כתובת שגויה, הרשאות גישה, שם מודל מיושן
- ⏳ פתוח: תקלת סנכרון תשלום בצד גוגל (לא תלוי בנו, דווח בפורום הרשמי שלהם)

### 💬 חלונות אישור מותאמים
- הוחלפו 36 שימושים ב-`alert()`/`confirm()` המובנים של הדפדפן בעיצוב ממותג ותואם-נושא, בכל 6 הקבצים
- כל חלון בכל עמוד נסגר גם בלחיצה מחוצה לו, לא רק בכפתור ביטול
- כל חלון נפתח באנימציית החלקה עדינה מלמטה (לא הייתה קיימת קודם באף אחד מהחלונות)
- כיבוד להגדרת "הפחת תנועה" (Reduce Motion) לנגישות

### 🗓️ תוכנית יומית
- זיהוי אוטומטי של "קורה עכשיו" לפי שעת הפעילות, עם טיפול חזותי שונה למצב בהיר (טבעת פועמת) וכהה (זוהר יציב)
- קישור דו-כיווני עם עמוד מקומות: כפתור "הוסף ליום" על כל מקום שמור, ו-autocomplete הפוך בשדה הוספת פעילות

### 🎒 רשימת ציוד
- קיבוץ חכם - זיהוי מילות מפתח (תרופות/תיק רחצה/אלקטרוניקה/מסמכים) עם הצעה אוטומטית להוספה כתת-פריט
- מבנה חדש: כותרת קבועה למעלה + רשימה גוללת בנפרד, שדה ההוספה עבר לכפתור צף

### 🛒 רשימת קניות
- אותו מבנה של כותרת קבועה + רשימה גוללת בנפרד

### 🔧 תיקוני יציבות ו-UX
- מחיקת טיולים מהאשף - תוקן כלל הרשאות ב-Firebase Security Rules
- שדה הוספת חבר בפאנל אדמין - ניגודיות טקסט במצב כהה, גודל, גלילה מעל המקלדת במובייל
- כמה מקרי RTL/LTR עם טקסט מעורב עברית-אנגלית (למשל "X days · Y hours to go")
- באג עדיפויות CSS שגרם לכרטיסי דף הבית "לקפוץ בחזרה" לגלוי אחרי הסתרה לפי שלב טיול
- באג מתיחת CSS Grid שגרם לריבועים ענקיים ריקים ברשימת ציוד קצרה
- הודעת "התנתקת בהצלחה" בעת יציאה מהחשבון

### ⏳ פתוח / נדחה לסבב נפרד
- הוספה/הסרה של מודולים (ציוד/קניות/וכו') דרך פאנל האדמין, אחרי שהטיול כבר נוצר
- שדרוג Node.js מגרסה 20 ל-22 בפונקציית הענן - הקובץ מוכן, ממתין לפריסה מחדש (עד 30/10/2026)
- תקלת AI בצד גוגל (סנכרון תשלום) - ממתינה לתיקון מצידם

---

## v3.1.0 ואילך (לפני שיחה זו)
ראו היסטוריית שיחות קודמת - כלל את בניית עמוד התוכנית היומית, אינטגרציית Spliit, שדרוג פונט, ותיקוני iOS.
