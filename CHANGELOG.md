# GitTrip — CHANGELOG

## v4.91.0 — achievements.html: geocoding ל-stateCode בביקור ידני בארה"ב (commit 2/4, שינוי סכמה)

### ✨ שינוי סכמה
`visits/{visitId}` מקבל שדה אופציונלי חדש: `stateCode` ("US-TX" וכו', ISO 3166-2:US) - נכתב רק לביקורים בארה"ב שבהם הוזנה עיר, `undefined`/חסר לכל שאר המקרים (כולל ביקורי ארה"ב בלי עיר, וביקורים ישנים - אין backfill, ר' חקירה קודמת: שם עיר לבד בלי state יכול להיות דו-משמעי, ולנחש בדיעבד מסוכן יותר משדה חסר).

**מנגנון**: `saveManualVisit()` קוראת ל-`window.geocodeLegDestination(cityName, "en")` הקיים (מ-`legGeocode.js`) רק כש-`countryCode==="US"` וגם הוזנה עיר - תמיד ב-`language:"en"` בלי קשר לעברית של הדף עצמו, כדי שההתאמה תהיה מול `usStates.js`'s `nameEn` בלבד, לא כפולה. **אומת בפועל מול Open-Meteo אמיתי**: "Austin, Texas"→US-TX, "Springfield, Illinois"→US-IL.

**וולידציה אקטיבית, לא רק בדיקת-חוסר**: התוצאה נבדקת מול ה-`countryCode` שה-geocoder עצמו פתר, לא רק נלקחת כמות שהיא - שם עיר חופשי בלי state (למשל "Paris" בזמן שנבחר US בקומבו, כשהכוונה האמיתית הייתה Paris, Texas) עלול להיפתר במדינה אחרת לגמרי; זה נתפס ונדחה, לא מנוחש. **אומת**: `geocodeStateForUSVisit("Paris")` → `null`, לא state שגוי.

**לא חוסם לעולם**: `try/catch` משלו + מסתמך גם על אי-הזריקה הקיימת של `geocodeLegDestination` עצמה - כישלון geocoding משאיר את `stateCode` חסר ומשך הכתיבה ל-RTDB ממשיך כרגיל. **עלות latency אמיתית, לא מוסתרת**: הקריאה ממתינה (`await`) לפני הכתיבה - שמירת ביקור בארה"ב עם עיר לוקחת קצת יותר זמן מכל ביקור אחר, במפורש לא ברקע.

### 🔍 מה שנבדק בפועל
- `geocodeStateForUSVisit` מול Open-Meteo אמיתי (לא פיקסצ'ר): שתי כתובות אמריקאיות אמיתיות תואמות נכון.
- מקרה הקצה של וולידציית ה-country - "Paris" נדחה נכון, לא מוחזר state שגוי.
- קלט ריק ומקום לא קיים - שניהם `null` בלי שגיאה.
- hook-בדיקה זמני הוסר ואומת (`grep -c "__test"` → 0).
- **לא נבדק קצה-לקצה דרך `saveManualVisit()` בפועל** - מצריך משתמש מאומת אמיתי (הסביבה כאן חסרת session מחובר); הלוגיקה המשולבת נבדקה בעיון קוד + בדיקת יחידה מלאה של `geocodeStateForUSVisit` עצמה. מומלץ בדיקה חד-פעמית בפרודקשן.

## v4.90.1 — תיקון: combobox בחירת מדינה (achievements.html) לא מוצא כמה מדינות אמיתיות

### 🐛 תיקון באג
דיווח: נייט חיפש "צ'כיה" ב-combobox הוספת ביקור ידני ולא מצא אותה, למרות שהיא כן קיימת ב-`window.COUNTRY_CONTINENT`. חקירה העלתה **שני באגים נפרדים**, שניהם תוקנו:

**באג א' - גרש עברי מול אפוסטרוף מוקלד**: `Intl.DisplayNames(['he'],{type:'region'}).of('CZ')` מחזיר `"צ׳כיה"` - עם **HEBREW PUNCTUATION GERESH** (`U+05F3`), לא אפוסטרוף ASCII רגיל (`U+0027`) שמקלדת מקלידה. `renderCountryList()` עשתה substring גולמי בלי שום נורמליזציה - אף אחת מ-3 הצורות הריאליות שמשתמש עלול להקליד (`'` ASCII, `'` מירכאה ימנית, או בלי סימן בכלל) לא תאמו. **נמצאו 13 מדינות** (מתוך 248) עם גרש/גרשיים בשם העברי הרשמי, כולן חשופות לאותו כישלון: `AZ` אזרבייג׳ן, `CL` צ׳ילה, `CZ` צ׳כיה, `DJ` ג׳יבוטי, `DZ` אלג׳יריה, `FJ` פיג׳י, `GS` ג׳ורג׳יה הדרומית ואיי סנדוויץ׳ הדרומיים, `JE` ג׳רזי, `JM` ג׳מייקה, `NE` ניז׳ר, `TD` צ׳אד, `TJ` טג׳יקיסטן, `UM` האיים המרוחקים הקטנים של ארה״ב (גרשיים).

**תיקון**: `normalizeForMatch()` הקיים (שעד כה שימש רק להתאמת בירות) חובר גם ל-`renderCountryList()`, והורחב לגם להסיר תווי גרש/גרשיים/אפוסטרוף/מירכאות (`'`, `"`, `’`, `“`, `”`, `׳`, `״`) - לא רק ניקוד. **נתפס תוך כדי בדיקה**, לא בחקירה המקורית: הרחבה ראשונית כללה רק את מחלקת הגרש היחיד (`'`/`׳`) - נבדק במפורש שהתיקון עובד גם על שם עם גרשיים (`ארה"ב`, קיצור) ונמצא שהוא **לא** תאם, כי מרכאות כפולות (`"`) לא היו במחלקת התווים המקורית. תוקן להוסיף גם את מחלקת המרכאות הכפולות, לפני ה-commit.

**באג ב' - שם CLDR רשמי שונה מהותית מהכינוי המדובר**: גם עם תיקון א', מדינות ששמן הרשמי בעברית שונה **לגמרי** (לא רק בסימן פיסוק) מהשם הנפוץ/הישן עדיין לא נמצאות. נוספה טבלת כינויי-חיפוש בלבד (`COUNTRY_NAME_ALIASES`) - **מרחיבה את החיפוש, לא משנה מה מוצג/נבחר בפועל** (נבדק: חיפוש לפי כינוי מציג ובוחר את שם ה-CLDR האמיתי, לא הכינוי). לא נבנתה בסריקה מכנית טהורה (בניגוד לבאג א') - "האם שם ישן/כינוי עדיין נחפש בפועל" הוא שיפוט, לא עובדה מהאפליקציה, ומסומן ברמות ודאות:

- **גבוהה** (מאומת ישירות מול Intl API, או מהדיווח המקורי): `SZ`→"סווזילנד" (שם רשמי עד 2018), `GE`→"ג'ורג'יה", `KR`→"דרום קוריאה", `KP`→"צפון קוריאה", `US`→"ארה\"ב"/"ארהב" (הקוד היחיד מתוך 248 שבו `Intl.DisplayNames` עם `style:"short"/"narrow"` שונה בפועל מ-`style:"long"` - עובדה נבדקת, לא ניחוש).
- **בינונית** (שמות רשמיים ישנים/כינויים מתועדים היטב): `GB`→"הממלכה המאוחדת", `CD`→"קונגו הדמוקרטית"/"זאיר" (שם רשמי עד 1997), `IR`→"פרס" (שם עד 1935), `ET`→"חבש" (שם מסורתי בעברית).
- **נמוכה/לא ודאית** (שמות ישנים ופחות שכיחים כיום, נכללו אך מסומנים): `TH`→"סיאם" (עד 1939), `LK`→"ציילון" (עד 1972), `KH`→"קמפוצ'יה" (1975-1989), `ZW`→"רודזיה" (שם קולוניאלי).
- **נשקל ונדחה במפורש**: `GB`→"אנגליה" - כינוי מדובר נפוץ מאוד, אך שגוי גאוגרפית (אנגליה היא רק אחת מ-4 המדינות המרכיבות את בריטניה, לא מילה נרדפת לה) - הוחלט שהסיכון לבלבול גובר על התועלת.

### 🔍 מה שנבדק בפועל
- **כל 13 מקרי גרש/גרשיים** (סעיף א') - חיפוש עם אפוסטרוף ASCII רגיל (או מרכאות ASCII ל-`UM`) - כולם נמצאים נכון, כולל `UM` שדרש את תיקון מחלקת המרכאות הכפולות (ר' למעלה).
- **כל 13 הכינויים בטבלה** (סעיף ב', 4 קודים ברמה גבוהה + 9 נוספים) - כל כינוי נמצא ומחזיר את הקוד הנכון.
- **בדיקת בחירה מקצה-לקצה בפועל** (לא רק לוגיקה): הקלדת "סווזילנד" בשדה האמיתי → הצעה מוצגת כ-"אסוואטיני" עם `data-code="SZ"` → לחיצה → שדה הקלט מציג "אסוואטיני" (השם האמיתי, לא הכינוי) - מאמת את דרישה 3 (כינוי מרחיב חיפוש בלבד, לא משנה בחירה/תצוגה).
- רגרסיה: חיפושים רגילים קיימים ("צרפת", "גרמניה", "France", שאילתה חסרת התאמה) - התנהגות זהה לפני התיקון.
- hook-בדיקה זמני (`window.__test`) הוסר ואומת שהוסר (`grep -c "__test"` → 0) לפני ה-commit.

## v4.90.0 — achievements.html: רשת התגים + הסטטיסטיקה עוברים לטאב "הישגים" רביעי

### ✨ תכונה חדשה
טאב רביעי ("הישגים") נוסף ל-toggle הקיים (מדינה/עיר/יבשת) ב-`achievements.html`. רשת 7 התגים + כל הטקסט הסטטיסטי (פילוגים לפי יבשת/מדינה, כרטיס הבירות) - שנוספו מעל הגלובוס ב-v4.89.0 - עברו להיות תוכן הטאב הזה בלבד, לא עוד קבועים מעל הגלובוס תמיד. **מסלול הדרגות (5 השלבים) נשאר קבוע בראש הדף בכל 4 הטאבים ללא שינוי** - ה"הירו" היחיד שממשיך להיות תמיד גלוי.

**מנגנון**: `applyPanelVisibility(view)` חדש (עצמאי מ-`applyView()` הקיים, שממשיך לשלוט אך ורק בציור הגלובוס עצמו) מחליף את כל אזור התוכן מתחת לטאבים - `globeContainer`+`fpsHud` מוסתרים ו-`achievementsPanel` (רשת+סטטיסטיקה) נחשף, ולהפך. לא מגודר מאחורי קיום ה-globe (`world`) כמו `applyView()` - זו נראות גרידא, נכון להריץ גם לפני שהגלובוס סיים להיטען.

**גלילה**: העמוד עצמו נשאר ללא גלילה (`overflow:hidden` ב-`html`/`body`, ללא שינוי). `achievementsPanel` מקבל `overflow-y:auto` משלו - בבדיקה במסך נמוך (420px) התוכן שלו (268px) עלה על השטח הפנוי (179px) וגלל *בתוך עצמו* בהצלחה, בלי שהעמוד כולו זלג לגלילה. בטאבי הגלובוס הרגילים לא נדרשה שום תוספת "נגד מסך ריק" - `globeContainer` הוא `flex:1` ממילא, אז ברגע שרשת התגים+הסטטיסטיקה כבר לא תופסים שטח קבוע מעליו, הגלובוס פשוט מקבל בחזרה את השטח שהתפנה (אומת: 431px מתוך 678px גובה בדיקה, לעומת 261px לפני השינוי) - לא "מסך ריק", ההפך.

### 🔍 מה שנבדק בפועל
- מעבר בין כל 4 הטאבים (מדינה→הישגים→מדינה) - `globeContainer`/`achievementsPanel`/`fpsHud` מקבלים בדיוק את מצב ה-`hidden` הנכון בכל מעבר.
- רשת התגים + הטקסט הסטטיסטי נבדקו יחד בטאב "הישגים" עם דאטה מדומה (12 מדינות, כולל בירה אחת) - שניהם מרונדרים נכון זה לצד זה מתחת לרשת.
- גלילה פנימית: מסך 375×420 (נמוך מכוון) - `achievementsPanel.scrollHeight`(268) > `clientHeight`(179) עם `overflow-y:auto` בפועל, בעוד `document.body.scrollHeight === clientHeight` (420===420, אין גלילת עמוד).
- שטח הגלובוס בטאבי מדינה/עיר/יבשת גדל בחזרה (431px מתוך 678px, לעומת 261px כשה"תגים" עוד ישבו קבוע מעליו) - אין "מסך ריק גדול מדי".
- מובייל (375px) ודסקטופ, בהיר וכהה - נבדק חזותית בשני המצבים, כולל טאב "הישגים" הפעיל.
- hook-בדיקה זמני (`window.__test`) הוסר ואומת שהוסר (`grep -c "__test"` → 0) לפני ה-commit.

## v4.89.0 — achievements.html: דרגת חוקר (מסלול 5 דרגות) + רשת 7 תגים + חגיגת פתיחה

### ✨ תכונה חדשה
מעל הגלובוס/הסטטיסטיקה הקיימים ב-`achievements.html` (שנשארים למטה כהקבלה תומכת, ללא שינוי): **מסלול דרגות אופקי גלילי** (5 דרגות, לפי סה"כ מדינות ביקור - 🎒1+ · 🧭5+ · 🗺️10+ · 🌍25+ · 👑50+, בהשראת Duolingo - תחנות שהושגו צבועות, הנוכחית זוהרת/פועמת, עתידיות גלויות אך אפורות) **+ רשת 7 תגים** (grid אמיתי, לא גלילה - החלטה מכוונת שהרשת תיראה שונה מהמסלול מעליה): מדינה ראשונה, 3+ יבשות, 5 בירות, 10 מדינות בשנה קלנדרית אחת, 5+ יעדים שונים בטיול אחד, כל היבשות חוץ מאנטארקטיקה, 5+ מדינות באירופה. תג נעול מציג סילואט אפור + "X/Y" כשיש ציר התקדמות מספרי.

**פתיחת דרגה/תג חדשים**: שכבה מלאה-מסך עם קונפטי (`canvas-confetti`, MIT, ~6KB gzipped, נטען lazy מ-jsDelivr באותו דפוס `import()` ל-UMD שכבר קיים בקובץ הזה עבור globe.gl), נסגרת בנגיעה בכל מקום. מספר פתיחות בו-זמנית (למשל משתמש ותיק שכבר עבר כמה ספים לפני שהתכונה עלתה) מוצגות בתור, אחת בכל פעם - לא מסך-סיכום מרוכז.

**מיקום בעמוד - החלטת מוצר מפורשת**: `achievements.html` בנוי כעמוד ללא גלילה בכלל (`overflow:hidden`, `height:100dvh`) - הגלובוס תופס את כל השטח שנשאר. מסלול הדרגות + רשת התגים **קבועים למעלה, מעל הסטטיסטיקה/גלובוס, והגלובוס מצטמצם** כדי לפנות מקום - לא נגררים לגיליון (sheet) נפרד, נבחר במפורש מול האלטרנטיבה.

### 🧮 שתי החלטות עיצוב-נתונים
**תג "10 מדינות" מול דרגה 3 ("חוקר מפות", גם היא 10+)** - חפיפה מלאה בסף המקורי. **נבדל, לא אוחד**: התג עבר לציר **שנה קלנדרית אחת** (`visitedAt.getFullYear()`, לא סה"כ-חיים מצטבר) - ציר קצב, לא רק עוד ייצוג לאותו מספר.

**תג "5+ יעדים בטיול אחד"** - במקור תוכנן על `users/{uid}/visits` (`tripId`), אך **נמצא שאין היום שום כותב-חי שממלא `tripId` על visit** (`saveManualVisit` לא כותב אותו בכלל; `source:'app'` קיים רק כ-backfill חד-פעמי לא-אוטומטי). **הוחלף בעודף למקור נתונים חי אמיתי**: `trips/{tripId}/legs[]`, דרך `users/{uid}/trips` (אינדקס אישי לכל טיול שהמשתמש חבר בו - בעלים או מצטרף, נכתב כבר בכל 4 העמודים הרלוונטיים) → `window.getLegs()` לכל טיול → `window.isRealVisit()` (פונקציה שכבר הייתה קיימת ב-`legs.js`, נכתבה מראש בדיוק בשביל התכונה הזו לפי התיעוד שלה, ומעולם לא נקראה עד עכשיו) מסננת עצירות-ביניים חד-יומיות (למשל דוחה בטרנזיט) → דה-דופליקציה לפי שם יעד מנורמל (טיול הלוך-חזור לאותו יעד נספר פעם אחת, לא פעמיים). `legs.js`/`datetime.js` נטענים כעת גם ב-`achievements.html` (לא היו קודם - העמוד "חוצה-טיולים" ולא החזיק בעבר אף תלות ב-legs).

**אין כלל RTDB חדש** - `users/{uid}/gamification` (currentLevel + seenBadges, לצורך "מה כבר נחגג") מכוסה במלואו על ידי כלל `users/$uid` הקיים.

### 🔍 מה שנבדק בפועל
- כל ספי הדרגות (0/1/4/5/9/10/24/25/49/50/51 מדינות) - `computeCurrentLevelIndex` מחזירה את האינדקס הנכון בכל גבול.
- `maxCountriesInOneCalendarYear` נבדק עם 12 מדינות ב-2025 + 3 ב-2026 - מחזירה 12, לא 15.
- **דה-דופליקציה של תג #5 נבדקה במפורש עם פיקסצ'ר שמדמה בדיוק את המקרה שהתבקש לבדוק**: טיול עם עצירת-ביניים חד-יומית (דוחה, אמורה להיפסל ע"י `isRealVisit`) ויעד חוזר (רומא בתחילת וסוף אותו טיול, אמור להיספר פעם אחת) - תוצאה: 3 יעדים ייחודיים (רומא/בנגקוק/טוקיו), לא 5 ולא 4. `window.getLegs`/`window.isRealVisit` אומתו כטעונים ותקינים גם בהקשר `achievements.html` (module script, לא רק בהקשר הקלאסי שהם כבר הוכחו בו).
- מחזור מלא evaluate→render→diff→תור-חגיגות→RTDB נבדק קצה-לקצה עם משתמש מדומה שכבר "מפגר מאחור" (15 מדינות, מעולם לא נשמר עבורו מצב) - מתור 3 דרגות + 3 תגים כמצופה מסימולציית backfill, כרטיסי הדרגה/תג ברשת מציגים נעול/פתוח/התקדמות נכון (למשל `2/3` יבשות, `0/5` בירות).
- **נתיב הכתיבה ל-`users/{uid}/gamification` נבדק בפועל מול Firebase אמיתי, לא מדומה**: קריאת `set()` עם uid מזויף חזרה עם `PERMISSION_DENIED` אמיתי מהשרת (מאמת שכלל `users/$uid` הקיים אכן חוסם, בלי כלל חדש) ו-`.catch` תפס את זה בשקט (console.warn, לא קריסה) - בדיוק כמו `logWriteEvent`/`mirrorVisitorStats` הקיימים.
- נבדק במסך 375px וגם בשני מצבי הבהירות (בהיר/כהה) - אין גלילת עמוד נוספת (scrollHeight===clientHeight), הגלובוס עדיין מקבל שטח סביר (261px מתוך 700px גובה בדיקה).
- hook-בדיקה זמני (`window.__test`) הוסר ואומת שהוסר (`grep -c "__test"` → 0) לפני ה-commit.
- **לא נבדק (מגבלת הסביבה)**: `computeMaxDestinationsInTrip` מול `users/{uid}/trips` אמיתי עם משתמש מאומת אמיתי - הסביבה כאן חסרת session מחובר; רק תת-הלוגיקה הטהורה (dedup/isRealVisit) אומתה כמתואר למעלה. מומלץ לבדוק פעם אחת בפרודקשן על משתמש עם טיול מרובה-יעדים אמיתי לפני שסומכים על התג הזה במלואו.

## v4.88.0 — רישום אירועי sign-in (7 עמודים) + דשבורד: גרף כניסות וזיהוי אנומליות

### ✨ תכונה חדשה - רישום, 7 עמודי האפליקציה
כל 7 עמודי האפליקציה (6 העמודים הראשיים + achievements.html) רושמים עכשיו אירוע ל-`_analytics/signIns` בפעם הראשונה בכל טעינת עמוד שבה מצב ה-auth עובר מ"לא מחובר" ל"משתמש אמיתי" - לא בכל רינדור חוזר.

**כלל RTDB חדש** (`_analytics/signIns`) - `.write: "auth != null"`, כמו `_analytics/writes` הקיים (**לא** כמו `visitorStats` - המבנה המבוקש הוא push-key (`{eventId}: {uid,timestamp,file}`), לא `$uid`, אז אי אפשר להגביל כתיבה לפי `auth.uid === $uid` באותה צורה). נוספה גם `.validate` על שדה ה-`uid` (`newData.val() === auth.uid`) - סוגר פער קטן שקיים גם ב-`writes` הקיים (משתמש יכול טכנית לכתוב uid מזויף) - לא חובה, אבל חינמי להוסיף ומשפר את איכות הדאטה למי שקורא (נייט בלבד, כרגיל).

**מנגנון הזיהוי - נבדק בפועל שהוא **לא** אחיד בין העמודים**:
- **index.html, wizard.html, shopping.html, packing.html, places.html, itinerary.html** - יש להם כבר משתנה מודול (`currentUser`/`user`) שנדרס בתוך `handleAuthStateChanged`. נלכד `wasSignedOut = !currentUser` (או `!user`) **כשורה הראשונה, לפני** הדריסה.
- **achievements.html** - **אין** לו משתנה כזה בכלל (המנגנון הקיים משתמש בדגלים `globeStarted`/`visitsListenerStarted`). נוסף דגל אחות חדש `signInLogged`, **נפרד** מ-`globeStarted` (לא שוכפל/עורבב איתו - שני עניינים לא קשורים).
- **wizard.html** - לא היה לו `logWriteEvent`/`_analytics/writes` logging בכלל קודם (רק 6 מתוך 7 העמודים כבר כתבו ל-`_analytics/writes`) - נדרש גם להוסיף `push`/`set` ל-import של Firebase modular שם.

**הערה חשובה על מה שהמדד הזה בפועל מודד**: הטריגר הוא "מעבר null→user בתוך טעינת עמוד אחת", לא "לחיצה על כפתור התחברות" - רענון טאב של משתמש שכבר מחובר **גם הוא** יירשם כ"מעבר", כי המשתנה המודולי מתאפס בכל טעינה מחדש. זה קרוב יותר ל"כמה פעמים אימות Firebase הצליח בטעינת עמוד" מאשר ל"כמה פעמים מישהו התחבר בפועל" - בדיוק הסיבה שדה-דופליקציה לפי uid+יום בגרף הדשבורד (למטה) פותר את זה נכון בשכבת התצוגה, לא דורש תיקון כאן.

### ✨ תכונה חדשה - דשבורד (dashboard.html, ללא version bump משלו - כלי פנימי, ר' למטה)
4 כרטיסים חדשים ב-dashboard.html: כרטיס KPI ("כניסות ייחודיות היום") + 3 גרפי קו לאורך זמן (כניסות ייחודיות, הצלחת כתיבה יומית, הצלחת קריאות שרת יומית), כולם עם **זיהוי אנומליות ויזואלי משותף**.

**מנגנון האנומליה** (`computeAnomalyFlags`, פונקציה אחת משותפת לשלושת המדדים): לכל יום, ממוצע נע של 7 הימים *שקדמו* לו (לא כולל היום עצמו - אחרת יום חריג "מדלל את עצמו"). מסומן אם `value < movingAvg*0.7` (ירידה של 30%+) **וגם** `movingAvg >= 3`. **הסף המינימלי-המוחלט (3)**: בלי זה, בסקייל קטן (משתמש אחד-שניים ליום כרגע) כל תנודה רגילה (2→1) הייתה "ירידה של 50%" ומסומנת כאנומליה - רעש קבוע, לא איתות אמיתי. 3 נבחר כי זה נותן מרווח קטן אך אמיתי (למשל 4→2 עדיין יסומן, 3→2 לא) בלי לדרוש היסטוריה גדולה מדי לפני שהמנגנון בכלל מתחיל לפעול. ימים 1-6 בכל סדרה (אין עדיין 7 ימי היסטוריה) - לא מסומנים, לא "false anomaly" על נתונים חסרים.

**ייצוג ויזואלי**: נקודה חריגה מקבלת `CHART_BAD` (אדום) **וגם** רדיוס גדול יותר (6 במקום 3) **וגם** שורת tooltip נפרדת ("⚠ ... (חריגה מהממוצע)") - צבע אף פעם לא הסימן היחיד, אותו כלל שכבר חל על כל אינדיקטור good/bad אחר בדף הזה.

**מה חדש אלגוריתמית מול מה שהועתק**:
- `computeSignInsTimeline` - דה-דופליקציה אמיתית (`Set` של uid לכל bucket, `.size` לא `.length`) - היחיד מבין 3 שדרש את זה; לא קיים בשום גרף קודם בדשבורד.
- `computeDailySuccessRateTimeline` (חדש, משותף להצלחת-כתיבה והצלחת-שרת) - עד עכשיו לא היה שום breakdown יומי למדדי ההצלחה האלה בדשבורד בכלל (רק אגרגט כולל + breakdown לפי context/function, בלי ציר זמן) - זה מימוש חדש, לא רק "צביעה מחדש" של גרף קיים.
- `chooseDateGranularity`, מבנה ה-render (`renderAnomalyLineChart`, מבוסס במלואו על `renderTripsTimelineChart` הקיים), `CHART_GOOD`/`CHART_BAD`, `updateKPI` - כולם שימוש חוזר ישיר, לא נכתבו מחדש.

**מיקום**: כל 4 הכרטיסים נרשמו ב-`LAYOUT_SECTIONS`/`LAYOUT_CARD_GROUPS` (מנגנון ה-DnD משלב 7) - `kpiSignInsToday` ל-`kpiGrid`, שלושת הגרפים ל-`writesChartsSection` (שהתווית שלה עודכנה מ"גרפי כתיבה ושרת" ל"גרפי כתיבה, שרת וכניסות" - נשאר מדויק מול התוכן בפועל).

### 🔍 מה שנבדק בפועל
- `node --check` נקי על כל בלוקי ה-script בכל 7 העמודים + dashboard.html.
- `database.rules.json` - JSON תקין (`JSON.parse` נקי). הכלל עדיין לא פורסם ב-Firebase Console - צריך פרסום ידני נפרד, כמו כל שינוי rules קודם בפרויקט הזה.
- `computeAnomalyFlags` נבדק ישירות עם ערכים מוכרים: רצף של 7×10 + יום עם 6 (ירידה 40%) - מסומן נכון; רצף של 7×2 + יום עם 1 (ירידה 50%, אבל ממוצע<3) - **לא** מסומן, מאמת את הסף המינימלי בפועל.
- `computeSignInsTimeline` נבדק עם דאטה מדומה הכוללת ביקורים כפולים (אותו uid, אותו יום) - התוצאה מדדפת נכון (5 משתמשים ייחודיים, לא 10 אירועים).
- **הענן הוויזואלי אומת ישירות מתוך מופע Chart.js החי** (לא רק מהדאטה המחושבת) - `pointBackgroundColor`/`pointRadius` של הנקודה החריגה הם `#F87171`/6, כל שאר הנקודות `#2DD4C8`/3. אותה בדיקה חזרה על `computeDailySuccessRateTimeline` (ירידה מ-90% ל-50% ביום בודד) - אנומליה מזוהה נכון.
- מצב ריק (`{}`) לכל 3 הגרפים - "אין נתונים עדיין" מוצג נכון, לא קורס.
- מצב עריכה (`layoutEditBtn`) - כל 4 הכרטיסים החדשים מקבלים `.layout-card-handle` בפועל, נספרים נכון בתוך `kpiGrid`(5 כרטיסים)/`writesChartsSection`(6 כרטיסים), תווית הסקציה מעודכנת.
- hook-בדיקה זמני (`__testInit`/`__testSignIns`/`__testDailyRate`/`__testAnomalyFlags`/`__testComputeSignIns`) הוסר ואומת שהוסר (`grep -c "__test"` → 0) לפני ה-commit.

**תכונה חדשה ב-7 עמודי האפליקציה (לא תיקון באג) → minor bump ל-version.js. dashboard.html - כלי פנימי, ללא version bump משלו, עקבי עם כל שינוי קודם בו.**

## v4.87.0 — ביטול הצמצום המבני של gittrip-showcase.html, הוספת מסך ההישגים

### ↩️ שחזור, לא תיקון - חזרה למבנה המקורי
הוחלט לבטל את הצמצום המבני מ-v4.86.0 (26 יחידות תוכן -> 2 פיצ'רים) - המבנה המלא (5 תחנות, 10 touches, 5 קוביות מודול, 7 תגיות אופי-טיול, 4 swatches) חזר במלואו, בלי לצמצם דבר. שוחזר ידנית מתוכן ה-commit שקדם לצמצום (3695732 - אחרי מיגרציית ה-domain ואחרי בניית שדה הטעימה, לפני הצמצום) ולא דרך `git revert` - כי ה-commit של הצמצום (b11638e) כלל גם את תיקון היררכיית הכותרת (רצוי לשמר), אז revert ישיר היה מבטל את שניהם ביחד.

**נשמר במלואו** (לא שוחזר לגרסה הישנה):
- **היררכיית הכותרת** - "GitTrip" נשאר `<h1>` (לוקאפ עם האייקון), הכותרת המקורית ("הטיול הבא שלכם, מתוכנן ביחד") חזרה בדיוק כמו שהייתה - אבל כ-`<h2>`, לא כ-H1 כמו לפני v4.86.1.
- **תיקוני CTA/domain** - קישור יחסי ל-wizard.html, בלי target="_blank" (במקרה בלי tripId), meta tags ל-gittrip.app.
- **שדה הטעימה** (v4.84.0) - geocoding+מזג אוויר אמיתיים, ללא שינוי.

### ✨ נוסף - מסך ההישגים (הגלובוס)
תכונה שלמה (`achievements.html`, שוחררה 2026-09-20 - אחרי העדכון התוכני האחרון של דף השיווק ב-2026-09-10) לא הופיעה בדף בשום צורה. נוספה כתחנה שישית (`stop6`), באותו דפוס בדיוק כמו 5 התחנות הקיימות (stop-marker+eyebrow+h2+p, בלי דימוי/placeholder - עקבי עם שאר התחנות המקוריות שאף אחת מהן לא כוללת תמונה). ממוקמת אחרונה, ממש לפני ה-CTA - נקודת סיום טבעית ("תמשיכו לחזור"), לא תוכן באמצע זרימת התכנון.

### 🔍 סריקה לשינויים רלוונטיים נוספים מאז 10/9 (לא dashboard.html)
נסרקו כל ה-commits בין 2026-09-10 להיום. מלבד הגלובוס, **לא נמצא שום שינוי/תוספת נוסף שמצדיק הוספה לדף השיווק**. הרוב המכריע הוא עבודת אמינות/אבטחה/נגישות בלתי-נראית למשתמש (safeWrite, WCAG contrast, form labels, RTDB rules, לוגינג אנליטיקס) או תיקוני באג לתוכן שכבר מוצג (אייקון/טקסט "זיכרונות", קומבו בחירת מדינה). נבדק ספציפית ונפסל: "index.html: אפשור עריכת תאריכי טיול כולל בטיולים רב-יעדים" (1f16412) - תיקון-תקלה בפאנל אדמין, לא יכולת חדשה שמצדיקה איזכור שיווקי.

### 🔍 מה שנבדק בפועל
- `node --check` נקי, `i18n-audit` - אותו ממצא יחיד קיים-מראש, 0 חדשים.
- `grep` אחר `nelevy1-sketch` (הדומיין הישן) - 0 מופעים; `legGeocode.js` - נוכח.
- נבדק בדפדפן חי: `.cta-btn` חזר למופע יחיד (לא 3 כמו ב-v4.86.0), `ctaBtn.href`="wizard.html" בלי target, meta tags עם gittrip.app - כל התיקונים שרדו.
- `document.querySelectorAll('.stop').length` === 6 בפועל - כל 5 התחנות המקוריות + התחנה החדשה.
- שדה הטעימה נבדק מחדש עם הקלדה אמיתית ("Lisbon") - geocoding+מזג אוויר אמיתיים, כפתור-המשך מוצג - לא נשבר מהשחזור.
- בלי צילומי מסך (כמו שסוכם) - כל האימות דרך קריאת DOM/`textContent` בפועל.

**שחזור תוכן + תכונה חדשה לדף שיווק → minor bump.**

## v4.86.1 — תיקון היררכיית כותרת בראש gittrip-showcase.html (המשך v4.86.0)

### 🐛 הבאג
בכותרת החדשה מ-v4.86.0, "GitTrip" עצמו לא היה כותרת כלל - ה-H1 היה משפט הכאב ("לא עוד קבוצת וואטסאפ עמוסה...") עם "GitTrip" מוזכר רק בתוך שורת התת-כותרת, בגודל קטן. סידור לא נכון עבור כותרת ראשית של דף מותג.

**תוקן** (סידור מחדש בלבד, בלי לשנות ניסוח קיים):
- **"GitTrip" עצמו הפך ל-`<h1>`** - לוקאפ חדש (`.brand-lockup`) עם האייקון הקיים (`#gtIcon`, אותו symbol שכבר שימש למסך הפתיחה) לצד השם, בעיצוב הגרדיאנט/גודל/משקל שה-H1 הישן (משפט הכאב) קיבל קודם.
- **משפט הכאב הקיים ירד ל-`<h2>`** (אותו id, `heroTitle`, אותו טקסט מדויק - עדיין `var(--text)` מלא, לא מוחלש ל-muted, כי זה עדיין המסר המרכזי) - גודל/משקל קטנים יותר מה-H1, בלי גרדיאנט.
- **הוסרה הכפילות** ב-`heroSub` - "GitTrip — מקום אחד, עדכני לכולם, בזמן אמת." → "מקום אחד, עדכני לכולם, בזמן אמת." (וכנ"ל באנגלית, עם אות ראשונה גדולה מחדש - "One place..." במקום "one place..." - שינוי מכני נדרש בעקבות הסרת התחילית, לא ניסוח חדש).

### 🔍 מה שנבדק בפועל
- `node --check` נקי, `i18n-audit` - אותו ממצא יחיד קיים-מראש, 0 חדשים.
- `grep` אחר `.plane`/`.hero h1` הישנים - 0 מופעים פונקציונליים נותרים.
- נבדק בדפדפן חי, עברית ואנגלית: `document.querySelector('.hero h1').tagName` → GitTrip ב-H1 בפועל, `heroTitle` ב-H2 בפועל, טקסטים תקינים בשתי השפות, RTL/LTR לא נשברו, שאר הדף (CTA-חוזר, פיצ'רים, placeholder-ים) לא הושפע.

**תיקון סידור/עיצוב (לא תוכן/פיצ'ר חדש) → patch bump.**

## v4.86.0 — צמצום מבני מלא של gittrip-showcase.html - 26 יחידות תוכן ל-2 פיצ'רים מרכזיים

### ✨ שינוי משמעותי בתוכן/מבנה - לא תוספת, החלפה
דף השיווק היה בנוי מ-5 "תחנות" גלילה, 10 "touches" בודדים, 5 קוביות מודול, 7 תגיות אופי-טיול ו-4 swatches - 26 יחידות תוכן נפרדות לפני שמגיעים ל-CTA היחיד בתחתית (עייפות גלילה, לא ריבוי CTA-ים - היה רק כפתור אחד גם קודם). נבנה מחדש ל-**2 פיצ'רים מרכזיים בלבד**, עם חזרה על ה-CTA אחרי כל אחד.

**כותרת ראשית**: `heroSub` הקיים (הניסוח אושר כבר טוב) הועלה ל-`<h1>` עצמו, מחולק לשני משפטים - הראשון (כאב) ככותרת, השני (המוצר) כתת-כותרת. הטקסט הישן של `heroTitle` ("הטיול הבא שלכם, מתוכנן ביחד") הוסר - לא הומצא ניסוח חדש, רק הועלה טקסט קיים.

**פיצ'ר 1/2 - מסך ההישגים (הגלובוס, `achievements.html`)**: תוכן חדש לגמרי - לא היה מוזכר בדף בכלל קודם (הדף נכתב לפני שהגלובוס נבנה, ר' החקירה הקודמת). כותרת+גוף חדשים, בעברית ואנגלית.

**פיצ'ר 2/2 - מסלול רב-יעדים + תוכנית יומית**: מיזוג של תוכן קיים בלבד, לא הומצא - `stop3` (מסלול, leg-pills) + הרעיון של "קורה עכשיו"/שעון-יעד (היה `t6Name`/`t6Desc`, `m4Desc`) לפסקה אחת. leg-pills הדגמה (רומא←פירנצה←ונציה) נשמרו כמו שהם.

**הושמט לגמרי** (לא נדחס לתוך הפיצ'רים, כמו שהתבקש): onboarding/הצטרפות עם קישור, תגיות אופי-טיול (משפחה/חברים/זוגי/סקי/גאווה/חתונה/פסטיבל), swatches סוג-טיול, רשימת חברים+וואטסאפ, 5 קוביות המודולים (ציוד/קניות/זיכרונות/תוכנית/הוצאות כרשימה נפרדת), מפת מושבים, הצעות AI, קיבוץ אוטומטי ברשימת ציוד, מקומות מתחברים לתוכנית, מצב כהה. CSS/JS מתה שהשתייכה אך ורק לאלמנטים שהוסרו (`.modules`/`.module`/`.touch`/`.tags`/`.tag`/`.swatches`/`.swatch` וה-stagger reveal שלהם) הוסרה גם היא, לא נשארה כקוד לא-נגיש.

**CTA חוזר**: `#ctaCard` (הכרטיס הקיים בתחתית, כולל שדה הטעימה שנבנה ב-v4.84.0, ללא שינוי) קיבל id; שני כפתורי `.cta-btn-repeat` חדשים (אחרי הכותרת ואחרי פיצ'ר 1) הם `<a href="#ctaCard">` - גלילה לאותו כרטיס יחיד, לא יעדים/כפתורים נפרדים עם לוגיקה משלהם.

**בלי testimonials** - כמאושר מראש, לא נוסף.

### 🖼️ החלטה לגבי תמונות - הפשרה, לא הישג
**לא נוצר אף נכס ויזואלי אמיתי** (לא screenshot, לא GIF) - שני הפיצ'רים מציגים placeholder מנוסח בבירור ("צילום מסך אמיתי... בקרוב"), לא מוקאפ מחופש לצילום. מה שנוסה בפועל: `achievements.html` עם test hook זמני (משתמש+ביקורים מדומים) כדי לרנדר את הגלובוס עם דאטה אמיתי - **זה עבד**, הגלובוס נראה מצוין (choropleth+טקסטורה+סטטיסטיקה, נבדק ויזואלית). הכשל היה בשלב הבא: אין ל-Browser tool דרך לשמור screenshot כקובץ; ניסיון לעקוף דרך `canvas.toDataURL()` נכשל (buffer ריק - WebGL context בלי `preserveDrawingBuffer`, 6KB בלבד לפריים 1024×624). המשך הניסיון הופסק ביוזמת המשתמש (עלות טוקנים, לא שווה לתמונת שיווק אחת). **`achievements.html` נבדק שאין בו שינוי net** (`git status`/`diff` נקיים) - ה-hook הוסר ואומת שהוסר.

### 🔍 מה שנבדק בפועל
- `node --check` נקי על שני בלוקי ה-script.
- `node scripts/i18n-audit.js gittrip-showcase.html` - אותו ממצא יחיד קיים-מראש (`#langHeBtn`), 0 ממצאים חדשים.
- `grep` אחר כל ה-ID/class-ים שהוסרו (`stop1`-`stop5`, `.tags`, `.modules`, `.swatches` וכו') - 0 מופעים נותרים בקוד (רק אזכור היסטורי בהערה).
- נבדק בדפדפן חי (שרת מקומי אמיתי, לא רק `file://`): שני הפיצ'רים מוצגים נכון (עברית+אנגלית), placeholder-ים מוצגים, leg-pills+חץ מתהפכים נכון בין RTL/LTR.
- **כפתור CTA-חוזר** (`ctaBtnMid1`) נבדק בפועל - גלילה אמיתית ל-`#ctaCard` (scrollY 0→1382, `ctaCard` נכנס ל-viewport). נכשל תחילה תחת `file://` (מצב "static snapshot" של סביבת הבדיקה, לא קשור לקוד) - אומת מחדש דרך שרת http אמיתי ועבד.
- **שדה הטעימה** (v4.84.0) נבדק מחדש אחרי הצמצום - הקלדה אמיתית ("Vienna") הניבה geocoding+מזג אוויר אמיתיים וכפתור-המשך מוצג - לא נשבר מהמבנה החדש.
- אין שינוי ב-`achievements.html` (נבדק לפני commit).

**שינוי תוכן/מבנה משמעותי (לא תיקון באג) → minor bump.**

## v4.85.0 — ניתוב אוטומטי של מבקר קר מ-index.html ל-gittrip-showcase.html (המשך v4.84.0)

### ✨ תכונה חדשה - החלק החסר מהתוכנית המקורית
עד עכשיו, `#authGate` הוצג לכל משתמש לא-מחובר בדיוק אותו דבר - בלי הבחנה בין מוזמן אמיתי (הגיע עם `tripId` דרך קישור וואטסאפ), מבקר קר (בלי שום הקשר), או **משתמש חוזר שסתם התנתק/פג לו ה-session**. עכשיו מבקר לא-מחובר **בלי `tripId` וגם בלי היסטוריית התחברות בדפדפן הזה** מנותב אוטומטית (`location.href`, לא רק לינק) ל-`gittrip-showcase.html` במקום לראות את הקיר הגנרי - סוגר את הפער בין פיצ'ר הטעימה (v4.84.0, כבר בנוי ב-showcase) לבין הדרך שמבקר קר בכלל מגיע לשם.

**דגל `localStorage['gittrip_hasSignedInBefore']`** - נקבע ל-`"true"` בפעם הראשונה שמשתמש מתחבר בהצלחה, בשתי נקודות הכניסה הקיימות היחידות שבהן התחברות בכלל קורית: `handleAuthStateChanged()` ב-`index.html` **וגם** ב-`wizard.html` (לא נקודת כניסה חדשה - הרחבה של הפונקציה הקיימת בשני המקומות). זה מה שמבדיל משתמש חוזר (session פג, אבל כבר התחבר פעם בעבר בדפדפן הזה) ממבקר חדש-לגמרי - שניהם נראים זהים כ-`!user` בלי הדגל.

**ההיגיון הסופי** ב-`handleAuthStateChanged()` של `index.html`:
- בלי `tripId` + בלי הדגל (מבקר חדש-לגמרי) → ניתוב אוטומטי ל-showcase.
- בלי `tripId` + עם הדגל (משתמש חוזר, כרגע לא מחובר) → `authGate` הרגיל (login wall), **לא** showcase.
- עם `tripId` → `authGate` הרגיל תמיד, ללא קשר לדגל - מוזמן אמיתי לא מושפע כלל, כולל הקופי המותאם-אישית ("X הזמין אתכם לטיול ל-Y") דרך `authGateCopy()` הקיים.

**אין סיכון ללולאת ניתוב**: `gittrip-showcase.html` לא בודק `tripId`/דגל כדי לנתב בחזרה ל-`index.html` באופן אוטומטי - ה-CTA/"חזרה לטיול" שם הם קישורים רגילים שהמבקר צריך ללחוץ עליהם, לא ניתוב יזום.

### 🔍 מה שנבדק בפועל
כל 3 התרחישים המבוקשים + מקרי קצה נוספים, בדפדפן חי (לא רק קריאת קוד):
- **מבקר חדש-לגמרי** (בלי `tripId`, `localStorage` ריק) - ניתוב אמיתי (`location.href` השתנה בפועל) ל-`gittrip-showcase.html`, עם ה-listener האמיתי (`auth.onAuthStateChanged`), לא סימולציה. הדגל נשאר `null` אחרי הניתוב - לא נקבע רק מביקור בעמוד.
- **משתמש חוזר, session פג/התנתק, בלי `tripId`** (הדגל כן מוגדר `"true"` מראש) - נשאר על `index.html`, `authGate` הרגיל מוצג ("כניסה לטיול") - **לא** מנותב. זה בדיוק התרחיש שהועלה.
- **`tripId` נוכח** - `authGate` הרגיל מוצג תמיד, גם עם הדגל מוגדר וגם בלעדיו (שני המצבים נבדקו בנפרד) - לא מנותב לעולם.
- **התחברות אמיתית מגדירה את הדגל** - `handleAuthStateChanged({uid:...})` מדומה קבע את הדגל ל-`"true"` והציג את `appContainer` הרגיל; קריאה עם `null` לא נוגעת בדגל בכלל (לא נדרס בטעות בכל בדיקת auth-state).
- שני המקרים "מחובר"/"משתמש חוזר" נבדקו דרך hook זמני (השבתת שורת ה-`auth.onAuthStateChanged` האמיתית, כדי לשלוט ב-timing בלי מרוץ מול הרזולוציה המהירה של Firebase ל-`null` בסביבה בלי חיבור אמיתי) - **הוסר ואומת שהוסר** (`grep` על `__TEST_DISABLED__` - 0 מופעים) לפני שהשורה האמיתית שוחזרה ונבדקה שוב בעצמה מקצה-לקצה.
- `node --check` נקי על כל בלוקי ה-script בשני הקבצים (`index.html`, `wizard.html`).

**שינוי זרימה/התנהגות חדש שהמשתמש חווה (המשך ישיר לתוכנית הטעימה, לא תיקון באג נקודתי) → minor bump**, עקבי עם הסיווג של v4.52.0 (אותו סוג שינוי - "מתי בדיוק מוצג gate").

## v4.84.0 — טעימה (teaser) למבקר קר ב-gittrip-showcase.html - יעד אחד, תצוגה מקדימה אמיתית

### ✨ תכונה חדשה
מבקר קר (בלי tripId, בלי הזמנה) שהגיע ל-`gittrip-showcase.html` יכול עכשיו להקליד יעד אחד בתוך כרטיס ה-CTA ולקבל תצוגה מקדימה חיה - **דאטה אמיתי, לא מוקאפ** - לפני שהוא בכלל נכנס לוויזארד. הכפתור הרגיל ("✈️ לפתיחת GitTrip") נשאר בדיוק כמו שהיה, תמיד עובד גם בלי להשתמש בשדה החדש כלל - זו תוספת אופציונלית מעליו, לא תחליף.

**מימוש**:
- `legGeocode.js` נטען עכשיו גם ב-`gittrip-showcase.html` (script קלאסי, כבר page-agnostic לגמרי - `window.geocodeLegDestination(name, language)` לא תלוי ב-tripId/DOM ספציפי, בדיוק כמו ב-wizard.html/index.html).
- **לוגיקת השדה** מועתקת מ-`wireLegPreview()` הקיים ב-wizard.html: debounce 600ms → `geocodeLegDestination()` → guard נגד תגובה מיושנת (`input.value.trim() === value` אחרי כל `await`) → `countryCodeToFlag()` (אותו unicode trick, מועתק).
- **מזג אוויר נוכחי** - fetch נפרד ל-Open-Meteo forecast עם ה-`lat`/`lon` שהתקבלו, עם טבלת `weather_code`→אייקון/טקסט מועתקת מ-`fetchWeather()` ב-index.html (7 הענפים המלאים: בהיר/מעונן חלקית/ערפל/גשום/שלג/ממטרים/סופת רעמים).
- **בלי המלצת מקום לדוגמה** - הושמט במכוון מ-v1, כמו שהוחלט בחקירה הקודמת (אין מקור דאטה קיים לזה).
- **כפתור "מעולה! בואו נמשיך לתכנן ←"** מופיע רק אחרי geocoding תקין (lat/lon אמיתיים, לא רק "משהו הוקלד"). בלחיצה: `localStorage['wizardDraft'] = {destination: <הטקסט הגולמי שהוקלד, לא השם הנקי מה-geocoding>}` (עקבי עם איך ש-wizard.html עצמו תמיד שומר `destination`/`legs[0]`) ואז ניווט (לא טאב חדש) ל-`wizard.html` - **אפס שינוי ב-`restoreWizardDraft()` הקיים שם**, הוא כבר קורא בדיוק את המפתח/הצורה הזו בטעינת עמוד.
- **מצבי קצה**: "מחפשים..." בזמן ה-await (לא ריק מוזר), הודעה עדינה לא-מפחידה כש-geocoding לא מזהה כלום (צבעים ניטרליים - `var(--muted)`/`var(--line)` הקיימים, לא אדום/אזהרה שלא קיים בשום מקום אחר בדף), ואיפוס מלא (טקסט+מצב) בכל הקלדה חדשה, לא רק כשה-debounce יורה.
- **i18n**: תוויות סטטיות (`tasteLabel`/`tasteLoadingText`/`tasteNotFoundText`/`tasteContinueBtn`) נרשמו כמו כל שאר האלמנטים ב-`SHOWCASE_TEXT` (אותו מנגנון `applyLanguage()` קיים). ה-placeholder (לא textContent, אז `applyLanguage()`'s הלולאה הגנרית לא מגיעה אליו) מטופל בפונקציה ייעודית חדשה (`updateTasteWidgetLanguage()`) שנקראת מתוך `applyLanguage()` עצמה - קריאה אחת, אף פעם לא שוכחת סנכרון. **החלטת עיצוב מכוונת**: מעבר שפה מאפס את השדה (טקסט+תוצאה) במקום לנסות לתרגם תוצאה שכבר הוצגה - v1 טעימה קלה, לא טופס מרובה-שלבים כמו wizard.html.
- **עיצוב** - טוקנים קיימים בלבד (`--surface-2`/`--line`/`--muted`/`--c-abroad`), אותה שפת עיצוב בדיוק כמו `.cta-card`/`.cta-btn` הקיימים - לא יובא סגנון מדף אחר.

### 🔍 מה שנבדק בפועל
- **יעד תקין באנגלית** ("Lisbon") - flag 🇵🇹, עיר "Lisbon, Lisbon District", מזג אוויר אמיתי ("🌙 23°C · Clear night") - נקרא ישירות מה-DOM אחרי הקלדה אמיתית (`computer` tool, לא `dispatchEvent`).
- **יעד תקין בעברית, דרך ה-Nominatim fallback** ("טוקיו") - Open-Meteo מחזיר ריק לעברית (בדיוק הבאג ש-`legGeocode.js` נבנה לתקן), נופל ל-Nominatim עם ה-throttle הקיים - flag 🇯🇵, עיר "טוקיו, טוקיו", מזג אוויר בעברית ("⛅ 30°C · מעונן חלקית").
- **round-trip מלא ל-wizard.html** - לחיצה על הכפתור אחרי geocoding מוצלח, ניווט אמיתי (לא סימולציה), ו-`document.getElementById('tripDestination').value` על wizard.html מציג בדיוק את מה שהוקלד ("Lisbon") - `restoreWizardDraft()` הקיים עבד בלי שום שינוי.
- **יעד לא מזוהה** (מחרוזת אקראית) - הודעה עדינה מוצגת, כפתור ההמשך נשאר מוסתר.
- **איפוס במעבר שפה** - הקלדת "רומא" (עברית, נמצא), החלפה ל-English - השדה מתאפס לגמרי (ערך ריק, placeholder אנגלי, תוצאה מוסתרת) בדיוק כמתוכנן.
- **רגרסיה**: הכפתור הרגיל (`ctaBtn`) נבדק אחרי כל התרחישים למעלה - `href="wizard.html"`, בלי `target` - בלתי מושפע.
- `node scripts/i18n-audit.js gittrip-showcase.html` - 0 ממצאים חדשים (הממצא היחיד שחזר, `#langHeBtn`, קיים מלפני ולא קשור).
- `node --check` נקי על שני בלוקי ה-script.
- קונסול נקי לאורך כל הבדיקות.

**תכונה חדשה, לא תיקון באג → minor bump.**

## v4.83.7 — מיגרציית domain: og:image/og:url/twitter:image + קישור CTA (index.html, gittrip-showcase.html)

### 🐛 הבאג
סריקה שיטתית של כל הריפו (לא רק gittrip-showcase.html) אחר אזכורי הדומיין הישן `nelevy1-sketch.github.io` (מלפני המעבר ל-`gittrip.app`) העלתה 7 מופעים פונקציונליים בשני קבצים - כל שאר מופעי ה-domain בריפו נבדקו ונפסלו כלא-רלוונטיים (Firebase RTDB/Storage URLs, User-Agent header של `scripts/backfill-place-coordinates.js` שמצביע נכון על `github.com/.../mykonos-list` - כתובת ה-**ריפו**, לא hosting - ורשומות היסטוריות ב-CHANGELOG שנשארות כמו שהן בכוונה).

**תוקן**:
- **`index.html`**: `og:image`/`twitter:image` → `https://gittrip.app/og-preview.png` (גם `/mykonos-list/` נעלם מהנתיב - היה חלק מ-GitHub-Pages-project-path, לא רלוונטי תחת custom domain).
- **`gittrip-showcase.html`**: אותו תיקון ל-`og:image`/`twitter:image`, ובנוסף `og:url` → `https://gittrip.app/gittrip-showcase.html`.
- **`gittrip-showcase.html` - קישור ה-CTA** (`#ctaBtn`, המקרה הדיפולטיבי בלי `tripId`): הוחלף מ-URL מוחלט (`https://nelevy1-sketch.github.io/mykonos-list/wizard.html`, `target="_blank"`) לנתיב יחסי (`href="wizard.html"`, אותו טאב) - `target="_blank"` הוסר **רק** במקרה הזה; ה-branches של `tripId`/`from=menu` ב-JS (`initCtaFromUrl()`) נשארו בלי שינוי - הם ממילא רק `removeAttribute("target"/"rel")`, אף פעם לא set, אז ההסרה מה-HTML הסטטי היא no-op מבחינתם, לא רגרסיה.

### 🔍 מה שנבדק בפועל
- **`og-preview.png` על `gittrip.app` (לא רק github.io)** - נבדק ישירות לפני שנסגר, לא הונח: `curl -I https://gittrip.app/og-preview.png` → `200 OK`, `image/png`, 28539 bytes. סוגר גם את הפריט הפתוח המקביל ב-`docs/roadmap.md` §7 ("לבדוק ש-og-preview.png באמת קיים").
- `grep` על שני הקבצים אחרי השינוי - 0 מופעים נותרים של `nelevy1-sketch`.
- ערכי ה-DOM בפועל בדפדפן חי: `ctaBtn.href` = `"wizard.html"`, בלי `target`/`rel`; שלושת ה-meta tags מציגים `gittrip.app` - נקרא ישירות מה-DOM, לא רק מהקוד המקור.
- `node --check` נקי על כל 5 בלוקי ה-script המשולבים בין שני הקבצים.
- הענף `tripId`/`from=menu` נבדק מקריאת קוד (לא live navigation - סביבת הבדיקה המקומית עם `file://`+query-params מפיקה "static snapshot" ולא מריצה JS אמיתי) - `removeAttribute` על attribute שכבר לא קיים הוא no-op מתועד, לא רגרסיה.

**מיגרציית תוכן/קונפיג אמיתית (לא רק תיעוד) → patch bump.**

## v4.83.6 — תיקון: טקסט מיושן "מקומות" במודול "תמונות ומוזיקה" (wizard.html, המשך v4.83.5)

### 🐛 הבאג
המשתמש ציין ש"מקומות, תמונות ומוזיקה" אינו נכון יותר - הפיצ'ר "מקומות" (נקודות עניין, ניווט) עבר מזמן ל-`itinerary.html` (ר' v4.70.0), ו-`places.html` עצמו מציג רק "תמונות"/"מוזיקה". טקסט זה נשאר ב-3 מקומות ב-`wizard.html` שלא נסרקו קודם (תווית הצ'קבוקס, מודאל "צפה בדוגמה", ושורת סיכום המודולים בסוף האשף).

**תוקן**:
- תווית הצ'קבוקס + `placesLabel` (2 שפות): "מקומות, תמונות ומוזיקה" → "תמונות ומוזיקה" / "Places, Photos & Music" → "Photos & Music".
- כותרת מודאל התצוגה המקדימה: אותו שינוי.
- **תיאור** מודאל התצוגה המקדימה (עדיין הזכיר "המקומות ששמרתם, קישורי ניווט" - פונקציונליות שלא קיימת יותר במודול): "מרכז את המקומות ששמרתם, קישורי ניווט, תמונות מהטיול ופסקול משותף במקום אחד" → "מרכז את תמונות הטיול ופסקול משותף במקום אחד" (ותרגום מקביל באנגלית).
- שורת סיכום המודולים בצ'ק-ליסט הסופי (`ציוד, קניות, ?, הוצאות`): "מקומות"/"Places" → **"זיכרונות"/"Memories"** (לא "תמונות ומוזיקה") - נבחר במפורש כדי לתאום לתבנית הקיימת כבר ב-`index.html` (כותרת קצרה="זיכרונות", תיאור ארוך יותר="תמונות ומוזיקה"), ואושר עם המשתמש לפני היישום.

### 🔍 מה שנבדק בפועל
- `grep` על `wizard.html` אחרי השינוי - 0 מופעים נותרים של "מקומות" הקשורים למודול הזה (שני מופעי "מקומות" שנותרו בקובץ שייכים ל-vibe-hint על הצעות ציוד לפי יעד - לא קשורים).
- ערכי התווית/התיאור נבדקו ישירות מה-DOM בדפדפן חי (`openPreview('places')` + קריאת הטקסט בפועל מהמודאל), אחרי רענון קשיח (cache-busting query) כדי לשלול טעינה ישנה מהשרת המקומי.
- `node --check` נקי על כל 4 בלוקי ה-script של `wizard.html`.

**תיקון תוכן/טקסט אמיתי (leftover משינוי שם קודם, המשך ישיר ל-v4.83.4/v4.83.5) → patch bump.**

## v4.83.5 — תיקון: אייקון סיכת מיקום ישן גם ב-wizard.html (המשך v4.83.4)

### 🐛 הבאג
המשתמש דיווח שהאייקון הישן עדיין מופיע - בבדיקה התברר שזה לא באג חוזר באותם 5 עמודים (v4.83.4 שם תוקן ואומת נכון), אלא מקום שישי, נפרד, שלא נסרק אז: שלב בחירת המודולים ב-`wizard.html` (יצירת טיול חדש), שם מוצג `📍` ליד "מקומות, תמונות ומוזיקה" - גם בתווית עצמה וגם במודאל "צפה בדוגמה" שלה (2 שפות × 2 מקומות = 4 מחרוזות, פלוס עותק נוסף של התווית עצמה = 5 מופעים בסה"כ). `wizard.html` לא נבדק ב-v4.83.4 כי אין לו bottom-nav כלל (אין עדיין טיול בשלב הזה) - אבל יש לו את אותו טקסט/אייקון במקום אחר לגמרי (טופס בחירת פיצ'רים), שהוחמץ באותה סריקה.

**תוקן**: `📍` → `📸` בכל 5 המופעים ב-`wizard.html`. **לא שונה**: הטקסט "מקומות, תמונות ומוזיקה" / "Places, Photos & Music" עצמו - כאן זה נשאר השם התיאורי המלא (לא "זיכרונות"), ולא התבקש שינוי טקסט, רק אייקון.

### 🔍 מה שנבדק בפועל
- `grep` על `wizard.html` אחרי השינוי - 0 מופעים נותרים של 📍.
- `grep` על שאר הריפו - שאר מופעי ה-📍 שנשארו כולם לא קשורים למודול הזה (`gittrip-showcase.html` - פיצ'ר "האפליקציה יודעת איפה אתם", `itinerary.html`/`mapView.js` - אייקון ברירת מחדל למיקום/פעילות ולנקודת מפגש במפה).
- ערך התווית נבדק ישירות מה-DOM בדפדפן חי (`document.getElementById('lblFeatPlaces').textContent`) - מציג 📸.
- `node --check` נקי על כל 4 בלוקי ה-script של `wizard.html`.

**תיקון באג ויזואלי אמיתי, המשך ישיר ל-v4.83.4 → patch bump.**

## v4.83.4 — תיקון: אייקון סיכת מיקום ישן בשורת הניווט התחתונה (5 עמודים)

### 🐛 הבאג
שורת הניווט התחתונה (bottom nav) של "זיכרונות" 📸 הציגה עדיין את ה-SVG הישן של סיכת מיקום (leftover מ-"places", מלפני שינוי השם ל"זיכרונות" - ר' v4.70.0/v4.70.4 ב-changelog הזה). זה לא אימוג'י (📍) אלא path SVG גולמי (`M12 2C8.13 2 5 5.13 5 9...`), ולכן לא נתפס בסריקת ה-📍/round_pushpin שכן בוצעה ב-v4.70.4 - אותה מלכודת בדיוק שכבר תועדה שם ("2 סבבים כדי לתפוס את כל המקומות"), הפעם על צורת ייצוג שונה של אותו אייקון ישן.

נמצא ב-5 עמודים - כל מקום שיש בו bottom-nav עם קישור ל-`places.html` (לא ב-wizard.html, שאין לו bottom-nav כלל - אין עדיין טיול בשלב הזה): `index.html`, `places.html` (ה-nav-item הפעיל של עצמו), `shopping.html`, `packing.html`, `itinerary.html`.

**תוקן**: הוחלף ב-5 העמודים לאייקון מצלמה (Material Icons "photo_camera", path יחיד עם 3 צורות מקוננות - גוף+טבעת עדשה+עדשה פנימית - זהה מבנית ל-5 אייקוני ה-nav האחרים באותה שורה, שכולם path יחיד ב-viewBox 24x24). זהו תיקון סטטי מיידי בלבד - אינו קשור לתכנון עתידי של אנימציית Lottie לאייקונים (עדיין לא התחיל, ר' roadmap).

### 🔍 מה שנבדק בפועל
- `grep` על כל 6 העמודים אחרי השינוי - 0 מופעים נותרים של ה-path הישן, path החדש מופיע פעם אחת בדיוק בכל אחד מ-5 העמודים.
- `getBBox()` על ה-`<path>` החדש בדפדפן אמיתי - מחזיר bounding box תקין (20×18 בתוך viewBox 24×24), מוודא שה-path בפועל מצויר ולא ריק/שבור.
- הושוותה סגנונית לחמשת אייקוני ה-nav האחרים (בית/ציוד/קניות/תוכנית/הוצאות) - כולם path בודד ממולא (fill), לא stroke - עקבי, לא ה-Tabler fallback-icon הנפרד שכבר קיים בכרטיס המודול בדף הבית (ספריית אייקונים שונה, קטע קוד אחר לגמרי, לא נגעתי בו).

**תיקון באג ויזואלי אמיתי (leftover משינוי שם קודם, לא שיפור עיצוב חדש) → patch bump.**

## v4.83.3 — תיקון: קומבו בחירת מדינה בטופס הידני (achievements.html) בוחר מדינה שגויה/כלום

### 🐛 שני באגים נפרדים, שניהם ב-achievements.html

**באג 1 - סדר תוצאות שגוי**: חיפוש המדינה בטופס "הוספת ביקור ידני" הוא substring רגיל (`.includes`) על שם מלא, והתוצאות ממוינות אלפביתית בלבד - לא לפי איכות ההתאמה. הקלדת שם מדינה מלא ומדויק לא הבטיחה שהמדינה הזו תופיע ראשונה: "ארצות הברית" (US) הוא substring של "איי הבתולה של ארצות הברית" (איי הבתולה של ארה"ב, VI) - וה-VI מתמיין אלפביתית לפני ה-US. אומת בפועל, לא רק חשד: נייט הוסיף ביקורים ידניים והם "נעלמו" מהדשבורד - זו הייתה הסיבה (הכתיבה הצליחה, פשוט תחת קוד מדינה אחר משהתכוון אליו, בלי שום שגיאה). סריקה שיטתית של כל 248 המדינות (כל מדינה מול השם המלא של עצמה) חשפה עוד 3 מקרים זהים: צרפת/גיאנה הצרפתית, הולנד/בונair, סודן/דרום סודן.

**תוקן**: מיון יציב (stable partition) - התאמה מדויקת (case-insensitive, עברית או אנגלית) קופצת לראש הרשימה; שאר התוצאות (התאמות חלקיות בלבד) שומרות על הסדר האלפביתי הקיים ביניהן, ללא שינוי.

**באג 2 - Enter לא בחר כלום, בשום מקרה, אף פעם**: תוך כדי אימות התיקון הראשון בפועל (טעינה + Enter אמיתי על 4 המקרים), התגלה שה-handler הקיים ל-Enter קורא `firstMatch.dispatchEvent(new MouseEvent("mousedown"))` בלי `{bubbles:true}` - ברירת המחדל של `MouseEvent` היא `bubbles:false`. הלוגיקה שבוחרת מדינה בפועל יושבת ב-listener על מיכל הרשימה (`visitCountryList`), לא על כל אופציה בנפרד - אירוע שלא מבעבע לעולם לא מגיע לשם. זה לא קשור לבאג הראשון ולא נגרם ממנו - זו תקלה עצמאית שהייתה שם מלכתחילה (אומת ישירות: probe בשלב ה-capture תפס את האירוע, אבל ה-listener האמיתי, שיושב בשלב ה-bubble, לא). **תוקן**: הוספת `{bubbles:true}` לקריאה.

### 🔍 מה שנבדק בפועל
- 4 המקרים שדווחו (ארצות הברית/צרפת/הולנד/סודן) - הקלדת השם המלא + Enter אמיתי (`KeyboardEvent`) בוחרים את המדינה הנכונה בכל ה-4, אחרי שני התיקונים יחד.
- Regression מלא: כל 248 המדינות בתור - הקלדת השם המלא + Enter על כל אחת בוחרת את הקוד הנכון שלה (0 כשלים).
- חיפושים חלקיים (למשל "לנד", "אר", מחרוזת ריקה) - הסדר וההתנהגות זהים לגמרי למצב לפני התיקון (אין התאמה מדויקת בהם → אין שינוי בסדר).
- `node --check` נקי. hook-בדיקה זמני הוסר ואומת שהוסר (0 מופעים).

**תיקון באג אמיתי בהתנהגות (לא שיפור עיצוב) → patch bump.**

## v4.83.2 — תיקון: setAuditStatus() נשמט מהתיקון הקודם (shopping.html)

### 🐛 הבאג
`setAuditStatus()` הופיעה ברשימת ה-🔴 המקורית (fire-and-forget, בלי טיפול שגיאות בכלל) אבל נשמטה בטעות מבקשת התיקון הקודמת (v4.83.1) - שני הענפים שלה (עדכון סטטוס פשוט, ועדכון 'partial' עם prompt לכמות שהתקבלה) כתבו `update()` בלי `safeWrite`, בלי `logWriteEvent`, ובענף הפשוט - בלי שום toast בכלל, גם בהצלחה וגם בכישלון.

**נבדק במפורש**: בניגוד ל-`setStatusAndSave` (v4.83.1), אין כאן state מקומי שמתעדכן מראש לפני אישור הכתיבה - אין צורך ב-rollback, רק בעטיפה + לוגינג + toast כישלון אמיתי.

**תוקן**: שני הענפים עטופים עכשיו ב-`safeWrite`/`logWriteEvent` (context אחד משותף - `setAuditStatus`, שתי הפעולות הן אותה פעולה לוגית עם שדות שונים, לא שני מנגנוני כתיבה שונים). ענף ה-`partial` שכבר היה לו toast הצלחה - עכשיו הוא מוצג רק אחרי אישור אמיתי, עם toast כישלון חדש. הענף הפשוט קיבל toast כישלון חדש; **לא הוספנו** toast הצלחה חדש שם - הוא לא היה קיים לפני התיקון, ולא נדרש לתקן את הבאג עצמו.

### 🔍 מה שנבדק בפועל
- ענף פשוט (`status='missing'`): כישלון (ללא auth אמיתי) - toast "העדכון נכשל. נסו שוב." מוצג; הצלחה מדומה (`safeWrite` מוחלף זמנית) - שום toast לא מוצג (כצפוי, תואם התנהגות מקורית).
- ענף `partial` (עם `prompt()` מדומה מחזיר "4" מתוך 10 שהוזמנו): כישלון - toast כישלון; הצלחה מדומה - "עודכן: התקבלו 4, חסרים 6" (חישוב נכון: 10-4=6).
- `dashboard.html`: 3 אירועים מפוברקים עם context `setAuditStatus` (2 הצלחה, 1 כישלון) - 2/3 67% תואם בדיוק.
- `node --check` נקי. `scripts/i18n-audit.js` - 0 ממצאים חדשים. hook-הבדיקה הזמני הוסר ואומת שהוסר.

**תיקון באג (silent-failure אמיתי, פער שנשמט) → patch bump.**

## v4.83.1 — תיקון: 10+ נקודות כתיבה fire-and-forget בלי טיפול שגיאות (packing.html, shopping.html)

### 🐛 הבאגים הדחופים
מהחקירה הקודמת (מציאת פערי כיסוי בדשבורד האנליטיקס) - כל הפונקציות הבאות כתבו ל-RTDB בלי `safeWrite`, בלי `try/catch`, בלי שום פידבק למשתמש בכישלון:

**packing.html**: `ensureStarterLists()` (**הכי דחוף** - רץ בלי משתמש בתוך ה-`onValue` בטעינה הראשונה של כל משתמש חדש; כישלון היה משאיר אותו לצמיתות בלי רשימות התחלתיות, בלי שום הודעה, ובלי ניסיון חוזר כי הדגל שסימן "כבר טופל" היה נדלק גם כשהכתיבה נכשלה), `renameList()`, מתג "בוצע" לתת-פריט (`data-sub-check`), `addSubItem()`, `addToSuggestedCategory()`, `importItem()`.

**shopping.html**: `setStatusAndSave()` (**תיקון לוגי אמיתי, לא רק logging** - ה-toast של הצלחה הופיע קודם ללא await על הכתיבה בכלל, ולכן תמיד "הצליח" גם כשהכתיבה נכשלה; עכשיו ה-toast מגיע רק אחרי await אמיתי, וה-state המקומי [`listStatus`/`isAuditMode`] מתאפס לערך הקודם בכישלון), `mergeOrCreateItem()` (+ 3 הקוראים לה: `transferSingleItemToInPerson`, `executeBulkTransfer`, `transferMissingToInPersonBtn`), `toggleClaimItem()`, `togglePriority()`, `editItem()`, `toggleItem()`, `savePersonalQtyBtn`/`removePersonalQtyBtn`.

**תוקן**: כל אחת עטופה עכשיו ב-`safeWrite`/`logWriteEvent` (אותה תבנית כמו 35 הנקודות הקיימות), עם toast אמיתי בכישלון. שינוי נוסף מעבר לתוספת פידבק: מתג תת-הפריט מחזיר את מצב הצ'קבוקס למצבו הקודם בכישלון (כדי שהתצוגה לא תמשיך "לשקר" על מה שנשמר בפועל).

**במפורש לא נגענו**: `setAuditStatus()` - הופיעה ברשימת ה-🔴 המקורית אבל לא נכללה בבקשת התיקון הזו. פער פתוח, ממתין להחלטה נפרדת.

### 🔍 מה שנבדק בפועל
- `ensureStarterLists`: קריאה ישירה - כישלון (ללא auth אמיתי) מחזיר `false` נכון, הצלחה מדומה (`safeWrite` מוחלף זמנית) מחזירה `true` ומעדכנת `activeList`, ומצב "כבר יש listMeta" מחזיר `true` בלי לנסות כתיבה כלל. גם ה-caller (בתוך ה-`onValue`) נבדק בקוד - לא מסמן `starterCheckDone` בכישלון.
- `renameList`: כישלון - המודל **נשאר פתוח** (לא נסגר בטרם עת), toast נכון; הצלחה מדומה - המודל נסגר, toast "הרשימה עודכנה".
- `setStatusAndSave`: כישלון - `listStatus` נשאר בערכו הקודם (לא "משקר"), toast כישלון; הצלחה מדומה - `listStatus` באמת מתעדכן, toast הצלחה עם הסטטוס הנכון.
- `mergeOrCreateItem`: שני הענפים (יצירה חדשה + מיזוג לפריט קיים) נבדקו ישירות - שניהם מחזירים `{merged,key,ok}` נכון, עם context labels נפרדים (`mergeOrCreateItem-create`/`mergeOrCreateItem-merge`).
- `toggleClaimItem`: נבדק ישירות - context ונתיב נכונים.
- `dashboard.html`: דאטה מפוברקת עם 5 מה-context-ים החדשים (`ensureStarterLists`, `setStatusAndSave`, `mergeOrCreateItem-create`, `toggleClaimItem`) - הפילוח לפי context ולפי file תואם בדיוק חישוב ידני.
- `node --check` נקי בכל 3 הקבצים. `scripts/i18n-audit.js` - 0 ממצאים חדשים. כל hook-בדיקה זמני הוסר ואומת שהוסר (0 בכל 3 הקבצים).

**תיקון באג (silent-failure אמיתי בשני עמודים) → patch bump.**

## v4.83.0 — שכבת aggregate לסטטיסטיקת ביקורים חוצת-משתמשים (_analytics/visitorStats)

### 🎯 מה השתנה
`database.rules.json`: `_analytics/visitorStats/$uid` חדש - `.write` מוגבל ל-`auth.uid === $uid` בלבד (כל משתמש כותב רק לרשומה של עצמו, בדיוק כמו `users/$uid` עצמו), קריא במלואו רק ל-UID הדשבורד (דרך `.read` הקיים כבר ברמת `_analytics`). **לא פורסם עדיין** - הפרסום בקונסולה בידי המשתמש, כרגיל.

`achievements.html`: פונקציה חדשה `mirrorVisitorStats(uid, visits)` - נקראת מתוך `startVisitsListener`'s `onValue` הקיים בכל פעם ש-`users/{uid}/visits` משתנה (כל מקור - טופס ידני היום, `app` עתידית), תוספתית לחלוטין, לא נוגעת ברינדור הקיים. משתמשת ב-`computeCountryVisitInfo` הקיימת (בלי לשכתב לוגיקה) לחשב את הפלט, ושומרת אותו יחד עם רשימת ביקורים מצומצמת (`source`/`countryCode`/`cityName` בלבד - בלי `ownerName`/חותמות זמן/`tripId`/`legId`, שאף סטטיסטיקה מצטברת לא צריכה) ל-`_analytics/visitorStats/{uid}`.

`dashboard.html`: קורא `_analytics/visitorStats` (קריאה אחת), מאחד את רשימות הביקורים המצומצמות של כל המשתמשים למערך שטוח אחד, ומזין אותו ל-`computeCountryVisitInfo`/`computeStats` - **מועתקות מ-achievements.html ללא שינוי** (אין קובץ משותף בין שני העמודים לפונקציות האלה, בדיוק כמו ש-`scripts/backfill-visits.js` כבר משכפל לוגיקת `legs.js` ל-Node - אותו נימוק). מציג: מדינות שביקרו בהן מתוך העולם, פילוח יבשות (ממוין מהגבוה ביותר), ובירות שביקרו בהן.

`scripts/backfill-visitor-stats.js` (חדש, בהשראת `scripts/backfill-visits.js`): סקריפט חד-פעמי, **Admin SDK** (לא טוקן רגיל כמו הסקריפט הקיים) - כי המשימה כאן היא בדיוק מה ש-`backfill-visits.js` כבר תיעד כדורש הרשאת Admin: קריאת `/users.json` כעץ שלם. Admin SDK עוקף את `database.rules.json` לגמרי, אז אין צורך גם בזיהוי UIDs דרך `memberProfiles` - קורא `/users.json` ישירות. אידמפוטנטי מבנית (`set()` דורס בכל הרצה, לא `push()`) - פשוט יותר מהאידמפוטנטיות של `backfill-visits.js`. **לא הורץ** - מוכן להרצה בידי המשתמש בלבד (`node scripts/backfill-visitor-stats.js --apply`), דורש `GOOGLE_APPLICATION_CREDENTIALS` (Admin SDK הרגיל, לא env var ייחודי ל-GitTrip).

### 🔍 מה שנבדק בפועל
- `mirrorVisitorStats`: קריאה ישירה (hook זמני) עם דאטה מפוברקת (2 מדינות, ביקור app+manual באותה מדינה) - `computeCountryVisitInfo` חישבה נכון (`hasApp`/`hasManual`/`count` לפי הציפייה המדויקת), הכתיבה האמיתית ל-`_analytics/visitorStats/{uid}` ניסתה את הנתיב הנכון (נדחתה - `PERMISSION_DENIED`, מגבלת הסביבה הידועה - ללא auth אמיתי).
- `dashboard.html`: 3 משתמשים מפוברקים (TH/GR/IT/FR, כולל חפיפת מדינה בין שני משתמשים) - התוצאה (4/248 מדינות, 2%, אירופה 3/51 6%, אסיה 1/50 2%, 4/4 בירות) תואמת בדיוק חישוב ידני עצמאי. כהה נבדק (צילום מסך).
- `node --check` נקי בכל 3 הקבצים (`achievements.html`, `dashboard.html`, `scripts/backfill-visitor-stats.js`). `database.rules.json` תקין JSON. `scripts/i18n-audit.js` - 0 ממצאים חדשים.
- הסקריפט **לא הורץ** מול production - רק אומת שהוא נטען, מתחבר ל-`firebase-admin` דרך `functions/node_modules` בהצלחה, ומנסה את הפעולה הנכונה (נכשל כצפוי בלי credentials אמיתיים - `app/invalid-credential`, לא נגע בשום דאטה אמיתי).
- כל hook-בדיקה זמני הוסר ואומת שהוסר (0 ב-2 הקבצים).

**תכונה חדשה (schema + לוגיקת aggregate) → minor bump.**

## v4.82.1 — תיקון: הוספת ציוד ידנית נכשלת בשקט; חיבור תמונות/ציוד ללוגינג האנליטיקס

### 🐛 הבאג הדחוף
`addItem()` ב-packing.html - הפונקציה המשותפת לארבעת מקורות ההוספה בפועל (הקלדה ידנית, quick-pick, קבלת הצעת AI, מודל "הוספת פריט") - כתבה `await push(...)` **בלי שום `safeWrite`, בלי `try/catch` בכלל**. כתיבה שנדחית הפכה ל-unhandled promise rejection: המשתמש לוחץ "הוספה", הפריט לא נוסף, ואין שום משוב - לא toast, לא console.error. בדיוק דפוס "כשלון שקט" ש-CLAUDE.md כבר מסמן כבעיה חוזרת בפרויקט. נתפס במהלך חקירת שלב 3 של האנליטיקס (איזה מ-8 נקודות ה-safeWrite הקיימות ב-packing.html זו, ולמה זה לא הופיע בדשבורד - זו לא הייתה אחת מהן).

**תוקן**: עטוף ב-`window.safeWrite(...,"addItem",()=>logWriteEvent("addItem",true))`, עם `logWriteEvent("addItem",false)` וtoast אמיתי ("ההוספה נכשלה. נסו שוב.") בענף הכישלון - אותה תבנית כמו שאר הכתיבות בקובץ. תיקון יחיד בפונקציה המשותפת חל אוטומטית על כל ארבעת מקורות הקריאה, בלי לגעת בהם בנפרד.

### 🔌 חיבור נוסף לאנליטיקס (לא באג, השלמת כיסוי)
`uploadPhotos()` ב-places.html (המנגנון היחיד שכותב בפועל ל-`photos/items`) חובר גם הוא ל-`safeWrite`/`logWriteEvent` (context: `"uploadPhotos"`) - **בלי לשנות את התנהגות ה-try/catch הקיימת**, שכבר עבדה נכון (תופסת כשלון Storage/RTDB כאחד, מציגה toast, ממשיכה לתמונה הבאה). מימוש: `safeWrite` בולע את הדחייה במקום להעביר אותה הלאה, אז בענף הכישלון נזרקת שגיאה ידנית כדי להעביר שליטה בחזרה בדיוק לאותו catch שכבר היה קיים - מה שנראה כלפי חוץ בדיוק כמו לפני, רק עם שכבת לוגינג מתחתיו.

### 🔍 מה שנבדק בפועל
- `addItem`: קריאה אמיתית מקצה לקצה מול production RTDB (tripId מזויף, ללא auth אמיתי) - הכתיבה האמיתית נדחתה כצפוי (`PERMISSION_DENIED`, מגבלת הסביבה הידועה), `safeWrite`/`logWriteEvent` תפסו נכון עם context `"addItem"`, וה-toast "ההוספה נכשלה. נסו שוב." אומת בפועל ב-DOM (לא היה שם קודם). ענף ההצלחה אומת בנפרד (`window.safeWrite` מוחלף זמנית שיחזיר `true`) - toast "הפריט נוסף" מוצג, לא toast הכישלון.
- `uploadPhotos`: קריאה אמיתית מקצה לקצה עם קובץ תמונה אמיתי - Storage עצמו נדחה (`storage/unauthorized`, אותה מגבלת סביבה, עוד לפני שמגיעים ל-RTDB) - אושר שה-catch הקיים עדיין תופס נכון ולא נשבר. שורת ה-RTDB/safeWrite החדשה עצמה בודדה ונבדקה בנפרד (hook זמני שמשחזר בדיוק את אותה שורה) - נדחתה עם אותו context `"uploadPhotos"`, אותה זריקה מלאכותית, אותו מנגנון תפיסה.
- `dashboard.html`: דאטה מפוברק עם ה-context-ים החדשים (`addItem` 2/3, `uploadPhotos` 3/4) - האחוז הכולל (71%), הפילוח לפי context ולפי file, והמיון מהגרוע-ביותר תואמים בדיוק לחישוב ידני.
- כל hook-בדיקה זמני הוסר ואומת שהוסר (0 בכל 3 הקבצים). `node --check` נקי. `scripts/i18n-audit.js` - 0 ממצאים חדשים.

**תיקון באג (addItem) → patch bump.**

## v4.82.0 — לוגינג הצלחה/כישלון כתיבות (safeWrite) לדשבורד האנליטיקס

### 🎯 מה השתנה
`safeWrite.js` (משותף, נטען ב-6 העמודים): פרמטר רביעי אופציונלי `onSuccess` - callback שמופעל (עטוף ב-try/catch משלו) רק אחרי כתיבה מוצלחת. **33 הקריאות הקיימות שלא מעבירות אותו לא השתנו בהתנהגותן** - אומת ישירות עם בדיקת יחידה טהורה (Node, בלי Firebase בכלל): הצלחה-בלי-onSuccess, הצלחה-עם-onSuccess, onSuccess שזורק/נדחה (לא שובר את התוצאה), וכישלון (onSuccess לא נקרא בכלל) - כל 5 המקרים תואמים בדיוק.

חוברו בפועל **כל 33 נקודות הקריאה** ב-6 הקבצים (index/itinerary/packing/places/shopping/achievements) - בכל אחת, גם הצלחה (`onSuccess`) וגם כישלון (הענף הקיים) כותבים אירוע ל-`_analytics/writes/{eventId}`: `{file, context, success, timestamp}`. עדכון הכתיבה המקורית עצמה - **אפס שינוי** בכל אחד מ-33 האתרים, מעבר לתוספת הזו.

`database.rules.json`: `_analytics/` חדש - `.read` נעול ל-UID אחד ספציפי (דשבורד בלבד), `.write` תחת `_analytics/writes` פתוח ל-`auth != null` (כל משתמש מחובר יכול לרשום אירוע - הלוגינג רץ בדפדפן שלו, לא של בעל הדשבורד). **לא פורסם עדיין** - ה-diff מוצג לאישור, הפרסום בקונסולה בידי המשתמש בעצמו (ר' commit הקודם על trips/).

`dashboard.html`: קורא `_analytics/writes`, מציג אחוז הצלחה כולל + פילוח לפי `context` ולפי `file`, ממוין מהגרוע ביותר (כדי להבליט "נקודת כשל חוזרת") עם הדגשה אדומה לשורה מתחת ל-90% הצלחה.

### 🐛 באג אמיתי שנתפס ותוקן תוך כדי בדיקה (לא רק תוכנן)
`index.html` (compat v8) המקורי כתב `db.ref(path).push(data).catch(...)`. **אומת ישירות (לא הונח)**: ה-`.catch()` הזה **אף פעם לא נורה** בפועל כשהכתיבה נכשלת - הדחייה יוצאת כ-"Uncaught (in promise)" במקום זאת, עוקפת את ה-`.catch()` לגמרי (אושר עם בדיקה מבודדת: משתנה שאמור להתעדכן בתוך ה-`.catch()` נשאר `false` גם אחרי המתנה). **תוקן** ל-`db.ref(path).push().set(data).catch(...)` (דו-שלבי: מפריד את יצירת המפתח מהכתיבה בפועל) - אומת שוב, נקי לגמרי. **חמשת הקבצים המודולריים (v9) לא סבלו מהבאג הזה** - `push(ref(db,path), data).catch(...)` שלהם תופס נכון, אומת ישירות בכל אחד מהם.

### 🔍 מה שנבדק בפועל
- `safeWrite` עצמו - unit test טהור ב-Node, 5 תרחישים (ר' למעלה).
- `achievements.html` - זרימה מלאה אמיתית מקצה לקצה (טופס → safeWrite נכשל → logWriteEvent(false) נכשל בעצמו) - שני הכישלונות נתפסים בשקט, UI נשאר במצב תקין (לא תקוע), בדיוק כמו שנדרש.
- שאר 5 הקבצים - `logWriteEvent` נבדק ישירות (hook זמני), הצלחה+כישלון, שניהם נתפסים נקי בלי "Uncaught" (אחרי תיקון ה-index.html).
- `dashboard.html` - דאטה מפוברק (25 אירועים, 6 קבצים/context שונים, כולל שתי נקודות כשל מכוונות ב-50%/80%) - האחוז הכולל (84%), הפילוח לפי context ולפי file, המיון מהגרוע-ביותר, וההדגשה האדומה - כולם תואמים בדיוק לחישוב ידני עצמאי. כהה ובהיר נבדקו.
- כל hook-בדיקה זמני הוסר ואומת שהוסר (0 בכל 7 הקבצים).

`node --check` נקי בכל 7 הקבצים. `scripts/i18n-audit.js` - 0 ממצאים חדשים.

**תכונה חדשה (תשתית לוגינג) → minor bump.**

## v4.81.2 — תיקון: רשימת המדינה חופפת ויזואלית לשדה העיר

### 🐛 הבאג
ברשימה הנפתחת של שדה בחירת המדינה (הטופס להוספת ביקור ידני): כשהרשימה פתוחה, קליק שמיועד לשדה "עיר" שמתחתיה יכול לפול על פריט ברשימה במקום, כי הרשימה עדיין פתוחה וחופפת אותו. **שורש הבעיה**: `.combo-list` היה `position:absolute` - "צף" מעל התוכן שאחריו במקום לדחוק אותו למטה, אז שדה העיר נשאר במקומו הרגיל בפריסה בזמן שהרשימה יושבת פיזית מעליו על המסך. קליק באזור החפיפה נבלע ע"י הרשימה (או הרקע השקוף שלה) ולא מגיע לשדה העיר בכלל - אומת בפועל: קליק ראשון על שדה העיר בזמן שהרשימה פתוחה לא הגיע אליו.

### ✅ התיקון
הסרת `position:absolute`/`top`/`inset-inline`/`z-index` מ-`.combo-list` - עכשיו זה אלמנט רגיל בזרימת הפריסה (עם `margin-top:4px` בלבד לרווח הקודם). כשהרשימה פתוחה, היא דוחקת בפועל את שדה העיר/תאריך/כפתורים למטה - אין יותר חפיפה פיזית על המסך שאפשר לקלוט עליה קליק שגוי. `.sheet` (shared.css) כבר `overflow:auto` עד `88dvh`, אז רשימה פתוחה פשוט מגדילה את אזור הגלילה הקיים - שום דבר לא נחתך.

### 🔍 מה שנבדק בפועל
1. **שחזור מדויק של הבאג לפני התיקון** - קליק ישיר על שדה העיר בזמן שהרשימה פתוחה (בלי לסגור אותה קודם) נבלע ע"י הרשימה, לא הגיע לשדה. אחרי התיקון - אותו קליק בדיוק מגיע נכון לשדה העיר בפעם הראשונה (נבדק גם עם קליק לפי קואורדינטה גולמית וגם לפי רפרנס DOM).
2. מצב כהה ובהיר - נבדקו ויזואלית, אין שינוי בעיצוב הקיים (רק המיקום של הרשימה עצמה, לא הצבעים/המידות).
3. מובייל (375px) ודסקטופ - נבדקו שניהם, אותה התנהגות תקינה בשניהם.
4. שום שינוי בלוגיקת הסינון/הבחירה עצמה (`renderCountryList`/`mousedown`/`blur`) - התיקון הוא CSS בלבד.

`node --check` נקי. `scripts/i18n-audit.js` - 0 ממצאים חדשים (אין טקסט חדש למשתמש).

**תיקון באג → patch bump.**

## v4.81.1 — תיקון: זום אוטומטי ב-iOS Safari בפוקוס על שדות הטופס

### 🐛 הבאג
הקלדה בשדות הטופס להוספת ביקור ידני (`achievements.html` - שדה חיפוש מדינה, שדה עיר, שדה תאריך) גרמה לדף כולו לזום-אין ולצאת מהמסגרת ב-iOS Safari. **שורש הבעיה**: ל-`<input>`/`<select>`/`<textarea>` בעמוד הזה לא היה font-size מפורש בכלל - הם נפלו ל-font-size ברירת המחדל הקטן של הדפדפן (מתחת ל-16px), וזה בדיוק הסף הידוע שמפעיל את הזום האוטומטי-בפוקוס של iOS Safari (לא Chrome, לא Android - ספציפי ל-WebKit/Safari).

`itinerary.html`/`places.html` כבר פתרו את זה מזמן עם `button,input,select,textarea{font:inherit}` - ב-`achievements.html` הכלל המקביל (נוסף בשלב 4) כלל בטעות רק `button{font:inherit;cursor:pointer}`, בלי `input,select,textarea`. לא באג חדש - פער שהיה שם מאז שלב 4, שהתגלה רק עכשיו.

### ✅ התיקון
`button{font:inherit;cursor:pointer}` → `button,input,select,textarea{font:inherit}` + `button{cursor:pointer}` (שתי הצהרות נפרדות - בדיוק כמו ב-itinerary.html/places.html, לא ממציא תבנית חדשה). `cursor:pointer` נשאר רק על `button` בכוונה - סמן טקסט על שדה קלט, לא סמן-אצבע.

**לא נגעתי ב-`.field input,.field select,.field textarea`** (min-height/border/padding/border-radius) - התיקון הוא רק `font-size` (דרך `font:inherit`), לא הצריך `!important` כי אין כלל מתחרה שדורס אותו.

### 🔍 מה שנבדק בפועל
- `getComputedStyle` על שלושת השדות (מדינה/עיר/תאריך) - כולם `16px` בדיוק, תואם ל-`body` (שגם הוא 16px, ברירת מחדל של הדפדפן - אין `font-size` מפורש בעמוד).
- `meta[name=viewport]` - זהה בדיוק ל-itinerary.html/places.html/packing.html (`width=device-width,initial-scale=1,viewport-fit=cover`), לא היה צריך שינוי.
- עיצוב לא נשבר - גובה/ריווח/מסגרת השדות זהים לפני/אחרי (רק גודל הטקסט השתנה), מצב כהה ובהיר נבדקו ויזואלית.
- הקלדה בפועל בכל שלושת השדות (כולל סינון ה-combobox) - עדיין עובד תקין.

**מגבלת בדיקה אמיתית, לא מוסתרת**: לא ניתן לשחזר/לאמת כאן את התנהגות הזום-בפוקוס הספציפית של iOS Safari עצמה - סביבת הבדיקה שלי (Chromium) לא מיישמת את ה-quirk הזה בכלל, בכל רזולוציה או user-agent שאדמה. 16px הוא הסף המתועד הרשמי שמונע את הזום (לא ניחוש - זו ההתנהגות הידועה של WebKit), ו-`itinerary.html`/`places.html` כבר מוכיחות בפרודקשן שהתבנית הזו עובדת. **אימות סופי בפועל על מכשיר iOS אמיתי (או Safari Simulator) נדרש מהמשתמש עצמו** - אותה מגבלה מבנית שכבר תועדה כמה פעמים בפרויקט הזה (OAuth אמיתי, כתיבות RTDB אמיתיות).

`node --check` נקי. `scripts/i18n-audit.js` - 0 ממצאים חדשים (אין טקסט חדש למשתמש).

**תיקון באג → patch bump.**

## v4.81.0 — "ההישגים שלי" - בלוק סטטיסטיקה (מדינות/יבשות/בירות)

### 🎯 מה השתנה
בלוק טקסט חדש בראש `achievements.html`, מעל ה-toggle, מתעדכן מה-`onValue` הקיים (אין כתיבה/רענון נפרד):

- **"ביקרת ב-X מדינות מתוך Y בעולם (Z%)"** - Y הוא סך כל 248 הקודים ב-`window.COUNTRY_CONTINENT` (אותו מקור אמת כמו הטופס/תצוגות).
- **שורה לכל יבשת שיש בה ביקור בפועל** (לא כל 6 היבשות תמיד - יבשות עם 0% מוסתרות, לפי בחירת המשתמש: "רק יבשות עם ביקור בפועל" כדי לא להציג רעש של אפסים): "X מתוך Y מדינות ביבשת (Z%)".
- **"ביקרת ב-X בירות מתוך Y מדינות שביקרת בהן"**.

### 🏛️ `capitalCities.js` (קובץ חדש)
מיפוי alpha-2 → שם בירה, **דו-לשוני (אנגלית + עברית)**, לפי החלטה מפורשת של המשתמש (לא רק אנגלית). מקור: **Wikidata Query Service** (SPARQL ציבורי, בלי אימות) - לא REST Countries: ה-API הישן (ללא מפתח) הוצא משימוש *באמצע החקירה הזו עצמה*, וה-v5 החדש דורש יצירת חשבון ומפתח API - פעולה שכללי הבטיחות של הפרויקט אוסרים עליי לבצע בשם המשתמש. Wikidata לא דורש כלום מזה.

- **כיסוי: 238 מתוך 248 הקודים.** 10 החסרים - **לא כשל שליפה, העדר אמיתי**: לאף אחת מהן אין בירה במובן הפוליטי הרגיל (אנטארקטיקה, איים לא-מיושבים, הונג קונג/מקאו כאזורים מנהליים מיוחדים ולא "בירה"). מתועד בפירוט בהערת הקובץ.
- **ערך נגזר אחד, לא הודבק ישירות**: לבירת AG (סנט ג'ונס) אין תווית "en" ב-Wikidata בכלל (אומת ישירות מול הישות) - השם האנגלי כאן נגזר מכותרת ה-enwiki sitelink של אותה ישות, בהסרה מכנית של הסיומת המבחינה ("St. John's, Antigua and Barbuda" → "St. John's") - עדיין נתון של Wikidata עצמו, לא ניחוש.
- **פער אמיתי אחד**: ל-MP (איי מריאנה הצפוניים) אין שם עברי בכלל ב-Wikidata (`he: null`) - אין ויקיפדיה עברית לעיר הזו, לא באג.
- **מדינות עם כמה בירות** (דרום אפריקה - 3, בוליביה/סרי לנקה/פקיסטן/אסוואטיני/תימן/מונסראט/מאיוט/פלסטין - 2 כל אחת) - נשמרות כמערך, ביקור בכל אחת מהן נחשב.

### 🔍 fuzzy matching - מתועד כלא-מושלם, בכוונה
`normalizeForMatch()`: הסרת case + diacritics/ניקוד עברי (שניהם combining marks ב-NFD, טווחים שונים) + בדיקת הכלה חלקית דו-כיוונית, מול השם האנגלי והעברי גם יחד. **לא** ניסיון לפתור fuzzy matching מושלם - אותה רוח בדיוק כמו מגבלת הכפילויות בתצוגת עיר (שלב 3).

### 🧪 מה שנבדק בפועל
עם hook-בדיקה זמני (הוסר ואומת שהוסר):
1. חישוב מדויק אומת ישירות (לא רק ויזואלית): 7 מדינות, 4 יבשות, 4 בירות תואמות מתוך 7 - כולל מדינה עם ביקור אך בלי נתוני בירה בכלל (אנטארקטיקה) שנספרת במכנה אך לא יכולה לתרום למונה.
2. Diacritics: "Torshavn" (בלי גרש) תואם "Tórshavn". Case: "mexico city" תואם "Mexico City". בירה מרובה: "Cape Town" (אחת מ-3 הבירות של דרום אפריקה) תואם. אי-התאמה: "Barcelona" מול אנדורה **לא** תואם (בדיקת false-positive).
3. עברית: "פריז" תואם את השם העברי של בירת צרפת.
4. מצב 0 ביקורים - שורה אחת בלבד ("0 מתוך 248"), בלי שורות יבשת/בירות חסרות-משמעות ("0 מתוך 0").
5. מצב כהה ובהיר - שניהם נבדקו ויזואלית.
6. FPS - 30fps בשלוש התצוגות (מובייל, גרירה אמיתית), זהה למצב לפני הבלוק - הבלוק הוא DOM טקסט טהור, בלי שום קשר ללולאת רינדור ה-WebGL.
7. מצב לא-מחובר - הבלוק מקונן בתוך `appContainer` הקיים, לא דולף.

`node --check` נקי. `scripts/i18n-audit.js` - 0 ממצאים חדשים.

**תכונה חדשה (סטטיסטיקות + קובץ נתונים חדש) → minor bump.**

## v4.80.1 — שדה מדינה בטופס ההזנה הידנית: combobox עם autocomplete במקום select

### 🎯 מה השתנה
שדה "מדינה" בטופס ההזנה הידנית (`achievements.html`, שלב 4) הוחלף מ-`<select>` גולל-בלבד ל-combobox מותאם: `<input>` טקסט + רשימה נפתחת (`div`) שמסתננת בזמן הקלדה. לא נעשה שימוש ב-`<datalist>` הטבעי - התנהגות הסינון שלו לא עקבית בין דפדפנים ותמיכת iOS Safari בו חלשה/לא אמינה (בעיה מתועדת בפועל), ולא הייתה חוסכת את לוגיקת הוולידציה ממילא. הרשימה הנפתחת בנויה כ-`div` רגיל בזרימת ה-DOM (לא popup דפדפן) - כך RTL ומצב כהה עוקבים אחרי ה-CSS של הדף עצמו, בלי צורך "להילחם" בעיצוב native.

מקור הנתונים לא השתנה - עדיין 248 הקודים מ-`window.COUNTRY_CONTINENT` (משלב 3) ועדיין `Intl.DisplayNames(['he'],...)` לשם התצוגה (משלב 4). **תוספת**: הסינון בודק גם כנגד `Intl.DisplayNames(['en'],...)` (לא מוצג, רק לסינון) - הקלדה באנגלית ("France") מוצאת גם היא את "צרפת" המוצגת, לא רק הקלדה בעברית.

ולידציה: `selectedCountryCode` (משתנה נפרד, לא ערך השדה עצמו) נקבע רק בלחיצה בפועל על פריט מהרשימה המסוננת (או Enter שבוחר את הפריט הראשון) - כל הקלדה אמיתית מאפסת אותו. טקסט חופשי שלא נבחר מהרשימה נחסם בשמירה **באותה צורה בדיוק** כמו היעדר בחירה לגמרי (משלב 4) - לא נוסף מסלול חדש.

### 🔍 מה שנבדק בפועל
עם hook-בדיקה זמני (הוסר ואומת שהוסר):
1. פוקוס בשדה ריק → הרשימה המלאה (248 מדינות, ממוינת עברית) מוצגת.
2. הקלדת "צרפ" → 4 תוצאות תואמות (כולן מכילות את המחרוזת), כולל "צרפת" עצמה - נדרש במפורש במשימה, אומת.
3. הקלדת "Franc" (אנגלית) → מוצאת "צרפת" (המוצגת בעברית) - מסלול הסינון האנגלי אומת בפועל, לא רק הונח.
4. בחירה בלחיצה → קוד נכון נשמר (`FR`), הרשימה נסגרת, שדה מציג את השם העברי.
5. הקלדת טקסט לא-תואם ("שטויות בטלנים") → הודעת "לא נמצאה מדינה תואמת", שמירה חסומה (לא נשלחת כתיבה כלל - אומת שאין ניסיון `push` חדש).
6. מצב כהה ובהיר - שניהם נבדקו ויזואלית, קונטרסט תקין בשניהם (הרשימה משתמשת באותם `--surface`/`--line`/`--surface-2` כמו שאר הדף - לא צבעים חדשים).
7. FPS - 60fps (מובייל, גרירה אמיתית) בכל שלוש התצוגות אחרי הפתיחה/סגירה של הטופס - אין רגרסיה. מעברי טאב (מדינה/עיר/יבשת) נבדקו ועובדים כרגיל.

`node --check` נקי. `scripts/i18n-audit.js` - 0 ממצאים חדשים.

**שיפור UX לרכיב קיים, בלי שינוי סכמה/יכולת חדשה → patch bump.**

## v4.80.0 — "ההישגים שלי" - הזנת ביקור ידני (שלב 4/4, סוגר את התכונה)

### 🎯 מה השתנה
כפתור FAB חדש (`.fab` הקיים ב-shared.css, אותה מחלקה כמו itinerary/packing/places) פותח טופס (`.overlay`/`.sheet` קיימים) להזנת ביקור מלפני שהיה שימוש באפליקציה. שלושה שדות:

- **מדינה** - `<select>`, לא טקסט חופשי (בדיוק כמו שהמשימה ביקשה - כדי למנוע `countryCode` שלא תואם קוד ISO קיים). האפשרויות נבנות מ-248 המפתחות של `window.COUNTRY_CONTINENT` (כבר טעון משלב 3 - אותו מקור-אמת ל"מה נחשב מדינה תקפה" בעמוד הזה, לא רשימה שנייה עצמאית). שמות התצוגה בעברית - לא טבלה מוטמעת בת 248 רשומות אלא `Intl.DisplayNames(['he'],{type:'region'})` בזמן ריצה, נתוני ה-CLDR של הדפדפן עצמו. **אומת אמפירית**: כל 248 הקודים נפתרים בלי כשלון אחד, ממוין לפי השם העברי.
- **עיר** - טקסט חופשי (אופציונלי), בדיוק כמו ברשומות app-derived.
- **תאריך ביקור** - אופציונלי (כלשון המשימה - מי שמזין ביקור מלפני האפליקציה לא בהכרח זוכר תאריך מדויק). ברירת מחדל "היום" כשריק - **לא** `null`: כל קורא קיים של `visitedAt` כבר מניח מחרוזת ISO תקפה שאפשר לעטוף ב-`new Date()` (הפלט המתועד ב-CLAUDE.md), ו-`null` היה שובר את ההנחה הזו בשקט. `cityName`, לעומת זאת, נשאר `null` כשריק - אין "ברירת מחדל" הגיונית לעיר, בדיוק כמו ברשומות ישנות שכבר `null` שם.

בשמירה: `push()` ל-`users/{uid}/visits` דרך `window.safeWrite` הקיים (לא כתיבה ישירה) - `{source:"manual", countryCode (uppercase), cityName, visitedAt, createdAt}`, בלי `legId`/`tripId` (אלה רק לרשומות app). אין קריאה נפרדת ל"רענון הגלובוס" אחרי שמירה - הגלובוס כבר מאזין ל-`onValue` על אותו נתיב (משלב 2), אז כתיבה מוצלחת מפעילה מחדש את אותה לוגיקת חישוב/צביעה קיימת מעצמה, בלי קוד נוסף.

### 🔍 מה שנבדק בפועל
עם hook-בדיקה זמני (הוסר ואומת שהוסר):
1. תוקף הטופס - שדה מדינה מתחיל על placeholder ריק (לא ברירת מחדל אלפביתית בשקט) - שמירה בלי בחירה נחסמת עם toast, לא נשלחת כתיבה.
2. **צורת הרשומה בפועל אומתה מדויקת** (נלכדה רגע לפני ה-`push`, בדיוק לפי המתודולוגיה המתועדת ב-CLAUDE.md לכתיבות RTDB שלא ניתנות לאימות מלא כאן): `{"source":"manual","countryCode":"IT","cityName":"רומא","visitedAt":"2019-06-15T00:00:00.000Z","createdAt":"...")}` - הכל תואם בדיוק.
3. **`safeWrite` בשימוש בפועל, לא רק מיובא** - `PERMISSION_DENIED` אמיתי (מזהה מזויף, ללא auth token אמיתי - אותה מגבלה מתועדת כל הסשן) נתפס ונרשם דרך `[safeWrite] saveManualVisit`, המודאל נשאר פתוח עם הנתונים שמורים (לא נסגר בכשלון, לא איבד קלט) - בדיוק ההתנהגות הנדרשת בכשלון.
4. **עדיפות-צבע אחרי הוספה - אומת מדויק, לא רק הונח**: מדינה עם ביקור app קיים (איטליה) + ביקור manual חדש לאותה מדינה → **נשארת זהב** (`count` עולה מ-1 ל-2, `hasApp`/`hasManual` שניהם `true`). מדינה חדשה לגמרי (גרמניה) עם רק ביקור manual → **כחול**. אומת ישירות דרך פונקציית הצבע לפני/אחרי, לא רק ויזואלית.
5. שלושת התצוגות נבדקו עם הדאטה החדשה: מדינה (איטליה זהב/גרמניה כחול), יבשת (אירופה כולה זהב - יש לה מדינה עם app), עיר (Milan/רומא/ברלין - שלוש נקודות נפרדות, רומא וברלין ה"חדשות" geocoded בהצלחה דרך אותו legGeocode.js קיים).
6. FPS - 60fps בשלושת התצוגות (מובייל, גרירה אמיתית) עם ה-FAB/מודאל קיימים בדף - אין רגרסיה.
7. משתמש לא מחובר - FAB/מודאל לא דולפים (מקוננים בתוך `appContainer` הקיים, אותו `.hidden` cascade).

`node --check` נקי. `scripts/i18n-audit.js` - 0 ממצאים חדשים.

**זהו commit הסגירה של תכונת ה-achievement map (4/4) - שלב 1 (שלד), שלב 2 (דאטה אמיתית ברמת מדינה), שלב 3 (toggle תצוגה), שלב 4 (הזנה ידנית) כולם חיים ב-production.**

**תכונה חדשה (הזנה ידנית) → minor bump.**

## v4.79.0 — "ההישגים שלי" - toggle מדינה/עיר/יבשת (שלב 3/4)

### 🎯 מה השתנה
בקר toggle חדש בראש `achievements.html` (3 טאבים, אותה תבנית `.tabs`/`.tab` כמו itinerary.html/places.html) עובר בין שלוש תצוגות. עדיין בלי ממשק הזנה ידנית (שלב 4).

- **מדינה** (קיים משלב 2, ללא שינוי לוגי).
- **יבשת**: `countryContinent.js` (נטען משלב 2, לא היה בשימוש עד עכשיו) נקרא סוף-סוף - `window.COUNTRY_CONTINENT` ממפה alpha-2 ליבשת. `computeContinentVisitInfo()` מקבץ את `countryVisitInfo` הקיים ליבשת (לא מקור-אמת חדש - רה-קיבוץ של אותו נתון), אותה עדיפות app-מנצח-manual, ברמת היבשת.
- **עיר**: שונה במהותו - לא choropleth אלא נקודות (`globe.gl` `pointsData`), כי רשומות `visits` שומרות `cityName` כמחרוזת גולמית, לא קואורדינטות. הנקודות ממוקמות ע"י geocoding בפועל של המחרוזת דרך `window.geocodeLegDestination` (מ-`legGeocode.js`, כבר טעון ע"י wizard.html/index.html לאותו צורך בדיוק - נעשה בו שימוש חוזר, לא מומש מחדש). **תוצאה מתועדת ומכוונת**: קיבוץ הוא לפי מחרוזת מדויקת, לא לפי מקום אמיתי - "Rome"/"רומא"/"Roma, Italia" עבור אותו יעד אמיתי מייצרים כמה נקודות נפרדות, לא אחת. **אומת ויזואלית בפועל** (לא רק תועד): "Rome" ו-"רומא" יוצרים שתי נקודות חופפות-כמעט באיטליה, בצבעים שונים (`app`/`manual` בהתאמה) - בדיוק כמו שתואר.

תוצאות geocoding נשמרות ב-cache לפי מחרוזת מדויקת לכל אורך חיי הדף - זה מזכור (memoization) של קריאה חיצונית אידמפוטנטית, **לא** אותו דבר כמו קאשינג ההחלטה הצבע/ספירה מ-visits (זו עדיין מחושבת fresh בכל שינוי, כמו בשלב 2). Geocoding רץ רק אחרי שהמשתמש בפועל פותח את תצוגת העיר - לא באופן ספקולטיבי.

### 🔍 מה שנבדק בפועל
עם hook-בדיקה זמני (הוסר ואומת שהוסר) ודאטה מפוברקת (IT/FR/JP/AU/US בין 4 יבשות, + "Rome"/"רומא"/Paris/Tokyo/Sydney/"New York"):
1. תצוגת מדינה - ללא שינוי מול שלב 2 (איטליה זהב, צרפת כחול).
2. תצוגת יבשת - **כל אירופה** (לא רק איטליה/צרפת) נצבעת זהב כי לאיטליה יש ביקור app - אומת ויזואלית וגם ישירות דרך פונקציית הצבע (Europe/Asia/Americas זהב, Oceania כחול - תואם בדיוק את `continentVisitInfo` הפנימי).
3. תצוגת עיר - geocoding אמיתי (לא מדומה) דרך רשת אמיתית ל-Open-Meteo/Nominatim הצליח לכל 6 המחרוזות, כולל "רומא" דרך הנתיב העברי-fallback של legGeocode.js. הנקודות הכפולות של רומא/רומא-עברית אומתו ויזואלית בצילום מסך מזום-אין.
4. Race-condition: קריאה כפולה ל-`refreshCityPoints()` לפני שה-geocode הראשון נפתר לא יוצרת בקשת רשת כפולה (placeholder `null` סינכרוני לפני הקריאה האסינכרונית).
5. מדינה בלי מיפוי יבשת / geocoding שנכשל - נופל בשקט ל-neutral/דילוג עם `console.warn`, לא קורס.
6. משתמש לא מחובר - ה-toggle לא דולף לפני האימות (נבדק - `appContainer` עדיין מוסתר כראוי).
7. מצב כהה - נבדק ויזואלית, קונטרסט תקין על ה-tabs.
8. FPS - **30fps ושלושת התצוגות זהות** תחת גרירה אמיתית עם אצבע (מובייל 375px) - אין רגרסיה יחסית בין התצוגות (המספר המוחלט תלוי-סביבה, כבר תועד בעבר בפרויקט הזה).

`node --check` על סקריפט ה-module (3 פעמים - אחרי המימוש, אחרי תיקון ה-race, ואחרי הסרת ה-hooks). `scripts/i18n-audit.js` - 0 ממצאים חדשים (העמוד הזה חד-לשוני עברית בלבד, כמו בשלבים 1-2, אין `tr()`).

**תכונה חדשה (toggle תצוגה) → minor bump.**

## v4.78.0 — "ההישגים שלי" - דאטה אמיתית ברמת מדינה (שלב 2/4)

### 🎯 מה השתנה
`achievements.html` קורא עכשיו את `users/{uid}/visits` של המשתמש המחובר (Firebase Database `onValue`, listener חי - לא קריאה חד-פעמית) וצובע את שכבת ה-choropleth לפי הדאטה האמיתית, במקום הצבע האחיד מ-שלב 1. `countryContinent.js` נטען (script tag בלבד, הלוגיקה הפנימית שלו לא נגעה) - עדיין לא בשימוש, מוכן לשלב 3 (toggle יבשת).

**גשר-זיהוי חדש, לא הונח**: `world-atlas`'s `countries-110m.json` מזהה כל פוליגון לפי קוד ISO 3166-1 **מספרי** (`feature.id`, למשל `"380"` לאיטליה) - רשומות `visits` שומרות `countryCode` כ-ISO **alpha-2** (`"IT"`). `ISO_NUMERIC_TO_ALPHA2` (טבלה חדשה, 248 רשומות) מגשרת בין השניים - **נגזרה מאותו מקור UN M49 שכבר אומת עבור `countryContinent.js`** (עמודת `m49Code` שלו, מאושר ישירות שווה לקוד ה-ISO המספרי לכל מדינה שנבדקה - איטליה 380, ארה"ב 840), לא הוקלדה/נוחשה מחדש. **ממצא אמיתי, לא הונח**: רק 177 מתוך 195 מדינות יש להן פוליגון בכלל ברזולוציה הזו (למשל אנדורה - חסרה לגמרי) - מגבלה אמיתית של `world-atlas`'s 110m, לא באג במיפוי.

**עדיפות-צבע וספירה, בדיוק לפי ההחלטה שכבר התקבלה**: `computeCountryVisitInfo()` בונה `{hasApp, hasManual, count}` מהרשומות הגולמיות בכל render (`onValue` callback) - שום דבר לא נשמר/נקאש. `capColorForFeature()` בוחר `COLOR_APP` (זהב) אם `hasApp`, אחרת `COLOR_MANUAL` (כחול) אם יש ביקור כלשהו, אחרת `COLOR_NONE` (כמעט שקוף - טקסטורת כדור-הארץ האמיתית מציצה דרך למדינות בלי שום ביקור). `count` מצטבר משני המקורות יחד תמיד, בלי קשר לצבע שנבחר.

### 🔍 מה שנבדק בפועל
עם hook-בדיקה זמני (הוסר ואומת שהוסר) ודאטה מפוברקת אך תואמת-סכימה במדויק (`source`/`countryCode`/`cityName`/`visitedAt`/`createdAt`, כמו הרשומות האמיתיות):
1. מדינה עם ביקור app בלבד (איטליה) → זהב.
2. מדינה עם ביקור manual בלבד (צרפת) → כחול, נבדל בבירור מהזהב.
3. מדינה עם שני הסוגים (יוון: 1 app + 1 manual) → **זהב** (app מנצח), `count:2` - נבדק גם ויזואלית (שלוש המדינות מוצגות יחד באותו צילום-מסך, כל אחת בצבע הנכון) וגם ישירות דרך `capColorForFeature`.
4. אותיות-קטנות ב-`countryCode` (`"us"`) - מנורמל נכון ל-`US` דרך `toUpperCase()`, עדיין מנצח/נספר נכון.
5. מדינה בלי שום ביקור (גרמניה) → `COLOR_NONE`.
6. מדינה בלי פוליגון בכלל באטלס (אנדורה, `id:"020"`) → לא קורס, נופל בשקט ל-`COLOR_NONE`.
7. FPS - 60 (מובייל, גרירה אמיתית עם האצבע) עם הצביעה האמיתית פעילה - עקבי עם הבסיס משלב 1, אין רגרסיה משכבת-המידע הנוספת.

**לא ניתן לבדוק כאן**: 32 הרשומות האמיתיות מ-production (8 משתמשים, 20 טיולים) - זה דורש Firebase Auth אמיתי שאין לי גישה אליו (ולא אמור להיות). נדרש אימות סופי ע"י המשתמש עצמו, מחובר עם חשבון אמיתי שיש לו רשומות.

`node --check` על סקריפט ה-module. `scripts/i18n-audit.js` - 0 ממצאים חדשים.

**שינוי סכמה (חיבור דאטה אמיתית) → minor bump.**

## v4.77.0 — עמוד "ההישגים שלי" - שלד (שלב 1/4, בלי דאטה אמיתית)

### 🎯 מה השתנה
עמוד חדש, `achievements.html` - שלד בלבד לאשכול הגיימיפיקציה: גלובוס + choropleth (צבע-בסיס אחיד, עוד בלי ביקורים אמיתיים) + atmosphere, במסך מלא (לא bottom-sheet), בהקשר-אמיתי של האפליקציה (auth flow, dark mode, hamburger menu, Firebase Hosting) - לא בסביבת-בדיקה מבודדת כמו `globe-test.html`.

**קישור חדש בתפריט ההמבורגר** - `#achievementsLink` ("🏆 ההישגים שלי"), מוסתר כש-`!user` (בניגוד ל-`showcaseLink` הסמוך, שתמיד גלוי) - "ההישגים שלי" חסר משמעות למשתמש לא-מחובר, זה דאטה אישית חוצת-טיולים.

**עמוד חדש, לא אחד משאר 5 הדפים המבוססי-טיול** - במפורש **לא** נטענים: `legs.js`/`legBuilder.js` (כל פונקציה בשניהם פועלת על `legs` של טיול בודד - אין טיול יחיד כאן), `permissions.js` (הפונקציה היחידה שם משווה משתמש ל-`ownerId` של טיול - אין מה להשוות כאן), `member.js` (קורא `trips/{tripId}/memberProfiles` - אין tripId). **בלי bottom-nav** - מוצג/מוסתר לפי `data-feature`/`enabledFeatures` של טיול ספציפי (`index.html`'s `applyFeatureVisibility`), שאין להם מקבילה כאן - אותו תקדים בדיוק כמו `wizard.html`/`gittrip-showcase.html`, שני העמודים היחידים האחרים שמגיעים מחוץ לזרימת-הטיול, ששניהם גם משמיטים את ה-bottom-nav. **מצב כהה בזיכרון-מפתח נפרד** (`achievementsDarkMode`, לא `trip-{tripId}-theme` של `itinerary.html`) - `data-theme` על `documentElement` ישירות (לא על `body` כמו הפתרון הישן של `index.html`, שהיה תיקון-עקיפה למגבלה היסטורית ספציפית שלא רלוונטית לעמוד חדש).

**globe.gl מוטמע בדיוק לפי השיטה שאומתה ב-`globe-test.html`** - dynamic import של `globe.gl.min.js` (לא +esm), זיהוי-פורמט AVIF/WebP/JPEG אמיתי (לא `<picture>` - הטקסטורה עוברת דרך Three.js TextureLoader), טעינה פרוגרסיבית עם placeholder, ותיקון ה-double-fetch (commit `d417bc3`) מובנה מההתחלה - `globeImageUrl()` נקרא ישירות עם הפורמט הנבחר, לא דרך preload נפרד.

**טקסטורות הועתקו (לא הועברו) למיקום קבוע חדש**, `globe-assets/` - `globe-test-assets/` וגם `globe-test.html` עצמו נשארו בדיוק כפי שהיו, לא נגעו. `firebase.json` קיבל כלל `Cache-Control` מקביל (`immutable, max-age=31536000`) לתיקייה החדשה - חריגה קטנה ומכוונת ל"אל תיגע בקובץ אחר" כדי לא לשלוח את הטקסטורות החדשות עם caching חלש יותר משכבר תוקן.

**ידוע ומוכר, נדחה לשלב אחר**: אין תמיכה באנגלית בעמוד הזה בכלל (אין `tr()`, אין `dashboardText` מקביל) - בניגוד לכל שאר העמודים. שלד-בדיקה בלבד, לא הוחלט בטעות - יתווסף כשהפיצ'ר עצמו מתקדם.

### 🔍 מה שנבדק בפועל
בדפדפן, עם hook-בדיקה זמני שהוסר ואומת שהוסר לפני ה-commit:
1. שער-כניסה (לא מחובר) - מוצג נכון, כניסה עם Google.
2. משתמש מדומה מחובר - הגלובוס נבנה, choropleth+atmosphere מוצגים נכון במסך מלא.
3. טקסטורה - `globe-assets/earth-blue-marble.avif`, **בקשת רשת אחת בלבד** (לא 2 - אומת ישירות ב-Resource Timing, לא הונח).
4. מצב כהה/בהיר - טוגל עובד, `achievementsDarkMode` נשמר נכון, לא משפיע על FPS.
5. FPS - 30 (דסקטופ) / 60 (מובייל, גרירה אמיתית עם האצבע) - עקבי עם הבסיס שכבר אושר ב-`globe-test.html`, אין רגרסיה מהאינטגרציה לעמוד האמיתי.
6. קישור התפריט - `href`/`aria-label`/נראות-לפי-משתמש נכונים, נבדק דרך `handleAuthStateChanged` אמיתי ב-`index.html`.

`node --check` על סקריפט ה-module. `scripts/i18n-audit.js` - 0 ממצאים חדשים (אבל ר' ההערה למעלה - זה כי אין תשתית i18n בעמוד בכלל, לא כי הכיסוי מלא).

**תכונה חדשה (עמוד + קישור) → minor bump.**

## v4.76.2 — תיקון: "חזרה לתפריט" - openMenu=1 נשמר דרך redirect פנימי

### 🎯 מה השתנה
**סיבת השורש אושרה** (v4.76.1's דיאגנוסטיקה, שוחזרה בפועל, לא רק נקראה בקוד): `resolveTripSelection()` - נקראת מתוך `handleAuthStateChanged` כש-`?openMenu=1` מגיע בלי `tripId` - מוצאת `lastTripId` שמור ב-localStorage (המקרה השכיח למשתמש-חוזר, לא edge-case) ומבצעת `location.replace("index.html?tripId="+lastTripId)`. **`location.replace()` לא עוצר את שאר הסקריפט הסינכרוני** - הבדיקה `openMenuOnLoad` עדיין רצה ופותחת את התפריט, אבל רגע אחר כך הדפדפן משלים את הניווט לכתובת החדשה - שאין בה `openMenu` בכלל - והתפריט נעלם עם כל שאר העמוד. בדיוק התצפית של נייט: "נפתח לשבריר שנייה ואז נסגר" זה לא סגירה - זה העמוד כולו מוחלף. אומת ישירות (לא הונח): שוחזר עם `lastTripId` מדומה, נמדד `menuOpen:true` מיד אחרי הקריאה, ואז `tripId=...` (בלי `openMenu`) אחרי הניווט בפועל.

**אותה מחלה בדיוק בנתיב redirect שני**, `renderTripPickerList()`'s single-trip auto-pick (משתמש בלי `lastTripId` אבל עם טיול יחיד ברשימה) - לא נבדק בדיווח המקורי, אבל אותו קוד בדיוק (`location.replace("index.html?tripId="+X)`), אז אותו תיקון.

**אין תקדים קיים בקוד** לשמירת פרמטר דרך redirect - אלה שני מקומות ה-redirect היחידים בכל הריפו, ואף אחד לא שימר שום פרמטר עד עכשיו. נוסף `redirectToTrip(tripId)` משותף (לא שכפול פעמיים) - קורא ל-`URLSearchParams` על ה-`location.search` **הנוכחי** (לא `invitationParams`, שהוא תמונת-מצב אחרת/מוקדמת יותר) ומעתיק `openMenu=1` לכתובת היעד רק אם הוא כבר שם. שני נתיבי ה-redirect עודכנו לקרוא לפונקציה המשותפת במקום לבנות את המחרוזת בעצמם.

**`console.log("[DIAG openMenu]")` הזמני (v4.76.1) הוסר** - הסיבה כבר אושרה, אין סיבה שישאר.

### 🔍 מה שנבדק בפועל
שחזור מדויק של התרחיש שנייט דיווח עליו (משתמש עם `lastTripId` שמור), צעד-אחר-צעד:
1. `?openMenu=1` בלבד, לפני התיקון: `menuOpen:true` מיד אחרי הקריאה (כצפוי), ואז לאחר הניווט - `tripId=...` **בלי** `openMenu`, `menuOpen:false`. משחזר את הבאג במדויק.
2. **אחרי התיקון**: אותו תרחיש בדיוק - הפעם כתובת ה-redirect בפועל היא `?tripId=...&openMenu=1` (שניהם). הפעלת `handleAuthStateChanged` על העמוד השני (מדמה auth אמיתי, שלא זמין בסביבה המקומית) - התפריט נפתח, ו**נשאר פתוח** אחרי המתנה נוספת (אין redirect נוסף שיסגור אותו - `tripId` כבר קיים, `resolveTripSelection` לא נקראת שוב).
3. נתיב ה-redirect השני (טיול יחיד, בלי `lastTripId`) - נבדק בנפרד עם `renderTripPickerList()` ישיר - גם שם הכתובת השמורה כוללת `openMenu=1`.
4. רגרסיה: redirect בלי `openMenu` מלכתחילה - הכתובת החדשה **לא** מקבלת `openMenu=1` שלא ביקש אותו - `redirectToTrip` לא מוסיף את זה בטעות.

`node --check` על הסקריפט הקלאסי. `scripts/i18n-audit.js` - 0 ממצאים חדשים.

**תיקון באג → patch bump.**

## v4.76.1 — דיאגנוסטיקה זמנית: באג "חזרה לתפריט" לא פותח את התפריט

### 🎯 מה השתנה
**זמני, יוסר ב-commit נפרד ברגע שהסיבה האמיתית מאושרת.** נמצא בפרודקשן: לחיצה על "חזרה לתפריט" (v4.76.0) חוזרת ל-`index.html?openMenu=1` אבל התפריט לא נפתח בפועל.

חקירה מקומית (לפני התוספת הזו) שללה שלוש מארבע ההשערות בוודאות, עם ראיות ישירות ולא רק קריאת קוד: (1) ה-URL בפועל אחרי לחיצה אמיתית מכיל `?openMenu=1` - אושר. (2)+(3) אין `return` מוקדם בשום מקום בין תחילת `handleAuthStateChanged` לבין הבדיקה החדשה, וקריאה ידנית ל-`handleAuthStateChanged({uid:...})` על אותו עמוד בדיוק אחרי ניווט אמיתי **פתחה את התפריט בהצלחה** - הקוד עצמו תקין ונגיש. **נמצא גם מסלול-קריאה שני, לא-קשור**: `window.armAuthGate` (`auth-gate.js`) - רשת-ביטחון ל-spinner-תקוע (12 שניות, ידוע ב-iOS), נבדק ונשלל כגורם (רץ רק אחרי טיים-אאוט + לחיצת "נסה שוב" ידנית, לא במסלול הרגיל).

מה שלא ניתן לאמת מקומית: האם `auth.onAuthStateChanged` בפועל יורה עם משתמש אמיתי בסביבת הפרודקשן בזרימה הזו - דורש session אמיתי + רשת ל-Firebase, לא זמין בסביבת הבדיקה המקומית.

**נוסף `console.log("[DIAG openMenu]", {...})`** בראש `handleAuthStateChanged` - מדפיס `hasUser`/`uid`/`openMenuOnLoad`/`hasAutoOpenedMenu`/`urlNow` בכל קריאה, כדי לבדוק בדפדפן אמיתי מה באמת קורה.

**תיקון בקוד המוצר → patch bump** (גם עבור שינוי דיאגנוסטי זמני - `index.html` הוא אחד מ-6 העמודים, מקבל bump בכל שינוי per CLAUDE.md, גם כזה שיוסר בקרוב).

## v4.76.0 — "מה זה GitTrip?" בתפריט ההמבורגר, עם "חזרה לתפריט"

### 🎯 מה השתנה
פריט חדש בתפריט ההמבורגר (`index.html`), `#showcaseLink`, בין `hamburgerShareBtn` ל-`logoutLink` - מוביל ל-`gittrip-showcase.html?from=menu`. בניגוד ל-3 הפריטים שלידו (`tripsLink`/`newTripLink`/`hamburgerShareBtn`), **לא** מוסתר כש-`!user` - אין לו תלות בנתוני משתמש, אותו נימוק כמו `darkModeToggle` שגם הוא תמיד גלוי. **אותו טאב, לא `target="_blank"`** - בניגוד ל-`authGateShowcaseLink` הקיים (ששומר על טאב-ההזמנה פתוח מאחור בכוונה) - כאן אין טאב-הזמנה לשמר, הזרימה היא ניווט והלוך-חזור באותו טאב.

**קישור חדש, לא שכפול של מנגנון קיים.** `gittrip-showcase.html` כבר תמך במקרה דומה - קישור "מה זה GitTrip?" קיים מה-`authGate` (לפני התחברות), שכבר מחליף את כפתור ה-CTA ל"חזרה לטיול" לפי פרמטר `tripId`. **בכוונה לא עורבב עם אותו פרמטר** - `tripId` כבר אומר "הגעתי מ-authGate, הראה חזרה-לטיול-הזה"; overload שלו למשמעות שנייה ("הגעתי מהתפריט, הראה חזרה-לתפריט") היה סותר. נוסף ענף עצמאי חדש בתוך אותו IIFE (`initCtaFromUrl`), על `?from=menu` - דגל `ctaFromMenu` נפרד, נבדק *לפני* בדיקת ה-`tripId` הקיימת (לא בתוכה). `updateCtaButton()` מקבל בדיקה מוקדמת נוספת לאותו דגל, לפני הבדיקה הקיימת - כך גם ה-re-render בהחלפת שפה (`applyLanguage`→`updateCtaButton`) עובד נכון למצב החדש בלי שכפול.

**מנגנון "חזרה לתפריט" הוא באמת פתיחה-מחדש של התפריט, לא סתם ניווט-אחורה.** אין תקדים קיים בריפו לפתיחת מודל/תפריט אוטומטית לפי URL param בטעינת עמוד (רק תקדים ל"מסך שונה" - `forceTripPicker`) - זה קוד חדש, לא שימוש-חוזר. `index.html` מקבל `openMenuOnLoad = params.get("openMenu")==="1"`, אותו דפוס בדיוק כמו `forceTripPicker` הקיים לידו, ונקרא בפועל בתוך `handleAuthStateChanged` (פעם אחת בלבד, מגן על ידי `hasAutoOpenedMenu`) - ברגע שיש `user` מאומת, קורא ל-`openHamburgerMenu()` הקיים (לא שכפול). ה-guard חיוני: `handleAuthStateChanged` יכול לירות יותר מפעם אחת בסשן אמיתי (מעבר התחברות/יציאה), ובלעדיו קריאה חוזרת הייתה פותחת את התפריט שוב בכוח אחרי שהמשתמש כבר סגר אותו.

### 🔍 מה שנבדק בפועל
בדפדפן, ללא צורך בהתחברות Google אמיתית (הפרמטר עצמו לא דורש רשת/Firebase):
1. פתיחת התפריט בפועל - `#showcaseLink` מוצג נכון, `href="gittrip-showcase.html?from=menu"`, בלי `target`.
2. `gittrip-showcase.html?from=menu` - כפתור "⬅️ חזרה לתפריט", `href="index.html?openMenu=1"`, בלי `target`/`rel`. גם ב-עברית וגם באנגלית (`setLang('en')` בפועל, לא רק קריאת קוד).
3. `gittrip-showcase.html?tripId=...&d=Rome` (המסלול הישן, מ-authGate) - **לא נשבר**, עדיין "✈️ חזרה לטיול לRome" בדיוק כמו לפני.
4. `gittrip-showcase.html` בלי שום param - **לא נשבר**, עדיין "✈️ לפתיחת GitTrip" ברירת המחדל, `target="_blank"`.
5. מנגנון הפתיחה-מחדש - הודגם ישירות: `window.handleAuthStateChanged({uid:...})` מדומה (בלי Firebase אמיתי) עם `?openMenu=1` בכתובת - התפריט (שהיה סגור) נפתח בפועל, `openHamburgerMenu()` נקרא. קריאה שנייה לאחר סגירה ידנית - **לא** נפתח שוב (ה-guard עובד).

`node --check` על הסקריפט הקלאסי של `index.html` ושני בלוקי ה-script המוטבעים ב-`gittrip-showcase.html`. `scripts/i18n-audit.js` - 0 ממצאים חדשים.

**תכונה חדשה → minor bump.**

## v4.75.1 — legs.js: isRealVisit() + countryContinent.js (הכנה לאשכול הגיימיזה, המשך)

### 🎯 מה השתנה
שני חלקים עצמאיים, שניהם הכנה בלבד לאשכול מפת-ההישגים - שום דבר עדיין לא קורא לאף אחד מהם.

**`legs.js`'s `isRealVisit(leg)`** - קריטריון "ביקור אמיתי" (לפחות לילה אחד, לא עצירת-ביניים), בנוי על `window.dateKeyInZone` (`datetime.js`) - השוואת מפתחות-יום קלנדריים בשעון הרגל עצמה, **לא** `endAt-startAt >= 24h`. בדיקה גולמית-בשעות הייתה מחזירה "אין ביקור" לשהות לילה אמיתית של כ-10 שעות (הגעה 22:00, יציאה 08:00 למחרת) - בדיוק המקרה שביקשת לוודא. זו בדיוק המחלה שכבר עלתה בפרויקט הזה 3 פעמים (`tripDayDates`'s `getPrimaryLeg`/`getLastLeg`, `zonedParts`, `daysBetweenKeys`) - ההערה בקוד מתעדת את זה במפורש כדי שזה לא יומצא-מחדש בפעם הרביעית.

**`countryContinent.js`** (קובץ חדש) - טבלת מדינה→יבשת, 248 רשומות. **מקור סמכותי אחד, לא הזיכרון**: הטבלה הרשמית של UN Statistics Division (M49 standard, https://unstats.un.org/unsd/methodology/m49/overview/) - נבחרה כי היא כבר כוללת ISO alpha-2 באותה טבלה עם שיוך היבשת, בלי צורך בצירוף alpha-3↔alpha-2. **נגזרה בפועל, לא הוקלדה/סוכמה**: פרסור דטרמיניסטי של ה-HTML הגולמי (הטאב האנגלי, `id="ENG_Overview"`) דרך סקריפט חד-פעמי - לא הקלדה ידנית ולא סיכום ע"י מודל-שפה, ששניהם היו מסתכנים בדיוק בסוג הטעות ששאלת עליה (סיווג שגוי ששקט ונראה תקין). אומת: 248 רשומות, אפס קודים כפולים.

**שלושת מקרי-הגבול, נבדקו בפועל מול הטבלה הרשמית** (לא הונחו): רוסיה (RU)→Europe (Eastern Europe, למרות שרוב שטחה גיאוגרפית באסיה), קזחסטן (KZ)→Asia (Central Asia), טורקיה/Türkiye (TR)→Asia (Western Asia, למרות הקשר הנפוץ לאירופה/מו"מ הצטרפות ל-EU). שלושתם מתועדים בהערת-הקוד עם ההסבר המדויק, כדי שאם מישהו יתפלא "למה רוסיה מסווגת ככה" - התשובה כבר שם.

**ממצא נלווה, גם הוא מאומת מול המקור הגולמי**: `NR` רשום כ-"Naoero" בטבלה הרשמית, לא "Nauru" - השם הרשמי הנוכחי של UN לאותה מדינה, לא טעות פרסור (grep ישיר על ה-HTML הגולמי מאשר). מתועד בהערה, כדי שלא ייראה כטעות-הקלדה בעתיד.

**Antarctica (AQ)** ממופה ל-`"Antarctica"` עצמה - היא קוד-אזור עליון ב-M49 (010), עמיתה של Africa/Americas/Asia/Europe/Oceania בהיררכיה, לא מקוננת תחת אחת מהן.

**`countryContinent.js` עדיין לא נטען באף עמוד** - שונה מ-v4.75.0's תוספת ל-`legGeocode.js` (קובץ שכבר טעון בכל מקום, רק תוספת לא-נקראת עדיין) - זה קובץ חדש לגמרי, לא מחובר לשום `<script>` tag. איזה עמוד/מסך ייטען אותו יוחלט כשמסך-ההישגים עצמו נבנה.

### 🔍 מה שנבדק בפועל
`isRealVisit`: 6 תרחישים בדפדפן מול `window.dateKeyInZone` האמיתי - לילה קצר שחוצה חצות (✓ true, אחרי שתיקנתי טעות בנתוני-הבדיקה שלי עצמי - חישוב UTC↔Rome שגוי בסבב הראשון, לא באג בפונקציה), עצירת-יום-בודד (✓ false), שהות רב-לילות (✓ true), timezone חסר - נופל לשעון המכשיר בלי לזרוק (✓), רגל חסרת-תאריכים ו-`null` - `false` בלי חריגה (✓ שניהם).

`countryContinent.js`: נטען ב-Node עם `global.window={}` מדומה, 248 מפתחות, אפס כפולים, 4 בדיקות-רוחב (RU/KZ/TR/NR) תואמות בדיוק לטבלה הרשמית.

`node --check` על שני הקבצים.

**הכנה בלבד, אפס שינוי-התנהגות למשתמש → patch bump.**

## v4.75.0 — legGeocode.js: שמירת cityName/region (הכנה לאשכול הגיימיפיקציה)

### 🎯 מה השתנה
חקירה לקראת מפת-הישגים (choropleth) מצאה ש-`legGeocode.js` (מ-v4.74.1) משליך שם-עיר נקי ואזור/מחוז שגם Open-Meteo וגם Nominatim כבר מחזירים בתגובה - נשמרים היום רק `lat`/`lon`/`timezone`/`countryCode`. **commit קטן ועצמאי**, לא תלוי באשכול הגיימיפיקציה עצמו ולא חוסם אותו - רק מפסיק לזרוק מידע שכבר יש בהישג-יד.

**נוסף**: `cityName`/`region` לאובייקט שמוחזר מ-`window.geocodeLegDestination()` - מ-Open-Meteo: `match.name`/`match.admin1`; מ-Nominatim (נתיב הגיבוי): `address.city||address.town||address.village`/`address.state` (המפתח בכתובת של Nominatim משתנה לפי סוג היישוב, ולכן שרשרת ה-fallback). `legBuilder.js`'s `buildLegs()` עודכן בהתאם להעתיק את שני השדות אל אובייקט הרגל עצמו - **בלי זה השדות החדשים היו מחושבים ומיד נזרקים**, לא ממש נשמרים ל-RTDB. **קדימה-בלבד**: רגל שנוצרה לפני ה-commit הזה פשוט לא נושאת את השדה, אותו דפוס-נפילה בדיוק כמו `countryCode` לרגליים שקדמו ל-v4.23.0.

**במפורש לא נעשה**: אין מירור ברמת-טיול (כמו ש-`destination`/`lat`/`lon`/`countryCode` ממוררים מ-`legs[0]` היום) - המירור הקיים קיים כדי לא לשבור קוד ישן שקורא משתני-trip ישירות; `cityName`/`region` הם שדות חדשים לגמרי בלי צרכן קיים כזה, אז אין סיבה אמיתית למרר אותם. `legs[n].name` (הטקסט החופשי שהמשתמש הקליד) **לא נגע** - השדות החדשים הם תוספת, לא תחליף.

### 🔍 מה שנבדק בפועל
מול ה-API-ים החיים, דרך `window.geocodeLegDestination` בדפדפן:
1. "Rome" (התאמה ישירה ב-Open-Meteo) - `cityName:"Rome"`, `region:"Lazio"`.
2. "שטוקהולם" (עברית, נופל ל-Nominatim) - `cityName:"סטוקהולם"`, `region:null` (Nominatim לא תייג state לתוצאה הזו - לא באג).
3. "גוטנבורג" (איות לא-סטנדרטי, נכשל בשני השירותים) - `cityName:null`, `region:null` - נופל בשקט, לא שובר.

`node --check` על `legGeocode.js`+`legBuilder.js`. אין consumer קיים שנשבר - שני קוראי `geocodeLegDestination` (wizard.html, index.html) מחלצים שדות ספציפיים בשם, לא סופרים/מבצעים iterate על מפתחות.

**שינוי סכמה (שדות חדשים) → minor bump.**

## v4.74.2 — authDomain: מעבר ל-gittrip.app (6 עמודים)

### 🎯 מה השתנה
`authDomain` עודכן בכל 6 ה-`firebaseConfig` הנפרדים (אין קובץ קונפיג משותף - כל עמוד מחזיק עותק עצמאי) מ-`"mykonos-list.firebaseapp.com"` ל-`"gittrip.app"`. זה השלב האחרון של מעבר Firebase Hosting + חיבור דומיין מותאם-אישית - המטרה המקורית: לתקן באג Safari הנובע מ-`authDomain` שונה מהמארח בפועל.

**וידוא לפני השינוי, לא הנחה**:
1. `gittrip.app` מאושר "Connected" בקונסולת Firebase (בדיקת המשתמש).
2. `gittrip.app` כבר ברשימת Authentication → Authorized domains - **אומת ישירות** מול ה-endpoint הציבורי של Identity Toolkit (`getProjectConfig`, אותו קריאה שה-SDK של הלקוח כבר עושה בכל טעינת עמוד, עם אותו מפתח API הציבורי שכבר משוטח בקוד) - לא הונח מהתיעוד. הרשימה בפועל: `localhost`, `mykonos-list.firebaseapp.com`, `mykonos-list.web.app`, `nelevy1-sketch.github.io`, `gittrip.app`.

**וידוא אחרי השינוי, לפני commit**: `grep` על כל 6 הקבצים - בדיוק 6 הופעות של `gittrip.app`, אפס הופעות שנשארו של הערך הישן. `node --check` על שני הסקריפטים הקלאסיים/מודולריים הרלוונטיים (index.html קלאסי, וכל 5 העמודים המודולריים) - תקין בכולם.

**סיכון קריטי שהובהר מראש**: זה commit שמשפיע בו-זמנית על GitHub Pages וגם Firebase Hosting - שניהם מגישים בדיוק את אותו קוד מהריפו הזה. ברגע שנדחף, גם המשתמשים החיים היום ב-GitHub Pages מקבלים את השינוי, לא רק מי שכבר עבר ל-gittrip.app.

### 🔍 מה שנבדק בפועל
בדיקת ה-authDomain/DNS/TLS/Authorized-domains בוצעה לפני ה-commit (לא נבדקה כאן ההתחברות בפועל - זו בדיקה שדורשת חשבון Google אמיתי, מבוצעת ע"י המשתמש עצמו מיד אחרי push, בטאב אינקוגניטו טרי, גם ל-gittrip.app וגם לוודא ש-github.io הישן לא נשבר).

**תיקון באג (Safari authDomain mismatch) → patch bump.**

## v4.74.1 — geocoding יעד: fallback ל-Nominatim כש-Open-Meteo ריק (עברית)

### 🎯 מה השתנה
Open-Meteo (המקור היחיד עד כה ל-`lat`/`lon`/`timezone`/`countryCode` של יעד-רגל, בוויזרד ובפאנל האדמין) מחזיר ריק לחלוטין לרוב החיפושים בעברית ("שטוקהולם", "קליפורניה") - **אומת ישירות מול ה-API החי**: פרמטר ה-`language` שהתיעוד הקודם (הערה ב-`geocode.js`) הניח שמשפיע על ההתאמה עצמה **לא משפיע בכלל** - אותה שאילתה עברית עם `language=he` ועם `language=en` חוזרת ריקה באותה מידה. זו מגבלה מבנית של Open-Meteo נגד טקסט לא-לטיני, לא פרמטר שגוי.

**נוסף `legGeocode.js`** (קובץ חדש, נטען ב-wizard.html+index.html בלבד) - פונקציה משותפת אחת, `window.geocodeLegDestination(name, language)`: מנסה Open-Meteo קודם (המסלול המהיר, ללא שינוי להתנהגות קיימת), ורק כשריק - Nominatim (אותו שירות ש-`geocode.js` כבר משתמש בו למקומות בודדים, אך **לא** דרך `geocode.js` עצמו - נדרש `addressdetails=1` כדי לקבל `country_code` בכלל, ש-`geocodeQuery` הקיים לא מבקש). `countryCode` מומר ל-אותיות גדולות בכניסה היחידה הזו (Nominatim מחזיר קטן, כל שאר הקוד - כולל שער ה-VAT היווני ב-shopping.html, `countryCode==="GR"` - מניח גדול). `timezone` נשאר `null` ברגל שניצלה ע"י Nominatim בכוונה (השירות הזה לא מחזיר timezone באף תצורה) - נסמך על שרשרת ה-fallback הקיימת (`docs/schema-legs.md` §4: רגל→טיול→שעון מכשיר), לא תלות חדשה.

**Throttle גלובלי אחד** (לא מוכפל per-call) - קריאות Nominatim מצטרפות לתור-הבטחות משותף אחד עם מרווח ~1.1 שנייה בין קריאה לקריאה, כדי לא להפר את מדיניות-הקצב של Nominatim (~1/שנייה) כשכמה רגליים נכשלות יחד ב-`Promise.all` הקיים (וויזרד + פאנל אדמין מרובה-רגליים).

הוסרו שלושת המימושים הכפולים (`wizard.html`'s `geocodeDestination`, `index.html`'s `geocodeAdminDestinationName` + הבלוק ה-inline של רגל-בודדת) - כולם קוראים עכשיו ל-`window.geocodeLegDestination`. תיקון נלווה: תנאי ה-`console.warn` על כשלון geocoding עבר מ-`!loc.timezone` ל-`loc.lat == null` בשני הקבצים - רגל שניצלה ע"י Nominatim יש לה `lat`/`lon` תקינים אך `timezone` ריק כדין, וזו כבר לא "כשלון".

### 🔍 מה שנבדק בפועל
מול ה-API-ים החיים (לא מדומה), דרך `window.geocodeLegDestination` ישירות בדפדפן:
1. "שטוקהולם"/"קליפורניה" - Open-Meteo ריק, Nominatim מוצא, `countryCode` `"SE"`/`"US"` (גדול, לא `"se"`/`"us"`).
2. "אתונה" - `countryCode:"GR"` (גדול) - שער ה-VAT היווני (`shopping.html`, לא נגע) ימשיך לעבוד נכון.
3. "Rome"/"אתונה"/"פירנצה" - נמצאים ישירות ב-Open-Meteo, `timezone` מגיע מלא כרגיל - אין שינוי התנהגות למסלול המהיר.
4. 2-3 יעדים שנכשלים ב-Open-Meteo יחד, דרך `Promise.all` - הקריאות ל-Nominatim מסתדרות בתור (נמדד: ~1.25 שניות לשתי קריאות, תואם ~1.1 שניות המרווח המחויב, לא כמה מאות ms שהיו בלי throttle).
5. "גוטנבורג" (איות עברי לא-סטנדרטי, מהדיווח המקורי) - עדיין ריק בשני השירותים, מתדרדר ל-`{lat:null,lon:null,timezone:null,countryCode:null}` בלי חריגה - רשת הביטחון הקיימת (`hasValidCoords`, "לא זמין" במזג אוויר) עדיין הכרחית, לא כל geocoding מצליח עכשיו.

`node --check` על שני הקבצים (הסקריפט המודולרי של wizard.html, הסקריפט הקלאסי של index.html) ועל `legGeocode.js` עצמו. `scripts/i18n-audit.js` - 0 ממצאים חדשים (3 הממצאים הקיימים ב-shopping.html לא קשורים לשינוי הזה).

**תיקון באג → patch bump.**

## v4.74.0 — index.html: עריכת תאריכי-קצה בטיול מרובה-רגליים

### 🎯 מה השתנה
`#adminFlightInput`/`#adminEndInput` (תחילת הרגל הראשונה / סיום הרגל האחרונה) היו חסומים לכל טיול עם ≥2 רגליים, עם רמז "בקרוב". **חקירה חשפה שההנחה המקורית הייתה שגויה**: זו לא הייתה תכונה חצי-בנויה - זה תיקון-חירום מכוון (`3edb459`, v4.20.1) לבאג ניתוק-נתונים אמיתי בפרודקשן, ותשתית עריכת-תאריכים לרגליים **כבר קיימת ונבדקה** לרגליים הפנימיות (`buildLegs()`/`deriveTripFields()`, v4.22.0) - כולל ולידציה (`startAt<endAt` לכל רגל, מספיקה כי הגבולות משותפים בבנייה).

**מה נעשה**: הוסרה החסימה על שני השדות בלבד. הם זורמים עכשיו **דרך אותו pipeline קיים בדיוק** (`buildLegs`/`deriveTripFields`) - **לא** נכתבים ישירות ל-`startAt`/`endAt` (זו הייתה בדיוק הטעות שגרמה לבאג המקורי). הוסר גם ה-`<p>` וה-2 מחרוזות i18n של הרמז, שהפכו מיותרים.

**הגנה קלה שנוספה** (לא מיגרציה - הוחלט במפורש לא לבנות אחת, מחוץ להיקף): לפני שמירת שינוי לתאריך הקצה הכולל, אם יש `itinerary/days` קיימים בטיול (בדיקת-קיום פשוטה, לא ניתוח-פגיעה מדויק), `customConfirm` מזהיר שפעילויות עלולות "להיעלם מהתצוגה" (נשארות ב-DB, רק לא משויכות ליום הנכון). ביטול עוצר את השמירה לגמרי.

**במפורש לא טופל** (סוכם מראש): הנחת-הסדר-הכרונולוגי שכמעט כל צרכן תאריך-רגל בקוד סומך עליה (17+ מקומות שנמפו) - תלוי-קיים באפליקציה כולה, לא ספציפי לתכונה הזו.

### 🔍 מה שנבדק בפועל
נבדק בדפדפן עם hook זמני (הוסר ואומת שהוסר) על טיול דו-רגלי מדומה (אתונה→רומא):
1. עריכת תאריך קצה + שמירה - `safeWrite("saveFlightBtn")` נקרא בפועל, נכשל ב-`PERMISSION_DENIED` אמיתי (מוכיח זרימה אמיתית דרך ה-pipeline, לא עקיפה).
2. תאריך לא-תקין (transition שהופך רגל לסיום-לפני-התחלה) - נחסם ע"י הולידציה הקיימת, אותה הודעה בדיוק כמו קודם.
3. טיול עם activities קיימות + שינוי תאריך - האזהרה מוצגת עם הטקסט הנכון; ביטול מונע את ה-`safeWrite` לגמרי.
4. טיול בלי activities - אין אזהרה, השמירה זורמת ישר.

`node --check`+`i18n-audit` נקיים.

v4.73.2 -> v4.74.0 (minor - תכונה חדשה, אין שינוי סכמה).

## v4.73.2 — packing.html: הסרת כפילות שם בתצוגה (userChip)

### 🎯 מה השתנה
מנגנון שונה לגמרי מ-itinerary/places, אותה תוצאה בפועל: שני `<span class="user-chip">` נפרדים, זה-לצד-זה ב-`.identity-row` - `#myName` (מציג `profile.name`) ו-`#userChip` (מציג `user.displayName||user.email`) - שניהם מתמלאים תמיד, בלי תנאי-השוואה, כלומר שני pills עם שם (זהה או שונה) גם יחד.

**תגלית מהתחקור**: הערת-קוד קיימת (v4.30.1) מסבירה ש-`#myName` הוא בעצם ה"תיקון הנכון" - ירש את מקומו של `<select>` שבור, והוחלף ב-span שמציג `profile.name` ישירות. `#userChip` הישן (התצוגה הגולמית של Google) נשאר בטעות ולא הוסר כשהתיקון הזה נכנס - לא כפילות מכוונת.

**התיקון**: הוסר `#myName` (span + populate line + ה-`<span>` ב-HTML), ו-`#userChip` היחיד שנשאר משתמש עכשיו ב-`profile?.name || user?.displayName || user?.email || ""` - עדיפות ל-profile.name (השם שנשמר בפועל), עם נפילה ל-Google רק בחלון הצר לפני שה-profile נטען בכלל.

### 🔍 מה שנבדק בפועל
נבדק בדפדפן עם hook זמני (הוסר ואומת שהוסר): (1) `profile.name===displayName` - pill יחיד. (2) שם מותאם אישית - pill יחיד, עם השם הנכון. (3) אומת ש-`#myName` הוסר לחלוטין מה-DOM (`document.getElementById('myName')` מחזיר `null`). צילום מסך בהקשר העמוד המלא מאשר - pill יחיד, ללא רווח ריק במקום השני שהוסר.

`node --check`+`i18n-audit` נקיים.

v4.73.1 -> v4.73.2 (patch - תיקון באג, אין שינוי סכימה).

**זהו commit אחרון לממצא הכפילות - כל 3 העמודים (itinerary/places/packing) מתנהגים אחיד עכשיו: pill יחיד, `profile.name` בלבד.**

## v4.73.1 — itinerary.html/places.html: הסרת כפילות שם בתצוגה (userChip)

### 🎯 מה השתנה
`userChip` הציג `` `${profile.name} · ${user.displayName||''}` `` - **תמיד** שני הערכים ביחד, בלי שום תנאי-השוואה - כולל כש-`profile.name===user.displayName` (למשל אחרי כפתור "איפוס לשם מ-Google" שנוסף ב-v4.73.0, או בטיול חדש שבו המשתמש לא ערך את השם). דווח ע"י המשתמש כ"שם כפול" - אומת בקוד ובדפדפן: הכפילות אכן קורית בשני המצבים (שם זהה/שונה), לא רק כשהם שונים.

שונה ל-`profile.name||''` בלבד - זה השם היחיד הרלוונטי לתצוגה (השם ששמור בפועל בטיול). `displayName` נשאר רלוונטי רק לפיצ'ר האיפוס עצמו (v4.73.0), לא לתצוגה הרגילה. ה-fallback הזמני לפני שה-profile נטען (`handleAuthStateChanged`, מציג `displayName||email`) לא נגע בו - זה לא הבאג שדווח (ערך יחיד זמני, לא כפול).

### 🔍 מה שנבדק בפועל
נבדק בדפדפן עם hook זמני (הוסר ואומת שהוסר) בשני הקבצים בנפרד: (1) `profile.name===displayName` - מוצג פעם אחת בלבד. (2) שם מותאם אישית - מוצג פעם אחת בלבד, בלי `· `. צילומי מסך בהקשר העמוד המלא מאשרים.

`node --check`+`i18n-audit` נקיים.

v4.73.0 -> v4.73.1 (patch - תיקון באג, אין שינוי סכימה).

## v4.73.0 — index.html: כפתור "איפוס לשם מ-Google" ב-profileModal

### 🎯 מה השתנה
נוסף קישור-טקסט (לא אייקון בתוך השדה) מתחת לשדה "שם תצוגה" ב-profileModal - "איפוס לשם מ-Google" - מציע למלא מחדש את השם מ-`currentUser.displayName`.

**מוצג רק כשיש מה לאפס**: `currentUser.displayName` קיים **וגם** שונה מהערך הנוכחי בשדה. אין לוגיקת השוואה קיימת בקובץ להעתיק - זה תנאי חדש.

**לחיצה לא שומרת אוטומטית** - רק ממלאת מחדש את השדה מ-`currentUser.displayName`; המשתמש עדיין לוחץ "שמירה" בעצמו, כמו כל שינוי ידני אחר.

**עדכון חי תוך כדי הקלדה**: נבדק - `currentUser` נקבע פעם אחת בלבד (ב-handler של auth-state-changed), לעולם לא משתנה מעריכת השדה - כך שהשוואה מול `currentUser.displayName` על כל הקשה (`input` listener על `profileNameInput`) בטוחה, לא רק בפתיחת המודל.

**עיצוב**: קישור-טקסט פשוט (`.text-link-btn`, underline, `var(--primary)`) ולא chip מלא כמו `.btn-sm-action` - פעולה משנית נדירה, לא ראוי למשקל ויזואלי של פעולה ראשית.

### 🔍 מה שנבדק בפועל
כל 4 התרחישים שהתבקשו, בדפדפן עם `currentUser`/`currentProfile` מדומים:
1. שם שונה מ-Google - הכפתור מופיע, עם הטקסט הנכון.
2. שם זהה ל-Google - הכפתור מוסתר.
3. לחיצה - השדה מתמלא מ-`currentUser.displayName`, הכפתור נעלם (כי הערכים תואמים עכשיו) - **ו-`currentProfile.name` (הערך השמור) נשאר ללא שינוי**, מוודא שאין שמירה אוטומטית.
4. הקלדה תוך כדי (`input` event אמיתי, לא רק בפתיחה) - הכפתור מופיע כשהשדה שונה מ-Google, נעלם כשחוזרים לערך זהה - בזמן אמת.

צילום מסך בהקשר העמוד המלא מאשר מיקום/עיצוב תקינים. `node scripts/i18n-audit.js` - אין ממצאים חדשים (הטקסט מכוסה דרך אותו מנגנון `dashboardText`/`applyTripLanguage()` שכל שאר הטקסטים במודל הזה כבר משתמשים בו).

v4.72.28 -> v4.73.0 (minor - תכונה חדשה).

## v4.72.28 — ניגודיות `--muted` בכל 6 העמודים (אשכול 5, ממצא 3, סגירה)

### 🎯 מה השתנה
`--muted`/`--text-muted`/`--gray` (4 הגדרות מקומיות: `shared.css` + שכפולים ב-index/shopping/wizard) היו נכשלים ב-4.5:1 (WCAG AA טקסט רגיל) במצב בהיר, בכל 4 ערכות הנושא, נגד כל הרקעים האמיתיים שנבדקו - כשל שיטתי (כל המקומות נכשלו, לא מקרים בודדים), לא פספוס נקודתי. מצב כהה כבר היה תקין בכל מקום (5.8-7.2:1) ולא נגע.

**נבחר תיקון גלובלי של הטוקן (4 מקומות), לא נקודתי בכ-89 אתרי-שימוש** - כל השימושים חולקים את אותה סיבת-כשל, ורובם (כ-80/89) טקסט קריא-חובה (הערות פריט, metadata, ניווט, כתובות מייל).

**הערך המינימלי שעובר, לא הכהיה מוגזמת** - חושב במדויק (נוסחת luminance מלאה, לא ניחוש) לכל אחד מה-4, נגד הרקע האמיתי **הקשה ביותר** שבו הוא בפועל מופיע:

| מיקום | לפני | אחרי | נגד (הכי קשה) | יחס לפני | יחס אחרי |
|---|---|---|---|---|---|
| `shared.css --muted` | `#6f7f8c` | `#57636E` | `--surface-2` (reserve) | 3.05:1 | 4.55:1 |
| `index.html --text-muted` | `#687889` | `#606E7E` | `--bg-color` | 3.94:1 | 4.54:1 |
| `shopping.html --text-muted` | `#5c6b73` | `#5B6971` | `--tag-bg` (`.item-note`) | 4.45:1 | 4.57:1 |
| `wizard.html --gray` | `#687889` | `#606E7E` | (זהה ל-index) | 3.94:1 | 4.54:1 |

כל ערך אומת גם נגד **כל** שאר הרקעים/ערכות-הנושא הרלוונטיים (12 שילובים ל-shared.css לבד) - לא רק המקרה הקשה ביותר - כולם עוברים 4.5:1+, עם buffer קטן (לא בדיוק 4.50, כדי לא ליפול מתחת בגלל עיגול).

### 🔍 מה שנבדק בפועל (לא רק המספר)
**בדיקה חזותית לפני/אחרי בדפדפן** (לא רק חישוב) - הוחלף הטוקן החי (`documentElement.style.setProperty`) על 3 עמודים שונים (itinerary.html עבור shared.css, shopping.html עבור `.item-note`, index.html עבור הטוקן המקומי שלו), צילום מסך בכל מצב. אושר: הטקסט קריא יותר במובהק, **ועדיין נראה בבירור פחות בולט מ-`--text` הרגיל** - לא הושווה אליו ויזואלית, נמנע מהסיכון שסומן מראש (הכהיה-יתר שמטשטשת את ההיררכיה).

`node --check`+`i18n-audit` נקיים על shopping.html/wizard.html.

v4.72.27 -> v4.72.28 (patch - תיקון נגישות, אין שינוי סכימה).

**זהו הממצא האחרון באשכול 5 - כל 3 הממצאים (תוויות, כפתורי סגירה, ניגודיות) סגורים.**

## v4.72.27 — shopping.html: אזור מגע וaria-label לכפתורי סגירה (אשכול 5, ממצא 2)

### 🎯 מה השתנה
9 כפתורי `.close-modal` (✕) - לא 8 כפי שהדוח ציין, נמצא תשיעי (`closeExportPreviewModalBtn`) - היו ללא `width`/`height`/`padding` בכלל (אזור מגע בפועל ~20-32px, מתחת לרף המומלץ 44px), וללא `aria-label` ב-markup הסטטי.

**`aria-label` - הבהרה מהחקירה**: כבר קיים JS שמוסיף `aria-label` לכל `.close-modal` בזמן ריצה (2 מקומות בקוד) - כך שמשתמשי קורא-מסך בפועל כבר קיבלו תווית. התוספת כאן היא static fallback בלבד, הגנה-בעומק למקרה שה-JS לא ירוץ (או לכלי סריקה שלא מריצים JS).

**אזור המגע**: נבחר `min-width:44px;min-height:44px;display:flex;align-items:center;justify-content:center` (לא רק padding) - נותן מובטח מדויק בלי תלות במטריקות הפונט, וממרכז את ה-✕ בבטחה.

### 🔍 מה שנבדק בפועל
נמדד בפועל (`getBoundingClientRect`) על `closeModalBtn` - 44x44px מדויק. **תשומת הלב המיוחדת שסומנה מראש**: 3 הכפתורים ב-header תלת-פריטי (`closeLangModalBtn`/`closeExportPreviewModalBtn`/`closeSettingsModalBtn`) נבדקו בנפרד ב-375px - לא הונח שמה שעבד ל-6 האחרים עובד גם שם. בשלושתם: מדידת קואורדינטות (`getBoundingClientRect`) אישרה רווח נקי בין כפתור-הסגירה, הכותרת, וכפתור-החזרה - ללא חפיפה, גם במקרה הצפוף ביותר (`closeExportPreviewModalBtn`, עם "חזור לשפות" - הטקסט הארוך ביותר מבין השלושה). צילומי מסך ב-375px מאשרים ויזואלית.

`node --check`+`i18n-audit` נקיים.

v4.72.26 -> v4.72.27 (patch - תיקון נגישות, אין שינוי סכימה).

## v4.72.26 — wizard.html: קישור תוויות טופס (אשכול 5, ממצא 1, commit 6/6 - סגירה)

### 🎯 מה השתנה
7 תוויות שבורות, לצד 10 תוויות שכבר היו מקושרות נכון **באותו קובץ בדיוק** - טופל בזהירות מיוחדת כדי לא לגעת בעשר התקינות.

**5 פשוטות** - נוסף `for="<id הקיים>"`: `lblDestination`→`tripDestination`, `lblTripType`→`tripType`, `lblTripCharacter`→`tripCharacter`, `lblParticipants`→`participantName` (כולל השדות הכי קריטיים שהוזכרו בדוח המקורי - יעד, סוג טיול).

**2 מקרי-קבוצה** (כמו packing/shopping, אבל בהיקף שונה):
- `lblVibe` - מתייג רשת של 4 כפתורי "vibe" (עירוני/חוף/טבע/מנוחה) - הוחלף ל-`<span>` + `role="group" aria-labelledby"` על `#vibeGrid` (מיכל קיים, לא נוסף wrapper חדש).
- `lblModules` - שונה מהמקרים הקודמים: לא מתייג קבוצת-שדות קטנה אלא **כל שלב 3 של הוויזרד** (5 feature-cards, שלכל אחד כבר יש label+checkbox תקינים משלו). נעשה שימוש באותה תבנית בכל זאת (`role="group" aria-labelledby"` על `.form-step[data-step="3"]` הקיים) - זה לא סותר את התוויות העצמאיות של כל checkbox, רק מוסיף הקשר של כותרת-השלב, כמו `<fieldset><legend>` היה עושה.

**באג שנתפס ותוקן תוך כדי**: בקובץ הזה יש כלל CSS `label{display:block;font-weight:500;margin-bottom:8px;font-size:.95rem}` (tag-selector גורף, לא ב-`.form-group` בלבד) - הורחב ל-`label, .field-label-text{...}` לפני החלפת התג בשני מקרי ה-`<span>`, כדי שלא לאבד עיצוב.

### 🔍 מה שנבדק בפועל
כל 6 ה-id-ים/יעדים אומתו כ-unique. **אומת במפורש שה-10 התוויות התקינות לא נפגעו** (grep נפרד לכל אחת, מציג `for` המקורי שלהן ללא שינוי). נבדק בדפדפן: כל 4 הזוגות הפשוטים + 2 קבוצות ה-`role="group"` מחזירים את הטקסט הנכון, צילומי מסך בשלב 1 (יעד/סוג/אופי/vibe) ובשלב 3 (מודולים) מאשרים אין רגרסיה חזותית - כולל אחרי מעבר לשלב 3 בפועל.

`node --check`+`i18n-audit` נקיים.

v4.72.25 -> v4.72.26 (patch - תיקון נגישות, אין שינוי סכימה).

**זהו הקובץ האחרון של ממצא 1 (תוויות) - כל 6 העמודים תוקנו.**

## v4.72.25 — index.html: קישור תוויות טופס (אשכול 5, ממצא 1, commit 5/6)

### 🎯 מה השתנה
16 תוויות (הכי הרבה מבין 6 העמודים) - פרופיל אישי, הכספת האישית (locker), ולוח הניהול (admin) - היו `<label>` בלי `for`. נוסף `for="<id הקיים>"` לכל ה-16: `profileNameLabel`, `profilePhoneLabel`, `lockerNameLabel`, `lockerFlightNumberLabel`, `lockerFlightTimeLabel`, `lockerSeatLabel`, `lockerLinkLabel`, `adminTripTitleLabel`, `adminTripTypeLabel`, `adminDestLabel`, `adminStartLabel`, `adminEndLabel`, `adminWhatsappGroupLabel`, `adminAirlineLabel`, `adminCheckinLabel`, `adminReturnSeatLayoutLabel`.

**6 תוויות-עטיפה של checkbox (`admin-checkbox-row`) - לא נגעתי בהן כלל**, כפי שסומן מראש - הן כבר תקינות (עוטפות את ה-`<input type="checkbox">` שלהן ישירות, אין להן ולא צריך `for`).

הקובץ הזה מכיל גם את פיצ'ר הברקוד הממתין (uncommitted) - כמו בכל commit קודם על הקובץ הזה, ה-commit בפועל ישתמש בטכניקת בידוד ה-git blob כדי לכלול רק את 16 השורות האלה.

### 🔍 מה שנבדק בפועל
כל 16 ה-id-ים אומתו כ-unique. נבדק בדפדפן בשלושת המודלים בנפרד (`profileModal`/`lockerModal`/`adminModal`) - כל 16 הזוגות מחזירים את התווית הנכונה (`querySelector('label[for="X"]')`), צילומי מסך בכל שלושת המודלים מאשרים אין רגרסיה חזותית. אומת גם במפורש ש-6 תוויות ה-checkbox נשארו עוטפות את השדה שלהן (`closest('label')` מחזיר true לכל השישה) - לא נפגעו.

`node scripts/i18n-audit.js` - אין ממצאים חדשים.

v4.72.24 -> v4.72.25 (patch - תיקון נגישות, אין שינוי סכימה).

## v4.72.24 — places.html: קישור תוויות טופס (אשכול 5, ממצא 1, commit 4/6)

### 🎯 מה השתנה
3 תוויות (מודל "הוספת מוזיקה" + מודל "הוספת כותרת") היו `<label>` בלי `for` - נוסף `for="<id הקיים>"` לכל השלוש: `musicLinkLabel`→`musicUrl`, `musicNameLabel`→`musicName`, `uploadCaptionLabel`→`uploadCaptionInput`.

**חשוב**: הקובץ הזה כבר היה מכיל תווית אחת מקושרת נכון (`commentInputLabel`→`commentInput`, `sr-only`) - **לא נגעתי בה בכלל**, רק בשלושת השבורות.

נבדק (ונשלל): אין כלל CSS מסוג `.field label{...}` בקובץ הזה.

### 🔍 מה שנבדק בפועל
כל 3 ה-id-ים אומתו כ-unique. נבדק בדפדפן בשני המודלים בנפרד - כל 3 הזוגות מחזירים את התווית הנכונה, צילומי מסך מאשרים אין רגרסיה חזותית.

`node --check`+`i18n-audit` נקיים.

v4.72.23 -> v4.72.24 (patch - תיקון נגישות, אין שינוי סכימה).

## v4.72.23 — itinerary.html: קישור תוויות טופס (אשכול 5, ממצא 1, commit 3/6)

### 🎯 מה השתנה
8 תוויות (מודל "הוספת פעילות" + מודל "הוספת מקום") היו `<label>` בלי `for`. כולן שדה-בודד קלאסי, בלי מקרה חריג הפעם (בניגוד ל-packing/shopping) - נוסף `for="<id הקיים>"` לכל השמונה: `timeLabel`→`activityTime`, `activityTitleLabel`→`activityTitle`, `activityNoteLabel`→`activityNote`, `placeNameLabel`→`placeName`, `categoryLabel`→`placeCategory`, `placeLegLabel`→`placeLeg`, `mapLabel`→`placeMap`, `noteLabel`→`placeNote`.

נבדק (ונשלל): אין כלל CSS מסוג `.field label{...}` בקובץ הזה, כך שאין סיכון לרגרסיה חזותית מהשינוי (בניגוד ל-shopping.html).

### 🔍 מה שנבדק בפועל
כל 8 ה-id-ים אומתו כ-unique. נבדק בדפדפן בשני המודלים בנפרד (activityOverlay/placeOverlay) - כל 8 הזוגות מחזירים את התווית הנכונה (`querySelector('label[for="X"]')`), צילומי מסך בהקשר העמוד המלא מאשרים אין רגרסיה חזותית בשני המודלים.

`node --check`+`i18n-audit` נקיים.

v4.72.22 -> v4.72.23 (patch - תיקון נגישות, אין שינוי סכימה).

## v4.72.22 — shopping.html: קישור תוויות טופס (אשכול 5, ממצא 1, commit 2/6)

### 🎯 מה השתנה
4 תוויות ב-`addModalForm` (מודל "הוספת פריט חדש") היו `<label>` בלי `for`. נוסף `for="<id הקיים>"` ל-3 (`productNameLabel`→`modalNameInput`, `unitLabel`→`modalUnitInput`, `noteLabel`→`modalNoteInput`).

**אותו מקרה חריג כמו ב-packing.html**: `baseQtyLabel` מתייג בורר-כמות מותאם (כפתור `-`, תצוגת מספר, כפתור `+`) - לא שדה יחיד. הוחלף ל-`<span>`+`role="group" aria-labelledby"` על `.modal-qty-picker`, אותה תבנית בדיוק כמו `listVisibilityLabel` ב-packing.html.

**באג שנתפס ותוקן תוך כדי**: לקובץ הזה יש כלל CSS ספציפי `.form-group label {font-size:11px;font-weight:700;color:var(--sea)}` (לא קיים ב-packing.html) - שינוי התג מ-`<label>` ל-`<span>` היה מאבד את העיצוב הזה בשקט. הורחב הסלקטור ל-`.form-group label, .form-group .field-label-text` והוסף `class="field-label-text"` ל-span - אומת חזותית שהעיצוב זהה לתוויות השכנות ("יחידה").

### 🔍 מה שנבדק בפועל
כל 4 ה-id-ים אומתו כ-unique. נבדק בדפדפן: כל שלושת הזוגות מחזירים את התווית הנכונה (`querySelector('label[for="X"]')`), הקבוצה מחזירה `aria-labelledby` נכון, **וצילום מסך אישר שאין רגרסיה חזותית** - "כמות בסיס משותפת" נראית זהה ל"יחידה" הסמוכה לה.

`node --check`+`i18n-audit` נקיים (רק ממצאי shopping.html הידועים מראש, שורות זזו ב-1 בגלל השורה שנוספה).

v4.72.21 -> v4.72.22 (patch - תיקון נגישות, אין שינוי סכימה).

## v4.72.21 — packing.html: קישור תוויות טופס (אשכול 5, ממצא 1, commit 1/6)

### 🎯 מה השתנה
9 תוויות ב-packing.html היו `<label>` בלי `for` בכלל - לא מקושרות ל-id של השדה שלהן (קורא-מסך לא מכריז את הטקסט כשה-focus עובר לשדה). נוסף `for="<id הקיים>"` תואם ל-8 מהן (`addItemCategorySelect`, `newListName`, `renameListName`, `editName`, `editVisibility`, `editListSelect`, `bulkMoveTarget`, `importCategorySelect`).

**מקרה חריג אחד** (`listVisibilityLabel`) - לא תויית-לשדה-בודד, אלא כותרת מעל **זוג כפתורים** ("הפוך הכל לפרטי"/"הפוך הכל לגלוי"). `for` לא מתאים כאן (אין שדה יחיד). הוחלף ל-`<span>` (הסרת סמנטיקת `<label>` שלא רלוונטית) + `role="group" aria-labelledby="listVisibilityLabel"` על ה-`<div>` שעוטף את שני הכפתורים - תבנית תקנית לתיוג קבוצת-כפתורים.

### 🔍 מה שנבדק בפועל
כל 8 הזוגות אומתו כ-unique (`grep -c 'id="X"'` = 1 לכל שדה, אין התנגשויות) ואומתו בדפדפן **דטרמיניסטית** (`document.querySelector('label[for="X"]')` מחזיר את התווית הנכונה לכל שדה, לא רק ויזואלית) - לא הסתפקתי בבדיקה ויזואלית של עץ הנגישות (שגילה חוסר-עקביות בתצוגת "השם הנגיש" של textbox ריק מול placeholder, כנראה מגבלת כלי התצוגה, לא באג אמיתי). קליק בפועל על תווית "שם הרשימה" הזיז focus ל-`newListName` - מוכיח את הקישור בפעולה, לא רק ב-markup. הקבוצה (`role="group"`) אומתה: `aria-labelledby` פותר נכון ל-`listVisibilityLabel`.

`node --check` על התוכן המדויק - תקין. `node scripts/i18n-audit.js` - אין ממצאים חדשים.

v4.72.20 -> v4.72.21 (patch - תיקון נגישות, אין שינוי סכימה).

## v4.72.20 — הגבלת מספרי טלפון חברים למארגן בלבד (אשכול 4, ממצא 1, commit 1)

### 🎯 מה השתנה
`renderMembersSheet()` (index.html, מסך "מי בטיול" - כפתור 👥) הציגה קישור וואטסאפ (`wa.me/<phone>`) לכל חבר טיול, לכל משתמש מחובר - בלי שום בדיקת הרשאה. `isOrganizer()` נוסף לתנאי שמציג את הקישור, בדיוק כמו `requireOrganizerAccess()` שכבר קיים ונבדק ב-`saveModulesBtn`/`releaseMember`.

**החלטת עיצוב**: שמות/אווטארים נשארים גלויים לכל חבר מחובר (לא רק מארגן) - שם כבר מוצג בכל מקום אחר באפליקציה (`ownerName` על כל מקום/תמונה/פעילות/תגובה), כך שהסתרתו כאן ספציפית לא הייתה מקטינה חשיפה אמיתית, ורק שוברת שימוש לגיטימי ("מי נוסע איתי"). **רק מספר הטלפון** - השדה הרגיש היחיד כאן - מוגבל למארגן.

**הבהרה מפורשת בקוד (חשוב מהדוח)**: זה סוגר את החשיפה **דרך המסך** בלבד. `trips/$tripId/.read` ב-`database.rules.json` נשאר `auth != null` בלי בדיקת חברות - כל משתמש מחובר שקורא את צומת הטיול ישירות (SDK/קונסולה, בלי UI) עדיין מקבל את כל הטלפונים. זה תועד בהערה בקוד עצמו, כדי שזה לא ייחשב "התיקון המלא" בעתיד. תיקון אמיתי לצד הקריאה דורש שינוי ארכיטקטוני נפרד (ר' התכתבות אשכול 4) - לא בוצע כאן, לא נוגע ב-Rules כלל בקומיט הזה.

### 🔍 מה שנבדק בפועל
נבדק ב-2 מצבים (`currentUser`/`tripOwnerId`/`memberProfiles` מדומים, קריאה ישירה ל-`renderMembersSheet()`, צילום מסך בהקשר העמוד המלא דרך `openMembersModal()` האמיתי):
- **מארגן** (`tripOwnerId===currentUser.uid`) - הכפתור 👥 נגיש, הרשימה מציגה שם + קישור "💬 שיחה" לחברים אחרים כרגיל.
- **לא-מארגן** - אותה רשימה, אותם שמות, **בלי** קישור הטלפון לחברים אחרים. הכפתור "➕ הוספת מספר" (למספר של המשתמש עצמו) לא הושפע - נשאר זמין לכולם ללא תלות בהרשאה, כמו שהיה.

הכפתור 👥 עצמו (`membersLink`) לא נגע - עדיין מותנה רק ב-`!user` (מי שמחובר בכלל), לא ב-`isOrganizer()` - כלומר הרשימה עצמה (בלי טלפונים) נשארת נגישה לכל חבר, לא רק מארגן.

`node scripts/i18n-audit.js` - אין ממצאים חדשים (רק ממצאי shopping.html הידועים מראש). אין מחרוזות UI חדשות (רק תנאי הצגה שהשתנה).

v4.72.19 -> v4.72.20 (patch - תיקון באג אבטחה בצד התצוגה, אין שינוי סכימה).

## v4.72.19 — scrollbar עכבר לרצועות גוללות אופקיות (אשכול 6, ממצא 1)

### 🎯 מה השתנה
`.day-tabs`/`.filter-chips` (itinerary.html), `.filter-chips` (places.html), `.forecast-strip` (index.html) - כולן היו עם `overflow-x:auto` + `scrollbar-width:none` בלי שום fallback למשתמשי עכבר (בלי touch gesture, ובלי scrollbar נראה - אין דרך להגיע לתוכן שגלש). אותו דפוס בדיוק שכבר תוקן ב-`.quick-picks`/`.category-chips` (packing.html) - **לא** wheel-listener (זה נשקל ונדחה שם במפורש, ר' ההערה ב-CSS) אלא `@media (hover:hover) and (pointer:fine)` שמחזיר scrollbar דק וגלוי רק למכשירי עכבר אמיתיים.

**הרחבת היקף מאושרת מראש**: `.filter-chips` (itinerary.html **וגם** places.html - שני קבצים נפרדים, אותה מחלקה, בלי CSS משותף) לא הוזכר בדוח המקורי אבל אותו באג בדיוק - תוקן באותו commit לפי בקשה מפורשת.

**הבדל מכוון בין הקבצים**: `itinerary.html`/`places.html` על `[data-tokens="core"]` - `var(--line)` מוגדר ומעוצב לפי ערכת הנושא (אומת: `#C7DFDC` באור, `#223038` בכהה, שניהם עובדים). `index.html` **לא** רשום ל-`core` ו-`--line` **לא קיים בו בכלל** (בניגוד ל-`--glow-color` שכן שוכפל מקומית) - שימוש עיוור ב-`var(--line)` שם היה נופל ל-scrollbar בצבע ברירת-מחדל של הדפדפן, לא צבע ממותג. נעשה שימוש ב-rgba קשיח (`rgba(255,255,255,.4)`) תואם לשכנים הקיימים של forecast-strip (`border-top: rgba(255,255,255,.16)`).

**באג שנתפס ותוקן תוך כדי הבדיקה**: הבלוק הראשון שנכתב ל-`.forecast-strip` כלל רק `scrollbar-color` בלי `scrollbar-width:thin` - `scrollbar-width:none` מהכלל הבסיסי המשיך לנצח (`getComputedStyle` הראה `scrollbarWidth:"none"` בפועל). תוקן להוסיף את שתי המאפיינים יחד, כמו בשלושת המקומות האחרים.

### 🔍 מה שנבדק בפועל
לכל אחד מארבעת המקומות: `getComputedStyle` על אלמנט אמיתי מהקובץ אישר `scrollbarColor`/`scrollbarWidth` נכונים - גם במצב בהיר וגם כהה (עבור itinerary/places). אומת גם שבמצב מגע (`resize_window` preset מובייל, מדמה touch) `hover:hover` **לא** תואם ו-`scrollbar-width` נשאר `none` - ההתנהגות המקורית במגע לא נפגעה. שני צילומי מסך בהקשר העמוד המלא (`.forecast-strip` ב-index.html, `.day-tabs` ב-itinerary.html) מראים scrollbar דק וגלוי מתחת לרצועה.

`node scripts/i18n-audit.js` - אין ממצאים חדשים (רק ממצאי shopping.html הידועים מראש). שינוי CSS בלבד, אין JS חדש שדורש `node --check`.

v4.72.18 -> v4.72.19 (patch - תיקון באג, אין שינוי סכימה).

## v4.72.18 — itinerary.html: safeWrite() בארבע פונקציות (אשכול 2, commit 5 - סגירה)

### 🎯 מה השתנה
נוסף `<script src="safeWrite.js">`, והוחל בארבע פונקציות (שבעה ענפי כתיבה) - הקובץ האחרון באשכול 2:

**`savePlace()`** - הוזכרה בבקשה - אותו דפוס בדיוק (toast "עודכן"/"נוסף" בלתי-מותנה, שני ענפים: `update()` לעריכה, `push()` להוספה).

**הרחבת היקף** (כמו ב-packing/shopping/places.html): נמצאו עוד שלוש פונקציות באותו קובץ עם אותו דפוס בדיוק - נכללו באותו commit:

1. **`addPlaceToDay()`** - `push()` להוספת מקום קיים לתוכנית יומית, toast הצלחה בלתי-מותנה, ה-modal (`dayPickerOverlay`) נסגר גם בכשלון.
2. **`toggleJoin()`** - שני `remove()`/`set()` נפרדים (על `joiners`ו-`joinerNames`) בלי שום `try/catch` ובלי שום toast בכלל - כשלון היה שקט לגמרי. **נקודה לשקיפות**: שני הכתיבות בכל ענף אינן אטומיות (בדיוק כמו `autoRegisterMember`/`deletePhoto` שכבר תועדו) - קובצו יחד תחת `Promise.all` בתוך קריאת `safeWrite` אחת לכל ענף (`toggleJoin-join`/`toggleJoin-leave`), כך שדיווח ההצלחה/כשלון הוא על שתיהן יחד, אבל אם אחת מהשתיים תצליח והשנייה תיכשל - אין רול-בק לזו שהצליחה. זהו סיכון קיים במבנה הנתונים עצמו (שני נתיבים נפרדים לאותה פעולה לוגית), לא נפתר על ידי `safeWrite` - רק הפך מ"נכשל בשקט" ל"נכשל עם הודעה ברורה".
3. **`saveActivity()`** - שני ענפים (`update()`/`push()`), בלי שום toast הצלחה/כשלון בכלל (לא רק בהצלחה - גם קודם לא הייתה שום הודעה). נוספה הודעת כשלון חדשה (`saveActivity-update`/`saveActivity-add`) - הצלחה נשארת שקטה כמו קודם, לא הומצא UX חדש מעבר לנדרש לתיקון.

בכל אחת: כשלון עוצר לפני סגירת modal/איפוס שדות - בדיוק כמו כל שאר התיקונים באשכול הזה.

### 🔍 מה שנבדק בפועל
כל ארבע הפונקציות (שבעה ענפים) נבדקו בדפדפן עם hook זמני (הוסר ואומת שהוסר לפני push - `grep -n "__test" itinerary.html` מחזיר קוד יציאה 1), עם כתיבות אמיתיות מול הפרויקט האמיתי ב-Firebase, דרך קליקים אמיתיים על ה-UI (טופס עריכת/הוספת מקום, בוחר-יום אמיתי, כפתור "אני בא/ה", טופס פעילות):

- **כשלון אמיתי (`PERMISSION_DENIED`)** בכל אחד משבעת הענפים - הודעת הכשלון הנכונה הוצגה עם ה-context label הנכון (`[safeWrite] <name>`), וה-modal/שדות **לא** נסגרו/התאפסו - כולל `savePlace` (שם "Acropolis"/"Test New Place" נשאר בשדה), `saveActivity` (כותרת נשארה), `addPlaceToDay`/`toggleJoin` (ה-overlay/מצב ה-UI נשארו כמו שהיו).
- **הצלחה** (`window.safeWrite` מוחלף זמנית בפונקציה שמחזירה `true`, אין גישה לחשבון Google אמיתי בסביבה הזו): `saveActivity`-add סגר את ה-modal נכון; `savePlace`-add סגר את ה-modal ואיפס את השדה נכון; `addPlaceToDay` סגר את בוחר-היום נכון; `toggleJoin` (שני הענפים) הסתיים בלי הודעת כשלון כוזבת.

`node --check` על התוכן המדויק שנכנס ל-commit - תקין. `node scripts/i18n-audit.js` - אין ממצאים חדשים (רק ממצאי shopping.html הידועים מראש).

**זהו הקובץ האחרון באשכול 2** - safeWrite() מוחל כעת על כל 6 העמודים (index/packing/shopping/places/itinerary; wizard.html כבר היה תקין מלכתחילה).

v4.72.17 -> v4.72.18 (patch - תיקון באג, אין שינוי סכימה).

## v4.72.17 — places.html: safeWrite() בשמונה פונקציות (אשכול 2, commit 4)

### 🎯 מה השתנה
נוסף `<script src="safeWrite.js">`, והוחל בשמונה פונקציות (תשעה ענפי כתיבה):

שלוש הפונקציות שהוזכרו בבקשה - `saveEditComment()`, `saveEditReply()`, `sendReply()` - אותו דפוס בדיוק כמו `deleteComment`/`deleteReply` שכבר תוקנו באשכול 3.

**הרחבת היקף** (כמו ב-packing.html/shopping.html): נמצאו עוד חמש פונקציות באותו קובץ עם אותו דפוס בדיוק (כתיבה לא מוגנת, toast-הצלחה/שינוי מצב כוזב) שלא הוזכרו בבקשה - נכללו באותו commit:

1. **`toggleReaction()`** - `set()` על ריאקציה, לא מוגן בכלל.
2. **`addComment()`** - `push()` לא מוגן; בכשלון, `$('commentInput').value=''` היה מתאפס למרות שהכתיבה נכשלה (טקסט שהמשתמש הקליד היה נעלם).
3. **`toggleCommentLike()`** - `set()`/`remove()` על לייק, לא מוגן.
4. **`saveMusic()`** - שני ענפים (`update()` לעדכון שיר קיים, `push()` להוספת שיר חדש) - שני context labels נפרדים: `saveMusic-update`/`saveMusic-add`.
5. **`saveCaption()`** - `update()` על כיתוב תמונה, לא מוגן, בלי הודעת כשלון בכלל קודם.

בכל אחת: כשלון עוצר לפני איפוס קלט/מצב עריכה (`editingCommentKey`/`replyingToCommentId` נשארים כמו שהיו) ומציג הודעת כשלון, במקום להניח הצלחה.

### 🔍 מה שנבדק בפועל
כל שמונה הפונקציות נבדקו בדפדפן עם hook זמני (הוסר ואומת שהוסר לפני push - `grep -n "__test" places.html` מחזיר קוד יציאה 1), עם כתיבות אמיתיות מול הפרויקט האמיתי ב-Firebase, דרך קליקים אמיתיים על ה-UI (תפריט "⋮"→"עריכה", טופס תגובה אמיתי, טופס מוזיקה אמיתי) ולא קריאה ישירה לפונקציה:

- **כשלון אמיתי (`PERMISSION_DENIED`)** באחת: הודעת הכשלון הנכונה הוצגה, `console.error` נרשם עם ה-context label הנכון (`[safeWrite] <name>`), ומצב העריכה/הקלט **לא** התאפס - כולל `addComment` (טקסט "New comment text" נשאר בשדה) ו-`sendReply` (טקסט "New reply text" נשאר בשדה).
- **הצלחה** (`window.safeWrite` מוחלף זמנית בפונקציה שמחזירה `true`, אין גישה לחשבון Google אמיתי בסביבה הזו): `saveMusic`'s add-branch סגר את הטופס נכון; `addComment` ניקה את השדה נכון; `toggleReaction`/`toggleCommentLike` הסתיימו בלי הודעת כשלון כוזבת. `saveEditComment`/`saveEditReply`/`sendReply` אימתו שהמצב הפנימי (`editingCommentKey=null`/`replyingToCommentId=null`) מתעדכן נכון בהצלחה - אבל ה-DOM לא משתקף מיד, כי שלוש הפונקציות האלה **גם בקוד המקורי, לפני השינוי הזה**, לא קראו ל-`renderComments()` אחרי הצלחה - הן מסתמכות על ה-listener החי של RTDB שירוץ מחדש render בעצמו. זה תוצר-לוואי של שיטת הבדיקה עם stub (אין round-trip אמיתי דרך ה-listener), לא רגרסיה - מתועד לשקיפות, באותה רוח כמו ממצא ה-self-correction ב-`itemsMap` שתועד ב-packing.html.

`node --check` על התוכן המדויק שנכנס ל-commit - תקין. `node scripts/i18n-audit.js` - אין ממצאים חדשים (רק ממצאי shopping.html הידועים מראש).

v4.72.16 -> v4.72.17 (patch - תיקון באג, אין שינוי סכימה).

## v4.72.16 — shopping.html: safeWrite() ב-updateBaseQty ו-addModalForm (אשכול 2, commit 3)

### 🎯 מה השתנה
נוסף `<script src="safeWrite.js">`, והוחל ב-3 נקודות כתיבה:

1. **`updateBaseQty()`** - `runTransaction()` לא היה אפילו `await`. עכשיו עטוף ב-`safeWrite`, עם הודעת כשלון במקום toast-הצלחה כוזב.
2. **`addModalForm`'s new-item branch** (`set()`) - בדיוק אותו דפוס שכבר תוקן ב-`deleteItem` (אשכול 3, commit 4): הכתיבה לא הייתה awaited, toast ההצלחה ("הוספנו את...") ירה מיידית לפני שהכתיבה בכלל נשלחה.
3. **`addModalForm`'s existing-item branch** (`runTransaction()`, עדכון כמות למוצר קיים) - לא הוזכר במפורש בבקשה, אבל זה בדיוק אותו handler ואותו דפוס בדיוק כמו #2 (`runTransaction` לא awaited, toast-הצלחה כוזב) - נכלל כדי לא להשאיר פער זהה באותה פונקציה ממש.

בכשלון בכל אחד מהשלושה - `return` לפני הודעת ההצלחה **וגם** לפני איפוס/סגירת ה-modal (ב-`addModalForm`) - בדיוק כמו כל שאר הפונקציות באשכול הזה: כשלון לא מוחק את מה שהמשתמש הקליד.

### 🔍 מה שנבדק בפועל
כל שלוש הנקודות נבדקו בדפדפן עם hook זמני (הוסר ואומת שהוסר לפני push), עם כתיבות אמיתיות מול הפרויקט האמיתי ב-Firebase - כולל **דרך הטופס האמיתי** (לא קריאה ישירה לפונקציה) עבור שני ענפי `addModalForm`: הקלדת "Milk" (מוצר חדש) ולחיצה על "הוסף לרשימה", והקלדת "Test Product" הקיים (מפעיל את דיאלוג "לעדכן כמות?") ואישור.

- **כשלון** בכל אחת משלוש - `runTransaction`/`set` נדחו עם `permission_denied`/`PERMISSION_DENIED` אמיתי. הודעת הכשלון הנכונה הוצגה, ה-modal **לא** נסגר, השדות **לא** התאפסו ("Milk" נשאר בשדה אחרי הכשלון).
- **`updateBaseQty` וריסק #3 שסומן מראש**: אומת ש-`itemsMap.testItem1.qty` נשאר `"3"` אחרי כשלון (לא "קפץ" ל-4 מקומית) - גם ויזואלית בדף. זה כי הפונקציה **מעולם לא עשתה עדכון אופטימי מקומי** - הכמות המוצגת מגיעה תמיד מה-listener החי, לא ממשתנה מקומי - כך שהסיכון הזה היה כבר לא-קיים מבנית, לא רק "טופל".
- **הצלחה + Undo** (`safeWrite` מוחלף זמנית בפונקציה שמחזירה `true`, אין גישה לחשבון Google אמיתי בסביבה הזו) - `updateBaseQty` הציג "הכמות עודכנה בהצלחה", `undoBtn` הופיע, ולחיצה עליו (הקוד הקיים, לא נגעו בו) הציגה "השינוי בוטל בהצלחה!" מיד - מוכיח שהתזמון של `saveUndoBackup()` (שעדיין רץ סינכרונית ראשון, לפני כל ניסיון כתיבה, לא השתנה) לא נפגע. אותה בדיקה חזרה על `addModalForm`'s existing-item branch - "עודכן ל-4 יח'", Undo זמין, modal נסגר.

`node --check` על התוכן המדויק שנכנס ל-commit - תקין. `node scripts/i18n-audit.js` - אין ממצאים חדשים.

v4.72.15 -> v4.72.16 (patch - תיקון באג, אין שינוי סכימה).

## v4.72.15 — packing.html: safeWrite() בשבע פונקציות (אשכול 2, commit 2)

### 🎯 מה השתנה
נוסף `<script src="safeWrite.js">`, והוחל ב-7 פונקציות שכתבו ל-RTDB בלי `try/catch` (או בלי `await` בכלל), עם UI-הצלחה שרץ תמיד ללא תנאי:

`createListReturningId`/`createList`, `duplicateList`, `setListVisibility`, `bulkToggleDone`, `bulkToggleShared`, `confirmBulkMove`, `saveEdit` (שני הענפים - שינוי-מקום ו"נשאר באותה רשימה", עם שני context labels נפרדים ל-`safeWrite`: `saveEdit-move`/`saveEdit`).

בכל אחת: קפיצת `activeList` (ב-`createList`/`duplicateList`/`saveEdit`'s move-branch) עברה להתבצע **רק אחרי** `safeWrite` מוצלח - בדיוק כמו שכבר תוקן ל-`deleteList` באשכול 3, לא לפני. `bulkToggleDone`/`bulkToggleShared`/`confirmBulkMove` - `setSelectionMode(false)` (שמנקה את הבחירה) גם הוא רץ רק אחרי הצלחה, לא לפני - כשלון משאיר את הבחירה כמו שהייתה, לא "תקוע" ולא נמחקת בטעות.

`createList()` עצמה קיבלה תיקון קטן נוסף: הבדיקה `if (!trimmed) return;` עברה **לפני** הקריאה ל-`createListReturningId`, כדי ש"שם ריק" (מקרה תקין, שקט) יישאר מובחן מ"הכתיבה נכשלה" (כשלון אמיתי, דורש הודעה) - שני המקרים היו מתכנסים לאותו `if (!id) return;` שקט קודם.

### 🔍 מה שנבדק בפועל
כל 7 הפונקציות (8 עם שני ענפי `saveEdit`) נבדקו בדפדפן עם hook זמני (הוסר ואומת שהוסר לפני push), עם כתיבות אמיתיות מול הפרויקט האמיתי ב-Firebase:
- **כשלון אמיתי (`PERMISSION_DENIED`)** בכל אחת - הודעת כשלון נכונה הוצגה, `console.error` נרשם עם ה-context הנכון, ו-`activeList`/`selectionMode`/`selectedIds`/מודאלים (`editOverlay`) **לא** השתנו/נסגרו.
- **`saveEdit`'s move-branch ספציפית** - נבדק פעמיים: פעם ראשונה עם `editListSelect` לא מאוכלס נכון (חשף פער בבדיקה שלי עצמה, לא בקוד - `<select>.value` לא נתפס בלי `<option>` תואם), ואז שוב דרך `openEdit()` האמיתית שממלאת את האפשרויות כמו שהמשתמש היה רואה - אישר ש-`activeList` נשאר על הקטגוריה המקורית בכשלון.
- **הצלחה** (`safeWrite` מוחלף זמנית בפונקציה שמחזירה `true`, אין גישה לחשבון Google אמיתי בסביבה הזו) - `saveEdit`'s move-branch קפץ נכון ל-`activeList` החדש וסגר את המודאל; `bulkToggleDone` ניקה את מצב הבחירה נכון.

**ממצא צדדי, לא קשור לתיקון הזה**: בבדיקת ההצלחה של `createList`/`duplicateList` נצפה ש-`activeList` קופץ לרגע לרשימה החדשה ואז חוזר ל-`"all"` - התברר שזה מנגנון תיקון-עצמי **קיים מראש ומתועד** ב-`renderLists()` (משם: "activeList קיים ותקין? אם לא, all") שמפעיל את עצמו בכל פעם ש-`activeList` לא נמצא עדיין ב-`ids` המקומי - וזה קורה כאן רק כי ה-stub לא עדכן את `items` המקומי כמו כתיבה אמיתית הייתה עושה (דרך ה-listener). לא נגרם ע"י השינוי הזה, ולא בהיקף המשימה.

`node --check` על התוכן המדויק שנכנס ל-commit - תקין. `node scripts/i18n-audit.js` - אין ממצאים חדשים.

v4.72.14 -> v4.72.15 (patch - תיקון באג, אין שינוי סכימה).

## v4.72.14 — index.html: safeWrite() בחמש נקודות כתיבה שקטות (אשכול 2, commit 1)

### 🎯 מה השתנה
נוסף `<script src="safeWrite.js">`, והוחל ב-5 נקודות כתיבה שהיו שקטות/כוזבות בכשלון:

1. **שמירת הגדרות טיול** (`saveFlightBtn`, הפונקציה הכי גדולה בקובץ, ~400 שורות) - עד עכשיו `db.ref().update(updates)` לא היה awaited בכלל; ה-modal נסגר וה-"עודכן בהצלחה!" נורה תמיד, מיידית, בלי קשר לתוצאה. נעטף **רק** קריאת ה-`update()` עצמה ב-`safeWrite` - שאר ~400 השורות (בניית legs, geocoding, ולידציה) לא נגעו בהן.
2. **כספת אישית** (`savePersonalLockerCard`) - הייתה שקטה לגמרי (גם הצלחה וגם כשלון). נוספה הודעת כשלון חדשה (מכוון - כבר סוכם כהחלטה, לא scope creep).
3. **תגיות משתתפים** (הוספה+הסרה, דרך `saveTripCollection`) - אותו דפוס, ועם זה - `groupMembers` עצמו מתעדכן רק **אחרי** הצלחה מאומתת (היה מתעדכן אופטימית לפני), כמו שכבר תוקן ל-`activeList` באשכול 3.
4. **`saveModulesBtn`** - כבר היה `await`, רק חסר `try/catch`. כשלון היה "נתקע בשקט" (שום קוד לא רץ אחרי ה-throw, לא modal נסגר ולא הודעה) - עכשיו מריץ הודעת כשלון מפורשת. גם כאן `enabledFeatures` עבר לעדכון-אחרי-הצלחה.
5. **`releaseMember()` (בונוס)** - פעולה הרסנית (מנתקת חבר מחשבון Google, מוחקת 3 נתיבים) שהייתה **לגמרי בלי** try/catch. עכשיו כשלון מציג הודעה ברורה במקום להיראות כמו הצלחה.

### 🔍 מה שנבדק בפועל
כל 5 הנקודות נבדקו בדפדפן עם hook זמני (הוסר ואומת שהוסר לפני push) שעוקף את שער האימות, עם כתיבות אמיתיות מול הפרויקט האמיתי ב-Firebase:
- **כשלון**: כל אחת מ-5 הנקודות נדחתה עם `PERMISSION_DENIED` אמיתי - הודעת הכשלון הנכונה הוצגה בכל אחת, `console.error` נרשם עם התווית הנכונה (`[safeWrite] saveFlightBtn` וכו'), ה-modal הרלוונטי **לא** נסגר.
- **`saveFlightBtn` ספציפית**: נבדק דרך הקליקים האמיתיים (פתיחת פאנל אדמין אמיתית, הוספת יעד אמיתית, מילוי "Rome", לחיצה אמיתית על "שמור") - לא קריאה ישירה לפונקציה. ה-geocoding הצליח בפועל (נראה "Rome — מושבים" בטופס אחרי הקליק), ורק אז הכתיבה עצמה נדחתה - מוכיח שהעטיפה לא בלעה שום דבר מהזרימה שלפניה.
- **הצלחה**: `window.safeWrite` הוחלף זמנית בפונקציה שמחזירה `true` תמיד (אין גישה לחשבון Google אמיתי בסביבה הזו) כדי לוודא את לוגיקת-ההצלחה עצמה - `saveFlightBtn` סגר את ה-modal והציג "עודכן בהצלחה!", `personalLockerCards`/`groupMembers` התעדכנו נכון אחרי ההצלחה המדומה.

`node --check` על התוכן המדויק שנכנס ל-commit - תקין. `node scripts/i18n-audit.js` - אין ממצאים חדשים.

v4.72.13 -> v4.72.14 (patch - תיקון באג, אין שינוי סכימה).

## v4.72.13 — itinerary.html: טיפול בכשלון במחיקת מקום/פעילות (סוגר אשכול 3)

### 🎯 מה השתנה
`deleteEntry` (משותפת - מקומות) ו-`deleteActivity` - נוסף `try/catch` סביב ה-`remove()` הקיים, עם `console.error`+`toast("המחיקה נכשלה. נסו שוב.")` בכשלון במקום כשלון שקט - אותה תבנית בדיוק כמו חמשת ה-commits הקודמים באשכול (`packing.html`/`shopping.html`/`places.html`). `deleteEntry` גם עברה לתבנית early-return, כמו ב-`places.html`, ללא שינוי התנהגות.

זה סוגר את אשכול 3 (עקביות נקודתית בין עמודים) - כל ארבעת הממצאים טופלו: race ב-`toast()` (v4.72.8), `escapeHtml()` וגרש בודד (v4.72.9), ואישור+טיפול-כשלון בכל נתיב מחיקה בכל 6 העמודים (v4.72.10 עד כאן).

### 🔍 מה שנבדק בפועל
נבדק בדפדפן עם hook זמני (הוסר ואומת שהוסר לפני push) שחושף את שתי הפונקציות זמנית ל-`window` וממלא state מקומי, עם כתיבות אמיתיות מול הפרויקט האמיתי ב-Firebase: `deleteActivity` ו-`deleteEntry('places/items',...)` נדחו שתיהן עם `PERMISSION_DENIED` אמיתי, `toast("המחיקה נכשלה. נסו שוב.")` הוצג בכל אחת, `console.error` נרשם עם השם הנכון.

**לא נבדק** (כמו בכל חמשת ה-commits הקודמים באשכול, מאותה סיבה): מחיקה שמצליחה בפועל - דורשת חשבון Google אמיתי שלא ניתן להשיג בסביבה הזו. מכיוון ששתי הפונקציות לא מציגות שום הודעת הצלחה או משנות state מקומי מעבר לקריאת ה-`remove()` עצמה (הרשימה מתעדכנת רק דרך ה-listener החי), אין כאן "מסלול הצלחה" נוסף לבדוק מעבר לכך שהקוד בתוך ה-`try` זהה לחלוטין לקוד המקורי לפני העטיפה.

`node --check` על התוכן המדויק שנכנס ל-commit - תקין. `node scripts/i18n-audit.js` - אין ממצאים חדשים.

v4.72.12 -> v4.72.13 (patch - תיקון באג, אין שינוי סכימה).

## v4.72.12 — places.html: טיפול בכשלון בכל נתיבי המחיקה

### 🎯 מה השתנה
`deleteEntry` (משותפת - קישורי מוזיקה), `deleteComment`, `deleteReply` - נוסף `try/catch` סביב ה-`remove()` הקיים, עם `console.error`+`toast("המחיקה נכשלה. נסו שוב.")` בכשלון במקום כשלון שקט. `deleteEntry` גם עברה לתבנית early-return (`if(!(await customConfirm(...)))return;`) לעקביות עם שלוש הפונקציות האחרות - ללא שינוי התנהגות.

**`deletePhoto` - זהירות מיוחדת כמו שסומן מראש**: לפונקציה הזו כבר היה `try/catch` חלקי, רק סביב מחיקת ה-Storage, עם `console.warn(e)` קיים. **לא נגעתי בענף הזה בכלל** - הוספתי `try/catch` נפרד ועצמאי רק סביב שורת ה-`remove()` של ה-RTDB, שהייתה היחידה בלי שום טיפול.

### 🔍 מה שנבדק בפועל
כל ארבע הפונקציות נבדקו בדפדפן עם hook זמני (הוסר ואומת שהוסר לפני push) שחושף אותן זמנית ל-`window` וממלא state מקומי, עם כתיבות אמיתיות מול הפרויקט האמיתי ב-Firebase (לא מדומות):
- `deleteComment`/`deleteReply`/`deleteEntry` - כל אחת נדחתה עם `PERMISSION_DENIED` אמיתי, `toast("המחיקה נכשלה. נסו שוב.")` הוצג בכל אחת, `console.error` נרשם עם השם הנכון של כל פונקציה.
- `deletePhoto` - **אומת בפועל ששני מסלולי הכשלון עצמאיים לגמרי, לא מתערבבים**: תמונת-בדיקה עם `storagePath` מזויף גרמה לכשלון Storage אמיתי (`storage/unauthorized`, 403) שהופיע כ-`console.warn` בודד בדיוק כמו קודם - ואז, בנפרד, כשלון RTDB אמיתי (`PERMISSION_DENIED`) הופיע כ-`console.error` חדש + `toast` כשלון. שני האזהרות הופיעו כל אחת פעם אחת בלבד - לא כפילות, לא הסתרה.

`node --check` על התוכן המדויק שנכנס ל-commit - תקין. `node scripts/i18n-audit.js` - אין ממצאים חדשים.

v4.72.11 -> v4.72.12 (patch - תיקון באג, אין שינוי סכימה).

## v4.72.11 — shopping.html: deleteItem לא מכריז הצלחה כוזבת

### 🎯 מה השתנה
`deleteItem()` היה `remove()` בלי `await` ובלי `try/catch` - ה-toast "הפריט נמחק" נורה מיידית וללא תנאי, גם אם הכתיבה בפועל נכשלה. עכשיו: `await remove(...)` בתוך `try/catch`, וה-toast "נמחק" עובר להיות רק אחרי כתיבה מוצלחת. בכשלון - `showToast("המחיקה נכשלה. נסו שוב.")` + `console.error`, לא הצלחה כוזבת.

**נבדק במפורש (סיכון שכבר סומן מראש)**: העיכוב הקטן החדש (await לפני toast) לא משנה שום דבר בתזמון `saveUndoBackup()`/`lastStateBackup` - `saveUndoBackup(id)` עדיין רץ סינכרונית ראשון, לפני כל ניסיון כתיבה, בדיוק כמו קודם. `undoBtn`/`lastStateBackup` (התיעוד הקיים ב-CLAUDE.md על השבריריות שלהם) לא נגעו בהם כלל.

### 🔍 מה שנבדק בפועל
נבדק בדפדפן עם hook זמני (הוסר ואומת שהוסר לפני push) שממלא state מקומי כדי לעקוף את שער האימות:
- **מחיקה שנכשלת** - כתיבה אמיתית מול הפרויקט האמיתי ב-Firebase (לא חשבון מחובר) נדחתה עם `PERMISSION_DENIED` אמיתי - `showToast("המחיקה נכשלה. נסו שוב.")` הוצג, לא "הפריט נמחק" (אומת גם ב-`console.error`).
- **מחיקה מוצלחת** - מכיוון שאין גישה לחשבון Google אמיתי בסביבה הזו (ואי אפשר לדמות הצלחה מול RTDB אמיתי בלי אחד), אומת באמצעות הרצת אותן שורות קוד בדיוק שרצות אחרי כתיבה מוצלחת (`saveUndoBackup` + `showToast` עם `showUndo:true`, ה-`remove()` עצמו מדולג) - toast "הפריט נמחק" מוצג, `undoBtn` הופך גלוי (`display:block`), `lastStateBackup` נלכד נכון.
- **Undo אחרי מחיקה מוצלחת** - הופעל `undoBtn.onclick` (הלוגיקה הקיימת, לא נגעו בה) - "השינוי בוטל בהצלחה!" מוצג מיד, ניסיון השחזור מול RTDB האמיתי נדחה (שוב `PERMISSION_DENIED` אמיתי, כצפוי בלי חשבון אמיתי) והציג "שחזור השינוי נכשל - נסו שוב" עם `lastStateBackup` נשמר לניסיון חוזר - בדיוק ההתנהגות הקיימת, לא השתנתה כלום.

`node --check` על התוכן המדויק שנכנס ל-commit - תקין. `node scripts/i18n-audit.js` - אין ממצאים חדשים.

v4.72.10 -> v4.72.11 (patch - תיקון באג, אין שינוי סכימה).

## v4.72.10 — packing.html: אישור מחיקה + טיפול בכשלון בכל נתיבי המחיקה

### 🎯 מה השתנה
מיזוג ממצא 1 (מחיקת תת-פריט בלי אישור) וממצא 4 (אין טיפול בכשלון כתיבה) עבור `packing.html` - שניהם נגעו באותם handlers בדיוק.

1. **מחיקת תת-פריט** (`data-sub-delete`) - הייתה `remove()` ישיר בלי `customConfirm` ובלי `await`. עכשיו: `customConfirm` (בדיוק כמו מחיקת פריט ראשי), ואז `try/catch` עם `customAlert`+`console.error` בכשלון.
2. **מחיקת פריט ראשי** (`deleteItem`) - כבר היה לה `customConfirm`, רק חסר טיפול בכשלון. נוסף `try/catch` סביב ה-`remove()` הקיים.
3. **`bulkDelete`** - נוסף `try/catch`. `setSelectionMode(false)` וההודעה "הפריטים נמחקו" רצים רק אחרי `await` מוצלח - כשלון משאיר את מצב הבחירה כמו שהיה (לא "תקוע" ולא מאבד את הבחירה, פשוט לא מתקדם), ולא טוען הצלחה כוזבת.
4. **`deleteList`** - נוסף `try/catch`, וגם תוקן הבאג שכבר זוהה בחקירה: `activeList = "main"` עבר להיות **אחרי** ה-`update()` המוצלח, לא לפני.

### 🔍 מה שנבדק בפועל
כל ארבעת התרחישים נבדקו בדפדפן עם hook זמני (`window.__test_*`, הוסר ואומת שהוסר לפני push) שממלא state מקומי (כדי לעקוף את שער האימות בלי גישה לחשבון Google אמיתי) - **אבל הכתיבות עצמן רצות מול הפרויקט האמיתי ב-Firebase**, לא מדומות:
- מחיקת תת-פריט: `customConfirm` מוצג, "ביטול" לא מוחק (אומת ב-state), "אישור" מנסה כתיבה אמיתית.
- הכתיבה (בלי חשבון Google מחובר באמת) נדחתה ע"י הכללים האמיתיים ב-RTDB עם `PERMISSION_DENIED` אמיתי - וזה בדיוק מה שחשף את התיקון: `customAlert("המחיקה נכשלה. נסו שוב.")` הוצג, `console.error` נרשם, במקום כשלון שקט.
- `bulkDelete` בכשלון אמיתי - אותה הודעת כשלון, מצב הבחירה נשאר פעיל (הפריט עדיין מסומן, סרגל הפעולות עדיין מוצג), ה-toast שהוצג היה עדיין ההודעה הישנה על כניסה למצב בחירה - "הפריטים נמחקו" מעולם לא נורה.
- `deleteList` בכשלון אמיתי - אותה הודעת כשלון, `activeList` נשאר `"extra"` (לא התאפס ל-`"main"`), המודאל לניהול הרשימה נשאר פתוח.

`node --check` על התוכן המדויק שנכנס ל-commit - תקין. `node scripts/i18n-audit.js` - אין ממצאים חדשים.

v4.72.9 -> v4.72.10 (patch - תיקון באג, אין שינוי סכימה).

## v4.72.9 — escapeHtml(): הוספת גרש בודד (index.html, wizard.html)

### 🎯 מה השתנה
`escapeHtml()` ב-`index.html`/`wizard.html` לא טיפלה בגרש בודד (`'`→`&#39;`), בשונה מ-`packing.html`/`places.html`/`itinerary.html` שכן. הוספה שורה אחת בכל קובץ.

**חשוב להיות מדויקים לגבי מה זה בפועל מתקן**: זו **הגנה מקדימה (defense-in-depth)**, לא סגירת דליפה שהייתה ניתנת לניצול. בדקתי בפועל (לא רק לוגית) את שלושת המקומות שהדוח ציין - `deleteTripPickerEntry`/`copyRecentTrip`/`deleteRecentTrip` - כולם מטמיעים ערך בתוך `onclick="fn('${escapeHtml(id)}')"`: attribute בגרשיים **כפולים**, עם מחרוזת JavaScript בגרש **בודד** בתוכו. אימתתי (השוואת המחרוזת שהוזנה ל-`innerHTML` מול מה שה-DOM שומר בפועל אחרי parsing) ש-HTML entities מפוענחים בזמן ה-parsing, **לפני** שהדפדפן מריץ את קוד ה-`onclick` - כלומר `&#39;` הופך בחזרה לגרש רגיל עוד לפני שקוד ה-JS בתוך ה-attribute בכלל מתבצע. escaping מסוג HTML **לא יכול, מבנית, להגן על תוחם מחרוזת JavaScript מקונן בתוך attribute** - זה דורש JS-string-escaping (backslash), לא HTML-entity-escaping. בדקתי גם: אין באף אחד מהקבצים האלה שימוש ב-`escapeHtml()` בתוך attribute עם גרש בודד כתוחם (`attr='...'`) - איפה שהתיקון הזה *כן* היה עוזר. כלומר בקוד הקיים היום, השינוי הזה לא משנה שום דבר בפועל בשני הכיוונים - לא שובר, לא מתקן וקטור אמיתי.

זה עדיין תיקון סביר: `tripId` (הערך היחיד שמגיע לשלושת המקומות האלה) לעולם לא יכול להכיל גרש (`'trip_' + base36`, מאומת בקוד היצירה ב-wizard.html) - כך שאין כרגע שום נתיב לניצול בפועל, גם בלי קשר לפער ב-escapeHtml. אבל אם בעתיד ייכתב `onclick` חדש עם שדה חופשי אמיתי (שם/כותרת) - התיקון הזה **לא** יגן עליו; יידרש טיפול אחר (ר' התיעוד ב-CLAUDE.md).

### 🔍 מה שנבדק בפועל
`escapeHtml("O'Brien")`/`escapeHtml("trip_o'brien")` בקונסולה - מחזיר `&#39;` נכון. בנייה ידנית של תבנית ה-`onclick` המדויקת מ-`deleteTripPickerEntry` עם ID שמכיל גרש, השוואת המחרוזת שהוזנה ל-innerHTML מול `getAttribute('onclick')`/`innerHTML` אחרי parsing - זה מה שחשף שה-entity מפוענח בחזרה. `node --check` על התוכן המדויק שנכנס ל-commit בשני הקבצים - תקין.

v4.72.8 -> v4.72.9 (patch - הקשחה מקדימה, אין שינוי סכימה).

## v4.72.8 — toast(): תיקון race condition בין קריאות מהירות

### 🎯 מה השתנה
`toast()`/`showToast()` ב-4 מתוך 5 העמודים שיש להם מנגנון כזה (`shopping.html`/`packing.html`/`places.html`/`itinerary.html` - `index.html` כבר טיפל בזה נכון, ל-`wizard.html` אין `toast()` בכלל) לא ביטלו את ה-`setTimeout` הקודם לפני קביעת חדש. שתי קריאות מהירות (למשל שני toast-ים עוקבים) גרמו לטיימר הישן לסגור את ה-toast **החדש** מוקדם מהמתוכנן - אומת בפועל (לא רק לוגית): הודעה שנייה שנקראה 300ms אחרי הראשונה נסגרה אחרי ~1920ms במקום 2200ms המלאים, נחתכת ע"י הטיימר של ההודעה הראשונה. התיקון: `clearTimeout` על טיימר שמור לפני כל `setTimeout` חדש, אותה תבנית שכבר קיימת ב-`index.html`.

### 🔍 מה שנבדק בפועל
שחזור מבודד (עותק מדויק של גוף הפונקציה, לא שחזור בדמיון) - לפני התיקון: ההודעה השנייה נעלמת ב-t=2221ms (טיימר של ההודעה הראשונה). אחרי התיקון: נעלמת ב-t≈2661ms, כלומר ~2200ms אחרי הקריאה השנייה עצמה - משך התצוגה המלא כמו שאמור. `node --check` על 4 הסקריפטים המודולריים שנגעו בהם - תקין. `node scripts/i18n-audit.js` - אין ממצאים חדשים (התיקון לא נוגע בשום טקסט למשתמש).

v4.72.7 -> v4.72.8 (patch - תיקון באג תזמון, אין שינוי סכימה).

## v4.72.7 — #memberGate: שדה טקסט חופשי כשהרישום האוטומטי נכשל

### 🎯 מה השתנה
`#memberGate` (index.html) הוא מסך המילוט היחיד כשהרישום האוטומטי (`autoRegisterMember`, member.js) נכשל - עד עכשיו היה בו רק `<select>` סגור עם שמות שהמארגן הקליד; משתמש ששמו לא ברשימה (או שהרשימה ריקה, למשל אחרי הסרת חסימת הוויזרד ב-v4.72.5) נתקע במבוי סתום. נוסף קישור "השם שלי לא ברשימה" מתחת לכפתור הקיים (לא מחליף את ה-select - הבחירה מרשימה עדיין הדרך הנוחה כשהשם כן שם) שחושף שדה טקסט + כפתור "הצטרפות". הכפתור קורא **לאותה** `autoRegisterMember()` הקיימת (member.js לא שונה כלל) עם הטקסט המוקלד כ-`displayName` במקום `currentUser.displayName` - שום נתיב כתיבה חדש, שום שינוי במפתחות/בכללי RTDB.

### 🔍 מה שנבדק בפועל
השוואה שורה-שורה בין שני ה-`ops` שמועברים ל-`autoRegisterMember()` (הנתיב הקיים מול החדש) - זהים לחלוטין מלבד `displayName`, כולל `tryClaim`/`writeProfile`/`rollbackClaim` (העתקה מדויקת). נבדק בדפדפן: (1) ה-select הקיים עדיין מאוכלס נכון ולא נפגע. (2) שדה ריק בלחיצה על "הצטרפות" - `customAlert` ברור, אושר ב-Network שאין שום קריאה ל-firebaseio לפני האימות (לא נשלחת כתיבה עם שם ריק). (3) הרשמה מוצלחת עם שם מוקלד (מדומה, ללא כתיבה אמיתית ל-RTDB) - השער נסגר, `currentProfile.name` מכיל בדיוק את הטקסט שהוקלד. (4) עברית/אנגלית ומסך 375px - טקסטים מתורגמים נכונים, אין גלישה אופקית, גם במצב כהה וגם בהיר. קוד בדיקה זמני (`window.__test_showMemberGate`) הוסר ואומת שהוסר (`grep`) לפני push.

**לא נבדק** (ולא ניתן היה לבדוק בסביבה הזו): כתיבה אמיתית מול RTDB עם חשבון Google אמיתי - דורש כניסה אינטראקטיבית שלא ניתן לבצע אוטומטית. במקום זאת אומת שהאובייקט הנכתב זהה במבנה לנתיב הקיים (שכן עובד בפרודקשן) ושתואם את כללי ה-RTDB שאומתו בנפרד השבוע.

v4.72.6 -> v4.72.7 (patch - תיקון מבוי סתום, אין שינוי סכימה).

## v4.72.6 — הודעה למשתמש כשכניסת Google ב-redirect נכשלת (index/shopping/wizard)

### 🎯 מה השתנה
`getRedirectResult().catch()` היה ריק לגמרי ב-index.html/shopping.html (אפס הודעה, אפס `console.warn`) ומדפיס רק ל-console ב-wizard.html - כשל שקט בדיוק מהסוג שה-checklist דורש `console.warn`+משוב למשתמש. הוספתי הודעה למשתמש (`customAlert`/`showToast`, לפי מה שכל עמוד כבר משתמש בו) בכל אחד מהשלושה - בדיוק אותה הודעה שכל עמוד כבר מציג במסלול ה-popup המקביל שלו (`dashboardGoogleBtn`/`shoppingGoogleBtn`/`handleProviderSignIn`), לא ניסוח חדש. `itinerary.html`/`places.html`/`packing.html` כבר עשו את זה נכון (`toast(error.message)`) - זו לא הייתה בעיה ב-6/6 העמודים.

`getRedirectResult()` נדחה (`reject`) רק כשבאמת בוצע redirect וההתחברות דרכו נכשלה - אם המשתמש מעולם לא ניסה redirect, ה-Promise מתמלא בהצלחה עם ערך ריק, לא נדחה. אין צורך בדגל נפרד כדי להבדיל "עוד לא ניסה" מ"ניסה ונכשל" - עצם ההגעה ל-`.catch()` היא כבר האות הרלוונטי.

### 🔍 מה שנבדק בפועל
נבדק בדפדפן שמסלול ה-popup הרגיל (הרוב המכריע של המשתמשים) לא מציג שום הודעה חדשה בטעות - `getRedirectResult()` נפתר בשקט (לא נדחה) כשלא בוצע redirect מעולם, כמו שהוסבר למעלה, כך שההודעה החדשה לא מופיעה בטעינת עמוד רגילה.

v4.72.5 -> v4.72.6 (patch - הודעת שגיאה, אין שינוי סכימה).

## v4.72.5 — ויזרד: הסרת חסימת "לפחות חבר קבוצה אחד"

### 🎯 מה השתנה
`validateStep()` ב-wizard.html חסמה מעבר משלב 2 בלי לפחות שם אחד ב-`participants`. הרשימה הזו כבר לא שער כניסה מאז הרישום האוטומטי (member.js) - היא רשימת-מעקב אופציונלית לאדמין בלבד, כמו שהקוד עצמו כבר מתעד. נבדק ב-grep מלא: אין שום צרכן טכני שמניח שהיא לא ריקה (packing/shopping כבר נותקו ממנה ב-v4.30.1/v4.30.2, Cloud Functions לא נוגעות בה, אינטגרציית Spliit חיצונית וידנית לגמרי). ההסרה מוגבלת לחסימה עצמה (`if (step === 2 && participants.length === 0)`) - השלב, השדה, ה-`error-text` וההודעה נשארו במקומם, פשוט לא נאכפים יותר.

### 🔍 מה שנבדק בפועל
מעבר משלב 2 לשלב 3 בלי להוסיף אף משתתף - עובר. הוספת משתתף עדיין עובדת בדיוק כמו קודם. שלבים 1/3 (וולידציה של יעד/תאריכים/features) לא נגעו בהם - נבדק שהם עדיין חוסמים כרגיל.

v4.72.4 -> v4.72.5 (patch - הסרת חסימה, אין שינוי סכימה).

## v4.72.4 — כספת אישית: ניסוח "(אופציונלי)" + placeholder "לדוגמה:" למספר טיסה/שעה

### 🎯 מה השתנה
1. **אומת (לא שונה)**: `flightNumber`/`flightTime` כבר לא חוסמים שמירה כשריקים - `savePersonalLockerCard()` בודקת רק `name`/`currentUser`, בדיוק כמו `seat` (ר' הממצא הזה כבר מ-commit 1 המקורי). לא נדרש שינוי קוד.
2. הלייבלים - "מספר טיסה (אופציונלי):" / "שעת המראה (אופציונלי):" (he), "Flight number (optional):" / "Departure time (optional):" (en) - אותה מוסכמה כמו `profilePhoneLabel`/`adminTripTitleLabel` הקיימים. ההדגמה הישנה בתוך הלייבל עצמו ("למשל LY391") הוסרה - התפקיד הזה עבר ל-placeholder.
3. ה-placeholder - "לדוגמה: LY391"/"לדוגמה: 14:30" (he), "e.g. LY391"/"e.g. 14:30" (en) - כמו `adminDestInput`/`adminTripTitleInput` הקיימים. מחרוזת סטטית בעברית ב-HTML (ברירת מחדל), מוחלפת לאנגלית ב-`applyTripLanguage()` - אותו דפוס בדיוק כמו שני השדות ההם.

### 🔍 מה שנבדק בפועל
עברית ואנגלית - לייבלים ו-placeholder נבדקו ישירות (`textContent`/`placeholder`), לא רק נקראו מהקוד. שמירה עם שני השדות ריקים - נשמר בהצלחה, בלי חסימה (אישוש #1). שולחן ו-375px - screenshot בכל שילוב. `node --check`/`i18n-audit` - נקיים. קונסול נקי.

v4.72.3 -> v4.72.4 (patch - ניסוח/UX, אין שינוי סכימה).

## v4.72.3 — כספת אישית: הסרת הבורר המפורש, חזרה לזיהוי-רגל אוטומטי

### 🎯 שינוי כיוון, אחרי חקירה
לפני השינוי הזה נחקר חשד לבאג-איבוד-נתונים בבורר הרגל שנוסף היום (v4.72.1). **הבאג לא אומת** - הרצף המדויק (ברלין→פריז→אמסטרדם→חזור, עם מעקב state אחרי כל שלב, לא רק ההודעה) הראה שההשוואה תמיד נקבה בשם הרגל הקיימת-בפועל, נכון. "ברלין נדרס" קרה, אבל בדיוק לפי המפרט שאושר ב-v4.72.1 עצמו - רשומה אחת, אזהרה-לפני-דריסה, לא דריסה-שקטה. זו הייתה התנהגות מכוונת, לא רגרסיה.

**למרות זאת** - הוחלט לוותר על הבורר ולחזור למנגנון הפשוט הקודם: `legId` נקבע אוטומטית לפי `getRelevantFlight()`/`currentLockerLegId()` (הרגל הראשונה מרגע יצירת הטיול, כל רגל הבאה 24 שעות לפני ה-`startAt` שלה). שינוי כיוון מוצרי, לא תיקון באג.

### ✏️ מה הוסר
- `<select id="lockerLegId">` וה-`<label>` שלו, כולל שני מפתחות `dashboardText` (he/en).
- `populateLockerLegSelect()`, `lockerCardForLeg()`, `lockerLegLabel()` - שלושתם שימשו רק את הבורר/האזהרה, נמחקו לגמרי (לא נשארו כקוד מת).
- ה-`change` listener על `lockerLegId`.
- כל בלוק אזהרת-הדריסה (`customConfirm` על "כבר יש לך פרטי טיסה שמורים ל...") - עם הבורר נעלם, אין יותר איך לבחור רגל "שגויה" מלכתחילה.
- `window.savePersonalLockerCard` חזרה להיות פונקציה רגילה (לא `async`) - ה-`await customConfirm(...)` היה הסיבה היחידה ל-`async`, ובלעדיו זה שריד מת.

### ✏️ מה נשאר, בכוונה (לא בוטל כחלק מה-commit הזה)
- שדות `flightNumber`/`flightTime` - עדיין שדות מידע שימושיים, בלי תלות בבורר שהוסר.
- `seatLayoutForLeg(legId)` - **לא** הוחזרה ל-`resolveActiveSeatLayout()` הישנה. עדיין קיימת כפונקציה נפרדת, אבל מוזנת עכשיו מ-`currentLockerLegId()` במקום מהבורר. הסיבה: יש הבדל אמיתי בין השתיים במקרה "אין טיסה רלוונטית כרגע" - `resolveActiveSeatLayout()` נופלת ל-`seatLayout` הכללי של הטיול, בעוד `seatLayoutForLeg()` נופלת קודם ל-seatLayout **של הרגל הראשונה עצמה** אם יש לה כזה. שיפור אמיתי שהיה שווה לשמר גם בלי הבורר שהביא אותו - תועד בהערה החדשה כדי שלא יוחזר בטעות ל-`resolveActiveSeatLayout()` בעתיד.

### 🔍 מה שנבדק - בפועל, עם `window.__test` זמני (הוסר ואומת שהוסר)
1. טיול דו-יעדי, "ברלין" כבר יצאה לדרך (`startAt` בעבר) ו"פריז" עוד 48 שעות (מחוץ לחלון) - `currentLockerLegId()` נופל נכון ל-"ברלין" (fallback), אין `<select>` בכלל בעמוד (`document.getElementById('lockerLegId')` מחזיר `null`).
2. מילוי מושב לברלין, שמירה - **בלי שום דיאלוג** (הוסר לגמרי), נשמר ישירות.
3. סימולציה של כניסת "פריז" לחלון 24 השעות שלה (`startAt` בעוד 10 שעות) - `currentLockerLegId()` עובר אוטומטית ל-"פריז" - **אותה חלונית בדיוק, נפתחת מחדש, "מדברת" על פריז ישר, בלי שום בחירה** - שדות ריקים (אין עדיין רשומה לפריז), כמו שצריך.
4. מילוי מושב לפריז, שמירה - שוב בלי דיאלוג, ישירות. הרשומה היחידה עכשיו `legId:"paris"` - ברלין הוחלפה, בדיוק כמו לפני שהבורר נוסף.
5. `node --check` - תקין. `i18n-audit` - 0 ממצאים. קונסול נקי.

v4.72.2 -> v4.72.3 (patch - שינוי כיוון/פישוט, אין שינוי סכימה - flightNumber/flightTime נשארים).

## v4.72.2 — תיקון: חלון אישור (customConfirm/customAlert) הוצג מתחת לחלונית פתוחה, לא מעליה

### 🎯 הבאג, כמו שדווח (נייד אמיתי)
בתוך `lockerModal` (כספת פרטים אישיים), עדכון מושב לרגל שכבר קיימת לה רשומה ואז שמירה לרגל אחרת הציג את חלון אזהרת-הדריסה **מתחת** לחלונית העריכה, לא מעליה - רוב הכפתורים לא נגישים, מה שנראה כמו נתונים "נדרסים באקראי".

### 🔍 מה נמצא (חקירה נפרדת, לפני התיקון)
`.custom-dialog-overlay` (shared.css, המחלקה שמריצה כל `customConfirm`/`customAlert` בכל 6 העמודים) עמד על `z-index: 999`, בעוד `.modal-overlay` (המכולה של כל חלונית bottom-sheet - `lockerModal`, `adminModal` וכו', index.html) עומד על `z-index: 9999`. **999 < 9999** - כל דיאלוג-אישור שנפתח בזמן שחלונית כלשהי כבר פתוחה נדחק מתחתיה, תמיד. אומת ישירות: `getComputedStyle` + screenshot אמיתי שהראה את כפתור "אישור" מציץ מאחורי `lockerModal`.

**לא ספציפי ל-`lockerModal` וגם לא חדש** - אותה בעיה קיימת מזמן ב-`adminCheckinUrl` (index.html, בחירת חברת תעופה כשכבר יש קישור שמור) - כל קריאה קיימת ל-`customConfirm()`/`customAlert()` מתוך חלונית `.modal-overlay` פתוחה נפגעת מאותה בעיה, פשוט לא נתפסה קודם.

**הכרעה, בעקבות דיון**: לא נפתח פרויקט סידור-מחדש לכל ה-z-index באפליקציה (יש כבר תיעוד לרשימה פתוחה בזיכרון) - רק תיקון ממוקד ליחס הספציפי הזה, שמתקן את כל המקרים (הווים ועתידיים) בבת אחת כי זה כלל CSS גלובלי אחד.

### ✏️ התיקון
`.custom-dialog-overlay` (`shared.css`): `z-index: 999` → `z-index: 10000` - מעל `.modal-overlay` (9999) בכל 6 העמודים. נבדק שאין קונפליקט חדש: הערך הגבוה הבא באפליקציה הוא `99999` (מסך טעינה ב-`wizard.html`, לא קשור לדיאלוגים בכלל, לא נפגע).

### 🔍 מה שנבדק - בפועל, קליקים אמיתיים (לא `querySelector`), עם `window.__test` זמני (הוסר ואומת שהוסר)
1. **תרחיש נייט המדויק**: פתיחת `lockerModal` עם רשומה קיימת לרגל 1 → עדכון מושב לאותה רגל, שמירה - בלי אזהרה (תקין, כבר אומת בעבר). בחירת רגל אחרת בבורר (`form_input` אמיתי על ה-`<select>`, לא `.value=` ישיר), שמירה - **חלון האישור מוצג במלואו מעל `lockerModal`**, שני הכפתורים נגישים לגמרי (screenshot מאשר). קליק אמיתי על "ביטול" - הרשומה נשארת ללא שינוי, החלון נסגר. קליק אמיתי נוסף (סבב שני) על "אישור" - הרשומה מתעדכנת לרגל החדשה. שניהם נבדקו בנפרד, כל אחד עם screenshot.
2. **הבונוס - `adminCheckinUrl`**: פתיחת פאנל האדמין, מילוי קישור צ'ק-אין קיים, בחירת חברת תעופה (`form_input` אמיתי על `#adminAirline`) - אותו חלון אישור, מוצג נכון **מעל** `adminModal`. קליק אמיתי על "אישור" - השדה מתעדכן לקישור הנכון של החברה שנבחרה.
3. קונסול נקי - גם בטאב שבו בוצעו הבדיקות, גם בטאב טרי נפרד.

v4.72.1 -> v4.72.2 (patch - תיקון UI, אין שינוי סכימה).

## v4.72.1 — כספת אישית: בורר טיסה מפורש (תיקון ל-commit 1/4, לפני commit 2)

### 🎯 הבעיה שנחקרה ואושרה לתיקון
`lockerModal` (v4.72.0) לא הציגה שום בורר רגל/יעד - ה-`legId` שנשמר נגזר תמיד מ-`currentLockerLegId()`, פונקציה טהורה-זמן (`getRelevantFlight()`, legs.js) בלי שום קלט מהמשתמש. בפועל: אין דרך לבחור במפורש רגל שנייה - רק לחכות שהזמן האמיתי ייכנס ל-24 השעות שלפני ההמראה שלה, או לערוך זמנית תאריכים בפאנל האדמין. תועד כממצא בתשובה נפרדת לפני הקוד הזה.

### ✏️ התיקון
- **`lockerLegId`** - `<select>` חדש ב-`lockerModal`, בין בחירת השם למספר הטיסה. אפשרויות: כל `currentLegs` (לפי שם) + "טיסת החזרה" בסוף תמיד (`populateLockerLegSelect()`). ברירת מחדל: בדיוק מה ש-`currentLockerLegId()` היה מחזיר היום - כך שבטיול חד-יעדי, או כשנפתח בחלון הזמן הרלוונטי, הבורר כבר פותח על האפשרות ההגיונית ולא דורש נגיעה.
- **`loadSelectedLockerCard()`/`savePersonalLockerCard()`** - עברו מ-`currentLockerLegId()`/`currentUserLockerCard()` לבורר המפורש (`lockerCardForLeg(legId)` חדש, אותו כלל בדיוק כמו `currentUserLockerCard()` אבל עם `legId` כפרמטר). שינוי הבורר בזמן שהחלונית פתוחה טוען מחדש את השדות לרגל שנבחרה (`change` listener חדש, אותו דפוס כמו `lockerMemberName` הקיים).
- **`getRelevantFlight()`/`currentLockerLegId()`/`currentUserLockerCard()` - לא נגעו בהם.** עדיין קובעים מתי מוצג הבאנר והסיכום בדף הבית (`updateLockerSummary()`) - דאגה נפרדת לגמרי, כמו שסוכם.
- **אזהרת הדריסה (מ-commit 1)** - נשארת, רק מושווית עכשיו מול הרגל שנבחרה בבורר (`targetLegId` מהבורר) במקום `currentLockerLegId()`.
- **ממצא נוסף, תוקן תוך כדי (לא התבקש במפורש - ראו הערה)**: ולידציית פורמט המושב (`resolveActiveSeatLayout()`) הייתה גם היא זמן-מבוססת, ולא הייתה תואמת יותר לבורר החדש - מושב תקין לרגל שנבחרה יכול היה להידחות (או ההפך) כי הוולידציה בדקה תצורת מושבים של רגל אחרת (הרלוונטית-בזמן). הוסף `seatLayoutForLeg(legId)` חדש (אותה נוסחה כמו `resolveActiveSeatLayout()`, בלי חיפוש הזמן) - `savePersonalLockerCard()` משתמשת בו עם הרגל מהבורר. `resolveActiveSeatLayout()` עצמה נשארה בדיוק כמו שהיא (עדיין משרתת רק את מסך מפת המושבים) - רק תוקן ההערה שלה, שתיארה אותה כמשותפת עם `savePersonalLockerCard()` וזה כבר לא נכון.

### 🔍 מה שנבדק - בפועל, עם `window.__test` זמני (הוסר ואומת שהוסר)
1. טיול רב-יעדים - הבורר מציג את כל היעדים + "טיסת החזרה" בסוף, ברירת מחדל נכונה.
2. בחירת רגל שונה מזו שכבר קיימת רשומה לה - אזהרת דריסה עם שם הרגל **הקיימת**, כמו קודם. אישור אמיתי (`computer` click) מעדכן לרגל החדשה; שמירה חוזרת לאותה רגל - בלי אזהרה.
3. טיול חד-יעדי - הבורר מציג 2 אפשרויות (היעד + טיסת החזרה), ברירת מחדל על היעד, לא מפריע ויזואלית (screenshot).
4. עברית ואנגלית, שולחן ו-375px.
5. **בדיקה נוספת (הממצא בסעיף התיקון)**: מושב תקין לפי תצורת הרגל **הנבחרת** (`2-4-2`, האות G קיימת) נשמר בהצלחה; אותו מושב בדיוק, כשנבחרה רגל עם תצורה שונה (`3-3`, אין G) - נדחה עם הודעת השגיאה הנכונה לתצורה של הרגל הנבחרת, לא של רגל אחרת.
6. `node --check` על ה-script הקלאסי - תקין. `i18n-audit` - 0 ממצאים. קונסול נקי.

v4.72.0 -> v4.72.1 (patch - תיקון התנהגות, אין שינוי סכימה - עדיין אותם שדות מ-commit 1).

## v4.72.0 — כספת אישית: מספר טיסה + שעת המראה, commit 1/4 (סריקת כרטיס טיסה)

### 🎯 מה השתנה
`personalLocker` (index.html) מקבל שני שדות חדשים - `flightNumber`, `flightTime` - לצד השדה הקיים `seat` (שהופך רשמית לא-חובה; ראו ממצא בהמשך). זה commit 1/4 בלבד: סכימה + UI ידני. שום מצלמה/פענוח ברקוד עדיין (commit 2).

### 🔍 ממצא לפני הקוד - seat כבר לא היה חוסם שמירה
נבדק הקוד הקיים לפני השינוי: `savePersonalLockerCard()` בדקה `if (!name || !currentUser) return` בלבד - שום תנאי לא חסם שמירה עם `seat` ריק, גם לפני הקומיט הזה. הבדיקה `seat && seat !== previousSeat` מדלגת על ולידציית הפורמט כש-`seat` ריק. כלומר: מושב היה אופציונלי בפועל עוד לפני השינוי הזה - שום קוד לא שונה כדי "להפוך אותו לאופציונלי", זה רק אומת ותועד (בדיקה 2 ברשימה למטה עוברת, לא בזכות שינוי כלשהו כאן).

### ✏️ המימוש
- **UI** - שני שדות טקסט רגילים (`lockerFlightNumber`, `lockerFlightTime`) בחלונית הקיימת (`lockerModal`), בין בחירת השם למספר המושב - עברית ואנגלית, שני השדות ב-`dashboardText`.
- **שמירה** - `personalLockerCards[uid]` כולל עכשיו `flightNumber`/`flightTime` לצד השדות הקיימים. `flightNumber` עובר `toUpperCase()` (כמו `seat`), `flightTime` נשמר כמחרוזת חופשית כמו שהוקלדה - שדה טקסט בלבד ב-commit הזה, בלי ולידציית פורמט.
- **אזהרת דריסה** - `customConfirm()` חדש: אם כבר קיימת רשומה (`personalLockerCards[uid]`) ל-`legId` **שונה** מזה שנשמר עכשיו, מוצגת אזהרה עם שם הרגל הקיים ("כבר יש לך פרטי טיסה שמורים לרגל \"X\" / לטיסת החזרה - שמירה תחליף אותם") לפני הדריסה. שמירה חוזרת לאותו legId (עדכון, לא דריסת רגל אחרת) לא מציגה שום דבר - עובר ישר לשמירה, כמו קודם. ההשוואה מול הרשומה הגולמית (`personalLockerCards[uid]`), לא מול `currentUserLockerCard()` (שכבר מסננת לפי leg תואם ומחזירה `null` בדיוק במקרה הזה) - שומר על ההגנה הקיימת "רשומה אחת, בלי הרחבת סכימה ל-per-leg" (ר' ההערה הקיימת בקוד), רק מוסיף אזהרה לפני מה שהיה קודם דריסה שקטה.

### 🔍 מה שנבדק - בפועל, עם `window.__test` זמני (הוסר ואומת שהוסר)
1. שמירה עם כל השדות כולל מושב תקין (`12C`) - נשמר בהצלחה, בלי שגיאת ולידציה.
2. שמירה בלי מושב - נשמר בהצלחה (ממצא: זה כבר עבד ככה, ר' למעלה).
3. שמירה לרגל **שונה** מזו שכבר שמורה - אזהרת דריסה מוצגת עם שם הרגל **הקיים** (לא זה החדש) - נבדק גם עברית ("...לרגל \"אתונה\"...") וגם אנגלית ("...for the \"Santorini\" leg...") - `customConfirm` אמיתי, `screenshot` מאשר, "אישור" אמיתי (`computer` click) בוצע ואומת שהרשומה אכן התעדכנה לרגל החדש אחריו.
4. שמירה חוזרת **לאותה** רגל (עדכון) - בלי אזהרה בכלל, שמירה ישירה.
5. עברית ואנגלית, שולחן ו-375px - החלונית עצמה (לא רק ההודעה) נבדקה ב-screenshot בכל השילובים.
6. `node --check` על ה-script הקלאסי של index.html - תקין. `i18n-audit` - 0 ממצאים.

### הערה למי שממשיך ל-commit 2
מסלול ה-RTDB האמיתי (`db.ref(...).set(...)`) לא נבדק מקצה-לקצה - אין גישת רשת אמיתית בסביבת הבדיקה המקומית (ר' CLAUDE.md). כל הבדיקות למעלה בוצעו בלי `tripId` ב-URL (כדי ש-`tripRoot` יהיה `null` ולדלג על הכתיבה בפועל), ותקפות ללוגיקת הטופס/האזהרה/השמירה ל-state המקומי בלבד.

v4.71.0 -> v4.72.0 (minor - שינוי סכימה, שני שדות חדשים).

## v4.71.0 — מצב ריק בטאב "מפה": כפתור אמיתי לטאב "מקומות", commit 5/5 (מעבר מקומות ל-itinerary.html)

### 🎯 מה השתנה
מצב ריק של טאב המפה (אין אף מקום עם קואורדינטות עבור הרגל הפעילה) קיבל כפתור אמיתי מתחת לטקסט הקיים - "מעבר לטאב \"מקומות\"" - שקורא ל-`setTab('places')` באותו עמוד, במקום להשאיר את המשתמש עם טקסט סטטי בלבד. רלוונטי רק עכשיו - מאז commit 3/5 הטאבים סמוכים באותו עמוד (לא שני עמודים נפרדים כמו לפני המעבר), אז מעבר טאב פנימי הוא הפעולה ההגיונית.

### ✏️ המימוש
- **`mapView.js`** - `renderTripMap()` מקבלת `options.onEmptyAction` אופציונלי (callback). הקובץ הזה בכוונה לא מכיר את שם פונקציית מעבר-הטאבים של הדף המארח (`setTab` שייכת ל-itinerary.html, לא ל-mapView.js - ר' ההערה הקיימת בראש הקובץ על היותו עצמאי-מכל-עמוד) - לכן זה callback גנרי, לא קריאה ישירה בשם. אם אין callback (קורא עתידי שלא מעביר אחד) - אין כפתור, רק הטקסט כמו קודם, בלי שינוי התנהגות.
- **`itinerary.html`** - `renderMap()` מעביר `onEmptyAction:()=>setTab('places')`.
- כפתור עם `class="primary-btn"` - קלאס קיים כבר ב-itinerary.html (לא CSS חדש), אותו סגנון בדיוק כמו שאר הכפתורים הראשיים בעמוד.

### 🔍 מה שנבדק - בפועל, עם `window.__test` זמני (הוסר ואומת שהוסר)
1. **רגל בלי מקומות ממוקמים** - מסך ריק עם הטקסט + כפתור, נבדק ב-screenshot.
2. **לחיצה אמיתית על הכפתור** (`computer` click, לא `.click()`) - עובר לטאב "מקומות" בהצלחה, אומת גם ויזואלית וגם דרך מצב הטאב הפנימי.
3. **הופעת סיכה** - הוזן מקום מדומה עם `lat`/`lon` ל-state, `renderMap()` הורץ מחדש - הסיכה מופיעה (מנגנון זהה למה שכבר נבדק ב-commit 3/5; לא ניתן לבדוק את מסלול ה-geocoding/RTDB האמיתי-מקצה-לקצה מקומית, ר' מגבלת הבדיקה המתועדת ב-CLAUDE.md).
4. עברית ואנגלית, שולחן ו-375px - כל השילובים נבדקו ב-screenshot, כולל RTL/LTR.
5. `node --check` על `mapView.js` ועל ה-JS המחולץ מ-itinerary.html - תקין. `i18n-audit` - 0 ממצאים.

v4.70.5 -> v4.71.0 (minor - תכונה חדשה, עקבי עם שאר ה-commits במעבר מקומות ל-itinerary.html שכולם עלו minor).

## v4.70.5 — כפתור הוצאות: הודעת מארגן עם קישור אמיתי (itinerary/places/packing.html)

### 🎯 הבעיה שנחקרה
דווח: לחיצה על כפתור $ (הוצאות) בסרגל הניווט התחתון עובדת נכון מ-`index.html`, אבל מכל עמוד אחר מציגה למארגן את ההודעה "המארגן עדיין לא הוסיף קישור" - כאילו הוא לא מזוהה כמארגן שם.

### 🔍 מה נמצא בחקירה (דווח בנפרד, לפני הקוד)
`isOrganizer()` לא הייתה קיימת כלל ב-`itinerary.html`/`places.html`/`packing.html` - ה-`else` (אין `spliitUrl`) הציג את אותה הודעה לכולם, מארגן ואורח כאחד, בלי בדיקת זהות. לא היה "אי-זיהוי" - הבדיקה לא הייתה שם. ה-URL/הנתונים (`trip.spliitUrl`) נטענים נכון בכל עמוד (`configureLinks()` רץ שוב בתוך ה-`onValue` listener של הטיול). הבאג האמיתי בפועל (למה למארגן חסר קישור בעמודים האלה בזמן שיש לו ב-index.html) נשאר לא-מוסבר סטטית - ייתכן קאש/tripId שונה - לא רלוונטי לתיקון שאושר כאן, שמטפל במסר, לא בזיהוי הבאג המקורי.

### ✏️ התיקון שאושר - שתי הודעות שונות לפי isOrganizer(), לא בניה מחדש של מסך ההוספה
- **`permissions.js` חדש** - `window.isOrganizer(user, ownerId)`, פונקציה טהורה בלי סגירה על משתנים גלובליים ספציפיים-לעמוד (כי `index.html`/`shopping.html` קוראים למשתנים `currentUser`/`tripOwnerId`, ואילו שלושת העמודים האלה קוראים להם `user`/`trip.ownerId`). נטען כ-script קלאסי, אותה תבנית כמו `legs.js`/`datetime.js`.
- **למה קובץ חדש ולא שכפול רביעי/חמישי** - `isOrganizer()` כבר הייתה קיימת (זהה) ב-`index.html` ו-`shopping.html`. שכפול לשלושת העמודים האלה היה מייצר 5 עותקים בבת אחת - אותו סף בדיוק שהצדיק את יצירת `datetime.js` בעבר (2 עותקים + עותק שלישי בדרך). `index.html`/`shopping.html` **לא** מוגרים למשותף בקומיט הזה - נשארים עם ההגדרה המקומית שלהם, כמו תבנית "שתי מערכות טוסט במקביל" הקיימת כבר עם toast.js.
- **מארגן, בלי `spliitUrl`**: דיאלוג (לא toast חולף) עם קישור אמיתי חזרה ל-`index.html` (`scoped('index.html')`, כולל `tripId`) - "עדיין לא הוספתם קישור להתחשבנות - **לחצו כאן** כדי לחזור לדף הבית ולהוסיף."
- **משתתף רגיל, בלי `spliitUrl`**: ההודעה הקיימת נשארת מילה-במילה כמו שהייתה - "המארגן עדיין לא הוסיף קישור להתחשבנות - ניתן להוסיף מדף הבית".
- מסך ההוספה עצמו (`spliitGate` ב-index.html) לא נבנה מחדש כאן ולא שוכפל - המארגן מגיע אליו דרך הקישור, בדיוק כמו שהיה לפני.

### 🔍 מה שנבדק - בפועל, בשלושת העמודים בנפרד
עם `window.__test` זמני (`trip`/`user` מדומים, הוסר ואומת שהוסר לפני push) בכל אחד משלושת העמודים בנפרד:
1. **מארגן בלי קישור** - דיאלוג מוצג, `<a href>` מכיל את ה-`tripId` הנכון, טקסט עברית ואנגלית שניהם נבדקו.
2. **משתתף רגיל בלי קישור** - ה-toast הישן, מילה-במילה, בלי דיאלוג.
3. **קישור קיים** - `href`/`target` מוגדרים ישירות לקישור האמיתי, בלי `onclick` בכלל (בדיוק כמו לפני השינוי) - regression=0 על הנתיב הזה.
4. קונסול נקי (בדיקה בטאב טרי לכל עמוד), `node scripts/i18n-audit.js` - 0 ממצאים בשלושתם, `node --check` על ה-JS המחולץ - תקין בשלושתם.
5. `index.html`/`shopping.html` - לא נגעו בהם כלל בקומיט הזה.

v4.70.4 -> v4.70.5 (patch - תיקון הודעה/UX, בלי שינוי סכימה).

## v4.70.4 — אייקון "זיכרונות" עודכן ל-📸 (index.html)

### 🎯 מה השתנה
בכל מקום ב-`index.html` שהציג את האייקון הישן ("Round pushpin" - סיכת מיקום, 3D+SVG fallback, או האימוג'י 📍) עבור מודול "זיכרונות" - הוחלף ל-📸 (מצלמה עם פלאש), עקבי עם שאר אייקוני הקוביות (🎒🛒📅💰).

### ✏️ 4 מקומות שנמצאו ועודכנו
1. **הכרטיס ברשת הקוביות** - התמונה המאוירת (`illustrated-icon`) הוחלפה ל-`Camera with flash/3D/camera_with_flash_3d.png` (fluentui-emoji, אומת ישירות מול ה-CDN לפני שימוש - לא ניחוש). ה-SVG fallback (מוצג רק אם התמונה נכשלת לטעון) הוחלף לאייקון "camera" קו-מתאר של Tabler (אותו מקור MIT שכבר משמש את map-pin.svg באפליקציה) - בלי גרסת "עם פלאש" נפרדת בסט הזה, התמונה התלת-ממדית כבר נושאת את פרט הפלאש.
2. **checkbox פאנל האדמין** - `adminFeatPlacesLabel` (markup + עברית + אנגלית).
3. **הבאנר החכם** - שדה `icon` באובייקט ה-fallback "places" (`preferredFeature()`).
4. `grep` על כל הקובץ אחרי השינוי - 0 מופעים נותרים של 📍/round_pushpin הקשורים למודול הזה.

### 🔍 מה שנבדק - בפועל
- **התמונה האמיתית** - נטענה בהצלחה (`naturalWidth:256`), מוצגת נכון, מאומתת ב-screenshot.
- **ה-SVG fallback** - הופעל ידנית (שבירת ה-`src` בכוונה) ואומת ש-fallback אכן מוצג נכון - אייקון מצלמה קו-מתאר, בלי הפניה חיצונית.
- **checkbox פאנל האדמין** - `textContent` נבדק ישירות: "📸 זיכרונות".
- `i18n-audit` - 0 ממצאים חדשים.

v4.70.3 -> v4.70.4 (patch - עדכון עקביות ויזואלית, בלי שינוי סכימה).

## v4.70.3 — תיקון: כפתור ☰ קרס בשקט בתוך tripPicker (index.html)

### 🎯 הבאג
בתוך מסך "הטיולים שלי" (`tripPicker`, מגיע כש-`tripId` חסר), לחיצה על כפתור ה-☰ לא עשתה כלום - לא פתחה תפריט, לא סגרה כלום.

### 🔍 מה נמצא - קליק אמיתי, לא רק קריאת קוד
קריאה ל-`window.openHamburgerMenu()` שכפלה שגיאה מפורשת: `TypeError: Failed to construct 'URL': Invalid URL` בתוך `updateAdminShareLink()`. הסיבה: `scopedTripUrl("index.html")` מחזירה, בכוונה, מחרוזת יחסית כש-`!tripId` - אבל `updateAdminShareLink()` עושה `new URL(...)` **בלי ארגומנט base**, שזורק על כל קלט לא-מוחלט. `openHamburgerMenu()` קורא ל-`updateAdminShareLink()` *לפני* `openSheet()` - החריגה הבלתי-מטופלת עוצרת הכל, כולל פתיחת הגיליון עצמו. קרה בכל פעם, לא רק במקרה קצה - כי `tripId` תמיד חסר במסך הזה בדיוק.

לא regression מעבודה קודמת - קוד ישן (`openHamburgerMenu`/`updateAdminShareLink`/`tripPicker`), נתפס בחקירה נפרדת.

### ✏️ התיקון - guard יחיד, כמו שאושר (אפשרות א)
`if (!tripId) return;` בתחילת `updateAdminShareLink()` - מדלג על חישוב קישור השיתוף (אין למה לשתף בלי טיול פעיל בכל מקרה), לא נוגע בשום דבר אחר. `hamburgerShareBtn`/`adminShareWhatsappBtn` כבר `class="menu-item hidden"` כברירת מחדל - לא נחשפים בהקשר הזה, לא תוקן/שונה.

### 🔍 מה שנבדק - בפועל
1. **קליק אמיתי על הכפתור** (`computer` click, לא `.click()`) בתוך tripPicker מדומה - התפריט נפתח (`.open` class נוסף) - לפני התיקון זה לא קרה בכלל.
2. **"מצב כהה"/"יצירת טיול חדש"/"יציאה"** - מוצגים ותקינים (שלושתם מוצגים לפי `Boolean(user)` בלבד, לא לפי `tripId` - אומת שהם גלויים ומקושרים נכון כשמדמים משתמש מחובר). **צילום מסך אמיתי** מאשר: 4 פריטים בתפריט (מצב בהיר/הטיולים שלי/יצירת טיול חדש/יציאה), בלי "שיתוף ההזמנה".
3. **`hamburgerShareBtn` נשאר `hidden`** - אומת ישירות אחרי הפתיחה.
4. **אין regression בנתיב הרגיל** - עם `tripId` אמיתי, `updateAdminShareLink()` עדיין מחשב קישור `wa.me` תקין במלואו, בלי guard חוסם.
5. קונסול נקי בטאב טרי, `i18n-audit` - 0 ממצאים.

v4.70.2 -> v4.70.3 (patch - תיקון באג ישן, בלי שינוי סכימה).

## v4.70.2 — תיקון: מפת המסלול לא הגיבה חי לשינוי מצב כהה/בהיר (itinerary.html)

### 🎯 הבאג
כשטאב "מפה" כבר פתוח ב-itinerary.html, מעבר בין מצב כהה/בהיר לא שינה את אריחי המפה - נשארה תקועה על מה שהיה בכניסה. עבד נכון ב-places.html המקורי.

### 🔍 מה נמצא
`applyTheme()` ב-itinerary.html **לא קרא בכלל** ל-`window.updateMapTheme()` - השורה הזו הייתה קיימת ב-`applyTheme()` של places.html, ופשוט לא הועברה ב-commit 3/5 (מעבר טאב המפה ל-itinerary.html). `grep` על הקובץ כולו אישר: אפס קריאות ל-`updateMapTheme` בכל הקובץ.

### ✏️ התיקון
שורה אחת: `if(window.updateMapTheme)window.updateMapTheme()` בסוף `applyTheme()`, זהה למה שכבר קיים ב-places.html.

### 🔍 מה שנבדק - בפועל, לא רק קריאת קוד
מפה פתוחה עם סיכה אמיתית, מצב כהה: `tileBefore` = CartoDB `dark_all`. לחיצה על `themeBtn` (כפתור אמיתי, לא קריאת פונקציה ישירה): `tileAfter` = OSM `tile.openstreetmap.org` - **הוחלף מיידית**, בלי כניסה חוזרת לטאב. נבדק גם בכיוון ההפוך (בהיר→כהה) - חזר נכון ל-CartoDB. קונסול נקי בטאב טרי.

v4.70.1 -> v4.70.2 (patch - תיקון regression מ-commit 3/5, בלי שינוי סכימה).

## v4.70.1 — תיקון: "מקומות ותמונות" נשאר ב-6 מקומות נוספים בדף הבית (index.html)

### 🎯 הבאג
commit 4/5 (מעבר מקומות ל-itinerary.html) עדכן את תווית הניווט התחתון ל"זיכרונות" בכל 6 העמודים, אבל לא סרק את שאר המקומות ב-`index.html` עצמו שמזכירים את השם הישן - נתפס בחקירה נפרדת, לא היה ידוע מראש.

### ✏️ מה תוקן - 6 מקומות, כולם ב-index.html
1. **הכרטיס בדף הבית** - כותרת "מקומות ותמונות"→"זיכרונות", תת-כותרת "נקודות עניין ומוזיקה"→"תמונות ומוזיקה" (כבר לא מדויקת - הדף לא מציג יותר "נקודות עניין").
2. **`dashboardText` עברית** - `placesTitle`/`placesDesc` (אותה תבנית).
3. **`dashboardText` אנגלית** - `placesTitle: "Memories"`, `placesDesc: "Photos and music"`.
4. **פאנל האדמין** - תווית checkbox המודול, עברית+אנגלית.
5. **הבאנר החכם** - אובייקט ה-fallback "places" (`preferredFeature()`): כותרת-משנה+טקסט כפתור עודכנו ("הזיכרונות שלכם זמינים בלחיצה אחת" / "לזיכרונות").
6. **מסך "הטיול הסתיים"** - תת-כותרת "חוזרים למקומות..."→"חוזרים לזיכרונות...".

### 🔍 מה שנבדק
`grep` על `index.html` כולו אחרי השינוי - 0 מופעים נותרים של "מקומות ותמונות"/"Places & photos"/"נקודות עניין ומוזיקה". `i18n-audit` - 0 ממצאים.

v4.70.0 -> v4.70.1 (patch - תיקון תווית לא-שלמה, בלי שינוי סכימה).

## v4.70.0 — places.html מתנקה, תווית "זיכרונות", commit 4/5 (מעבר מקומות ל-itinerary.html)

### 🎯 מה נוסף/השתנה
`places.html` מאבד את הטאבים "מקומות"/"מפה" (שכבר עברו במלואם ל-itinerary.html ב-commits 2-3/5) - נשארים "תמונות"/"מוזיקה" בלבד, פעילים כרגיל. הניווט התחתון בכל 6 הדפים מציג "זיכרונות" 📸 במקום "מקומות" עבור הקישור ל-`places.html` (ה-URL עצמו לא השתנה - קישורים/סימניות ישנים ימשיכו לעבוד).

### ✏️ מה הוסר, מה נשאר
- **הוסר**: כל 19 הפונקציות/markup שהועברו ב-commit 2-3 (`categories`, `savePlace`, `renderPlaces`, `renderMap`, `openPlaceForm`, `openDayPicker`, GEO_RULES וכו') + 4 תגי head (leaflet.css/js, mapView.js, geocode.js) + כל ה-CSS הייחודי להם (טולבר/חיפוש/AI-suggest/מפה).
- **נשאר, לא נגוע**: `renderPhotos`/`renderMusic`/כל מנגנון התגובות-ריאקציות-אפלود ותיוג מוזיקה.
- **`.place-card,.music-card` → `.music-card` בלבד**; `.place-top`/`.place-icon`/`.place-copy`/`.entry-meta`/`.tag`/`.card-top-actions`/`.filter-chips`/`.cards`/`.primary-btn` **נשארו** - אלה קלאסים משותפים שכרטיס המוזיקה עצמו כבר משתמש בהם (לא "places-only" כמו שהשם עשוי לרמז).
- **`.tabs{grid-template-columns:repeat(2,1fr)}`** - תוקן ממילא כתוצאה של הסרת 2 טאבים (4→2) - זה בדיוק אותו באג-חוסר-התאמה שכבר נתפס קודם (task נפרד, נזנח עכשיו כי הוא כבר לא רלוונטי).
- **`updateFab()`/`fabAction()`/`setTab()`** - הצטמצמו ל-2 טאבים בלבד, בלי לוגיקת "מפה"/"מקומות".

### 🐛 באג ה-GEO_RULES (מ-v4.68.0) - נעלם, לא "תוקן"
כל הבלוק שהכיל את הבאג (`categories()`/`GEO_RULES`/וכו') הוסר לגמרי מהקובץ הזה - אין יותר קוד שיכול לזרוק את ה-`ReferenceError`. לא reorder כמו ב-itinerary.html (שם הקוד עדיין נחוץ) - כאן הוא פשוט לא קיים יותר.

### 🔍 מה שנבדק בפועל
1. **places.html נטען בלי שגיאות** - נבדק בטאב נקי, כולל תרחיש ה-bootstrap עם שפה שמורה (אותו תרחיש שחשף את באג ה-GEO_RULES ב-v4.68.0) - עכשיו נקי כי אין `categories()` לזרוק שגיאה.
2. **מוזיקה+תמונות עובדים במלואם** - נבדק: מעבר טאבים, FAB פותח את הטופס הנכון בכל טאב (עם ה-toast הרגיל "בחרו קודם שם" כשאין פרופיל - לא נשבר), ולידציה בסיסית.
3. **תווית "זיכרונות" בניווט התחתון** - נבדק בפועל בשני דפים שונים (`itinerary.html`, `index.html`) + באותו הדף עצמו - כולם מציגים "זיכרונות". **הבחנה חשובה**: itinerary.html מכיל *גם* טאב פנימי בשם "מקומות" (רשימת המקומות שעברה אליו) - זה **לא** שונה, בכוונה - זה תיאור מדויק של מה שהטאב עושה, לא קשור לניווט התחתון ששמו הוחלף.
4. **אין קוד מת** - נבדק בפועל: `document.getElementById()` על כל אלמנט שהוסר (placeOverlay, dayPickerOverlay, mapView, mapContainer, placesTab, mapTab, legFilterSelect, placeFilters, placeSuggestions, placesList) מחזיר `null`. `i18n-audit` - 0 ממצאים חדשים (3 שנמצאו ב-shopping.html לא קשורים - כפתורי שפת-ייצוא קיימים מראש).

v4.69.0 -> v4.70.0 (minor - שינוי UI גלוי, בלי שינוי סכימה - places/{id} עדיין לא השתנה).

## v4.69.0 — טאב המפה עובר ל-itinerary.html, commit 3/5 (מעבר מקומות ל-itinerary.html)

### 🎯 מה נוסף
טאב שלישי, "מפה", ב-itinerary.html - אותה מפת Leaflet+CartoDB (סיכות צבועות, אנימציית כניסה, אריחים כהים/בהירים לפי theme) שכבר קיימת ב-places.html, עכשיו לצד "מקומות"/"תוכנית יומית".

### ✏️ מה הועבר, מה הותאם
- **`window.renderTripMap()`/`mapView.js` - 0 שינוי, בדיוק כמצופה**: הקובץ עצמו הוא classic script עצמאי, window-attached - לא נגעתי בו בכלל. כל מה שהיה צריך זה 3 תגי `<head>` (`leaflet.css`, `leaflet.js`, `mapView.js` - `geocode.js` כבר היה שם מ-commit 2/5, אז לא 4 כמו שתוכנן במקור), בלוק CSS (~15 כללים, verbatim מ-places.html), ו-`renderMap()` - השכבה הדקה שכבר הייתה placeholder ריק מ-commit 2/5, עכשיו עם המימוש האמיתי (verbatim).
- **`.tabs{grid-template-columns:repeat(3,1fr)}`** - עודכן מ-2 ל-3 כדי לשקף את הטאב השלישי בפועל - **בכוונה לא נופל לאותה מלכודת** שנתפסה ב-places.html (4 טאבים אבל `repeat(3,1fr)`, לא תוקן שם, מחוץ להיקף).
- **`updateFab()`/`setTab()`**: FAB מוסתר שוב בטאב "מפה" (אין פעולת-הוספה שם) - חוזר לאותה התנהגות כמו ב-places.html, אחרי ש-commit 2/5 השאיר אותו גלוי בכוונה (כי שני הטאבים דאז כן היו עם פעולה).

### 🔍 מה שנבדק - עם hook זמני (`window.__test`), הוסר ואומת שהוסר לפני push
1. **רינדור מפה עם סיכות אמיתיות** - נבדק חזותית: אריחים כהים, ייחוס CARTO תקין, 2 סיכות צבועות נכון במיקום גיאוגרפי נכון (פריז), FAB מוסתר.
2. **מעבר טאבים חזרה-וקדימה** (מפה→מקומות→יומי→מפה) - המפה נבנית מחדש נכון בכל כניסה, בלי הבהוב (תיקון ה-flicker מ-v4.66.1 עבר יחד עם ה-CSS, נבדק).
3. קונסול נקי בטאב נקי (לא רק אותו טאב שבו נבדק כל השאר), `i18n-audit` - 0 ממצאים.

v4.68.0 -> v4.69.0 (minor - תכונה חדשה גלויה למשתמש, בלי שינוי סכימה).

## v4.68.0 — רשימת המקומות עוברת ל-itinerary.html, commit 2/5 (מעבר מקומות ל-itinerary.html)

### 🎯 מה נוסף
טאב "מקומות" ב-itinerary.html (מסגרת בלבד מ-commit 1/5) מקבל תוכן אמיתי: רשימת המקומות המלאה - חיפוש, סינון לפי קטגוריה/יעד, הצעות AI, הוספה/עריכה/מחיקה, וה-geocoding בזמן שמירה (לטובת טאב המפה שיגיע ב-commit 3/5). `places.html` עצמו **לא נגע** בקוד הזה - נשאר עובד במקביל, יימחק/יעודכן רק ב-commit 4/5.

### ✏️ מה הועבר, מה הותאם
- **~19 פונקציות + כל ה-markup הרלוונטי** (`placeOverlay`, `dayPickerOverlay`, תוכן `#placesView`) - הועברו כמעט מילה-במילה מ-places.html.
- **`places` (לא `savedPlaces`)**: itinerary.html כבר טען `trip.places.items` בשם `savedPlaces` (ל-autocomplete של שם פעילות בלבד) - שונה לשם `places` בכל מקום, כדי שיתאים למוסכמה של places.html ולא יהיו שני שמות לאותו דבר.
- **`canManageEntry()` במקום `canDelete()` נוסף**: itinerary.html כבר יש לו `canManageEntry()` (אותה לוגיקה בדיוק) - שורת הקריאה היחידה ב-`renderPlaces()` הותאמה לזה, לא נוסף כפילות.
- **`renderMap()` - placeholder ריק בלבד**: `renderPlaceLegFilter()` (הועתקה verbatim) קוראת ל-`renderMap()` ב-onchange שלה - הוגדרה כ-no-op זמני כדי לא לזרוק שגיאה; commit 3/5 יחליף אותה במימוש האמיתי.
- **`legIdForDay()` - לא מומש, בניגוד למה שהוערך בחקירה הקודמת**: בבדיקה מול הקוד בפועל (לא רק תיאוריה) התברר ש-`activeLegFilter` הוא state עצמאי, מונע ע"י `<select>` משלו (`renderPlaceLegFilter()`), **לא** נגזר מ-`activeDay`/היום הנוכחי בכלל - וגם `openDayPicker()` כבר מקבל leg ישירות מ-`tripDayDates()`, בלי צורך בעזר נוסף. פונקציה כזו הייתה קוד מת מהיום הראשון - לא נכתבה.

### 🐛 באג אמיתי שנתפס בבדיקה - `GEO_RULES` (תוקן, לא רק תועד)
בדיקה בדפדפן (לא רק קריאת קוד) חשפה `ReferenceError: Cannot access 'GEO_RULES' before initialization` - מוסתר לגמרי ע"י ה-`catch(e){}` הריק של ה-bootstrap שרץ בכל טעינת עמוד עם שפה שמורה ב-localStorage (רץ *לפני* Firebase). אומת ישירות ש**אותו באג בדיוק קיים כבר היום ב-places.html המקורי, ללא שינוי** - לא נגרם ע"י המעבר הזה, אבל תוקן **רק ב-itinerary.html** (מיקום מחדש של הבלוק לפני קריאת ה-bootstrap, לא אחריה) כי זה בדיוק הקוד שהועבר עכשיו. לא תוקן ב-places.html - מחוץ להיקף, וגם ממילא יימחק שם ב-commit 4/5.

### 📝 CLAUDE.md
נוספה שורה מתעדת את הכפילות `addPlaceToDay()`/`openActivityForm()` (שתיהן כותבות לאותו path) - הכרעה מפורשת: לא לאחד עכשיו.

### 🔍 מה שנבדק - עם hook זמני (`window.__test`), הוסר ואומת שהוסר לפני push
1. **רשימת מקומות עם נתונים אמיתיים** - כרטיסים, אייקונים לפי קטגוריה, תגית קטגוריה, הערה - נבדק חזותית מול screenshot.
2. **סינון קטגוריה וחיפוש** - נבדקו בנפרד, שניהם מסננים נכון.
3. **טופס הוספת מקום, חד-leg ורב-leg** - שדה היעד מוסתר/מוצג נכון, אפשרויות הקטגוריה מתעדכנות לפי `geoRefLeg`.
4. **FAB בשני הטאבים** - "מקומות" פותח טופס הוספת מקום; "תוכנית יומית" ממשיך לפתוח טופס פעילות בדיוק כמו לפני (לא נשבר).
5. **תיקון ה-GEO_RULES** - נבדק לפני ואחרי: `placeCategory` ריק לגמרי לפני התיקון, מלא נכון (6 קטגוריות) אחריו, באותו תרחיש bootstrap מדויק.
6. קונסול נקי, `i18n-audit` - 0 ממצאים.

v4.67.0 -> v4.68.0 (minor - תכונה חדשה גלויה למשתמש, בלי שינוי סכימה - places/{id} לא השתנה).

## v4.67.0 — תשתית טאבים ב-itinerary.html, commit 1/5 (מעבר מקומות ל-itinerary.html)

### 🎯 מה נוסף
`itinerary.html` מקבל שורת טאבים ("תוכנית יומית" / "מקומות") - התשתית שעליה יעברו רשימת המקומות וטאב המפה בשני ה-commits הבאים. עדיין **בלי תוכן אמיתי** בטאב "מקומות" - placeholder ("בקרוב - רשימת המקומות עוברת לכאן") בלבד. תוכנית ה-5-commits המלאה תועדה בהשקעה נפרדת; זה ה-commit הראשון.

### ✏️ פרטי מימוש
- **אין `.tabs`/`setTab()` קודמים ב-itinerary.html** (ממצא מהחקירה) - בניגוד ל-places.html, לעמוד הזה היה view יחיד בלבד (`activeDay`/`.day-tabs` הוא קונספט אחר - בחירת יום קלנדרי, לא בחירת-תצוגה). נבנה מאפס, **באותו דפוס בדיוק** כמו ב-places.html - לא המצאה חדשה.
- **`#itineraryView`/`#placesView`** עוטפים את התוכן הקיים (day-tabs+export-button+activities) ואת ה-placeholder החדש, בהתאמה - `display:flex;flex-direction:column;flex:1;min-height:0;overflow:hidden` על שניהם, מוחלף לפי `setTab()`.
- **`grid-template-columns:repeat(2,1fr)`** ל-`.tabs` - נכון למספר הטאבים בפועל כאן (2). **הערה, לא תוקן כאן**: ב-`places.html` יש 4 טאבים אבל `.tabs{grid-template-columns:repeat(3,1fr)}` (כנראה נשאר מלפני שנוסף טאב המפה) - נתפס תוך כדי החקירה, מחוץ להיקף ה-commit הזה.
- **`fabAction()`/`updateFab()` הוכללו** - ה-FAB עבר מ-handler קבוע ("הוספת פעילות" תמיד) לפונקציות שמסתכלות על `tab`, בדיוק כמו ב-places.html. FAB מוסתר לגמרי בטאב "מקומות" (אין עדיין פעולת-הוספה שם) - אותו נימוק כמו הסתרת ה-FAB בטאב "מפה" ב-places.html.

### 🔍 מה שנבדק - ודאתי שההתנהגות הקיימת לא נשברה, לפני שממשיכים ל-commit 2
1. **FAB הקיים ("הוספת פעילות") עדיין עובד בדיוק כמו קודם** - נבדק ישירות (`fabAction()` בלי profile מציג את אותו טוסט "בחרו קודם שם" כמו לפני השינוי, בלי קריסה).
2. **מעבר טאבים חי, שני הכיוונים** - נבדק ישירות: לחיצה על "מקומות" מסתירה את `#itineraryView`, מציגה את ה-placeholder, מסתירה FAB; חזרה ל"תוכנית יומית" משחזרת הכל (כולל FAB) בדיוק.
3. **מסך צר (375px) ומצב בהיר** - נבדק חזותית, בלי גלישה/שבירה.
4. **קונסול נקי, `i18n-audit` - 0 ממצאים** (כל הטקסט החדש עובר דרך `tr()`).

v4.66.1 -> v4.67.0 (minor - שינוי UI גלוי, בלי שינוי סכימה).

## v4.66.1 — תיקון באג: הבהוב בהיר בכניסה לטאב "מפה" גם במצב כהה (places.html)

### 🎯 הבאג
דווח בפועל: כניסה לטאב "מפה" גרמה להבהוב - מפה בהירה, ואז ריבוע קטן יותר בתוכה, ורק אז המפה הכהה. שבר חזותית את אנימציית כניסת הסיכות (commit 5/5) - הסיכות נכנסות באמצע ההבהוב, לא על רקע יציב.

### 🔍 מה נמצא - נבדק בפועל, לא ניחוש
נתפס `L.TileLayer.prototype.createTile` וגם צבע הרקע המחושב של מיכל המפה, ונבדק חי מול `renderTripMap()`. **תוצאה חד-משמעית**: כל בקשות האריחים היו מ-`basemaps.cartocdn.com` בלבד - `renderTripMap()` **לא** יצרה שכבת OSM בהירה ואז החליפה אותה, ההשערה הראשונית (שכבר נבדקה ונפסלה בחקירה הקודמת) לא הייתה נכונה. הסיבה האמיתית: **הרקע הדיפולטיבי המובנה של `.leaflet-container` עצמו הוא `#ddd` קבוע (מ-leaflet.css), בלי שום קשר ל-theme של האפליקציה** - גלוי לרגע לפני שהאריחים מכסים את השטח, ומחדש בכל כניסה לטאב (`renderTripMap()` הורסת ובונה מופע Leaflet חדש בכל קריאה, במכוון - כדי להישאר מסונכרנת עם מקומות שנוספו בזמן שהיה בטאב אחר).

**שני תסמינים נפרדים, לא אותה סיבה**: "המפה הבהירה" = הרקע הדיפולטיבי של Leaflet; "הריבוע הקטן בתוכה" = התנהגות ה-fade-in הרגילה של Leaflet לכל אריח בנפרד (תקנית בכל מפת-אריחים, לא באג) - שנראתה שבורה רק כי הרקע שמתחתיה היה בצבע הלא-נכון.

### ✏️ התיקון - CSS בלבד
`.trip-map-canvas.leaflet-container{background:...}` (בהיר) + `[data-theme=dark] .trip-map-canvas.leaflet-container{background:#0B1015}` (כהה) - selector מורכב (compound, בלי רווח), לא selector-צאצא: אומת ישירות מול ה-DOM החי ש-`L.map(mapDiv)` מוסיף את `leaflet-container` כמחלקה **נוספת על אותו אלמנט** שכבר קיבל `trip-map-canvas` (`mapView.js`), לא על אלמנט-ילד - גרסה עם רווח (`.trip-map-canvas .leaflet-container`) הייתה "נראית" נכונה אך לא תואמת כלום בשקט (נתפס בפועל בבדיקה ראשונה - `getComputedStyle` המשיך להחזיר `#ddd` אחרי ההוספה, עד שתוקן). לא נוגע ב-`mapView.js` - שינוי CSS טהור.

### 🔍 מה שנבדק
1. **רקע המנוחה לפני טעינת אריחים, שני המצבים** - `getComputedStyle` מיד אחרי `renderTripMap()`, לפני שאריח כלשהו נטען: כהה → `rgb(11,16,21)` (`#0B1015`, תואם למטא theme-color), בהיר → `rgb(233,233,233)` (`#e9e9e9`) - שניהם נכונים.
2. **חזותי, שני המצבים, מפה מלאה עם סיכות** - screenshot אחרי טעינה מלאה: כהה - רקע כהה יציב, 3 סיכות צבועות נכון, בלי הבהוב/ריבוע זר; בהיר - אריחי OSM רגילים, ייחוס תקין.
3. **אנימציית הסיכות (commit 5/5)** - קוד האנימציה עצמו לא נגע (רק CSS חדש, לא קשור ל-selectors של `.trip-map-pin`/`.trip-map-pin-inner`) - אומת ויזואלית שהסיכות יושבות נקי על הרקע היציב החדש, בלי חפיפה/קונפליקט.

v4.66.0 -> v4.66.1 (patch - תיקון באג, בלי שינוי סכמה).

## v4.66.0 — אריחי מפה כהים לפי מצב תצוגה (mapView.js, places.html - המשך למפת מסלול)

### 🎯 מה נוסף
מפת המסלול (טאב "מפה") עוברת לאריחי CartoDB `dark_all` כהים כשהאפליקציה במצב כהה, ולאריחי OSM הרגילים במצב בהיר - במקום אריחים בהירים תמיד, גם על רקע כהה. ההחלטה בין השניים לא ממתינה לכניסה מחדש לטאב - היא מתחלפת חי, באותו רגע שהמשתמש לוחץ על כפתור הכהה/בהיר, בדיוק כמו כל שאר האלמנטים בעמוד.

### ✏️ מה נבדק לפני שנבחר CartoDB - חקירה נפרדת, לא רק תיעוד
נבדקו side-by-side, מול מפה חיה (לא רק לפי specs): CartoDB `dark_all` (דורש מפתח API מאז סוף אוגוסט 2026) מול Esri "Dark Gray Canvas" (בלי מפתח בכלל). **Esri נפסל בבדיקה החזותית עצמה** - התברר כאפור-בינוני/בהיר, לא כהה אמיתי, במיוחד לצד ה-`#0B1015` הכהה-עמוק של האפליקציה עצמה. CartoDB `dark_all` היה כהה אמיתי בבדיקה. גם אומת בפועל (לא רק בתיעוד) שה-URL של Esri דורש סדר `{z}/{y}/{x}` ולא `{z}/{x}/{y}` הרגיל - סדר שגוי החזיר אריחים ממיקום גיאוגרפי שונה לגמרי.

### ✏️ פרטי מימוש
- **מפתח CartoDB - לא סוד, לא Cloud Function proxy**: מפתחות אלה מיועדים להיות גלויים ב-URL של בקשת אריח מהדפדפן - אין "תשובה יקרה" להגן עליה כמו ב-Gemini API key. `CARTO_DARK_KEY` ב-`mapView.js` הוא **placeholder בלבד נכון לרגע הזה** (`YOUR_CARTO_KEY_HERE`) - עם הערה מפורשת בקוד על איך ומאיפה להשיג מפתח אמיתי (carto.com/basemaps/apikey - מייל בלבד, מיידי, 5 מיליון בקשות/חודש חינם).
- **`setUrl()` על layer יחיד, לא שני layers מתחלפים**: `currentTileLayer` הוא instance יחיד ש-URL שלו מוחלף (`.setUrl()`) הן ברינדור הראשוני והן בכל קריאה ל-`window.updateMapTheme()` - לא remove/add של שני layers נפרדים. ייחוס משולב קבוע (OSM+CARTO) מוצג תמיד, לא מוחלף לפי ה-layer הפעיל - קל יותר לתחזוקה, ולא בעייתי מבחינת רישיון (יותר-מדי ייחוס אף פעם לא אסור, פחות-מדי כן).
- **`window.updateMapTheme()` מחובר ל-`applyTheme()` הקיים**: `applyTheme()` (`places.html`) כבר רץ סינכרונית גם בלחיצת כפתור הכהה/בהיר וגם בכל עדכון Firebase - שורה אחת (`if(window.updateMapTheme)window.updateMapTheme()`) בסופו מספיקה כדי שהמפה תעדכן חי, בלי מנגנון האזנה חדש. `updateMapTheme()` הוא no-op בטוח (לא שגיאה) אם טאב המפה עוד לא נפתח בכלל השבתיה - `renderTripMap()` הבא ממילא יבחר את האריח הנכון.
- **Fallback ל-OSM בהיר - נבדק מול כשל רשת אמיתי, לא מדומה**: `attachTileFallback()` מאזין ל-`tileerror` על ה-layer; 3+ כשלים בתוך חלון של 3 שניות → `setUrl()` בחזרה לאור OSM + `console.warn`. **נבדק בפועל** מול domain לא-קיים (כשל DNS אמיתי) - חזר לאור בהצלחה. מנגנון חד-פעמי (`layer.off('tileerror')` אחרי המעבר) - אין תיאום עם עוד שכבת fallback, OSM כבר המוצא האחרון.

### ⚠️ מגבלה אמיתית שנתפסה בבדיקה - לא מכוסה ע"י ה-fallback
בדיקה ישירה (`fetch()` על ה-URL עם placeholder key) גילתה: CartoDB **תמיד מחזיר `200 OK` עם PNG תקין** - גם כשהמפתח שגוי/placeholder/חסר, רק עם watermark "API KEY REQUIRED" מודפס על התמונה. **זו לא שגיאת רשת ולא `tileerror`** - ה-fallback שנבדק (למעלה) מכסה כשל-רשת אמיתי (outage/DNS/חיבור), **לא** מצב שבו המפתח נפסל/quota אזל אחרי שהיה תקין - מצב כזה יישאר "תקוע" על watermark בשקט, בלי שה-JS יכול לזהות זאת (בדיקת פיקסלים חסומה ע"י CORS - נבדק ישירות, `getImageData` זרק "canvas tainted"). התקבל כסיכון ידוע, לא טופל בהיקף הזה - דורש מעקב ידני-מדי-פעם על מפתח בתוקף, לא ניטור אוטומטי.

מפתח CartoDB אמיתי הוחלף בפועל ב-`CARTO_DARK_KEY` (לא ה-placeholder), ואומת חזותית לפני commit: אריחים כהים אמיתיים, בלי watermark, ייחוס "CARTO" מוצג נכון בפינת המפה.

v4.65.0 -> v4.66.0 (minor - תכונה חדשה גלויה למשתמש, בלי שינוי סכמה).

## v4.65.0 — אנימציית כניסה לסיכות (mapView.js, places.html, commit 5/5 - מפת מסלול)

### 🎯 מה נוסף
סיכות המפה נופלות פנימה אחת אחרי השנייה בטעינה, לא כולן ביחד: נפילה+קפיצה קטנה, ואז זוהר חד-פעמי שדועך כשהסיכה נוחתת. חד-פעמי בלבד - לא לולאה. `prefers-reduced-motion` מדלג לגמרי על הדירוג-בזמן - כל הסיכות מופיעות יחד, מיידית, לא "מהר" אחת-אחרי-השנייה.

### ✏️ פרטי מימוש
- **דירוג-הזמן ב-JS (`mapView.js`), האנימציה עצמה ב-CSS (`places.html`)**: `renderTripMap()` קורא ל-`marker.addTo(map)` לכל סיכה בתוך `setTimeout` מדורג (`STAGGER_MS=90`), לא הכל בבת אחת. ברגע שסיכה נכנסת ל-DOM, ה-`@keyframes` שלה (`tripMapPinDrop`+`tripMapPinGlow`) כבר רץ אוטומטית - אין class נוסף שצריך "להדליק" בזמן הנכון.
- **האנימציה על div פנימי, לא על האלמנט ש-Leaflet ממקם**: Leaflet קובע מיקום סיכה ע"י `transform: translate3d(...)` inline על האלמנט התואם ל-`className` שלו (`.leaflet-marker-icon`) - אנימציית CSS על `transform` **על אותו אלמנט/מאפיין** הייתה מתנגשת עם המיקום של Leaflet עצמו (inline מנצח class). הפתרון: מחלקת האנימציה (`.trip-map-pin-inner`) יושבת על `<div>` פנימי בתוך ה-`html` של ה-divIcon - Leaflet אף פעם לא נוגע בו.
- **נמנע במפורש מבאג ה-specificity שכבר נתפס בעבר (`gittrip-showcase.html`, `git show dc394ea`)**: שם, class של "לפני" (`is-armed`) לא הוסר אחרי שselector יותר ספציפי של "אחרי" היה אמור לנצח, כי חסר לו `:not(.is-visible)` - השאיר אלמנט תקוע. כאן אין זוג class-ים מתחלף בכלל - `.trip-map-pin-inner` הוא class יחיד, תמיד-נוכח, שה-`@keyframes` שלו עצמו עובר מכניסה למצב מיושב (`animation-fill-mode:both`) - אין קרב specificity אפשרי מבנית.
- **הגנת generation מפני רינדור מיושן**: אותו דפוס בדיוק כמו `profileLoadGen`/`shareLinkRequestId` הקיימים באפליקציה - אם `renderTripMap()` נקרא שוב (סינון-רגל/שפה השתנו) לפני שכל טיימרי ה-stagger של הקריאה הקודמת סיימו, טיימר ישן לא יוסיף סיכה למפה חדשה/מסוננת-אחרת. נבדק ישירות: רינדור של 5 מקומות שהוחלף ב-50ms ע"י רינדור חדש של 2 מקומות אחרים - התוצאה הסופית היא בדיוק 2 הסיכות החדשות, בלי דליפה משום הקריאה שבוטלה.
- **`prefers-reduced-motion` - `.01ms` ולא `animation:none`**: אותה קונבנציה קיימת כבר בכל הרכיבים המשותפים באפליקציה (`shared.css`), לא המצאה חדשה - `animation-fill-mode:both` אומר שביטול מלא (`none`) היה משאיר את הסיכה במצב הבסיס הלא-מונפש שלה (`opacity:0`, לפי 0% ב-keyframe), לא במצב המיושב שהאנימציה אמורה להגיע אליו. משך כמעט-אפס עדיין "רץ" עד הסוף, רק בלתי-נתפס.

### 🔍 מה שנבדק, ומה לא ניתן היה לבדוק ישירות בסביבה הזו
1. **פער-זמן אמיתי בין סיכות** - נמדד ישירות (לא משוער): 5 סיכות הופיעו ב-~31/120/211/301/391ms - כמעט בדיוק הפרשי 90ms כמתוכנן.
2. **יציבות אחרי נחיתה (בדיקת ה-specificity)** - **מגבלת סביבה ידועה, לא נבדק בזמן-אמת**: ה-Browser pane כאן לא מקדם שעון אנימציה בכלל (`document.timeline.currentTime`/`getAnimations()[0].currentTime` קפואים על 0 גם אחרי המתנה אמיתית - כבר מתועד ב-CLAUDE.md). נבדק במקום זאת בעקיפין: הצבת `animation:none` זמנית על הסיכות חשפה את מצב-המנוחה הבסיסי שלהן כ-`transform:none; opacity:1; filter:none` - אין שום declaration מתחרה שתייצר תקיעות אחרי נחיתה אמיתית בדפדפן אמיתי. `animationFillMode` מאומת כ-`"both, both"`. ביטחון גבוה, לא צפייה ישירה.
3. **`prefers-reduced-motion`** - צד ה-JS (הדירוג-בזמן) נבדק ישירות: `window.matchMedia` מדומה ל-reduced, 3 מקומות → כל 3 הסיכות מופיעות מיידית, בלי שום פער (`countImmediately:3, countAfterWait:3`). צד ה-CSS (`.01ms`) לא אומת מחדש בזמן-אמת (אין כלי לאמת `prefers-reduced-motion` אמיתי בסביבה הזו) - נשען על אותה קונבנציה כבר מוכחת בשאר האפליקציה.
4. **replay בכניסה חוזרת לטאב המפה** - לא נבדק כתכונה נפרדת ולא דרש שינוי קוד: תוצאת-לוואי טבעית של `renderTripMap()` שבונה הכל מחדש בכל קריאה, כפי שכבר קורה היום עם שינויי סינון/שפה.

v4.64.1 -> v4.65.0 (minor - תכונה חדשה גלויה למשתמש: אנימציית כניסה, בלי שינוי סכמה).

## v4.64.1 — תיקון באג חמור: geocoding נותן קואורדינטות שגויות-אבל-תקינות-טכנית למקום עם שם כללי (geocode.js, places.html)

### 🎯 הבאג
דווח בפועל: מקום נשמר בשם "מסעדה" (המילה הכללית, בלי mapUrl) בטיול שיעדו ברלין - קיבל קואורדינטות **תקינות טכנית אבל שגויות לגמרי**: כפר בשם "מסעדה" ברמת הגולן, ישראל - לא ברלין. **לא כישלון גלוי** ("לא אותרו", שכבר יש לו טיפול הגון) - תשובה שגויה שהתקבלה כתקינה, בשקט. שוחזר במדויק מול ה-API החי: `q=מסעדה` (בלי שום הקשר) מחזיר את הכפר האמיתי בגולן - Nominatim לא טעה, פשוט לא קיבל שום סיבה להעדיף ברלין.

### 🔍 מה נמצא - איך זה קרה
דרישת "לשלב שם-מקום + שם-יעד" (כדי ש-"קפה"/"מסעדה" לא יחפשו בעולם כולו) הוכרעה במפורש בחקירה **הראשונה** של הפיצ'ר - התכנון המקורי, קישור Google Maps חיצוני, לפני המעבר למפה מוטמעת. כש-`geocode.js` נבנה מחדש סביב השאלות של התכנון החדש (Nominatim מול Open-Meteo, retry-או-לא) - ההחלטה הזו **לא הועברה מחדש**, ונפלה בין שני התכננים. `window.geocodeName(name, options)` שלח תמיד רק את שם המקום הגולמי, בלי שום הקשר-יעד - מאומת ישירות בקוד, לא ניחוש.

### ✏️ התיקון - `countrycodes` (פילטר API מובנה), לא הקשר-טקסט
**לא** שילוב מחדש של שם-היעד כטקסט בתוך השאילתה (כמו בתכנון המקורי) - זה כבר הוכח שביר מול Nominatim בחקירה קודמת (הוספת הקשר עברי ל"ארומה אספרסו בר, תל אביב" **שברה** התאמה שכבר עבדה, לא רק נכשלה לעזור). במקום זה: `countrycodes` - פרמטר API נפרד של Nominatim, ISO 3166-1 alpha-2 (`leg.countryCode`, כבר קיים בנתונים מ-Open-Meteo מזמן יצירת הטיול) - מצמצם את **מרחב החיפוש עצמו**, לא נוגע במחרוזת. מאומת ישירות: `q=מסעדה&countrycodes=de` מחזיר `[]` (ריק, לא הכפר בגולן) - "לא אותרו" כן/הגיוני, לא תשובה שגויה במדינה הלא-נכונה.

`savePlace()` פותר את הרגל הרלוונטית של המקום (`legId` שלו, או `getPrimaryLeg(trip)` כברירת מחדל כשאין שיוך - אותו fallback ש-`geoRefLeg()`/`aiSuggestLeg()` כבר משתמשים בו) ומעביר את `countryCode` שלה. רגל ישנה בלי השדה (מלפני v4.23.0) - נופלת חזרה לחיפוש בלי פילטר, **בדיוק רמת הסיכון של היום, לא נסיגה/קריסה/כישלון שקט חדש**.

### 🔍 מה שנבדק - שלוש הבדיקות הקריטיות, מול ה-API החי
1. **המקרה המדויק שדווח**: `geocodeName('מסעדה', {countryCode:'DE'})` → `null` - נכנס נכון ל"לא אותרו", לא נופל שוב על הגולן
2. **התאמות קיימות לא נשברו**: מגדל אייפל (FR), שוק הכרמל (IL), Tel Aviv Museum of Art (IL) - כל השלושה עדיין מחזירים בדיוק את אותן קואורדינטות כמו לפני התיקון
3. **רגל בלי countryCode**: `geocodeName('רומא', {countryCode:undefined})` - עדיין מחזיר את קואורדינטות רומא הנכונות, בלי פילטר, בלי קריסה

נבדק בפועל: כל מה שלמעלה, מול Nominatim החי בכל מקרה, לא מדומה. v4.64.0 -> v4.64.1 (patch - תיקון באג, בלי תכונה חדשה).

## v4.64.0 — רינדור בפועל: סיכות צבועות, popup, רשימת "לא אותרו" (places.html, commit 4/5 - מפת מסלול)

### 🎯 מה נוסף
`renderTripMap()` (mapView.js) עכשיו בונה מפת Leaflet אמיתית: מרכוז/zoom לפי כל המקומות המסוננים (רגל פעילה בלבד, `activeLegFilter` - זהה בדיוק לתנאי הסינון של רשימת המקומות הרגילה), סיכה צבועה-לפי-קטגוריה לכל מקום עם `lat`/`lon`, לחיצה על סיכה פותחת popup עם שם המקום. מקומות בלי קואורדינטות מוצגים ברשימה נפרדת מתחת למפה ("לא אותרו על המפה: [שמות]") - לא נעלמים בשקט. 0 מקומות עם קואורדינטות ברגל הנוכחית מציג מסך-ריק הגיוני עם הסבר, לא מפה לבנה.

### ✏️ מיפוי צבע-קטגוריה - אומת חזותית לפני מימוש, לא רק תיאורטית
9 צבעים, לפי האימוג'י הסופי בכל מחרוזת קטגוריה (עמיד יותר לשינויי ניסוח עתידיים מהתאמה למחרוזת המלאה) - מכסה את כל 19 הקטגוריות האפשריות שנספרו ישירות מקוד `categories()`. **שני הזוגות שנבדקו במפורש (אטרקציה/רוגע, אוכל/לוגיסטי) עברו** - נבדקו side-by-side על רקע אריח-OSM בהיר, רקע בהיר של האפליקציה, ורקע כהה. **בדיקת מרחק-צבע מספרי חשפה זוג שלישי, לא-מסומן, שהיה קרוב יותר מהשניים שנבדקו**: אוכל (`#E67E22`) מול חשוב (`#E74C3C`, מקורי) - מרחק 106.9, לעומת 160.9/193.4 של שני הזוגות שנבדקו. "חשוב ⭐" שונה לזהב `#F1C40F` (רלוונטי רק לטיולי מילואים, בניגוד ל"אוכל" שמשותף לכל סוגי הטיולים) - מרחק עלה ל-143.9, אושר חזותית שוב. קטגוריה לא-מוכרת (למשל אחרי שהארגון שינה `tripType`/vibes) נופלת לאפור ניטרלי, לא קורסת.

### ✏️ פרטי מימוש
- **סיכת המפה**: `map-pin.svg` (מ-commit 3) מוטמע כמחרוזת ב-`mapView.js` פעם אחת, לא נשלף מחדש לכל סיכה - `stroke="currentColor"` בקובץ המקורי הוא מה שמאפשר לצבוע אותו per-marker רק עם CSS `color` על ה-wrapper, בלי גרסה נפרדת לכל צבע.
- **ניקוי מופע מפה קודם**: `renderTripMap()` הוא re-entrant (קורה שוב בכל שינוי סינון-רגל/שפה/עדכון נתונים) - מופע Leaflet קודם נהרס (`currentMap.remove()`) לפני בנייה מחדש, כדי לא לדלוף listeners בין קריאות חוזרות באותו session.
- **`fitBounds()` במקום ממוצע+zoom קבוע, כסטייה מוצהרת**: עם 2+ מקומות, `fitBounds()` מבטיח שכל הסיכות יישארו בתצוגה (ממוצע+zoom קבוע היה עלול לחתוך סיכות מחוץ לתצוגה במקומות מפוזרים) - עדיין ממורכז סביב אזור המקומות עצמם, מה שממוצע-מרכוז ניסה להשיג מלכתחילה. מקום בודד משתמש בממוצע (הקואורדינטה היחידה שלו) + zoom קבוע, כפי שהתבקש במדויק.
- **`renderMap()` נשמר מוגן ב-`tab!=='map'`**: Leaflet מודד את גודל הקונטיינר שלו בזמן `L.map()` init ולא בודק מחדש - איתחול בזמן ש-`#mapView` מוסתר (`display:none`, טאב אחר פעיל) היה מרנדר אריחים שבורים/גודל-אפס לצמיתות, גם אחרי מעבר לטאב. נקרא בלי תנאי מ-`renderAll()`/פילטר-הרגל (כמו כל view אחר בעמוד) אבל no-op זול כשהמפה לא פעילה; `setTab()` קורא לו במפורש ברגע שהטאב הופך פעיל.
- **תיקון נוסף שנתפס בבדיקת נייד (375px), לא היה בתוכנית**: כפתור ה-FAB ("+") לא עושה כלום בטאב "מפה" (`fabAction()` תמיד היה חסר ענף מתאים) והיה חופף חזותית לרשימת "לא אותרו" במפה קצרה - תוקן ע"י הסתרת ה-FAB בטאב הזה (`updateFab()`), פותר את שתי הבעיות בבת אחת.

### 🔍 מה שנבדק - כל אחד מול Leaflet חי, לא מדומה
1. **רגל עם כמה מקומות ממוקמים** - 4 סיכות מוצגות, צבע נכון לכל קטגוריה (מאומת ויזואלית בצילום מסך אמיתי, לא רק בקוד)
2. **מקומות שנכשלו ב-geocoding** - רשימת "לא אותרו" מוצגת עם השמות הנכונים, לצד מפה עם רק המקום הממוקם
3. **0 מקומות ברגל** - מסך ריק הגיוני עם הסבר, לא מפה שבורה
4. **לחיצה על סיכה** - popup עם השם הנכון (מאומת גם ב-DOM וגם בצילום מסך בזום)
5. עברית ואנגלית, נייד (375px) - כולל תיקון חפיפת ה-FAB שנתפס תוך כדי
6. אריחי OSM נבדקו בפועל שנטענו (`leaflet-tile-loaded`, לא רק שהמפה "נראית" תקינה)
7. קונסול נקי, `node scripts/i18n-audit.js places.html` — 0 ממצאים

נבדק בפועל: כל מה שלמעלה. v4.63.0 -> v4.64.0 (minor - יכולת חדשה גלויה למשתמש). **commit 5/5 (אנימציית כניסה מדורגת + זוהר) הבא בתור.**

## v4.63.0 — תשתית Leaflet + Tabler, טאב "מפה" ריק (places.html, commit 3/5 - מפת מסלול)

### 🎯 מה נוסף
טאב רביעי, "מפה", ליד מקומות/תמונות/מוזיקה ב-places.html - **מבנה בלבד, בלי תוכן פנימי עדיין.** Leaflet (1.9.4, BSD-2-Clause, ללא מפתח API) נטען מ-jsDelivr - script קלאסי + stylesheet, אותה תבנית טעינה כמו שאר הספריות בפרויקט. `mapView.js` חדש - קובץ נפרד (לא קוד מוטמע בתוך places.html), חושף `window.renderTripMap(containerId, places, options)` שכרגע רק מדפיסה `console.log` - שום רינדור אמיתי עדיין.

### ✏️ Tabler - SVG בודד, לא webfont
הורד קובץ `map-pin.svg` בודד מ-`@tabler/icons` (v3.46.0, קו-מתאר, לא `-webfont`) ונשמר בריפו עצמו (שורש הריפו, אותו מיקום כמו כל שאר קובצי האייקונים הסטטיים הקיימים - `favicon.svg`, `gittrip-icon.svg` וכו') - **לא** תלות CDN חיה. זה פותר גם את בעיית המשקל (673KB ל-webfont המלא מול כמה מאות בייטים לקובץ בודד) וגם את הבאג המתועד (`tabler/tabler-icons#1452` ואחרים) שבו אייקוני filled לא נטענים כלל מה-CDN - קובץ SVG בודד לא תלוי במנגנון ה-webfont/CSS class בכלל. הקובץ לא בשימוש בשום מקום בקוד עדיין - מוכן לקומיט עתידי שבו הסיכות בפועל יצוירו על המפה.

### ✏️ `mapView.js` - עצמאי מכוונה, לא קשור ל-places.html
נבנה כ-entry point עצמאי (`window.renderTripMap`), לא כפונקציה פנימית של places.html - בהתאם לדרישה המפורשת שלא להניח איפה זה יושב (הדיון העתידי על העברת תצוגת המקומות ל-itinerary.html). הפילטור לפי רגל (`activeLegFilter`) הוא באחריות הקורא, לא של `mapView.js` עצמו - אותה חלוקת אחריות שכבר קיימת בשאר הפונקציונליות בעמוד (`geoRefLeg()`/`aiSuggestLeg()`).

### 🔍 מה שנבדק
1. `window.L` זמין בקונסולה, `window.L.version === "1.9.4"`
2. `window.renderTripMap` זמינה כפונקציה
3. הטאב "מפה" קיים ב-DOM, לחיצה עליו (`document.getElementById('mapTab').click()`) עוברת נכון - `mapTab` הופך active, `mapView` נחשף, `placesView` מוסתר (נבדק ישירות ב-DOM)
4. צילום מסך מאשר ויזואלית - 4 טאבים, "מפה" מודגש כשנבחר
5. קונסול נקי - שום שגיאת טעינה מ-jsDelivr
6. `fabAction()` נבדק - קליק על ה-FAB בטאב "מפה" הוא no-op נקי (אין ענף `else` שמתאים, אין שגיאה) - לא נגעו בזה, מחוץ להיקף ה-commit הזה
7. `node scripts/i18n-audit.js places.html` — 0 ממצאים

נבדק בפועל: כל מה שלמעלה. v4.62.0 -> v4.63.0 (minor - טאב חדש גלוי למשתמש, גם אם עדיין ריק). **commit 4/5 (רינדור סיכות בפועל + רשימת "לא אותרו") הבא בתור.**

## v4.62.0 — geocoding למקומות שמורים - קואורדינטות בזמן שמירה (places.html, commit 1/5 - מפת מסלול)

### 🎯 מה נוסף
`places/{id}` מקבל `lat`/`lon` (אותם שמות שדה בדיוק כמו `legs[n].lat`/`.lon` - עקביות עם המוסכמה הקיימת) בזמן שמירת מקום - geocoding חד-פעמי, לא בזמן הצגת מפה עתידית. בלי UI חדש עדיין - זו תשתית נתונים בלבד, לקראת מפת מסלול מוטמעת (Leaflet) שתבוא ב-commits הבאים.

### 🔍 שינוי כיוון משמעותי תוך כדי - Open-Meteo נפסל, לא רק "נבחר שירות אחר"
התכנון המקורי הניח להשתמש באותו שירות geocoding שכבר בשימוש ל-legs (`geocoding-api.open-meteo.com`, wizard.html/index.html). **נבדק ישירות מול ה-API החי, לא הונח**: Open-Meteo מחזיר **אפס תוצאות** לנקודות עניין אמיתיות ("Eiffel Tower", "Louvre Museum", "שוק הכרמל") - זה gazetteer של ערים/יישובים, לא POI geocoder. מתאים למה שהוא כבר משרת (יעד-רגל), לא מתאים למקומות בודדים (בדיוק מה ש-commit הזה צריך). **Nominatim** (הגיאוקודר של OpenStreetMap עצמו - הבן-זוג הטבעי לאריחי ה-OSM שכבר נבחרו למפה) פתר את כל שלוש השאילתות שנכשלו - מאומת ישירות, לא מהתיעוד.

### ✏️ קובץ משותף חדש - `geocode.js`
נבנה כקובץ נפרד (לא הרחבה של `geocodeDestination()`/`geocodeAdminDestinationName()` הקיימים ב-wizard.html/index.html) - **בכוונה, לא שכפול שלישי**: אלה עדיין לא שונו בסבב הזה. איחוד עתידי איתם הוא החלטה נפרדת. script קלאסי, מוצמד ל-`window` (`window.geocodeName`), אותה תבנית כמו `legs.js`/`datetime.js`.

### ⚠️ retry-באנגלית - נוסה, נכשל, הוסר במפורש (לא רק דולג)
התכנון המקורי כלל ניסיון חוזר באנגלית אם הניסיון הראשון (בשפת הטיול) נכשל - בהשראת אותו דפוס שהיה עוזר ב-Open-Meteo. **נבדק ישירות מול Nominatim ונמצא לא-יעיל**: `accept-language` ב-Nominatim משפיע רק על שפת התצוגה של תוצאה שכבר נמצאה - לא על ההתאמה עצמה. אותה שאילתה עברית, נשלחת שוב עם `accept-language=en` (עם או בלי הקשר-יעד מצורף) - עדיין `NO RESULT` לפער אמיתי ומאומת ("מוזיאון תל אביב לאמנות" נכשל בעברית, נפתר רק עם תרגום אנגלי אמיתי של השם - לא קיים בקוד). **הוסר לגמרי, לא רק לא-מומש** - כדי שאף אחד בעתיד לא "יתקן" את זה בחזרה בלי לדעת שזה כבר נבדק. תיעוד מלא בהערת הקובץ עצמה.

### 🔍 גבולות אמיתיים של הפיצ'ר - נבדק ישירות, לא ניחוש
מה שבאמת קובע אם מקום נפתר: (1) עד כמה הוא מתויג בעברית ב-OSM (תלוי-תרומת-קהילה, לא רק פרסום - "מגדל אייפל"/"סגרדה פמיליה" נפתרים ישירות בעברית, "הקולוסיאום"/"מגדל פיזה הנוטה" לא, למרות פרסום זהה), (2) ה"ה" הידיעה בעברית - "קולוסיאום"/"מגדל פיזה" (בלי ה"ה") נפתרים, "הקולוסיאום"/"מגדל פיזה הנוטה" (עם ה"ה") לא, גם עבור מקום שכן מתויג בעברית. שני הממצאים מתועדים ב-`geocode.js` עצמו, כדי שתוצאת "לא אותר" למקום מוכר לא תיראה כמו באג.

### 🔍 מה שנבדק - הכל מול ה-API החי, לא מדומה
1. מקום שנפתר (`מגדל אייפל`) מקבל `lat`/`lon` נכונים ב-`fields` שנבנה
2. מקום שלא נפתר (`הקולוסיאום`) - אין שדה `lat`/`lon` בכלל ב-`fields` (לא `0`/`null`) - מאומת ישירות על אובייקט ה-`fields` שנבנה בפועל
3. `window.geocodeName` נבדק ישירות מול Nominatim החי - גם הצלחה וגם כישלון נקי
4. קונסול נקי
5. `node scripts/i18n-audit.js places.html` — 0 ממצאים (אין טקסט חדש למשתמש)

נבדק בפועל: כל מה שלמעלה. v4.61.1 -> v4.62.0 (minor - שינוי סכמה, שדות חדשים ל-`places/{id}`). **commit 2/5 (backfill) הבא בתור - עם דיליי מפורש בין קריאה לקריאה, מגבלת 1/שנייה של Nominatim.**

## v4.61.1 — תיקון: "הטיולים שלי" נחת ישירות בטיול במקום להציג רשימה (index.html)

### 🎯 הבאג
דיווח: לחיצה על "הטיולים שלי" בתפריט ההמבורגר נוחתת ישירות בתוך טיול ספציפי - בלי לעבור דרך רשימת הבחירה (`#tripPicker`) בכלל. קרה גם למי שיצר כמה טיולים וגם למי שלא יצר אף אחד - המשותף: ל-`users/{uid}/trips` שלהם הייתה **בדיוק רשומה אחת** (בין אם כי יצרו רק טיול אחד, ובין אם כי הצטרפו לטיול אחד בלי ליצור בעצמם).

### 🔍 מה נמצא (חקירה, ר' גם commit קודם על שלושת באגי הזהות/session)
`resolveTripSelection()` מדלגת נכון על `lastTripId` כש-`skipLastTripId` דלוק (זה עבד תמיד) - אבל `renderTripPickerList()` מכילה **מנגנון auto-redirect שני, עצמאי לגמרי**, שלא הוגן על ידי שום דגל: "טיול יחיד ברשימה → קפיצה ישירה אליו, בלי להציג רשימה". זה תקף גם כשהמשתמש הגיע במפורש דרך `?trips=1` ("הטיולים שלי") מתוך כוונה לראות/לנהל רשימה.

### ✏️ התיקון
`options.forceTripPicker` (חדש) עובר מ-`resolveTripSelection()` אל `renderTripPickerList()`, ומגן על ה"קיצור" של טיול יחיד: `if (entries.length === 1 && !options.forceTripPicker)`. **רק** כש-`forceTripPicker` דולק (המשתמש ביקש במפורש לראות את הרשימה) הקיצור מנוטרל.

**שני מקומות אחרים שבהם הקיצור הקיים היה מכוון - זוהו ונשארו ללא שינוי, במפורש:**
1. **נחיתה טרייה בלי `lastTripId` שמור** (הפעלה ראשונה/מהאייקון, לא דרך "הטיולים שלי") - `forceTripPicker` לא מגיע כלל מ-URL כזה, אז הקיצור ממשיך לפעול - תואם את הכוונה המקורית ("no real choice to make").
2. **fallback של "טיול לא נמצא"** (`startTripListener()`, כש-`tripId` בעייתי) - הקריאה `resolveTripSelection({ skipLastTripId: true })` נשארה **ללא שינוי**, לא מעבירה `forceTripPicker` - קיצור לטיול היחיד הנותר, במקרה שהטיול הנוכחי נכשל, נשאר כפי שהיה.

**מקום שלישי, לא התבקש אבל תוקן באותו עקרון**: `deleteTripPickerEntry()` - רענון הרשימה אחרי מחיקת/הסרת פריט מקבל `forceTripPicker: true` גם הוא. המשתמש כבר נמצא בפועל בתוך `#tripPicker` ומתעסק בו (רק מחק ממנו) - קפיצה פתאומית לטיול שנשאר, מיד אחרי מחיקה, היא אותה הפתעה בדיוק שהתיקון הזה נועד למנוע. שיקול דעת, לא דווח כבאג בפני עצמו - ראוי לציין במפורש.

### 🔍 מה שנבדק
1. **המקרה המדווח**: `renderTripPickerList({...טיול אחד...}, {forceTripPicker:true})` - הרשימה מוצגת בפועל (מאומת ב-DOM ובצילום מסך), `location.replace` לא נקרא
2. **הקיצור המכוון נשמר**: אותה קריאה בלי `forceTripPicker` - `location.replace` כן נקרא בפועל (אומת ש-`location.href` השתנה בפועל ל-URL של הטיול, לא רק בדיקת spy)
3. קונסול נקי
4. `node scripts/i18n-audit.js index.html` — 0 ממצאים (אין טקסט חדש למשתמש)

נבדק בפועל: כל מה שלמעלה. v4.61.0 -> v4.61.1 (patch - תיקון באג, בלי תכונה חדשה).

## v4.61.0 — "יצירת טיול חדש" בתפריט ההמבורגר, נגיש לכל משתמש מחובר (index.html)

### 🎯 מה נוסף
פריט "יצירת טיול חדש" ➕ חדש בתפריט ההמבורגר, בין "הטיולים שלי" ל"שיתוף ההזמנה". קישור `href="wizard.html"` פשוט, בלי פרמטרים - נחקר קודם: wizard.html לא קורא שום URL param (מלבד דגל דיבוג), ואין לו מנגנון ביטול/חזרה משלו, אז פרמטר `tripId`/`from` כאן היה משקל מת, לא תכונה. עד עכשיו הדרך היחידה ל-wizard.html מ-index.html הייתה `adminNewTripBtn` (בפאנל האדמין, מארגנים בלבד) או `tripPickerCreateBtn` (מוצג רק למשתמש שאין לו אף טיול) - **משתתף עם טיול קיים שאינו מארגן לא הייתה לו שום דרך להגיע לוויזרד**. עכשיו כל משתמש חתום-פנים יכול.

### ✏️ המימוש
- מבנה זהה בדיוק ל-`tripsLink`/`hamburgerShareBtn`: `<a class="menu-item hidden">` עם `menu-item-icon` + span טקסט, `newTripLinkText` ב-`dashboardText`, `aria-label`/`title` מצביעים ישירות עליו - נבנה נכון מההתחלה, אין טרנרי נפרד.
- טקסט זהה בכוונה ל-`adminNewTripBtn`/`tripPickerCreateBtn` הקיימים ("יצירת טיול חדש" / "Create a new trip") - לא ניסוח שלישי לאותה פעולה.
- מוצג/מוסתר ב-`handleAuthStateChanged()` לכל משתמש חתום-פנים (`!user`) - **לא** מותנה ב-`isOrganizer()` כמו `adminNewTripBtn`, בדיוק כמו שאושר: יצירת טיול הופכת את היוצר למארגן שלו, בלי קשר לתפקיד שלו בטיולים אחרים - ו-wizard.html עצמו מעולם לא הניח אחרת (`requireOrganizer()` שם בודק רק "מחובר?", לא זהות/הרשאה).

### ⚠️ פער קיים, לא רגרסיה - הוכרע במפורש לא לטפל בו כאן
משתמש שלוחץ בטעות (מכל אחת משלוש נקודות הכניסה - הישנות או החדשה) אין לו דרך "לחזור" לטיול הקודם חוץ מ-back של הדפדפן - ל-wizard.html אין מנגנון ביטול/חזרה משלו. זה פער קיים כבר היום ב-`adminNewTripBtn`, לא נוצר כאן - הכפתור החדש רק מוסיף עוד נקודת כניסה לאותה התנהגות קיימת, לא מחמיר אותה. הוכרע במפורש להשאיר תוספת מנגנון ביטול/חזרה בוויזרד למשימה נפרדת, לא לערבב עם ה-commit הזה.

### 🔍 מה שנבדק
1. **משתתף שאינו מארגן** רואה את הפריט ומקבל `href="wizard.html"` תקין - בדיוק המקרה שחסר היום
2. `.hamburger-menu-list` - 5 ילדים בסדר הנכון: `darkModeToggle`, `tripsLink`, `newTripLink`, `hamburgerShareBtn`, `logoutLink`
3. שתי שפות - `aria-label`/`title`/טקסט גלוי שלושתם מאוחדים
4. קונסול נקי לכל אורך הבדיקה
5. `node scripts/i18n-audit.js index.html` — 0 ממצאים

נבדק בפועל: כל מה שלמעלה. v4.60.0 -> v4.61.0 (minor - נקודת כניסה חדשה שלא הייתה קיימת לחלק מהמשתמשים).

## v4.60.0 — תפריט המבורגר: שיתוף ההזמנה נגיש לכל משתתף, לא רק למארגן (index.html, commit 3/3)

### 🎯 מה נוסף
פריט "שיתוף ההזמנה" 💬 חדש בתפריט ההמבורגר, בין "הטיולים שלי" ל"יציאה". קורא לפונקציה הקיימת `updateAdminShareLink()` - לא ממומש מחדש. עד עכשיו הפעולה הזו הייתה נגישה אך ורק דרך פאנל האדמין, שחסום ל-`isOrganizer()` בלבד - כל משתתף אחר שרצה לשתף את קישור ההזמנה לא יכול היה. עכשיו כל משתתף חתום-פנים רואה את הפריט ויכול לשתף.

### ✏️ המימוש
- **`updateAdminShareLink()` עצמה לא בדקה הרשאת מארגן מעולם** - החסימה הייתה עקיפה לגמרי: נקודת הכניסה היחידה שלה (`openAdminPanelIfAllowed()`) יושבת מאחורי `requireOrganizerAccess()`, וה-`<a>` היחיד שהיא עדכנה (`adminShareWhatsappBtn`) קבור בתוך `#adminModal` שמשתתף רגיל אף פעם לא פותח. הפתרון: לא מוסיפים בדיקת הרשאה חדשה לשום מקום - **מוסיפים אלמנט יעד שני** (`hamburgerShareBtn`), והפונקציה מעדכנת את שני ה-`href` יחד (מערך `whatsappBtns`, לא שני עותקי קוד) בכל קריאה.
- `openHamburgerMenu()` קוראת ל-`updateAdminShareLink()` לפני הפתיחה - בדיוק כמו ש-`openAdminPanelIfAllowed()` כבר עושה לפאנל האדמין - כדי שהקישור יהיה טרי לכל מי שפותח את התפריט, לא רק למי שפתח את פאנל האדמין לאחרונה.
- `hamburgerShareBtn` מוצג/מוסתר ב-`handleAuthStateChanged()` בדיוק כמו `tripsLink`/`logoutLink`/`membersLink` - לכל משתמש חתום-פנים, **לא** מותנה ב-`isOrganizer()` כמו `adminGearBtn` (זה בדיוק ההבדל שהמשימה הזו נועדה ליצור).
- i18n נבנה נכון מההתחלה (לא תיקון בדיעבד כמו ב-commit 2): `hamburgerShareText` ב-`dashboardText`, ו-`aria-label`/`title` מצביעים ישירות עליו - אין טרנרי נפרד שהיה צריך להסיר.

### 🔍 הבדיקה הקריטית שביקשת - משתתף שאינו מארגן, נבדק בפועל
1. **משתתף (`tripOwnerId` לא תואם ל-`uid`)**: `isOrganizer()` מחזיר `false`, `adminGearBtn` מוסתר (פאנל האדמין נשאר חסום - `requireOrganizerAccess()` הציג את הדיאלוג האמיתי "רק מארגן הטיול יכול לשנות את ההגדרות האלה" ומנע פתיחה, `openAdminModal` נשארה `false`), **אבל** `hamburgerShareBtn` מוצג (`hidden` לא קיימת) עם `href` תקין - קישור `wa.me` אמיתי עם הודעה מלאה וקישור לטיול, לא `#`
2. **מארגן (`tripOwnerId === uid`)**: `openAdminPanelIfAllowed()` פותח את הפאנל בפועל ומעדכן את שני הכפתורים - `adminShareWhatsappBtn` וגם `hamburgerShareBtn` - עם `href` זהה בדיוק, מאמת שהם באמת מסונכרנים ולא שני מקורות שיכולים להתפצל
3. שתי שפות - `aria-label`/`title`/טקסט גלוי שלושתם "שיתוף ההזמנה"/"Share invite"
4. `.hamburger-menu-list` - 4 ילדים בסדר הנכון: `darkModeToggle`, `tripsLink`, `hamburgerShareBtn`, `logoutLink`
5. קונסול נקי לכל אורך הבדיקה, כולל צילום מסך בהקשר מלא (בהיר וכהה)
6. `node scripts/i18n-audit.js index.html` — 0 ממצאים
- **לא נבדק**: קישור מקוצר אמיתי מ-TinyURL (מגבלת הסביבה - אין רשת; `window.shortenUrl` כבר מכוסה מבדיקות קודמות, לא נבדק מחדש כאן)

נבדק בפועל: כל מה שלמעלה. v4.59.2 -> v4.60.0 (minor - יכולת חדשה למשתמש: שיתוף נגיש שלא היה נגיש קודם לכל מי שאינו מארגן). **זהו commit 3/3 האחרון בתוכנית תפריט ההמבורגר** - שלושת ה-commits הקודמים (תשתית, הזזת פריטים, שיתוף) משלימים את הרול-אאוט שתואר בתחילת התכנון.

## v4.59.2 — טקסט גלוי ליד ☰ (index.html)

### 🎯 מה נוסף
כפתור `hamburgerMenuBtn` (☰) מקבל טקסט גלוי לצידו - "תפריט"/"Menu" - באותה מוסכמה בדיוק שהייתה ל-`darkModeToggle`/`logoutLink` לפני שעברו לתוך התפריט ב-commit 1/2 (סמל + span טקסט נפרד). עד עכשיו ☰ היה אייקון-בלבד, בלי שום טקסט - נראה "תלוש" בתוך שורת ה-session.

### ✏️ המימוש
- נבדק קודם: `hamburgerMenuBtn` הציג רק את הסמל ☰, שום טקסט - אז זו הוספה אמיתית, לא כפילות.
- `hamburgerMenuBtnText` נוסף ל-`dashboardText.he`/`.en` (מכוסה ע"י הלולאה הגנרית). `aria-label`/`title` מצביעים ישירות על `text.hamburgerMenuBtnText` - **לא** טרנרי נפרד - בדיוק אותו תיקון מבני שכבר בוצע ל-`tripsLink` ב-commit 2, כדי לא לחזור על אותה בעיית i18n (שני מקורות שיכולים להתפצל).
- הטרנרי הישן `hamburgerMenuBtnLabel` (שהיה קיים בדיוק בשביל המצב "אייקון-בלבד") - הוסר במלואו.

### 🔍 מה שנבדק - כולל האיזון של .session-row
1. **טקסט גלוי מוצג נכון** - "☰ תפריט" בעברית, "☰ Menu" באנגלית, כולל `aria-label`/`title` מאוחדים לאותו ערך
2. **איזון `.session-row`** - נבדק עם `getBoundingClientRect()`: הכפתור (~75px) מול `.session-right` (~342px, נשלט ע"י שם המשתמש) - `justify-content:space-between` מפזר את השניים כראוי בלי קשר להבדל ברוחב. **מקרה קצה**: שם תצוגה ארוך מאוד (`"A Very Long Display Name Indeed"`) - `.session-right`'s טיפול overflow/ellipsis הקיים (`min-width:0`) סופג את הרוחב העודף, השורה לא נשברת ולא גולשת, גם באנגלית (LTR)
3. קונסול נקי לכל אורך הבדיקה
4. `node scripts/i18n-audit.js index.html` — 0 ממצאים

נבדק בפועל: כל מה שלמעלה. v4.59.1 -> v4.59.2 (patch - שיפור נגישות/בהירות לרכיב קיים, לא יכולת חדשה). **חוזרים עכשיו ל-commit 3/3 (שיתוף ההזמנה בתפריט) שעדיין ממתין לאישור סופי.**

## v4.59.1 — תפריט המבורגר: הזזת tripsLink/logoutLink + עיצוב אחיד (index.html, commit 2/3)

### 🎯 מה נוסף
`tripsLink` ו-`logoutLink` עוברים פיזית מתוך `.session-row` אל תוך `#hamburgerMenu` - שם, לצד `darkModeToggle` שכבר עבר ב-commit 1, מקבלים טקסט גלוי (לא רק סמל) ועיצוב אחיד (`.menu-item`/`.menu-item-icon`) לשלושתם. `.session-row`/`.session-right` נשארות עם 3 ילדים בלבד: `sessionUserLabel`, `editNameBtn`, `membersLink`.

### ✏️ המימוש
- `.hamburger-menu-list` (flex column, gap 4px) עוטפת את שלושת הפריטים. `.menu-item` - שורה אחידה (אייקון + טקסט, ריפוד 14px/12px, `border-radius:14px`, `hover`/`active` עם `var(--card-bg)`) - נבנתה מ-`.logout-link` הקיימת כנקודת ייחוס, אבל כמחלקה נפרדת: `.logout-link` היא flex-row צר שמתחרה על מקום עם `#sessionUserLabel` (`flex-shrink:0` בכוונה בגלל זה), `.menu-item` יושבת ברשימה אנכית בעמודה משלה - הקשר עיצובי שונה לגמרי, לא היה נכון לשתף מחלקה אחת בין השניים.
- `tripsLink` (`<a>`) ו-`logoutLink` (`<button>`) שניהם מקבלים פנימית `<span class="menu-item-icon">` + `<span id="...Text">` - `darkModeToggle` נשאר עם המבנה הפנימי שלו כפי שהיה (`applyDarkModeUI()` קובעת טקסט+אייקון כמחרוזת אחת משולבת, לא בנפרד) - רק `class="menu-item"` משותפת, לא צורת ה-markup הפנימית.
- **תיקון i18n מבני ל-`tripsLink`**: `tripsLinkText` נוסף ל-`dashboardText.he`/`.en` (מכוסה אוטומטית ע"י הלולאה הגנרית ב-`applyTripLanguage()`, כמו כל טקסט אחר בדשבורד). הטרנרי הנפרד `tripsLinkLabel` שקבע את `aria-label`/`title` באופן עצמאי - **הוסר**. עכשיו `aria-label`/`title` של `tripsLink` מוצבים ישירות מאותו `text.tripsLinkText` שקובע גם את הטקסט הגלוי - מקור אמת אחד, לא שניים שיכולים להתפצל. `membersLink` (אייקון-בלבד, בלי תוכנית לטקסט גלוי) נשאר בתבנית הטרנרי הישנה בכוונה - התיקון חל רק על אלמנט שקיבל טקסט גלוי בפועל.

### 🔍 מה שנבדק - 3 הבדיקות שביקשת, כולן בפועל בדפדפן אמיתי
1. **שלוש הפעולות עובדות מהמיקום החדש**: מצב כהה - `darkModeToggle` בתוך התפריט אכן קובע `body.dataset.theme='dark'` ומחליף טקסט/אייקון (`☀️ מצב בהיר`), בפועל דרך קליק אמיתי; הטיולים שלי - `tripsLink.href` תקין; יציאה - קליק אמיתי על `logoutLink` הפעיל את ה-listener הקיים (`toast` + `auth.signOut()`), בלי שינוי בקוד ה-wiring עצמו (אותו `id`, אותה קריאת `getElementById` בדיוק כמו לפני ההעברה)
2. **שתי שפות כולל ה-aria-label המאוחד**: עברית - `tripsLink` aria-label/title/טקסט גלוי שלושתם "הטיולים שלי"; אנגלית (`currentLanguage='en'` + `applyTripLanguage()`) - שלושתם "My trips" - מאומת שהם אותו ערך בדיוק, לא שני מקורות שיכולים להתפצל. גם `hamburgerMenuTitle`/`logoutLinkText` נבדקו בשתי השפות
3. **`.session-right` - 3 ילדים, לא יותר**: `Array.from(sessionRight.children).map(c=>c.id)` בדום חי החזיר בדיוק `["sessionUserLabel","editNameBtn","membersLink"]`
- בונוס: Escape סוגר את התפריט גם אחרי הפעולות למעלה (עדיין יורש מהחיווט הגנרי, לא נשבר מההעברה)
- קונסול נקי לכל אורך הבדיקה, כולל 375px
- `node scripts/i18n-audit.js index.html` — 0 ממצאים
- **לא נבדק**: sign-in אמיתי מול Firebase (מגבלת הסביבה - נבדק דרך `handleAuthStateChanged()` האמיתית עם משתמש מדומה, לא reimplementation, כדי לחשוף את `.hidden`/`.session-right` כמו ב-production)

נבדק בפועל: כל מה שלמעלה. v4.59.0 -> v4.59.1 (patch - המשך תשתית ה-commit 1, אין תכונה חדשה למשתמש שלא הייתה שם ב-commit 1 מלבד המיקום/העיצוב). **commit 3 (הוספת "שיתוף ההזמנה" לתפריט, קורא ל-`updateAdminShareLink()` הקיימת) הבא בתור.**

## v4.59.0 — תפריט המבורגר: תשתית + כפתור ☰ (index.html, commit 1/3)

### 🎯 מה נוסף
משטח `.modal-overlay` שישי, `#hamburgerMenu` - עדיין כמעט ריק, זו רק התשתית. כפתור ☰ חדש **מחליף** את `darkModeToggle` באותו slot בדיוק ב-`.session-row` (לא נוסף לצידו) - `.session-row` נשארת דו-חלקית (עוגן יחיד מול `.session-right`) בדיוק כמו היום.

### ⚠️ סטייה מכוונת מהתכנון - דווחת, לא בשקט
התכנון ביקש "bottom-sheet ריק". בפועל: `applyDarkModeUI()` והמאזין ל-click על מצב כהה שניהם קוראים `document.getElementById("darkModeToggle")` **בלי תנאי** - מחיקה/הסתרה של האלמנט לגמרי הייתה **שוברת** את מתג המצב הכהה לחלוטין (או קורסת עם `TypeError`) מהרגע שה-commit הזה היה עולה, עד ש-commit 2 היה "רשמית" מטפל בזה. **הפתרון**: `darkModeToggle` **הועבר כמות שהוא** (אותו `id`, אותו קוד JS, בלי שינוי) לתוך `#hamburgerMenu` - לא נמחק, לא שוכפל. המשמעות המעשית: מתג המצב הכהה **ממשיך לעבוד** מהרגע הזה, רק ממוקם עכשיו בתוך התפריט. commit 2 (לפי התוכנית: "הזזת darkModeToggle/tripsLink/logoutLink") נשאר בפועל עם `tripsLink`+`logoutLink` בלבד להזזה, ועיצוב-פריט-תפריט אמיתי לשלושתם - `darkModeToggle` כבר במיקומו הסופי.

### ✏️ המימוש
- `openHamburgerMenu()`/`closeHamburgerMenu()` - זהים בדיוק לתבנית `openXModal()`/`closeXModal()` הקיימת בכל 5 המשטחים האחרים - יורשים backdrop-click ו-Escape **בחינם** מהחיווט הגנרי הקיים (`querySelectorAll(".modal-overlay")`/`document.addEventListener("keydown"...)`) - אפס קוד קואורדינציה חדש
- `hamburgerMenuTitle` נוסף ל-`dashboardText` (תבנית רגילה). `hamburgerMenuBtn` (אייקון-בלבד, ☰ בלי טקסט מתוכנן) מקבל `aria-label`/`title` באותה תבנית טרנרי-מקומי כמו `tripsLink`/`membersLink` הקיימים - לא ב-`dashboardText`, כי הוא לא עתיד לקבל טקסט גלוי (בניגוד ל-`tripsLink` שכן, ב-commit 2)

### 🔍 מה שנבדק - 5 הבדיקות שביקשת, כולן בפועל
1. **פתיחה/סגירה בלחיצה אמיתית על ☰** - `document.getElementById('hamburgerMenuBtn').click()` פתח את `#hamburgerMenu` בפועל
2. **קליק מחוץ לחלונית סוגר** - `dispatchEvent` של קליק על ה-backdrop עצמו (לא ה-sheet) - נסגר, בלי קוד ייעודי
3. **Escape סוגר** - `KeyboardEvent('keydown', {key:'Escape'})` אמיתי - נסגר
4. **`.session-row` מאוזנת ויזואלית** - `sessionRow.children` אומת: 2 ילדים בלבד (`#hamburgerMenuBtn` + `.session-right`), בדיוק כמו `darkModeToggle`+`.session-right` היום - צילום מסך ב-375px מאשר ויזואלית, אין שבירה
5. **5 המשטחים הקיימים לא מתנגשים** - כל אחד מ-`profileModal`/`membersModal`/`lockerModal`/`seatsModal`/`adminModal` נפתח ונסגר בנפרד בפועל, ואחרי כל אחד ☰ עדיין עובד ופותח את התפריט
- **בונוס, לא התבקש אבל נבדק בפועל**: מתג המצב הכהה עדיין עובד מהמיקום החדש שלו בתוך התפריט (`darkModeToggle.click()` שינה בפועל את `body.dataset.theme`) - מוודא שהסטייה מהתכנון (סעיף למעלה) באמת פותרת את הבעיה שהיא נועדה לפתור
- עברית ואנגלית (כותרת התפריט, `aria-label`/`title` של ☰)
- קונסול נקי לכל אורך הבדיקה
- `node scripts/i18n-audit.js index.html` — 0 ממצאים

נבדק בפועל: כל מה שלמעלה. v4.58.0 -> v4.59.0 (minor - תשתית UI חדשה, גלויה למשתמש). **commit 2 (הזזת tripsLink/logoutLink + עיצוב פריטי תפריט + תיקון i18n מבני ל-tripsLink) הבא בתור.**

## v4.58.0 — בורר חברת תעופה גם בוויזרד, לא רק בפאנל האדמין

### 🎯 מה נוסף
`#checkinGroup` בשלב 1 של הוויזרד (שכבר הכיל את שדה "קישור לצ'ק-אין" הקיים, ומוצג/מוסתר לפי `tripType==='abroad'` דרך `syncTripTypeFields()` הקיימת) מקבל בורר "חברת תעופה (אופציונלי)" - 9 חברות מאומתות + "אחר" - ממש כמו בפאנל האדמין. מארגן שכבר יודע את חברת התעופה בזמן יצירת הטיול לא צריך יותר לחזור לפאנל האדמין כדי למלא את קישור הצ'ק-אין.

### ✏️ המימוש - שכפול מכוון, לא שיתוף, כמו כל שכפול אחר היום
`AIRLINE_CHECKIN_URLS` (9 כתובות מאומתות, אותה רשימה בדיוק) ולוגיקת ה-`change` (בדיקת ריק/זהה/שונה, `customConfirm` לפני דריסה, "snap back ל-Other" בביטול) - כתובים פעם שנייה, עצמאית, באידיום הטרנרי של הוויזרד (אין `tr()` כאן). **הערה צולבת נוספה בשני הכיוונים** - index.html מצביע על wizard.html ולהפך: "אם מוסיפים/מעדכנים חברה כאן, עדכנו גם שם".

**שלוש ההכרעות שאושרו מומשו במדויק**:
1. **הבורר לא נשמר ב-draft כשדה נפרד** - `restoreWizardDraft()` משחזרת רק את `checkinUrl` (כמו תמיד), ואז מבצעת חיפוש-הפוך ב-`AIRLINE_CHECKIN_URLS`: אם ה-URL המשוחזר תואם בדיוק לאחת מ-9 הכתובות, הבורר נקבע לחברה הזו אוטומטית; אחרת (הוקלד ידנית, או לא נמצאה התאמה) - נשאר "אחר", בלי לנחש
2. הבורר יושב **בתוך** `#checkinGroup` עצמו, מעל שדה הקישור - יורש את התניית ה-`abroad` בחינם, בלי קוד תנאי נוסף
3. "אחר" נכלל ברשימה - זהה במלואה ל-9+1 האפשרויות בפאנל האדמין

### 🔍 מה שנבדק - 7 הבדיקות שביקשת, כולן קצה-לקצה אמיתי
1. **abroad** - הבורר מוצג (`display:block`), יורש את ההתניה מ-`syncTripTypeFields()` הקיימת בלי שינוי בה
2. **לא-abroad** (`local`) - כל `#checkinGroup` מוסתר, כמו היום
3. **בחירת חברה** (Lufthansa, שדה ריק) - ממלאת את `checkinUrl` בקישור הנכון בדיוק
4. **draft עם קישור ידני שלא תואם אף חברה** - `localStorage['wizardDraft']` אמיתי + טעינת עמוד אמיתית (לא סימולציה) - הבורר משוחזר כ"אחר", לא בחירה שגויה
5. **draft עם קישור תואם** (KLM) - אותה שיטה, הבורר משוחזר אוטומטית ל-`klm` - **מאומת עם `restoreWizardDraft()` האמיתית שרצה בטעינת עמוד, לא reimplementation**
6. **שני תרחישי `customConfirm`**: (א) הקלדת קישור ידני ואז בחירת חברה שונה - הדיאלוג האמיתי נפתח (`.custom-dialog-overlay` ממשי בדום, לא מדומה) עם הטקסט הנכון כולל שם החברה; ביטול מחזיר את הבורר ל"אחר" בלי לגעת ב-URL; אישור מחליף את ה-URL. (ב) **בדיוק התרחיש שביקשת בנפרד**: draft עם קישור קיים שמשוחזר, ואז בחירת חברה אחרת - גם כאן הדיאלוג האמיתי נפתח, עם ה-URL המקורי נשאר עד לאישור
7. **עברית ואנגלית** - לייבל, hint, וכל 10 אפשרויות הבורר, וגם טקסט דיאלוג ה-`customConfirm` - כל אחד נבדק בנפרד ואומת מדויק

- כל הבדיקות רצו נגד DOM אמיתי, כולל קליקים אמיתיים על דיאלוג אמיתי (לא `window.customConfirm` מוחלף) ו-`localStorage`+טעינת-עמוד אמיתיים ל-draft (לא הרצת פונקציה מבודדת) - הרמה הגבוהה ביותר של אימות שהיה ניתן בסביבה הזו
- קונסול נקי לכל אורך הבדיקה, כולל 375px בהקשר מלא
- `node scripts/i18n-audit.js wizard.html index.html` — 0 ממצאים
- **לא נבדק**: יצירת טיול אמיתית מקצה לקצה מול Firebase (מגבלת הסביבה)

נבדק בפועל: כל מה שלמעלה. v4.57.0 -> v4.58.0 (minor - יכולת חדשה).

## v4.57.0 — מחיקה/הסרה מ-#tripPicker (index.html, commit ב/ב)

### 🎯 מה נוסף
כרטיס בכל שורה ב-`#tripPicker` ("הטיולים שלי" ב-index.html) מקבל כפתור 🗑️ נפרד, לצד הקישור הקיים לפתיחת הטיול - לא בתוכו. לחיצה בודקת `ownerId` אמיתי מול הטיול (אותו מנגנון בדיוק שתוקן ב-commit א ב-wizard.html) ומציגה את אחת משתי פעולות שונות מהותית: מארגן מקבל "מחיקה לצמיתות" (משפיעה על כולם), חבר מקבל "הסרה מהרשימה שלי" (משפיעה רק על ה-mirror שלו-שלו).

### ✏️ המימוש - מימוש שני, עצמאי, לא שיתוף קוד עם wizard.html
`deleteTripPickerEntry()` חדשה ב-index.html משקפת את `deleteRecentTrip()` ב-wizard.html (commit א) - אותה בדיקת `ownerId`, אותו fallback מכוון כש-`ownerId` חסר, אותו פיצול לשתי פעולות - אבל כתובה מחדש ב-SDK ה-compat של index.html (`db.ref(path).once("value")`, `db.ref().update({...})`) ולא כשיתוף קוד עם הגרסה המודולרית של wizard.html - אותה סיבה בדיוק שכבר תועדה היום פעמים רבות (5+1 העמודים, `tr()` קיים באחד וחסר בשני).

**מבנה כרטיס שונה מהותית מ-#tripPicker הישן**: הכרטיס עבר מ-`<a>` יחיד (כל הכרטיס לחיץ) ל-`<div>` שמכיל `<a class="trip-picker-card-link">` (החלק הלחיץ לפתיחה) **וגם** `<button class="trip-picker-delete-btn">` בתור אח נפרד - לא כפתור מקונן בתוך קישור (HTML לא תקין, והיה גם גורם ללחיצת מחיקה לפתוח את הטיול בטעות).

### 🐛 באג פריסה שנמצא ותוקן תוך כדי בדיקה - `.trip-picker-list`
בדיקה עם כותרת ארוכה חשפה **גלישה אופקית אמיתית**: ל-`.trip-picker-list` (`display:grid`) לא היה `grid-template-columns` מפורש - טור גריד implicit מתרחב לפי ה-max-content של הכרטיס הרחב ביותר, במקום להיות מוגבל לרוחב הקונטיינר, כך שה-ellipsis הקיים על הכותרת (`.trip-picker-card-info strong`) לא הספיק לתפוס בזמן. **כנראה קיים גם לפני השינוי הזה** (`.trip-picker-list` לא נגעתי בו קודם) - אבל כפתור המחיקה החדש מצמצם עוד יותר את השטח הפנוי לכותרת, מה שהופך את זה לסביר לקרות גם בכותרות רגילות, לא רק קיצוניות. תוקן: `grid-template-columns: minmax(0, 1fr);` מפורש.

### 🔍 מה שנבדק
- **מבחן קצה-לקצה אמיתי על `deleteTripPickerEntry()` עצמה** (לא reimplementation נפרד): `db.ref`/`customConfirm` הוחלפו זמנית כדי ללכוד מה שהפונקציה האמיתית שולחת, בלי לגעת ב-Firebase אמיתי - 3 מקרים: **חבר** (הודעת "הסרה מהרשימה שלי", ה-payload נוגע **רק** ב-`users/{uid}/trips/{tripId}` שלו - `trips/{tripId}` נעדר לגמרי מהאובייקט), **מארגן** (הודעת מחיקה מלאה, שני הנתיבים ב-payload), **`ownerId` חסר** (נופל בחזרה למארגן, כמו שהוחלט)
- שתי השפות לשתי ההודעות - נבדק ישירות
- **הבאג שנמצא תוקן ואומת מחדש**: אותה כותרת ארוכה, 375px - עכשיו נחתכת ב-ellipsis נכון, אין גלילה אופקית
- כהה/בהיר, כרטיס עם כותרת ובלי - כפתור המחיקה יושב נכון משני הכיוונים (RTL - בצד שמאל, לא נדחף החוצה)
- קונסול נקי לכל אורך הבדיקה
- `node scripts/i18n-audit.js index.html` — 0 ממצאים
- **לא נבדק**: זרימה מלאה מול Firebase אמיתי (מגבלת הסביבה) - `db.ref`/`customConfirm` הוחלפו זמנית בבדיקה, לא נבדקה כתיבה אמיתית ל-RTDB

### 📋 מה שנשאר פתוח (לא בהיקף, כמו ב-commit א)
אותו פער בדיוק כמו ב-wizard.html: mirror של חברים אחרים וקבצי Storage לא מנוקים בשום נתיב מחיקה, כולל זה החדש. ר' v4.56.1.

נבדק בפועל: כל מה שלמעלה. v4.56.1 -> v4.57.0 (minor - יכולת חדשה). **זה סוגר את שני ה-commits המתוכננים (א+ב) של משימת המחיקה.**

## v4.56.1 — 🔒 תיקון אבטחה: חבר-לא-מארגן יכול היה למחוק טיול שלם (commit א/ב)

### 🔒 זה תיקון אבטחה, לא רק ניקוי קוד
`deleteRecentTrip()` ב-wizard.html ("טיולים אחרונים") בדקה רק אם הטיול קיים ב-`cloudTrips` של המשתמש הנוכחי (`users/{uid}/trips`) - **לא** בדקה `ownerId` בכלל, כי ה-mirror הזה מעולם לא כלל את השדה. המשמעות בפועל: **כל משתמש מחובר שהטיול הופיע ברשימה שלו-שלו - בין אם יצר אותו ובין אם רק הצטרף אליו - יכול היה למחוק את `trips/{tripId}` כולו, עבור כל המשתתפים, ללא הבחנה.** נחשף בחקירה שביקשת על הוספת מחיקה ב-`#tripPicker` - לפני שמוסיפים עוד דרך נגישה להפעיל את אותה פעולה, תוקן המקור.

### ✏️ התיקון
- קריאת `ownerId` אמיתית מהטיול (`get(ref(db, \`trips/${tripId}/ownerId\`))`) **לפני** קביעת איזו פעולה מותרת - `get` נוסף ל-import של wizard.html (לא היה קיים קודם)
- **`ownerId` חסר (טיול ישן, לפני שהשדה נוסף) - נופל בחזרה במכוון להתנהגות הישנה** (מחיקה מלאה מותרת) - **החלטה מכוונת, לא פער שנשכח**: זו הדרך היחידה שטיול כזה נשאר ניתן-למחיקה בכלל על ידי מי שיצר אותו בפועל, כי אין שדה לבדוק מולו. תועד גם בהערת קוד
- **מארגן אמיתי** (`ownerId === currentUser.uid`, או `ownerId` חסר): אותה התנהגות בדיוק כמו היום - מחיקה מלאה, אותו ניסוח אישור
- **חבר שאינו מארגן**: פעולה חדשה - מוחקת **רק** את `users/{currentUser.uid}/trips/{tripId}` שלו-שלו, `trips/{tripId}` הראשי לא נוגע בו כלל. ניסוח אישור נפרד: "להסיר את הטיול הזה מהרשימה שלך? הטיול עצמו יישאר קיים עבור שאר המשתתפים." - זו בעצם "יציאה מטיול", בניסוח לא-דרמטי

### 🔍 מה שנבדק
- **לוגיקת ההחלטה** (הביטוי המדויק): כל 4 השילובים - לא-בענן (מקומי בלבד), מארגן מוחק, חבר "מוחק", ownerId חסר (fallback) - כל אחד נותן את סוג ההודעה ואת ה-payload הנכונים
- **ה-payload המדויק שנשלח ל-`update()`**: אומת בנפרד - עבור חבר, `trips/{tripId}` **נעדר לגמרי** מהאובייקט שנשלח (לא רק "ריק" - לא קיים בו בכלל). עבור מארגן, שני הנתיבים קיימים, זהה למקור
- **6 הודעות האישור** (3 מצבים × 2 שפות) - כל אחת נבדקה בנפרד, מנוסחות נכון וברורות לגבי מה בדיוק קורה
- קונסול נקי, אין שגיאות ייבוא (`get` מיובא נכון)
- `node scripts/i18n-audit.js wizard.html` — 0 ממצאים
- **לא נבדק**: קריאת `get()` אמיתית מול Firebase (מגבלת הסביבה - אין רשת אמיתית מקומית) - נבדקה לוגיקת ההחלטה וה-payload בבידוד, לא זרימת ה-`await get(...)` המלאה מול RTDB אמיתי

### 📋 מה שנשאר פתוח (לא בהיקף המשימה הזו)
יתומי מחיקה - ה-mirror של חברים אחרים (`users/{their-uid}/trips/{tripId}`) וקבצי תמונות ב-Firebase Storage (`trip-media/{tripId}/...`) לא מנוקים בשום נתיב מחיקה, גם אחרי התיקון הזה. פער ידוע, מתועד, לא נעלם - ר' סעיף 7+9 בדוח החקירה. לא מתוקן כעת - מחוץ להיקף.

נבדק בפועל: כל מה שלמעלה. v4.56.0 -> v4.56.1 (patch - תיקון אבטחה בהתנהגות קיימת, אין שינוי סכמה). **commit ב (הוספת שתי הפעולות המקבילות ל-#tripPicker) בתור.**

## v4.56.0 — קיצור קישורים בהודעות וואטסאפ (TinyURL, לא מנגנון עצמי)

### 🎯 מה נוסף, ולמה TinyURL ולא is.gd
קישורי השיתוף בוואטסאפ (הוויזרד ופאנל האדמין) התקצרו - רק בגוף ההודעה, לא בשדה "העתק לינק"/`openTripBtn`/`finalTripLink` שנשארים מלאים כמו תמיד. **is.gd (ואחיו v.gd) נבדקו ונפסלו בפועל, לא בתיעוד**: `curl` ישיר עם `Origin` header הראה **שאין להם בכלל `Access-Control-Allow-Origin`** - `fetch()` מהדפדפן היה נכשל אצל **כל** משתמש אמיתי, לא רק בסביבת הבדיקה (אומת גם מול production אמיתי, לא רק מקומי). **TinyURL (`api-create.php`, ה-endpoint הישן) נבחר במקום spoo.me** (ששלח `Access-Control-Allow-Origin: *` נדיב יותר) **מסיבת אמון, לא טכנית**: קישור מדומיין מוכר (tinyurl.com) שמישהו מקבל מחבר בוואטסאפ נראה אמין יותר מדומיין אלמוני - בדיוק הבעיה המקורית ("קישור לא מקצועי") שהתחלנו לפתור.

**redirect אומת בפועל, לא הונח**: `curl -I` על קישור tinyurl.com אמיתי החזיר `301 Moved Permanently` עם `Location` header ישיר (`x-tinyurl-redirect-type: redirect`) - בלי מסך ביניים. redirect אמיתי ברמת HTTP, כך שסורק התצוגה המקדימה של וואטסאפ עוקב אחריו בדיוק כמו כל לקוח HTTP ומגיע לעמוד האמיתי, איפה שה-og:title/og:image הקיימים כבר עובדים.

### ⚠️ סיכון מתועד בכוונה
`api-create.php` הוא **endpoint ישן ולא-רשמי** של TinyURL - לא חלק מה-API הרשמי הנוכחי שלהם (שדורש חשבון/טוקן), ואין הבטחה שימשיך לעבוד. תועד במפורש בהערת הקוד ב-`urlShortener.js` - אם זה יפסיק לעבוד **לכולם בבת אחת** (לא מקרים בודדים), זו הסיבה הסבירה, לא לחקור מחדש מאפס.

### ✏️ הארכיטקטורה
- **`urlShortener.js` חדש** - נטען רק ב-index.html וב-wizard.html (לא בכל 6 העמודים - זה משמש רק את שניהם, לא הצדיק תשתית רחבה יותר). `window.shortenUrl(longUrl)` - פונקציה טהורה, בלי תלות ב-`tr()`/שפה, `AbortController` עם timeout של 6 שניות (אותו ערך כמו `detectMusicTitle()` ב-places.html - התקדים הקיים היחיד לקריאת API חיצוני שלישי באפליקציה), מחזירה `null` בכל כשלון (רשת, CORS, timeout, תגובה לא תקינה) - אף פעם לא זורקת
- **wizard.html** (כבר `async`): `await window.shortenUrl(shareUrl)` **לפני** הצגת מסך ההצלחה - `whatsappLink = (await shortenUrl(shareUrl)) || shareUrl`. הקיצור, אם מצליח, כבר בהודעה מהרגע שהכפתור מוצג
- **index.html** (`updateAdminShareLink()`, נשארת סינכרונית לחלוטין ל-href הראשוני): הכפתור מקבל קישור מלא **מיד**, בלי לחכות לרשת בכלל. קריאה אסינכרונית "בצד" ל-`shortenUrl()` מחליפה את ה-`href` בשקט אם/כשהיא מצליחה. **`shareLinkRequestId`** (אותו דפוס `profileLoadGen` שכבר קיים ב-itinerary.html/places.html) מונע ממענה ישן לדרוס `href` חדש יותר אם הפאנל נפתח פעמיים ברצף

### 🔍 מה שנבדק - קריאות רשת אמיתיות, לא מדומות
- **קיצור מצליח בפועל (לא תיאורטי)**: `window.shortenUrl()` הורצה בדפדפן מול TinyURL האמיתי, פעמים רבות, בשני הקבצים - כולם החזירו קישור `https://tinyurl.com/...` תקין. **redirect** אומת בנפרד דרך `curl -I` (301 + Location, לא interstitial) - לא נבדק בפועל בתוך אפליקציית וואטסאפ עצמה (אין גישה לזה בסביבה הזו), אבל redirect אמיתי ב-HTTP לא תלוי בזה
- **נפילה בשקט (שירות מדומה שנכשל)**: `window.shortenUrl` הוחלף זמנית בפונקציה שמחזירה `null` - בשני הקבצים, הכפתור/ההודעה חזרו לדיוק מה שהיה לפני התכונה, בלי שגיאה
- **לחיצה לפני שהקיצור הגיע (מרוץ, index.html)**: נבדק ישירות - `href` בזמן `t=0` (מיד אחרי הקריאה) כבר מכיל את הקישור **המלא** התקין, לא ריק/שבור. אחרי ~2 שניות (השדרוג האמיתי חוזר), אותו `href` מכיל את הקישור המקוצר
- **race guard בפועל**: קריאה כפולה מהירה ל-`updateAdminShareLink()` עם שני ערכי כותרת שונים - הקישור הסופי משקף רק את הקריאה השנייה (החדשה), התשובה המאוחרת של הקריאה הראשונה (הישנה) לא דרסה אותו
- שתי השפות, עם/בלי כותרת - כל השילובים נבדקו מול קריאה אמיתית
- קונסול נקי לכל אורך הבדיקה
- `node --check urlShortener.js`, `node scripts/i18n-audit.js index.html wizard.html` — 0 שגיאות/ממצאים

נבדק בפועל: כל מה שלמעלה, כולל קריאות רשת אמיתיות (לא מדומות) לשירות חיצוני אמיתי. v4.55.0 -> v4.56.0 (minor - יכולת חדשה).

## v4.55.0 — כפתור "שיתוף בוואטסאפ" במסך הסיום של הוויזרד

### 🎯 מה נוסף
מסך הסיום של הוויזרד (אחרי יצירת הטיול) הציג עד עכשיו רק "העתק לינק". נוסף כפתור שני, "💬 שיתוף בוואטסאפ", שפותח את WhatsApp עם קישור הטיול והודעה מוכנה מראש - שימוש חוזר באותה תבנית `wa.me/?text=` שכבר קיימת בפאנל הניהול (`updateAdminShareLink()`), לא בנייה מאפס.

### ✏️ המימוש
- **פריסה**: שורה נפרדת, ברוחב מלא, מתחת לקופסת "העתק לינק" (`.share-link-box`) - לא דחוסה כפריט שלישי באותה שורה עם שדה הטקסט. `class="btn btn-outline"` - סגנון קיים כבר בוויזרד (`newTripBtn`), לא סגנון חדש
- **חיווט**: בתוך אותו בלוק קוד שכבר בונה את `shareUrl` ומזין אותו ל-`finalTripLink.value` - לא פונקציה נפרדת, לא קריאה מאוחרת. `href`/`style.display` נכתבים מחדש בכל הרצה של `generateTripAndSave()`, כך שטיול שני שנוצר **באותו session** (אחרי "צור טיול חדש") לא נשאר עם קישור מהטיול הקודם. `resetWizard()` מחזיר אותו ל-`display:none`
- **טקסט ההודעה**: אותה לוגיקה בדיוק כמו `updateAdminShareLink()` (מעדיפה `tripTitle`, נמנעת מ"לטיול לטיול X" כפול) - **אבל מומשת פעם שנייה, עצמאית**, באידיום הטרנרי של wizard.html (`currentLanguage === 'he' ? ... : ...`), לא דרך `tr()` שלא קיים בקובץ הזה. **הערה צולבת נוספה בשני הקבצים** - כל אחד מצביע על השני, כדי שתיקון ניסוח עתידי (כמו זה שכבר קרה היום) לא יישכח להתעדכן בשני המקומות

### 🔍 מה שנבדק
- **הביטוי המדויק** (לא רק "נראה נכון"): עם כותרת - "היי חברים, הצטרפו אליי **לטיול חברים לפורטוגל**! כל הפרטים כאן:..." (בלי כפילות, `dt=1` בקישור המוטמע). בלי כותרת - "היי חברים, הצטרפו אליי **לטיול ל**ליסבון!..." - זהה לניסוח הישן
- **פריסה בהקשר מלא**: צילום מסך של מסך הסיום המלא (דסקטופ, כהה) - השורה החדשה יושבת טבעי בין קופסת הקישור לשתי הכפתורים "פתח את הטיול"/"צור טיול חדש". גם אנגלית/בהיר/375px - אין גלישה, ההיררכיה הצבעונית עקבית (Copy Link/Open trip ראשיים בטורקיז, Share on WhatsApp/Create another trip משניים באפור)
- **`resetWizard()`**: מוודא בפועל שהכפתור חוזר ל-`display:none` אחרי איפוס - כדי שטיול שני לא "יירש" קישור ישן
- קונסול נקי לכל אורך הבדיקה
- `node scripts/i18n-audit.js wizard.html index.html` — 0 ממצאים
- **לא נבדק**: יצירת טיול אמיתית מקצה לקצה מול Firebase (מגבלת הסביבה) - נבדק דרך הרצת הביטוי המדויק מהקוד נגד ה-DOM האמיתי של העמוד, כולל אימות ש-`resetWizard()` בפועל מחזיר את הכפתור למצב מוסתר

נבדק בפועל: כל מה שלמעלה. v4.54.0 -> v4.55.0 (minor - יכולת שיתוף חדשה).

## v4.54.0 — עריכת "שם הטיול" בפאנל האדמין (commit 6/6, אחרון בסדרה)

### 🎯 הסיבה: טיולים קיימים אין להם דרך להוסיף כותרת בדיעבד
כל 5 ה-commits הקודמים הניחו ש-tripTitle כבר קיים (נקבע פעם אחת, בוויזרד, בזמן יצירת הטיול). לטיול שכבר נוצר - כולל כל טיול שנוצר **לפני** ש-tripTitle בכלל היה קיים בסכימה - לא הייתה שום דרך להוסיף כותרת. commit זה סוגר את הפער: שדה חדש, "שם הטיול (אופציונלי)", בתוך "🌍 פרטי טיול" הקיים בפאנל האדמין - לא box חדש.

### ✏️ המימוש
- **מיקום**: לפני "סוג הטיול" (לא אחרי) - עקבי עם ההחלטה כבר ב-wizard.html שכותרת קודמת ליעד
- **טעינה**: `openAdminPanelIfAllowed()` ממלאת את השדה מ-`tripTitle` (המשתנה הגלובלי הקיים, שכבר עוקב אחרי `data.tripTitle` מאז commit 2)
- **שמירה**: `saveFlightBtn`'s handler קורא `newTripTitle`, מעדכן את `tripTitle` הגלובלי ואת `destinationName` (מעדיף את הכותרת החדשה על פני היעד, לא מחכה ל-listener - אותו נימוק כמו `authGateCopy()`), וכותב `tripTitle: newTripTitle || null` ל-RTDB - **`null`, לא `""`** - כדי שניקוי השדה בפועל **ימחק** אותו (לא ישאיר מחרוזת ריקה), אותה מוסכמה בדיוק כמו `settings.legs = null` הקיים באותה פונקציה
- **שדה trip-level, לא תלוי multiLeg**: מעודכן ללא תנאי (בניגוד ל-`destination`/`lat`/`lon` שנכתבים רק ב-single-leg) - כותרת חופשית לא קשורה למבנה היעדים
- **כתיבה כפולה, באותה קריאת `update()` בדיוק** (כמו בתוכנית המקורית, ובדיוק כמו commits 1/2ב): `db.ref().update(updates)` יחיד, עם `settings` (כל השדות שהשתנו, כולל `tripTitle`) משוטח לנתיבים מלאים תחת `trips/{tripId}/...`, **ועוד מפתח נוסף** באותו אובייקט - `users/{tripOwnerId}/trips/{tripId}/tripTitle`. לא שתי קריאות `update()` נפרדות - קריאה אטומית אחת. השטחה (לא `db.ref(tripRoot).update(settings)` הישן) נדרשה בגלל ש-Firebase `update()` על נתיב שמכיל אובייקט **מחליף אותו כולו**, לא ממזג - השטחה לשדות בודדים שומרת בדיוק על התנהגות המיזוג השטוח שהייתה קודם, בלי לאבד שדות אחרים בטיול (participants, features וכו') שלא נמצאים ב-`settings`

### ⚠️ מגבלה קיימת שנשארת, לא חדשה
הכתיבה הכפולה מעדכנת את ה-mirror **של המארגן בלבד** (`tripOwnerId`, אותו scope בדיוק כמו commit 1). **חברים שכבר הצטרפו** לפני העריכה - ה-`users/{uid}/trips/{tripId}` שלהם-שלהם לא מתעדכן, כי אין להם רשומה נפרדת שנכתבת כאן. בדקתי: זו לא רגרסיה - שום שדה אחר בפאנל (יעד, סוג טיול) גם לא מתפשט לחברים כאלה כשמשתנה. תיקון מלא (סנכרון חי לכל חברי הטיול, לא רק המארגן, בכל עריכה) הוא משימה נפרדת וגדולה משמעותית - מחוץ להיקף.

### 🔍 מה שנבדק
- **טעינת השדה**: טיול עם כותרת קיימת - השדה נטען מלא עם הערך הנכון בפתיחת הפאנל. טיול בלי כותרת - השדה נטען ריק (לא `undefined`, לא היעד)
- **לוגיקת שמירה** (הביטוי המדויק, לא רק "נראה נכון"): הגדרת כותרת חדשה - `tripTitle`/`destinationName`/ה-payload שיישלח כולם מתעדכנים נכון, `destinationName` מעדיף את הכותרת החדשה מיידית. ניקוי כותרת - `settings.tripTitle` הופך ל-`null` (לא `""`), `destinationName` נופל בחזרה ל-destination
- **הכתיבה הכפולה** (הביטוי המדויק מ-`updates`): עם כותרת - `trips/test123/tripTitle` **וגם** `users/organizer1/trips/test123/tripTitle` שניהם מכילים את הכותרת, באותו אובייקט `updates` יחיד. ניקוי - שניהם הופכים ל-`null` יחד. `tripOwnerId` ריק (טיול ישן היפותטי בלי `ownerId`) - הכתיבה ל-mirror מדולגת בשקט, בלי נתיב שבור כמו `users//trips/...`
- עברית/אנגלית (`applyTripLanguage()` - הלייבל וה-placeholder), 375px, מצב בהיר - צילום מסך מלא של הפאנל, אין גלישה, השדה משתלב טבעי בין הכותרת לשדה סוג הטיול
- קונסול נקי לכל אורך הבדיקה
- `node scripts/i18n-audit.js index.html` — 0 ממצאים
- **לא נבדק**: שמירה אמיתית מול Firebase/RTDB (מגבלת הסביבה - אין רשת אמיתית, וגם ה-geocoding שרץ באותו handler לא נבדק כאן כי הוא לא קשור לשינוי) - נבדק דרך הרצת הביטויים המדויקים מהקוד, לא לחיצה אמיתית על הכפתור

נבדק בפועל: כל מה שלמעלה. v4.53.6 -> v4.54.0 (minor - יכולת עריכה חדשה). **זה ה-commit האחרון בסדרת 6 ה-commits המתוכננת** (שדה נפרד לכותרת/יעד ב-wizard, כל 6 העמודים מעדיפים tripTitle בתצוגה, תיקוני ניסוח, ועכשיו יכולת עריכה בדיעבד).

## v4.53.6 — עוד שני מקומות עם "לטיול לטיול X", שנחשפו בחקירת באג אמיתי

### 🔍 מה קרה - חקירת דיווח על הבאג שכבר "תוקן"
דווח באג: מסך ההזמנה עדיין מציג "לטיול לטיול חברים לפורטוגל" אחרי v4.53.5. חקירה בפרודקשן (לא רק מקומית) אישרה: **הקוד תקין** - הרצתי את `updateAdminShareLink()` ו-`authGateCopy()` בפועל בפרודקשן, עם ובלי `dt=1` - התוצאה הייתה זהה בדיוק למצופה, כולל שחזור מדויק, מילה במילה, של הבאג המדווח כשאין `dt`. **הסיבה**: `d=<כותרת>` עובד מ-v4.52.0 (כמה גרסאות לפני ש-`dt` נוסף ב-v4.53.5) - כל קישור שנוצר בין שתי הגרסאות (כולל קישורים ששותפו/הועתקו קודם) יכיל כותרת אבל לעולם לא `dt`, כי הוא פשוט קפוא מרגע היצירה שלו. זה לא תלוי בקוד הנוכחי בכלל.

**החקירה הזו גם חשפה שני מקומות נוספים עם אותה בעיה בדיוק**, שלא נכללו בתיקון של v4.53.5 (רק הגייט וה-showcase תוקנו שם):

### ✏️ תיקון ב׳: wizard.html - "הנתיב הכי נפוץ", לפי ההערה בקוד עצמו
`generateTripAndSave()`'s share-link ([wizard.html:2071-2083](wizard.html:2071)) היה בונה `d` מ-`destination` הגולמי בלבד - **מעולם לא בדק tripTitle**, ולא כתב `dt` כלל. זה הקישור שמוצג **מיד אחרי יצירת הטיול** (ההערה בקוד: "the most common path for the link a real recipient sees" - נפוץ יותר מפאנל הניהול). תוקן לאותו דפוס בדיוק כמו `updateAdminShareLink()`: `d = tripTitle || destination`, `dt=1` רק כש-tripTitle קיים.

### ✏️ תיקון ג׳: הודעת השיתוף בוואטסאפ (index.html)
`updateAdminShareLink()` עצמה כתבה `dt` נכון לקישור, אבל **טקסט ההודעה** שהיא בונה ("היי חברים, הצטרפו אליי לטיול ל-X!") עדיין השתמש בתבנית הישנה תמיד, בלי לבדוק tripTitle. תוקן: כש-tripTitle קיים, "הצטרפו אליי ל-X" (בלי "לטיול"); אחרת נשאר "הצטרפו אליי לטיול ל-X" כמו קודם.

### 🔎 חיפוש מקום רביעי - לא נמצא
חיפוש כללי אחרי "לטיול ל" ואחרי "invited"/"on a trip to"/"join me" בכל הפרויקט העלה רק את ארבע התבניות שכבר ידועות (עכשיו כולן תוקנות) - ושני false positives לא קשורים: תגי `og:description`/`twitter:description` הסטטיים ב-index.html (טקסט גנרי קבוע, "הוזמנת לטיול." בלי שום שם יעד/כותרת - לא יכול לכפול כלום כי אין לו מה לשרשר) ו-shopping.html's "להזמין את המוצרים הבאים" (הזמנת מוצרים מחנות, לא הזמנה לטיול - פיצ'ר אחר לגמרי). גם חיפוש חוזר אחרי כל בוני-URL עם `searchParams.set("d"...)` העלה בדיוק 4 מופעים - wizard.html (עכשיו תוקן), index.html פעמיים (updateAdminShareLink + הקישור ל-showcase, שניהם כבר תקינים), ו-gittrip-showcase.html (backUrl, כבר תקין) - שום בונה-קישור חמישי לא נמצא.

### 🧾 תיקון א׳: תיעוד בלבד, לא קוד
**קישורי שיתוף שנוצרו לפני v4.53.5 לא יכילו `dt`, ויציגו את הניסוח הישן** ("לטיול ל-X" גם כש-X הוא כותרת). פתיחת פאנל הניהול מחדש (או יצירת טיול חדש, אחרי v4.53.6) מרעננת את הקישור באופן טבעי. **אין צורך במיגרציה** - זה נפתר לבד ככל שקישורים ישנים "מתים" ומוחלפים בשימוש רגיל של האפליקציה.

### 🔍 מה שנבדק - שני המקומות החדשים, עם/בלי כותרת, שתי שפות
- **wizard.html, עם כותרת**: `d`/`dt` נבנים נכון (`dt=1`), ונבדק סגירת מעגל מלאה - ניווט לקישור שנוצר מציג בגייט "נירה הזמין אותך **לטיול חברים לפורטוגל** ✈️" (אומת גם בצילום מסך)
- **wizard.html, בלי כותרת**: `d=<destination>` בלי `dt` - זהה למקור
- **הודעת WhatsApp, עם כותרת (עברית)**: "הצטרפו אליי **לטיול חברים לפורטוגל**!" (בלי כפילות)
- **הודעת WhatsApp, בלי כותרת (עברית)**: "הצטרפו אליי לטיול לליסבון!" - זהה למקור
- **הודעת WhatsApp, עם/בלי כותרת (אנגלית)**: "join me for **Friends trip to Portugal**!" / "join me for our trip to Lisbon!" - שניהם נכונים
- קונסול נקי לכל אורך הבדיקה
- `node scripts/i18n-audit.js index.html wizard.html` - 0 ממצאים
- **לא נבדק**: יצירת טיול אמיתית מקצה לקצה מול Firebase (מגבלת הסביבה) - נבדק דרך הרצת הביטוי המדויק מהקוד + סגירת מעגל מלאה עד לגייט, לא הרצת `generateTripAndSave()` המלאה

נבדק בפועל: כל מה שלמעלה. v4.53.5 -> v4.53.6 (patch - תיקון ניסוח נוסף, אין שינוי סכמה). **commit 6 (עריכת tripTitle בפאנל האדמין) הבא בתור.**

## v4.53.5 — תיקון ניסוח "לטיול לטיול X" כשה-d הוא כותרת (index.html + gittrip-showcase.html)

### 🐛 הבאג שנתפס בבדיקת commit 5
בדיקת commit 5 (וידוא שכפתור "חזרה לטיול" ב-showcase יורש כותרת דרך פרמטר `d`) חשפה שהתבניות הקיימות - "הזמין אותך **לטיול ל**-X" (index.html) ו"חזרה **לטיול ל**-X" (showcase) - הניחו תמיד ש-X הוא שם מקום. כש-X הוא כותרת חופשית שכוללת בעצמה את המילה "טיול" (למשל "טיול חברים לפורטוגל"), זה יצר כפילות: "הזמין אותך לטיול לטיול חברים לפורטוגל".

### ✏️ הפתרון: דגל `dt` חדש ב-URL, לא ניחוש מהתוכן
אי אפשר להבחין בין כותרת לבין יעד רגיל **מתוך הערך עצמו** (`d` כבר ממוזג משני המקורות מאז commit 2) - צריך איתות נפרד. נוסף פרמטר `dt=1` שמצוין **רק** כש-`d` מקורו ב-`tripTitle` בפועל, לא ב-fallback ל-destination:

- **index.html**: משתנה מודול חדש `tripTitle` (נפרד מ-`destinationName` הממוזג) נוסף לצד ה-listener הקיים. `updateAdminShareLink()` מוסיפה `dt=1` לקישור השיתוף רק כש-`tripTitle` קיים בפועל. `authGateCopy()` קוראת `invitationDestinationIsTitle` (`dt==="1"`) ובוחרת בין 2 התבניות (עם/בלי "לטיול") - בכל אחד מ-4 השילובים הקיימים (host+dest / dest בלבד, עברית/אנגלית)
- **מסך "מה זה GitTrip?"** (הקישור מהגייט ל-showcase) מעביר את `dt` הלאה, לא רק `d`/`h`/`l`
- **gittrip-showcase.html**: קוראת את `dt` מה-URL (`ctaDestinationIsTitle`), בוחרת תבנית מתאימה לכפתור, **ומעבירה את `dt` הלאה גם ב-`backUrl` שלה** (הקישור בחזרה ל-index.html) - כדי שהעקביות תישמר בשני הכיוונים, לא רק בכניסה

**כשאין כותרת (destination בלבד)**: שום `dt` לא נכתב בשום מקום, שתי הפונקציות נופלות בדיוק לתבנית הישנה ("לטיול ל-X") - נבדק שזה נשאר בית-לבית זהה.

### 🔍 מה שנבדק - שתי נקודות המגע, שני המצבים, שתי השפות
- **index.html, עם כותרת (עברית)**: "נירה הזמין אותך **לטיול חברים לפורטוגל** ✈️" (בלי "לטיול" כפול)
- **index.html, בלי כותרת (עברית)**: "נירה הזמין אותך **לטיול ל**ליסבון ✈️" - זהה למקור
- **index.html, עם כותרת (אנגלית)**: "Nira invited you **to** טיול חברים לפורטוגל ✈️" (בלי "on a trip")
- **index.html, בלי כותרת (אנגלית)**: "Nira invited you **on a trip to** Lisbon ✈️" - זהה למקור
- **showcase, עם כותרת (עברית)**: "✈️ חזרה **לטיול חברים לפורטוגל**" (אומת גם ב-accessibility tree וגם בצילום מסך)
- **showcase, בלי כותרת (עברית)**: "✈️ חזרה **לטיול ל**ליסבון" - זהה למקור
- **showcase, עם כותרת (אנגלית)**: "✈️ Back to Friends trip to Portugal" - אומת בצילום מסך בהקשר מלא
- **`dt` מאומת בפועל בכל שלוש נקודות המעבר**: `authGateShowcaseLink.href`, showcase's `backUrl` (ה-href של הכפתור עצמו) - לא רק בקריאה הראשונה
- קונסול נקי לכל אורך הבדיקה בשני הקבצים
- `node scripts/i18n-audit.js index.html` - 0 ממצאים (gittrip-showcase.html לא בסקירה של הכלי - לא אחד מ-6 העמודים)
- **לא נבדק**: זרימה מלאה מול Firebase אמיתי (מגבלת הסביבה) - `dt` הוא state שנטען מה-URL בלבד, לא תלוי ב-RTDB, אז זה פחות קריטי כאן מבשאר commits

נבדק בפועל: כל מה שלמעלה. v4.53.4 -> v4.53.5 (patch - תיקון ניסוח, אין שינוי סכמה).

## v4.53.4 — packing.html + shopping.html מעדיפים tripTitle (commit 4/6)

### ⚠️ לא זהה מבנית ל-commit 3 - נבדק בנפרד, לא הונח
**packing.html** זהה ל-itinerary/places (משתני `trip`, `$('destination')`, קורא ל-`window.getPrimaryLeg(trip).name` בנפרד בכל נקודה) - רק מפורמט רב-שורתי במקום ממוזער. אותו תיקון בשלוש נקודות בדיוק כמו commit 3.

**shopping.html שונה מהותית**: משתמש במשתנה מודול יחיד `destinationName` (בדיוק כמו index.html ב-commit 2) - לא קורא ל-`getPrimaryLeg().name` בנפרד בכל אתר תצוגה. המשמעות: **תיקון נקודה אחת** (`destinationName = tripData.tripTitle || window.getPrimaryLeg(tripData).name || destinationName`, ב-`syncTripUI()`) מספיק לכל הצרכנים - לא שלוש נקודות נפרדות כמו בשלושת העמודים האחרים. ה-kicker (`#destinationKicker`, לא `#destination` - id שונה כמו שכבר ידוע) ו-`document.title` שניהם כבר קוראים מ-`destinationName` ולא נגעתי בהם בנפרד. cache ה-`trip-<id>-name` (ב-`syncTripUI()` גם הוא, לא ב-`applyTheme()` נפרד כמו בשאר) הוא כן נקודת מגע שנייה, נפרדת - קורא ל-`getPrimaryLeg().name` משלו, לא דרך `destinationName`, אז תוקן בנפרד.

**תגלית נוספת, לא מתוכננת**: הודעת השיתוף ב-WhatsApp של shopping.html (`` `...של ${destinationName}...` ``, שתי שורות ~3259-3260) גם היא קוראת מ-`destinationName` - יורשת את התיקון אוטומטית, בלי נגיעה נפרדת. אומת בבדיקה, לא רק הונח.

### 🔍 מה שנבדק - checklist נפרד לכל קובץ
**packing.html**: נטען בלי שגיאת קונסול. הביטוי המדויק מהקובץ הורץ נגד ה-DOM האמיתי (`document.title`, `#destination`) - עם כותרת: "רשימת ציוד • טיול חברים לפורטוגל". בלי כותרת: "רשימת ציוד • רומא"

**shopping.html**: נטען בלי שגיאת קונסול. הביטוי המדויק מ-`syncTripUI()` הורץ נגד ה-DOM האמיתי, כולל שלושת הצרכנים (kicker, title, הודעת WhatsApp) - עם כותרת: kicker="טיול חברים לפורטוגל", title="רשימת קניות • טיול חברים לפורטוגל", הודעת WhatsApp כוללת את הכותרת ולא את היעד. בלי כותרת: שלושתם נופלים נכון ל-"רומא". גם צילום מסך בהקשר מלא - ה-kicker בראש העמוד מציג את הכותרת נכון

- קונסול נקי לכל אורך הבדיקה בשני הקבצים
- `node scripts/i18n-audit.js packing.html shopping.html` - 3 ממצאים ב-shopping.html, אותם ממצאים שכבר אומתו כקיימים מראש ב-commit 2ב (כפתורי ייצוא שפה) - לא חדשים, לא קשורים
- **לא נבדק**: זרימה מלאה מול Firebase אמיתי (מגבלת הסביבה)

נבדק בפועל: כל מה שלמעלה. v4.53.3 -> v4.53.4 (patch - תיקון תצוגה, אין שינוי סכמה). **commit 5 (אימות showcase) הבא בתור, ואז commit 6 (עריכת tripTitle בפאנל האדמין).**

## v4.53.3 — itinerary.html + places.html מעדיפים tripTitle (commit 3/6)

### ✏️ שלוש נקודות מגע בכל קובץ, לא אחת
שני העמודים כמעט זהים שורה-שורה (אותו קוד, לא רק אותו רעיון - אומת ב-commit הקודם על נקודת ה-auto-register). בכל אחד תוקנו שלוש נקודות, לא רק `document.title`:
- **`applyTheme()`** - כתיבת ה-cache המוקדם `trip-<id>-name` ל-localStorage (אותו cache שקורא ממנו הבוטסטרפ בראש הקובץ, לפני שה-JS הראשי בכלל רץ)
- **`applyLanguage()`** - `document.title` (כותרת הטאב)
- **`applyLanguage()`** - `$('destination').textContent` (ה"kicker" מעל הכותרת בראש העמוד)

כל שלוש: `trip.tripTitle || window.getPrimaryLeg(trip).name || ...` - אותו דפוס בדיוק כמו commit 2 ב-index.html.

### 🧩 מה שהוחלט לא לגעת בו
- **`places.html:214`** (`alt` של תמונה בלי כיתוב) - נשאר כמו שסוכם, שימוש שולי
- **`places.html:323` / `itinerary.html:271`** (`destination:` בכתיבת ה-auto-register) - זה בכוונה destination טהור, לא display name - `tripTitle` כבר נוסף שם בנפרד ב-commit 2ב, לא נוגעים שוב

### 🔍 מה שנבדק - checklist נפרד לכל קובץ (כמו שביקשת)
**שני הקבצים נבדקו בנפרד, לא הונח ששינוי זהה בקוד זהה = תוצאה זהה בלי לבדוק:**

**itinerary.html**: נטען בלי שגיאת קונסול. הרצתי את הביטוי המדויק מהקובץ (title + kicker) נגד ה-DOM האמיתי של העמוד (`document.title`, `#destination` אמיתי) עם `trip` מדומה - עם כותרת: `document.title` הפך בפועל ל"תוכנית יומית • טיול חברים לפורטוגל" (אומת גם דרך שם הטאב עצמו בדפדפן, לא רק קריאת ערך). בלי כותרת: נפל נכון ל"תוכנית יומית • רומא"

**places.html**: אותה בדיקה בדיוק, בנפרד. עם כותרת: "מקומות ותמונות • טיול חברים לפורטוגל". בלי כותרת: "מקומות ותמונות • רומא". גם **צילום מסך בהקשר מלא** - ה-kicker בראש העמוד מציג את הכותרת נכון, לצד "📍 מקומות ותמונות". נבדק גם באנגלית עם כותרת ארוכה ("The girls' Eurotrip to Portugal 2026") ב-375px - עוטפת שורה בלי גלישה

- קונסול נקי לכל אורך הבדיקה בשני הקבצים
- `node scripts/i18n-audit.js itinerary.html places.html` — 0 ממצאים
- **לא נבדק**: זרימה מלאה מול Firebase אמיתי (מגבלת הסביבה) - הבדיקה הריצה את הביטויים המדויקים מהקוד נגד ה-DOM האמיתי, לא סימולציה נפרדת

נבדק בפועל: כל מה שלמעלה. v4.53.2 -> v4.53.3 (patch - תיקון תצוגה, אין שינוי סכמה). **commits 4 (packing+shopping), 5 (showcase) עדיין בתוכנית, ו-commit 6 חדש (עריכת tripTitle בפאנל האדמין) נוסף אחרי commit 5.**

## v4.53.2 — tripTitle נכתב גם כשחבר (לא מארגן) מצטרף לטיול (commit 2ב)

### 🔐 הבעיה: כתיבה כפולה, רק חצי תוקנה
commit 1 תיקן כתיבה אחת (`generateTripAndSave()` בוויזרד, למארגן היוצר את הטיול). אבל יש עוד **ארבע** נקודות כתיבה עצמאיות ל-`users/{uid}/trips/{tripId}` - אחת בכל אחד מ-places.html/itinerary.html/packing.html/shopping.html, כולן ב-`loadProfile()`'s auto-register (חבר חדש שמצטרף לטיול דרך קישור, לא המארגן) - ותועדו בקוד עצמו כמי שקיימות **בשביל** ה-tripPicker: `"so index.html's trip picker can list this trip too"`. אף אחת מהארבע לא כללה `tripTitle`. המשמעות: אחרי commits 1+2, טיול עם כותרת היה מוצג נכון **רק** ל-tripPicker/"הטיולים שלי" של המארגן - כל חבר אחר שהצטרף היה ממשיך לראות destination בשלו, בלי שום שגיאה גלויה.

### ✏️ התיקון - זהה בארבעת הקבצים, במקום המתאים לכל אחד
`...(trip.tripTitle ? { tripTitle: trip.tripTitle } : {})` נוסף לאובייקט שנכתב ב-`update()`, מיד אחרי `destination` - אותו דפוס "מושמט כשריק" כמו בכל שאר נקודות המגע. `shopping.html` משתמש במשתנה `tripData` (לא `trip`) ובאופטיונל-צ'יינינג (`tripData?.tripTitle`) - תואם למוסכמה הקיימת באותו קובץ, לא הועתק עיוור משלושת האחרים.

### 🔍 מה שנבדק
- **הבדיקה הקריטית שביקשת**: לא ניתן להריץ הצטרפות אמיתית מול Firebase בסביבה הזו (אין רשת), אז בניתי את השרשרת המלאה בצורה שקרובה ככל שניתן ל"בדיקה אמיתית, לא הנחה": חילצתי את הביטוי המדויק שנכתב בקוד (אותו conditional-spread) והרצתי אותו בפועל בדפדפן עם שני אובייקטי `trip` מדומים - אחד עם `tripTitle`, אחד בלי. תוצאה: ה-payload שנוצר כולל `tripTitle` **רק** כשהוא קיים, לגמרי נעדר (לא מחרוזת ריקה) כשלא - בדיוק ההתנהגות המכוונת
- **סגירת המעגל בפועל**: לקחתי את אותו payload בדיוק (כאילו הוא מה שהצטרף חבר שני היה כותב ל-`users/{ה-uid שלו}/trips/{tripId}`) והזרקתי אותו ל-`window.renderTripPickerList()` על index.html - **מאומת בצילום מסך**: הכרטיס מציג "טיול חברים לפורטוגל" (הכותרת), לא "ליסבון" (destination). זו בדיוק הבדיקה שהתבקשה - מהכתיבה של המצטרף ועד לתצוגה שלו-שלו, לא רק "הקוד נראה נכון"
- כל ארבעת העמודים נטענים בלי שגיאת קונסול (מוודא שאין שגיאת תחביר ב-JS שהוזרק לתוך שורות ממוזערות ב-places.html/itinerary.html)
- `node scripts/i18n-audit.js places.html itinerary.html packing.html shopping.html` - 3 ממצאים ב-shopping.html (כפתורי ייצוא שפה בעברית/אנגלית/יוונית) - **אומתו כקיימים מראש**, לפני השינוי שלי (`git stash` + audit + `git stash pop`), לא קשורים לשינוי הזה
- קונסול נקי לכל אורך הבדיקה
- **לא נבדק**: זרימת הצטרפות אמיתית מקצה לקצה מול Firebase אמיתי (מגבלת הסביבה - אין רשת) - כולל `runTransaction`/`autoRegisterMember`/`participantClaims` שסביב הקריאה הזו, שלא נגעתי בהם ולא נבדקו כחלק מהעבודה הזאת

נבדק בפועל: כל מה שלמעלה. v4.53.1 -> v4.53.2 (patch - תיקון כתיבה חסרה בזרימה קיימת, אין שינוי סכמה). **זה סוגר את commit 2 המלא (2+2ב). commits 3, 4, 5 (itinerary+places, packing+shopping, showcase) עדיין בתוכנית.**

## v4.53.1 — index.html מעדיף tripTitle בכל מקום שמציג שם טיול (commit 2/5+)

### 🎯 מקור יחיד לתיקון
`destinationName` הוא המשתנה היחיד ב-index.html שמזין את כל נקודות התצוגה (כותרת העמוד, פוטר, קישור שיתוף, הודעת WhatsApp, שתי התראות) - תיקון נקודה אחת ([index.html:4540](index.html:4540)) `destinationName = data.tripTitle || primaryLeg.name || destinationName` מספיק לכולן. לא נגעתי בהן בנפרד - אומת בפועל, לא רק הונח.

### 🔍 שני ממצאים נוספים מה-commit הקודם - שניהם תוקנו כאן
לפני שהתחלתי, המשתמש דיווח שראה destination במקום כותרת גם ב"הטיולים שלי" - התברר בחקירה שזה **לא** `renderRecentTrips()` בוויזרד (שכבר תוקנה ב-commit 1), אלא שני מקומות נפרדים לגמרי ב-index.html שלא נסרקו בחקירה המקורית כי נבנו בסבב עבודה קודם:
- **`renderTripPickerList()`** ([index.html:5891](index.html:5891)) - "הטיולים שלי" ב-index.html עצמו (נפרד מזה שבוויזרד). תוקן: `item?.tripTitle || item?.destination || id`
- **כפתור "חזרה לטיול ל-X"** - מקור הנתונים שלו הוא `localStorage['trip-<id>-name']`, אותו cache בדיוק שכותרת העמוד קוראת ממנו מראש (pre-paint). תיקון יחיד בצד הכתיבה ([index.html:4550](index.html:4550), `data.tripTitle || data.destination`) מספיק לשני הצרכנים - **לא נדרש תיקון נפרד לכפתור עצמו**, אומת בבדיקה

### 🧩 מה שהוחלט לא לגעת בו, ולמה
- **`trip-<id>-headline`** ([index.html:4642](index.html:4642)) - cache שלישי, נפרד, ששומר את `#appTitle.textContent` המלא (כולל דגל) - יורש את התיקון אוטומטית כי הוא רק מעתיק את מה ש-`#appTitle` כבר מציג אחרי שהוא עצמו תוקן. לא נגעתי בתנאי השמירה שלו (`if (data.destination)`) - destination תמיד קיים בטיול תקין, אז זה לא צריך "או tripTitle"
- **מסך ניהול (admin) - שינוי יעד ידני** ([index.html:6712](index.html:6712), `destinationName = newDestination`) - נבדק ואומת: זו state מקומי בלבד, הכתיבה בפועל ל-RTDB ([index.html:6919](index.html:6919)) היא `update()` חלקי שלא כולל `tripTitle` בכלל - אז `tripTitle` קיים בטיול נשאר בלתי פגוע גם כשהמארגן משנה יעד. לא הוספתי עריכת tripTitle לפאנל הניהול - מחוץ להיקף, לא התבקש
- **places.html:214** (`alt` של תמונה בלי כיתוב) - שימוש שולי, לא הצגת "שם טיול" בפועל. נפילה ל-destination תואמת את מה שכבר סוכם

### 🔍 מה שנבדק (שרת מקומי, gate עוקף ידנית - אין Firebase אמיתי)
- **`renderTripPickerList()` ישירות** (`window.renderTripPickerList` - נגישה מ-window כי index.html סקריפט קלאסי) עם שני טיולים מזויפים - אחד עם `tripTitle`, אחד בלי. תוצאה מאומתת בצילום מסך: "טיול חברים לפורטוגל" (כותרת) מוצג לטיול הראשון, "רומא" (destination) לשני
- **כפתור "חזרה לטיול"**: הוזרק `trip-backtest123-name` ל-localStorage עם ערך כותרת, נטען העמוד עם `?trips=1&from=backtest123`, `window.enterTripPickerMode()` הופעל ישירות - הטקסט שהתקבל: **"חזרה לטיול חברים לפורטוגל"**, לא "חזרה לטיול" הגנרי
- קונסול נקי לכל אורך הבדיקה
- `node scripts/i18n-audit.js index.html` — 0 ממצאים
- **לא נבדק**: זרימה מלאה מול Firebase אמיתי (מגבלת הסביבה) - כולל commit 2ב (ארבע נקודות ה-auto-register) שעדיין לא בוצע, ולכן חבר שמצטרף (לא המארגן) עדיין לא יראה tripTitle נכון בשום מקום עד שזה יתוקן

נבדק בפועל: כל מה שלמעלה. v4.53.0 -> v4.53.1 (patch — תיקון תצוגה, אין שינוי סכמה). **commit 2ב (תיקון הכתיבה בארבע נקודות ה-auto-register) עדיין פתוח.**

## v4.53.0 — שדה "שם הטיול" נפרד מהיעד (commit 1/5: סכימה + wizard.html)

### 🌍 הבעיה שזה פותר
שדה אחד ("שם הטיול / היעד") שירת שתי כוונות שונות. משתמש שממלא כותרת חופשית ("טיול חברים לפורטוגל") במקום יעד אמיתי שובר את כל מה שתלוי ב-geocoding: דגל, מזג אוויר, אזור זמן (ר' החקירה שקדמה ל-commit הזה - אותה משפחת באג כמו "קליפורניה"/"שטוקהולם" שכבר תועדה). זה commit 1 מתוך 5 בתוכנית שאושרה מראש - הבא בתור: index.html, ואז itinerary+places, packing+shopping, ואימות showcase.

### ✏️ שני שינויים ביחד, לא שדה בלבד (כמו שהוחלט מראש)
- **שדה חדש `tripTitle`** — טקסט חופשי, אופציונלי, לפני שדה היעד. לייבל "שם הטיול (אופציונלי)" — נבחר בכוונה על פני "(רשות)" שהוצע בהתחלה, כדי להתאים למוסכמה הקיימת בקובץ (`characterLabel`/`checkinLabel`/`spliitLabel` כולם "(אופציונלי)")
- **שדה היעד הקיים משתנה גם הוא**: לייבל מ"שם הטיול / היעד" ל-"יעד" בלבד (הלייבל הכפול היה מקור הבלבול), placeholder מ"למשל: ברלין, תאילנד או קמפינג בגליל" ל-"מקום אמיתי - למשל: ברלין, תאילנד, הגליל" - מדגיש "אמיתי" בלי להפוך לפסקה, שומר על "הגליל" כדוגמה (לא רק ערים - camping/local לגיטימיים כאזור)
- שני מפתחות תרגום חדשים (`tripTitleLabel`/`tripTitlePlaceholder`) מיושמים כמחרוזת שטוחה ב-`translations`, לא span נפרד ב-HTML - נמנע בכוונה מהתקלה הקיימת ב-`checkinLabel`/`spliitLabel` (ה-span שלהם עם "(אופציונלי)" באפור נדרס לטקסט רגיל בכל מעבר שפה, כי `changeGlobalLanguage()` עושה `textContent =` גורף - לא תוקן כאן, מחוץ להיקף, רק לא שוכפל)

### 🔐 כתיבה כפולה, באותה קריאה (הדגש המרכזי שביקשת)
`generateTripAndSave()` כותב `tripTitle` (כשלא ריק, אותו דפוס כמו `tripCharacter` - מושמט לגמרי כשריק, לא נשמר כמחרוזת ריקה) לשני המקומות **בתוך אותה קריאת `update(ref(db), {...})`**: `trips/${tripId}` וגם `users/${organizer.uid}/trips/${tripId}`. אין שתי כתיבות נפרדות.

### 🧩 נקודות מגע נוספות באותו קובץ (לא נשכחו)
- `renderRecentTrips()` ("הטיולים שלי"): `item.tripTitle || item.destination || item.tripId`
- `renderSummary()` (שלב 4, סיכום לפני יצירה): שורת "שם הטיול" מתווספת לגריד רק כשיש ערך - לא הוזכר בדיווח המקורי, נוסף כי זה נמצא באותו קובץ/אותו commit ועקביות עם "מה שיישמר" חשובה יותר מלהשאיר את זה בחוץ
- `saveWizardDraft()`/`restoreWizardDraft()`: הטיוטה כוללת `tripTitle`, משוחזרת בדיוק כמו destination
- `resetWizard()`: מנקה גם את השדה החדש

### 🔍 מה שנבדק (שרת מקומי, `http://localhost:8080/wizard.html`, gate עוקף ידנית לבדיקה בלבד - אין Firebase אמיתי בסביבה)
- **הבדיקה שביקשת במפורש**: הזרקתי שני רשומות ל-`localStorage['trip-organizer-recent-v1']` - אחת עם `tripTitle`, אחת בלעדיו - וקראתי ל-`renderRecentTrips()` בפועל. תוצאה מאומתת: הטיול עם כותרת מציג את הכותרת ("טיול חברים לפורטוגל"), לא את היעד ("ליסבון"); הטיול בלי כותרת נופל נכון ל-destination ("רומא") - בדיוק הבדיקה שהייתה מפספסת את באג הכתיבה הכפולה אם לא היתה מבוצעת
- מילוי שדה יעד ("ליסבון") אחרי מילוי כותרת חופשית ("טיול חברים לפורטוגל") - preview הדגל (PT) עדיין עובד נכון על שדה היעד, לא מושפע מהכותרת
- מעבר מלא דרך 4 השלבים עד למסך הסיכום - שורת "שם הטיול" מופיעה נכון מעל "יעד"
- עברית/אנגלית, כהה/בהיר, 375px - התרגום החדש מוצג נכון בשתי השפות, אין גלישה
- קונסול נקי לכל אורך הבדיקה
- `node scripts/i18n-audit.js wizard.html` — 0 ממצאים
- **לא נבדק**: שמירה אמיתית ל-Firebase (מגבלת הסביבה, אין רשת אמיתית) - הקוד שנכתב נבדק דרך קריאה ישירה, לא הרצה live מול RTDB אמיתי

נבדק בפועל: כל מה שלמעלה. v4.52.0 -> v4.53.0 (minor — שדה חדש בסכימה).

## v4.52.0 — התחברות בתחילת תהליך יצירת הטיול, לא בסוף (wizard.html)

### 🔐 שינוי זרימה: gate מלא לפני שהטופס נגיש בכלל
- **הבעיה**: בדיקת ההתחברות הייתה קורית רק בלחיצה על "צור טיול" בסוף השלב הרביעי (`requireOrganizer()`) — משתמש שלא מחובר היה ממלא טופס שלם ומקבל רק אז הודעה לגלול למעלה ולהתחבר, ומאבד את מה שמילא בלחיצה חוזרת. `requireOrganizer()` עצמו נשאר במקומו כרשת ביטחון (מקרה קצה שבו ההתחברות פגה תוך כדי מילוי הטופס), אבל כבר לא אמור להיתקל בו משתמש רגיל
- **הפתרון שנבחר (אופשן א׳, מאושר מראש)**: מסך הזמנה מלא, לא באנר — הוויזרד כולו לא נגיש עד להתחברות. **שימוש חוזר במסך ה-`auth-gate`/`auth-card` הקיים כבר ב-`index.html`/`shopping.html`** (מבנה + CSS מ-`shared.css`, לא נבנה מחדש) - כולל `auth-gate.js` (רשת ביטחון עם timeout של 12 שניות ל-iOS, כבר משמש 2 עמודים אחרים)
- **`#wizardApp` (עוטף `.top-bar` + `.wizard-container` + `#recentTripsPanel`) מתחיל `class="hidden"` ב-HTML הסטטי עצמו** - לא רק ב-JS אחרי שה-callback רץ. זה שיפור ביחס לתבנית הקיימת ב-`index.html` (ש-`#appContainer` שלו *אין* לו `hidden` דיפולטיבי ב-HTML, רק הסתמכות על תזמון JS) - כאן אין אפילו רגע תיאורטי של flash לפני שה-JS הראשון רץ
- **`onAuthStateChanged` הפך משם אנונימי לפונקציה בשם `handleAuthStateChanged(user)`** (נדרש כדי ש-`armAuthGate`'s retry callback יוכל להפנות אליה בחזרה) - כל הלוגיקה הקיימת (`cloudTrips` listener, `updateAuthUI()`, `renderRecentTrips()`) נשמרה במלואה, רק נוסף הטוגל של `authLoading`/`authGate`/`wizardApp`
- **`--card-bg`/`--line`/`--shadow-float` נוספו כ-alias ב-`:root` של wizard.html**, ממופים למשתנים הקיימים כבר (`--surface`/`--border`/`--shadow`) - `.auth-card` ב-`shared.css` קורא לשמות האלה וwizard.html (בניגוד ל-index/shopping) לא רשום ל-`[data-tokens="core"]` ולא הגדיר אותם; בלי ה-alias הכרטיס היה שקוף לגמרי (רק הטקסט/הכפתור צפים על גבי ה-backdrop, בלי קופסה)
- **מקשי i18n חדשים** (`gateTitle`/`gateText`) ב-`translations.he`/`translations.en`, מוזנים דרך `updateAuthUI()` הקיימת (נקראת גם ב-`changeGlobalLanguage()` וגם ב-`handleAuthStateChanged()`) - לא נדרש שינוי ב-`changeGlobalLanguage()` עצמה
- **לא כלול (במכוון, שלב נפרד)**: שמירת טיוטה למשתמש שממלא ואז מתחבר - הוחלט מפורש מראש לא לכלול בשלב הזה

### 🔍 מה שנבדק (שרת מקומי, `http://localhost:8080/wizard.html`)
- משתמש לא מחובר: מסך הזמנה מלא (איקון ✈️, כותרת/תת-כותרת/כפתור נכונים), הטופס מאחוריו באמת `display:none` (לא רק מוסתר ויזואלית מאחורי ה-overlay) - אומת ישירות דרך `getComputedStyle`
- עברית ואנגלית (כולל כיווניות RTL/LTR נכונה בכרטיס)
- מצב כהה ובהיר - אחרי הוספת ה-`--card-bg`/`--line`/`--shadow-float` alias, הכרטיס מוצג כקופסה לבנה/כהה עם גבול וצל, לא רק טקסט צף
- 375px - אין גלישה
- לחיצה על כפתור הכניסה מפעילה בפועל את `handleProviderSignIn(googleProvider)` (אומת: ניווט ל-Firebase auth handler, ה-popup נחסם בסביבת הבדיקה אז נפל אוטומטית ל-`signInWithRedirect` - בדיוק ההתנהגות הקיימת שתוכננה)
- קונסול נקי לכל אורך הבדיקה
- `node scripts/i18n-audit.js wizard.html` — 0 ממצאים
- **לא נבדק** (מגבלת סביבת הבדיקה - אין Firebase אמיתי): הזרימה המלאה "משתמש כבר מחובר נכנס לוויזרד ולא רואה gate בכלל" - הלוגיקה זהה בדיוק לתבנית העובדת כבר ב-`shopping.html`, אבל לא אומתה end-to-end מול חשבון אמיתי. מומלץ בדיקה נוספת ב-production בטאב חדש/incognito (ר' "בדיקת UI בדפדפן" ב-CLAUDE.md) לפני שנחשב סגור לגמרי

נבדק בפועל: כל מה שלמעלה. v4.51.3 -> v4.52.0 (minor — שינוי התנהגות/זרימה חדש שהמשתמש חווה, לא רק תיקון באג נקודתי).

## v4.51.3 — פיצול פאנל האדמין, commit 3 (אחרון): בורר-המושבים-בכל-יעד עובר ל"טיסות ומושבים" (index.html)

### 🎨 שינוי מבני + התנהגות חדשה אחת (תווית חיה)
- **`seatRow` מנותק מ-`row`** בתוך `buildAdminLegRow()` — לא עוד "שורה שנייה" מתחת לשם היעד ב"פרטי טיול", אלא שורה שטוחה עצמאית שמצורפת ל-container חדש, `#adminLegsSeatsList`, בתוך "✈️ טיסות ומושבים". `descriptor.seatSelect`/`descriptor.seatLabel` נשארו **אותם אובייקטים** — כל קוד אחר שקורא/כותב דרכם (`saveFlightBtn`'s handler, `legSeatLayouts = legDescriptors.map(r => r.seatSelect.value)`) לא השתנה ולא היה צריך להשתנות
- **`updateAdminLegChrome()` מקבל toggle נפרד ל-`seatRow`** — לפני כן, הסתרת/הצגת שורת-יעד (`r.row`) הסתירה אוטומטית גם את בורר-המושבים שלה כי הוא היה ילד שלה. עכשיו, כשהם בשני containers נפרדים, נדרש `r.seatRow.classList.toggle("hidden", !visible)` מפורש - נוסף ל-descriptor גם שדה `seatRow` (לא היה שם קודם, רק `seatSelect`/`seatLabel`)
- **תווית מזהה חדשה: "מושבים — {שם יעד}"** (היה "תצורת מושבים:" גנרי) — פונקציה משותפת חדשה, `updateSeatLabel(seatLabel, inputEl)`, קוראת בכל 6 הנקודות שמשנות שם יעד (בנייה ראשונית, `addAdminLeg`, `removeAdminLeg` - גם בלולאת ההזזה וגם בשורה המפונה, `moveAdminLegUp` - שתי הרשומות המוחלפות, `seedAdminLegsEditor` - גם בזרימה הרגילה וגם בניקוי שורות-יתר) — שם ריק נופל לתווית הגנרית המקורית, לא ל"מושבים — " עם כלום אחריו
- **התנהגות חדשה שלא הייתה קיימת קודם**: **listener נפרד (לא ממוזג עם ה-listener הקיים ל-dirty flag) על אותו `inputEl`** מעדכן את התווית בזמן אמת בכל הקשה, לא רק בטעינה/שמירה — תווית שמתעדכנת תוך כדי הקלדה, לא קיימת היום בשום שדה אחר בפאנל
- **ניקוי תוך כדי (השלכה ישירה של המהלך, לא הרחבת-scope)**: `.hide-seat-rows` (המחלקה שהסתירה שורות-מושבים בטיול לא-abroad) הפכה מיותרת לגמרי ברגע שה-seat-rows עברו לגור בתוך `#flightsSectionBox` (שכבר מוסתר כולו ב-non-abroad, מ-commit 2) — הוסרה: הכלל ב-CSS, קריאת ה-`classList.toggle` ב-`syncAdminFields()`. גם ה-`padding-inline-start:24px` (הזחה שהתאימה כששורת-המושבים ישבה מתחת לשורת-היעד שלה) הוסר - השורה כבר לא מקוננת תחת שום דבר

### 🔍 מה שנבדק — כל 8 הבדיקות שביקשת, בדפדפן בפועל
1. **הוספת יעד**: `addAdminLeg()` → בורר חדש מופיע ב-`#adminLegsSeatsList` עם תווית "מושבים — {שם}" נכונה ברגע שהוקלד שם
2. **הסרת יעד**: `removeAdminLeg()` → הבורר של היעד שהוסר נעלם מהתצוגה (`hidden:true`, לא נשאר גלוי/יתום); שני היעדים הנותרים מציגים נכון
3. **הזזת יעד**: `moveAdminLegUp()` עם 3 יעדים בעלי ערכי מושבים שונים (2-2/3-3/2-4-2) — לאחר הזזה, כל תווית ועובר **ביחד** עם הערך הנכון שלה (אומת: "רומא" קיבלה "2-4-2" שהיה שלה, לא "3-3" של המיקום הפיזי הישן)
4. **הקלדה חיה**: הקלדת "פריז" בשם היעד עדכנה את התווית ל"מושבים — פריז" מיידית, בלי לחיצת שמירה
5. **טיול חד-יעד**: רגל בודדת מציגה שורת-מושבים אחת, תקינה
6. **נפילה-לאחור**: רגל עם `seatLayout:null` נופלת נכון לערך הטיול הישן (`seatLayout` גלובלי, "3-4-3" בבדיקה) — לא ל"3-3" קשיח
7. **שמירה אמיתית**: `window.buildLegs` יורט זמנית לפני הקריאה האמיתית - `legSeatLayouts` שנשלח (`["2-4-2","3-4-3"]`) ותוצאת הבנייה (`resultSeatLayouts`) תואמים בדיוק לערכים שנקבעו בכל בורר, ובסדר הנכון לפי `legNames` (`["פריז","לונדון"]`)
8. **עברית/אנגלית, 375px, כהה/בהיר**: אין גלישה; "Seats — פריז"/"Seats — לונדון" מוצגים נכון באנגלית (כולל שם יעד בעברית בתוך תווית אנגלית - טקסט רגיל, בלי בעיית כיווניות); הסתרה מלאה של `#flightsSectionBox` (כולל `#adminLegsSeatsList`) לטיול לא-abroad אומתה גם אחרי המעבר
- קונסול נקי לאורך כל הבדיקות (כולל בדיקות שרשרת: הוספה→הקלדה→הזזה→הסרה→שמירה ברצף אחד)
- `node scripts/i18n-audit.js index.html` — 0 ממצאים

נבדק בפועל: כל מה שלמעלה. v4.51.2 -> v4.51.3 (patch — שינוי מבני, לא סכמה/יכולת-נתונים חדשה - השדה `leg.seatLayout` עצמו לא השתנה, רק המבנה הוויזואלי/DOM שמעדכן אותו).

**זה ה-commit האחרון בסדרת פיצול פאנל האדמין (3 commits, מאושרים מראש). הפאנל עכשיו: "🌍 פרטי טיול" / "✈️ טיסות ומושבים" / "🧩 מודולים פעילים" / "👥 ניהול חברי הקבוצה" / "🔗 שיתוף הטיול" / "✈️ טיול נוסף" - שישה קטעים מובחנים ויזואלית, "טיסות ומושבים" מכיל כל מה שתלוי ב-`tripType===abroad` ומוסתר כיחידה אחת לכל סוג טיול אחר.**

## v4.51.2 — פיצול פאנל האדמין, commit 2: box נפרד ל"טיסות ומושבים" (index.html)

### 🎨 שינוי מבני (חיתוך-הדבקה, אפס שינוי לוגיקת JS על 3 השדות)
- **Box חדש: "✈️ טיסות ומושבים"** — מכיל `airlineField`/`checkinField`/`returnSeatLayoutField`. שלושתם הועברו מ-Box 1 ללא כל שינוי בקוד ה-JS שלהם (מאותרים לפי `id` בכל מקום בקוד, לא לפי מיקום DOM יחסי)
- **Box 1 שונה שם ל"🌍 פרטי טיול"** (מ"סוג טיול ויעד") — מכיל עכשיו: סוג טיול, עורך-יעדים (**עדיין** עם בורר המושבים בכל יעד — זה commit 3), `adminMultiLegHint` (נשאר באותו מיקום יחסי, מסביר מגבלה של עורך היעדים עצמו, לא קשור לטיסות), תאריכים, וואטסאפ, ומבטא צבעוני
- **התנגשות שנתפסה ותוקנה לפני מימוש** (ר' דיון): אם כפתור השמירה היה עובר יחד עם 3 השדות ל"טיסות ומושבים", טיול לא-abroad (camping/local/reserve) היה נשאר בלי שום כפתור שמירה נגיש — כי ה-box היה מוסתר לגמרי. **הפתרון**: `saveFlightBtn` נשאר/עובר ל**תחתית "פרטי טיול"**, לא ל"טיסות ומושבים" — הטקסט "שמור פרטי טיול וטיסות" תקף בכל מצב כי הוא מתאר מה *עשוי* להישמר, לא מבטיח ששדות טיסה קיימים
- **`syncAdminFields()` — גם שופר תוך כדי**: 3 הטוגלים הנפרדים (`checkinField`/`airlineField`/`returnSeatLayoutField`, כל אחד בנפרד) אוחדו לטוגל יחיד על `#flightsSectionBox` (ה-container המשותף שלהם עכשיו) — אותו תנאי בדיוק בשלושתם, כך שלא יכול לסטות ביניהם. `.hide-seat-rows` (בורר-המושבים-בכל-יעד) נשאר טוגל נפרד במכוון — הוא עדיין לא בתוך `#flightsSectionBox` (זה commit 3)

### 🔍 מה שנבדק
- **טיול abroad**: שני ה-boxes מוצגים; הכפתור בתחתית "פרטי טיול", לא נתקע בין הקטעים (`saveFlightBtn.closest('.admin-section-box')` אומת שהוא בתוך ה-box הראשון, לא השני)
- **טיול לא-abroad (local/camping/reserve)**: "טיסות ומושבים" מוסתר לגמרי (`display:none` על ה-box כולו, לא רק על שדותיו) — והכפתור **עדיין גלוי ועובד**
- **מעבר חי בין 5 סוגי טיול ברצף בפאנל פתוח** (abroad→camping→reserve→abroad→local) — הכפתור נשאר אותו אלמנט DOM, גלוי בכל שלב, לא נעלם/קופץ באף מעבר
- **שמירה אמיתית**: מולאו שדות משני ה-boxes יחד (יעד, תאריכים, checkinUrl, returnSeatLayout, וואטסאפ, accent, בורר-מושבים-רגל-0) ונלחץ הכפתור פעם אחת — כל הערכים משני ה-boxes נכתבו יחד לאותם משתנים גלובליים (`checkinLink`, `returnSeatLayout`, `whatsappGroupLink`, `showAccent`, `seatLayout`, `destinationName`), מודל ההצלחה הרגיל הופיע (המסך נסגר, "הגדרות הטיול עודכנו בהצלחה!")
- עברית ואנגלית (כולל התווית החדשה "טיסות ומושבים"/"Flights and seats" והכפתור "שמור פרטי טיול וטיסות"/"Save trip details and flights")
- 375px, בהיר וכהה — אין גלישה, המרווח בין הקטעים (מ-commit 1) נשמר גם סביב ה-box החדש
- `node scripts/i18n-audit.js index.html` — 0 ממצאים
- קונסול נקי לאורך כל הבדיקות
- לא נבדק: בורר-המושבים-בכל-יעד עדיין בתוך עורך היעדים ב"פרטי טיול" — זה commit 3, במכוון מבודד מהשינוי הזה

נבדק בפועל: כל מה שלמעלה. v4.51.1 -> v4.51.2 (patch — שינוי מבני/ויזואלי, אין שינוי סכמה/יכולת חדשה).

## v4.51.1 — פיצול פאנל האדמין, commit 1: בידול ויזואלי בין קטעים (index.html)

### 🎨 שיפור ויזואלי (ללא שינוי מבנה שדות)
- **פתיחת פרויקט**: פאנל האדמין הצטבר לאורך היום עם שדות חדשים (מושבים פר-רגל, חברת תעופה) על גבי מה שכבר היה שם — חקירה מלאה מיפתה את המצב הקיים (5 `.admin-section-box` קיימים כבר, ~22 שדות, גובה נמדד עד 2026px ב-375px). זה commit 1 מתוך 3 בתוכנית שאושרה: פיצול ויזואלי + מבני, בלי לגעת בשום לוגיקת DOM/lazy (הכל נשאר ב-DOM מההתחלה, בדיוק כמו היום)
- **אימוג'י הכותרת של כל אחד מ-5 ה-`.admin-section-box` הקיימים מקבל רקע עגול רך** (`--primary-soft`, אותו טוקן שכבר מזווג עם `--primary-dark` בבאנר המושבים) — האימוג'י יצא משדה הטקסט המתורגם (`dashboardText`) לתוך `<span class="admin-section-icon">` סטטי, לא-מתורגם (אימוג'י לא צריך תרגום; השארתו במילון הייתה מחייבת אותו תו פעמיים, זהה, בעברית ובאנגלית — עותק מיותר)
- **מרווח מוגדל בין קטעים** — `.admin-section-box + .admin-section-box { margin-top: 10px }`, **לא** נגיעה ב-`gap` המשותף של `.bottom-sheet` (16px) שכל המודלים באפליקציה חולקים (לוקר/מושבים/חברים). 26px אפקטיבי בין קטעים עכשיו, בלי להשפיע על שום מודל אחר

### 🔍 מה שנבדק
- כל 5 הכותרות: האימוג'י נשאר בתוך `.admin-section-icon` נכון גם אחרי מעבר שפה (he→en→he) — האימוג'י לא נמחק/משוכפל, רק טקסט התווית מתעדכן (מאומת: הטקסט הפנימי `id`-ם עבר לתגית `<span>` פנימית, לא נמחק מה-DOM)
- מרווחים: נמדד בפועל `getBoundingClientRect()` בין כל שני קטעים סמוכים — 26px עקבי בין כל 4 הזוגות
- 375px, עברית ואנגלית, בהיר וכהה — אין גלישה, העיגולים קריאים בשני המצבים
- `node scripts/i18n-audit.js index.html` — 0 ממצאים
- קונסול נקי לאורך כל הבדיקות
- לא נבדק: שום שינוי בפיצול השדות עצמם (זה commit 2/3) — ה-DOM עדיין זהה למבנה של היום, רק העטיפה של 5 הכותרות והמרווח ביניהן השתנו

נבדק בפועל: כל מה שלמעלה. v4.51.0 -> v4.51.1 (patch — שיפור ויזואלי, אין שינוי סכמה/יכולת חדשה).

## v4.51.0 — בורר "חברת תעופה" ממלא את קישור הצ'ק-אין (index.html)

### ✨ תכונה חדשה
- **שדה חדש בפאנל האדמין, מעל "קישור לצ'ק-אין (URL)"**: `<select>` "חברת תעופה (אופציונלי):" — 9 חברות + "אחר" (ברירת מחדל, תמיד ראשון, אף פעם לא מוסק אוטומטית מהקישור השמור). בחירת חברה ממלאת את שדה ה-URL הקיים בקישור **לעמוד הצ'ק-אין הכללי** של אותה חברה — לא קישור לעמוד הבית, ולא קישור שדורש כבר להיות מחובר לחשבון. שדה הטקסט החופשי הקיים נשאר בדיוק כמו שהיה — ניתן לערוך את הערך שמולא ידנית בכל שלב, ובחירת "אחר" לא נוגעת בו בכלל
- **ניסוח שנבחר בכוונה כדי לא להבטיח יותר ממה שקורה בפועל**: "בחירת חברה תמלא למטה את הקישור לעמוד הצ'ק-אין הכללי שלה — עדיין תצטרכו להזין שם מספר הזמנה/שם" — לא "מילוי אוטומטי"/"צ'ק-אין אוטומטי". שום חברת תעופה שנבדקה לא חושפת כתובת ציבורית שמקבלת מספר הזמנה כפרמטר ב-URL (הזמנה תמיד מוזנת בעמוד החברה עצמו) — ר' פירוט מלא בדיון התכנון שקדם ל-commit הזה
- **אם כבר יש ערך שונה בשדה הטקסט** — לא נדרס בשקט: `customConfirm()` (אותה פונקציה שכבר משמשת ב-`releaseMember`) שואלת "כבר יש קישור שמור. להחליף אותו בקישור הצ'ק-אין של [שם החברה]?". סירוב לא משנה את השדה, ומאפס את הבורר בחזרה ל"אחר" (כדי שלא יישאר מצב שהבורר מראה חברה אחת והשדה מציג קישור אחר). אם השדה ריק, או שהוא כבר זהה לכתובת החברה — ממלאים ישירות בלי לשאול
- **גייטינג זהה בדיוק ל-`checkinField`**: מוצג רק לטיול מסוג `abroad`, מוסתר ומתאפס (ל"אחר") בכל פתיחה מחדש של הפאנל — לא מוסק/משוחזר מהערך השמור, גם אם הוא במקרה זהה לכתובת של אחת החברות ברשימה (החלטה מפורשת, לא פיקוח: הבורר הוא כלי מילוי חד-פעמי, לא שדה שנשמר על הטיול)

### 🔍 9 הכתובות — כל אחת אומתה בדפדפן אמיתי, לא בטקסט חיפוש בלבד
לפני כתיבת הקוד נבדקו 17 חברות תעופה בסך הכול (בשלושה סבבים) — כל אחת בניווט דפדפן אמיתי (לא WebFetch/טקסט חיפוש בלבד, שנתקל בחסימות בוט/דפי "עומס גבוה" מטעים בכמה חברות) עד לוידוא בפועל של שדות הטופס:
- ✅ **נכנסו לרשימה** (טופס אנונימי אמיתי, בלי חשבון, בכתובת ישירה): אל על, אייר פרנס, לופטהנזה, טורקיש איירליינס, בריטיש איירווייז, KLM, אג'יאן איירליינס, וולינג, יונייטד איירליינס
- ❌ **ריינאייר, ישראייר** — נבדקו ונפסלו: הצ'ק-אין דורש בפועל חשבון (myRyanair Portal / My Israir Personal Zone), לא רק מספר הזמנה
- ⚠️ **אריקיה** — נבדקה ונפסלה: הטופס האנונימי עצמו עובד, אבל כותרת העמוד עצמה מצהירה "לטיסות אילת בלבד" — לא רלוונטי לרוב הטיסות
- ❓ **וויז-אייר, איזי-ג'ט, פגסוס, דלתא, אמריקן איירליינס** — נבדקו ולא נכנסו: התהליך האנונימי קיים ועובד (אומת בפועל, למשל טופס "Find Booking" של איזי-ג'ט/"Check in online" של פגסוס/"Find trip" של דלתא ואמריקן), אבל בכולן הטופס מוצג רק כטאב/אקורדיון שמתחלף ב-JavaScript בתוך עמוד הבית (`href="javascript:void(0)"` אצל דלתא) — לא נמצאה כתובת ישירה שנוחתת עליו בלי טעינה מחדש של דף הבית וקליק ידני. וויז-אייר נכשלה גם טכנית, בנוסף לכך (עמוד ריק לגמרי בכל ניסיון)

**תיעוד מפורש כמבוקש**: זו רשימה שנבנתה ותתוחזק **ידנית** בקוד (`AIRLINE_CHECKIN_URLS` ב-index.html) — **אין שום מנגנון בקוד שמזהה קישור שהפך שבור** לאחר עיצוב-מחדש של אתר חברת תעופה. אם קישור יתיישן, זה ידווח רק אם מישהו ילחץ עליו ויבחין שהוא לא עובד.

**מגבלת כיסוי ידועה ומתועדת בקוד, לא באג**: כתובת לופטהנזה מכסה גם Austrian Airlines ו-SWISS (אומת בפועל — אותה מערכת צ'ק-אין בדיוק, שלושת המותגים נוחתים על אותו טופס) — אבל **לא** Brussels Airlines/Eurowings (אתרי צ'ק-אין נפרדים משלהם, למרות שהן באותה קבוצה), ולא טיסת קודשייר שמופעלת בפועל על ידי חברה אחרת (מסומן "operated by" בכרטיס).

### 🔍 מה שנבדק בקוד עצמו
- מילוי לשדה ריק, כולל עם יונייטד (החברה שנוספה בסבב האימות השני) — ישיר, בלי דיאלוג
- מילוי כשהערך הקיים כבר זהה לכתובת החברה — ישיר, בלי דיאלוג (לא "כבר קיים ושונה")
- מילוי כשיש ערך *שונה* קיים — דיאלוג `customConfirm` מופיע עם שם החברה הנכון; אישור מחליף; ביטול משאיר את הערך המקורי **ומאפס את הבורר**
- מעבר לסוג טיול לא-`abroad` — שני השדות (בורר + URL) מוסתרים יחד, אותו גייטינג
- אנגלית — תווית, הרמז, וכל שמות החברות (כולל "Other") מתורגמים נכון
- 375px — אין גלישה, הרמז נשבר לשורות בצורה קריאה
- `node scripts/i18n-audit.js index.html` — 0 ממצאים
- קונסול נקי לאורך כל הבדיקות
- לא נבדקה כתיבה אמיתית ל-RTDB (מגבלה מוכרת) — הבדיקה עד נקודת מילוי השדה, לא מעבר לה (השמירה בפועל של `checkinLink` לא השתנתה ב-commit הזה)

נבדק בפועל: כל מה שלמעלה. v4.50.0 -> v4.51.0 (minor — תכונה חדשה גלויה למשתמש/מארגן).

## v4.50.0 — מפת מושבים פר-רגל, commit 5 (אחרון): personalLocker מקבל legId (index.html)

### ✨ תכונה חדשה
- **`personalLocker/{uid}` מקבל שדה `legId`** — נכתב בשמירה (`savePersonalLockerCard`) עם ה-legId של הטיסה הרלוונטית כרגע (`currentLockerLegId()`, עוטפת את `window.getRelevantFlight` — אותה טיסה שכבר קובעת איזה seatLayout מוצג, ר' commit 4). טיסת חזרה נכתבת עם הסנטינל `legId:"return"` — אותו ערך בדיוק ש-`getRelevantFlight` עצמו כבר מחזיר, לא מוסכמה שנייה
- **כל 7 נקודות הקריאה/כתיבה של `personalLockerCards` שמופו בחקירה נבדקו במפורש ואחת-אחת הפכו legId-מודעות (או תועדו במכוון כלא-מסוננות, עם נימוק)**:
  1. **הידרציה** (`data.personalLocker` → `personalLockerCards`) — נשארת ללא סינון בכוונה (כל האובייקט נטען גולמי, הסינון הוא אחריות נקודת התצוגה, לא ההידרציה)
  2. **`releaseMember` (מחיקת חבר)** — עדיין מוחקת את **כל** הרשומה, לא רק הרגל הנוכחית, בכוונה (ניתוק חשבון = איפוס מלא)
  3. **`loadSelectedLockerCard`** (טעינת הטופס בפתיחת המודאל) — מציגה מושב/קישור רק אם הרשומה שייכת לרגל הרלוונטית כרגע, אחרת ריק
  4. **`savePersonalLockerCard`'s `previousSeat`** (בדיקת "האם הערך השתנה") — משתמשת באותו סינון, כדי שמושב שנשמר לרגל אחרת לא ייחשב "לא השתנה"
  5. **`savePersonalLockerCard`'s ולידציית תצורה** — עברה מ-`seatLayout` (שדה טיול גלובלי, ישן) ל-`resolveActiveSeatLayout()` (פונקציה משותפת חדשה, אותה לוגיקה בדיוק כמו ב-`renderPlaneSeats`, ר' למטה) — מושב נבדק מול התצורה של **הרגל שהוא בפועל נשמר עבורה**, לא מול שדה קבוע
  6. **`updateLockerSummary`** (טקסט הבאנר "הכרטיס שלי") — אותו סינון כמו הטופס; עכשיו גם רצה כל שנייה מתוך `updateClockAndPhases()` (לא רק על שינוי נתונים) כי הפלט שלה תלוי בזמן עכשיו, לא רק ב-`personalLockerCards`
  7. **`renderPlaneSeats`'s בניית `seatMap`** — מסננת ל-`lockerCardLegId(card) === currentLockerLegId()` לפני שמושב נכנס למפה בכלל
  8. **`renderSeatMismatchList`** ("מושבים לא תואמים לתצורה") — אותו סינון, **לפני** בדיקת התקינות עצמה — מושב של רגל אחרת לא "מושב לא תואם", הוא פשוט לא רלוונטי כרגע ולא מופיע ברשימה בכלל
- **`resolveActiveSeatLayout()` חדשה, משותפת** — מיצוי הלוגיקה שנוספה ב-commit 4 בתוך `renderPlaneSeats` בלבד, עכשיו גם ולידציית השמירה קוראת לה. תוקן פער אמיתי: הוולידציה המשיכה להשתמש ב-`seatLayout` הישן אחרי commit 4, בזמן שהמפה עצמה כבר עברה ל-legId-aware — יכול היה לדחות מושב תקין לרגל השנייה, או לקבל מושב לא תקין
- **רשומה בלי `legId`** (מושב שנשמר לפני ה-commit הזה) **מתפרשת כ-`legs[0]`** — `lockerCardLegId(card)`, לא ה"אין legId = כל הטיול" הכללי של packing/places (מושב שייך לטיסה אחת ספציפית, לא רלוונטי לכל הטיול)
- **הערת קוד ליד הכתיבה, כמבוקש**: מתעדת במפורש שרשומת `personalLocker/{uid}` נדרסת בשמירה הבאה (אין היסטוריה), שזה בטוח רק כי חוק ההצגה מבטיח רגל רלוונטית אחת בו-זמנית, ושמי שמרפה את חוק ההצגה בעתיד (למשל מסך "כל הטיסות") צריך לעבור למבנה פר-רגל קודם

### 🔍 מה שנבדק — בפועל בדפדפן
- **מושב לטיסה 1, מעבר לטיסה 2 (24 שעות לפני)**: מפת טיסה 2 ריקה, לא מציגה את מושב טיסה 1 — גם במפה (`renderPlaneSeats`) וגם בטופס (`loadSelectedLockerCard`) וגם בבאנר (`updateLockerSummary`)
- **מושב נפרד לטיסה 2**: נשמר ותוייג `legId:"legB"` נכון, מוצג נכון על מפת טיסה 2 עם ה-`seatLayout` של טיסה 2 (לא טיסה 1). **הערה חשובה**: המבנה המוסכם (סעיף "שלושה דברים מסוכנים" #1 בתכנון שאושר) הוא רשומה **יחידה** שנדרסת, לא רשומה פר-רגל — "שתי הטיסות נפרדות" נבדק כ"כל טיסה, כשהיא הרגל הרלוונטית, שומרת ומציגה את המושב שלה בנפרד ונכון" (לא כאחסון בו-זמני של שתי הרשומות ב-DB, שאינו חלק מהמבנה שאושר)
- **מושב ישן בלי `legId`**: מוצג נכון תחת `legs[0]` — מופיע כשרגל 0 רלוונטית, נעלם כשרגל אחרת רלוונטית
- **"מושבים לא תואמים לתצורה"**: מושב לא-תקין לרגל הנוכחית מופיע ברשימה; מושב לא-תקין של רגל **אחרת** (אפילו אם הוא גם היה נחשב לא-תקין שם) לא מופיע כלל — לא מעורב בין טיסות
- **טיסת החזרה**: מושב שנשמר בחלון 24 השעות שלפני החזרה מתויג `legId:"return"`, מוצג נכון מול `trip.returnSeatLayout` (2-2, 5 עמודות grid, נבדק ויזואלית ומספרית)
- מצב ריק/pre-hydration (`currentLegs=[]`, לפני טעינת טיול) — `currentLockerLegId()`/`resolveActiveSeatLayout()` לא קורסים, נופלים לברירת מחדל סבירה
- צילום מסך של מפת טיסת החזרה בפועל (2-2, מושב 1A מסומן) — אימות חזותי
- `node scripts/i18n-audit.js index.html` — 0 ממצאים (אין טקסט משתמש חדש, רק לוגיקה)
- קונסול נקי לאורך כל הבדיקות
- לא נבדקה כתיבה אמיתית ל-RTDB (הסביבה המקומית, מגבלה מוכרת) — נבדק עד נקודת הכתיבה (`personalLockerCards[uid]` + הארגומנט ל-`db.ref(...).set(...)`), לא מעבר לה

נבדק בפועל: כל מה שלמעלה. v4.49.0 -> v4.50.0 (minor — commit אחרון בתכנית מפת המושבים הפר-רגל, סוגר את הפרויקט: כל הרכיבים - `legs.js`, `legBuilder.js`, פאנל אדמין, תצוגה, ו-`personalLocker` - עכשיו מודעים לרגליים מקצה לקצה).

## v4.49.0 — מפת מושבים פר-רגל, commit 4: חיווט התצוגה (index.html)

### ✨ תכונה חדשה
- **באנר "הכרטיס שלי"/"מפת הושבה" (`seatsBannerBtn`, `lockerCardContainer`) עברו מ-`isFlight && diffOut>0` (טיסה ראשונה בלבד, ללא מודעות לרגליים) ל-`window.getRelevantFlight({legs: currentLegs})` (legs.js, commit 1)** — כעת מופיעים לפני כל טיסה רלוונטית בטיול: לפני `legs[0]` (כמו היום), 24 שעות לפני כל מעבר-יעד נוסף, ו-24 שעות לפני טיסת החזרה (`legId:"return"` — לא רגל, ר' commit 1/2) — לא רק פעם אחת בתחילת הטיול
- **`renderPlaneSeats()` בוחרת seatLayout לפי הטיסה הרלוונטית, לא לפי שדה טיול יחיד קבוע**: `relevantFlight.isReturn` → `trip.returnSeatLayout`; אחרת הרגל התואמת ב-`currentLegs` לפי `legId` → `leg.seatLayout`, עם נפילה-לאחור לשדה הטיול הישן `seatLayout` לרגל שקדמה לשדה (אותו שרשרת נפילה כמו ב-`seedAdminLegsEditor()`, commit 3) או כשאין טיסה רלוונטית בכלל
- **`personalLockerCards` (בניית `seatMap`, ולידציית מושב בטופס) עדיין לא מסוננות לפי רגל** — נשאר גלובלי, בכוונה: מחוץ להיקף ה-commit הזה, מתוכנן ל-commit 5 (7 נקודות קריאה/כתיבה, כולל הערת קוד על הנחת "טיסה רלוונטית אחת" שהמשתמש ביקש)

### 🔍 מה שנבדק — בפועל בדפדפן, לא רק בקוד
- **טיול חד-רגל, לפני המראה**: באנר מוצג, המפה מציגה את `leg.seatLayout` של הרגל היחידה (2-4-2), לא את `trip.seatLayout` הישן (3-3) — אומת גם ספירת עמודות ה-grid (10 מול 7) וגם `getRelevantFlight` עצמו
- **טיול חד-רגל, רגל בלי `seatLayout` משלה** (קודמת לשדה) — נופל נכון ל-`trip.seatLayout` הישן
- **טיול חד-רגל, אמצע הטיול** (אחרי המראה, רחוק מהחזרה) — באנר נעלם, `getRelevantFlight` מחזיר `null` — התנהגות זהה למה שהיה לפני ה-commit הזה
- **טיול דו/תלת-רגלי**: הרצף המלא לאורך הטיול — באנר נעלם אחרי המראת רגל א׳ (רגל ב׳ רחוקה מ-24 שעות), מופיע שוב מדויק ב-23 שעות לפני רגל ב׳ עם ה-`seatLayout` שלה (2-4-2, 10 עמודות, לא 3-4-3/3-3), נעלם שוב, מופיע ב-23 שעות לפני רגל ג׳ עם השדה שלה (3-4-3, 12 עמודות), נעלם שוב אחרי המראתה — כל מעבר אומת גם ב-`getRelevantFlight` וגם בתוצאת הרינדור בפועל, לא רק אחד מהשניים
- **24 שעות לפני טיסת החזרה**: באנר מופיע עם `legId:"return"`, המפה מציגה את `trip.returnSeatLayout` (ערך ייחודי לבדיקה, 19 עמודות) — לא את `seatLayout` של הרגל האחרונה (12 עמודות) ולא את שדה הטיול הישן (7 עמודות) — שלושתם נבדלים מספרית כדי לשלול טעות בחירה שקטה
- צילום מסך של המפה בפועל (רגל עם 2-4-2, מושב 3C מסומן עם שם מהאובייקט האמיתי `personalLockerCards`) — אימות חזותי, לא רק ספירת עמודות
- קונסול נקי לאורך כל הבדיקות
- לא נבדקה כתיבה אמיתית ל-RTDB (הסביבה המקומית, מגבלה מוכרת) — הבדיקה כולה קריאה/רינדור, אין כתיבה ב-commit הזה בכלל

נבדק בפועל: כל מה שלמעלה. v4.48.1 -> v4.49.0 (minor — שינוי התנהגות גלוי למשתמש: הבאנר מופיע/נעלם אחרת מהיום, לא רק תשתית).

## v4.48.1 — תיקון רגרסיה: גלישה אופקית בפאנל האדמין במובייל (commit 3)

### 🐛 תיקון באג
- **שורת "תצורת מושבים" הפר-רגל (v4.48.0) גרמה לפאנל האדמין כולו לגלוש אופקית ב-375px** — `.admin-leg-seat-row .admin-input{flex:0 0 auto}` שינה את ה-`flex`, אבל לא נגע ב-`width` שהכלל הבסיסי `.admin-input,select.admin-input{width:100%}` כבר קבע; כש-`flex-basis` הוא `auto` (מקרה של `flex:0 0 auto`), הדפדפן נופל חזרה על ה-`width` המפורש של האלמנט אם יש כזה — אז ה-`select` תפס `width:100%` **מתוך שורת ה-flex הפנימית שלו** (`.admin-leg-seat-row`, לא כל הפאנל), ודחף אותה לרוחב גדול משמעותית משורת היעד הרגילה מעליה. נמדד בפועל לפני התיקון: `select` ברוחב 291-293px בתוך שורה של 317px, `scrollWidth` 401px מול `clientWidth` 317px — הגלישה תפשטה בלי חיתוך דרך `.admin-leg-row`→`.admin-legs-list`→`.admin-field`→`.admin-section-box` עד ל-`.bottom-sheet` עצמו (`scrollWidth:430` מול `clientWidth:375`)
- **תיקון**: `.admin-leg-seat-row .admin-input` מקבל `width:104px` מפורש (לא `max-width` — רוחב קבוע וצפוי, לא תלוי בגודל-תוכן-טבעי של `<select>` שמשתנה בין פלטפורמות; 104px מכסה בנוחות גם "2-4-2"/"3-4-3", הערך הארוך ביותר האפשרי). `.admin-leg-seat-label` עובר מ-`flex:0 0 auto` ל-`flex:1;min-width:0` — כך התווית היא זו שמתכווצת בעדינות במקום שה-`select` יידחס או יגלוש, אותו דפוס בדיוק שכבר עובד ב-`.admin-leg-row .admin-input{flex:1;min-width:0}` הקיים (לא נגעו בכלל הבסיסי `.admin-input,select.admin-input{width:100%}` עצמו — הוא תקין ומשמש מקומות אחרים כהלכה)

### 🔍 מה שנבדק — בפועל בדפדפן, מספרי, לא רק חזותי
- **RTL, 375px**: `#adminModal .bottom-sheet` — `clientWidth===scrollWidth===375`. `#adminLegsList` — `clientWidth===scrollWidth===317`. שני יעדים ("פריז"/"2-4-2", "לונדון"/"3-4-3") — כל `select` ברוחב 104px בדיוק, ערכים שונים ונכונים לכל רגל בנפרד (לא רק אחת)
- **LTR (אנגלית), 375px**: אותה בדיקה חוזרת במלואה — `dir==="ltr"`, `bottom-sheet` ו-`adminLegsList` שוב `clientWidth===scrollWidth` בדיוק, שני היעדים עם ערכים שונים (`2-4-2`/`3-4-3`) ותוויות מתורגמות נכון ("Seat layout:"), שני ה-`select` ב-104px — אותה הגנה עובדת גם כששמות היעדים והתוויות מתחלפים לכיוון ההפוך
- **`#adminReturnSeatLayout` (השדה הנפרד לטיסת חזרה, לא נגעו בו בתיקון הזה) נבדק מחדש אחרי השינוי, לא רק לפניו**: `display:flex`, `clientWidth===scrollWidth` הן על `#returnSeatLayoutField` (317/317) והן על ה-`select` עצמו (315/315) — לא הושפע, כצפוי, כי הוא חי מחוץ להקשר ה-flex של `.admin-leg-seat-row` ששונה
- קונסול נקי לאורך כל הבדיקות

נבדק בפועל: כל מה שלמעלה. v4.48.0 -> v4.48.1 (patch — תיקון רגרסיה, לא תכונה חדשה).

## v4.48.0 — מפת מושבים פר-רגל, commit 3: פאנל אדמין (index.html)

### ✨ תכונה חדשה
- **כל יעד בעורך הרגליים מקבל בורר "תצורת מושבים" משלו** — שורה שנייה תחת שם היעד (לא דחוס לשורה הראשונה, צפיפות ב-375px), נשמר כ-`legs[n].seatLayout`. עובד זהה לטיול חד-יעד ומרובה-יעדים — אותו קוד, בלי הסתעפות (עקבי עם עקרון "מסלול קוד אחד" של הסכימה)
- **בורר נפרד, עצמאי: "תצורת מושבים לטיסת החזרה"** — נשמר כ-`trip.returnSeatLayout`, שדה ברמת הטיול (לא רגל — אין `legs[N+1]`)
- **הבורר הגלובלי הישן (`#adminSeatLayout`/`#seatLayoutField`) הוסר לגמרי** — אין יותר שדה יחיד לכל הטיול; כל מקום שהתייחס אליו עודכן (0 אזכורים נשארו, אומת ב-grep)
- **שני השדות החדשים, כמו הישן שהם מחליפים, מוסתרים לחלוטין לטיול לא-`abroad`** — `.hide-seat-rows` על `#adminLegsList` (כלל CSS יחיד שמכסה כל שורת-יעד קיימת ועתידית, לא לולאת JS פר-שורה) + `#returnSeatLayoutField` באותה לוגיקה כמו `#checkinField`

### 🔍 מה שנבדק — בפועל בדפדפן, לא רק בקוד
- **נפילה-לאחור לטיול קיים**: טיול מדומה בלי `legs[]` בכלל (רק `trip.seatLayout` ישן) — `getLegs()` מסנתזת רגל יחידה בלי `seatLayout` משלה, והבורר ברגל 0 מציג נכון את הערך הישן כברירת מחדל (`leg.seatLayout || seatLayout || "3-3"`)
- **טיול דו-רגלי מדומה**: רגל בלי `seatLayout` משלה נופלת לערך הטיול; רגל עם `seatLayout` מפורש מציגה את שלה — שתי הרגלים נכונות בו-זמנית, לא רק אחת
- **שמירה**: שוחזרו הביטויים המדויקים של ה-handler (`legSeatLayouts` פר-שורה, `buildLegs`, `newSeatLayout`/`newReturnSeatLayout`) ואומת שהתוצאה נכונה — `legs[i].seatLayout` תואם לבורר של כל שורה. לא הופעל כתיבה אמיתית ל-RTDB (הסביבה המקומית בלי `tripId` אמיתי, מגבלה מוכרת ומתועדת) — אומת עד נקודת הכתיבה, לא מעבר לה
- מעבר בין סוגי טיול (abroad↔camping) — שתי השורות (פר-יעד + חזרה) מוסתרות/מוצגות נכון
- עברית ואנגלית (כולל תוויות שנוצרות דינמית ב-JS, לא רק HTML סטטי) ונייד 375px — נבדק חזותית
- `node scripts/i18n-audit.js index.html` — 0 ממצאים
- קונסול נקי לאורך כל הבדיקות

נבדק בפועל: כל מה שלמעלה. v4.47.3 -> v4.48.0 (minor — תכונה חדשה גלויה למשתמש/מארגן).

## v4.47.3 — מפת מושבים פר-רגל, commit 2: legSeatLayouts (legBuilder.js)

### 🔧 תשתית (עדיין אף UI/קריאה אמיתית לא שולחת את הפרמטר החדש)
- **`buildLegs()` מקבלת `legSeatLayouts` אופציונלי** — מקביל ל-`legNames`/`legLocations` (אותה גישה לפי אינדקס), כותבת `leg.seatLayout` לכל רגל. חסר/undefined → `null`, **לא** נופל ל-"3-3" כאן — הפעלת ברירת המחדל היא אחריות הקורא בזמן קריאה, בדיוק כמו `countryCode` הקיים (§8ব.11ব.3: רגל שקדמה לשדה לא מאופסת, נופלת לערך הטיול הישן)
- **`trip.returnSeatLayout` לא עובר דרך הפונקציה הזו בכלל** — שדה טיול נפרד, לא רגל, אין `legs[N+1]` לתלות עליו את טיסת החזרה. מטופל בנפרד ב-commit 3
- **תאימות לאחור מאומתת מול הקוראים האמיתיים, לא רק הנחה**: קריאת `wizard.html` בפועל ([wizard.html:1919-1922](wizard.html:1919)) לא שולחת את הפרמטר בכלל — עדיין עובדת בלי שינוי, `seatLayout:null` על כל רגל. קריאת `index.html` הקיימת גם היא לא שולחת אותו עדיין (יחובר ב-commit 3) — אין רגרסיה

### 🔍 מה שנבדק
- `buildLegs()` נקראה ישירות עם `legSeatLayouts` מלא (כל רגל מקבל את הערך שלה לפי אינדקס), בלי הפרמטר בכלל (תואם את קריאת ה-wizard האמיתית, `seatLayout:null` בלי קריסה), ועם מערך חלקי (רגל אחת מוגדרת, השנייה נופלת ל-`null`) — שלושת המצבים נכונים
- `node --check legBuilder.js` עובר
- קונסול נקי ב-`index.html` (הקורא האמיתי היחיד לקובץ הזה כרגע מלבד wizard) אחרי הטעינה

נבדק בפועל: כל מה שלמעלה. v4.47.2 -> v4.47.3 (patch — תשתית פנימית, שום קורא אמיתי עדיין לא שולח את הפרמטר החדש).

## v4.47.2 — מפת מושבים פר-רגל, commit 1: getRelevantFlight (legs.js)

### 🔧 תשתית (פונקציות עזר בלבד, אף קוד עדיין לא קורא להן)
- **`window.getRelevantFlight(trip)`** — "יש טיסה רלוונטית עכשיו, ואיזו". חוק א-סימטרי, לפי ההחלטה שהתקבלה: `legs[0]` (טיסת ההלוך מהבית) — רלוונטית מרגע יצירת הטיול ועד ההמראה שלה, בלי שער-24-שעות. `legs[1..]` (כל מעבר נוסף בין יעדים) — רלוונטית רק ב-24 השעות שלפני `startAt` שלה. **טיסת החזרה** (`legs[אחרון].endAt`/`trip.endAt`) — לא רגל בכלל, אין `legs[N+1]` שאליה אפשר לתלות אותה; אותו חוק 24-שעות, מסומנת ב-`legId` הקבוע `"return"` (לא מתנגש עם שום `legId` אמיתי — אלה תמיד `"main"` או `leg_<אקראי>`, ר' `legBuilder.js`)
- **לא בנוי על `getLegForDate`/`tripDayDates`** — במפורש. אלה עונות "לאיזו רגל שייך יום קלנדרי" (גרנולריות יום); כאן צריך "האם רגע מדויק נמצא בטווח 24 שעות לפני המראה" — חשבון מילישניות גולמי, לא בדיקת גבול-יום. שתי שאלות שונות, לא מקרה פרטי של אותה שאלה
- **בלי תלות ב-timezone** — `startAt`/`endAt` הן מחרוזות ISO עם offset מפורש (UTC), אז `new Date(x) - now` הוא הפרש בין שני רגעים מוחלטים. timezone רלוונטי רק לגזירת תאריך-קלנדרי/שעון-קיר מרגע, לא לבדיקת "כמה זמן נשאר" גרידא
- **לא פותרת `seatLayout`** — רק "איזו טיסה". אותו עיקרון one-job-per-function כמו `getCurrentLegTimezone`/`getCurrentLegCoords`/`getCurrentLegName` הקיימות באותו קובץ

### 🔍 מה שנבדק
- 10+ מקרי בדיקה דרך `window.getRelevantFlight` ישירות, כולל: טיול חד-רגל לפני/אחרי המראה, קרוב/רחוק מטיסת החזרה, טיול מרובה-רגליים בכל שלביו, וגבול מדויק (24 שעות פחות/יותר 10ms, בדיוק ברגע ההמראה) — כל התוצאות תואמות את החוק בדיוק, כולל שהגבול כולל (`<=`) ולא פתוח
- `node --check legs.js` — עובר
- נבדק בשני ההקשרים: `index.html` (סקריפט קלאסי) ו-`itinerary.html` (מודול) — `window.getRelevantFlight` נגישה ועובדת זהה בשניהם
- קונסול נקי

נבדק בפועל: כל מה שלמעלה. v4.47.1 -> v4.47.2 (patch — תשתית פנימית, שום דבר לא קורא לזה עדיין, אין שינוי חזותי).

## v4.47.1 — טבעת הנשימה נמסה בצד כמו הרקע, ומחליפה את מנגנון v4.47.0 (index.html)

**מחליף את מנגנון הטבעת של v4.47.0, לא מוסיף לו.** הטבעת המלאה ששוחררה שם (זוהר/צל סביב כל 4 צדי הכרטיס) יורדת כאן לגמרי, ומוחלפת במנגנון שונה. מי שכבר עדכן ל-v4.47.0 יראה טבעת שנעלמת בצד אחד במקום מסגרת סגורה — לא תוספת קטנה, שינוי של איך האפקט עובד.

### 🎨 עידון עיצובי
- **הטבעת המלאה (v4.47.0) יצרה מסגרת סגורה שלמה — לא עקבי עם הרקע שכבר נמס רק בצד אחד.** עכשיו הטבעת עצמה נמסה **באותה נקודה בדיוק** שהרקע נמס (55%, אותו כיוון, מתהפך תחת `[dir="ltr"]` באותו אופן) — תחושה של שכבה אחת רציפה, לא שני אפקטים לא-קשורים
- **ממומש על `::after` נפרד (לא `box-shadow` ישיר על `.action-card`), עם `border` + `box-shadow: inset` — לא `box-shadow` רגיל.** נבדק בפועל, לא רק תיאורטית: `box-shadow` רגיל (כלפי חוץ) תחת אותה מסכה כמעט נעלם לגמרי — `mask-image` תחום כברירת מחדל ל-border-box של האלמנט, ורוב האנרגיה הוויזואלית של `box-shadow` כלפי-חוץ (הבלור/spread) מצטיירת **מחוץ** לתחום הזה ולכן לא נמסכת בכלל, אלא נעלמת. `border` (תמיד בדיוק על גבול הקופסה) ו-`box-shadow:inset` (דולף פנימה, לא החוצה) נשארים בתוך תחום המסכה במלואם ולכן נמסים חלק ונקי
- **z-index של ה-`::after` (-1) יושב באותו הקשר-ערימה כמו ה-`::before`** — מ-`.action-card{z-index:0}` (commit א), בלי סיכון stacking חדש; מצטייר מעל ה-`::before` (אחריו ב-DOM) אבל עדיין מתחת לכל תוכן אמיתי — אותה בדיקה בדיוק כמו commit א, לא הונחה כמובנת מאליה

### 🔍 מה שנבדק
- **השוואה ויזואלית ישירה, שלוש גרסאות זו לצד זו, אותה מסכה בדיוק**: box-shadow רגיל (כמעט בלתי-נראה), border בלבד (עובד נקי), border+inset shadow (עשיר יותר, גם עובד נקי) — לפני שנבחרה הגרסה הסופית
- כל 4 ערכות הטיול (abroad/local/camping/reserve) נבדקו במצב בהיר — הצבע/עוצמה קוראים נכון בכל אחת, לא רק שהמסכה עובדת
- `[dir="ltr"]`/`[dir="rtl"]` — נבדק ישירות ש-`mask-image` המחושב מתהפך נכון בשני הכיוונים
- **`getAnimations()` על ה-`::after`** (`effect.pseudoElement === "::after"`) — 3 keyframes אומתו: `0%`/`100%` זהים (עוצמה בינונית), `50%` שיא — reduced-motion יתכנס לבינוני
- קובייה בלי `has-content` — אין `::after` בכלל
- קונסול נקי (שגיאת `contextActionText`/`null.style` שהופיעה בבדיקה היא רעש קיים מסביבת הבדיקה הסינתטית שלי, לא קשורה לשינוי הזה — timer תקופתי שמניח סבב אתחול מלא עם נתונים אמיתיים)

נבדק בפועל: כל מה שלמעלה. v4.47.0 -> v4.47.1 (patch — עידון/תיקון מנגנון, לא תכונה חדשה).

## v4.47.0 — אפקט "נשימה" על קוביות עם תוכן (index.html, commit ג — סיום עיצוב מחדש)

### ✨ תכונה חדשה
- **קובייה עם תוכן (`.has-content`, מ-commit ב) "נושמת" בעדינות — טבעת זוהר/צל על שפת הכרטיס שמתחזקת ונחלשת בלולאה (3.6s), בצבע `--glow-color` של ערכת הטיול הנוכחית.** קובייה בלי תוכן — סטטית לגמרי, בלי אנימציה ובלי צל קבוע כלשהו מעבר ל-`--shadow-card` הרגיל
- **`--glow-color` במצב בהיר — שדה חדש, 4 ערכים (אחד לכל ערכה), לא הומצא מאפס: `var(--primary)`** של אותה ערכה בדיוק. נמנע כפילות צבע מיותרת (ר' הערת CLAUDE.md על שכפול `--glow-color`) ומבטיח ניגודיות שכבר אומתה בשאר האפליקציה לכפתורים/טקסט על אותה ערכת רקע. במצב כהה `--glow-color` כבר קיים מזמן (4 ערכים, ללא שינוי) — מתבטא שם כזוהר סימטרי (`box-shadow` בלי היסט), במצב בהיר כצל (עם היסט אנכי, כמו drop-shadow) — לא אותה תחושה חזותית בכוונה, לפי איך שכל אחד קורא נכון על הרקע שלו
- **`0%`/`100%` בקיימת = עוצמה בינונית, `50%` = שיא — לא `0%`/`100%`=כלום כמו `adminGearPulse`, בכוונה.** הכלל הגורף הקיים `@media(prefers-reduced-motion:reduce){*{animation-iteration-count:1}}` (כבר בקובץ, לא נוסף) גורם לאנימציה עם `reduced-motion` להתכנס לפריים ה-100% — שזהה ל-0% (בינוני), לא לשיא ולא לכלום. **אומת בפועל דרך Web Animations API** (`getAnimations()[0].effect.getKeyframes()`), לא רק קריאת קוד — 3 ה-keyframes (0/0.5/1) הוחזרו עם הערכים הצפויים בדיוק
- **`box-shadow` ישיר על `.action-card`, לא `::before`/mask נפרד** — בניגוד להמסה הצידית (commit א), טבעת-קצה לא צריכה מיסוך כיווני: `box-shadow` הוא כבר אפקט שמכבד את צורת הקופסה/`border-radius` של האלמנט עצמו, לא מילוי-רקע שצריך לדהות לכיוון מסוים. גם אין סיכון stacking-escape כמו ב-`::before{z-index:-1}` — `box-shadow` הוא תכונת ציור על האלמנט שכבר יש לו הקשר-ערימה משלו (`z-index:0`, מ-commit א), לא צאצא ממוקם

### 🔍 מה שנבדק
- קובייה עם תוכן נושמת בפועל (לא רק ב-keyframes) — ערכות abroad ו-local, שני מצבים, טבעת צבועה נכון לכל ערכה (`var(--primary)` שלה)
- קובייה בלי תוכן — ללא אנימציה כלל (`getComputedStyle(...).animationName === "none"`), ללא צל קבוע
- `getAnimations()` אימת 3 keyframes עם עוצמות נכונות (0/1=בינוני זהה, 0.5=שיא)
- עברית ואנגלית ("X activities"/"X פעילויות" מה-badge של commit ב), נייד 375px
- `node scripts/i18n-audit.js index.html` — 0 ממצאים
- קונסול נקי

נבדק בפועל: כל מה שלמעלה. v4.46.0 -> v4.47.0 (minor — תכונה חדשה, גלויה למשתמש). זהו commit ג האחרון — עיצוב מחדש רשת המודולים הושלם (א: הסרת צבע-למודול + המסה, ב: סיגנל תוכן תקין, ג: אפקט נשימה).

## v4.46.0 — סיגנל "יש תוכן" תקין לכל 5 קוביות המודולים, כולל תוכנית (index.html, commit ב)

### ✨ תכונה חדשה
- **`itineraryBadge` מתמלא בפועל לראשונה** — קיים ב-markup מאז v4.37.0 אבל אף קוד לא כתב אליו טקסט או הסיר ממנו `hidden`, אז הוא תמיד נשאר מוסתר גם כשיש הרבה פעילויות בתוכנית. עכשיו סופר את כל הפעילויות בכל הימים (`data.itinerary.days[key].activities`, אותו נתון שכבר קיים ב-render הזה — אין קריאת RTDB נוספת) ומציג "X פעילויות" באותו סגנון בדיוק כמו שאר 4 הבאדג'ים

### 🔧 שינוי פנימי (הכנה ל-commit ג — עדיין בלי שום שינוי חזותי)
- **`.action-card.has-content`** — class חדש שמתעדכן על כל אחת מ-5 הקוביות, עם המקור הנכון לכל אחת (לא "הראה/הסתר badge" גנרי, כי זה שגוי לפחות בשתיים):
  - ציוד/קניות/מקומות/תוכנית: אותו חישוב "יש פריטים" שכבר קובע את ה-badge שלהן
  - **הוצאות: `Boolean(data.spliitUrl)`, לא נראות ה-badge** — ה-badge שם הפוך במכוון (מוצג כשאין קישור מוגדר, כהזמנה למארגן; מוסתר כשיש) — שימוש בנראות שלו כ"יש תוכן" היה נותן את התוצאה ההפוכה
- שום כלל CSS לא מתייחס עדיין ל-`has-content` — ה-class קיים ב-DOM אבל לא משנה שום דבר ויזואלית עד commit ג

### 🔍 מה שנבדק
- קריאה ישירה ל-`renderActionBadges`/`updateExpensesCard` עם נתונים מדומים: 3 ואז 5 פעילויות → badge מציג את המספר הנכון, לא מוסתר, `has-content:true`; 0 פעילויות → `has-content:false`, badge מוסתר
- הוצאות: `spliitUrl` ריק → `has-content:false` (גם כש-badge עצמו מוסתר, כי `isOrganizer()` false בבדיקה המדומה — אישור שה-signal לא תלוי בלוגיקת ה-badge); `spliitUrl` מוגדר → `has-content:true`
- `node scripts/i18n-audit.js index.html` — 0 ממצאים חדשים
- קונסול נקי

נבדק בפועל: כל מה שלמעלה. v4.45.4 -> v4.46.0 (minor — badge שלא עבד מעולם מתחיל לעבוד, שינוי גלוי למשתמש).

## v4.45.4 — תיקון היקף: מסגרת בסיס ניטרלית עדיין נשארה על .action-card (index.html)

### 🎨 תיקון היקף (לא באג — עיצוב v4.45.2 לא הושלם עד הסוף)
- **`.action-card` עדיין נשאה `border: 1px solid var(--border-color)` + `border-inline-end: 3px solid var(--border-color)`** — לא היו חלק מ-6 נקודות הצבע-למודול שהוסרו ב-v4.45.2 (כי מעולם לא היו צבעוניות-למודול), אבל יצרו מסגרת מלאה סביב הכרטיס — בניגוד לעיצוב המאושר: "בלי מסגרת בכלל, רק הרקע שנמס". גם ה-override התואם ל-dark mode (`[data-theme="dark"] .action-card{border-inline-end:1px solid var(--border-color)}`) הוסר באותה רוח
- **`border-radius`/`overflow:hidden` לא תלויים ב-`border` בכלל** — הפינות המעוגלות והחיתוך של ה-`::before` הממוסך ממשיכים לעבוד זהה בלי הקו

### 🔍 מה שנבדק
- מצב כהה ובהיר, ערכת abroad — אין שום קו מתאר, רק הרקע הכהה/בהיר שנמס שמאלה מול רקע העמוד, פינות מעוגלות שלמות
- קונסול נקי

נבדק בפועל: כל מה שלמעלה. v4.45.3 -> v4.45.4 (patch — תיקון היקף בעיצוב, לא תכונה חדשה).

## v4.45.3 — תיקון: ההמסה הצידית לא הוצגה בפועל (index.html)

### 🐛 תיקון באג
- **`.action-card::before` (שכבת ההמסה, v4.45.2) לא הוצג בכלל — לא רק חלש/עדין, לגמרי בלתי-נראה.** סיבת שורש: `.action-card` הוא `position:relative` בלי `z-index` מפורש, ולכן לא יוצר הקשר-ערימה (stacking context) משלו. `z-index:-1` של ה-`::before` "ברח" להקשר-הערימה של `#appContainer` (גם הוא `position:relative`, עם רקע אטום) ונצבע מתחתיו — נעלם לגמרי, בלי קשר ל-mask עצמו
- **אומת בבידוד לפני התיקון**: רקע כתום מלא + בלי mask על ה-`::before`, עם `z-index:-1` המקורי — אפס כתום נראה. עם `z-index:auto` על ה-`::before` — כתום מלא מוצג (אבל מכסה את הטקסט, כמצופה). עם `z-index:0` נוסף ל-`.action-card` בלבד (בלי לגעת ב-overflow/mask) — כתום מוצג נכון, מאחורי הטקסט. זה מבודד את התיקון בוודאות ל-`z-index:0` על `.action-card` — לא ל-`overflow`, לא ל-mask
- **התיקון**: `z-index: 0;` נוסף ל-`.action-card` (שורה אחת). הערה צמודה בקוד מתעדת את המנגנון, כדי שלא יישבר שוב באותה טעות אם מישהו יסיר את ה-`z-index` בעתיד מבלי להבין למה הוא שם

### 🔍 מה שנבדק
- מצב כהה וגם בהיר, ערכת abroad — ההמסה מוצגת בפועל אחרי התיקון (לא רק ב-computed style, גם ויזואלית, כולל דרך בדיקת שכבה זמנית עם רקע כתום בולט כדי לאשר את שכבות הציור בוודאות)
- הסיבה השורשית מבנית (לא תלוית-צבע) — לא נבדקו כל 4 הערכות בנפרד, אין סיבה טכנית שתתנהג אחרת

נבדק בפועל: כל מה שלמעלה. v4.45.2 -> v4.45.3 (patch — תיקון באג, לא תכונה חדשה).

## v4.45.2 — רשת המודולים בדף הבית: הסרת צבע-לפי-מודול, רקע נמס (index.html, commit א)

### 🎨 עיצוב מחדש
- **6 touch-points שנתנו לכל אחת מ-5 קוביות המודולים (ציוד/קניות/מקומות/תוכנית/הוצאות) צבע ייחודי משלה יורדים, בלי יוצא מן הכלל** — מסגרת (`border-inline-end-color` בהיר), טבעת זוהר (`box-shadow` כהה), רקע עיגול `.action-icon` (בהיר), זוהר `::before` מאחורי האייקון + `--icon-glow` (כהה), קו מתאר ה-SVG הפולבק (בהיר), ומילוי הצורות הדקורטיביות ב-SVG (כהה). כולם היו מקומיים ל-index.html בלבד (לא ב-shared.css, לא בעמוד אחר) — נבדק לפני הכתיבה. **גם `expensesTitle` ירדה מהתייחסות מיוחדת** — `style="color:var(--expense)"` inline שהייתה שריד יחיד לצבע-מודול על טקסט (4 הכותרות האחרות היו כבר ניטרליות) — הוסרה כדי לא להשאיר יוצא-מן-הכלל בודד
- **`.action-icon` בלי שום רקע/מסגרת** — האייקון (תמונה/SVG פולבק) נשאר לגמרי חשוף, בלי עיגול-רקע מאחוריו, גם בהיר וגם כהה. גם הבדל הגודל 52px↔56px בין המצבים (שהיה קיים רק כדי לפנות מקום לזוהר שהוסר) בוטל — 52px אחיד
- **רקע הקובייה "נמס" הצידה במקום מלבן אחיד** — `.action-card::before` (מאחורי התוכן, `z-index:-1`, לא על `.action-card` עצמו) עם `mask-image: linear-gradient(...)`, מלא בצד ה-start (ימין ב-RTL), דוהה לשקיפות עד שמתמזג ברקע העמוד. **מכוון: לא על האלמנט עצמו** — מיסוך ישיר על `.action-card` היה מדהה גם את הטקסט (כותרת/תת-כותרת), לא רק את הרקע. `-webkit-mask-image`+`mask-image` יחד (Safari דרש היסטורית פריפיקס). כיוון ה-gradient הפוך תחת `[dir="ltr"]` — אותו עיקרון כמו כל אלמנט כיווני אחר באפליקציה, לא הזנחה של המקרה האנגלי

### 🔍 מה שנבדק
- CSS בלבד ב-commit זה — לא נוגע בלוגיקת ה-badge (זו commit ב נפרדת, ר' תיעוד עתידי)
- `--primary-soft` (שהוסר משימוש ב-`.action-icon`) עדיין בשימוש פעיל בעוד כ-6 מקומות אחרים ב-index.html — לא משתנה יתום

נבדק בפועל: פרטים למטה, אחרי בדיקה חזותית. v4.45.1 -> v4.45.2 (patch — עיצוב מחדש, בלי יכולת חדשה למשתמש).

## v4.45.1 — ניסוח ותצוגה למטייל יחיד (index.html, itinerary.html)

### 🐛 תיקון ניסוח/תצוגה
- **פאנל "מי בטיול": הערה חדשה כשיש בדיוק חבר אחד** — "רק את/ה בטיול הזה בינתיים" / "Just you on this trip so far", מתחת לשורה היחידה. **תיקון-דיוק, לא תיקון-טעות**: נבדק במפורש לפני הכתיבה שאין היום שום ניסוח קיים שמניח קבוצה במצב הזה — מצב 0-חברים ("עדיין אין חברים בטיול") כמעט לעולם לא מתרחש בפועל (המשתמש כבר רשום ל-`memberProfiles` ברגע שהוא רואה את הפאנל), אז ב-1 חבר לא היה עד היום שום ניסוח בכלל, לא ניסוח שגוי
- **צ'יפ RSVP ("אני בא/ה") מוסתר על כל פעילות כשיש בדיוק חבר אחד בטיול** — רעש ויזואלי בלי ערך: ברור שאתה בא לפעילות שיצרת. שאר השורה (שעה, כותרת, תפריט ⋮) לא משתנה
- **שתי הבדיקות רצות בכל רינדור מול `memberProfiles` בפועל, לא ערך שנשמר פעם אחת ביצירת הטיול.** במפורש **לא** דגל `isSolo`/שדה חדש — docs/roadmap.md פריט 11 כבר קבע "בחירה, לא זיהוי אוטומטי" לגבי מצב סולו כתכונה עתידית, אבל שני התיקונים האלה אינם תכונת "מצב סולו" — הם תיקוני ניסוח/תצוגה נקודתיים שכל מה שהם צריכים הוא לבדוק מה נכון **עכשיו**, בכל רגע נתון

### 🔍 מה שנבדק בפועל — כולל המעבר עצמו, לא רק שני המצבים בנפרד
- טיול עם חבר אחד: ההערה מוצגת בפאנל, הצ'יפ נעלם מהפעילויות
- **הצטרפות חבר שני, בלי רענון עמוד**: ב-`itinerary.html` אומת דרך אותו נתיב תגובתי בדיוק שה-`onValue` האמיתי קורא לו (`applyLanguage()`, לא קריאה ישירה ל-`renderActivities()`) — הצ'יפ חוזר אוטומטית. **הבדל אמיתי בין שני העמודים, מתועד ולא מוסתר**: ב-`index.html`, `renderMembersSheet()` רץ רק כשהפאנל **נפתח** (לא תלוי ב-listener החי) — ההערה תיעלם בפתיחה הבאה של הפאנל אחרי שחבר הצטרף, לא באמצע פאנל שכבר פתוח על המסך
- רגרסיה: טיול שמתחיל עם 2+ חברים מלכתחילה — לא מוצגת הערה בפאנל, הצ'יפ מוצג רגיל על כל פעילות, בשני העמודים
- עברית ואנגלית, מצב בהיר, 375px
- `node --check` על שלושת בלוקי הסקריפט הקלאסיים ב-index.html ועל בלוק המודול ב-itinerary.html. `node scripts/i18n-audit.js` על שניהם — 0 ממצאים. קוד בדיקה זמני (`window.__test`) הוסר משני הקבצים ואומת שהוסר

נבדק בפועל: כל מה שלמעלה. v4.45.0 -> v4.45.1 (patch — תיקוני ניסוח/תצוגה, לא תכונה חדשה).

## v4.45.0 — ייצוא פעילויות ליומן כקובץ .ics (itinerary.html, commit א)

### ✨ תכונה חדשה
- **ייצוא פעילות בודדת ("📅 ייצוא ליומן") ו-ייצוא יום שלם ("📅 ייצוא היום ליומן")** — שני כפתורים, לא אחד. פעילות בודדת מייצאת תמיד, בלי תלות ב-RSVP — המשתמש בחר במפורש איזו פעילות. ייצוא-היום מסונן ל-RSVP בלבד (`joiners[uid]`) — לא כל מה שקיים באותו יום, רק מה שהמשתמש בעצמו סימן "אני בא/ה" עליו
- **כפתור "ייצוא היום" מוצג `disabled` מראש כשאין אף RSVP באותו יום** — לא כפתור פעיל עם הודעה אחרי לחיצה. עקבי עם התווית "לא מאומת" בהצעות AI למקומות ורשימת "לא בתצורה" במפת המושבים — שקיפות מיידית, לא הפתעה אחרי הלחיצה. הבדיקה סינכרונית וזולה: `itineraryDays` (כולל `joiners` של כל פעילות) כבר בזיכרון במלואו ממאזין `onValue` יחיד על שורש הטיול — אין קריאת רשת נוספת
- **תפריט ⋮ על כל כרטיס פעילות זמין עכשיו לכולם, לא רק ליוצר/למארגן.** לפני כן היה מוצג רק כשה-`canManageEntry(x)` התקיים (עריכה/מחיקה). ייצוא ליומן הוא לא פעולת-ניהול — כל חבר קבוצה שרואה פעילות צריך להיות מסוגל לייצא אותה לעצמו, כולל כאלה שלא יצרו אותה. שינוי מכוון: התפריט מוצג תמיד, עם "ייצוא ליומן" תמיד בפנים, ו"עריכה"/"מחיקה" רק כשיש הרשאה — לא שני מנגנונים נפרדים
- **אין Google Places API, אין Cloud Function — הכל בצד לקוח.** אין תשתית קיימת ליצירת קובץ להורדה באפליקציה (נבדק גלובלית לפני הכתיבה — 0 שימושים ב-Blob/download בכל 6 העמודים), אז זה הדפוס הראשון מסוגו: `Blob`+`URL.createObjectURL`+`<a download>`

### 🔒 מה שהוכרע לפני הכתיבה, ולמה
- **אזור זמן נגזר מ-`tripDayDates()`, לא מ-`getLegForDate`.** נתפס במפורש בתכנון לפני הכתיבה, לא אחריה: `getLegForDate` היא מבוססת-instant, ותפסה בעבר יום שנופל ברגל הלא-נכונה קרוב לגבול-רגליים כשה-timezone של המכשיר הצופה שונה מזה של הרגל (מתועד ב-`legs.js`, fixture רומא/בנגקוק). `tripDayDates(trip, tripId)` (כבר קיימת, `legs.js`) נמנעת מזה בכוונה — משווה מחרוזות `dayKey`, לא instants. המרת השעה עצמה עוברת דרך `window.localInputToUtcInZone()` הקיימת ב-`datetime.js`, כבר בשימוש באותה בעיה בדיוק ב-`legBuilder.js`/`wizard.html`/`index.html`
- **פונקציית בניית VEVENT אחת בלבד (`buildIcsEvent`)** — גם ייצוא-פעילות-בודדת וגם ייצוא-יום קוראות לה, יום קורא בלולאה. לא שני מימושים נפרדים. מתועד כהערה צמודה לפונקציה עצמה, באותו דפוס שכבר נהוג בפרויקט למלכודות דומות (למשל ההערה על `dateKeyOf`/`getLegForDate` ב-`legs.js` עצמו)
- **פעילות "גמישה" (בלי שעה) — לא מדלגים עליה.** מיוצאת כאירוע-כל-היום (`DTSTART;VALUE=DATE`, בלי `DTEND` — RFC 5545 מניח משך של יום שלם כברירת מחדל). דילוג שקט נפסל במפורש: "תוצאה חלקית שנראית מלאה" הוא בדיוק סוג הבאג שכבר טופל היום פעמיים (הצעות AI למקומות, מפת המושבים)
- **משך ברירת מחדל לפעילות עם שעת התחלה בלבד: שעה אחת.** אין רמז בממשק לציפייה אחרת — הפעילויות מוצגות כרשימה שטוחה ממוינת-לפי-שעה, לא ציר-זמן/גריד עם בלוקים לפי משך. שעה אחת היא ברירת המחדל הכמעט-אוניברסלית שכל אפליקציית יומן מרכזית (Google Calendar, Outlook, Apple Calendar) כבר משתמשת בה לאירוע חדש — לא ניחוש ספציפי ל-GitTrip

### 🔍 מה שנבדק בפועל
- המרת שעה נכונה בשני legs בפועל: בנגקוק (UTC+7) 14:00 מקומי → `DTSTART:...T070000Z`, טוקיו (UTC+9) 09:00 מקומי → `DTSTART:...T000000Z` — שני אזורי זמן שונים, לא רק אחד
- פעילות גמישה → `DTSTART;VALUE=DATE`, בלי `DTEND`, בלי ניסיון המרת-שעה
- בריחה נכונה של תווים מיוחדים (`,`/`;`/שבירת שורה) בכותרת/הערה, לפי RFC 5545 — נבדק ישירות על מחרוזת עם כל השלושה
- ייצוא-יום מסונן RSVP בפועל: מתוך 3 פעילויות באותו יום (אחת בלי RSVP, שתיים עם), הקובץ שנוצר בפועל הכיל רק את השתיים המסומנות — אומת ישירות על תוכן ה-Blob, לא רק על שהכפתור לא-disabled
- ייצוא-פעילות-בודדת על פעילות **בלי** RSVP — מיוצאת בהצלחה, מאשר שאין תלות ב-RSVP שם
- כפתור "ייצוא היום" `disabled` כשאין RSVP ביום, מופעל כשיש — נבדק בשני הכיוונים
- תפריט ⋮ מוצג על פעילות שלא נוצרה ע"י המשתמש הנוכחי, עם "ייצוא ליומן" בלבד (בלי עריכה/מחיקה) — מאשר את שינוי ההרשאות המכוון
- עברית ואנגלית (כותרת הכפתור, פריט התפריט), מצב בהיר, 375px
- `node --check` על בלוק המודול. `node scripts/i18n-audit.js itinerary.html` — 0 ממצאים. קוד בדיקה זמני (`window.__test`) הוסר ואומת שהוסר

נבדק בפועל: כל מה שלמעלה. v4.44.1 -> v4.45.0 (minor — תכונה חדשה).

## v4.44.1 — תיקון: אין אינדיקציית טעינה בהצעות AI למקומות כשהרשימה לא ריקה

### 🐛 תיקון באג
- **`$('suggestPlacesBtn').onclick=requestPlaceSuggestions;` — הפניה ישירה לפונקציה, לא עטופה בפונקציית חץ.** כשדפדפן קורא ל-handler שהוצב ככה, הוא מעביר את **אובייקט ה-`Event` של הקליק** כארגומנט הראשון, לא `undefined`. `requestPlaceSuggestions(triggerBtn)` קיבלה בפועל את ה-`MouseEvent` בתור `triggerBtn` — ומכיוון שהוא ערך אמת (truthy), הביטוי `triggerBtn||$('suggestPlacesBtn')` בחר בו במקום בכפתור האמיתי. כל שינויי מצב הטעינה (`button.disabled=true`, `button.textContent='חושב...'`) הוחלו על אובייקט ה-Event הזמני — בלי שגיאה, בלי אפקט גלוי. הכפתור עצמו לא זז
- **קרה רק כשברשימה כבר היו מקומות.** כפתור ההצעות במצב הריק (commit א, v4.42.0) חוט מראש נכון: `document.querySelectorAll('[data-suggest-places-ai]').forEach(b=>b.onclick=()=>requestPlaceSuggestions(b))` — עטוף בפונקציית חץ שמעבירה את האלמנט עצמו. רק חיווט כפתור שורת הכלים (שהוא היחיד הפעיל אחרי שיש מקום אחד ברשימה) חסר את העטיפה הזו
- **התיקון**: אותה עטיפה בדיוק גם עבור כפתור שורת הכלים — `$('suggestPlacesBtn').onclick=()=>requestPlaceSuggestions($('suggestPlacesBtn'));`. שני נקודות הכניסה משתמשות עכשיו באותה מוסכמת-קריאה (מעבירות אלמנט DOM אמיתי במפורש), לא שתי מוסכמות שונות שרק אחת מהן בטוחה

### 🔍 מה שנבדק בפועל
- רשימה עם מקומות קיימים, לחיצה על ההצעות — "חושב..." מופיע על הכפתור האמיתי (`disabled:true`), נעלם ומחזיר את התווית המקורית כשהתוצאות מגיעות
- רשימה ריקה — אותה התנהגות בדיוק, ללא רגרסיה
- תרחיש כשל (בקשה שנדחית) — האינדיקציה חוזרת למצב רגיל, לא נתקעת על "חושב..." (`finally` הקיים כבר דואג לזה ברגע שהכפתור הנכון בהישג יד)
- עברית ואנגלית, מצב בהיר, 375px

נבדק בפועל: כל מה שלמעלה. v4.44.0 -> v4.44.1 (patch — תיקון באג).

## v4.44.0 — ולידציית הזנת מושב תלוית-תצורה (index.html, commit ב)

### ✨ תכונה חדשה
- **`savePersonalLockerCard()` דוחה עכשיו מושב שלא תואם את `seatLayout` הנוכחי של הטיול** — אות שלא בסט התקין (נגזר מ-`parseSeatLayout()` המשותפת, לא טווח קבוע: `2-2`→A-D, `3-3`→A-F, `2-4-2`→A-H, `3-3-3`→A-J, `3-4-3`→A-K, כולן בלי I) או מספר שורה מחוץ ל-1-45 — חוסמים שמירה, עם הודעת שגיאה ליד השדה (`#lockerSeatError`, לא טוסט), אותו דפוס בדיוק כמו ולידציית הטלפון/קישור הוואטסאפ
- **שתי הודעות נפרדות, לא אחת גנרית** — אחת לאות לא-תקפה (כוללת את התצורה הנוכחית ואת רשימת האותיות התקפות בפועל, לא רק "לא תקין"), אחת לשורה לא-תקפה. כל אחת אומרת למשתמש בדיוק מה לתקן

### 🔒 מה שנבדק/הוכרע לפני הכתיבה
- **הוולידציה לא רצה רטרואקטיבית על מושב שכבר נשמר** — נבדק במפורש כי זה בדיוק החשש שהועלה מראש: אם המארגן משנה `seatLayout` אחרי שמישהו כבר שמר מושב תקף (אותו מקרה כמו רשימת "לא בתצורה" מ-commit א), פתיחת הטופס מחדש כדי לערוך שדה אחר (למשל קישור הכרטיס) לא הייתה אמורה "לנעול" את המשתמש בגלל שדה מושב שהוא לא נגע בו. הפתרון: משווים את הערך המנורמל מול מה שכבר שמור (`personalLockerCards[uid].seat`) — הוולידציה מופעלת רק כשהערך **משתנה בפועל**, לא בכל שמירה. נבדק משני הכיוונים: שמירת שדה אחר עם מושב-ישן-שהפך-לא-תקף עוברת בלי שגיאה; הזנת מושב **חדש** לא-תקף כן נחסמת, גם כשכבר יש מושב-ישן-לא-תקף שמור

### 🔍 מה שנבדק בפועל
- טיול ב-`2-2`, הזנת מושב עם `E` — נדחה, לא נכתב ל-`personalLockerCards`
- טיול ב-`3-4-3`, הזנת מושב עם `K` — מתקבל ונשמר
- מספר שורה `50` — נדחה בכל תצורה (נבדק על `3-4-3`, שהאות שם כן הייתה תקפה)
- תרחיש שינוי-תצורה-אחרי-שמירה (התרחיש הקריטי) — מאומת בשני הכיוונים כמתואר למעלה
- עברית ואנגלית (כולל שתי הודעות השגיאה, עם רשימת האותיות התקפות בפועל בכל שפה), מצב בהיר, 375px
- `node --check` על שלושת בלוקי הסקריפט הקלאסיים. `node scripts/i18n-audit.js index.html` — 0 ממצאים. קוד בדיקה זמני (`window.__test`) הוסר ואומת שהוסר

נבדק בפועל: כל מה שלמעלה. סוגר את סדרת תצורת המושבים (commit א + ב). v4.43.0 -> v4.44.0 (minor — שדה קלט חדש עם ולידציה תלוית-הקשר).

## v4.43.0 — תצורת מושבים ניתנת לבחירה במפת ההושבה (index.html, commit א)

### ✨ תכונה חדשה
- **בורר תצורה חדש בפאנל האדמין ("תצורת מושבים במטוס")** — 5 תצורות: `2-2`, `3-3`, `2-4-2`, `3-3-3`, `3-4-3`. שדה חדש `trip.seatLayout`, ברירת מחדל `"3-3"` (בדיוק מה שהיה קשיח בקוד עד היום) לטיולים ישנים בלי השדה. אותו דפוס בדיוק כמו `checkinLink`/`whatsappGroupLink` — שדה שטוח ברמת הטיול, מוצג/נשמר רק לטיולי `abroad` (אותה הסתרה מותנית כמו שדה הצ'ק-אין, כי מפת ההושבה עצמה כבר מוצגת רק לטיסות)
- **`renderPlaneSeats()` הופכת פרמטרית — לא עוד `3-3` קשיח בשני מקומות נפרדים.** מערך העמודות הקשיח (`["A","B","C","AISLE","D","E","F"]`) ו-`grid-template-columns` הקשיח ב-CSS (`1fr 1fr 1fr 20px 1fr 1fr 1fr`) שניהם הוחלפו בחישוב יחיד מ-`seatLayout`, דרך פונקציה משותפת חדשה `parseSeatLayout()` — אותיות מוקצות ברצף אחד על פני כל הקבוצות (לא מתאפסות בכל קבוצה), מדלגות על I בלבד (A-H ואז J,K — טווח של 10, מכסה את `3-4-3` המקסימלית)
- **מושב שהוזן כדין אך לא תואם את התצורה הנוכחית — לא נעלם בשקט.** מוצג ברשימה נפרדת מתחת למפה (שם + מושב), במקום להיעלם בלי שום סימן שמשהו לא בסדר (ההתנהגות הקיימת עד היום לכל מושב לא-תואם, לא רק לתצורה שהשתנתה — נבדק ותועד לפני הכתיבה). תקף לפי שתי בדיקות: האות קיימת בתצורה הנוכחית, **וגם** מספר השורה בטווח 1-45 (טווח קבוע לכל 5 התצורות, לא שדה נוסף למארגן)

### 🔒 באג אמיתי שנתפס בבדיקה, לא בתכנון
- **מושב עם מספר שורה קיצוני (למשל "99A") ניפח את חלון השורות המוצג לעשרות שורות ריקות, וגם צויר כתפוס על המפה — בו-זמנית שהוא הופיע ברשימת "לא בתצורה".** הסיבה: זיהוי טווח השורות המוצג (`rowsToDisplay`, ±1 סביב שורות תפוסות) לא היה קשור בכלל לבדיקת "1-45" החדשה — שורה חריגה עדיין נכנסה לחישוב הטווח המוצג ועדיין נופלת בתוכו. התיקון: אותו סינון 1-45 מוחל **גם** על זיהוי טווח השורות המוצג, לא רק על בדיקת ה"תואם תצורה" — כך ששורה חריגה לעולם לא מצוירת (ומתאם עם רשימת "לא בתצורה" בעקביות, לא סותרת אותה)

### 🔍 מה שנבדק בפועל
- 5 התצורות: `parseSeatLayout()` הופק ידנית לכל אחת — `2-4-2`→8 אותיות (A-H בדיוק), `3-3-3`→9 (A-H+J), `3-4-3`→10 (A-H+J+K) — כולן תואמות את החלטת הטווח המורחב
- רינדור בפועל ב-`2-4-2`: 8 עמודות מושב + 2 מעברים (12 טורים בסה"כ), מספר השורה מופיע בשני המעברים (לא רק אחד) — כדי שלא יהיה צריך לסרוק עד קצה השורה כדי לדעת איזו שורה זו
- מושב עם אות לא-תקפה לתצורה, ומושב עם שורה מחוץ ל-1-45 — שניהם ברשימת "לא בתצורה", **אף אחד לא** מצויר גם על המפה (אומת אחרי התיקון לבאג שנמצא)
- מעבר בין תצורות ובין סוגי טיול (`abroad`↔`local`) — השדה מופיע/נעלם נכון, `seatLayout` מתאפס ל-`"3-3"` כשלא-abroad (מראה checkinLink)
- עברית ואנגלית (תווית הבורר, כותרת רשימת ה"לא בתצורה"), מצב בהיר, 375px
- `node --check` על שלושת בלוקי הסקריפט הקלאסיים ב-index.html. `node scripts/i18n-audit.js index.html` — 0 ממצאים. קוד בדיקה זמני (`window.__test`) הוסר ואומת שהוסר

**commit ב (ולידציית הזנת מושב בטופס) עדיין לא נכתב** — ממתין לתחילת עבודה נפרדת, לפי הסבב הבא.

נבדק בפועל: כל מה שלמעלה. v4.42.0 -> v4.43.0 (minor — תכונה חדשה, שינוי סכמה).

## v4.42.0 — הצעות AI למקומות (places.html, commit א)

### ✨ תכונה חדשה
- **כפתור "✨ הצע מקומות עם AI" חדש ב-`places.html`**, מעל רשימת המקומות. קורא ל-Cloud Function חדשה (`suggestPlaces`) עם יעד הרגל הפעילה (או הראשית, אם המסנן על "הכל"), `tripCharacter`, `vibes`, ושפה — עד 5 הצעות, כל אחת עם שם + עיר/שכונה + הנמקה קצרה (למה זה מתאים לאופי ולvibes של הטיול)
- **אין Google Places API — אין תשתית חדשה, אין מפתחות, אין billing נוסף.** נבדק במפורש לפני התכנון (`places-ai-investigation.md`): `places.html` היום שומר קישור מפה כטקסט חופשי בלבד שהמשתמש מדביק, בלי שום geocoding/חיפוש. הפיצ'ר הזה ממשיך באותה שיטה — Gemini מציע, לא מאמת
- **כל כרטיס הצעה נושא תווית שקיפות מפורשת: "⚠️ לא מאומת — בדקו במפות לפני שמירה".** לא חוסמת, רק שקיפות — כי אין Places API שיתפוס הזיה, המשתמש הוא שכבת האימות היחידה, וצריך שידע את זה
- **שני כפתורים גלויים בכל כרטיס, לא תפריט נסתר**: "🔍 חפש במפות" (קישור חדש, `https://www.google.com/maps/search/?api=1&query=<שם>,<עיר>` — לא Places API, רק URL עם `encodeURIComponent`, נפתח ב-`target="_blank"`) ו-"שמור הצעה זו" (פותח את טופס ההוספה הקיים עם שם+הנמקה ממולאים מראש, שדה קישור-המפה נשאר ריק לגבי המשתמש למלא אחרי שהוא בעצמו מצא ואימת)
- **הצעה שנשמרת הופכת למקום רגיל, בלי תג "AI" קבוע** — עוברת דרך אותו `savePlace()` בדיוק כמו הוספה ידנית, אין דגל "הוצע ע"י AI" בשום מקום בסכימה (גם לא תקדים לזה בציוד — נבדק)
- **`openPlaceForm(id, draft)` — הרחבת חתימה קטנה ועצמאית**: מקבלת עכשיו גם אובייקט טיוטה inline (לא רק `id` של מקום קיים ב-RTDB), כדי שהצעה שעוד לא נשמרה תוכל למלא את הטופס מראש
- **מצב ריק (0 מקומות בטיול) מוביל עכשיו לשני כיוונים גלויים, לא רק להוספה ידנית**: "➕ הוספה ידנית" ו-"✨ הצע מקומות עם AI" זה לצד זה, אותה שורת עזר חדשה ("שמרו מקום בעצמכם, או בקשו הצעות מ-AI"). **לא** תפריט/בחירה מוסתרת — נבדק במפורש ש-`.action-menu` הקיים (⋮) מיועד ל"secondary/rare actions" לפי התיעוד שלו ב-`shared.css`, לא לשתי פעולות שוות-משקל, אז לא נעשה בו שימוש כאן
- **כפתור הכניסה ל-AI בשורת הכלים מוסתר בדיוק כשמוצג המצב הריק הזה** — שני כפתורי "הצע מקומות עם AI" זהים, אחד מעל השני, היו כפילות מבלבלת. `requestPlaceSuggestions()` מקבלת עכשיו את הכפתור שנלחץ בפועל כפרמטר (ברירת מחדל: כפתור שורת הכלים), כך שמצב הטעינה ("חושב...") תמיד מופיע על הכפתור הנכון, לא על אחד מוסתר
- **הגנת פרופיל נוספה ל-`requestPlaceSuggestions()`** (כמו שכבר יש ל-`fabAction`) — נקודת הכניסה החדשה במצב הריק עוקפת את השער הקיים, ובלעדיה `savePlace()` היה יכול לקרוס על `profile.name` אם משתמש לוחץ על הצעת AI לפני שההרשמה האוטומטית הסתיימה

### 🔒 מה שנבדק/הוכרע לפני הכתיבה
- **הפרומפט מנוסח נגד הזיה, לא רק נגד סגנון** — בניגוד לשלושת סבבי הכיוונון של פרומפט הציוד (אורך/כפילויות/תיאורים, כולם עניין של פורמט), כאן ההוראה המרכזית היא עובדתית: "only suggest places you are confident actually exist," איסור מפורש על שיוך שם-לעיר שגוי, והעדפה מנוסחת בזהירות ("less commonly covered... but it's fine for one or two to be a well-known anchor") כדי לא לדחוף למקומות "נסתרים" מומצאים. `temperature` הורד ל-0.4 (לעומת 0.7 בציוד) כמנוף נוסף נגד הזיה, לא רק ניסוח
- **`finishReason === "MAX_TOKENS"` נבדק לפני `JSON.parse`, בדיוק כמו ב-`suggestPackingList`** — אותו באג שכבר עלה פעמיים בפרויקט הזה (תשובה חתוכה → JSON לא נפרסר → כשלון שקט). הועתק במפורש, לא "נשכח כי זו פונקציה חדשה"
- **מבנה תשובה שונה מהציוד בכוונה**: `{places:[{name,city,reason}]}` — מערך אובייקטים, לא מחרוזות שטוחות — כי צריך שלושה שדות לכל הצעה, לא שם בלבד
- **אין פיצול לפי רגל (`scope`)** — בניגוד לציוד, שם יש קריאה נפרדת לכל רגל תחת מסנן "הכל". כאן: קריאה בודדת תמיד, ליעד הרגל הפעילה (או הראשית). הוחלט כפישוט מכוון להיקף commit א — לא הוכרע כפיתרון קבוע
- **תיקון פריסה שנתפס בבדיקה חזותית, לא בתכנון**: כרטיסי ההצעה (עד 5, עם כפתורי פעולה בתחתית הכרטיס — בניגוד לכרטיסי מקום רגילים שהפעולות שלהם למעלה) התנגשו עם ה-FAB הצף — לא רק חזותית, אלא שכרטיסים 3-5 היו יכולים להיות בלתי-נגישים לגמרי, כי אזור ההצעות היה בלוק קבוע (`flex-shrink:0`) בלי גלילה משלו. התיקון: `#placesScroll` — עטיפה משותפת חדשה להצעות ולמקומות האמיתיים, עם אותה גלילה ואותו `padding-bottom` שכבר פותר בדיוק את הבעיה הזו לרשימת המקומות הרגילה. `renderPlaces()` ממשיכה לכתוב רק ל-`#placesList` (ילד פנימי), כך שהצעות לא נמחקות כשרשימת המקומות מתעדכנת

### 🔍 מה שנבדק בפועל
- זרימה מלאה עם `fetch` מדומה (אין Firebase/Gemini אמיתי בסביבה המקומית): לחיצה על הכפתור שולחת גוף בקשה נכון (`destination`/`tripCharacter`/`vibes`/`language`), מרנדרת את הכרטיסים עם כל השדות
- קישור "חפש במפות" נבדק ישירות (`href`/`target`/`rel`) — URL מקודד נכון, נפתח בטאב חדש
- "שמור הצעה זו" — הטופס נפתח עם שם+הנמקה (בשדה `note`) ממולאים, `mapUrl` ריק, `savePlace` מציג "שמירה" (לא "עדכון" — נשמר כמקום חדש, לא עריכה)
- הכרטיס שנשמר מוסר מרשימת ההצעות; הכרטיס האחרון שנשאר בעינו אחרי ביטול הטופס — "חפש במפות" (קישור פשוט, `target="_blank"`) לא נוגע ברשימת ההצעות בכלל, כמצופה
- מחיקת הכרטיס האחרון מסתירה את כל אזור ההצעות
- מצב ריק: שני הכפתורים מוצגים עם הטקסט הנכון; לחיצה על "הצע מקומות עם AI" בלי פרופיל מציגה טוסט ולא קוראת ל-`fetch` בכלל; אחרי שיש פרופיל — אותו כפתור נכנס למצב "חושב..." ונטען מחדש כשמסתיים, בלי לגעת בכפתור שורת הכלים (מוסתר באותו רגע); ברגע שיש מקום אחד אמיתי, כפתור שורת הכלים חוזר והמצב הריק נעלם
- תרחיש שגיאה (תשובה לא-ok עם `reason:"truncated"`) — הכפתור חוזר למצב רגיל (לא נשאר תקוע ב-"disabled")
- עברית ואנגלית (כולל התווית, שתי הכפתורים, הודעת האי-אימות), מצב בהיר, 375px — כולל גלילה עד הסוף לוודא שהכרטיס האחרון מתנקה מה-FAB
- `node --check` על `functions/index.js` ועל בלוק המודול של `places.html`. `node scripts/i18n-audit.js places.html` — 0 ממצאים. קוד בדיקה זמני (`window.__test`) הוסר ואומת שהוסר

נבדק בפועל: כל מה שלמעלה. commit ב (כיוונון פרומפט נוסף) מותנה בתוצאה בפרודקשן — לא נפתח כעת. v4.41.0 -> v4.42.0 (minor — תכונה חדשה).

## v4.41.0 — שיתוף/ביטול שיתוף חוצה-קטגוריות בבחירה מרובה (packing.html, commit ב)

### ✨ תכונה חדשה
- **כפתור "שיתוף"/"Share" חדש בשורת הפעולות של מצב הבחירה המרובה**, בין "סימון" ל"מחיקה" — שתי פעולות "היפוך בוליאני על הבחירה" (נארז/משותף) מקובצות יחד, מחיקה נשארת אחרונה
- **`bulkToggleShared()` חדשה, אותה תבנית בדיוק כמו `bulkToggleDone`**: קוראת כל פריט מהדלי של הקטגוריה שלו (לא `currentItems()` — כדי שבחירה חוצת-קטגוריות תחת מסנן "הכל" תמשיך לחשב נכון), מחשבת `allShared` (רוב), כותבת `updates` אחד ל-`update()` בודד. כפתור אחד, לא toggle-שני-כפתורים — **רוב-שקר מנצח**: אם לא כל הנבחרים משותפים, כולם הופכים משותפים; אם כולם כבר משותפים, כולם הופכים לפרטיים
- **תווית ניטרלית ("שיתוף"/"Share"), לא כיוונית ("שתף הכל")** — הכפתור לא תמיד משתף, לפעמים מבטל שיתוף (תלוי ברוב הבחירה), אז תווית שמבטיחה כיוון הייתה מטעה באותו אופן בדיוק כמו "סימון" (לא "סמן הכל כנארז")

### 🔒 מה שנבדק לפני הכתיבה
- **תוקן שוב אותו טעות הנחה כמו בסבב הקודם**: אין פונקציה בשם `toggleShareItem` בשום מקום בקוד — שיתוף פריט בודד הוא handler אנונימי מוטבע על `[data-toggle-visibility]`, בלי חתימה קיימת להרחיב. `bulkToggleShared` היא פונקציה עצמאית חדשה, לא הרחבה של משהו שלא קיים — לא נוגעת ב-handler הבודד בכלל
- **החלטה מפורשת: לא "שתף הכל"/"בטל שיתוף הכל" כשני כפתורים נפרדים** — נדחה לטובת כפתור-רוב יחיד, כמו `bulkToggleDone` הקיים. פחות מקום בשורה, אותו דפוס בדיוק שהמשתמש כבר מכיר מ"סימון"

### 🔍 מה שנבדק בפועל
- בחירת פריטים לא-משותפים משתי קטגוריות יחד (`main`+`gear`, תחת מסנן "הכל") → שיתוף → שניהם הופכים משותפים, כל אחד נכתב תחת נתיב הקטגוריה שלו
- תמהיל (משותף אחד + לא-משותף אחד) → הפעלה → רוב-שקר מנצח, שניהם הופכים משותפים
- הפעלה נוספת על בחירה שכולה כבר משותפת → כולם הופכים לפרטיים (ביטול שיתוף מרובה)
- `window.__lastBulkUpdates` (זמני, הוסר) אימת ישירות את אובייקט ה-`updates` שנשלח ל-`update()` בכל אחד משלושת המקרים למעלה — לא רק ש-`selectedIds`/`items` נראים נכון, אלא שהכתיבה שהייתה נשלחת בפועל תואמת את הרוב המחושב
- עברית ואנגלית (תווית הכפתור מתעדכנת בהחלפת שפה), מצב בהיר, 375px — שורת הפעולות עברה מ-5 ל-6 כפתורים; `.bulk-actions{overflow-x:auto}` הקיימת (לא שונתה) כבר מטפלת בזה, אומת: `scrollWidth(420px) > clientWidth(257px)` ב-375px, כפתור השיתוף נגיש בגלילה אופקית
- `node scripts/i18n-audit.js packing.html` — 0 ממצאים. קוד בדיקה זמני (`window.__test`, `window.__lastBulkUpdates`) הוסר ואומת שהוסר

נבדק בפועל: כל מה שלמעלה. v4.40.1 -> v4.41.0 (minor — תכונה חדשה בבחירה מרובה).

## v4.40.1 — צ'קבוקס כפול במצב בחירה מרובה (packing.html)

### 🐛 תיקון באג
- **מצב בחירה מרובה הציג שני צ'קבוקסים זהים בכל שורה במקום להחליף אחד באחר.** `.select-check` (בחירה) התווסף לצד `.check`/`.sub-progress` (נארז/תת-פריטים) הקיים, לא במקומו — ושניהם `24×24px` עם אותו `accent-color`, בלתי-ניתנים להבדיל ויזואלית. נתפס בבדיקה חזותית של commit א (v4.40.0) ולא בזמן הכתיבה שלו — הבדיקות המקוריות אימתו ש"אפשר לבחור ולסמן", לא ש"הצ'קבוקסים נבדלים"
- **התיקון: משבצת אחת, לא שתיים.** בזמן `selectionMode` היא מציגה `.select-check`; אחרת `.sub-progress` (לפריט עם תת-פריטים) או `.check` (נארז) בדיוק כמו לפני commit א. אף פעם לא שניהם יחד — זה עדכון `innerHTML` סינכרוני שלם ב-`renderMine()`, אין רגע ביניים שבו שני האלמנטים קיימים יחד ב-DOM
- **`.item.selection-mode{grid-template-columns:auto auto 1fr auto}` הוסרה** — עם משבצת אחת בלבד, הרשת של `.item` הרגילה (`auto 1fr auto`) מספיקה גם במצב בחירה; ה-override הישן היה מיועד לשתי משבצות שכבר לא קיימות

### 🔍 מה שנבדק בפועל
- פריט שכבר נארז (`isDone`), נכנסים לבחירה — עדיין נראה מעומעם עם קו חוצה (`.item.done{opacity:.64}` + `text-decoration`), למרות שהצ'קבוקס שלו הוחלף. נבדק ישירות דרך `getComputedStyle`, לא רק חזותית — מאשר שהוויתור על ראיית מצב "נארז" דרך הצ'קבוקס בזמן בחירה לא מאבד את המידע לגמרי, הוא עדיין גלוי דרך עיצוב השורה
- פריט עם תת-פריטים: `.sub-progress` נעלם בזמן בחירה בדיוק כמו `.check` הרגיל, מוחלף ב-`.select-check` יחיד
- כניסה ויציאה ממצב בחירה — כל שורה חוזרת בדיוק למשבצת המקורית שלה (`.check` או `.sub-progress`, תלוי אם יש תת-פריטים)
- עברית ואנגלית, מצב בהיר, 375px
- `node scripts/i18n-audit.js packing.html` — 0 ממצאים. קוד בדיקה זמני (`window.__test`) הוסר ואומת שהוסר

נבדק בפועל: כל מה שלמעלה. v4.40.0 -> v4.40.1 (patch — תיקון ויזואלי, לא תכונה חדשה).

## v4.40.0 — "בחר הכל" במצב בחירה מרובה (packing.html, commit א)

### ✨ תכונה חדשה
- **כפתור "בחר הכל" חדש, ראשון בשורת הפעולות של מצב הבחירה המרובה** (לפני "העברה"/"סימון"/"מחיקה"). מסמן את `currentDisplayedEntries()` — בדיוק מה שמוצג על המסך כרגע, כולל שני הפילטרים המצטברים (`activeList`+`activeLegFilter`) — לא "הכל בטיול" ולא רק מה שנגלל אליו (אין lazy-render בעמוד, `renderMine()` מזריק את כל הרשימה המסוננת בבת אחת)
- **סטטי, לא toggle** — לא הופך ל"נקה הכל" כשהכל כבר נבחר. כפתור "ביטול" הקיים כבר יוצא ממצב הבחירה כולו; כפתור שני למחיקת בחירה חלקית נשפט כמיותר
- **`currentDisplayedEntries()` חדשה, חולצה מתוך `renderMine()`** — מקור אמת יחיד לחישוב "מה מוצג", כדי ש"בחר הכל" לעולם לא יסטה מרשימה שנייה שמחושבת בנפרד
- **`updateBulkBar()` משתנה: הסרגל התחתון מוצג לאורך כל מצב הבחירה, לא רק כשמשהו כבר מסומן.** קודם הותנה גם ב-`selectedIds.size > 0` — מה שהיה מסתיר בדיוק את הכפתור שאמור לעזור להגיע למצב "יש בחירה", ממצב ההתחלה הכי שימושי שלו (0 נבחרו). שאר הכפתורים (מחיקה/סימון/העברה) כבר מוגנים בעצמם על בחירה ריקה — לא נדרש שינוי בהם

### 🔒 מה שנבדק לפני הכתיבה, ולא נכלל בכוונה
- **חקירה נפרדת קדמה לקוד** (`docs/roadmap.md` חלק ג׳ סעיף 9 פריט ו) — מיפתה ש"שיתוף לפי קטגוריה" (`setListVisibility`) הוא מנגנון נפרד לגמרי מבחירה מרובה, לא הרחבה שלה כפי שהונח בהתחלה
- **"שתף הכל"/"בטל שיתוף הכל" חוצה-קטגוריות נדחה במפורש להיקף עתידי (commit ב), לא בוצע כאן.** ההחלטה: אחרי ש"בחר הכל" יהיה בפרודקשן, לבדוק אם בחירה מרובה + שיתוף פר-פריט קיים כבר עונה בפועל על הבקשה המקורית, לפני שנפתח כפתור ייעודי נוסף — שאלה נפרדת מ-commit א, לא חלק ממנו

### 🔍 מה שנבדק בפועל
- בחירת הכל בתוך קטגוריה בודדת פעילה
- בחירת הכל תחת מסנן "הכל" — נבחר בדיוק מה שמוצג על המסך מכל הקטגוריות, לא "הכל בטיול" תיאורטית
- אותו מסנן "הכל" בשילוב מסנן יעד (`activeLegFilter`) פעיל בו-זמנית — פריט בלי `legId` נכלל תמיד, כמתועד ב-`docs/schema-legs.md §5.1`
- צורת `selectedIds` (Map מ-id ל-categoryId) נבדקה ישירות אחרי בחירת-הכל — זהה למה שבחירה פריט-פריט הייתה מייצרת, כך ש-`bulkDelete`/`bulkToggleDone`/`confirmBulkMove` (לא שונו) ממשיכות לפעול נכון בלי הבחנה בין שתי דרכי הבחירה
- עברית ואנגלית (תווית הכפתור מתעדכנת בהחלפת שפה), מצב בהיר, 375px — הסרגל התחתון מוצג מיד עם הכניסה למצב בחירה, גם עם 0 נבחרו
- `node scripts/i18n-audit.js packing.html` — 0 ממצאים. קוד בדיקה זמני (`window.__test`) הוסר ואומת שהוסר

נבדק בפועל: כל מה שלמעלה. v4.39.0 -> v4.40.0 (minor — תכונה חדשה בבחירה מרובה).

## v4.39.0 — קישור לקבוצת וואטסאפ (index.html, ד3 — האחרון בסדרת "מי בטיול")

### ✨ תכונה חדשה
- **שדה `whatsappGroupLink` חדש ברמת הטיול, אופציונלי לחלוטין.** נוסף בפאנל האדמין מיד אחרי שדה `checkinLink` (אותו מבנה `.admin-field` בדיוק), **בלי** ההגבלה של `checkinLink` לפי סוג טיול — קבוצת וואטסאפ רלוונטית לכל סוגי הטיול, לא רק לחו״ל
- **ולידציה קפדנית: `chat.whatsapp.com`/`www.chat.whatsapp.com` בלבד**, `http`/`https` שניהם, עם או בלי `www`. פרמטרים ו-hash אחרי הקוד לא נדחים — הקוד עצמו יושב ב-path, וזנב לא-מזיק (כמו `?utm_source=` שהתלווה מאיזשהו שיתוף) לא אמור לפסול קישור תקין. השדה נשמר **בדיוק כפי שהודבק**, כולל כל פרמטר — לא מנוקה/מנורמל
- **מכוון, ולא בטעות, שונה מ-`spliitUrl` הקיים** (מקבל כל URL תקין, `new URL()` גרידא): כפתור שמתיימר לפתוח "קבוצת וואטסאפ" ופותח בפועל דבר אחר (למשל `wa.me/<מספר>` — שיחה אישית, לא קבוצה) מטעה בצורה שה-spliitUrl הכללי שלו לא — הוא לא מבטיח יעד ספציפי. אימות ידני: קישור `wa.me` נדחה בהצלחה, קישור `chat.whatsapp.com` עם פרמטר `?src=...` נשמר בהצלחה כולל הפרמטר
- **ולידציה חוסמת עם הודעה ליד השדה** (לא `customAlert`, בניגוד ל-spliitUrl) — אותו עיקרון בדיוק כמו הטלפון ב-ד2
- **בחלונית "מי בטיול": כפתור "💬 הצטרפות לקבוצת הוואטסאפ" מתחת לרשימת השמות, מופרד בקו** (`border-top`) — לא עוד שורה באותה רשימה, פעולה אחרת. מופיע **רק** אם יש קישור — בלי קישור, שום דבר נוסף לא נרנדר (אומת ב-DOM: `innerHTML` ריק לגמרי, לא רק `hidden`)

### 🔒 מה שנבדק לפני הכתיבה
- **spliitUrl אותר ונבדק כתקדים לא-לחיקוי בוולידציה**: משתמש ב-`new URL()` גרידא + `customAlert`, ללא בדיקת דומיין. **לא** אותה תבנית UI כמו `checkinLink` בכלל — `spliitUrl` הוא שער מסך-מלא עצמאי (`#spliitGate`) שנפתח בהקשר (בפעם הראשונה שניגשים ל"הוצאות" בלי קישור מוגדר), לא שדה בפאנל האדמין. אושר מפורשות: לחקות את `checkinLink` במיקום ובמבנה (שדה שטוח ברמת הטיול, אותה קריאת `update()`), לא את spliitUrl בכלל
- **הרשאת כתיבה**: אין קובץ rules בריפו (כרגיל — מנוהל רק בקונסולת Firebase). מה שכן אומת: `openAdminPanelIfAllowed()` חוסם את **כל** פאנל האדמין מאחורי `requireOrganizerAccess()` — חבר רגיל לא רק לא יכול לשמור, הוא לא יכול אפילו לפתוח את הפאנל ולראות את השדה. אומת בדפדפן החי: קריאה ל-`openAdminPanelIfAllowed()` עם `currentUser` שאינו `tripOwnerId` לא פותחת את `#adminModal` בכלל (`adminModalOpen: false`)
- `whatsappGroupLink` מצטרף לאותו אובייקט `settings` שכבר נכתב היום עם `checkinLink`/`tripType` — לא נתיב כתיבה חדש שדורש כיסוי חדש בכלל ה-RTDB, אלא שדה נוסף בכתיבה שכבר קיימת ועובדת

נבדק בפועל: כל הפורמטים (http/https, עם/בלי www, עם פרמטרים/hash) נבדקו מבודד ב-Node לפני הכתיבה. בדפדפן החי: השדה מופיע בדיוק אחרי checkinLink בפאנל; `wa.me` נדחה עם הודעה ליד השדה; קישור תקין עם פרמטר נשמר במלואו (`db.update` מדומה, כי אין חיבור Firebase אמיתי בסביבה המקומית); כפתור הקבוצה מופיע בחלונית מופרד בקו, נעלם לגמרי (לא `hidden` בלבד — `innerHTML` ריק) כשאין קישור; חבר לא-מארגן לא מגיע לפאנל בכלל. עברית ואנגלית, 375px. `node --check`, `node scripts/i18n-audit.js index.html` — 0 ממצאים. v4.38.0 -> v4.39.0 (minor — שדה חדש).

## v4.38.0 — מספר טלפון וכפתור וואטסאפ (index.html, ד2)

### ✨ תכונה חדשה
- **שדה `phone` חדש ב-`memberProfiles`, אופציונלי לחלוטין.** נכתב רק דרך המודאל החדש "הפרטים שלי" — לא נוגע ב-`participantKey` (נשאר כפי שנקבע ב-auto-register, בדיוק כמו קודם). הכלל ב-RTDB אומת ישירות בקונסולת Firebase (לא בריפו — ר׳ v4.37.0) שמרשה את השדה
- **נורמליזציה בלי ניחוש קוד מדינה**: `050-1234567`/`0501234567`/`+972501234567`/`972501234567` — כולם מתכנסים לאותו `972501234567`. מספר בלי `0`/`+`/`00`/`972` בהתחלה נדחה, לא מנוחש כאילו כבר יש בו קוד מדינה — ה-placeholder של השדה מרמז במפורש שצריך קוד מדינה מחוץ לישראל
- **ולידציה חוסמת, עם הודעה ליד השדה — לא טוסט.** מספר לא תקין לא נשמר בכלל; ה-`wa.me` שהיה נוצר ממנו היה שולח הודעה לזר במקרה הטוב
- **כפתור "💬 שיחה" בחלונית "מי בטיול", לכל חבר שהוסיף מספר — חוץ מהמשתמש עצמו** (לא הגיוני כפתור "שיחה איתי"). `https://wa.me/<מספר מנורמל>`, `target="_blank" rel="noopener"` — אותו תבנית בדיוק כמו `adminShareWhatsappBtn` הקיים כבר בעמוד
- **מי שלא הוסיף מספר לא רואה כלום נוסף בשורה שלו** — לא כפתור מנוטרל, לא "לא הוסיף", לא מקום שמור ריק. אומת ישירות ב-DOM (לא רק חזותית): לחבר בלי מספר `hasAction === false`, אין אלמנט `.btn-sm-action` בשורה שלו בכלל, וגובה השורה כמעט זהה לשורה עם כפתור (58px מול 60px — לא רווח מיוחד)
- **המשתמש עצמו, אם עוד אין לו מספר, רואה "➕ הוספת מספר" בשורה שלו בחלונית** — נקודת כניסה שנייה, מובילה לאותה חלונית עריכה בדיוק כמו העיפרון. אומת: לחיצה סוגרת את חלונית החברים ופותחת את חלונית הפרטים
- **הסרת מספר בתוך חלונית העריכה** — כפתור "הסרת המספר" מרוקן את השדה ומסתתר; ההסרה בפועל קורית רק בלחיצת "שמירה" (פעולת שמירה אחת מפורשת, לא שני מנגנוני-שמירה נפרדים בטופס אחד)

### 🔒 שינוי התנהגות מכוון — עריכת השם עברה מ-inline לחלונית קטנה
- **הסימולציה שביקש המשתמש הכריעה**: הוזרק שדה טלפון שני, בפועל, לתוך `.session-row` הקיימת לצד שדה עריכת השם — שני השדות נחתכו לכמה תווים (`"נתנא"`, `"050-"`), לא שימושי. טקסט ההסבר ("יוצג לחברי הטיול") גם לא יכול להיכנס לשורה חד-שורתית מבנית, לא רק מבחינת צפיפות
- **הפתרון**: העיפרון פותח עכשיו מודאל קטן ("הפרטים שלי", `openSheet`/`closeSheet` הקיימים — אותה תשתית כמו `#lockerModal`/`#membersModal`, בלי מנגנון חדש), עם שם וטלפון יחד. `beginEditName`/`endEditName`/`saveDisplayName` והמנגנון ה-inline הישן (`#editNameInput`) **הוסרו לגמרי**, לא הושארו לצד החדש — שום קורא אחר לא נשאר תלוי בהם (נבדק בריפו כולו)
- **נבדק בפועל שהמעבר לא מרגיש כבד יותר**: אותה פוקוס+סימון אוטומטי על שדה השם בפתיחה (כמו קודם), אותו Enter-לשמירה. ההבדל היחיד למקרה "רק לתקן טעות בשם" — Save מפורש במקום blur-לשמירה, ואנימציית פתיחה (300ms, כבר קיימת בכל שאר המודאלים באפליקציה). זו בדיוק רמת המשקל של `lockerModal`/`adminModal` הקיימים — האפליקציה כבר "מלמדת" את המשתמש את הדפוס הזה בכל מקום אחר; ה-inline הישן היה החריג היחיד, לא ההפך

### 🔍 מה שנבדק בפועל, לא רק תיאורטית
- כל 4 הפורמטים מהדוח (`050-1234567`/`0501234567`/`+972501234567`/`972501234567`) מתכנסים לאותה תוצאה — נבדק ב-Node מבודד, לא רק בקוד
- קלט לא תקין (`"abc"`) נבדק בדפדפן החי: הודעת שגיאה מופיעה ליד השדה, ה-`fetch`/שמירה לא מתבצעים
- שמירה מוצלחת נבדקה עד הסוף (עם `db.update` מדומה, כי אין חיבור Firebase אמיתי בסביבה המקומית — מגבלה מתועדת): `currentProfile.phone` מתעדכן, המודאל נסגר
- הסרת מספר נבדקה קצה-לקצה: לחיצת "הסרה" → שדה מתרוקן → שמירה → `currentProfile.phone === null`
- קישור ה-`wa.me` נבדק ישירות ב-DOM (`href`/`target`/`rel`), לא רק חזותית: `https://wa.me/972521112222`, `target="_blank"`, `rel="noopener"`
- מצב בהיר וכהה, עברית ואנגלית (כולל `placeholder`/`aria-label` שמתעדכנים בהחלפת שפה, לא רק טקסט קבוע), 375px
- `node --check` על שלושת בלוקי הסקריפט הקלאסיים. `node scripts/i18n-audit.js index.html` — 0 ממצאים
- גרפ מלא על הריפו: אין עוד קורא ל-`beginEditName`/`endEditName`/`saveDisplayName`/`editNameInput` בשום מקום

נבדק בפועל: כל מה שלמעלה. v4.37.0 -> v4.38.0 (minor — שדה חדש בסכמה).

## v4.37.0 — חלונית "מי בטיול" (index.html, ד1)

### ✨ תכונה חדשה
- **אייקון 👥 חדש ב-`.session-row`, לחיץ, פותח חלונית עם שמות ותמונות פרופיל של כל מי שהצטרף בפועל** (`memberProfiles`, לא `participants` — אין התאמה בין השניים מאז v4.30.0). מוצג תמיד למשתמש מחובר, גם בטיול עם חבר יחיד (מציג את המשתמש עצמו) — **בכוונה לא מוסתר** גם כשאין עוד חברים: הסתרה הייתה דורשת מאזין חי כדי להופיע כשמישהו מצטרף, סיבוך בלי תמורה אמיתית מול מקרה קצה
- **"יציאה מהחשבון" קוצר ל"יציאה"** (עברית בלבד — "Sign out" האנגלי כבר היה קצר) כדי לפנות מקום לאייקון החדש בשורה
- **נפילה ל-ראשי תיבות כשאין `photoURL`, או כשהתמונה נכשלת בטעינה בפועל** (`onerror`) — לא רק חסר, גם שבור. שתי מילים ומעלה: אות ראשונה של המילה הראשונה + אות ראשונה של האחרונה ("נתנאל לוי" → "נל", "Netanel Levy" → "NL"). מילה בודדת (לא לכולם יש רווח בשם התצוגה): שני התווים הראשונים שלה, לא אות אחת שנראית כמו טעות הקלדה
- **רינדור חד-פעמי בפתיחה, לא מאזין חי** — `memberProfiles` עצמו כבר "חי" (מגיע מה-`onValue` הראשי של הטיול, בדיוק כמו `renderLinkedMembers()` בפאנל האדמין), אבל החלונית לא נרשמת לעדכונים תוך כדי שהיא פתוחה. החלטה מכוונת: תוכן שמתעדכן מתחת לקורא באמצע קריאה הוא הפרעה, לא תכונה

### 🔒 מה שלא נכלל בכוונה, ל-ד2/ד3
- מספר טלפון וכפתור שיחה בוואטסאפ — ד2
- קישור לקבוצת וואטסאפ (שדה חדש ברמת הטיול, ליד `checkinLink`) — ד3. נבדק: `checkinLink` עצמו לא ניתן להעתקה ישירה — יש לו לוגיקה ספציפית משלו (מתאפס ל-`""` עבור כל `tripType` שאינו `abroad`) שלא רלוונטית לקישור קבוצתי

### 🔍 מה שנבדק לפני הכתיבה — ותיקון לשלוש הנחות שגויות
- **שאלה 1 (הכלל ב-RTDB): לא ניתן לאימות מהריפו.** אין קובץ `.rules` בשום מקום בפרויקט, ו-`firebase.json` אפילו לא מגדיר יעד `database` — כללי האבטחה של ה-RTDB לא נמצאים תחת בקרת גרסה בכלל, רק בקונסולת Firebase. **המשתמש אימת ישירות בקונסולה** שהכלל מרשה `phone` לפני שהעבודה התחילה — לא נבדק כאן, לא ניתן היה להיבדק כאן
- **שאלה 3 ("`buildModal` קיים"): לא נמצא תחת השם הזה בשום מקום בריפו** (נבדק גם מחוץ ל-`index.html`). הדפוס הקיים בפועל — ותוקן להשתמש בו — הוא `openSheet()`/`closeSheet()` + `.modal-overlay`/`.bottom-sheet`, עם `#lockerModal` כתבנית: `window.openXModal = () => { openSheet("xModal"); renderX(); }`. אין קוד חדש שהומצא; זה בדיוק הדפוס שכבר קיים
- **שאלה 6, ניסוח מתוקן**: הדוח הקודם *אישר* שאפשר לבנות מאזין חי (`renderLinkedMembers()` כבר עושה את זה), **לא המליץ** נגד זה — ההחלטה "רענון בסגירה/פתיחה, לא מאזין חי" והנימוק (מודאל שמתעדכן מאחורי מודאל אחר הוא הפרעה) הם של המשתמש, לא שחזור של המלצה קודמת. יושמו כמבוקש, בלי מאזין
- **מדידת המקום בשורה, לא הערכה**: נטען העמוד בפועל ב-375px עם שם בדיקה מכוון-קיצוני (35 תווים). מדידה: "יציאה מהחשבון"→"יציאה" משחרר **54.5px** (117px→62px) — לא 79px שצוינו בסבב הבא (המספרים לא תואמים; לא נבדק מחדש כי שניהם תומכים במסקנה "מספיק ונוח", לא שינו החלטה). עם האייקון החדש בפועל (לא סימולציה) ועם השם הארוך: שורה נשארה חד-שורתית (32px), בלי גלישה

נבדק בפועל: כל 6 שילובי photoURL (תקין/חסר/שבור) × שם (רב-מילים עברית/אנגלית/מילה בודדת/ריק/ארוך-40-תו) נבדקו בדפדפן על הדף החי — לא סימולציה. `onerror` אומת ישירות ב-DOM (`img.style.display`/`fallback.style.display` נבדקו בפועל, לא רק חזותית). מצב כהה ובהיר, עברית ואנגלית (כולל `aria-label` באנגלית). `node --check` על שלושת בלוקי הסקריפט הקלאסיים ב-index.html. `node scripts/i18n-audit.js index.html` — 0 ממצאים. v4.36.0 -> v4.37.0 (minor — תכונה חדשה).

## v4.36.0 — הרחבת רשימת הקטגוריות בקניות: 8 ← 10, 122 ← 413 מילות מפתח (shopping.html)

### ✨ תכונה חדשה
- **`CATEGORY_DICTIONARY` גדלה מ-122 ל-413 מילות מפתח, ומ-8 ל-10 קטגוריות.** שתי קטגוריות חדשות: **קפואים 🧊** (11 מילים — מוצהרת **ראשונה**, בכוונה: "בסופר הולכים למקפיא, לא למדף הירקות") ו-**מזווה 🧂** (64 מילים — אורז/פסטה/קטניות/קמח/סוכר/שמן/רטבים/תבלינים/שימורים, לא הייתה להם קודם בית). שאר 361 המילים החדשות התמזגו לתוך 8 הקטגוריות הקיימות (`cleaning`+`toiletries` שתיהן ל-פארם וניקיון)
- **כלל הכרעה חדש בשוויון ספציפיות: ההתאמה שמתחילה קודם במחרוזת מנצחת — לא סדר הצהרת הקטגוריה.** נמצאו בפועל שני מקרים שבהם "בורקס גבינה" ו"קמח לבן" נחתו ב"חלב וביצים" כי המילה המתחרה (גבינה/לבן) הייתה באותו אורך בדיוק (5/3 תווים) והקטגוריה שלה מוצהרת מוקדם יותר. **בעברית שם העצם (מה שהמוצר בעצם הוא) קודם לתיאור** (טעם/צבע) — בורקס גבינה, קמח לבן, שמן זית, חזה עוף. העדפת ההתאמה המוקדמת = העדפת שם העצם על פני התיאור
- **חריג אחד לכלל החדש: קפואים מנצחת שוויון עוד לפני שההתאמה-המוקדמת נבדקת.** בניגוד ל"בורקس גبينة", הצירוף "X קפוא/קפואה/קפואים" תמיד שם את מילת המצב **אחרי** שם העצם ("תירס קפוא") — כלל ההתאמה-המוקדמת לבדו היה נותן לתירס (ירקות) לנצח תמיד, וקפואים לא הייתה זוכה אף פעם למרות שהוצהרה ראשונה בכוונה. אומת בפועל: "תירס קפוא" נחת ב"פירות וירקות" לפני שהחריג נוסף
- **"תה" הוסרה** (2 תווים, בלתי-ניתנת-לשיפור — אין דרך להאריך אותה שתישאר "תה"). הוחלפה ב-**"תה ירוק"**, **"תה נענע"**, **"שקיות תה"** — ארוכות מספיק, בטוחות. מי שיכתוב "תה" לבד ייפול ל"מוצרים נוספים" — מקובל, ידוע ומכוון
- **"שקיות" נוספה** לכלי בית וציוד

### 🔒 תיקון בטבלת המיפוי, לפני שנכנס לקוד
- הטבלה שליוותה את רשימת המילים סימנה "מזווה" כקטגוריה **קיימת** למיזוג — שגוי. אין קטגוריה קיימת לתבלינים/קטניות/שמן. תוקן מראש: מזווה היא קטגוריה **חדשה**, באותה רמה כמו קפואים. כתוצאה מזה 10 קטגוריות סופיות, לא 11 כפי שצוין במקור — ה"11" התייחס ל-11 המדורים בקובץ המילים, לא ל-11 קטגוריות סופיות

### 🔍 מה שנבדק לפני הכתיבה
- **9 כפילויות מדויקות בין הרשימה החדשה לקיימת נמצאו** (לא 2, לא 5 — שני מספרים קודמים שהתבררו כלא-מדויקים באותה בדיקה) — כולן "הרשימה החדשה מנצחת, הסדר מכריע", כמאושר: גלידה/ארטיק/קרח→קפואים, עוגיות→מאפה ודגנים, נייר סופג→פארם וניקיון, אורז/פסטה/שיבולת שועל/קורנפלקס→מזווה
- **כלל ההתאמה-המוקדמת וחריג הקפואים נבדקו נגד 636 בדיקות** (122 מילות המפתח המקוריות + 46 כינויי `SYNONYMS_MAP` + 107 שמות מוצר ידועים + כל 361 המילים החדשות) — **אפס שינוי** מעבר למה שדירוג-הספציפיות כבר הפיק. שני הכללים החדשים קיימים במפורש למקרים **שלא** בקורפוס הזה כלל — "קמח לבן"/"בורקס גבינה"/"תירס קפוא" הם ניסוח ריאליסטי, לא ערך מילון/פריט ידוע מילולי
- **50 פריטים ריאליסטיים (לא מתוך אף אחת מהרשימות, כולל כמה שלא מכוסים בכלל) הורצו פעמיים** — פעם לפני כלל ההתאמה-המוקדמת (חשף את "תירس קפוא" כנסיגה), ופעם אחרי התיקון (0/50 נסיגות נוספות). 5/50 נפלו ל"כללי" — 2 כצפוי (סושי, מחברת — לא קניות/לא מכוסה בכוונה), 3 חשפו פערים ידועים ולא-תוקנו בקומיט הזה: מצב סמיכות בעברית ("גלידת וניל" — "גלידת" לא מתחילה ב"גלידה", ה/ת בסוף המילה משתנה, לא רק סיומת נוספת), התאמה לא-רציפה ("מזון יבש לכלב" — מילה שהוכנסה בין שתי מילות מילת מפתח דו-מילית שוברת את ההתאמה), וכיסוי יחיד/רבים חלקי ("ויטמין C" מול מילת המפתח "ויטמינים" בלבד)
- כל שלושת "הבאגים" שדווחו בסבב קודם (קפה נמס→קפואים, נייר טואלט/שמפו לילדים לא נתפסים) **לא שוחזרו** — הוסק שהם השערות מזיכרון, לא ממצאים ממשיים (המדווח אישר זאת)

נבדק בפועל: syntax נבדק (`node --check` על ה-module script שחולץ), `node scripts/i18n-audit.js shopping.html` — 3 ממצאים קיימים מראש, לא קשורים (כפתורי שפת ייצוא). הפונקציה החיה שחולצה מהקובץ (לא שחזור שלה) נבדקה ישירות מול 275 הבדיקות הקיימות (השוואת HEAD הקודם מול העריכה) — 33 שינויים, כולם מוסברים ומכוונים (תה, מעברי קפואים/מזווה, פריטי מזווה חדשים). v4.35.8 -> v4.36.0 (minor — תכונה חדשה/שינוי סכמה, לא רק תיקון).

## v4.35.8 — זיהוי הקטגוריה בקניות: התאמת התחלת-מילה + ההתאמה הכי ספציפית מנצחת (shopping.html)

### 🐛 תיקון באג — מילת מפתח קצרה בולעת מילה לא קשורה, וסדר הקטגוריות מכריע שרירותית
- **מקור הבאג המקורי שדווח**: פריט "אפרסקים" נחת ב"מוצרים נוספים 🛒". לא באג ברשימה — "אפרסק"/"אפרסקים" פשוט לא היו ברשימה מעולם (נבדק מול היסטוריית git). החקירה שהובילה מזה חשפה שתי בעיות אמיתיות נפרדות ב-`detectCategory()`
- **בעיה 1 — הכלה חופשית (`includes()`) תופסת גם באמצע/בסוף מילה**: "שום" תופס גם "שומשום", "תה" תופס גם "תהילה". וקטור מסוכן יותר שאומת: מילת מפתח היפותטית שמסתיימת בצליל נפוץ (כמו "ים") הייתה תופסת בטעות כל מילה שמזדמן שמסתיימת באותה צורה (למשל "ימים" — ימים, לא משקה)
- **בעיה 2 — גם אחרי שמתקנים את #1, סדר הקטגוריות עדיין מכריע שרירותית**: אם בעתיד תתווסף מילת מפתח קצרה לקטגוריה שמוצהרת מוקדם (כמו "פירות וירקות", ראשונה ברשימה) שמזדמן להיות התחלה של מילה ארוכה יותר, היא תנצח מילת מפתח ארוכה וספציפית יותר בקטגוריה שמוצהרת מאוחר יותר (כמו "פירות ים" בבשר/דגים) — **גם אם ההתאמה הארוכה נכונה יותר**. אומת עם תרחיש סינתטי: מילת מפתח בת-3-תווים שמתחילה כמו "פירות" בקטגוריית ירקות, מול מילת מפתח דו-מילית "פירות ים" בקטגוריית בשר/דגים — לפי סדר, ירקות מנצחת תמיד, גם כשבשר/דגים היא ההתאמה הנכונה
- **הפתרון לשתיהן**:
  1. פיצול השם לאסימונים **לפני** הנרמול (כל אסימון מנורמל בנפרד), עם שני מסלולי בדיקה: מילת מפתח בת-מילה-אחת (108 מתוך 122) — האם איזשהו אסימון בפריט מתחיל בה; מילת מפתח מרובת-מילים (14 מתוך 122, כמו "תפוח אדמה") — רצף אסימונים רציף בפריט שבו כל אסימון מתחיל במקביל שלו במילת המפתח, לא רק המילה האחרונה (נדרש בגלל סמיכות בעברית — ב"תפוחי אדמה" המילה **הראשונה** היא זו שמוטה)
  2. **ההתאמה הכי ספציפית מנצחת, על פני כל הקטגוריות יחד — לא הקטגוריה הראשונה שמוצהרת עם התאמה כלשהי.** ספציפיות = (מספר מילים במילת המפתח, ואז אורך המחרוזת) — מילת מפתח דו-מילית תמיד מנצחת חד-מילית; בין שתי חד-מיליות, הארוכה יותר מנצחת. תיקו (ספציפיות זהה) נופל חזרה לסדר ההצהרה, בדיוק כמו היום
- **נשמרות צורות ריבוי בעברית שעובדות היום**: ריבוי בעברית נוצר בהדבקת סיומת (`עגבניה`→`עגבניות`), לא מילה נפרדת — לכן `startsWith` שומר על ההתאמה, בניגוד להתאמת-מילה-שלמה-מדויקת (שנבדקה ונדחתה בנפרד — שוברת 7 צורות ריבוי אמיתיות, ראו docs/roadmap.md)
- **תופעת לוואי מבורכת, בלי טיפול מיוחד**: קולז'יית `חלב`/`חלבון` (מ-v4.35.6/7) נפתרת מעצמה — "חלבון" (5 תווים) מנצחת "חלב" (3 תווים) בזכות הספציפיות שלה, אז חטיפי חלבון נוחתים נכון בחטיפים ומתוקים, לא בחלב וביצים

### 🔒 מה שנשאר פתוח, בכוונה, מתועד בהערה בקוד
- **`תה`/`תהילה`**: "תה" (2 תווים) אכן מתחילה בתוך "תהילה" (שם, לא מוצר) — נשאר כך. אין מתחרה ספציפי יותר שהיה פותר את זה, וההסרה של "תה" עצמה (מוצר קניות לגיטימי ונפוץ) תעלה יותר משהיא שווה
- **`מים`/`שמים` — נבדק ונשלל כלל כסיכון**: `"שמים".startsWith("מים")` הוא `false` (ם/ה בעמדה הראשונה שונים) — הצמד הזה מעולם לא היה קולז'יה אמיתית, גם לפני התיקון
- **הכפילות `ספוג`** (פארם וניקיון וגם כלי בית וציוד) — שתי מילות מפתח זהות, ספציפיות זהה, מוכרעת עדיין לפי סדר הצהרה. לא טופלה כאן — החלטת תוכן, לא באג באלגוריתם

### 🔍 מה שנבדק לפני הכתיבה
- **נבדק בפועל, לא הונח, מול הפונקציה החיה עצמה (לא שחזור)**: כל 122 מילות המפתח, כל 46 הכינויים של `SYNONYMS_MAP`, כל 107 שמות המוצרים הידועים של `TRANSLATION_DICTIONARY.items` — לפני ואחרי. **8 שינויים מתוך 275 בדיקות, כולם מוסברים ע"י פתרון חלב/חלבון** — 2 מתוכם (משקאות "חלב" צמחיים כמו חלב שקדים/שיבולת שועל, שעוברים לחטיפים/מאפה) הם החלטת טעם ניתנת לוויכוח, לא באג; השאר תיקון נכון. אפס נסיגות אחרות
- `scripts/keyword-conflicts.js` עודכן עם `--match-mode=prefix` — מציג 6 קולז'יות שנותרות מתוך 7 המקוריות (`תות`/`פיתות` נעלמת, כי `תות` מעולם לא הייתה prefix אמיתי של `פיתות` — רק תת-מחרוזת באמצע). המספר "19" שהוזכר בדיון מתייחס למילות המפתח בנות 3 תווים בדיוק — נבדק ואומת, לא ניחוש

נבדק בפועל: 275 בדיקות רגרסיה (122+46+107) מול הפונקציה החיה שחולצה מ-shopping.html, כולל תרחיש סינתטי מוכח (לא רק תיאורטי) להוכחת פתרון בעיית סדר-הקטגוריות. `node --check` על ה-module script, `node scripts/i18n-audit.js shopping.html` — 3 ממצאים קיימים מראש, לא קשורים לשינוי הזה (כפתורי שפת ייצוא). v4.35.7 -> v4.35.8 (patch — תיקון באג בזיהוי קטגוריה).

## v4.35.7 — שלושה תיקונים בטאב המוזיקה (places.html)

### ✨ תכונה חדשה
- **צ׳יפי הסינון (מעל הרשימה — "הכל · Spotify · YouTube · …") צבועים לפי מקור — רק כשהם נבחרים.** לא נבחר נשאר אפור כמו היום; נבחר עובר לצבע המותג. הסינון הוא בחירה יחידה, כך שלכל היותר צ׳יפ אחד צבוע בכל רגע — לא שלושה-ארבעה צ׳יפים צבעוניים על המסך בו-זמנית. "הכל" לא מקבל `data-source` בכלל ונשאר עם ההדגשה הרגילה. ה-CSS מוגבל ל-`#musicFilters .filter-chip.active[data-source]` — צ׳יפי הסינון בטאב "מקומות" (`#placeFilters`) חולקים את אותה מחלקת `.filter-chip` ונשארים בלי שינוי, כי הם לעולם לא מקבלים `data-source`
- **הוסר הרקע האפור מאחורי אייקון/עיגול המקור בכרטיס השיר.** `.place-icon` משותף בפועל בין טאב "מקומות" (אייקון קטגוריה) לטאב "מוזיקה" (אייקון מקור) — לא הוגדר בשום עמוד אחר. השינוי הוגבל ל-`.music-card .place-icon{background:transparent}`, כך שטאב "מקומות" לא נגע כלל
- **תג/צ׳יפ המקור עברו לטקסט לבן על גוון מותג כהה יותר, במקום טקסט שחור על הגוון הבהיר המקורי (v4.35.6).** ראו "תוקן" למטה — זו לא רק בחירת צבע חדשה, גם תיקון מדידה

### 🤔 ההתנגשות עם --glow-color, ואיך נפתרה
- **הבעיה**: `.filter-chip.active` כבר מקבל `box-shadow` עם `--glow-color` במצב כהה (הילה בצבע ערכת הטיול). אם ההילה נשארת גם על צ׳יפ שצבוע עכשיו בצבע מותג, מתקבלת הילה בצבע אחד סביב מילוי בצבע אחר לגמרי — למשל הילה כתומה (ערכת "בארץ") סביב צ׳יפ Spotify ירוק, או סביב צ׳יפ YouTube אדום
- **המדידה, לא ההנחה**: נבדקו בפועל בדפדפן שני המצבים זה לצד זה (Spotify ירוק ו-YouTube אדום, שניהם עם הילה כתומה, בערכת "בארץ" הכהה) — ההילה נראתה בבירור **מלוכלכת**, בדיוק כמו שהוערך מראש, וברורה יותר על אדום מאשר על ירוק
- **הפתרון**: `#musicFilters .filter-chip.active[data-source]{box-shadow:none}` — ההילה מוסרת רק מצ׳יפים שצבועים לפי מקור; צ׳יפ "הכל" הפעיל (בלי `data-source`) שומר על ההתנהגות הרגילה במלואה, הילה כלולה. הצבע עצמו — לא ההילה — הוא מה שמסמן "זה הסינון שנבחר"

### 🐛 תוקן — טקסט לבן לא עבר על יוטיוב באדום המקורי
- כל ארבעת הצבעים נמדדו בפועל (יחס ניגודיות WCAG, לא הונח) מול טקסט **לבן**, לא שחור כמו ב-v4.35.6:
  - Spotify `#1DB954` → לבן: **2.59:1**. נכשל
  - YouTube `#FF0000` → לבן: **4.00:1**. נכשל — **בניגוד להנחה שהאדום המקורי כבר עובד עם לבן**. נמדד, לא הונח, ונמצא שגם הוא זקוק להכהיה קלה
  - Apple Music `#FA243C` → לבן: **3.90:1**. נכשל
  - SoundCloud `#FF5500` → לבן: **3.21:1**. נכשל
- כל הארבעה הוכהו (באותו גוון, פחות בהירות ב-HSL) עד למינימום 4.5:1 + מרווח ביטחון (לא בדיוק על הסף):
  - Spotify → **`#15803d`** (5.02:1)
  - YouTube → **`#e60000`** (4.81:1)
  - Apple Music → **`#e2001a`** (4.94:1)
  - SoundCloud → **`#cc4400`** (4.78:1)
- זהות המותג נשמרה בכוונה — אותו גוון, רק כהה יותר, לא צבע אחר. אותם ארבעה `hex` משמשים גם את התג בכרטיס וגם את צ׳יפ הסינון (לא פעיל), כך שהם תמיד עקביים זה עם זה

### 🔒 מה שלא נגעו בו, בכוונה
- `--glow-color` וערכות הצבע (§3.4) עדיין לא נגעו בהם — כל הצבעים החדשים `hex` קבוע, בלי תלות במשתנה עיצוב. ההילה עצמה (לא הצבע) הוסרה רק כשהצ׳יפ גם פעיל וגם צבוע — ראו הפתרון להתנגשות למעלה
- טאב "מקומות" (`#placeFilters`, `.place-card .place-icon`) לא נגע — שלושת התיקונים (סינון, רקע אייקון, הילה) מוגבלים ב-CSS לטאב המוזיקה בלבד
- **תגיות המקור בתוך כרטיס השיר עצמו (`.tag[data-source]`, v4.35.6) לא השתנו בכיוון הצביעה** — הן לא מתחרות עם שום "מסומן/לא מסומן", אז נשארות צבועות תמיד, רק עם הטקסט הלבן/הצבעים המתוקנים

נבדק בפועל: הוזרקו 5 שירים מדומים (אחד לכל מקור + אחד לא מזוהה) דרך hook זמני, ונבדקו ישירות ב-DOM (`outerHTML` של `#musicFilters`, לא רק חזותית) — `data-source` נכון על כל צ׳יפ חוץ מ"הכל"/"קישור". צילומי מסך של **שני המצבים** (עם הילה, בלי הילה) הושוו לפני ההחלטה, בערכת "בארץ" הכהה (ה-`--glow-color` הכתום, מקרה הקצה הגרוע) על Spotify ירוק ועל YouTube אדום — ואז מצב בהיר (ערכת "בארץ") ומצב כהה (ערכת "חו״ל") לוודא שהתוצאה הסופית תקינה בכל השילובים. צ׳יפ פעיל נבדק בפועל (לחיצה/`setFilter` אמיתיים) — אפור→צבע רק במעבר לפעיל, לא ההפך. אין רקע אפור מאחורי אף אייקון/עיגול. עברית, 375px. `node scripts/i18n-audit.js places.html` — 0 ממצאים (לא נוסף טקסט משתמש חדש). hook זמני (`window.__test`) הוסר ואומת שהוסר לפני push. v4.35.6 -> v4.35.7 (patch — תיקון עיצובי, כולל תיקון מדידה שגויה על יוטיוב).

## v4.35.6 — צביעת תגי המקור בטאב המוזיקה (places.html)

### ✨ תכונה חדשה
- **תג המקור של כל שיר (`.tag`, ליד "מוזג/יוסי/…") צבוע עכשיו לפי המקור** — ספוטיפיי ירוק (`#1DB954`), יוטיוב אדום (`#FF0000`), Apple Music ורוד-אדום (`#FA243C`), SoundCloud כתום (`#FF5500`). מקור לא מזוהה נשאר בדיוק כמו היום — `--surface-2`/`--primary-dark` הרגילים, בלי שינוי
- **המקור מזוהה מה-URL** (`musicSourceFor()`, כבר קיים) — אין שדה נפרד שמאחסן אותו. `musicSourceMeta()` עודכנה להחזיר גם את מפתח המקור הגולמי (`source`), לא רק אייקון+תווית, כדי שה-render לא יצטרך לקרוא ל-`musicSourceFor()` פעם שנייה
- **טקסט שחור, לא לבן — נמדד, לא הונח.** יחס ניגודיות (WCAG) חושב בפועל לכל ארבעת הצבעים: טקסט לבן על כל אחד מהם נופל מתחת ל-4.5:1 (הכי גרוע — ספוטיפיי, 2.59:1). טקסט שחור עובר בנוחות על כולם (הכי הדוק — יוטיוב, 5.25:1; ספוטיפיי 8.12:1)

### 🔒 מה שלא נגעו בו, בכוונה
- **`--glow-color` וערכות הצבע (§3.4) לא נגעו בהם.** הצבעים חדשים לגמרי, `background` קבוע (hex), לא דרך שום משתנה עיצוב קיים — בלי תלות בסוג הטיול או במצב כהה/בהיר
- **צ׳יפי הסינון (`.filter-chip`, מעל הרשימה) לא נצבעו.** יש להם כבר אינטראקציה עם `--glow-color` במצב כהה (`box-shadow` כשפעילים) — צביעה נוספת שם הייתה יוצרת בדיוק את סוג ההתנגשות שהתבקש להימנע ממנה. ההיקף נשאר על תג המקור לכל שיר בלבד, כמו שתואר

### 🔍 מה שנבדק לפני הכתיבה
- **אין "כפתור מקור" יחיד — שלושה אלמנטים נפרדים לכל שיר**: אייקון אמוג'י (`.place-icon`, כבר בעל צביעה חלקית דרך הגלואי עצמו — 🟢🟠), התג הטקסטואלי (`.tag`, מה שנצבע כאן), והקישור עצמו (`.music-link`, שם השיר, שקוף/ללא עיצוב ייחודי). רק ה-`.tag` תואם "כפתור מקור" — שני האחרים לא נגעו
- **המקורות הנתמכים היום**: Spotify (`open.spotify.com`), YouTube (`youtube.com`/`music.youtube.com`/`youtu.be`), Apple Music (`music.apple.com`), SoundCloud (`soundcloud.com`/`on.soundcloud.com`) — וכל דבר אחר נופל ל"קישור"/Link ניטרלי

נבדק בפועל: הוזרקו 5 שירים מדומים (אחד לכל מקור נתמך, אחד לא מזוהה) והתגים נבדקו ישירות (`getComputedStyle` — לא רק חזותית) — כל ארבעת הצבעים תואמים, מקור לא מזוהה נשאר `--surface-2`/`--primary-dark` בלי `data-source`. עברית, 375px, צילום מסך. מצב בהיר ומצב כהה × 3 מתוך 4 ערכות סוגי הטיול (abroad-light, local-light, reserve-dark) — אין התנגשות חזותית, הצבעים נשארים זהים וקריאים בכל שילוב (כצפוי — הם hex קבוע, לא תלויים בערכה). `node scripts/i18n-audit.js places.html` — 0 ממצאים. hook זמני (`window.__test`) הוסר ואומת שהוסר לפני push. v4.35.5 -> v4.35.6 (patch — תכונה קטנה, עיצובית בלבד).

## v4.35.5 — תיקון באג: שינוי שם לא מגיע ל-packing.html

### 🐛 תיקון באג — זהות תלויה ב-<select> שלא מתעדכן (packing.html)
- **`#participantSelect` היה `<select>`, לא שדה תצוגה — אותו כשל בדיוק שתוקן ב-shopping.html (v4.30.2).** האופציות נבנו מ-`trip.participants` (הרשימה הקבועה שהמארגן הקליד ב-wizard.html), לא מ-`memberProfiles`. שם שנקבע ע"י הרשמה אוטומטית (מזהות Google, או נערך אחר כך דרך העיפרון ב-`index.html`) כמעט אף פעם לא נמצא באותה רשימה. הצבת `.value` על `<select>` לערך בלי `<option>` תואם היא **no-op שקט**: הבורר נשאר על הפלייסהולדר ("בחר/י את שמך…"), מנוטרל, נראה בדיוק כמו "אין זהות" — גם כשהייתה זהות תקינה וכל שאר העמוד עבד
- **נמצא באמת בבדיקה עם משתמש אמיתי**: אחרי שינוי שם בדף הבית, כל העמודים הציגו את השם החדש חוץ מהציוד
- **למה זה שרד**: `#participantSelect` נבדק פעם אחת (v4.30.1, הסרת גייט הבחירה הסגורה) ונשאר בכוונה כי "נראה כמו אלמנט נפרד, לא הגייט" — בלי לבדוק אם יש לו את אותו באג. שבוע אחרי, אותו באג בדיוק נמצא ותוקן ב-`shopping.html`'s `#myName` (v4.30.2), אבל `packing.html` לא נסרק מחדש לאור הממצא
- **התיקון**: אותו פתרון בדיוק כמו shopping.html — `#myName` (שם חדש, span לקריאה בלבד) מציג `profile.name` ישירות, בלי בורר. `participantOptions()`/`saveProfile()` הוסרו כליל — לא נמחקו רק לחצי, כי חיפוש מלא בכל הריפו אישר שאין קורא נוסף להן (רק שתי נקודות הקריאה שכבר ידועות)

### 🔍 מה שנבדק לפני התיקון
- **`places.html`/`itinerary.html` — נקיים, לא רק "לא נמצא".** שניהם מתעדים במפורש בקוד: "the closed-roster #memberSelect flow are gone, not frozen" — נבנו אחרי שהמערכת הרב-טיולית כבר הייתה קיימת, אין להם אלמנט מקביל בכלל. `packing.html` היה היחיד מבין 6 העמודים שעדיין נשא את השריד

### 🐛 תיקון נלווה — כשלון שקט כשהפרופיל עדיין נטען
- **`requireProfile()` חדשה** — `addItem()`, `importItem()`, `addToSuggestedCategory()` בדקו `!profile` ופשוט חזרו, בלי שום משוב למשתמש. עכשיו מציגות טוסט ("עדיין טוען את הפרופיל שלך, נסו שוב בעוד רגע") באותו דפוס בדיוק כמו `requireUserName()` ב-shopping.html. הבדיקות הלא-קשורות לזהות (שם ריק, פריט לא נמצא) נשארו כפי שהיו — no-op שקט, לא טעות

נבדק בפועל: `#participantSelect` הישן נעלם מה-DOM (אומת), `#myName` מוצג ומתעדכן מיד עם שינוי `profile.name` — בלי צורך ברענון (`syncIdentity()` נקרא בכל נקודת רזולוציית פרופיל). מצב "עדיין טוען" מציג טקסט ריק (לא "בחר/י את שמך…" השגוי). `requireProfile()` נבדק ישירות: מציג טוסט נכון בעברית ובאנגלית (`trip.language`), מחזיר `null`. שלושת הקריאות (addItem/importItem/addToSuggestedCategory) אומתו שעברו לתבנית החדשה. צילום מסך ב-375px: שתי הצ׳יפים (`#myName`, `#userChip`) על שורה אחת, בלי גלישה, גם עם שם ארוך. `node scripts/i18n-audit.js packing.html` — 0 ממצאים. hook זמני (`window.__test`) הוסר ואומת שהוסר לפני push. **לא נבדק מול Firebase אמיתי** (מגבלת סביבה מתועדת) — הזרימה המלאה (שינוי שם ב-index.html → ניווט ל-packing.html → שם חדש מוצג) תלויה בקריאה טרייה של `memberProfiles` בטעינת עמוד אמיתית, לא סימלצה כאן. v4.35.4 -> v4.35.5 (patch — תיקון באג).

## v4.35.4 — starterLists() ל-4 סוגי הטיול: אותו מספר קטגוריות, gear+clothing תמיד קיימים

### 🐛 תיקון באג — קטגוריות לא עקביות בין סוגי הטיול (packing.html)
- **`local` קיבל בחזרה קטגוריית "מסמכים"** — עם פריטים מתאימים לטיול בארץ (תעודת זהות, רישיון נהיגה, כרטיס אשראי, אישור הזמנה), לא רשימת `abroad` (דרכון/כרטיס טיסה). `local` היה היחיד מבין 4 סוגי הטיול עם שתי קטגוריות בלבד (`gear`/`clothing`) — עכשיו שלוש, כמו כל השאר
- **`reserve` קיבל קטגוריית "ביגוד" חדשה** ("בגד חם", "בגד להחלפה") — לפני התיקון, `reserve` היה סוג הטיול היחיד בלי `clothing`. המשמעות בפועל: פריט הביגוד של `characterClothing` (docs/schema-legs.md §3.2 — כרגע רק `pride`/`bachelor` תורמים לזה) היה נשמט בשקט על טיול מילואים — `appendDeduped()` (packing.html) לא מוצאת קטגוריית יעד ופשוט לא כותבת כלום, בלי אזהרה. אומת בפועל: `reserve`+`pride` עכשיו כן כולל "תלבושות למסיבות" בביגוד; לפני התיקון זה לא היה מגיע לשום מקום
- **כל 4 סוגי הטיול מכילים עכשיו גם `gear` וגם `clothing`** — שני המפתחות היחידים ש-`characterGear`/`characterClothing` מפנות אליהם. אומת ישירות: הרצת `starterLists()` על כל 4 הסוגים × 2 השפות מחזירה בדיוק 3 קטגוריות בכל שילוב, עם `gear`+`clothing` תמיד נוכחים

### 📝 תיעוד, לא שינוי התנהגות
- **הערה חדשה מעל `ensureStarterLists()`** — מסבירה שהיא כותבת `listMeta` פעם אחת בלבד לכל משתמש בטיול (בביקור הראשון), בכוונה: לא רצה שוב כדי לא לדרוס שינויים ידניים שהמשתמש עשה (שינוי שם/מחיקה/סדר). `listLabelFor()` נופלת ל-id הגולמי כשאין עדיין `listMeta` — לא פער, רשת ביטחון מכוונת לזמן שבין הביקור הראשון לכתיבה

### 🔍 מה שהניע את זה — השערה, לא ודאות
- **החקירה המקורית**: נייט (משתמש אמיתי) דיווח על שתי קטגוריות בלתי-צפויות בטיול `local` — "טיול" ו"מילואים". קונסול על הטיול האמיתי שלו הראה `tripType: local`, `tripCharacter: undefined`, `vibes: [urban, beach]`, **`listMeta: undefined`** (ריק לגמרי ב-RTDB)
- **`starterLists()` הנוכחית לא יכולה לייצר "טיול"/"מילואים"** — אומת ישירות (הרצה בפועל, לא רק קריאת קוד) על כל 4 סוגי הטיול, שתי השפות: שום שילוב לא מפיק את השמות האלה. לא נמצא גם תיעוד ל-listMeta מטיול אחר (שכפול/תבנית) או לכל נתיב אחר שכותב `listMeta` בלי אינטראקציה מפורשת
- **ההשערה הסבירה ביותר, לא מאומתת**: `listMeta: undefined` פירושו ש-`ensureStarterLists()` מעולם לא כתבה בהצלחה לטיול הזה — מה שנייט ראה הגיע ישירות מ-`starterLists()` בזיכרון (מחושב מחדש בכל רינדור, לא נשמר), לא מ-RTDB. התסמין ("קטגוריות נראות, נעלמות ברענון") תואם את המנגנון הזה בדיוק. **אבל** לא אותר תוכן — בקוד הנוכחי או בהיסטוריית git — שהיה מפיק "טיול"/"מילואים" באמת, מה שמשאיר פער בין ההסבר המבני (בזיכרון, לא ב-RTDB) לתוכן הספציפי שנצפה. ייתכן שמדובר בגרסה קודמת של הטבלה שכבר נערכה, או בתוכן שהגיע ממקור שלא אותר
- **מה שכן ידוע בוודאות**: המבנה הישן (`local`/`reserve` עם שתי קטגוריות בלבד) היה בעייתי בפני עצמו — חסר עקביות, וגורם לכשל שקט ב-`tripCharacter`. התיקון הזה מתקן את זה בכל מקרה, גם אם אינו סוגר סופית את מה שנייט ראה

### ⚠️ מה שלא נגעו בו
- **`camping` לא שונה** — כבר יש לו שלוש קטגוריות (לינה/ציוד/ביגוד), כולל `gear` ו-`clothing`
- **טיולים קיימים לא מושפעים** — `ensureStarterLists()` כותבת פעם אחת בלבד (ר' למעלה); טיול שכבר יש לו `listMeta` (כולל הרשימה הישנה, דו-קטגורית, של `local`/`reserve`) לא נכתב מחדש. השינוי חל רק על משתמשים חדשים שמבקרים לראשונה מכאן ואילך

נבדק בפועל: `starterLists()` הורצה ישירות (לא רק קריאת קוד) על כל 4×2 השילובים (סוג טיול × שפה) — כולם מחזירים 3 קטגוריות, `gear`+`clothing` בכל אחד. נבדק גם עם `tripCharacter` אמיתי: `local`+`family` מוסיף נכון ל-`gear`; `reserve`+`ski` מוסיף נכון ל-`gear`; `reserve`+`pride` מוסיף נכון ל-`clothing` החדשה (התרחיש שהיה שקט לפני התיקון). `node scripts/i18n-audit.js packing.html` — 0 ממצאים. hook זמני (`window.__test`) הוסר ואומת שהוסר לפני push. **לא נבדק מול Firebase אמיתי**. v4.35.3 -> v4.35.4 (patch — תיקון עקביות/באג).

## v4.35.3 — תיקון באג: ייבוא פריט משותף יצר קטגוריה חדשה בלי סימוכין ל-tripType

### 🐛 תיקון באג — מסך ייבוא פריט משותף (packing.html)
- **`openImportCategoryPicker()` הציעה כברירת מחדל מסומנת מראש "צור קטגוריה בשם X"** — כש-X הוא שם הקטגוריה **אצל החבר המשתף**, בלי שום קשר ל-`tripType` של הטיול המייבא. אישור מהיר (או לא-שימת-לב לברירת המחדל) יצר קטגוריה תואמת אצל המייבא, בכל שם שהחבר בחר לרשימה שלו
- **התיקון**: אותו עיקרון כמו `tripCharacter` (docs/schema-legs.md §3.2) — הוספות נוחתות בקטגוריה **קיימת**, לעולם לא יוצרות חדשה. אפשרות "צור קטגוריה" הוסרה מהמסך לגמרי; הבורר מציג רק קטגוריות קיימות, עם `main` ("רשימה ראשית") כברירת מחדל תמיד-קיימת. יצירת קטגוריה ידנית דרך כפתור "+" הרגיל (`createList`) **לא נגעה** — זו עדיין תכונה זמינה, רק הטריגר האוטומטי של הייבוא הוסר

### ⚠️ מה שלא טופל, בכוונה
- **טיולים שכבר יש להם קטגוריות שנוצרו כך לא נוגעים** — `listMeta`/`items` שכבר נכתבו ל-RTDB (עם פריטים אמיתיים) נשארים כפי שהם. מחיקה הייתה מוחקת פריטים אמיתיים של משתמשים. התיקון מונע **הישנות מכאן ואילך** בלבד — לא ניקוי רטרואקטיבי

נבדק בפועל: `openImportCategoryPicker()` הופעלה ישירות עם מקור מדומה — הבורר המתקבל מכיל רק קטגוריות קיימות, בלי `__create__`, נבחר כברירת מחדל `main`. אישור (`confirmImportCategory` click) הושלם בלי שגיאה, האוברליי נסגר. `node scripts/i18n-audit.js packing.html` — 0 ממצאים. hook זמני (`window.__test`) הוסר ואומת שהוסר לפני push. **לא נבדק מול Firebase אמיתי** (מגבלת סביבה מתועדת) — הכתיבה בפועל (`push`) לא רצה מקצה-לקצה. **הערה מאוחרת (ר' v4.35.4)**: זה תיקון תשתיתי נכון בפני עצמו, אבל התברר שהוא לא היה המקור בפועל לתקרית שהניעה את החקירה — ר' ההשערה שם. v4.35.2 -> v4.35.3 (patch — תיקון באג).

## v4.35.2 — שלושה תיקוני באגים משוב אמיתי: הצעות AI, תצוגת קבוצה, ספירה לאחור

### 🐛 תיקון באג — הצעות AI לא מתאפסות בין קטגוריות (packing.html)
- **בוחרים הצעות בקטגוריה אחת, סוגרים, עוברים לקטגוריה אחרת — ההצעות הישנות עדיין שם.** `resetAiSuggestions()` חדשה נקראת בפתיחת מודאל ההוספה (`addItemFab`) — נקודת חנק יחידה, לא שתיים: המודאל הוא overlay מלא-מסך שחוסם גישה לצ׳יפ הקטגוריה ולמסנן היעד מאחוריו, כך שסגירה ופתיחה מחדש היא הדרך היחידה להחליף קטגוריה *או* יעד ממילא
- **גם החלפת יעד מאפסת**, בנוסף לפתיחת המודאל — `resetAiSuggestions()` נקרא גם מ-`onchange` של מסנן היעד (destination-scoped suggestions מאז v4.28). כשהמודאל כבר סגור (המקרה הרגיל) זו קריאה בלי עלות
- **פריטים שכבר נבחרו נשארים** — `addItem()` כותב אותם ישירות לנתוני הטיול ומסיר אותם מ-`#aiSuggestions` מיד; האיפוס מנקה רק את מה שעדיין מוצג כהצעה, לא נוגע במה שכבר נשמר

### 🐛 תיקון באג — הרשימה שלי מופיעה בתצוגת החברים (packing.html)
- **`renderGroup()` כבר בדקה `isMe` קודם — לא היה שריד לא-מטופל**, רק החלטה להראות את הפריטים העצמיים בכל זאת (בלי כפתור ייבוא). הצ׳קבוקסים (◻️/✅) מעולם לא היו אינטראקטיביים, **לאף אחד** — `<span>` סטטי, לא `<input>` אמיתי כמו ברשימה האישית (`class="check" type="checkbox"`). נראה שבור רק בשורה של המשתמש עצמו כי שם אין כפתור ייבוא לצידו שמסביר "זה של מישהו אחר"
- **הפתרון: סינון מלא**, לא disabled — מסך מלא פריטים מנוטרלים הוא רעש. `Object.entries(users).filter(([uid]) => uid !== user?.uid)` לפני שהבלוקים נבנים בכלל
- **הודעת המצב הריק עודכנה**: "עדיין לא שותפו פריטים" → "אף אחד אחר עדיין לא שיתף" — הישנה הייתה שגויה במקרה שהמשתמש עצמו כן שיתף (הפריטים שלו מסוננים מהתצוגה עכשיו, לא נעלמו מהנתונים) — בדיוק המקרה שהמצב הריק היה עלול להראות אחרי הסינון החדש שלא הראה קודם

### 🐛 תיקון באג — "עוד 1 ימים" בספירה לאחור (index.html)
- **לא היה מנגנון מחר/היום/שעות בכלל** — כל הטווח (מרגע יצירת הטיול ועד ההמראה) הציג "עוד X ימים · Y שעות" תמיד, כולל X=0 ביום ההמראה עצמו ("עוד 0 ימים")
- **נוסף מדורג**: 3+ ימים (הפורמט הקיים, רק דקדוק שעות מתוקן) → יומיים (צורת זוגי עברית) → מחר (בלי מספר שעות, פשוט וחם) → היום + שעות → מתחת לשעה: דקות (גם יחיד/זוגי — "דקה"/"דקתיים") → מתחת לדקה: "תיכף"
- **מבוסס על השוואת תאריך-לוח (`window.dateKeyInZone`), לא חלוקת מילישניות גולמית** — `daysBetweenKeys`/`dayKeyToUtcMs` הקיימות כבר בקובץ (index.html) מחשבות הפרש ימי-לוח אמיתי. הבדל מוחשי: 71 שעות ו-59 דקות היו נטבעות "2 ימים" בחלוקה גולמית (`Math.floor(71.98/24)=2`), אבל אם הרגע הזה חוצה שלושה תאריכי לוח שונים (היום/מחר/מחרתיים/היעד) זה בפועל "3 ימים" מבחינת מי שקורא את הספירה — ולהפך, "מחר" נקבע לפי תאריך-לוח (חצות אמיתי), לא סף של 24 שעות גולמיות
- **עקביות בין סוגי הטיול, אומתה**: הטקסט כבר היה גנרי (בלי "טיול"/"טיסה" מפורש, רק אייקון) — לא נדרש טיפול נפרד למילואים. הנוסח הנפרד הקיים ("הפעילות בעיצומה") הוא רק בשלב "במהלך", לא בספירה שלפני — וכל 4 סוגי הטיול (חו״ל/בארץ/קמפינג/מילואים) עכשיו עוברים באותה לוגיקה, שונים רק באייקון

נבדק בפועל: שלושתם דרך `resolveTripSelection`/`renderGroup`/`updateClockAndPhases` שהופעלו ישירות (לא רק קריאת קוד). **א**: פתיחת מודאל עם `#aiSuggestions` מלא-תוכן מדומה — מתנקה. **ב**: `renderGroup()` עם משתמש+חבר נוסף — רק החבר מוצג; עם המשתמש בלבד — מצב ריק עם הטקסט החדש; בדיקה עברית+אנגלית ב-`trip.language`. **ג**: `Date` הוחלף זמנית בעוגן קבוע (09:00, 10 בספטמבר) לבדיקה דטרמיניסטית של כל המדרגות — 75 שעות (3 ימים), 49 שעות (יומיים), 25/20 שעות (מחר, כולל חצייה לילית), 5/1/2 שעות (היום, כולל יחיד/זוגי), 30/1/2 דקות (יחיד/זוגי), פחות משנייה ("תיכף") — כל 11 המקרים אומתו בעברית, תת-קבוצה גם באנגלית. שלב "במהלך"/"אחרי" נבדקו בנפרד לוודא שלא נשברו. `resetAiSuggestions()` נבדק עם hook זמני (`window.__test`, הוסר ואומת שהוסר לפני push — ר׳ "בדיקת UI בדפדפן" ב-CLAUDE.md). `node scripts/i18n-audit.js index.html packing.html` — 0 ממצאים. **לא אומת מול Firebase אמיתי** (מגבלת סביבה מתועדת) — `resetAiSuggestions()` על מסנן יעד אמיתי (טיול מרובה-יעדים אמיתי) לא הורץ קצה-לקצה. v4.35.1 -> v4.35.2 (patch — שלושה תיקוני באגים).

## v4.35.1 — כרטיס "חזרה לטיול" ברשימת הטיולים

### 🐛 תיקון באג
- **כפתור "הטיולים שלי" (v4.35.0) הוציא את המשתמש בלי דרך חזרה** — אותו דפוס בדיוק שכבר תוקן ב-`gittrip-showcase.html` (מסלול בלי דרך חזרה לטיול). מי שלחץ עליו מתוך טיול פעיל הגיע לרשימה, ומשם היה צריך לזכור/לחפש איך חוזרים
- **`tripsLink`'s href עכשיו נושא את ה-`tripId` הנוכחי**: `index.html?trips=1&from=<tripId>` — מחושב פעם אחת מ-`tripId` הקיים, לא ערך סטטי בקוד
- **כרטיס "‹ חזרה ל-{יעד}" חדש בראש הרשימה (`#tripPickerBack`)**, מוצג רק כש-`from` קיים בקישור. שם היעד מגיע מ-`localStorage["trip-{tripId}-name"]` — מטמון שכבר נכתב על ידי `hydrateTripData()` בכל טעינת טיול מוצלחת, בלי צורך בקריאת RTDB נוספת כדי להציג את הכרטיס. בלי שם במטמון (מקרה קצה — מטמון נוקה, או המטמון עדיין לא נכתב) — נופל ל"חזרה לטיול" הגנרי, לא נעלם
- **בלי `from` — אין כרטיס, בדיוק כמו לפני התיקון.** זה כולל את מסלול "הטיול לא נמצא" הקיים (`startTripListener`, כשטיול נמחק): הוא קורא ל-`resolveTripSelection({skipLastTripId:true})` בלי לשנות את כתובת ה-URL בכלל — ה-`tripId` השגוי שכבר בכתובת אף פעם לא הופך ל-`from`, כך שאין כרטיס "חזרה" שגוי לטיול שכבר נכשל

נבדק בפועל: שלושה מסלולים הורצו ישירות דרך `resolveTripSelection()` (לא רק קריאת קוד) — (1) מתוך טיול עם `from` תקין ושם במטמון: הכרטיס מופיע, `href` נכון, טקסט "חזרה ל{שם}"; (2) `?trips=1` בלי `from`: אין כרטיס, אין שבירה; (3) מסלול "הטיול לא נמצא" (`skipLastTripId:true` בלי `from` בכתובת): אין כרטיס. גם מקרה הקצה של `from` בלי שם במטמון (`l=en`) נבדק — נופל ל-"Back to trip" הגנרי. עברית ואנגלית, 375px — צילום מסך אומת שהכרטיס יושב בראש הרשימה, שורה אחת, בלי גלישה. `node scripts/i18n-audit.js index.html` — 0 ממצאים. **לא נבדק מול Firebase אמיתי** (אותה מגבלת סביבה כמו v4.35.0) — שם היעד במטמון תלוי בכך ש-`hydrateTripData()` רץ לפחות פעם אחת קודם על אותו מכשיר, מה שקורה בפרודקשן בכל טעינת טיול רגילה. v4.35.0 -> v4.35.1 (patch — תיקון באג בתכונה שזה עתה שוחררה).

## v4.35.0 — כפתור "הטיולים שלי" ב-index.html, דלת כניסה למסך בחירת טיול שכבר קיים

### ✨ תכונה חדשה
- **`#tripPicker` כבר היה קיים ב-`index.html` מעבודה קודמת, בלי שום דרך להגיע אליו** — חקירה ייעודית (`multi-trip-investigation.md`) גילתה שהמסך (empty-state, רשימת כרטיסים, קיצור-דרך לטיול יחיד) כבר קורא בפועל מ-`users/{uid}/trips`, שנכתב גם על ידי יוצר הטיול (`wizard.html`) וגם על ידי מי שהצטרף (packing/shopping/places/itinerary — לא `index.html` עצמו, זו נשארה נקודה פתוחה). הבעיה לא הייתה במסך — הייתה של-`resolveTripSelection()` תמיד קדמה קפיצה ל-`lastTripId` (משבצת "הטיול האחרון שנטען" בלבד, לא רשימה), ושום קישור ניווט באפליקציה לא מוביל למסך הזה
- **`index.html?trips=1` חדש עוקף את הקפיצה** — מעביר `skipLastTripId:true` ל-`resolveTripSelection()` הקיימת (האופציה כבר הייתה שם, פשוט לא נעשה בה שימוש מלבד הזרימה הפנימית של "הטיול הנוכחי נמחק"). לא נוגע בהתנהגות ברירת המחדל — בלי הפרמטר הכול נשאר בדיוק כמו קודם
- **כפתור 🧳 חדש בשורת המשתמש (`index.html`), ליד "יציאה מהחשבון"** — `<a href="index.html?trips=1">`, מוסתר עד התחברות (אותו דפוס כמו `logoutLink`). אייקון בלבד, בלי טקסט — `aria-label`/`title` מתעדכנים לפי שפה (עברית/אנגלית) באותה נקודה שמעדכנת את `editNameBtn`

### 🎯 למה בדף הבית בלבד, ולמה אייקון בלבד
- **רק `index.html`, לא בכל 6 העמודים** — כל קישורי "בית" ב-5 העמודים האחרים כבר עוברים דרך `scoped()` (למשל `packing.html`, `configureLinks()`) שמצמידה את ה-`tripId` הנוכחי בחזרה — הניווט שם הוא "בין מודולים של אותו טיול", לא "בין טיולים". כפתור מתחרה שם היה שובר את המודל הזה ותופס מקום בבר-ניווט תחתון שכבר צר
- **אייקון בלבד, לא אייקון+טקסט** — `.session-row` מכוילת בכוונה סביב תקציב רוחב מדויק: הערת קוד קיימת (`#sessionUserLabel`) מתעדת באג שכבר קרה בפרודקשן — שם ארוך גרם לשורה לגלוש לשתי שורות, לפני ש-`darkModeToggle`/`logoutLink` קיבלו `flex-shrink:0` כדי ש-`#sessionUserLabel` לבדו יספוג את לחץ הרוחב דרך אליפסיס. אלמנט שלישי קבוע-רוחב מקטין את מה שנשאר לשם — טקסט היה מחריף בדיוק את זה, ועכשיו השם גם ניתן לעריכה (לא בהכרח קצר כמו שם Google המקורי)

### ⚠️ מחוץ להיקף בכוונה — מתועד ב-roadmap, לא כאן
- **אין מחיקה/יציאה מתוך מסך הבחירה עצמו, ואין ניקוי לרשומות-רפאים** אצל משתתפים אחרי שהמארגן מוחק טיול. תועד כפריט נפרד ב-`docs/roadmap.md` חלק ג׳ סעיף 10 — במפורש נדחה מההיקף הנוכחי לפי בקשה, לא נשכח

נבדק בפועל: `node scripts/i18n-audit.js index.html` — 0 ממצאים. שורת המשתמש נבדקה ב-375px עם שם מדומה של 41 תווים — גובה השורה נשאר 32px (שורה אחת, אין רגרסיית גלישה), `scrollWidth` (277px) גדול מהרוחב המוצג (43px) ו-`text-overflow:ellipsis` מאומת ב-`getComputedStyle`. הכפתור עצמו אומת: `href="index.html?trips=1"`, `aria-label`/`title` נכונים בעברית ומתעדכנים לאנגלית. `forceTripPicker` אומת שמפוענח נכון מ-`?trips=1`. **הזרימה המלאה אומתה ישירות** (לא רק קריאת קוד): עם `?trips=1` ו-`lastTripId` מדומה קיים ב-localStorage ומשתמש מדומה מחובר, `resolveTripSelection()` דילגה על הקפיצה (`location.replace` לא נקרא) והציגה את `#tripPicker` בפועל. **לא נבדק מול Firebase אמיתי** (מגבלת סביבה מתועדת) — כתיבה/קריאה אמיתית מ-`users/{uid}/trips` (כולל הרשומות שכבר נכתבות היום מ-4 העמודים) לא אומתה מקצה-לקצה כאן. v4.34.3 -> v4.35.0 (minor — תכונה חדשה, נגישה למשתמש).

## v4.34.3 — היפוך צבעי האייקון לרקע כהה, og-preview חדש, og-tags ל-index.html

### 🎨 שינוי חזותי - היפוך צבעים באייקון (מחליף את v4.34.2)
- **כל 10 קבצי האייקון הוחלפו שוב** - אותה צורה בדיוק כמו ב-v4.34.2 (פין+מסלול מקווקו), אבל הפעם רקע כהה `#0B1015` (בדיוק הרקע של האפליקציה) וציור טורקיז `#2DD4C8`, במקום רקע טורקיז מלא. הסיבה: נבדק במסך הבית ב-iOS - ריבוע טורקיז מלא בלט מדי במצב כהה, ו-iOS לא מחליף אייקונים לפי מצב תצוגה של המכשיר. כל 10 הקבצים (`icon-512/192.png`, `apple-touch-icon.png`, `icon-maskable-512/192.png`, `favicon-32/16.png`, `favicon.svg`, `gittrip-icon.svg`, `gittrip-icon-maskable.svg`) עקביים בהיפוך - **אומת שאף קובץ לא נשאר אחורה בגרסה הישנה** (נמצא בפועל: המשלוח הראשון כלל רק 5 מתוך 10 קבצים - `icon-maskable-*.png`/`favicon-16/32.png`/`gittrip-icon-maskable.svg` היו חסרים ונשארו זמנית ברקע טורקיז מ-v4.34.2; המיזוג נעצר ולא בוצע חלקית עד שכל 10 הקבצים אומתו עקביים)
- **`manifest.json` ו-`<meta name="theme-color">` הסטטי בכל 6 העמודים** - `theme_color`/`background_color` שניהם `#0B1015` עכשיו (זהים - הרקע הכהה של האייקון הוא גם רקע הסימה של האפליקציה). **לא נגעו** בדריסה הדינמית ב-`index.html`/`shopping.html` (`--primary-dark`/`--sea-deep`) - נשאר פער נפרד, כמו ב-v4.34.2

### ⚠️ מגבלת מטמון PWA - שוב רלוונטי
- **מכשיר שכבר הוסיף למסך הבית בין v4.34.2 ל-v4.34.3 שומר את בייטי האייקון הישנים (רקע טורקיז)** - אותה מגבלה כמו ב-v4.34.2, ועכשיו רלוונטית פעם שנייה כי הצבעים התהפכו שוב. תיקון: הסרה והוספה מחדש של הקיצור למסך הבית

### 🖼️ og-preview חדש - מתקן פסים לבנים בתצוגה המקדימה בוואטסאפ
- **`og-preview.png`/`og-preview.svg` הוחלפו** - הישן היה מטוס הנייר על כחול (לא קשור לאייקון הנוכחי). **תוקן גם באג בפועל**: וואטסאפ מציג את תמונת התצוגה המקדימה כמלבן, בלי לחתוך לפינות מעוגלות - האייקון הישן (`rx="114"`, שקוף מחוץ לפינה) הראה פסים לבנים בפינות כי וואטסאפ ממלא שקיפות בלבן. `og-preview.png` החדש: 1200×630, RGB בלי ערוץ אלפא (אומת בבייטים - `colorType 2`), מלבני ומלא לגמרי - אין שקיפות שיכולה להתמלא בלבן
- **`icon-square-512.png`/`icon-square.svg` נוספו לריפו** - ריבוע מלא בלי פינות מעוגלות. **לא מחובר לשום דבר קיים** - נשמר למקרה שיעלה צורך בהקשר שכבר חותך פינות בעצמו

### 🔗 og:/twitter: tags חדשים ב-index.html - לא היו קיימים בכלל
- **`index.html` הוא הקישור שבפועל משותף להזמנת חברים לטיול** (לא `gittrip-showcase.html`) - עד עכשיו לא היה לו שום תג `og:`/`twitter:` בכלל, אז שיתוף בוואטסאפ הציג כותרת+קישור בלי תמונה. נוספו `og:title`/`og:description`/`og:image`/`og:type` ו-`twitter:card`/`twitter:title`/`twitter:description`/`twitter:image`, מצביעים על `og-preview.png` - אותו דפוס כמו ב-`gittrip-showcase.html`
- **הכותרת/התיאור סטטיים בכוונה, לא לפי הטיול** - וואטסאפ קורא את ה-HTML הגולמי לפני שה-JS רץ, אז `tripId`/`d`/`h` שבקישור לא זמינים לתג סטטי. `og:description`: "הוזמנת לטיול. הצטרפו וראו מה לארוז, מה לקנות ומה עושים כל יום." - בלי "GitTrip" בפנים (זה כבר ב-`og:title`, וחזרה על השם באותה כרטיסייה גוזלת מקום בלי תועלת)
- **`og:url` הושמט בכוונה מ-`index.html`, בניגוד ל-`gittrip-showcase.html` שכן יש לו אחד** - נבדק: הקישור הלחיץ שוואטסאפ מציג הוא תמיד ה-URL האמיתי שנשלח (כולל `tripId`/`d`/`h`/`l`), לא `og:url` - גם המטמון של וואטסאפ נשמר לפי ה-URL המדויק שנשלח, לא לפי `og:url`. אבל `og:url` קבוע (בלי פרמטרים) הוא עדיין הזמנה לבלבול מיותר - עדיף בלי מאשר עם כתובת שלא תואמת את מה שהתגלה בפועל בקישור. ב-`gittrip-showcase.html` יש `og:url` קבוע כי הכתובת שם באמת קבועה - אין שם פרמטרים ייחודיים לכל שיתוף

נבדק בפועל: `manifest.json` נבדק כ-JSON תקין. כל 11 קבצי האייקון/og/square אומתו נטענים (200) בשרת המקומי. `theme-color` הסטטי (מקור ה-HTML, לא ערך רץ) ו-`background_color`/`theme_color` ב-manifest אומתו `#0B1015` בכל 6 העמודים. תגי ה-og/twitter ב-`index.html` נקראו בדפדפן חי ואומתו מדויק, כולל היעדר `og:url`. `og-preview.png` אומת 1200×630 בלי ערוץ אלפא ישירות מבייטי ה-PNG (`IHDR`), לא רק חזותית. **לא אומת שיתוף אמיתי בוואטסאפ** (דורש קישור ציבורי אמיתי ב-production, לא זמין בסביבה המקומית) - זו בדיקה שנשארת למשתמש לבצע בפועל אחרי ה-deploy. v4.34.2 -> v4.34.3 (patch - שינוי חזותי בלבד).

## v4.34.2 — סט אייקונים חדש: מסלול לירוק כהה על טורקיז, מחליף את מטוס הנייר

### 🎨 שינוי חזותי
- **כל אייקוני האפליקציה הוחלפו** - `icon-512.png`, `icon-192.png`, `apple-touch-icon.png` (מטוס נייר לבן על גרדיאנט כחול, עד היום) הוחלפו באייקון חדש: פין+מסלול מקווקו בירוק כהה (`#06312D`) על רקע טורקיז (`#2DD4C8`). נוספו גם `icon-maskable-512.png`/`icon-maskable-192.png` (אמנות מוקטנת ל-66% עם שוליים, לצורך המסכה שה-OS/launcher מפעיל על אנדרואיד) ו-`favicon-32.png`/`favicon-16.png`/`favicon.svg` (גרסת פין בלבד, בלי המסלול המקווקו - מתחת ל-48px הנקודות של המסלול מתמזגות לכתם לא קריא). שני קובצי המקור (`gittrip-icon.svg`, `gittrip-icon-maskable.svg`) נשמרו בריפו גם הם, לעיון עתידי
- **`manifest.json`**: שתי כניסות `icons` חדשות ל-`icon-maskable-*.png` עם `"purpose": "maskable"`, לצד ה-192/512 הקיימים (`"purpose": "any"`, כעת מצביעים על האייקון החדש). `theme_color` עודכן מ-`#123B63` ל-`#2DD4C8` - הצבע הדומיננטי באייקון החדש הוא הרקע, לא המסלול. `background_color` עודכן מ-`#123B63` ל-`#0B1015` (הרקע הכהה של האפליקציה) - זה הרקע שמוצג ב-splash screen של ה-PWA לפני שהעמוד נטען, כלומר הדבר הראשון שנראה בפתיחה; `#123B63` הכחול הישן לא היה קשור לשום דבר באפליקציה, ועכשיו גם היה מתנגש עם האייקון החדש. לא טורקיז - מסך פתיחה מלא בטורקיז ייצא צעקני
- **`<link rel="icon">` חדש בכל 6 העמודים** - לא היה קיים בכלל עד היום (רק `rel="manifest"`/`rel="apple-touch-icon"`). שלושה תגים לכל עמוד: `favicon.svg` (`type="image/svg+xml"`), ואחריו `favicon-32.png`/`favicon-16.png` כנפילה לדפדפנים בלי תמיכת SVG בפאב-אייקון
- **`<meta name="theme-color">` הסטטי עודכן ל-`#2DD4C8` בכל 6 העמודים.** **לא נגעו** בדריסה הדינמית שקיימת ב-`index.html` (מ-`--primary-dark`) וב-`shopping.html` (מ-`--sea-deep`) - זה נשאר פער נפרד, כבר מתועד ב-roadmap (`--sea-deep` הוא בפועל צבע טקסט, לא מיועד לזה). ל-4 העמודים האחרים (wizard/places/packing/itinerary) אין דריסה דינמית בכלל - הערך הסטטי הוא הערך הסופי שם

### ⚠️ מגבלת מטמון PWA - לא ניתן לתיקון מקוד
- **אין service worker בפרויקט** - אין שכבת מטמון ברמת קוד לנקות. מטמון ה-edge של GitHub Pages מתפנה תוך עד כ-10 דקות (מתועד). **אבל**: מכשיר שכבר ביצע "הוספה למסך הבית" שמר את בייטים של האייקון הישן במכשיר עצמו, ולא ירענן אותם לבד גם אחרי שהקובץ (אותו שם קובץ בדיוק) משתנה בשרת - הפתרון היחיד הוא הסרה והוספה מחדש של הקיצור. זו מגבלת פלטפורמה, לא באג בקוד

נבדק בפועל: `manifest.json` נבדק כ-JSON תקין. שלוש כתובות ה-icon בכל אחד מ-6 העמודים אומתו קיימות בריפו (אין 404 פוטנציאלי). לא נמצאו קובצי אייקון ישנים/יתומים מעבר לשלושת אלה שהוחלפו במקום. **לא אומת חזותית בדפדפן חי מול production** (טאב/PWA מותקן לא זמינים לבדיקה בסביבה המקומית) - `theme-color` הסטטי ותגי ה-favicon כן נבדקו כטקסט מול ה-HTML הנטען בפועל בשרת המקומי. v4.34.1 -> v4.34.2 (patch - שינוי חזותי בלבד, בלי שינוי סכמה/פיצ׳ר).

## v4.34.1 — showcase מכיר tripId, סוגר את מעגל ההזמנה (commit 2ג מתוך 3, אחרון)

### ✨ תכונה חדשה
- **`gittrip-showcase.html` מזהה `tripId` בקישור ומחליף את כפתור ה-CTA** - "✈️ חזרה לטיול ל{d}" (כש-`d` ידוע) או "✈️ חזרה לטיול" (בלעדיו), מוביל ל-`index.html` עם `tripId`/`d`/`h`/`l` מועברים הלאה. בלי `tripId` - בדיוק כמו קודם, "✈️ לפתיחת GitTrip" מוביל לוויזרד ליצירת טיול חדש
- **הכפתור עובר לאותה כרטיסייה, לא פותח חדשה** - בניגוד ל"מה זה GitTrip?" ב-authGate (v4.34.0, שנשאר `target="_blank"` בכוונה כדי שכרטיסיית ההזמנה המקורית תישאר פתוחה מאחור). "חזרה לטיול" מסיר את `target`/`rel` - חוזר לאותו הקשר, לא מערים כרטיסייה שלישית
- **ההיקף מוגבל בכוונה לכפתור בלבד** - `l=en` משפיע רק על טקסט הכפתור עצמו (למשל "Back to the trip to Berlin"); `lang`/`dir` של הדף, וכל שאר התוכן (הכותרות, ההסברים), נשארים בעברית קשיחה כמו היום. תרגום מלא של ה-showcase הוא משימה נפרדת, לא בהיקף כאן

### 🔒 אבטחה ותצוגה - אותם כללים כמו ב-authGate
- **`d`/`h` הם קלט מקישור** - הכותרת/הטקסט של הכפתור נבנים דרך `.textContent` בלבד, בדיוק כמו ב-authGate. אומת עם payload XSS אמיתי (`<img onerror>`) - לא רץ, מוצג כטקסט מילולי
- **אותה תקרת 30 תווים לפני בניית הטקסט**, לא CSS ellipsis - אותה הנמקה בדיוק כמו ב-`truncateForGate()` (index.html). לא שותפה כפונקציה משותפת - חמש שורות, קובץ עצמאי, לא שווה קובץ שלישי בשביל זה (ר' CLAUDE.md על שלושת בוני ה-URL, שזה כן שווה לאחד)

נבדק בפועל - **סגירת המעגל המלאה**: authGate עם `d`/`h` אמיתיים → קליק על "מה זה GitTrip?" → showcase מציג כפתור "חזרה לטיול ל-{d}" נכון → קליק עליו → אותה כרטיסיית הזמנה, אותה כותרת מדויקת. כל השילובים (עם/בלי `tripId`, עם/בלי `d`, `l=en`) נבדקו בניווט אמיתי. `node scripts/i18n-audit.js gittrip-showcase.html` - 0 ממצאים. עברית+אנגלית (כפתור), 375px. **לא אומת מקצה-לקצה מול RTDB אמיתי** - לא רלוונטי כאן, showcase לא נוגע ב-RTDB בכלל. v4.34.0 -> v4.34.1 (patch - `gittrip-showcase.html` לא טוען את `version.js`, אבל המונה המשותף עדיין מתקדם כדי שהקומיט לא ייעלם מהרצף, ר' "ניהול גרסה" ב-CLAUDE.md).

## v4.34.0 — מסך ההזמנה עצמו (commit 2ব מתוך 3)

### ✨ תכונה חדשה
- **`#authGate` (index.html) מציג כותרת אישית מ-`d`/`h` שבקישור** - "X הזמין אותך לטיול ל-Y ✈️" כששניהם ידועים, נופל בהדרגה ל"הוזמנת לטיול ל-Y" (בלי `h`) או "הוזמנת לטיול" (בלי `d`). **בלי שניהם - הנוסח המקורי במלואו, כותרת ותת-כותרת, מילה במילה** - לא הנוסח הגנרי החדש. תת-הכותרת (פסקת "מה זה GitTrip") היא חדשה ואחידה בכל שלושת המקרים שיש בהם מידע - רק הכותרת משתנה
- **קישור "מה זה GitTrip?" חדש בשער** - טקסט אפור קטן ומשני מתחת לכפתור הכניסה, לא מתחרה בו. פותח את `gittrip-showcase.html` ב-`target="_blank"` (הטיול נשאר פתוח מאחור), מעביר `tripId`/`d`/`h`/`l`
- **`.app-container` מוסתרת כל עוד השער פתוח** (הפוך מ-`#authGate` באותה נקודת קוד, `handleAuthStateChanged`) - הפלייסהולדר השקרי (מזג אוויר תל אביב, "הטיול הבא") שהיה נראה מטושטש מאחורי השער נעלם לגמרי. מסך נקי במקומו

### 🌐 פרמטר שפה חדש - `l`, לא היה בתכנון המקורי
- **נמצא תוך כדי: מעולם לא היה מנגנון לקבוע את שפת השער לפני התחברות** - `currentLanguage` תמיד התחיל כברירת מחדל קבועה ל"he", ומתעדכן רק פעם אחת, מ-`trip.language` האמיתי, אחרי טעינת נתוני הטיול (כלומר אחרי התחברות). אין `localStorage`, אין `navigator.language` - **זה לא היה ידוע לפני שנבדק בקוד**. המשמעות: כל טיול שאורגן באנגלית קיבל שער כניסה בעברית
- **תוקן, לא נדחה** - `l=en` נוסף לקישור המשותף (שני המקומות שכבר מקבלים `d`/`h`: `updateAdminShareLink()` ב-index.html, וה-handler ב-wizard.html) ונקרא ב-`index.html` לפני שכל דבר אחר נטען - `currentLanguage` מאותחל ממנו ישירות במקום מברירת המחדל הקבועה, אז לא רק טקסט השער אלא גם `dir`/`lang` של המסמך כולו מתהפכים נכון ל-LTR/אנגלית. בלי `l` - עברית, בדיוק כמו היום, נפילה נקייה. מושמט בכוונה עבור עברית (לא רק כשלא ידוע) - "אין l" כבר אומר עברית

### 🔒 XSS - לא רק הערה
- **`d`/`h` הם קלט מקישור, לא מהימן.** כל בנייה של כותרת/תת-כותרת השער עוברת דרך `.textContent` בלבד - קישור עם `d=<script>...`/`d=<img onerror=...>` מוצג כטקסט מילולי, לא רץ. אומת בפועל, לא רק נטען: קישור עם payload אמיתי הורץ בדפדפן חי, `window.__xss` (משתנה שה-payload היה אמור להגדיר) נשאר `undefined`
- **תקרת אורך תצוגה, לא CSS ellipsis** - `truncateForGate()` חדש חותך `d`/`h` ל-30 תווים + "…" *לפני* בניית המשפט, לא ellipsis על התיבה כולה. הבדל מכוון מהדפוס שנבנה ב-commit ג (שינוי שם תצוגה): שם כותרת מורכב ממשפט טבעי (טקסט קבוע + שם דינמי) שאמור להיגלל לכמה שורות במקרה הרגיל - ellipsis על כל ה-`<h2>` היה חותך משפט סביר לגמרי אחרי כמה מילים. חיתוך המחרוזת עצמה מבטיח ששם סביר לא נחתך בכלל, ושם של 200 תווים לא יכול לנפח את הכרטיס

### ⚠️ לא בהיקף - מתועד כדי שלא ייראה כמו באג
- **`gittrip-showcase.html` עדיין לא מכיר `tripId` - זה commit 2ג.** מי שלוחץ "מה זה GitTrip?" מהשער יגיע לעמוד שמוביל בחזרה לוויזרד ליצירת טיול חדש, לא בחזרה לטיול שהוזמן אליו. הקישור עצמו (עם tripId/d/h/l) כבר עובד נכון - `gittrip-showcase.html` פשוט לא קורא את הפרמטרים שלו עדיין

נבדק בפועל: כל שילובי d/h (שניהם, רק d, רק h, אף אחד) הורצו בדפדפן חי עם ניווט אמיתי (לא הזרקת state) - הכותרת/תת-כותרת אומתו מדויק בכל מקרה, כולל הנפילה למקרה "בלי שניהם" שמחזירה את המחרוזות המקוריות בדיוק. XSS payload אמיתי (`<img onerror>`) הורץ בקישור - לא בוצע, מוצג כטקסט. `l=en` אומת מקצה-לקצה: `document.documentElement.dir`/`lang` התהפכו נכון, הכותרת/הכפתור/הקישור כולם באנגלית. `updateAdminShareLink()` ו-wizard.html's handler (דרך `window.__test` זמני, הוסר ואומת) - שניהם בונים קישור עם/בלי `l` נכון לפי שפת הטיול. `.app-container` מוסתרת בפועל - נבדק חזותית, אין פלייסהולדר גלוי מאחורי השער. עברית+אנגלית, בהיר+כהה, 375px. `node scripts/i18n-audit.js` - 0 ממצאים חדשים. **לא אומת מקצה-לקצה מול RTDB אמיתי** (מגבלת סביבה מתועדת).

## v4.33.0 — organizerName + פרטי הזמנה בקישור המשותף (commit 2א מתוך 3)

### ✨ תכונה חדשה, שינוי סכמה
- **`organizerName` חדש ב-`tripData`, נכתב פעם אחת ב-wizard.html ברגע יצירת הטיול** (`organizer.displayName || organizer.email || ''`) - השם הראשון שנשמר על טיול בכלל. `ownerId` הקיים משמש רק להרשאות, שום קוד לא פתר אותו לשם. לא ב-`memberProfiles` בכוונה: מסך ההזמנה (2ב) מוצג *לפני* התחברות, ואז אי אפשר לקרוא מ-RTDB בכלל (`.read: auth != null`) - ומי שיצר טיול ומיד שלח קישור עדיין אין לו `memberProfiles` בכלל
- **שני מקומות נפרדים בונים את הקישור המשותף - שניהם עודכנו**: `updateAdminShareLink()` (index.html, פאנל המנהל) ו-`generateTripAndSave()`'s success handler (wizard.html, `#finalTripLink`/`#openTripBtn` - **הקישור שמוצג מיד אחרי יצירת הטיול, המסלול הנפוץ ביותר בפועל**). שניהם מוסיפים `d` (יעד) ו-`h` (שם מארגן) - `?tripId=X&d=ברלין&h=נתנאל` - עם `URLSearchParams.set()`, לא הרכבת מחרוזת (מקודד נכון אוטומטית, בלי `encodeURIComponent` ידני מעל - זה היה מקודד כפול). כל אחד מהשניים מתווסף רק אם קיים - טיול ישן בלי `organizerName` פשוט משתף קישור בלי `h`
- **`organizerName` נטען ב-`hydrateTripData()`**, אותה נקודה בדיוק שבה `destinationName` כבר נטען מ-`primaryLeg.name` - עקבי עם התבנית הקיימת
- **נסרקו כל 6 העמודים לבנייה נוספת של קישורי שיתוף** - נמצאו 3 מימושים עצמאיים ל-URL עם `tripId` (לא אחד): `scopedTripUrl` (index.html), `tripUrl` (wizard.html), `scopedUrl` (shopping.html). `wizard.html`'s `renderRecentTrips()` (גם משתמשת ב-`tripUrl`) **לא נגעה** - אלה קישורים לטיולים *של המשתמש עצמו*, `d`/`h` לא רלוונטיים שם. `shopping.html`'s `updateShareLink()` **לא נגעה בכוונה, מתועד בהערת קוד**: זו שיתוף מחדש של טאב רשימת קניות ספציפי בין מי שכבר בקבוצה (לא הזמנה ראשונית), והיא מובילה ל-`shopping.html` ממילא - מסך ההזמנה קיים רק ב-`authGate` של `index.html`, אז `d`/`h` שם לא היו נקראים בכל מקרה. **שלוש הפונקציות לא אוחדו** - נוסף לכך CLAUDE.md, כדי שהכפילות לא תיסגר בטעות ("מימוש רביעי") ולא תיפתח בטעות ("בואו נאחד עכשיו") בלי משימה ייעודית

### ⚠️ הערת אבטחה - לא רק תיעוד
- **`d`/`h` הם קלט מקישור, לא מהימנים** - כשמסך ההזמנה (2ב) יציג אותם, זה *חייב* להיות דרך `textContent`, לא `innerHTML`/הרכבת מחרוזת. קישור עם `d=<script>...` הוא XSS רפלקטיבי אמיתי אם זה לא נאכף. זיוף התוכן עצמו (שם/יעד שקריים) מקובל ולא מסוכן - `d`/`h` הם רק להצגה, לא משפיעים על מה שנקרא בפועל מ-RTDB אחרי התחברות

נבדק בפועל: גם `updateAdminShareLink()` (index.html) וגם בניית `shareUrl` ב-wizard.html הורצו בדפדפן חי עם שמות עברית - `URLSearchParams.get()` על הקישור שנבנה פענח בדיוק את אותם ערכים שנכנסו, בלי קידוד כפול. wizard.html נבדק דרך hook זמני `window.__test` (כי `tripUrl` מקומית ל-module, לא נגישה מהקונסול) - הוזרק, אומת, הוסר ואומת שהוסר. קונסול נקי בשלושת העמודים (index.html, wizard.html, shopping.html). `node scripts/i18n-audit.js` - 0 ממצאים חדשים (אין טקסט משתמש חדש, רק שדה נתונים ותיקוני קישור). **לא אומת מקצה-לקצה מול RTDB אמיתי** (מגבלת סביבה מתועדת) - `organizerName` בפועל ייכתב רק ביצירת טיול אמיתי דרך הוויזרד בפרודקשן.

## v4.32.2 — שינוי שם תצוגה (commit ג, אחרון בשרשרת ההרשמה האוטומטית)

### ✨ תכונה חדשה
- **עיפרון ליד השם ב-index.html פותח שדה inline לשינוי שם התצוגה** - הכתיבה מוגבלת ל-`name` בלבד ב-`memberProfiles/{tripRoot}/{uid}` (לא נוגעת ב-`participantKey` - הכלל דורש שהוא יישאר כפי שנקבע ב-auto-register), מוגבלת ל-40 תווים עם `trim` לפני שמירה. Enter ו-blur שומרים אם השם השתנה ולא ריק; Escape מאפס את הערך ואז מטשטש (blur) - כך "ביטול" ו"נלחץ הצידה בלי שינוי" חולקים נתיב קוד אחד. שם ריק אחרי trim: ב-Enter (ניסיון שמירה מפורש) - טוסט + השדה נשאר פתוח וממוקד; ב-blur (התחרטות) - נסגר בשקט בלי הודעה
- **כשלון כתיבה**: לא אופטימי - לא מציג את השם החדש עד שה-`update()` נפתר בפועל. בכשלון: טוסט + חזרה לשם הישן + סגירת השדה

### 🏗️ תיקון ארכיטקטורה - מקור הנתונים של #sessionUserLabel
- **`#sessionUserLabel` הציג עד היום `user.displayName` (Google), לא `memberProfiles.name`** - שני מקורות נתונים נפרדים לגמרי. שינוי שם לא היה משפיע עליו בכלל אם היה נכתב רק ל-`memberProfiles`. תוקן: `currentProfile?.name || user.displayName || user.email` - מעדיף את השם הניתן-לעריכה של הטיול הנוכחי, נופל לשם מ-Google לפני שטיול נטען. **הבהרה של מה שהיה קיים תמיד, לא סיבוך חדש**: אין "שם גלובלי" במודל הנתונים - `memberProfiles` הוא per-trip מאז ומתמיד, פשוט לא היה עורך עד עכשיו
- **`syncSessionUserLabel()` חדשה, נקודת קריאה אחת** לעדכון התווית + נראות העיפרון - `currentProfile` ב-`index.html` נטען עם `.once("value")`, לא מאזין חי (בשונה מ-`renderLinkedMembers()`, שמתעדכן לבד מהמאזין החי של הטיול). נקראת מ-4 מקומות: `handleAuthStateChanged`, סוף `loadMemberProfile()`, סוף `claimMember()` (מסלול הבחירה הידני החלופי), ואחרי שינוי שם מוצלח

### 🎨 ellipsis בכל מקום שמציג שם (למעט אחד, בכוונה)
- **תוקן**: `#sessionUserLabel` (index, לא היה שם כלל CSS ייעודי קודם), `renderLinkedMembers()` (index, פאנל מנהל - נמצא בסריקה, לא הוזכר בפרומפט המקורי), `.group-title` (packing, כותרת "פריטי X" בתצוגה משותפת), `.comment-owner` (places, תגובות+תשובות), `.photo-info`/figcaption (places, מי העלה תמונה - הכי מסוכן, יושב מעל thumbnail מרובע קטן), `.activity-owner-name` (itinerary - **היה `flex:0 0 auto` בלי שום `overflow`/`max-width`, לא נגלש/נחתך, פשוט דוחף את הכרטיס לרוחב - אומת בפועל: קלף התרחב במקום להישבר. תוקן `flex:0 1 auto;min-width:0` + ellipsis, אותו דפוס כמו `.activity-note` הסמוכה שכבר טופלה בעבר**
- **`.logout-link` (index.html, משותף ל-`darkModeToggle` ו-`logoutLink`) קיבל `flex-shrink:0;white-space:nowrap`** - בלעדיו, ellipsis על `#sessionUserLabel` לא הספיק: שם ארוך עדיין דחק את שני הכפתורים האלה לכיווץ, הטקסט שלהם עטף שורה, וה-`.session-row` גדל לשתי שורות (32px → 52px, נמדד בפועל). התיקון האמיתי היה לתת לשני הכפתורים גודל קבוע כדי ש-`#sessionUserLabel` יספוג את כל לחץ הרוחב לבדו - ellipsis לבדו לא היה מספיק
- **הושאר בכוונה, בלי ellipsis**: `.item-by`/`.item-buyer` ב-shopping.html - `.item-sub-info` הוא `flex-wrap:wrap` כבר, אז שם ארוך עוטף שורה במקום לשבור פריסה, והמידע נשאר קריא במלואו. ellipsis שם היה מסתיר מידע (מי הוסיף/מי קונה) בלי תועלת פריסתית שה-wrap כבר לא נותן. תועד בהערת קוד כדי שלא "יתוקן" בטעות לעקביות
- **RSVP באיתוד (itinerary.html) לא רלוונטי לסעיף הזה** - ה-UI מציג רק מספר מגיעים ("3 מגיעים ✓"), לא שמות. `joinerNames` (שדה שקיים ב-RTDB) לא נקרא בשום מקום באפליקציה

נבדק בפועל: זרימת העריכה המלאה (שינוי, ללא-שינוי, ריק+Enter, ריק+blur, כשלון כתיבה) הורצה ישירות מול הפונקציות בדפדפן חי - `PERMISSION_DENIED` אומת כצפוי (אותה מגבלת סביבה מתועדת). `.session-row` נמדד בפועל לפני ואחרי תיקון ה-`flex-shrink` (52px→32px עם שם ארוך). כל 5 מיקומי ה-ellipsis אומתו במדידת `scrollWidth`/`clientWidth` וחזותית (צילום מסך) עם שם ארוך מכוון. עברית ואנגלית, בהיר וכהה, 375px. `node scripts/i18n-audit.js` על 5 הקבצים - `#editNameBtn` נמצא כממצא חדש (aria-label/title מקודדים בלי תרגום), תוקן דרך פונקציית השפה הקיימת (אותו דפוס כמו `adminGearBtn`), אומת נקי אחרי התיקון. קונסול נקי מלבד שגיאות הרשת הצפויות. **לא אומת מקצה-לקצה מול RTDB אמיתי** (אותה מגבלת SDK) - מומלץ לבדוק בפרודקשן.

## v4.32.1 — מצבים ריקים: שיהיה ברור שזה כפתור

### 🎨 שיפור ויזואלי
- **6 מצבי הריק מ-v4.31.0 היו כפתורים אמיתיים, אבל בלי שום סימן ויזואלי שזו לחיצה** - נראו כמו טקסט במרכז מסך ריק. שלושה תיקונים: **א.** מסגרת מקווקוות עדינה סביב כל אזור (דפוס מוכר מהעלאת קבצים - "משהו יכול להיכנס לכאן"). **ב.** שורת CTA בתוך הכפתור ("+ הוסף פריט" וכו', משתנה לפי עמוד) שנראית כמו כפתור אמיתי - אבל היא `<span>`, לא `<button>` מקונן (קינון כפתורים בתוך כפתור לא תקין ב-HTML ומבלבל קוראי מסך; האלמנט האמיתי הלחיץ הוא כל הקובייה). **ג.** אדווה - `animation-iteration-count:3`, בלי JS, בלי localStorage. עוצרת מעצמה ואינה חוזרת בלי re-render של האלמנט (בניגוד ל-`adminGearPulse` ב-index.html, שיש לו זיכרון בין ביקורים דרך `localStorage`/`.admin-gear-seen` - כאן אין זיכרון בכלל, וזה מכוון: מצב ריק יכול לחיות ימים, ואדווה אינסופית שרואים שוב למחרת הופכת מהזמנה לרעש)
- **`prefers-reduced-motion: reduce` מכבה את האדווה לגמרי** בכל 4 הקבצים - המסגרת ושורת ה-CTA נשארות

### 🔀 shopping.html שונה בכוונה - בלי שורת CTA
- **`#openModalBtn` ("➕ הוסף פריט") אינו FAB צף - הוא כפתור אמיתי ב-`.main-actions-row`, ממש מעל `#categoriesContainer`** שבו מוצג המצב הריק. שורת CTA זהה ("+ הוסף פריט") ממש מתחתיו הייתה נראית ככפתור כפול/שבור, לא כרמז. shopping.html מקבל רק מסגרת מקווקוות (כבר הייתה שם, `.empty-state`) ואדווה - בלי שורת CTA. **תועד בהערת קוד ב-shopping.html במפורש** כדי שהחוסר-עקביות הזה לא "יתוקן" בטעות בעתיד

נבדק בפועל: מבנה/CSS מחושב אומת בכל 6 המקומות (`animation-name`/`animation-iteration-count`/`border-style` נכונים דרך `getComputedStyle`), קליק עכבר על פינת האלמנט (לא על שורת ה-CTA) הפעיל את ה-handler - מאמת שהקובייה כולה לחיצה, לא רק השורה. עברית+אנגלית, בהיר+כהה, 375px, שלושת הטאבים ב-places.html. **מגבלת בדיקה חדשה שנתפסה כאן, מתועדת ב-CLAUDE.md**: לטאב ב-Browser pane אין לולאת רינדור רציפה בזמן שהוא לא נצפה ישירות (`requestAnimationFrame` לא יורה, `getAnimations().currentTime` קפוא) - אי אפשר לאמת מקצה-לקצה בזמן אמת שהאדווה באמת נעצרת אחרי 3 חזרות ולא רק דרך `getComputedStyle` הסטטי. `animation-iteration-count` היא תכונת CSS בסיסית ותקנית לגמרי - אין סיבה אמיתית לחשוד בהתנהגותה בדפדפן אמיתי, אבל זו אינה אימות מדוד כמו שהצ'ק ליסט דורש.

## v4.32.0 — shopping.html: buyer ו-personalQtyMap עוברים משם ל-uid (commit ব2)

### ⚠️ שינוי סכמה, שבירת תאימות מכוונת
- **`buyer` (מי אחראי לקנות פריט) ו-`personalQtyMap` (מפתח הבקשה האישית) היו שמורים לפי שם, לא לפי `uid`** - בדיוק התרחיש שזוהה אחרי commit ব1: רינוי שם משאיר פריטים ישנים עם השם הקודם, שנראה כמו שני אנשים שונים. שני השדות עכשיו לפי `uid` - עקבי עם `joiners` ב-`itinerary.html` ועם הנתיב הפרטי ב-`packing.html` (`packing/users/{uid}`), שכבר בנויים ככה
- **לא נשמרה מיגרציה לנתונים קיימים - החלטה מודעת.** כל הטיולים כרגע הם טיולי בדיקה. פריט ישן עם `buyer`/`personalQtyMap` לפי שם לא נשבר: `memberDisplayName(uid)` (חדש) נופל ל-`tr('חבר/ה לקבוצה', 'a group member')` עבור כל מפתח שלא תואם `memberProfiles` חי - כולל `uid` אמיתי שכבר לא קיים וגם מחרוזת שם ישנה מלפני המעבר, אותו נתיב בדיוק. הפריט מוצג כלא-שלי לכל הצופים (`isUserBuyer`/`isUserWanting` לעולם לא יתאימו למחרוזת שם ישנה), לא כתקוע או שבור

### 🔧 מה זז
- **`toggleClaimItem`/`openPersonalQtyModal`/`savePersonalQtyBtn`/`removePersonalQtyBtn`**: כותבים/משווים לפי `currentUser.uid` במקום השם שמחזיר `requireUserName()`. `requireUserName()` עצמו לא השתנה - עדיין שער הטעינה (`currentProfile?.name`), עדיין המקור לתווית `item.by` ("הוסיף: X") שנשארת לפי שם בכוונה (תמונת מצב תיאורית, לא בדיקת זהות - אותה החלטה כמו `ownerName` בעמודים האחרים)
- **`render()`**: המשתנה המקומי `myName` הוחלף ב-`myUid` (`currentUser?.uid`) בשלוש הבדיקות (`isUserWanting`/`isUserBuyer`/חיפוש הכמות האישית שלי)
- **`memberDisplayName(uid)` חדש** - פותר `uid` לשם תצוגה חי מ-`tripData.memberProfiles`, אותו דפוס בדיוק כמו ה-fallback הקיים כבר ב-`packing.html`'s `importItem()` (`trip.memberProfiles?.[uid]?.name || ...`). נקרא משלוש נקודות: תווית "🛒 אחראי" ברינדור, וייצוא הרשימה לטקסט (`generateExportText`) - שתיהן הציגו את `buyer` הגולמי קודם, מה שהיה מדפיס `uid` גולמי לטקסט משותף לוואטסאפ בלי התיקון הזה
- **נסרק כל הקובץ לשדות אחרים ששומרים שם** - נמצא רק `item.by` (תווית "הוסיף: X"), ואומת שאף קוד לא משווה אותו ל-`===` לזיהוי - תווית תצוגה בלבד, נשאר כפי שהוא

נבדק בפועל: `memberDisplayName` - 4 מקרים (uid ידוע, uid לא ידוע/"עזב", מחרוזת שם ישנה מלפני המעבר, מחרוזת ריקה) הורצו מול הפונקציה החיה בדפדפן (הוזרק hook זמני `window.__test`, אומת ידנית, הוסר ואומת שהוסר) - כל הארבעה נופלים בדיוק כמתוכנן. קונסול נקי מלבד שגיאות הרשת הצפויות. `node scripts/i18n-audit.js shopping.html` - אותם 3 ממצאים ידועים, 0 חדשים. **לא אומת מקצה-לקצה מול RTDB אמיתי** (אותה מגבלת סביבה מתועדת מ-commit ব) - מומלץ לבדוק בפרודקשן.

## v4.31.0 — מצבים ריקים: מהודעה להזמנה

### ✨ תכונה חדשה
- **6 מצבים ריקים הופכים להזמנה לחיצה, במקום הודעת טקסט פסיבית**: `packing.html` (ריקה לכל קטגוריה), `shopping.html`, `places.html` (מקומות, גלריה, מוזיקה), `itinerary.html` (יום ריק). כל אחד עכשיו `<button>` אמיתי (לא `div` עם `onclick`) שקורא לאותו handler בדיוק שה-FAB/כפתור ההוספה של אותו עמוד פותח - לא מסלול מקביל. `packing.html`: `emptyActionBtn.onclick` מפעיל `$("addItemFab").click()` (שומר על הלוגיקה הנלווית - בניית select הקטגוריות כש-`activeList==="all"`, רמז תיוג רגל). `shopping.html`: `openModalBtn.click()`. `places.html`: `fabAction()` (מנתב לפי הטאב הפעיל - מקומות/גלריה/מוזיקה). `itinerary.html`: `$('fab').click()`
- **הנוסח עצמו: שאלה במקום הצהרה, ואז מה עושים, עם רמז שהפעולה משותפת** - למשל "מה לוקחים לברלין?" במקום "הרשימה עדיין ריקה". ברגע שיש פריט אחד, המצב הריק נעלם לגמרי - אין שני כפתורים מתחרים (הכפתור עצמו + ה-FAB)
- **`getCurrentLegName(trip)` חדש ב-`legs.js`** - איזה יעד רלוונטי "עכשיו" לצורך נוסח: הרגל הפעילה, נופלת לרגל הראשונה לפני שהטיול מתחיל או לאחרונה אחריו (אותה שרשרת נפילה בדיוק כמו `getCurrentLegCoords` הקיימת - לא הומצאה מחדש). `packing.html`/`places.html` משתמשות בה לנוסח הדינמי; טיול בלי שם יעד בכלל מקבל נוסח חלופי בלי השם ("מה לוקחים לטיול?")

### 🔒 נשאר טקסט, במפורש
- **"עדיין לא שותפו פריטים" (packing.html, `renderGroup()`) נשאר `div` פסיבי, לא כפתור** - שיתוף הוא toggle על פריט קיים (🌐/🔒 בשורת הפריט), אין פעולה יחידה טבעית להצביע עליה ממסך ריק. יש `makeListPublicBtn` (שיתוף כל הקטגוריה בבת אחת) אבל הוא פעולה גורפת בתפריט משני - לא נכון לקשר אליו מכאן
- **"אין תוצאות לפילטר" (places.html/shopping.html/itinerary.html) לא נגעו בהם** - זרימה נפרדת לגמרי, עם כפתור "הצג הכל" משלה; המשתמש יודע בדיוק מה קרה, זה לא "ריק" במובן שהפרומפט הזה מטפל בו

נבדק בפועל: `getCurrentLegName` - 4 מקרים (טיול רגל-אחת, בלי יעד, רגל פעילה באמצע טיול מרובה, לפני תחילת טיול מרובה) הורצו מול הפונקציה החיה בדפדפן, כולם נכונים. מרקאפ כל 6 הכפתורים אומת חזותית (עברית+אנגלית, בהיר+כהה, 375px) - `button` אמיתי, לא `div`, גובה מגע 168px בפועל (מעל 44px המינימום). `node scripts/i18n-audit.js` על 4 הקבצים - אותם 3 ממצאים ידועים מראש, 0 חדשים. קונסול נקי בכל 4 העמודים מלבד שגיאות הרשת הצפויות בסביבה הזו.

## v4.30.2 — תיקון shopping.html: זהות הייתה תלויה ב-<select> שלא התעדכן (commit ב1)

### 🐛 תיקון
- **`#myName` היה `<select>`, לא שדה טקסט - וזה מה ששבר את auto-register מ-commit ב.** `requireUserName()` ו-`render()` קראו את הזהות מ-`myNameInput.value` (ערך ה-DOM שנבחר), לא מ-`currentProfile`. האופציות של הבורר נבנו אך ורק מ-`tripData.participants` (הרשימה הקבועה שהמארגן הקליד ב-wizard.html) - ושם שנקבע ע"י auto-register (מה-Google identity) כמעט תמיד לא נמצא באותה רשימה. הצבת `.value` על `<select>` לערך בלי `<option>` תואם היא **no-op שקט** ב-DOM: הבורר נשאר על הפלייסהולדר הריק, ו-`requireUserName()` חסם כל פעולה לצמיתות - נמצא בבדיקה עם חברים אמיתיים בפרודקשן (packing/places/itinerary עבדו, shopping לא)
- **`#myName` הפך ל-`<span>` לקריאה בלבד**, לא select מנוטרל - אין יותר "בחירה מרשימה", הזהות מגיעה כולה מ-`currentProfile.name`. `requireUserName()`/`render()` (3 שימושים: `isUserWanting`/`isUserBuyer`/`personalMap` lookup) קוראים עכשיו `currentProfile?.name` ישירות
- **`requireUserName()` נשאר, בתפקיד מצומצם**: לא יותר "תבחר שם", אלא רשת ביטחון להבדל התזמון הצר בין טעינת הדף לרגע ש-auto-register מסיים (`currentProfile` עדיין `null`) - טוסט מעודכן ("עדיין טוען את הפרופיל שלך") במקום ההודעה הישנה שהפנתה לבורר שכבר לא קיים

### 🗑️ הסרת קוד legacy (לא רק תיקון)
- **הוסר ענף `!tripRoot` שלם ב-`shopping.html`**: הודעת "מצב תאימות" + בורר עם 9 שמות מקודדים בקוד (שריד מלפני שהאפליקציה תמכה בטיולים מרובים - חבורת חברים ספציפית, לא רלוונטית לאף טיול פעיל). כל טיול היום נוצר עם `tripId`, ו-`index.html` בלי `tripId` מפנה למסך בחירת טיול - הענף בלתי־נגיש, ואם כן היה נגיש, בורר עם שמות של חבורה אחרת גרוע מכלום
- **`renderParticipants()` נמחקה כולה** - כל שלושת הקריאות אליה (משתי נקודות בזרימת ה-auto-register, ומהענף שהוסר) התייתרו יחד עם ה-`<select>` שהיא מילאה
- **נסרקו כל שאר העמודים לשאריות `!tripRoot`/`!root` דומות** - לא נמצא דבר להסרה: `packing.html`'s "Missing tripId" toast ו-`index.html`'s הפניה למסך בחירת טיול הם התנהגות נוכחית ומכוונת (לא שרידי legacy), לא נגעתי בהם. `itinerary.html`/`places.html` נבנו אחרי שהמערכת הרב-טיולית כבר הייתה קיימת - אין להם ענף מקביל בכלל

נבדק בפועל: מרקאפ ה-`<select>` נעלם מה-DOM (אומת), ה-`<span>` מציג שם כשמוזרק ידנית (אומת חזותית, זהה בגובה/מיקום ל-select הישן - אין תוספת שורה). קונסול נקי מלבד שגיאות רשת הצפויות בסביבה הזו. `node scripts/i18n-audit.js shopping.html` - אותם 3 ממצאים קיימים מראש (כפתורי ייצוא, כבר מתועדים כדחייה מכוונת ב-v4.13.3), 0 חדשים. **לא אומת מקצה-לקצה מול RTDB אמיתי** (אותה מגבלת SDK מודולרי כמו commit ב) - ממתין לבדיקת המשתמש בפרודקשן.

## v4.30.1 — הרשמה אוטומטית בארבעת עמודי המשנה (commit ב מתוך 3)

### 🐛 תיקון
- **`packing.html`/`places.html`/`itinerary.html`/`shopping.html`**: אותה חסימה מוחלטת ש-commit א תיקן ב-`index.html` תוקנה עכשיו גם בארבעת העמודים האחרים - כל אחד החזיק `#memberOverlay`/`#memberGate` משלו עם אותה רשימה סגורה חוסמת. `loadProfile()`/`loadMemberProfile()` בכל עמוד קוראות עכשיו ל-`window.autoRegisterMember()` (מ-`member.js`, לא שוכפל) כשאין פרופיל קיים
- **`#memberOverlay`/`#memberGate` נמחקו פיזית מהמרקאפ, לא הוקפאו** - כולל כל הפונקציות שהתייתמו: `participantKey` המקומי (הוחלף ב-`window.participantKey`, ואז גם זה התייתר - `member.js` מחשב את מפתח התביעה פנימית, אף עמוד לא צריך לקרוא לזה יותר), `profileOptions`/`renderParticipants`'s חצי ה-`memberSelect`, ו-`saveProfile`/`claimMember` - הפכו לקוד מת (אין UI שקורא להם) בכל עמוד חוץ מ-`packing.html`, שם `#participantSelect` (אלמנט שונה, תמיד גלוי, לא הגייט) עדיין משתמש ב-`saveProfile` כנתיב חלופי אם מישהו בוחר שם ידנית - **לא נמחק, לא נדרש למחיקה**
- **כשלון בהרשמה האוטומטית מציג טוסט במפורש, לא נשאר שקט** - עם הגייט הישן שהיה חוזר בטיימאאוט (8 שניות) כרשת ביטחון עצמה נעלמת, כשלון (טיימאאוט, או ההרשמה עצמה נכשלת) חייב הודעה כלשהי במקום מסך שנשאר תקוע בלי הסבר - זה בדיוק הדפוס ש-CLAUDE.md כבר מתעד כחוזר בפרויקט הזה
- **הכתיבה ל-`users/{uid}/trips/{tripId}`** (כדי שהטיול יופיע בבורר הטיולים של המצטרף ב-index.html - נכתבה עד היום רק מ-`saveProfile`/`claimMember` הישנים) **נשמרה במלואה בנתיב ההרשמה האוטומטית החדש** בכל 4 העמודים - best-effort, לא חוסמת, בדיוק כמו במקור

### ⚠️ לא בהיקף - נמצא תוך כדי, מתועד ולא תוקן
- **`index.html` (commit א) לא כותבת `users/{uid}/trips/{tripId}` בנתיב ההרשמה האוטומטית שלה** - ה-`claimMember` הישן של `index.html` מעולם לא עשה את זה גם הוא (רק 4 עמודי המשנה עשו, מ-v4.8.0). כלומר מצטרף שנוחת ישירות על `index.html` (בדיוק המסלול שההרשמה האוטומטית נבנתה בשבילו) לא יראה את הטיול בבורר הטיולים שלו. פער אמיתי, לא תוקן כאן - `index.html` לא בהיקף של commit ב

נבדק בפועל: מרקאפ הגייט נעלם פיזית משני ה-4 עמודים (`#memberOverlay`/`#memberSelect`/`#saveMemberBtn` ב-3, `#memberGate`/`#memberGateSelect`/`#claimMemberBtn` ב-shopping) - אומת ב-DOM בדפדפן, לא רק בקוד. קונסול נקי (מלבד PERMISSION_DENIED הידוע) בכל 4 העמודים. **מגבלת בדיקה אמיתית**: בניגוד ל-index.html (Firebase compat, `firebase.database.Reference.prototype` ניתן לדריסה זמנית מהקונסול ואומת קצה-לקצה מול קריאות Firebase מדומות) - 4 העמודים האלה על ה-SDK המודולרי (v9), שאין לו גלובל שניתן לדרוס באותה צורה; `ref`/`get`/`set`/`runTransaction` הם imports מקומיים למודול, לא נגישים מבחוץ. אומת חלקית: 14 בדיקות היחידה על `member.js` עצמו (מ-commit א, SDK-אגנוסטי), התאמה מדויקת בין תחביר קריאות ה-Firebase שנכתב לכאן לתחביר הקיים שכבר עבד ב-`saveProfile`/`claimMember` המקוריים (אותם דפוסי `runTransaction`/`set` בדיוק, לא הומצא תחביר חדש), והרצה אמיתית מול הכלים החיים (נכשלת ב-Permission denied כצפוי בסביבה הזו - מוודאת שהנתיב מגיע לקוד הנכון, לא שהכתיבה מצליחה). **לא אומת מקצה-לקצה מול RTDB אמיתי** - מומלץ לבדוק בפרודקשן לפני שממשיכים ל-commit ג

## v4.30.0 — הרשמה אוטומטית במקום בחירת שם (שלב חוויית המשתמש הראשון, commit א מתוך 3)

### ✨ תכונה חדשה
- **`member.js` חדש** - שכבה משותפת באותה תבנית כמו `legs.js`/`datetime.js`: script קלאסי, לא module, נטען עכשיו ב-`index.html` (5 העמודים המגודרים בהרשאות יטענו אותו ב-commit ב). מכיל `participantKey()` (הועברה, לא שוכפלה) ו-`autoRegisterMember()` חדשה - לוגיקת ההחלטה בלבד (חישוב שם, תביעה, כתיבת פרופיל, נסיגה בכשלון), בלי קריאות Firebase ישירות משלה - `index.html` הוא compat v8 (`db.ref(path).once/transaction`), 4 מתוך 5 העמודים האחרים הם modular v9 (`get`/`runTransaction` כפונקציות עצמאיות) - אותו הבדל שכבר מתועד ב-CLAUDE.md. כל עמוד מזריק את פעולות ה-Firebase שלו (`tryClaim`/`writeProfile`/`rollbackClaim`) לפי הטעם שלו; הקובץ המשותף שולט רק ברצף
- **`index.html`**: מי שנכנס לטיול בפעם הראשונה כבר לא רואה חלונית בחירת שם מרשימה סגורה. `loadMemberProfile()` קוראת ל-`autoRegisterMember()` כשאין פרופיל קיים - שם מ-`displayName`/`email`/נפילה ל'משתתף', בלי מחסום. `#memberGate` נשאר במרקאפ כרשת ביטחון בלבד - אם ההרשמה האוטומטית עצמה נכשלת (שגיאת רשת, אי-התאמת כלל בלתי צפויה), הוא עדיין מוצג במקום להשאיר את המשתמש תקוע בלי שום אינדיקציה
- **התביעות (`participantClaims`) נקבעות עכשיו לפי `participantKey(uid)`, לא לפי `participantKey(name)`** - ההכרעה המרכזית של השינוי. הרשימה של המארגן הייתה המשאב הנדיר בעבר; בלעדיה, ייחודיות שם היא חסימה מלאכותית. שני משתמשים אמיתיים בשם "דני כהן" נרשמים שניהם בהצלחה, בלי התנגשות, כי ה-uid שלהם תמיד שונה - אומת ב-14 בדיקות יחידה (Node, בלי דפדפן - הקובץ לא תלוי ב-DOM/Firebase בכלל)
- **נבדק שזה לא שובר שלושה צרכנים במורד הזרם** ש-`participantClaims`/`participantKey` כבר משמשים: `renderAdminTags()` קוראת רק מ-`groupMembers` (הרשימה, לא נוגעת); `renderLinkedMembers()` (רשימת "מי הצטרף" בפאנל האדמין) קוראת ישירות מ-`memberProfiles` לפי uid, לא חוצה-מפנה ל-`participantClaims` בכלל; `releaseMember()` קורא את `profile.participantKey` **המאוחסן** כדי לדעת מה לנתק, לא מחשב אותו מחדש מהשם - שלושתם כבר מתייחסים ל-`participantKey` כמפתח אטום, לא כמשהו שמניח שהוא נגזר משם

נבדק בפועל: 14 בדיקות יחידה על `member.js` בבידוד (Node, ללא דפדפן) - שם מ-displayName, מפתח תביעה נגזר מ-uid ולא משם, שני uid-ים שונים עם אותו displayName נרשמים שניהם בלי התנגשות, נפילה ל-email כשאין displayName, נפילה ל-fallbackName כששניהם חסרים, שגיאת writeProfile מפעילה rollback ומועברת הלאה, מפתח בטוח ל-RTDB גם לשם עברי. **הזרימה המלאה ב-`index.html` נבדקה מקצה לקצה** - לא רק הקובץ המשותף בבידוד: `firebase.database.Reference.prototype` (once/transaction/set) נדרסו זמנית כדי לדמות תגובת Firebase אמיתית בלי חיבור אמיתי (שאינו זמין בסביבת הבדיקה המקומית) - משתמש חדש עם displayName אמיתי עבר את `handleAuthStateChanged`→`loadMemberProfile`→`autoRegisterMember` בפועל, נכתב `participantClaims/{base64url(uid)}` ואז `memberProfiles/{uid}` עם הערכים הנכונים, והשער התכסה; משתמש עם פרופיל קיים - `autoRegisterMember` לא נקרא כלל (בדיקת הרגרסיה), השער התכסה מהפרופיל הקיים בלבד. `node --check member.js` תקין. `node scripts/i18n-audit.js index.html member.js` - 0 ממצאים (אין טקסט משתמש חדש - `tr('משתתף','Traveler')` כבר קיים בפרויקט). **לא נבדק מול הכללים האמיתיים** - `ops.tryClaim`/`ops.writeProfile` בקוד האמיתי (לא בבדיקה) עדיין לא רצו מול RTDB חי; הכלל ל-`memberProfiles` תואם למה שנבדק בעבר (v4.8.0, מפתח מסוג `participantClaims/{key}` תואם) אבל לא אומת ספציפית מול השינוי הזה

## v4.29.0 — הצעות AI לכל רגל (שלב 12, commit ג מתוך 3 - האחרון)

### ✨ תכונה חדשה
- **`packing.html`/`functions/index.js`**: כשמסנן היעד על "הכל" בטיול מרובה-רגליים, לחיצה על "הצע רשימה עם AI" מייצרת עכשיו קריאה לכל רגל (עם היעד/תאריכים/vibes שלה, והוראה מפורשת לא להציע פריטים כלליים) + קריאה נפרדת אחת לפריטים משותפים לכל הטיול (`scope:"shared"` חדש, פריט שרות אופציונלי בבקשה - השרת מתעלם ממנו לגמרי אם לא נשלח). מסנן על רגל ספציפית, וטיול רגל אחת, נשארים על נתיב הקריאה הבודדת מ-commit ב - ללא שינוי, לא נגעו
- **`functions/index.js`**: פרומפט חדש למקרה `scope:"shared"` - מבקש 8 עד 14 פריטים כלליים שרלוונטיים לאורך כל הטיול (לא תלויי אקלים/יעד ספציפי), עם `listName` נשמר בפרומפט כדי שה"כלליות" תישאר בתוך הקטגוריה הנוכחית (למשל ביגוד כללי, לא מסמכי נסיעה, כשמבקשים הצעות לרשימת ביגוד). `scope:"leg"` משתמש באותו פרומפט הקיים בדיוק + משפט אחד נוסף שאוסר פריטים כלליים. **פרומפט ברירת המחדל (`scope` לא נשלח) נשאר זהה תו-בתו** לזה שהיה - אומת בקוד השוואה ישיר (לא רק נומק) לפני הפריסה
- **`legId` נקבע בקוד לפי מקור ההצעה, לא לפי מסנן העמוד** - כל פריט שחוזר מקריאת רגל מסומן ב-`legId` שלה, פריטים מהקריאה המשותפת נשארים בלי שיוך. עד עכשיו ההצעות עברו כמחרוזות שטוחות בלי שום מטא-דאטה - שינוי מבני ל-`{name,legId}[]` דרך כל הצינור (render, דדופ, קבלה), לא רק בשכבת ה-fetch
- **התנגשות בין רגליים - שיוך במקום ניחוש**: כשאותו פריט מוצע באופן עצמאי משני מקורות שונים (שתי רגליים, או רגל והקריאה המשותפת), המיזוג יוצא בלי שיוך במקום לבחור מקור שרירותי - תועד בהערת קוד במפורש (ר' §7ב, `renderAiSuggestions`) כי זו לא בחירה מובנת מאליה. **אומת מול הפונקציה החיה, לא מדומה**: טיול בנגקוק+קנקון (שני יעדי חוף) - דרכון/בגד ים/קרם הגנה/משקפי שמש/כובע שמש/מטען נייד/מתאם שקע/מעיל גשם התנגשו בין שתי הרגליים בפועל, וגם מטרייה מתקפלת התנגשה בין רגל לקריאה המשותפת - כל המקרים יצאו בלי שיוך
- **`Promise.allSettled`** - כשלון בקריאה אחת לא מפיל את השאר. נבדק מכסת Gemini (Tier 1: RPM/TPM/RPD בסביבות 0.3% ניצול) לפני הבחירה במקבילי מלא, בלי השהיות או batching
- **כשלון חלקי - הודעה נפרדת, לא שקט**: ההצעות שהצליחו מוצגות תמיד, ובנוסף טוסט שאומר מה חסר - שם היעד כשרגל אחת נכשלה, מספר יעדים כשיותר. כל הקריאות נכשלות → אותו טוסט כללי שהיה קיים תמיד
- **טקסט הכפתור בזמן ריבוי קריאות שונה מהקריאה הבודדת** - "אוסף הצעות לכל היעדים..." / "Gathering suggestions for every destination..." במקום "חושב..." - נמדד בפועל שריבוי קריאות מקביליות לוקח פי 1.4-1.5 מקריאה בודדת (7-10 שניות, לא 5-7), לא זהה כמו שהונח לפני המדידה

נבדק בפועל: טיול רגל אחת - קריאה אחת, payload זהה בית-לבית (ללא `scope`); מסנן על רגל ספציפית בטיול מרובה - קריאה אחת, ללא שינוי מ-commit ב; מסנן "הכל" בטיול מרובה - 3 קריאות בפועל (2 רגליים + משותף) עם `scope` נכון בכל אחת; **הבדיקה המרכזית** - קבלת הצעה שמקורה ברגל ספציפית בזמן שמסנן העמוד עדיין על "הכל" כותבת את ה-`legId` של הרגל שייצרה (`leg_rome`), לא את מסנן העמוד - נלכד payload בפועל לפני הכתיבה; כשלון חלקי מדומה (רגל אחת נכשלת) - שאר ההצעות מוצגות, טוסט נפרד עם שם היעד; שתי רגליים נכשלות - טוסט עם מספר; כל הקריאות נכשלות - טוסט כללי. **הרצה חיה מול הפונקציה שנפרסה, לא מדומה, כמו שנדרש**: רומא (עירוני, קריר) מול בנגקוק (טרופי, חם) החזירו רשימות ברורות ושונות (רומא: ז'קט ג'ינס, צעיף כתפיים, חולצה מכופתרת; בנגקוק: בגד ים, כפכפי ים, מעיל גשם, כובע שמש) והפריטים המשותפים היו כלליים בתוך קטגוריית הביגוד (טי-שירטים, ג'ינס, פיג'מה, סוודר) לא ספציפיים ליעד - בדיוק מה שהתבקש. `console` נקי (מלבד PERMISSION_DENIED הידוע). `window.__test` הזמני (כולל לוגר זמני להוכחת מנגנון ההתנגשות) הוסר ואומת שהוסר. `node scripts/i18n-audit.js packing.html` - 0 ממצאים. `node --check functions/index.js` - תקין

## v4.28.5 — הצעות AI מתעלמות ממסנן היעד (שלב 12, commit ב מתוך 3)

### 🐛 תיקון
- **`requestAiSuggestions()` (`packing.html`) שלחה תמיד `window.getPrimaryLeg(trip).name` - הרגל הראשונה - ללא קשר למסנן היעד הפעיל.** מסנן על בנגקוק ולחיצה על "הצע רשימה" שלחה בקשה על ניו יורק (הרגל הראשונה בטיול) בלי שום סימן שמשהו לא בסדר - הצעות ליעד הלא נכון שנראות תקינות לגמרי. תוקן: `aiSuggestLeg()` חדשה פותרת את הרגל שהמסנן מצביע עליה בפועל, עם נפילה לרגל הראשונה כשהמסנן על "הכל" - אותו דפוס בדיוק שכל צרכן אחר של `activeLegFilter` בקובץ הזה כבר משתמש בו
- **`vibes`/`startDate`/`endDate` נלקחים עכשיו מאותה רגל, לא מ-`trip.vibes`/`trip.startAt`/`getLastLeg(trip).endAt`** - בקשה על בנגקוק צריכה לתאר את ה-vibes וטווח התאריכים של בנגקוק, לא של הטיול כולו. `leg.vibes` זהה כרגע בין כל הרגליים (הוויזרד עדיין כותב אותו מערך משותף לכולן - §3.3 במסמך), אז זה לא-שינוי בפועל להיום, אבל יקרא נכון כשלוויזרד יהיה קלט vibes per-leg בלי שיידרש תיקון נוסף כאן
- **טיול רגל אחת - אפס שינוי**: `aiSuggestLeg()` נופלת לרגל הראשונה בדיוק כמו היום (המסנן תמיד "הכל" בטיול כזה), וה-`leg` היחידה שמחזירה `getLegs()` כבר מכילה בדיוק את אותם `startAt`/`endAt`/`vibes` ש-`trip` עצמו מחזיק (המבנה הסינתטי הקיים ב-`legs.js`)
- **כפתור ההצעות נשאר פעיל כשהמסנן על "הכל" בטיול מרובה-רגליים - לא הושבת**, בניגוד להשבתה הקיימת כש-`activeList` (קטגוריה) על "הכל". החלטה מכוונת: ל"הכל" של הקטגוריות אין ברירת מחדל הגיונית (אי אפשר להציע לאיזו קטגוריה), אבל ל"הכל" של היעד יש - הרגל הראשונה - וההשבתה הייתה מרגיזה בלי סיבה אמיתית

### לא בהיקף (commit ג)
ריבוי קריאות (קריאה לכל רגל + קריאה משותפת), שיוך `legId` בקוד להצעות שהתקבלו, ומסנן "הכל" שמפעיל בקשה לכל רגל - כל אלה נשארים כמו שהם: קריאה בודדת אחת, בדיוק כמו היום.

נבדק בפועל: טיול רגל אחת - ה-payload שנלכד זהה בית-לבית לזה שהקוד הישן היה שולח לאותו אובייקט טיול (הושוו במפורש, לא רק נומק); מסנן על בנגקוק (טיול שתי-רגליים בדוי: ניו יורק → בנגקוק) - `destination` יוצא "Bangkok" עם התאריכים והvibes של הרגל ההיא; מסנן על "הכל" - `destination` יוצא "New York" (הרגל הראשונה); מסנן מפורש על ניו יורק - אותה תוצאה, לוודא שהפתרון עובד בשני הכיוונים. **בדיקה 4 - הרצה אמיתית מול הפונקציה שנפרסה (לא מדומה)**: בנגקוק החזירה "תרסיס נגד יתושים", "מעיל גשם", "סנדלי הליכה" (עונת הגשמים, טרופי); ניו יורק החזירה "נעלי הליכה", "ז'קט", "משקפי שמש", "אוזניות" - שתי רשימות שונות בבירור, כל אחת סבירה ליעד שלה. הוסר `window.__test` הזמני ואומת שהוסר. `node scripts/i18n-audit.js packing.html` - 0 ממצאים (אין טקסט משתמש חדש בשינוי הזה - רק זרימת נתונים)

## v4.28.4 — זיהוי חיתוך בהצעות AI (שלב 12, commit א מתוך 3)

### 🐛 תיקון
- **`functions/index.js`**: תגובת Gemini שנחתכה (`finishReason === "MAX_TOKENS"`) מזוהה עכשיו במפורש, לפני ניסיון `JSON.parse` ובנפרד ממנו - docs/schema-legs.md §7ב. עד כה חיתוך היה נופל לענף השגיאה הכללי (JSON לא תקין), נרשם ומדווח בדיוק כמו כל כשלון אחר - בדיוק הכשלון השקט שכבר עלה חודשים כש-`maxOutputTokens` היה 500. `logger.warn` נפרד (לא `logger.error` הכללי), והתגובה ללקוח כוללת `reason:"truncated"` כדי שהלקוח יוכל להבדיל
- **`packing.html`**: `requestAiSuggestions()` קוראת את גוף השגיאה לפני שהיא זורקת, ומציגה הודעה שונה כשהסיבה `truncated` - "התשובה נקטעה, נסו שוב" במקום "לא ניתן לקבל הצעות AI כרגע" הכללית. כל כשלון אחר (שגיאת רשת, JSON לא תקין מסיבה אחרת, תגובה ריקה) ממשיך להציג את ההודעה הכללית כפי שהיה
- **תיקון, לא תכונה** - שלב 12 (הצעות AI לכל רגל) עדיין לא התחיל. commit זה סוגר פער קיים שעלה בחקירה: תשתית זיהוי החיתוך שהמסמך דרש מעולם לא נבנתה, אף שהסיכון תועד. נסגר עכשיו, לפני שריבוי הקריאות (commit ג) יכפיל את מספר ההזדמנויות לאותו כשלון

נבדק: לוגיקת הענף החדש בשרת אומתה מבודדת ב-Node (4 מקרים - MAX_TOKENS, הצלחה רגילה, JSON פגום עם finishReason שאינו MAX_TOKENS שנשאר על הענף הישן, ו-MAX_TOKENS שבמקרה כן היה מתפרסר - נשאר מסומן כחיתוך בכל זאת כי הבדיקה על finishReason קודמת לניסיון הפרסור) - כל 4 עברו. `node --check functions/index.js` - תקין. בצד הלקוח, `fetch` דומה (mock) הרץ נגד `requestAiSuggestions()` דרך `window.__test` זמני: תגובת חיתוך מציגה את ההודעה הספציפית, 502 לא-חיתוך ושגיאת רשת ממשיכות להציג את ההודעה הכללית, ומסלול ההצלחה (הצעות מוצגות, ניתן לבחור) לא נפגע. `window.__test` הוסר ואומת שהוסר. `node scripts/i18n-audit.js packing.html` - 0 ממצאים. **לא נבדק מול Gemini עצמו** - זה חוזה חיצוני; דורש `firebase deploy --only functions` שלא בוצע, ממתין לאישור נפרד

## v4.28.3 — תיקון אמיתי: הפס הלבן ב-shopping.html (v4.28.2 לא הספיק)

### 🐛 תיקון
- **האבחנה ב-v4.28.2 (html בלי background) הייתה נכונה אבל לא מספקת** - המשתמש דיווח שהפס עדיין שם אחרי שהגרסה עלתה לפרודקשן, ותיאר אותו כ**לבן ממש**, לא כחול (מה ש-`theme-color` היה מייצר) ולא כהה. חקירה חוזרת, הפעם מול הדף החי (לא רק מקומי): `getComputedStyle(document.documentElement).backgroundColor` על shopping.html בפרודקשן החזיר `rgba(0,0,0,0)` - שקוף לגמרי, למרות ש-`background-image` (ה-gradient) כן היה שם
- **הסיבה: `background:<gradient>` בקיצור קובע רק `background-image`, אף פעם לא `background-color`.** `html{background:var(--page-bg-gradient)}` מ-v4.28.2 קבע gradient אבל השאיר את `background-color` בברירת המחדל שלו - שקוף. איפה שה-gradient (image layer) לא הגיע לצייר - נשאר שקוף לגמרי, לא צבע כלשהו, ולכן לבן (ברירת המחדל של הדפדפן). `packing.html`/`places.html`/`itinerary.html` לא נתקלו בזה כי ה-`html{background:var(--bg)}` שלהם הוא ערך צבע מלא, שכן נופל תחת `background-color` בקיצור - בדיוק ההבדל בין gradient לצבע מלא בהתנהגות הקיצור, לא ב"איך זה נמתח על הקנבס"
- **`shopping.html`**: `html` מקבל עכשיו גם `background-color:var(--bg-gradient-end)` מפורש, לצד ה-`background` הקיים - צבע אטום מתחת ל-gradient, לא רק תחתיו. `--bg-gradient-end` נבחר כי זה הצבע שה-gradient עצמו יציב ממנו והלאה (מ-40% ועד הסוף) - התאמה מדויקת לרוב הקנבס, וקרוב מאוד גם בקצה העליון
- **סקר את כל 6 העמודים** לאיתור מקום נוסף שבו `background` עם gradient בלבד יושב על אלמנט שאמור להיות שכבה אטומה (לא רק דקורטיבי מעל הורה אטום כבר) - `html`/`body`/מעטפת עמוד מלאה. נמצא רק המקום הזה. כל שאר ה-gradient-ים בכל 6 העמודים (כרטיסי hero, badges, פס התקדמות, אפקטי fade על תמונות/רצועות גוללות) יושבים על הורה כבר אטום, ולא נפגעים מהבאג הזה
- **`index.html`/`wizard.html`**: `window.syncHtmlBackground()` מ-v4.28.2 מעתיק `getComputedStyle(body).backgroundColor` - נבדק במפורש שזה לא אותו באג במסווה: ל-`body` בשני העמודים האלה אין gradient בכלל (`--bg-color`/`--background` הם ערכי צבע מלא בכל 8 השילובים שנבדקו), אז ה-`backgroundColor` שמועתק תמיד אטום. לא נדרש תיקון שם - נבדק, לא רק נומק

נבדק בפועל: כל 8 השילובים (4 סוגי טיול × בהיר/כהה) ב-shopping.html - `background-color` על `html` אטום ותואם בדיוק ל-`--bg-gradient-end` הנוכחי בכל אחד; index.html/wizard.html - `background-image` נשאר `none` בכל 8 השילובים, `background-color` אטום כפי שהיה; packing.html - עדיין אטום, ללא שינוי. אומת גם מול הדף החי בפרודקשן (github pages), לא רק מול שרת מקומי - כך בדיוק נמצא הפער בין v4.28.2 המקומי (שנראה תקין) לבין מה שהמשתמש ראה בפועל. קונסול נקי (מלבד PERMISSION_DENIED הידוע).

## v4.28.2 — תיקון: רקע לא מגיע לקצוות בנייד (safe-area)

### 🐛 תיקון
- **`shopping.html`/`index.html`/`wizard.html`**: ברקע `<html>` פינה שני קצות המסך (מעל התוכן ומתחת לתפריט/כפתורים) בנייד - האזורים שנחשפים ע"י `viewport-fit=cover` מאחורי אזור השעון/הבית ב-iOS. הרקע היה מוגדר רק על `body`, ו-`<html>` (שצובע את כל הקנבס, כולל האזורים האלה, לפי ספק ה-CSS) נשאר בברירת המחדל של הדפדפן. `packing.html`/`places.html`/`itinerary.html` כבר הגדירו את זה נכון על שני האלמנטים - **נבדקו במפורש ואומת שהם לא צריכים תיקון**
- **`shopping.html`**: `html{background:var(--page-bg-gradient)}` (משתנה חדש שמאחד את ה-gradient הקיים, לא שכפול) לצד ההגדרה הקיימת על `body` - כאן `[data-theme]`/`.theme-X` כבר על `documentElement`, אז CSS גרידא מספיק
- **`index.html`/`wizard.html` - תיקון שונה, לא רק אותה הוספת CSS**: בשני הדפים האלה `[data-theme="dark"]` (ו-ב-index גם `.theme-X`) יושבים על `document.body`, לא על `documentElement` - הפער היחיד מבין 6 הדפים בנקודה הזו (תועד עכשיו ב-CLAUDE.md). כלל CSS גרידא על `html` לא יכול לעקוב אחרי מצב שמוגדר רק על הילד שלו (custom properties לא עוברים בירושה כלפי מעלה). הפתרון: `window.syncHtmlBackground()` חדש - מעתיק את הרקע המחושב בפועל מ-`body` ל-`html`, בכל נקודה שבה מצב הדארק/סוג הטיול של `body` משתנה (הסקריפט הראשוני לפני הצביעה הראשונה, `applyTripPresentation()` ב-index, וכפתור החלפת מצב כהה בשניהם) - לא שכפול של הצבעים עצמם ב-JS, רק העתקה של מה ש-CSS כבר חישב. ל-`html` עצמו נשאר ערך ברירת מחדל סטטי (הצבע הבהיר של `:root`) כ-fallback לרגע שלפני שה-JS רץ

נבדק בפועל (קונסול, לא רק נומק): שמונה השילובים (4 סוגי טיול × בהיר/כהה) ב-shopping.html - `html`/`body` תואמים בכל אחד; index.html - כנ"ל דרך `applyTripPresentation()` המדומה וגם דרך כפתור מצב כהה האמיתי; wizard.html - בהיר/כהה וגם דרך כפתור מצב כהה האמיתי; packing/places/itinerary.html - נבדקו ונשארו תואמים ללא שינוי. `console` נקי משגיאות בשלוש הדפים ששונו (מלבד PERMISSION_DENIED הידוע בסביבת הבדיקה המקומית). **לא אומת במכשיר אמיתי** - הבדיקה בדפדפן מחשב לא מדמה safe-area-inset אמיתי (notch/home indicator), אז הפס עצמו לא נראה כאן; מה שכן נבדק ואומת הוא שה-`html`/`body` background מחושבים לזהות בפועל בכל שילוב, שזה בדיוק המנגנון שאמור לתקן את הפס במכשיר אמיתי

## v4.28.1 — בורר יעד בטופס המקום (השלמת שלב 9)

### ✨ תכונה חדשה
- **`places.html`**: בטופס הוספה/עריכה של מקום נוסף בורר יעד (`#placeLeg`), ליד בורר הקטגוריה - סוגר שני פערים: אין דרך לשייך מקום כשמסנן העמוד על "הכל", ואין דרך לשנות שיוך של מקום קיים. מודל על `editListSelect` מ-packing.html - `select` פשוט בתוך הטופס, לא פקד חדש ברמת העמוד
- **ביצירה: ברירת המחדל היא מסנן העמוד הפעיל; אם המסנן על "הכל" - ברירת המחדל "ללא שיוך". בעריכה: מציג את השיוך הנוכחי, ניתן לשנות לכל יעד או בחזרה ל"ללא שיוך"** - "ללא שיוך" הוא אפשרות מפורשת ברשימה (`value=""`), לא היעדר בחירה, כי מקום כזה מופיע בכל מסנן (התנהגות `renderPlaces()` הקיימת, ללא שינוי)
- **טיול רגל אחת - הבורר לא מוצג בכלל** - אותה בדיקה (`window.isMultiLeg`) כמו הבורר ברמת העמוד. אפס שינוי מבני
- **הרמז הקודם ("יתווסף ל-X") הוסר** - עם בורר מפורש בטופס עצמו, הרמז הפך למידע כפול שעלול לצאת מסנכרון עם הבורר שהוא מתאר. הקוד המת נמחק, לא הושאר מוסתר
- **`GEO_RULES` עוקב עכשיו אחרי בורר היעד של הטופס, לא אחרי מסנן העמוד** - `geoRefLeg()`/`categories()` מקבלות `legIdOverride` אופציונלי: קריאה בלי ארגומנט (כמו ב-`applyLanguage()`) ממשיכה לקרוא את מסנן העמוד בדיוק כמו היום; קריאה עם ערך מפורש (גם `""`) עוקפת אותו לגמרי. בחירת "רומא" בבורר הטופס מציגה "חוף 🏖️" כקטגוריה גם כשמסנן העמוד על "הכל" או על יעד אחר
- **קטגוריה שכבר נבחרת לא נעלמת בשקט כששינוי הבורר מסנן אותה גיאוגרפית** - שינוי בורר הטופס ליעד יבשתי בזמן שקטגוריית "חוף" כבר נבחרה משמר אותה כ"extra" ברשימת האפשרויות, באותו דפוס canon+extra שכבר קיים ב-`distinctPlaceCategories()` באותו קובץ - לא המצאה, הרחבה. **אומת בפועל**: מקום עם קטגוריית "חוף", עריכה ושינוי יעד לרגל יבשתית בלי לגעת בקטגוריה, שמירה - ה-payload שנלכד מראה `category:"חוף 🏖️"` ללא שינוי

### 🐛 תיקון
- **`savePlace()` כתבה `legId` רק ביצירה, ורק ממסנן העמוד** - ההערה הישנה תיעדה את זה כמצב קבוע ומכוון. עם בורר הטופס, ההגבלה בטלה: `legId` נכתב גם ביצירה וגם בעריכה, מהבורר בטופס. עריכה שמחזירה לבורר ל"ללא שיוך" כותבת `legId:null` במפורש (לא משמיטה את המפתח) - `update()` נוגע רק במפתחות שהוא מקבל, אותו לקח בדיוק מתיקון `legs:null` בפאנל הניהול

נבדק בפועל: טיול רגל אחת - הבורר מוסתר, אפס שינוי; יצירה במצב מסנן "הכל" - הבורר מאפשר שיוך, ה-payload שנלכד מראה `legId` שנבחר; יצירה כשמסנן פעיל - ברירת המחדל של הבורר תואמת את מסנן העמוד; עריכה - שינוי שיוך בשני הכיוונים (`leg1→leg2`, ואז `leg2→""` שנכתב כ-`legId:null`) - שני ה-payloads נלכדו בפועל; "ללא שיוך" - מקום בלי `legId` מופיע תחת כל מסנן עמוד; `GEO_RULES` עוקב אחרי בורר הטופס ולא אחרי מסנן העמוד - קטגוריית "חוף" מופיעה/נעלמת לפי בחירת הבורר בלבד, לא לפי מסנן העמוד; עברית ואנגלית, מצב כהה ובהיר, מסך נייד 375px - צולם מסך: שם היעד הנבחר קריא בבורר גם בלי הרמז שהוסר. הוסר `window.__test` הזמני ואומת שהוסר
`node scripts/i18n-audit.js places.html` - 0 ממצאים

## v4.28.0 — סינון מקומות לפי יעד (שלב 9)

### ✨ תכונה חדשה
- **`places.html`**: מקומות יכולים עכשיו להיות משויכים ליעד ספציפי (`legId`) ולהיות מסוננים לפיו - §5.4 במסמך. מסנן, לא רשימות נפרדות: מקום בלי `legId` ממשיך להופיע בכל מסנן
- **רק בטאב "מקומות" - לא תמונות, לא מוזיקה** - נבדק לפני כתיבה: למוזיקה כבר יש ציר סינון משלה (מקור - Spotify/YouTube וכו׳), ולשיר אין מושג מיקום בכלל. לתמונות אין שום UI סינון קיים היום ואין להן מטא-דאטה של מיקום - זה היה תוסף חדש, לא הרחבה. גם תואם את §5.4 עצמו, שמזכיר רק "מקומות"
- **`select` קומפקטי בתוך `.toolbar` הקיימת, לא שורה נוספת** - שדה החיפוש (`flex:1`) מצטמצם, הבורר (`flex:0 0 auto`) לצידו. **נמדד בפועל ב-375px לפני שהוחלט, לא רק הונח**: עם 4 יעדים והשם הארוך ביותר ("קואלה לומפור") נבחר, לשדה החיפוש נשארו 231px - נבדק חזותית (צילום מסך) שזה עדיין שדה קריא ונוח להקלדה, לא רק מדידת פיקסלים יבשה
- **מחלקות CSS נפרדות מ-packing.html (לא משותפות), עם הפניה הדדית** - `.place-leg-filter-select` לא `.leg-filter-select`: `min-height` שונה (46px כאן מול 42px שם) היה גורם להזזה של 4px אילו שותפו. `.leg-tag-hint` (האינדיקציה החזותית) כן זהה בערכים בין שני הקבצים (שני העמודים רשומים ל-`[data-tokens="core"]`) אבל נשאר כלל CSS נפרד בכל זאת - שלוש שורות משותפות בשני קבצים לא מצדיקות תלות בין-עמודית
- **מקום חדש עם מסנן יעד פעיל מקבל `legId` אוטומטית + אינדיקציה חזותית ("יתווסף ל-X")** - רק ביצירה, לא בעריכה, אותו היקף בדיוק כמו שלב 6. **תועד בהערת קוד**: אין דרך לשנות שיוך של מקום קיים - מקום שנוצר לפני שהיו רגליים, או כשהמסנן היה על "הכל", יישאר בלי שיוך לצמיתות. זה תקין לפי §5.4 (מופיע בכל מסנן), אבל מצב קבוע שכדאי להיות מודעים אליו, לא הפתעה
- **`openDayPicker` (הוספת מקום ליום) מסנן את הימים המוצעים לפי היעד של המקום** - מקום המשויך לבנגקוק כבר לא מציע ימי רומא. מספרי הימים ("יום N") נשארים הספירה המקורית של כל הטיול, לא ממוספרים מחדש בתוך קבוצת המשנה המסוננת - נבדק בפועל: מקום בנגקוק הציג "יום 5/6/7", לא "יום 1/2/3". רגל שנמחקה מאז (legId לא תקין) נופלת לכל הימים במקום בורר ריק - מקום בלי `legId` לא מושפע כלל

### 🐛 תיקון
- **`GEO_RULES` (מסתיר "חוף 🏖️"/"ספורט ים 🏄" ביעדים לא-חופיים) הייתה תמיד עוגנת לרגל הראשונה, ללא קשר לאיזה יעד פעיל בפועל - באג אמיתי, לא היפותטי, שנתפס תוך כדי הוספת המסנן.** בטיול שהרגל הראשונה יבשתית והבאה חופית, טופס הוספת מקום לא הציע "חוף" כקטגוריה גם כשמוסיפים מקום ליעד החופי בפועל. תוקן: `geoRefLeg()` חדשה משתמשת ברגל שתואמת את מסנן היעד הפעיל כשיש כזה, ונופלת לרגל הראשונה בדיוק כמו היום כשהמסנן על "הכל" - אפס שינוי לטיול רגל אחת או לתצוגה לא-מסוננת. **אומת בפועל**: טיול ציריך (יבשתית, ראשונה) ← רומא (חופית, שנייה, `vibes:["beach"]`) - מסנן "הכל"/ציריך לא מציע "חוף", מסנן רומא כן מציע - זה בדיוק התיקון
- **`window.hasValidCoords` (legs.js, v4.25.3) מגנה על רגל בלי קואורדינטות אמיתיות (geocoding כושל) בתוך `geoRefLeg()`** - לא כי `isNearCoast()` הייתה קורסת (היא כבר מטפלת ב-lat/lon לא-תקינים ומניחה "קרוב לחוף" כברירת מחדל בטוחה), אלא כי נפילה לקואורדינטות האמיתיות של הרגל הראשונה היא תשובה משמעותית יותר מברירת מחדל עיוורת. **אומת בפועל, לא רק נומק**: רגל עם `lat:null,lon:null` כמסנן פעיל - אין קריסה (`threw:null`), והתוצאה נופלת נכון לרגל הראשונה האמיתית (ציריך, יבשתית - "חוף" נשאר מוסתר, לא מוצג בטעות)

נבדק בפועל: טיול רגל אחת - הבורר מוסתר וריק, אפס שינוי מבני; טיול מרובה - הבורר מופיע; סינון - מקום משויך לרומא ומקום בלי `legId` שניהם מופיעים במסנן "רומא", מקום בנגקוק נעלם (ואותו דבר בכיוון ההפוך); מקום חדש - נלכד ה-payload בפועל לפני השליחה (`{legId:"leg_bkk"}`), לא רק נומק; `GEO_RULES` עדיין עובד ומתוקן לרגל הנכונה; רגל בלי קואורדינטות כמסנן פעיל - אין קריסה; `openDayPicker` מסנן נכון עם מספור ימים מקורי, ונופל לכל הימים על `legId` לא תקין; עברית ואנגלית; מסך נייד 375px. הוסר `window.__test` הזמני ואומת שהוסר
`node scripts/i18n-audit.js places.html` - 0 ממצאים

## v4.27.0 — סינון רשימת הציוד לפי יעד (שלב 6)

### ✨ תכונה חדשה
- **`packing.html`**: פריטי ציוד יכולים עכשיו להיות משויכים ליעד ספציפי (`legId`) ולהיות מסוננים לפיו - §5.1 במסמך. מסנן, לא רשימות נפרדות: מד ההתקדמות נשאר אחד לכל מה שמוצג, ופריט בלי `legId` ממשיך להופיע בכל מסנן
- **`select` קומפקטי, לא רצועת צ׳יפים שנייה** - נבדק לפני כתיבה מול רצועת הקטגוריות הקיימת: קטגוריות הן בלתי-מוגבלות ונוצרות ע"י המשתמש (בדיוק בשביל זה קיימת רצועה גוללת), רגליים חסומות ב-6 (`MAX_ADMIN_LEGS`) וברוב הטיולים 2-4. שורה שנייה קבועה הייתה עולה בצפיפות ויזואלית בדיוק במקום שהמשתמש ציין שהמסך כבר צפוף. הפתרון: `<select>` בתוך `.list-head` הקיימת - אפס גובה חדש, ותואם את המוסכמה הקיימת בקובץ הזה (`participantSelect`/`addItemCategorySelect`/`importCategorySelect`/`editListSelect` כבר משתמשים ב-`select` רגיל בדיוק לצורך הזה - "לבחור אחד מתוך רשימה קצרה, כשדה טופס" - לא ממציא דפוס אינטראקציה חדש (פופאובר מותאם) עבור הצורך הזה
- **נבדק בפועל ב-375px, כולל תרחיש הגרוע ביותר**: 4 יעדים (המקסימום המעשי) עם השם הארוך ביותר שנבדק ("קואלה לומפור") נבחר - השורה לא נשברת, לא גולשת, לא עוטפת. צפופה יותר מ-4 פקדים ל-5, אבל זו תחושה מדודה מפורשות ולא באג: נבדק `scrollWidth`/`clientWidth` (אין חריגה) לצד צילום מסך שהוצג למשתמש לאישור לפני שהומשך - כי רוב הטיולים הם רגל אחת, ושם ה-`select` כלל לא מוצג (ר' למטה)
- **טיול רגל אחת - הבורר לא מוצג בכלל, לא רק מנוטרל** - `window.isMultiLeg(trip)` (אותה בדיקה כבר בשימוש ב-index.html/itinerary.html, לא מומשה מקומית מחדש). אפס שינוי מבני
- **פריט חדש עם מסנן יעד פעיל מקבל `legId` אוטומטית + אינדיקציה חזותית** - כל שלוש נקודות ההוספה (הקלדה ידנית, אישור הצעת AI, אישור quick-pick) עוברות כולן דרך `addItem()` יחיד - התיוג נוסף שם פעם אחת ומכסה את כולן, בלי חיווט נפרד לכל אחת. האינדיקציה ("יתווסף ל{שם היעד}") מוצגת בגיליון ההוספה הידנית, לפני ההקלדה - לא נבנה רכיב חדש, נעשה שימוש באותו תפקיד טקסט-מעומעם-קטן שכבר ממלא `quick-picks-label` באותו גיליון
- **הצעות AI - תנאי הנטרול לא השתנה** (עדיין `activeList === "all"`, לא קשור ליעד) - וכיוון שהצעה מאושרת עוברת דרך `addItem()` הקיים, היא מקבלת את תיוג היעד באופן אוטומטי, בלי קוד נוסף

### 🐛 תיקון
- **פריט מיובא מחבר לא יורש את ה-`legId` של המקור - בכוונה, ומתועד למה** (§5.1 + חידוד מפורש): קטגוריה עוברת בייבוא כי היא סמנטית - "מסמכים" זה מסמכים לכולם. שיוך ליעד הוא הקשר אישי של מי שארז, לא עובדה משותפת - חבר שתייג פריט לרומא אולי נשאר שם יומיים נוספים; זה לא אומר שהמשתמש המייבא צריך את זה שם. הכלל: פריט מיובא מתויג לפי מסנן היעד הפעיל של **המשתמש המייבא**, אותו כלל בדיוק כמו פריט חדש שהוקלד - `importItem()` לא עוברת דרך `addItem()` (יש לה `push()` משלה מאז ומתמיד), אז זה חוט חיווט נפרד, לא קיבל את זה בחינם
- **נתפס באודיט ה-i18n, לפני push**: `aria-label` הסטטי של `#legFilterSelect` היה בעברית בלבד. תוקן באותו דפוס בדיוק שכל שאר כפתורי `.list-head` כבר משתמשים בו - עדכון ב-`applyLanguage()` דרך `tr()`, לא מחרוזת סטטית ב-HTML

נבדק בפועל: טיול רגל אחת - הבורר מוסתר וריק, 4 פקדים ב-`.list-head-actions` בדיוק כמו לפני; טיול מרובה - הבורר מופיע; סינון לפי יעד - פריט משויך לרומא ופריט בלי `legId` שניהם מופיעים במסנן "רומא", פריט בנגקוק נעלם; אותה בדיקה בכיוון ההפוך (מסנן בנגקוק); שני המסננים יחד (קטגוריה="ביגוד" + יעד="רומא") - התוצאה היא החיתוך של שניהם, כולל מד ההתקדמות שמשקף רק את מה שמוצג בפועל; פריט חדש עם מסנן יעד פעיל - נלכד ה-payload בפועל לפני השליחה (`{targetCategory:"main",legId:"leg_bkk"}`), לא רק נומק; עברית ואנגלית (כולל היפוך סדר ה-`select`/האייקונים ב-LTR); מסך נייד 375px. הוסר `window.__test` הזמני (הוצג פעמיים - לבדיקות הליבה ולבדיקת השפה הנוספת) ואומת שהוסר בשני הסבבים
`node scripts/i18n-audit.js packing.html` - 0 ממצאים (אחרי התיקון; 1 ממצא נתפס ותוקן לפני כן)

## v4.26.2 — מפריד בין רגליים בטאבי הימים (שלב 7, כולל 4ב שנדחה משלב 4)

### ✨ תכונה חדשה
- **`itinerary.html`**: רצועת טאבי הימים מסמנת עכשיו מעבר בין רגליים - חץ + שם היעד הבא, בין הטאב האחרון של רגל אחת לטאב הראשון של הבאה. §5.2 במסמך, מוזג לכאן מ-4ב שנדחה בשלב 4
- **`.day-leg-separator` חדשה - לא שכפול של `.forecast-leg-separator` (index.html, שלב 8 commit ג), למרות רעיון עיצובי זהה.** נבדק לפני כתיבה: הפריסה זהה (רצועת flex אופקית גוללת), אבל מערכת הצבע שונה מהותית - `.forecast-strip` יושבת על כרטיס תמיד-כהה (`color:#fff` קשיח, נכון שם), `.day-tabs` יושבת על המשטח של האתר עצמו (`[data-tokens="core"]`, `var(--muted)`). **אומת חזותית, לא רק נומק**: צילום מסך במצב בהיר עם הצבעים הקשיחים של index.html היה מציג טקסט לבן כמעט בלתי-נראה על רקע בהיר - זו ההוכחה הקונקרטית לכך שהחלטה לשכפל ולא לחלץ הייתה נכונה, לא רק עקרונית. `--glow-color` (CLAUDE.md) הוא התקדים המתועד למקרה הזה - שתי מערכות צבע אמיתיות, לא אותו דבר שנשכח בשני מקומות
- **חץ הכיוון גם הוא לא חולץ** - `legRouteArrow()` (index.html) פונקציה בת שורה אחת בסקריפט הקלאסי של אותו עמוד, לא נגישה מ-`itinerary.html` (עמוד HTML נפרד לגמרי) גם אם הייתה מיוצאת. וגם: זו טרנרי יציב-לנצח (שתי שפות בלבד, לא ישתנה) - לא באותה קטגוריית סיכון כמו `zonedParts` (15 שורות, מקרה קצה אמיתי) שהצדיק את יצירת `datetime.js`. `itinerary.html` כבר כותב `trip.language==='en'` inline ממש ליד (לפורמט התאריך) - אותו דפוס מקומי, לא קובץ משותף חדש בשביל טרנרי אחד
- **תיעוד הפיצול בשני הכיוונים** - הערת קוד ב-`index.html` וב-`itinerary.html`, כל אחת מפנה לשנייה ומסבירה שזה פיצול מכוון. **גם ב-`CLAUDE.md`, ליד ההערה הקיימת על `--glow-color`** - בעוד חצי שנה מישהו שרואה שתי מחלקות כמעט-זהות עלול לחשוב שזו כפילות לניקוי; איחוד ישבור את המצב הבהיר באחד מהעמודים
- **המפריד אחרי יום המעבר, לא לפניו (§5.2, בדיקה 4)** - `tripDayDates()` (legs.js, כבר משלב 4א) כבר משייכת את יום המעבר לרגל **היוצאת** ("יום המעבר שייך לרגל היוצאת"), אז תנאי ההשוואה הפשוט (`days[i].leg.id !== days[i-1].leg.id`) ממקם את המפריד נכון מעצמו - לא נדרש טיפול מיוחד, זו תוצאה של החלטה שכבר התקבלה. **אומת בפועל, לא רק בהיגיון**: טיול 3 רגליים עם גבולות בימים מדויקים - יום המעבר (13.9) הופיע כיום הרביעי והאחרון של רומא, והמפריד "← בנגקוק" רק אחריו
- **טיול רגל אחת - אפס מפרידים, מבחינה מבנית** - מפריד מוכנס רק כש-`days[i].leg.id !== days[i-1].leg.id`; טיול רגל אחת אף פעם לא מגיע לתנאי הזה
- **הגלילה לטאב "היום" (v4.23.2) נבדקה מחדש עם מפרידים ברצועה** - עדיין נוחתת נכון (`scrollIntoView` פועל על האלמנט עצמו, לא על אינדקס/היסט מחושב, כך שמפרידים נוספים לא משפיעים על הדיוק)

נבדק בפועל: טיול רגל אחת - 0 מפרידים; טיול 3 רגליים עם גבולות ימים מדויקים - 2 מפרידים במקום הנכון (כולל בדיקת יום-מעבר הדקה שתוארה למעלה); גלילה ל"היום" עם 2 מפרידים ברצועה - הטאב נכנס לתחום הנראה של המכולה; עברית ואנגלית (כיוון חץ הפוך, פורמט תאריך שונה); מצב בהיר וכהה - אומת חזותית שהטקסט קריא בשניהם; מסך נייד (375px) - הרצועה גוללת חלק. הוסר `window.__test` הזמני ואומת שהוסר (`grep` נקי בשני הקבצים) לפני push
`node scripts/i18n-audit.js itinerary.html` - 0 ממצאים

## v4.26.1 — מפריד בין רגליים ברצועת מזג האוויר (שלב 8, commit ג)

### ✨ תכונה חדשה
- **המעבר בין יעדים ברצועת מזג האוויר כבר לא מסומן רק ע"י קפיצת טמפרטורה** - נבדק בפרודקשן: `18°` ואז `32°` ברצף, בלי שום סימון שהיעד השתנה. עכשיו כל מעבר בין רגל לרגל מסומן בפריט מפריד עם חץ ושם היעד הבא
- **עיצוב חדש, לא שכפול** - §5.3 מפנה ל"אותו דפוס ויזואלי כמו התוכנית היומית", אבל הדפוס הזה (§5.2: קו מלא ברוחב + טקסט במרכז) לא קיים עדיין בשום מקום בקוד - שלב 7 (המפריד בטאבי הימים) עוד לא מומש. גם ההקשר שונה מבנית: שם רשימה אנכית ברוחב מלא, כאן רצועת צ׳יפים אופקית גוללת. המפריד כאן הוא איבר `flex` צר בגודל תוכן, בלי רקע ובלי padding כמו `.forecast-day` - עיצוב מכוון, לא סתם השמטה: מפריד בצורת צ׳יפ היה מתחרה חזותית בנתונים במקום להיות כפוף להם (עקרון הצפיפות)
- **`legRouteArrow()` הקיימת משמשת לכיוון החץ** (לא נגזרת מחדש) - אותה פונקציה שכבר משמשת את טקסט המסלול בכותרת דף הבית (§5.8) - RTL/LTR נכונים בלי לוגיקה כפולה
- **מוצג גם לפני רגל בלי נתונים** (הצ׳יפ המעומעם, geocoding כושל או מעבר ל-16 יום) - המפריד מסמן שהיעד השתנה, וזה נכון גם כשליעד הבא אין תחזית. בלי המפריד, הצ׳יפ המעומעם היה נראה כמו פער לא-מוסבר באמצע הרצועה
- **טיול רגל אחת - אפס מפרידים, מבחינה מבנית לא רק ויזואלית** - מפריד מוכנס רק בין הפריטים של שתי רגליים *שונות*; טיול רגל אחת אף פעם לא מגיע לתנאי הזה

נבדק בפועל: טיול רגל אחת - 0 מפרידים, אותה כמות צ׳יפים כמו לפני commit ג; טיול 3 רגליים - בדיוק 2 מפרידים, במקום הנכון בין הרגליים (לא לפני הרגל הראשונה, לא אחרי האחרונה); עברית ואנגלית עם שמות יעד ארוכים ("קואלה לומפור", "סן פרנסיסקו") - רוחב המפריד נשאר בערך כמו צ׳יפ בודד, לא שני צ׳יפים; גודל הטקסט (`.62rem`, נבדק בפועל `9.92px` ברזולוציית מכשיר אמיתית) גדול במעט מטקסט שם היום בצ׳יפ עצמו (`9.6px`) - קריא במסך 375px, לא רק בצילום מוקטן; גלילה בכמה נקודות אמצע (`scrollLeft` שונים) - המפריד לא נחתך בצורה שמסתירה חלקית רק את החץ או רק את השם, אותה התנהגות קצה כמו צ׳יפ נתונים רגיל; אין מנגנון הצללת-קצה (`has-more`) לרצועה הזו בקוד בכלל - נבדק ולא נמצא, אז אין מה לוודא התאמה אליו
`node scripts/i18n-audit.js index.html` - 0 ממצאים

## v4.26.0 — רצועת מזג האוויר הופכת מרובת-רגליים (שלב 8, commit ב)

### ✨ תכונה חדשה
- **`fetchForecast()` כבר לא מציגה תמיד את הרגל הראשונה** - הליבה של שלב 8 (docs/schema-legs.md §5.3). בטיול ניו יורק→בנגקוק→הונולולו, הרצועה הציגה תמיד את מזג האוויר של ניו יורק, בלי קשר לאיפה הטיול נמצא בפועל. עכשיו כל רגל תורמת את הימים שלה, כל אחד עם מזג האוויר של המקום הנכון - ברצף אחד, בלי מפריד ויזואלי בין רגליים (זה commit ג - דפוס עיצוב חדש לגמרי, לא שכפול של משהו קיים, כמו שכבר תועד כשנחקר). פחות ברור מבחינה ויזואלית, אבל נכון - ועדיף על תחזית של המקום הלא נכון
- **קריאה רב-מיקומית יחידה, לא N מקביליות** - **אומת מול ה-API האמיתי לפני הכתיבה**, לא הונח: Open-Meteo תומך בקואורדינטות מופרדות בפסיק. ממצא לא-מובן-מאליו: **צורת התשובה תלויה במספר הקואורדינטות** - קואורדינטה אחת מחזירה אובייקט בודד (הצורה המוכרת), 2+ מחזירות **מערך**, אובייקט אחד לכל מיקום, **באותו סדר שבו הקואורדינטות נשלחו**. הקוד בודק `Array.isArray(data)` ומנרמל ל-`[data]` כשזו לא רשימה - כך טיול רגל אחת ממשיך לשלוח קואורדינטה בודדת ולקבל את אותה צורה בדיוק כמו היום, ולא נשלחת קואורדינטה מיותרת רק כדי לאחד את הצורה. **הקורלציה בין תשובה לרגל היא לפי מיקום במערך בלבד** - אין `id`/שם בתשובה שאפשר להתאים לפיו (יש `location_id` שמופיע החל מאינדקס 1 בלבד, נעדר באינדקס 0 - קוריוזיטה של ה-API, לא בסיס לקורלציה) - מתועד בהערת קוד כאזהרה: אם Open-Meteo אי-פעם יפסיק לשמור על סדר הקלט, זה יצמיד בשקט מזג אוויר לרגל הלא נכונה בלי שום דרך לזהות זאת מהתשובה עצמה
- **תגלית נוספת שמומשה בחינם**: לכל מיקום בתשובה יש `timezone` משלו (מ-`timezone=auto`), ו-`daily.time[0]` שלו מעוגן ל"היום" **של אותו מיקום עצמו** - לא לתאריך משותף אחד לכל הבקשה. זה בדיוק מה שסעיף 5 (todayKey per-leg) היה צריך, בלי חישוב לקוח נוסף - כל רגל משווה מול ה-`dateKey` שלה עצמה, בשעון שלה עצמה
- **רגל בלי קואורדינטות (geocoding כושל, מקרה "קליפורניה") ורגל מעבר ל-16 הימים** - שתיהן מקבלות אותה התנהגות: צ׳יפ יחיד עם שם היעד, בלי נתונים, מעומעם - אותה תבנית ויזואלית בדיוק כמו העמעום הקיים לימים 7+ (`.unreliable`), לא CSS חדש. **צ׳יפ אחד לרגל, לא אחד לכל יום חסר** - רגל ארוכה שחורגת מ-16 הימים מקבלת קצת ימים אמיתיים ואז צ׳יפ נגררת יחיד לשארית, לא שרשרת של צ׳יפים זהים. `window.hasValidCoords` (legs.js, v4.25.3) קובעת אילו רגליים בכלל נכנסות לבקשת ה-API - לא `hasRealCoords` הגלובלי הישן, שנבדק רק מול הרגל הראשונה ומסתיר את **כל** הרצועה אם היא נכשלה, גם כשרגליים אחרות תקינות
- **`todayKey`/הטווח הרלוונטי - לכל רגל בנפרד, בשעון שלה עצמה** - זה בדיוק מה שנעל את commit ג של שלב 5 ("הקריאה עוד ממוקדת לרגל ראשונה בלבד"): עכשיו כשהקריאה עצמה מפוצלת, כל רגל משווה מול ה-`dateKey` שלה. רגל שכבר הסתיימה (ה"היום" שלה כבר עבר את ה-`endAt` שלה) לא תורמת שום דבר - אותה השפעה כמו הבדיקה הישנה "rangeStartKey > tripEndKey" הגלובלית, רק מוערכת פר-רגל

### 🐛 תיקון
- **מפתח המטמון תוקן משני כיוונים, לא רק "נוסף legId"**: הבקשה המקורית הייתה `legId + שפה`, אבל שני מקרים אמיתיים מראים ש-`legId` לבדו לא מספיק. **(1)** טיול רגל אחת: הרגל הסינתטית מ-`getLegs()` (legs.js) נושאת תמיד `id: "main"` - מפתח לפי id בלבד היה **אף פעם לא משתנה** בשום עריכה של רוב הטיולים באפליקציה (כל טיול רגל אחת). **(2)** שינוי שם רגל קיימת בפאנל אדמין שומר את ה-`id` שלה (v4.25.1, בכוונה - יציבות שיוך) אבל **כן** משנה `lat`/`lon` (geocoding מחדש); ועריכת תאריך מעבר בלי שינוי שם לא מסמנת אף רגל כ-`dirty` בכלל. **הפתרון**: `id:lat:lon:startAt:endAt` לכל רגל רלוונטית + השפה - אותו תפקיד בדיוק שמילא `tripEndKey` במפתח הישן (לתפוס עריכת תאריך), רק פר-רגל במקום גלובלי לטיול
- **הוספת השפה למפתח מתקנת גם באג שכבר תועד**: הרצועה הייתה נשארת בשמות ימים בשפה הקודמת אחרי החלפת שפה, כי `lastForecastKey` הישן לא כלל שפה בכלל - שינוי שפה לא היה גורם ל-refetch, ורינדור מחדש קורה רק בתוך `fetchForecast()`. אומת בפועל: החלפת שפה (בלי לגעת ברגליים) מפעילה כעת קריאת רשת נוספת ומעדכנת את שמות הימים; שמירה על שדה לא-קשור (למשל `showAccent`) **לא** מפעילה קריאה נוספת - המטמון מחזיק
- **נתפס בבדיקות, לא בעיון**: הגרסה הראשונה של הקוד קראה ל-`window.dateKeyInZone(leg.startAt, tz)`/`(leg.endAt, tz)` עם מחרוזת ISO גולמית במקום עטופה ב-`new Date(...)` - זרקה `RangeError: Invalid time value` מיד עם טעינת כל טיול. בדיוק האזהרה התיעודית ב-`legs.js`'s הראש ("any future code that reads leg.startAt/leg.endAt directly... without wrapping in new Date() first will silently misbehave") - כאן זה לא נכשל בשקט, זה זרק מיד, מה שאיפשר לתפוס ולתקן לפני commit

נבדק בפועל (לא רק על אובייקטים שנבנו לפי ההנחות - `fetch` הוחלף ב-spy שלוכד את ה-URL האמיתי שהקוד בונה, ובחלק מהבדיקות הושוו ל-HTML שהקוד הישן (`git stash`) הפיק על אותו קלט בדיוק):
- טיול רגל אחת - **HTML זהה בית-לבית** לקוד הישן (השוואה ישירה, לא רק "נראה דומה"), ואותו URL בדיוק (`latitude=X&longitude=Y`, בלי פסיק)
- טיול 3 רגליים (רומא פעילה, בנגקוק+הונולולו עתידיות) - קריאה רב-מיקומית יחידה אחת (`latitude=41.9,13.75,21.3&longitude=...`), טמפרטורות ייחודיות לכל רגל (100/200/300 במבחן) אימתו שאין ערבוב בין המיקומים; "היום" מסומן נכון על היום הראשון של הרגל הפעילה
- רגל בלי קואורדינטות ("קליפורניה") - צ׳יפ מעומעם עם השם, `--°`, אין קריאת רשת עבורה (רק הרגל התקינה נכנסת ל-URL); רגל ארוכה שחוצה את 16 הימים - ימים אמיתיים לחלק המכוסה ואז צ׳יפ נגררת יחיד לשארית
- החלפת שפה - שמות הימים מתעדכנים בפועל, ורק אז; שמירה על שדה לא-קשור - אין קריאת רשת מיותרת (זו הבדיקה שמוכיחה שהמטמון עובד)
- עברית ואנגלית, מסך נייד (375px) - שני צילומי מסך, כולל הצ׳יפ המעומעם והרצועה הגוללת

`node scripts/i18n-audit.js index.html` - 0 ממצאים

## v4.25.4 — תיקון: קריסה חוזרת כשלרגל הפעילה אין timezone (רגרסיה מ-v4.25.0)

### 🐛 תיקון
- **רגרסיה מ-v4.25.0, נתפסה תוך כדי בדיקת commit ב של שלב 8 (v4.25.3), לא ממצא צדדי**: כשהרגל הפעילה בטיול מרובה-רגליים כשלה ב-geocoding (`timezone: null` - אותו מקרה "קליפורניה"), `multiLegDuringStatusLine()` זרקה `RangeError: Invalid time zone specified: null` מתוך `new Intl.DateTimeFormat({timeZone: activeTz})` - `activeTz` נקרא ישירות מ-`activeLeg.timezone`, בלי הגנה. `updateClockAndPhases()` (הקוראת לפונקציה) רצה כל שנייה - **קריסה חוזרת בקונסול, לא אירוע חד-פעמי**: אומת בפועל שהטאב המשיך לזרוק את אותה שגיאה כל טיק במשך כל הזמן שנשאר טעון
- **שתי שכבות הגנה, לא אחת**: (1) `activeTz` עברה מ-`activeLeg.timezone` הגולמי ל-`window.getCurrentLegTimezone({legs: currentLegs})` - נופלת לרגל האחרונה כשלרגל הפעילה אין timezone משלה, אותה שרשרת נפילה שכבר קיימת ב-§4. (2) קריאת ה-`Intl.DateTimeFormat` עצמה שולחת `activeTz || undefined` במקום `activeTz` - `undefined` גורם ל-Intl.DateTimeFormat להשתמש בשעון הריצה (המכשיר) כברירת מחדל; `null` מפורש זורק. הגנה שנייה כי `getCurrentLegTimezone` עצמה עדיין יכולה להחזיר `null` (גם הרגל הפעילה וגם האחרונה בלי timezone) - לא כפילות, קו הגנה אחרון בנקודת הקריאה עצמה
- **למה `getCurrentLegTimezone` לא תפסה את זה קודם**: היא פשוט לא הייתה בשימוש כאן בכלל - `multiLegDuringStatusLine` קראה `activeLeg.timezone` ישירות, לא דרך הפונקציה המשותפת
- **חיפוש רחב, בכל 6 העמודים ובקבצים המשותפים (`legs.js`, `datetime.js`): 11 מקומות עם `Intl.DateTimeFormat`/`toLocale*`, 10 מהם כבר מוגנים כראוי, מקום אחד יחיד לא - וזה בדיוק זה שקרס.** 7 לא מקבלים `timeZone` בכלל (בטוחים מבנית - `wizard.html`×3, `index.html`×2, `itinerary.html`×1, `places.html`×1). מתוך 3 שמקבלים `timeZone`: `zonedParts` (`datetime.js`) בודקת `!timeZone` פנימית לפני כל קריאה; `localInputToUtcInZone` (`datetime.js`) ו-`toZonedInputValue` (`index.html`) לא מוגנות פנימית, אבל **כל אחת** מ-4 הקריאות הקיימות אליהן (`legBuilder.js`×2, `index.html`×2) עוטפת בבדיקת אמת לפני הקריאה. **10 מתוך 11 - זו לא רק סטטיסטיקה: זה אומר שאין כאן משפחת-באגים נסתרת, ושהעבודה הקודמת על אזורי זמן (§4, §8ব סעיף 7, v4.23.1) הייתה יסודית - המקום היחיד שנשבר הוא זה שנכתב הבוקר ועדיין לא עבר את אותו משטר הגנה.** נרשם כאן במפורש בשביל מי שיוסיף `Intl.DateTimeFormat` חדש בעתיד: זה הדפוס לשמור עליו - אם `timeZone` יכול להיות `null`, או לבדוק `!timeZone` לפני הקריאה (כמו `zonedParts`) או לוודא שכל קריאה מוגנת בנקודת הקריאה (כמו `localInputToUtcInZone`/`toZonedInputValue`) - לא להעביר ערך גולמי ישירות
- נבדק בפועל: קריסה שוחזרה במלואה לפני התיקון (`git stash` זמני לחזרה לקוד הישן, `RangeError` נלכד עם ה-stack המדויק, כולל שהטאב המשיך לקרוס כל שנייה ברקע); אחרי התיקון - אותו fixture בדיוק (טיול 3 רגליים, הרגל האמצעית הפעילה בלי timezone) בטאב נקי לחלוטין: אין `RangeError` בקונסול אחרי כמה טיקים של ה-interval החי, השורה מציגה `✈️ ב-10.9 טסים לHonolulu` (תאריך המעבר בשעון המכשיר, סביר)
- `node scripts/i18n-audit.js index.html legs.js` - 0 ממצאים (אין טקסט משתמש חדש)

## v4.25.3 — תיקון: geocoding כושל נשלח ל-Open-Meteo כ-(0,0) במקום להיחסם

### 🐛 תיקון
- **נתפס תוך כדי בדיקת commit א של שלב 8 (רגל בלי קואורדינטות), לא בעיון**: `hasRealCoords`/`latCoord`/`lonCoord` (`index.html`, ליד שורה 3510) בדקו `Number.isFinite(Number(primaryLeg.lat))` בלי לבדוק `!= null` קודם. `Number(null)` הוא `0`, לא `NaN` - אז רגל שנכשלה ב-geocoding (`lat: null, lon: null`, הערך שכשל `legBuilder.js` כותב בפועל) עברה כאילו יש לה קואורדינטות תקינות. **חמור משנראה**: לא רק ש-`hasRealCoords` היה שגוי (`true` במקום `false`) - `latCoord`/`lonCoord` עצמן נדרסו ל-`0`/`0` (Null Island, נקודה באוקיינוס האטלנטי), לא נשארו בערך הקודם כפי שההערה הישנה בקוד הניחה
- **הוכח בפועל, לא רק נומק**: טיול מרובה-רגליים עם הרגל הראשונה (רומא) כושלת geocoding והרגל הפעילה (בנגקוק) תקינה - לפני התיקון, `fetchForecast()` שלחה בפועל `https://api.open-meteo.com/v1/forecast?latitude=0&longitude=0&...` ל-Open-Meteo (נלכד עם fetch spy על הקוד האמיתי, לא אובייקט שנבנה לפי הנחות). אחרי התיקון: `hasRealCoords=false`, `latCoord`/`lonCoord` נשארים בערך הקודם, וה-guard הקיים של `fetchForecast()` (`if (!hasRealCoords || ...)`) עוצר את הקריאה לגמרי - בלי לשנות את ה-guard עצמו, רק את הערך שהוא בודק
- **`window.hasValidCoords(obj)` חדשה ב-`legs.js`** - `lat`/`lon` `!= null` בדיקה מפורשת לפני ההמרה למספר. חולצה כפונקציה משותפת אחרי שאותה טעות בדיוק קרתה פעמיים בישיבה אחת: פעם ב-`getCurrentLegCoords()` (v4.25.2, נתפסה בבדיקות שלה) ופעם כאן, כבר בפרודקשן. `getCurrentLegCoords()` עודכנה להשתמש בפונקציה המשותפת במקום בעותק הפנימי שלה
- **`index.html`'s שני ה-`if` הנפרדים (בדיקת lat ובדיקת lon בנפרד) אוחדו לבדיקה אחת**: בפועל lat/lon תמיד מגיעים יחד מאותה קריאת geocoding (`legBuilder.js`) - אף פעם לא תקינים בנפרד. `hasRealCoords` (המחושב נכון עכשיו) שולט על עדכון שניהם יחד, לא שני checks כפולים שיכולים לדרוש תחזוקה נפרדת
- **ממצא נוסף, לא תוקן כאן - מחוץ להיקף**: באותה בדיקה, טיול מרובה-רגליים שבו ה**רגל הפעילה עצמה** (לא רק רגל אחרת בטיול) כושלת geocoding (`timezone: null`) גרם ל-`multiLegDuringStatusLine()` (v4.25.0, שורת המצב בכרטיס הבית) לזרוק `RangeError: Invalid time zone specified: null` מתוך `new Intl.DateTimeFormat(locale, {timeZone: activeTz})` - קריסה לא-תפוסה בתוך `updateClockAndPhases()`. תועד, לא תוקן - commit נפרד
- נבדק בפועל: טיול רגל אחת עם קואורדינטות תקינות - אפס שינוי (`hasRealCoords=true`, `latCoord`/`lonCoord` נכונים, שתי הקריאות ל-Open-Meteo יוצאות עם הקואורדינטות הנכונות); טיול מרובה-רגליים עם רגל ראשונה כושלת ורגל פעילה תקינה - `fetchWeather()` (מ-v4.25.2) כבר משתמשת ברגל הפעילה כך שלא הושפעה; `fetchForecast()` (שעדיין רגל-ראשונה-בלבד, מחוץ להיקף) עוברת מקריאה שגויה ל-(0,0) לחסימה נכונה
- `node scripts/i18n-audit.js index.html legs.js` - 0 ממצאים (אין טקסט משתמש חדש)

## v4.25.2 — תיקון: שורת "עכשיו" מציגה מזג אוויר של הרגל הראשונה תמיד

### 🐛 תיקון
- **docs/schema-legs.md §8, commit א**: בטיול מרובה-רגליים, שורת "עכשיו" (טמפ׳/רוח/UV/מצב) הציגה תמיד את מזג האוויר של הרגל **הראשונה** - `fetchWeather()` קראה קואורדינטות מ-`latCoord`/`lonCoord`, שתי גלובליות שמוזנות מ-`primaryLeg` בלבד. משתמש שכבר בבנגקוק (רגל 2 מתוך 3) ראה את מזג האוויר של ניו יורק - מידע שגוי, לא חסר
- **`window.getCurrentLegCoords(trip)` חדשה ב-`legs.js`** - האנלוגיה הקואורדינטית ל-`getCurrentLegTimezone()` הקיימת, לא באותה צורה בדיוק: ל-timezone יש "שעון המכשיר" כתשובה סופית תמיד-תקינה; לקואורדינטה אין מקבילה. במקום זאת, לפני/אחרי הטיול (`getLegForDate` מחזירה `null`) המשתנה משקף את הפיצול הקיים ב-§4 עצמו - `trip.startAt` נקרא מהרגל הראשונה, `trip.endAt` מהאחרונה: לפני הטיול = רגל ראשונה, אחריו = רגל אחרונה, ובמהלכו = הרגל הפעילה, כמו `getCurrentLegTimezone`
- **רגל עם קואורדינטות חסרות לא מסתירה את השורה**: אם הרגל שנבחרה (פעילה, ראשונה או אחרונה) חסרת `lat`/`lon` תקינים (geocoding כושל/מעורפל - למשל "קליפורניה"), מחפשת רגל אחרת שכן יש לה - רגל אחרונה, ואז ראשונה. מחזירה `null` רק כשאף רגל בטיול כולו לא נושאת קואורדינטות אמיתיות
- **נתפס בבדיקה, לא בעיון**: הבדיקה הראשונה של הבדיקה 5 (רגל בלי קואורדינטות) החזירה `{lat:0, lon:0}` במקום ליפול לרגל הבאה - `Number(null)` הוא `0`, לא `NaN`, אז `Number.isFinite(Number(leg.lat))` לבדו מקבל `lat:null` (הערך שכשל geocoding כותב בפועל, `legBuilder.js`) כאילו זו קואורדינטה אמיתית ב-Null Island. תוקן: בדיקת `!= null` מפורשת לפני ההמרה. **אותו דפוס בדיוק (`Number.isFinite(Number(leg.lat))` בלי בדיקת `null` קודם) קיים כבר ב-`hasRealCoords` (`index.html`, שורה קרובה ל-3510) - לא תוקן כאן, מחוץ להיקף ה-commit הזה, אבל זה ממצא אמיתי שכדאי לטפל בו בנפרד**: `fetchForecast()`'s guard עלול לא לתפוס בפועל רגל עם `lat:null`/`lon:null` מוצהר
- **`fetchWeather()` (`index.html`)** קוראת ל-`getCurrentLegCoords({legs: currentLegs})` רק כש-`tripId` אמיתי; במצב "אין טיול טעון" (עמוד נחיתה בלי `tripId`, שם `currentLegs` עדיין `[]` הראשוני) ממשיכה להשתמש ב-`latCoord`/`lonCoord` הגלובליים כמו היום - אפס שינוי לאותו מצב, לא נבדק בבקשה המקורית אבל נמצא כנקודת קריאה אמיתית ל-`fetchWeather()` ולא הושאר ללא טיפול
- **התוצאה נשארת מקומית**, לא נכתבת חזרה ל-`latCoord`/`lonCoord` הגלובליים - אלה עדיין מזינות את `fetchForecast()` (עדיין רגל-ראשונה-בלבד במפורש, עד commit נפרד בהמשך §8) ואת האיפוס בפאנל האדמין; דריסה כאן הייתה מזליגה את מיקום הרגל הפעילה לקוד שעדיין לא אמור להכיר אותו
- נבדק בפועל מול `fetchWeather()` האמיתית (fetch מוחלף ב-spy שלוכד את ה-URL, לא אובייקט שנבנה לפי ההנחות): (1) טיול רגל אחת - קואורדינטות זהות בית-לבית למקור; (2) מרובה-רגליים, הרגל האמצעית פעילה - קואורדינטות הרגל האמצעית, לא הראשונה; (3) לפני הטיול - רגל ראשונה; (4) אחרי הטיול - רגל אחרונה; (5א) הרגל הפעילה בלי קואורדינטות, האחרונה כן - נופל לאחרונה; (5ב) גם האחרונה בלי קואורדינטות, הראשונה כן - נופל לראשונה; (5ג) אף רגל בטיול לא נושאת קואורדינטות - "לא זמין" מוצג, אין קריאת רשת בכלל; מצב "אין טיול" - זהה בית-לבית למקור
- אין שינוי טקסט משתמש חדש ("לא זמין" כבר קיים ב-catch block הקיים של אותה פונקציה) - לא נדרש `i18n-audit.js`, הורץ בכל זאת ללא ממצאים

## v4.25.1 — תיקון: legId לא היה יציב בין שמירות בפאנל אדמין

### 🐛 תיקון
- **נתפס בחקירת שלב 8 (docs/schema-legs.md, רצועת מזג האוויר), לפני שהוא הפך לבאג בפרודקשן**: `buildLegs()` (`legBuilder.js`) המציאה `id` אקראי חדש לכל רגל בכל קריאה - כולל רגליים שלא נגעו בהן (`dirty=false`, קואורדינטות מועתקות מ-`originalLeg`). זרימת השמירה בפאנל אדמין (`index.html`) קוראת ל-`buildLegs()` על כל הרגליים יחד בכל שמירה שנוגעת בטיול מרובה-רגליים - לא רק ברגליים ששונו. §2.2 קובעת במפורש `id` יציב "כי פריטי ציוד ופעילויות מצביעים על רגל" - שמירה שמאפסת את כל ה-id-ים הייתה מנתקת שיוכים כאלה בשקט, בכל שמירה, לא רק בעריכת מבנה הרגליים עצמו
- **אף קוד קיים לא קרא `leg.id`/`legId` עד כה** (אומת בגרפ) - שלבים 6/9 (שיוך ציוד/מקומות לפי רגל) יהיו הצרכן הראשון, וה-instability הייתה מתגלה רק אז, מאוחר מדי לאבחן בקלות
- **התיקון**: `buildLegs()` מקבלת פרמטר אופציונלי חדש, `legIds` - כשיש ערך לאינדקס נתון, הוא נשמר כ-`id` של הרגל במקום למינות חדש. הוויזרד לא מעביר `legIds` בכלל (כל הרגליים שם חדשות מיסודן) - שם ההתנהגות זהה לפני ואחרי. הפאנל אדמין מעביר `legDescriptors.map(r => r.originalLeg?.id || null)` - שורה חדשה יחידה לפני הקריאה הקיימת ל-`buildLegs()`
- **שינוי סדר רגליים ממשיך לעבוד נכון**: `moveAdminLegUp()` כבר מחליפה את `originalLeg` (כולל ה-`id` שבתוכו) יחד עם `input.value`/`dirty` בין שתי השורות - נבדק ישירות שהמיפוי `legDescriptors → legIds` אחרי סימולציה של אותה החלפה מחזיר את ה-id-ים בסדר החדש, לא הישן
- נבדק בפועל (קריאה ישירה ל-`window.buildLegs()` בקונסול, לא רק אובייקט שנבנה לפי ההנחות): (1) כל הרגליים עם `legIds` קיים - ה-id-ים חוזרים זהים בית-לבית; (2) בלי `legIds` (מקרה הוויזרד) - כל הרגליים מקבלות id טרי וייחודי; (3) `legIds` מעורב (רגל אמצעית `null`, שתי הקצוות עם id קיים) - הקצוות נשמרים, האמצעית מקבלת id טרי; (4) סימולציית שינוי סדר על מערך descriptors - ה-id-ים זזים עם הרגל, לא נשארים במיקום
- אין שינוי ל-UI/טקסט משתמש - לא נדרש `i18n-audit.js`

## v4.25.0 — שורת המצב בכרטיס הבית ביעדים מרובים (index.html בלבד)

### ✨ תכונה חדשה
- **שורת המצב (`#timerTitle`) בשלב "במהלך הטיול" מתעדכנת ליעד הפעיל, לא נשארת תקועה על היעד הראשון** - עד עכשיו טיול מרובה-יעדים המשיך להציג "הטיול בניו יורק התחיל" גם אחרי שהמשתמש עבר לבנגקוק, בזמן שהכותרת והרמז (§5.8) כבר הראו את היעד הנוכחי. שלוש השורות בכרטיס נועדו למסור שלושה דברים שונים - כותרת=איפה אנחנו, רמז=מיקום ברצף, שורת מצב=מה הלאה - וזו הייתה היחידה שנשארה בעבר
- **`multiLegDuringStatusLine()` חדשה** - 4 מצבים, לפי זהות הרגל הפעילה (`idx === currentLegs.length - 1`) ולא לפי תאריך: יש רגל הבאה ואין מעבר היום → `✈️ ב-13.9 טסים לבנגקוק`; המעבר הוא היום → `✈️ היום טסים לבנגקוק`; ברגל האחרונה עם ימים שנותרו → `✈️ נשארו 4 ימים`; ברגל האחרונה ביום האחרון → `✈️ היום האחרון`. פיצול לפי זהות רגל ולא תאריך נבחר כי שתי השאלות ("יש מעבר הבא" מול "זו הרגל האחרונה") הן אותה שאלה בשני ניסוחים - לא יכולות להתנגש גם ברגליים לא-רציפות, מתועד בהערת קוד
- **טיול רגל אחת - אפס שינוי, מבחינה מבנית לא רק ויזואלית**: `multiLegDuringStatusLine()` אפילו לא נקראת כש-`currentLegs.length <= 1` - הענף הקיים (`הטיול ב-X התחיל`) נשאר המסלול היחיד
- **`reserve` לא נוגע כלל** - הענף הקיים ("הפעילות בעיצומה", בלי שם יעד) בודק `tripType === 'reserve'` לפני שהוא בכלל שואל אם יש כמה רגליים
- **פועל תלוי-סוג טיול**: `abroad` → "טסים"/"Flying", `local`/`camping` → "נוסעים"/"Heading" - "טסים" לא מתאים לטיול גליל→נגב
- **אזורי זמן, לפי המסמך**: "היום"/תאריך המעבר נקראים בשעון הרגל היוצאת (`activeLeg.timezone`) דרך `window.getLegForDate`/`dateKeyInZone` הקיימות - לא שעון המכשיר, לא שעון הרגל האחרונה (בדיוק הבאג שתוקן ב-v4.23.1). "נשארו X ימים" נספר עד `legs[last].endAt` בשעון הרגל האחרונה, בימי-לוח (`dateKeyInZone` diff) לא שעות גולמיות - כמו `tripDayDates`/רצועת התחזית
- תאריך המעבר מוצג עם `Intl.DateTimeFormat` (`he-IL`/`en-GB`, `day/month` בלבד, עם `timeZone` מפורש כי הקלט הוא ISO instant גולמי) - אותה תבנית תצוגה כמו טאבי הימים
- נבדק בפועל: טיול רגל אחת במהלך - טקסט זהה ל-byte למקור (הקריטי); מרובה-רגליים באמצע רגל 1 מתוך 3 - "ב-X טסים ל-Y"; יום המעבר - "היום טסים ל-Y"; ברגל האחרונה עם ימים שנותרו - "נשארו X ימים"; ביום האחרון - "היום האחרון"; לפני/אחרי טיול מרובה-רגליים - אפס שינוי; עברית ואנגלית כולל החלפת הפועל לפי סוג טיול; צילום מסך 375px
- `node scripts/i18n-audit.js index.html` - 0 ממצאים

## v4.24.0 — כותרת דף הבית ביעדים מרובים (§5.8, index.html בלבד)

### ✨ תכונה חדשה
- **הכותרת עונה על שאלה שונה בכל שלב**: לפני הטיול - "לאן נוסעים" (מסלול חצים); במהלכו - "איפה אנחנו עכשיו" (היעד הפעיל + דגלו + רמז "יעד N מתוך M"); אחריו - שוב המסלול המלא ("לאן נסענו", לא רק היעד האחרון)
- **דגל רק ביעד יחיד** - במסלול-חצים (2+ יעדים, לפני/אחרי) אין דגל, רק שמות ומחברים; חוזר ברגע שהכותרת מציגה יעד אחד (במהלך הטיול, או תמיד בטיול חד-יעד)
- **טיול רגל אחת - אפס שינוי, מבחינה מבנית לא רק ויזואלית**: הענף הקיים ב-`applyTripPresentation()` לא נגע כלל; הלוגיקה החדשה (`refreshDestinationTitle()`) אפילו לא נקראת כש-`currentLegs.length <= 1`
- **הרמז ("יעד 2 מתוך 3") ב-markup חדש, מוסתר לגמרי (`display:none`) כשאין אותו** - לא רק טקסט ריק, כדי שלא יתפוס גובה בטיול חד-יעד או לפני/אחרי הטיול
- **מדידה בזמן ריצה במקום סף קבוע**: מסלול מלא → אם לא נכנס בשורה אחת, "ראשון ← … ← אחרון" → אם גם זה לא נכנס, "N יעדים". נבדק בפועל שגם השלב האמצעי מגיע (לא רק הקצוות) - ראשון קצר + אחרון קצר + אמצעי ארוך במיוחד. ellipsis-פר-שם (שנשקל ונדחה קודם) היה חותך כל שם בנפרד לבלתי-קריא גם באורך טיפוסי, ובעיקר מאבד את היעד האחרון - בדיוק מה שהקיצור נועד לשמר
- **הגנות על מדידה שבירה, לפי בקשה מפורשת**: לא סומכת על `scrollWidth`/`clientWidth` כשהפונטים לא נטענו (`document.fonts.status !== 'loaded'`) או כשהאלמנט ברוחב 0 - נופלת ישר ל-"N יעדים" הבטוח, עם ריצה חוזרת חד-פעמית דרך `document.fonts.ready`; רצה מחדש ב-resize (עם debounce) וב-שינוי שפה (כבר קורה כחלק מ-`applyTripPresentation()`); רצה מחדש בכל מעבר שלב אמיתי (לפני→במהלך וכו') דרך ה-guard הקיים כבר ב-`applyCardsForPhase()`, לא polling נפרד
- `lastCardsPhase` **לא** שימש למקור האמת על השלב בתוך `refreshDestinationTitle()` - נמצא בבדיקה ש-`applyTripPresentation()` (בתוך `hydrateTripData()`) רץ **לפני** `updateClockAndPhases()` באותו מעבר הידרציה, אז `lastCardsPhase` היה עדיין משקף את השלב הקודם (או `null` בטעינה ראשונה). השלב מחושב מקומית מ-`flightOut`/`tripEnd` ישירות, כמו `updateClockAndPhases()` עצמה
- נבדק בפועל: טיול רגל אחת בשלושת השלבים - כותרת זהה בכל שלב (`getBoundingClientRect`/טקסט מדויק, לא רק "נראה בסדר"); 2 יעדים לפני - חצים בלי דגל בלי רמז; 3 יעדים טיפוסיים - נכנס במלואו; 3 יעדים ארוכים - נופל ל-"3 יעדים" (גם "ראשון…אחרון" לא נכנס, אומת במדידה ישירה); מקרה מכוון שבו רק "ראשון…אחרון" נכנס (קצוות קצרים, אמצעי ארוך) - אומת שהשלב האמצעי בקסקדה נבחר בפועל; במהלך - יעד פעיל+דגל+רמז נכון; אחרי - מסלול מלא; עברית ואנגלית (כיוון חץ, טקסט רמז/ספירה); resize מפעיל מדידה מחדש (אומת עם קונטיינר מוצר בכוח); מעבר שלב מדומה (`applyCardsForPhase`) מרענן את הכותרת; `document.fonts.status='loading'` מדומה - נופל בבטחה ל-"N יעדים" בלי לנסות למדוד; צילומי מסך 375px בשלושת השלבים
- `node scripts/i18n-audit.js index.html` - 0 ממצאים (מחרוזות דינמיות עם `tr()` inline, אותו דפוס כמו "N פעילויות מתוכננות להיום" הקיים)

## v4.23.2 — תיקון: טאב "היום" לא נגלל לתצוגה ברצועת הימים (itinerary.html)

### 🐛 תיקון
- **הטאב של "היום" סומן נכון (מסגרת מודגשת), אבל הרצועה לא נגללה אליו** - בטיול ארוך, מי שנוחת על העמוד רואה את יום 1 ולא את היום עצמו, כי הרצועה מתחילה תמיד ללא גלילה
- **תוקן**: אחרי `renderDayTabs()`, קריאה אחת ל-`.day-tab.today`'s `scrollIntoView({block:'nearest', inline:'center'})` - רק כשהטאב הזה בכלל קיים (כלומר היום בטווח הטיול; לפני/אחרי הטיול הרצועה נשארת בהתחלה, לא מרכזת את יום 1)
- **רק בטעינה הראשונית**, לא בכל render - דגל מודול חדש (`dayTabsScrolledOnce`) שמונע קפיצה גם בבחירת טאב ידנית וגם בכל עדכון realtime שמגיע מ-`listen()`'s `onValue` (שמפעיל `applyLanguage()`←`renderDayTabs()` בכל שינוי בטיול, לא רק בטעינה) - נבדק בהמשך שהוא לא נדרס בטעות
- נבדק בפועל: טיול ארוך (21 ימים) עם "היום" באמצע - הרצועה נגללת כך שהטאב שלו גלוי (אומת עם `getBoundingClientRect`, לא רק ויזואלית); טיול עתידי (היום לא בטווח) - `scrollLeft` נשאר `0`, אין קפיצה; בחירת טאב ידנית אחרי גלילה ידנית של המשתמש - הרצועה **לא** קופצת בחזרה (אומת: `scrollLeft` זהה לפני ואחרי הקליק); עברית (RTL, `scrollLeft` שלילי - תואם לממצא הקיים מ-`.quick-picks`) ואנגלית (LTR, `scrollLeft` חיובי) - שתיהן עובדות נכון עם אותה קריאת `scrollIntoView` בלי טיפול מיוחד לכיוון; מסך 375px
- `node scripts/i18n-audit.js itinerary.html` - 0 ממצאים (לא נוספו מחרוזות משתמש)

## v4.23.1 — תיקון: "היום" חושב לפי אזור הזמן של הרגל האחרונה, לא הפעילה (שלב 5, commit ג)

### 🐛 תיקון
- **קטגוריה שלישית של באג אזור-זמן (§8ব סעיף 7), נדחתה עד שאפשר היה לבדוק על טיול מרובה-רגליים אמיתי - עכשיו אפשר.** ארבעה מקומות חישבו "מה התאריך/השעה עכשיו" לפי `getLastLeg(trip).timezone` - נכון רק כשהרגל הפעילה היא גם האחרונה. בטיול 3 רגליים, כשהמשתמש ברגל האמצעית, "היום" חושב לפי אזור הזמן של יעד עתידי שעוד לא הגיעו אליו
- **`window.getCurrentLegTimezone(trip)` חדש ב-`legs.js`** - `getLegForDate(trip, new Date())?.timezone`, נופל ל-`getLastLeg(trip).timezone` ואז `null` (הרחבת שרשרת ה-fallback הקיימת ב-§4 "רגל בלי timezone → timezone של הטיול → שעון המכשיר", לא כלל חדש). פונקציה, לא משתנה גלובלי - הרגל הפעילה משתנה תוך כדי הטיול, מחושבת מחדש בכל קריאה
- **תוקנו 3 מתוך 4 המקומות**: `index.html`'s `todaysPlanContext()`, `itinerary.html`'s `renderDayTabs()` ו-`computeLiveActivityId()`. `index.html` אין לו אובייקט `trip` (הכל משוטח ב-`hydrateTripData()`) - נקרא עם עטיפה `{legs: currentLegs}`, לא טיול אמיתי; `currentLegs` כבר `getLegs()`'s output כך שזה תקין לרגל אחת ומרובה כאחד
- **`fetchForecast()`'s `todayKey` (index.html) - במפורש לא תוקן.** נבדק ונדחה: הקריאה ל-Open-Meteo עדיין שולחת קואורדינטה יחידה (הרגל הראשונה, `timezone=auto` - מגבלה מתועדת בשלב 8), כך שכל `d.dateKey` שחוזר כבר מעוגן לרגל הראשונה. שינוי `todayKey` לרגל הפעילה בלי לתקן גם את הקריאה עצמה היה שובר את ההתאמה ביניהם - רגרסיה בתחפושת של תיקון. נשאר `tripTimezone`, מתועד ב-`docs/schema-legs.md` §8 (שלב 8) כחלק ממה שצריך להיסגר שם
- נבדק בפועל, מול הזמן האמיתי כרגע (לא רק תאריכים מזויפים): טיול 3 רגליים עם פערי אזורי-זמן גדולים (ניו יורק/בנגקוק/הונולולו) - ב-2026-09-08 בפועל "היום" בבנגקוק (הרגל הפעילה) ו"היום" בהונולולו (הרגל האחרונה, המקור הישן) **נבדלו בפועל ביום שלם** (`2026-09-08` מול `2026-09-07`) - `todaysPlanContext()` ו-`renderDayTabs()` בחרו נכון לפי בנגקוק; לפני הטיול ואחריו - הערך זהה **בית-לבית** למה ש-`getLastLeg` כבר החזירה היום, לא רק "לא קורס"; טיול רגל אחת - זהה ל-`getLastLeg` תמיד, אפס שינוי; `computeLiveActivityId` סימנה נכון את הפעילות האחרונה שכבר עברה, לפי שעון בנגקוק בזמן אמת
- `node scripts/i18n-audit.js index.html itinerary.html` - 0 ממצאים (לא נוספו מחרוזות משתמש)

## v4.23.0 — `countryCode` עובר לשדה per-leg (שלב 11ব.3, תיקון עוקב)

### ✨ שינוי סכמה
- **`legs[i].countryCode` חדש** - `buildLegs` (`legBuilder.js`) שומרת אותו לכל רגל מ-geocoding, בדיוק כמו `timezone`. `deriveTripFields` גוזרת את `trip.countryCode` מ-`legs[0].countryCode` במקום מ-`legLocations[0]` הזמני
- **פותר את הממצא שדווח בבדיקות 11ব.2**: שינוי סדר שהזיז רגל לא-`dirty` (לא נגעו בשם שלה) למיקום 0 לא ריענן את `trip.countryCode` - עכשיו כל רגל נושאת את הדגל שלה, אז אחרי שינוי סדר `deriveTripFields` קוראת מהרגל הנכונה. הקוד המיוחד באדמין (`i === 0 ? countryCode : ""`) הוסר - מיותר
- **fallback הכרחי לתאימות אחורה**: רגליים שנוצרו לפני v4.23.0 לא נושאות את השדה - `deriveTripFields` הייתה מחזירה '' עבורן. בלי נפילה חזרה, השמירה הראשונה של רגל ישנה כזו (גם בלי לגעת בשם שלה) הייתה מאפסת דגל שכבר היה נכון. נוסף: `tripFields.countryCode = tripFields.countryCode || countryCode` (הערך הגלובלי הקיים) מיד אחרי `deriveTripFields` בפאנל האדמין
- נבדק בפועל: טיול חדש דרך הוויזרד עם 2 רגליים במדינות שונות (רומא/בנגקוק) - כל רגל עם `countryCode` משלה (`IT`/`TH`), נכון; **שינוי סדר בפאנל - בדיוק הבדיקה שהתחילה את זה** - הדגל ברמת הטיול עבר מ-`IT` ל-`TH` בהתאמה לרגל שהפכה ראשונה; טיול "ישן" עם רגליים בלי `countryCode` בכלל - שמירה בלי לגעת בכלום השאירה `trip.countryCode` בדיוק כפי שהיה (fallback עבד), הרגליים עצמן נשארו בלי השדה (יקבלו אותו רק כששמן ישתנה בפעם הבאה - צפוי, לא תוקן רטרואקטיבית); רגל אחת - אפס שינוי, `legs` לא נכתב, זהה להיום
- `node scripts/i18n-audit.js index.html wizard.html` - 0 ממצאים (לא נוספו מחרוזות משתמש)

## v4.22.0 — פאנל אדמין: שמירה בפועל של רגליים (שלב 11ব.2)

### ✨ תכונה חדשה
- **`saveFlightBtn` כותב עכשיו רגליים בפועל** - לא כפתור חדש, אותו כפתור, תנאי ההסתעפות עבר מ-`currentLegs.length > 1` (מצב שמור) ל-`adminLegRowCount > 1` (מצב העורך החי) - זה מה שבאמת קובע מה יישמר. `tripType`/`checkinLink`/`showAccent` ממשיכים להישמר תמיד
- **geocoding רק לרגליים ששמן השתנה או שנוספו** (`dirty`, מ-11ব.1) - רגל שלא נגעו בה משתמשת ב-`lat`/`lon`/`timezone` המקוריים שלה (מ-`originalLeg`, אובייקט הרגל המלא שהשורה נזרעה ממנו - לא רק שם), אפס קריאת רשת
- **`buildLegs` ואז `deriveTripFields`** (שתיהן מ-`legBuilder.js`) - **לא שכפול inline**. זה בדיוק הסכנה שסומנה לפני הכתיבה - `deriveTripFields` כבר תיקנה פעם אחת באג ש-inline-קוד יצר (v4.20.2), וזה בדיוק למה היא קיימת כפונקציה משותפת אחת
- **`vibes` פר-רגל נשמרים** - שינוי שם רגל לא מוחק את ה-`vibes` הקיימים שלה (נשמרים מ-`originalLeg.vibes`, בלי קשר ל-`dirty`); רגל חדשה מקבלת `[]` (אין UI לעריכת vibes בפאנל עדיין - לא ניחוש)
- **ולידציית סדר-כרונולוגי לפני שמירה** - כל רגל נבדקת ש-`endAt` שלה מאוחר מ-`startAt` שלה (מספיק כדי להבטיח שכל שרשרת ה-boundaries עולה מונוטונית, כי הרציפות בין רגליים כבר מובטחת מבנית). כשל **חוסם את השמירה** ומציג הודעה שמזהה איזה יעד — לא "שגיאה" כללית. תוקן אחרי שהוכר שזה תיקון לחור אמיתי, לא רק הגנה - בניגוד לוויזרד (שם הגבולות תמיד מחושבים אוטומטית), כאן המנהל מקליד תאריכי מעבר ידנית, אז משך שלילי דורש טעות הקלדה אחת
- **מחיקה חזרה לרגל אחת כותבת `legs: null` במפורש** כש-`currentLegs.length > 1` (היה מרובה) - **תיקון לבאג שקט אמיתי**, לא רק ניקוי: `update()` לא מוחק מפתחות שלא מצוינים, אז בלי זה `legs` הישן היה נשאר ב-RTDB ו-`getLegs()` ממשיכה להעדיף אותו על פני השדות הסקלריים החדשים (§6) - העריכה "מצליחה" ולא קורית בפועל
- נבדק בפועל (console fixtures + לכידת `db.ref().update`, ספירת קריאות `fetch` לגיאוקוד): רגל אחת + שינוי שם - `legs` לא נכתב, זהה להיום; הוספת רגל שנייה - `legs` נכתב עם 2, רגל 0 חדשה נגזרת (`destination`/`lat`/`lon`), אפס geocoding לרגל שלא נגעו בה, vibes שלה נשמרים; מחיקת הרגל האחרונה מתוך 3 - `timezone`/`endAt` מתעדכנים לרגל שהפכה אחרונה; שינוי סדר - `destination`/`timezone` מתחלפים; שינוי שם רגל בלבד - geocoding רץ בדיוק פעם אחת (נספר בפועל), console.warn נבדק בכשל geocoding; שינוי תאריך מעבר בלבד - אפס קריאות geocoding; תאריך מעבר לא תקין (לפני תחילת הטיול) - השמירה נחסמה, הודעה מזהה את היעד הבעייתי; רגל אחת → מרובה → אחת - `legs: null` נכתב בפועל, ואומת ש-`getLegs()` על האובייקט שאחרי הכתיבה מחזירה נכון רגל סינתטית מהשדות הסקלריים; רציפות (`legs[n].endAt === legs[n+1].startAt`) מאומתת בכל אחת מהבדיקות למעלה; startAt זהה בית-לבית לפני/אחרי בטיול מרובה-רגליים (השדה חסום, לא נערך) - לא רק "נראה נכון"
- **ממצא שנתפס בבדיקה, לא תוקן**: שינוי **סדר** שמזיז רגל לא-משתנה-שם למיקום 0 לא מרענן את `countryCode` ברמת הטיול (עדיין הערך הישן, מהרגל שהייתה במיקום 0 לפני השינוי) - כלל ה-geocoding-רק-לרגליים-ששונו לא כולל מנגנון "רענן countryCode כשמיקום 0 מתחלף". משפיע רק על תצוגת דגל-מדינה - lat/lon/timezone/תאריכים כולם נכונים. לא תוקן, מחוץ להיקף שהוגדר
- `node scripts/i18n-audit.js index.html` - 0 ממצאים (לא נוספו מחרוזות משתמש חדשות - הודעת השגיאה נבנית עם `tr()` inline, אותו דפוס כמו שאר ה-`customAlert`-ים בקובץ)

## v4.21.0 — פאנל אדמין: עורך רגליים בזיכרון (שלב 11ב.1)

### ✨ תכונה חדשה
- **רשימת יעדים ניתנת לעריכה בפאנל האדמין** - הוספה, הסרה, שינוי סדר, שינוי שם - **בזיכרון בלבד, בלי geocoding ובלי שמירה** (commit הבא, 11ব.2, יסגור את זה). `#adminDestInput` הופך לשורה הראשונה של אותה רשימה - בדיוק כמו `#tripDestination` בוויזרד (שלב 10, commit א) - לא שדה נפרד. אותו איחוד קוד: `saveFlightBtn`'s handler ממשיך לקרוא מאותו אלמנט בדיוק, בלי שינוי
- **זריעה מ-`currentLegs`** (כבר `window.getLegs()`'s output, מתעדכן ב-`hydrateTripData()`) - לא קריאה חוזרת ל-`getLegs()` עם אובייקט טיול מאולתר (אין ל-`index.html` כזה, §8ব.1). זה מה שהופך טיפול ברגל-אחת ומרובה-רגליים לאותו קוד בדיוק - `currentLegs.length` הוא 1 או N, בלי `if` נפרד
- **הרשימה מתקפלת לשדה בודד ברגל אחת** - בדיוק אותו היגיון שכבר הוכרע בוויזרד (§5.6): בלי תג סידורי, בלי חץ, בלי כפתור הסרה, כשנשארת רגל אחת (בין אם זה מצב הטיול המקורי או שהמנהל מחק חזרה לאחת באותה עריכה)
- **תאריכי מעבר בין רגליים** - שורה בין-רגלית לכל גבול, בדיוק כמו בוויזרד. **בפתיחת הפאנל** נזרעים מהגבול האמיתי השמור (`legs[i].endAt`, מפורש בשעון הרגל היוצאת - `toZonedInputValue`/`window.dateKeyInZone`) - לא ברירת מחדל. **בכל הוספה/הסרה** מחושבים מחדש לברירת מחדל בחלוקה שווה (`window.splitEqualBoundaries`, מ-`legBuilder.js` - לא שוכפל) על כל התאריכים הנראים, **כולל דריסת גבולות אמיתיים שכבר היו שמורים** - אותו כלל בדיוק שכבר אושר בוויזרד ("תאריך מעבר שנערך ידנית לא שורד הוספה/הסרה מאוחרת יותר"), מוחל עכשיו גם על גבולות אמיתיים, לא רק טיוטות. שום שמירה אוטומטית לא קורית - המנהל רואה את הניחוש ומתקן לפני ש-11ব.2 בכלל קיים
- **דגל "השתנה" (`dirty`) לכל רגל** - לא לגיאוקוד, נועד ל-11ব.2: מסמן אם שם הרגל השתנה מאז שנטען (או שהיא חדשה), כדי שהשמירה העתידית תבצע geocoding רק לרגליים ששונו/נוספו - לא לכל רגל בכל שמירה (קריאת רשת מיותרת, סיכון שרירותי לקואורדינטה שונה במקצת לאותה עיר)
- **שדות התאריך הכוללים (`adminFlightInput`/`adminEndInput`) נשארים חסומים** לטיול מרובה-רגליים - לא בהיקף כאן; טקסט ה-hint צומצם בהתאם (רק תאריכים, לא יעדים)
- **`legBuilder.js` נטען עכשיו גם ב-`index.html`** (אחרי `datetime.js`, לפני `auth-gate.js`)
- נבדק בפועל: טיול רגל אחת - נראה בדיוק כמו היום, הוספה חושפת שרשרת (מספור, חץ, הסרה) + שורת מעבר עם ברירת מחדל מחושבת נכון; טיול 3 רגליים אמיתי (רומא/בנגקוק/טוקיו, timezone שונה) - נזרע עם שלושת השמות והתאריכים האמיתיים (אומת נומרית: `06:00 10/10/2026`=`04:00Z` בשעון רומא/CEST, `14:00 12/10/2026`=`07:00Z` בשעון בנגקוק/UTC+7); הסרת רגל אמצעית - שינוי סדר נכון + מעברים מחושבים מחדש; שינוי סדר (הזזה למעלה); קיפול חזרה לרגל אחת אחרי הוספה+הסרה; **הגגה המתועדת מאומתת בפועל** - הוספת רגל + שמירה בטיול (עדיין) רגל-אחת כותבת רק את `destination` המעודכן של רגל 0, מתעלמת בשקט מהרגל השנייה שלא נשמרה, בלי שגיאה; עברית/אנגלית (כולל תיקון: תווית "מעבר"/"Transition" מתעדכנת מחדש בכל פתיחת פאנל, לא רק בבנייה הראשונה); מצב כהה; מסך 375px
- `node scripts/i18n-audit.js index.html` - 0 ממצאים

## v4.20.3 — חילוץ `legBuilder.js` מ-wizard.html (שלב 11, commit ב - רה-פקטור)

### ♻️ רה-פקטור (אפס שינוי התנהגות)
- **קובץ חדש `legBuilder.js`** - פונקציות טהורות לבניית `legs[]` מקלט גולמי (בלי DOM, בלי מצב גלובלי): `splitEqualBoundaries` (חלוקה לינארית טהורה), `buildLegs` (בניית `boundaries[]` והנגזרת ל-`legs[]`, כולל אכיפת הרציפות המבנית וכלל שעון-הרגל-היוצאת), `deriveTripFields` (גזירת `destination`/`lat`/`lon`/`countryCode`/`timezone`/`startAt`/`endAt` מ-`legs[0]`/`legs[legs.length-1]`). נטען כרגע רק ב-`wizard.html` - לא ב-4 העמודים שלעולם לא בונים `legs` (packing/places/shopping/itinerary), ולא עדיין ב-`index.html` (יתווסף ב-11ב/ג כשהפאנל בונה רגליים בפועל)
- **לא ב-`legs.js`, במכוון** - `legs.js` הוא קריאה/נרמול בלבד ("בהינתן טיול שכבר יש לו legs, תחזיר תצוגה"); הפונקציות החדשות הן בנייה ("בהינתן קלט גולמי, תבנה legs חדש"). אותו נימוק בדיוק שהוציא את `datetime.js` מ-`legs.js` (ר' הערת הכותרת של `datetime.js`) - ציר שונה (זמן-ביום מול מבנה-טיול), אותה מסקנה: לא לערבב שתי אחריות בקובץ אחד
- **`wizard.html`'s `generateTripAndSave()` ו-`recomputeTransitionDefaults()` עוברות לקרוא ל-`window.buildLegs`/`window.deriveTripFields`/`window.splitEqualBoundaries`** - הלוגיקה המקומית נמחקה פיזית, לא הוקפאה. קריאות DOM (ערכי טופס, כתיבה ל-`<input>`) נשארות בוויזרד; החישוב עצמו זז
- **`localInputToUtcInZone` הועברה ל-`datetime.js`** - הייתה משוכפלת בית-לבית בין `wizard.html` ו-`index.html` (אומת ב-diff לפני ההעברה - רק סגנון מרכאות/הזחה שונה, שתי הפעמים העתק זהה בהתנהגות). שני העותקים נמחקו פיזית; `index.html`'s קריאה נשארה `localInputToUtcInZone(...)` ללא שינוי (script קלאסי - הזיהוי הגלובלי עובד אוטומטית מ-`datetime.js` שכבר נטען שם)
- **ולידציית רציפות (`legsAreContiguous`) - נדחתה ל-11ב במכוון**, לא חולצה עכשיו. אין קוד קיים לה היום - הרציפות מובטחת מבנית ב-`buildLegs`, לא נבדקת בנפרד, ובוויזרד אין דרך למשתמש לשבור אותה. תוסיף ערך רק כשהפאנל (11ב) בונה רגליים מעריכת משתמש בפועל
- נבדק בפועל: **בדיקה 2 (ההוכחה)** - טיול רומא/בנגקוק/טוקיו, אותו קלט בדיוק לפני ואחרי החילוץ - payload זהה בית-לבית (חוץ מ-`id`/`createdAt` האקראיים): `destination`/`lat`/`lon`/`countryCode`/`timezone`/`startAt`/`endAt` ברמת הטיול, ושלוש הרגליים (שם/קואורדינטות/timezone/`startAt`/`endAt`) זהים; טיול ברלין יעד יחיד - payload זהה בית-לבית להיום, `legs` לא נכתב; `splitEqualBoundaries` בקונסול - טיול 3 ימים/4 רגליים מייצר 3 גבולות שונים (לא באורך אפס, לא כפולים); רציפות `legs[n].endAt === legs[n+1].startAt` מאומתת בבדיקה 2. אין שגיאות קונסולה חדשות בטעינת `wizard.html`/`index.html` (רק רעש `PERMISSION_DENIED` הרגיל של הסביבה)
- `node scripts/i18n-audit.js wizard.html index.html` - 0 ממצאים (לא נוספו מחרוזות משתמש)

## v4.20.2 — תיקון: trip.timezone נכתב מהרגל הראשונה במקום האחרונה (שלב 11, commit א.5)

### 🐛 תיקון
- **כל טיול מרובה-רגליים שנוצר מאז v4.20.0 (2026-09-07) נשמר עם `timezone` שגוי.** `generateTripAndSave()` ([wizard.html](wizard.html)) כתבה `timezone: legs[0].timezone` - הרגל **הראשונה** - בעוד `destination`/`lat`/`lon` נכונים מהראשונה (§2.1) ו-`endAt` נכון מהאחרונה (§4). `trip.timezone` צריך לשקף את **אותה רגל** כמו `endAt` (שתיהן "הרגל האחרונה", אותו נימוק) - `index.html`'s `hydrateTripData()` כבר קורא לפי ההנחה הזו (`tripTimezone = lastLeg.timezone`), אז כל טיול מרובה-רגליים היה עם `timezone` שלא תואם לאזור הזמן שבו `endAt` בפועל מחושב
- נבדק את שאר השדות ברמת הטיול (`destination`/`lat`/`lon`/`countryCode`/`startAt` מהרגל הראשונה, `endAt` מהאחרונה) - כולם נכונים, `timezone` היה החריג היחיד
- **תוקן**: `timezone: legs[legs.length - 1].timezone`
- `scripts/find-desynced-legs.js` (נוסף ב-v4.20.1) כבר בודק בדיוק את זה - `trip.timezone !== legs[last].timezone` - לא נדרש שינוי בסקריפט
- נבדק בפועל (console fixture, לכידת ה-payload לפני הכתיבה): טיול 3 רגליים (רומא/בנגקוק/טוקיו) - `trip.timezone` כעת `"Asia/Tokyo"`, תואם ל-`legs[2].timezone` ול-`trip.endAt`; טיול רגל אחת (ברלין) - רגרסיה, ללא שינוי

## v4.20.1 — תיקון: פאנל האדמין דרס legs בשינוי יעד/תאריכים (שלב 11, commit דחוף)

### 🐛 תיקון
- **באג מאושר בפרודקשן**: שינוי יעד בטיול מרובה-רגליים מהפאנל כתב `destination`/`lat`/`lon`/`countryCode`/`timezone` ברמת הטיול בלי לגעת ב-`legs` בכלל - `trip.destination` ו-`legs[0].name` נותרו סותרים (`getLegs()` ממשיכה להחזיר `legs`, אז התוכנית היומית ומזג האוויר ממשיכים לפי הרגל הישנה בעוד הכותרת מציגה את החדשה). **וקטור שני, פחות מיידי**: אותה שמירה כתבה מחדש `startAt`/`endAt` ברמת הטיול **בכל שמירה**, גם בלי לגעת ביעד - סותר את `legs[0].startAt`/`legs[last].endAt` (§4) בלי קשר לשינוי יעד. עלה בחקירת שלב 11 (עריכת רגליים בפאנל האדמין)
- **תוקן בחסימה, לא בסנכרון** - הוחלט במפורש נגד סנכרון חלקי: עדכון `legs[0]` רק בשם/קואורדינטות/timezone בלי לגעת ב-`startAt`/`endAt` היה משאיר את הרגל **פנימית לא עקבית** (שדה `timezone` מצביע על יעד אחד, בעוד האינסטנט ב-`startAt`/`endAt` חושב עבור אחר) - קורפציה חדשה, גרועה מהבאג הנוכחי. `getLegs()`/§6 כבר קובעים ש-`legs` הוא מקור האמת בטיול מרובה-רגליים - הפאנל הישן לא בנוי לערוך רגל בודדת מתוך רשימה, אז חוסמים עד שעריכת רגליים אמיתית (§5.7) קיימת
- שדה היעד, מועד ההתחלה ומועד הסיום בפאנל **מנוטרלים (לא מוסתרים) עם הסבר** כש-`currentLegs.length > 1` - `disabled` על שלושת השדות + שורת `admin-hint` ("עריכת יעדים ותאריכים בטיול מרובה יעדים תתאפשר בקרוב"). שאר שדות הפאנל (סוג טיול, צ'ק-אין, מבטא צבעוני, מודולים) נשארים פעילים ללא שינוי
- **החסימה גם בצד הכתיבה, לא רק ב-UI**: `saveFlightBtn`'s handler מדלג לגמרי על geocoding, ולידציית תאריך/יעד, ועדכון המשתנים הגלובליים (`destinationName`/`latCoord`/`lonCoord`/`countryCode`/`tripTimezone`/`flightOut`/`tripEnd`) כש-`multiLeg` אמת - לא רק על נטרול השדה. שדה `disabled` בלבד לא היה מספיק (קריאה ל-`.value` דרך JS עדיין עובדת על שדה מנוטרל) - האובייקט שנכתב ל-RTDB כולל רק `tripType`/`checkinLink`/`showAccent` בטיול מרובה-רגליים, לא `destination`/`lat`/`lon`/`countryCode`/`timezone`/`startAt`/`endAt`
- **`scripts/find-desynced-legs.js`** (כלי חדש, לא נטען לדפדפן - בלי version bump) - סורק את `/trips` ב-RTDB דרך ה-REST API, ומזהה טיולים מרובי-רגליים שבהם `trip.destination`/`lat`/`lon` לא תואמים ל-`legs[0]`, או `trip.timezone`/`endAt` לא תואמים ל-`legs[אחרון]` (ומדווח גם, כמידע משני, אי-התאמת `startAt` ל-`legs[0].startAt` - §4 מגדיר את אלה כשני שדות שונים סמנטית, אבל אין כיום מנגנון לגיטימי שגורם להם לסטות חוץ מהבאג הזה)
- נבדק בפועל (console fixtures, `db.ref().update` מוחלף זמנית בלכידה): טיול רגל אחת - שלושת השדות פעילים, השמירה כותבת `destination`/`lat`/`lon`/`countryCode`/`timezone`/`startAt`/`endAt` בדיוק כמו לפני התיקון (רגרסיה); טיול 3 רגליים (רומא/בנגקוק/טוקיו) - שלושת השדות מנוטרלים, ההסבר מוצג, השמירה כותבת אך ורק `{tripType, checkinLink, showAccent}` - `currentLegs` ומשתני רמת-הטיול (`destinationName`/`latCoord`/`lonCoord`/`tripTimezone`/`flightOut`/`tripEnd`) נשארים בדיוק כפי שהיו; עברית/אנגלית; מסך 375px, מצב בהיר
- `node scripts/i18n-audit.js index.html` - 0 ממצאים

## v4.20.0 — ויזרד: תאריכי מעבר, geocoding מקבילי, שמירת legs (שלב 10, commit ב מתוך 2)

### ✨ תכונה חדשה
- **תאריכי מעבר בין רגליים** - N-1 שדות, לא 2N: כל שדה הוא בו-זמנית `legs[n].endAt` וגם `legs[n+1].startAt`, אותו ערך בדיוק. נערכים לאורך כל הוויזרד כמחרוזת נאיבית (`datetime-local`/`date`, בדיוק כמו `#tripStart`/`#tripEnd` היום) - אין timezone פר-רגל ידוע לפני geocoding, אז אין מה לזון בזמן עריכה
- **חלוקת ברירת מחדל שווה** של הטווח הכולל (`#tripStart`→`#tripEnd`) בין הרגליים, ניתנת לתיקון ידני. מחושבת מחדש רק בהוספה/הסרה של רגל (`recomputeTransitionDefaults()`) - לא על כל הקלדה, ולא בהזזה (סדר בלבד, לא משנה טווח). חלוקה לינארית של טווח חיובי לעולם לא מייצרת גבול כפול או באורך אפס - רק קצר (טיול 3 ימים/4 רגליים ⇐ רגליים של כ-18 שעות) - מקובל, בהתאם לעיקרון "ניתנות לתיקון ידני"; לא נוספה ולידציית אורך-מינימלי
- **geocoding מקבילי** לכל הרגליים (`Promise.all`, אותה `geocodeDestination()` הקיימת ללא שינוי). כשל ברגל בודדת לא עוצר את השמירה - אותה רגל נשמרת בלי `lat`/`lon`/`timezone` (null) + `console.warn` עם שם הרגל, שאר הרגליים והטיול נשמרים תקין, בלי הודעת UI - אותו דפוס הידרדרות שקיים היום כשגם המסלול הבודד נכשל ב-geocoding
- **`generateTripAndSave()` בנוי מחדש לנתיב אחיד**: תמיד בונה `legs[]` פנימית (גם עבור יעד יחיד), ושדות רמת-הטיול (`destination`/`lat`/`lon`/`timezone`/`countryCode`/`startAt`/`endAt`) נגזרים אחידה מ-`legs[0]`/`legs[אחרון]`. `legs` **מצורף ל-payload רק כש-`legs.length > 1`** - טיול יעד יחיד נשמר זהה בדיוק להיום (§2.1/§10, לא רגרסיה)
- **רציפות מובטחת מבנית, לא בבדיקה**: כל גבולות הרגליים (התחלת טיול, כל מעבר, סוף טיול) נבנים פעם אחת למערך `boundaries[]` משותף; `legs[i].startAt`/`legs[i-1].endAt` קוראים את **אותו איבר מערך** פעמיים - לא שני חישובים נפרדים שעלולים לסטות. אומת בפועל: `legs[n].endAt === legs[n+1].startAt` כמחרוזת ISO זהה, לא ערך קרוב
- **תאריך מעבר מתפרש בשעון הרגל היוצאת** (לא הנכנסת) - הכללה של שני כללים שכבר הוכרעו, לא החלטה חדשה: §5.2 ("יום המעבר שייך לרגל היוצאת", מיום ליחידת-זמן), וההגדרה הקיימת ש-`endAt` תמיד בשעון הרגל שלו עצמו (§4, שכבר חל על `trip.endAt`, כאן מוכלל לכל גבול פנימי)
- **`legs[0].startAt = trip.startAt` בדיוק, בלי re-zoning** - הערת קוד מפנה במפורש ל-§4 ("שני דברים סמנטית שונים שקורים להיות זהים היום") כדי שזה לא "יתוקן" בעתיד למשהו שנראה עקבי יותר
- **`vibes`** - הקלט נשאר בחירה יחידה ברמת הטיול בוויזרד (כמו שהוכרע); בשמירה, אותם vibes שנבחרו נכתבים למערך `vibes` של **כל** רגל
- **שלב הסיכום (4) מציג יעדים מרובים** - "יעד"/"יעדים" (יחיד/רבים) עם שרשרת חצים תלוית-כיוון (`←` בעברית RTL, `→` באנגלית LTR)
- נבדק בפועל מול Open-Meteo אמיתי: טיול רומא/בנגקוק/טוקיו (3 רגליים, אזורי זמן אמיתיים כולל DST שונה) - `legs` נכתב, רציפות מלאה, `destination`/`lat`/`lon` מהרגל הראשונה, `endAt`/`timezone` מהאחרונה; טיול ברלין יעד יחיד - **רגרסיה**: `legs` לא נכתב כלל, payload זהה לפני השינוי; כשל geocoding מודמה ברגל 2 - הרגל נשמרת עם שדות null + warning, השאר תקין; חלוקת תאריכים כפונקציה טהורה בקונסול; **הקבלה האמיתית** - הרצת `getLegs()`/`tripDayDates()` (מ-`legs.js`) ישירות מול הטיול שזה עתה נשמר: 3 רגליים נכונות, ימים משויכים נכון, אפס אזהרות פער
- `node scripts/i18n-audit.js wizard.html` - 0 ממצאים

## v4.19.0 — קלט רשימת יעדים בוויזרד (שלב 10, commit א מתוך 3)

### ✨ תכונה חדשה
- **רשימת יעדים דינמית בוויזרד** - הוספה, הסרה, הזזה למעלה, עד 6 יעדים. `#tripDestination` הקיים הופך לשורה הראשונה של אותה רשימה - **לא שדה נפרד** - "מסלול אחד" לאותו נתון, בדיוק הנימוק שכבר תיקן את הכפילות ב-`tripDayDates`/`zonedParts`
- **מאגר שורות קבוע (עד 6), לא DOM שנוצר/נהרס.** הוספה/הסרה/הזזה מזיזות **ערכים** בין שורות קיימות, לא צמתים ב-DOM - כך `#tripDestination` תמיד נשאר אותו רכיב פיזי (כל שאר הקוד בקובץ - `validateStep`, `generateTripAndSave`, `saveWizardDraft`/`restoreWizardDraft`, `resetWizard`, `renderSummary` - ממשיך לקרוא ממנו בלי שום שינוי)
- **תצוגה מקדימה (דגל) עצמאית לכל שורה** - טיימר דיבאונס נפרד per-row, לא משתנה מודול יחיד כמו קודם. הדגל **מחוץ** לשדה (לא overlay כמו הגרסה הישנה) - נבדק בפועל: עם overlay, טקסט יעד ארוך הגיע ל-5px משוליים מהדגל; בגרסה החדשה השדה חותך ב-ellipsis במקום לחפוף
- **חצי סידור-מחדש: חץ יחיד "הזז למעלה", לא זוג חצים מוערמים.** נבדק בפועל בנייד: זוג מוערם ב-32px רוחב יוצא כ-22px גובה לחץ - מתחת לסף מגע (48px, לפי הקונבנציה הקיימת בקובץ). חץ יחיד מלא = 44-48px אמיתי. אפשר להגיע לכל סדר עם כיוון אחד בלבד
- **מחיקה בחזרה ליעד אחד מקפלת את הרשימה** - חוזרת בדיוק למראה שדה בודד (התג הסידורי, החץ וה-✕ נעלמים, "נוסעים ליותר ממקום אחד?" חוזר) - לא נשארת פתוחה עם שורה יתומה
- `saveWizardDraft`/`restoreWizardDraft`/`resetWizard` מעודכנים - `draft.legs` הוא הפורמט הנוכחי, `draft.destination` נשאר רק כ-fallback לטיוטה ישנה מגרסה קודמת
- **בהיקף במפורש**: הקלט בזיכרון בלבד. **לא בהיקף**: תאריכי מעבר, geocoding בשמירה, כתיבת `legs` ל-RTDB, `vibes` - הכל commit ב. ה-payload הנשמר זהה לגמרי להיום; טיול עם יעד אחד נשמר בדיוק כמו קודם
- נבדק בפועל: הוספה עד 6 (הכפתור נעלם), הסרת שורה אמצעית (המספרים הסידוריים מתעדכנים, `+ הוספת יעד` חוזר), הזזה למעלה כולל שורה 1 (החץ שם מנוטרל, הקליק לא עושה כלום), תצוגה מקדימה על 2 שורות בו-זמנית (דגלים נכונים, בלי החלפה), draft (3 יעדים, שמירה, רענון עמוד אמיתי, שחזור אוטומטי), `resetWizard()` (הכל חוזר ל-1, כולל ניקוי שורות מוסתרות), **רגרסיה** (יעד בודד בלי לגעת ברשימה - `legRowCount` נשאר 1, `#tripDestination.value` בדיוק כמו קודם), החלפת שפה (מחרוזות + aria-label מתעדכנים בכל השורות), עברית/אנגלית, צילום נייד 375px עם 4 שורות
- `node scripts/i18n-audit.js wizard.html` - 0 ממצאים

## v4.18.2 — תיקון: fetchForecast פירשה flightOut לפי אזור הזמן הלא נכון (שלב 5, commit ב מתוך 3)

### 🐛 תיקון
- **`tripStartKey` ב-`fetchForecast()` פירשה `flightOut` (=`trip.startAt`) לפי `tripTimezone` (רגל אחרונה) במקום רגל ראשונה** - אותה משפחת באג שתוקנה ב-`tripDayDates()` בשלב 4א. שורה אחת בלבד שונתה - `todayKey` ו-`tripEndKey`, השורות מעליה ומתחתיה, נשארו כפי שהיו (שתיהן תקינות)

### ✨ תכונה חדשה
- **`currentLegs` - מערך רגליים גלובלי חדש ב-index.html**, מוגדר ב-`hydrateTripData()` ליד `tripTimezone` (`window.getLegs(legInput, tripId)` - לא מ-`data` הגולמי, כדי להתאים ל-`primaryLeg`/`lastLeg` שכבר מחושבים מ-`legInput` באותה פונקציה, כולל נפילת התאריכים הישנים ל-`flightOut`/`tripEnd`). לא `primaryLegTimezone` בודד - מערך שלם, כי הקטגוריה הבאה (§8ב סעיף 7, "הרגל הפעילה עכשיו") לא ניתנת לפתרון עם משתנה קבוע יחיד, ואין טעם להוסיף משתנה גלובלי חדש לכל צורך עתידי
- **`tripStartKey` משתמשת ב-`currentLegs[0].timezone`** - לא ב-`getPrimaryLeg(trip)` ישיר כפי שהוצע קודם ב-§8: אומת ש-`index.html` לא מחזיקה אובייקט `trip` בשום מקום (הכל משוטח למשתנים ב-`hydrateTripData` ונזרק) - קריאה ישירה ל-`getPrimaryLeg(trip)` הייתה דורשת בניית אובייקט מזויף, שהיה חסר את ה-`legs` האמיתיים ומחזיר בדיוק את אותו ערך שגוי מחדש
- נבדק בפועל: ברלין (רגל אחת) - הרצועה נטענת עם נתונים אמיתיים מ-Open-Meteo, `tripStartKey` תואם ליום הראשון, אפס שינוי. טיול מזויף רומא/בנגקוק (2 רגליים, timezone שונה) - נוסחה ישנה (`tripTimezone`) נותנת `2026-10-06` (שגוי), `currentLegs[0].timezone` נותן `2026-10-05` (נכון) - אותם ערכים בדיוק כמו בדיקה 10 של שלב 4א. אין שגיאות קונסולה חדשות
- `node scripts/i18n-audit.js index.html` - 0 ממצאים

## v4.18.1 — תיקון: פאנל האדמין כתב endAt לפי אזור הזמן הישן (שלב 5, commit א מתוך 3)

### 🐛 תיקון
- **`tripEnd` חושבה לפני שהחיפוש הגאוגרפי עדכן את `tripTimezone`.** בשמירת פאנל האדמין, אם אדמין שינה יעד **וגם** את מועד הסיום באותה שמירה - `tripEnd` נכתבה לפי אזור הזמן של היעד **הישן**, בעוד `trip.timezone` עצמו נכתב עם הזמן **החדש**. לא קשור למעבר ל-legs בכלל - קיים גם על טיול רגל אחת, קיים היום
- **תוקן**: חישוב `tripEnd` הועבר לאחר בלוק ה-geocoding, לא לפניו. נבדק שזו הנקודה היחידה בתוך ה-handler הזה עם התלות הזו - `latCoord`/`lonCoord`/`countryCode` נכתבים רק על ידי ה-geocoding ונקראים פעם אחת אחריו (סדר תקין); `tripType`/`checkinLink`/`destinationName`/`showAccent` לא תלויים בו בכלל
- נבדק בפועל: החלפת יעד ל"טוקיו" (מדומה, `fetch` הוחלף זמנית כדי לא תלוי ברשת אמיתית) + שינוי שעת סיום ל-10:00 באותה שמירה → `tripEnd` נכתב כ-01:00 UTC (10:00 Asia/Tokyo, נכון) ולא 08:00 UTC (10:00 בברלין הישנה, מה שהיה קורה לפני התיקון). רגרסיה: `tripType:"reserve"` (מדלג על geocoding) - `tripTimezone` נשאר ללא שינוי, `tripEnd` עדיין מחושב נכון
- `node scripts/i18n-audit.js index.html` - 0 ממצאים

## v4.18.0 — `tripDayDates` עובר ל-`legs.js` + תיקון startAt (שלב 4, commit א מתוך 2)

### 🐛 תיקון
- **`tripDayDates()` פירשה גם `startAt` וגם `endAt` לפי `getLastLeg().timezone`.** `startAt` שייך ליום הראשון של הטיול וצריך להתפרש בשעון **הרגל הראשונה** (`getPrimaryLeg`), לא האחרונה. בטיול רגל אחת השתיים זהות - בדיוק למה זה שרד בלי שאף בדיקה תפסה. אומת עם בדיקה שנכשלת לפני התיקון ועוברת אחריו (רומא/בנגקוק, `startAt` קרוב לחצות בשתי הערכות הזמן)

### ✨ תכונה חדשה
- **`window.tripDayDates(trip, tripId)` עברה ל-`legs.js`, ומחזירה `{date, leg}[]`** במקום `Date[]` - כל יום מתויג ברגל שלו, לא רק בתאריך. `itinerary.html` ו-`places.html` **החזיקו עותק זהה מילה-במילה** של הפונקציה (אומת ב-diff מפורש לפני המחיקה) - זו הפעם השלישית שאותו דפוס כפילות חוזר בקוד הזה. הפתרון: לא "לזכור לתקן שניים" אלא שלא יהיה שניים - שתי ההגדרות המקומיות **נמחקו פיזית**, שני הקבצים קוראים עכשיו ל-`window.tripDayDates`
- **יום שנופל בפער בין רגליים נופל לרגל הקודמת ברצף; לפני הרגל הראשונה - הרגל הראשונה.** פער הוא נתון פגום (רגליים תקינות ורציפות לא מייצרות אותו) - `console.warn` + `window.__gtLegGap={tripId,date,at}` בכל פעם שזה קורה, כדי שזה לא יישאר שקט
- **`renderDayTabs()` (itinerary.html) ו-`openDayPicker()` (places.html) עודכנו** לצרוך `.date`/`.leg` במקום `Date` ישיר - `tripId` מועבר כפרמטר מפורש (כמו ב-`getLegs`), לא נקרא ממשתנה גלובלי
- **הכרעה נדחית, לא שכחה**: `index.html`'s `hydrateTripData()` לא בהיקף - הבדיקה גילתה שהיא כבר נכונה (`tripTimezone=lastLeg.timezone` תואם את המפרט). הבאג האמיתי, מאותה משפחה, הוא ב-`fetchForecast()` (רצועת מזג האוויר) שמפרשת `flightOut` לפי `tripTimezone` - זה שלב 8, לא שלב 4. `primaryLegTimezone` גלובלי לא נוסף עכשיו כהכנה - הוחלט לא ליצור משתנה שאף קוד לא קורא לו; ההכרעה תתקבל בשלב 8 עצמו (מתועדת ב-`docs/schema-legs.md` §8)

### באג אמיתי נוסף שנתפס תוך כדי מימוש, ותוקן באותו commit
- **הטמעה ראשונה של תיוג הימים ברגל השתמשה ב-`getLegForDate(trip, date)`** (השוואת instant מוחלט מול `leg.startAt/endAt`) - נכון לפעילות אמיתית עם timestamp, אבל שגוי כאן: `date` נבנה עם `new Date(year,month,day)`, שמתפרש ב-timezone **של הדפדפן**, לא של הרגל. נבדק בפועל מדפדפן ב-`Asia/Jerusalem` מול טיול רומא/בנגקוק - יצר אזהרות "פער" כוזבות בימי הגבול של שתי הרגליים. **תוקן**: השוואת מחרוזות `"YYYY-MM-DD"` שמחושבות דרך ה-timezone של כל רגל בנפרד - אותה גישה שכבר קיימת ב-`fetchForecast()` (index.html) בדיוק מהסיבה הזו (ר' ההערה שם: "sidesteps device-vs-destination instant confusion entirely")
- נבדק בפועל אחרי התיקון, מאותו דפדפן (`Asia/Jerusalem`): טיול רומא/בנגקוק ללא פער - אפס אזהרות כוזבות, כל יום ברגל הנכונה כולל יום המעבר (רגל יוצאת). טיול עם פער מכוון - אזהרה אחת בדיוק, ביום הנכון, נופלת לרגל הקודמת. טיול רגל אחת - זהה לגמרי להיום, אפס אזהרות
- `node scripts/i18n-audit.js itinerary.html places.html` - 0 ממצאים

## v4.17.1 — תיקון: פס המבטא של ski נבלע בטורקיז של חו״ל

### 🐛 תיקון
- **פלטת `character-ski` בלבד שונתה** - `#4FC3F7 → #B3E5FC` (שני גוונים בהירים, קרובים בגוון ל-`--glow-color` של theme-abroad, `#2DD4C8`) הוחלף ב-`#0B3D91 → #4FC3F7 → #FFFFFF`. הצורה, המיקום ו-`character-pride` (שעובד היטב) לא נגעו בהם
- **כחול עמוק כעוגן, לא כמעט-שחור.** נוסה גם `#0F172A` (כמעט שחור-כחול) כקצה כהה - נבלע ב-`--bg` הכהה (`#0B1015`) באותה מידה שהגרסה המקורית נבלעה בטורקיז, רק בכיוון ההפוך. `#0B3D91` כהה/רווי מספיק כדי להיבדל מטורקיז (משפחת גוון שונה לגמרי), אבל לא כהה מספיק כדי להיעלם ברקע
- נבדק בפועל, בטלפון (375px): 4 וריאציות הושוו בתוך theme-abroad כהה (הקונפליקט המקורי) לפני הבחירה. הנבחרת נבדקה גם ב-theme-abroad בהיר, וב-theme-camping כהה (לוודא שלא נוצר קונפליקט חדש עם ירוק). `character-pride` צולם מחדש לאישור שלא השתנה
- `node scripts/i18n-audit.js` - לא רלוונטי, אין מחרוזות בשינוי

## v4.17.0 — שכבת מבטא צבעוני לפי אופי הטיול (שלב 3ב מתוך §8 ב-docs/schema-legs.md)

### ✨ תכונה חדשה
- **פס 3px מתחת לכותרת העמוד, לפי `tripCharacter`.** `tripType` ממשיך לשלוט בכל הצבע הקיים — `--glow-color`/`--primary`/וכו׳ לא משתנים לעולם לפי אופי. רק 2 מתוך 7 אופי נושאים מבטא: `pride` (גרדיאנט קשת), `ski` (גרדיאנט קר). שאר האופי — אין פס, אפס שינוי
- **`--accent-gradient` מוגדר פעם אחת ב-shared.css, בלי שכפול פר-עמוד.** בשונה מ-`--glow-color` (מוגדר מאחורי `[data-tokens="core"]` כי ל-index/shopping/wizard כבר יש ערך מקומי משלהם) - `--accent-gradient` הוא משתנה חדש לגמרי בלי ערך קודם בשום עמוד, אז כלל top-level אחד מספיק לכל 6 העמודים. `docs/schema-legs.md` §3.4 ו-`CLAUDE.md` עודכנו עם ההנמקה, כדי שזה לא "יתוקן" בעתיד לחקות את השכפול של `--glow-color`
- **הפס ממומש כ-`::after` עם `flex-basis:100%`, לא `position:absolute`.** שלושת יעדי הכותרת (`.page-head` המשותף ל-packing/places/itinerary, `.hero-top-row` ב-index, `<header>` ב-shopping) הם כולם `display:flex` - `::after` רגיל היה נהפך לפריט פלקס נוסף באותה שורה, לא לפס מתחתיה. `flex-basis:100%` + `flex-wrap:wrap` (חדש על שלושתם) מכריח אותו לשורה נפרדת, בלי לשנות איך התוכן האמיתי מתנהג
- **מחלקות `character-pride`/`character-ski` תמיד על `documentElement`, בכל 6 העמודים - כולל index.html**, למרות ש-`theme-X` שם דווקא על `document.body`. עקביות מכוונת למחלקות חדשות, לא חיקוי של הפיצול הקיים ב-`theme-X` (`documentElement` בחלק מהעמודים, `body` באחרים) - מתועד ב-CLAUDE.md כדי שהפיצול לא יתפשט הלאה
- **`showAccent` (בוליאני, ברירת מחדל `true`) - מתג חדש בפאנל האדמין**, בתוך "🌍 סוג טיול ויעד". חל אחיד על כל המבטאים, לא רק גאווה - נועד למי שלא רוצה פס קשת גלוי בשימוש ציבורי באפליקציה
- **wizard.html לא נגע** - אין שם טיול טעון ואין כותרת עמוד; לא רלוונטי
- נבדק בפועל: טיול עם `tripCharacter:"pride"` - פס קשת. `"ski"` - פס קר. שאר האופי וטיול בלי `tripCharacter` - אין פס, אפס שינוי. `showAccent:false` - אין פס גם עם אופי תואם. נבדק בכל 4 ערכות `tripType` - הפס לא מתנגש עם `--glow-color`. מצב כהה ובהיר. `flex-wrap:wrap` החדש על שלושת יעדי הכותרת לא גרם לתוכן האמיתי (כפתורי אייקון, כותרת) לגלוש לשורה נוספת בנייד (375px) - רק ה-`::after` עצמו עוטף. צילום נייד: הפס לא מגדיל את הגלילה בעמוד
- `node scripts/i18n-audit.js index.html shopping.html packing.html places.html itinerary.html` - 3 ממצאים קיימים מראש בשלושת כפתורי הייצוא ב-shopping.html (אומת מול ה-baseline לפני השינוי - לא רגרסיה, מחוץ להיקף), 0 חדשים

## v4.16.0 — ייבוא פריט מחבר נוחת בקטגוריה התואמת, לא ב-activeList (שלב 6ב מתוך 6, commit ב מתוך 2)

### ✨ תכונה חדשה
- **`importItem()` כבר לא כותב תמיד ל-`activeList`** - עכשיו היא מחפשת קטגוריה **בשם זהה** (מנורמל, כמו `addItem()`) לקטגוריה שממנה הפריט הגיע אצל החבר, בין הקטגוריות של המשתמש הנוכחי. אם נמצאה התאמה - הייבוא נוחת שם ישירות, בלי חלונית. תיקון אגב: מצב "הכל" היה גורם לכתיבה לקטגוריה שקוראים לה פשוט "all" (אף פעם לא טופל בשלב 5, כי `importItem` לא היה בהיקף) - עכשיו לא רלוונטי, כי היעד תמיד נגזר מהתאמת שם, לא מ-`activeList`
- **השם של קטגוריית המקור אצל החבר נקרא דרך `listLabelFor(uid, id)` חדשה** - חילוץ של `listLabel()` הקיימת (שנשארה זהה לגמרי בהתנהגות, מבצעת `listLabelFor(user.uid, id)`) עם `uid` כפרמטר, כי `listLabel()` המקורית הייתה נעולה ל-`user.uid` שלי ולא יכלה לקרוא metadata של משתמש אחר
- **אם אין התאמה בשם - חלונית חדשה (`importCategoryOverlay`)**: ברירת המחדל **מסומנת מראש** (`selected` + נאכף גם ב-`.value`, לא רק בהינתן HTML) היא "צור קטגוריה בשם X"; שאר האפשרויות הן הקטגוריות הקיימות של המשתמש. בחירה ידנית של קטגוריה קיימת אחרת - תקינה, בלי אזהרה נוספת
- **`createListReturningId(name)` חדשה** - חילוץ של הליבה מ-`createList()` (אותה יצירת id, אותה כתיבה ל-`listMeta`) בלי תופעות הלוואי הספציפיות ל-`newListOverlay` (סגירה, ניקוי שדה, טוסט) - `createList()` עצמה נשארה זהה בהתנהגות, רק עכשיו קוראת לפונקציית הליבה החדשה
- **אומת בפועל (לא רק הונח): אחרי שנוצרה קטגוריה פעם אחת דרך החלונית, ייבוא שני מאותה קטגוריית-מקור אצל החבר מתאים בשם ומדלג על החלונית לגמרי** - הבעיה של "החלונית תקפוץ בכל ייבוא מאותה קטגוריה לא-מותאמת" אכן נפתרת מעצמה אחרי הפריט הראשון, בדיוק כמו שהניחו
- נבדק בפועל (hook זמני, הוסר ואומת): (א) ייבוא לקטגוריה בשם זהה אצלי → נוחת שם ישירות, החלונית לא נפתחת. (ב) ייבוא מקטגוריה שאין לי → חלונית נפתחת, "צור קטגוריה" מסומן מראש, אחרי יצירה מדומה הפריט נוחת בקטגוריה הנכונה (אומת דרך נתיב הכתיבה המדויק שהופיע באזהרת ה-`permission_denied`). (ג) ייבוא שני מאותה קטגוריית-מקור אחרי היצירה → בלי חלונית, נוחת ישירות באותה קטגוריה חדשה. (ד) שם עם רווחים מיותרים ואותיות שונות → הנרמול תפס את ההתאמה. בנוסף: בחירה ידנית של קטגוריה קיימת אחרת מתוך החלונית → נוחתת שם, בלי חלונית נוספת. סגירה בלחיצה על הרקע (backdrop) עובדת כמו בכל שאר החלוניות. `renderMine`/`renderGroup` לא נשברו. נבדק גם בנייד (375px) - החלונית לא מגדילה את הגלילה בעמוד
- `node scripts/i18n-audit.js packing.html` - 0 ממצאים

## v4.15.0 — "main" מוסתרת מרצועת הצ׳יפים כשהיא ריקה (שלב 6א מתוך 6, commit א מתוך 2)

### ✨ תכונה חדשה
- **"main" - רשימת הרפאים (נוצרת תמיד ב-`allListIds()`, אף פעם לא נוצרת בפועל ב-RTDB, אי אפשר לשנות לה שם או למחוק אותה) - מוסתרת מרצועת הצ׳יפים כשאין לה לא פריטים ולא `listMeta` מפורש.** לא נמחקת, לא ממוזגת - טיול שכן צבר בה פריטים ימשיך לראות אותה בדיוק כמו קודם. ההסתרה מוגבלת לרצועת הצ׳יפים בלבד - `editListSelect`, `bulkMoveTarget` ו-`addItemCategorySelect` (בחירת קטגוריה במודאל ההוספה, מצב "הכל") ממשיכים להציג אותה תמיד - אלה הקשרי "בחר יעד", ששם יעד ריק-אבל-אמיתי עדיין לגיטימי להציע
- **בדיקת ה-`listMeta` המפורש נבדקת גם כשהיא לא אמורה להתממש היום** (אין דרך ליצור `listMeta.main` דרך שום מסך קיים - שינוי שם חסום ל-main) - נבדקה בכל זאת, לא הונחה כבלתי אפשרית, למקרה שקוד עתידי כן יכתוב אליה
- **ההגנה על ערך `activeList` לא תקין ב-`renderLists()` נופלת עכשיו ל-"הכל", לא ל-"main"** - שינוי הכרחי, לא רק ניקוי: `allListIds()` **תמיד** מחזירה "main" (רשימת רפאים, לא פער אמיתי), כך שההגנה הישנה (`!ids.includes(activeList)`) מעולם לא הייתה תופסת את המקרה "המסנן הפעיל היה main, ו-main כרגע מתחבאת" - בלי התיקון, משתמש שנשאר על main הריקה היה רואה מסך ריק בלי אף צ׳יפ מסומן ובלי הסבר. הבדיקה הורחבה לכסות גם את המקרה הזה במפורש, לא רק "הקטגוריה לא קיימת בכלל"
- **לא נגעו ב-`deleteList()`'s ה-`activeList = "main"` המפורש שלה** - `renderMine()` קוראת ל-`renderLists()` מיד אחרי כל שינוי שמקצה מחדש את `activeList`, כך שהבדיקה המרכזית הזו מתקנת בכל מקרה, בלי קשר לאיזו משבע נקודות ההקצאה קבעה את הערך הביניים - אותו נימוק שכבר קבע למה נקודת השמירה ל-localStorage יושבת כאן לבד ולא משוכפלת בשבע הנקודות
- נבדק בפועל (hook זמני, הוסר ואומת): main ריקה בלי `listMeta` - מוסתרת. main עם פריט - מוצגת. main עם `listMeta` מפורש בלי פריטים - עדיין מוצגת (המקרה ה"בלתי אפשרי" שנבדק בכל זאת). `activeList="main"` כשיש בה תוכן, ואז התוכן מתרוקן - נופל ל"הכל" עם צ׳יפ מסומן, לא נשאר תקוע. קטגוריה שנמחקה לגמרי (לא רק main) - גם היא נופלת ל"הכל" עכשיו, לא ל"main" - רגרסיה מכוונת, לטובה. אין שגיאות קונסולה בטעינה נקייה
- `node scripts/i18n-audit.js packing.html` - 0 ממצאים

## v4.14.1 — תיקון: רצועת הצ׳יפים קטומה בנייד, מאחורי כפתורי ⚙️/בחירה מרובה

### 🐛 תיקון
- **הרצועה קיבלה שורה משלה, ברוחב מלא**, במקום לחלוק שורה עם 3 כפתורי האייקון (⚙️/☑️/➕). ⚙️ ובחירה מרובה (וגם ➕ "רשימה חדשה", שלא הוזכר במפורש אך שייך לאותה קבוצה) עברו לשורת הכותרת/מונה הקיימת (`.list-head`), שהתפנתה ברגע שהרצועה יצאה ממנה
- **נבדק לפני שינוי הפריסה שיש מקום**: ב-375px, אחרי הפרשי הריפוד, נשארים כ-339px לשורת הכותרת. שלושת הכפתורים (3×42px + רווחים) + המונה (עד כ-100px גם במצב "הכל" עם מספרים גדולים) - כ-240px, מקום נוח, לא צפוף
- **לא נדרש שינוי במנגנון ההצללה/גלילה עצמו** - הוא כבר היה יחסי לרוחב הקונטיינר (`calc(100% - 24px)`, כמו ב-`.quick-picks`), לא קשיח. הבעיה הייתה אך ורק ברוחב שהוקצה לרצועה (כ-137px מתוך 375px כשחלקה שורה עם 3 כפתורים), לא בקוד ההצללה - נסיון קודם לחזק את ההצללה עצמה (v4.14.0, לא שוחרר) לא היה יכול לפתור את זה: אין הצללה שפותרת רוחב שמראה בקושי שניים וחצי צ׳יפים
- נבדק בפועל, נייד (375px), לפני ואחרי - צילום מסך: המונה קריא במלואו ("50% · 1/2"), הכפתורים לא צפופים, הרצועה מציגה 3+ צ׳יפים במקום כ-1.5 קטומים. `has-more`/הצללה אומתו שעדיין עובדים נכון עם יותר קטגוריות ממה שנכנס ברוחב (6 צ׳יפים, `scrollWidth:426 > clientWidth:339`)
- `node scripts/i18n-audit.js packing.html` - 0 ממצאים

## v4.14.0 — רצועת צ׳יפים במקום בורר הרשימות ב-packing.html (שלב 5 מתוך 6)

### ✨ תכונה חדשה
- **`#listSelect` (בורר `<select>`) הוחלף ברצועת צ׳יפים גוללת אופקית** - צ׳יפ ראשון "הכל", אחריו קטגוריה לכל רשימה. לחיצה מסננת; הצ׳יפ הפעיל מסומן במילוי (כמו `.day-tab` ב-itinerary.html - בחירה יחידה, נשארת מסומנת - לא כמו `.pick` בהצעות המהירות, שהן כפתורי פעולה חד-פעמיים). מנגנון הגלילה (הצללה שנעלמת בסוף, כיוון תלוי RTL/LTR, סרגל דק במחשב בלבד, spacer בסוף) מועתק ישירות מ-`.quick-picks` - אותה תבנית שכבר תוקנה ונבדקה
- **"הכל" הוא ערך `activeList` תקין**, נשמר ב-`localStorage` בדיוק כמו כל קטגוריה אמיתית - מחרוזת פשוטה, בלי קידוד מיוחד (עקבי עם איך ש"main" וכל שאר ה-id-ים כבר מטופלים)
- **מד ההתקדמות ומספר הפריטים ב"הכל" מציגים את כל הפריטים מכל הקטגוריות** - לא נדרש קוד חדש: `renderMine()` כבר קראה מ-`itemsForCategoryFilter(activeList)` משלב 2, ופונקציה הזו כבר יודעת לאחד הכל כש-`filter==="all"` משלב 1
- **כפתור הצעות ה-AI מושבת ב"הכל"**, עם הטקסט עצמו (לא רק tooltip) משתנה להסבר ("בחרו קטגוריה לקבלת הצעות AI") - כפתור עם טקסט גלוי, לא אייקון בלבד, אז ההסבר צריך להיות קריא בלי hover (שלא עובד במגע ממילא)
- **כפתור ⚙️ ניהול הרשימה מושבת ב"הכל"**, עם `title`/`aria-label` מוסברים - "הכל" אינה רשימה אמיתית, אין `listMeta`/`items` משלה לנהל
- **הוספת פריט ב"הכל": בורר קטגוריה בתוך אותו מודאל הוספה**, לא השבתה. `addItem()` מקבלת פרמטר `categoryId` מפורש אופציונלי (ברירת המחדל `activeList`, כל שאר הקריאות הקיימות - הצעות מהירות, הצעות AI - לא השתנו). קופץ רק כש-`activeList==="all"`; מדלג עליו ומוסיף ישירות בכל מצב אחר, בדיוק כמו קודם. הבורר עצמו הוא עותק של `editListSelect` הקיים - אותה תבנית `.field`+`<select>`, לא רכיב חדש
- **תיבת ההצעה "זה נשמע כמו X - להוסיף כתת-פריט?" (`updateCategorySuggestion`) לא מוצגת ב"הכל"** - `addToSuggestedCategory()` עצמה כותבת ל-`activeList` בלי שינוי, ולא הפכה מודעת-קטגוריה (בניית בורר שני בשבילה הייתה חורגת מהיקף הצעד הזה) - במקום זאת, הדרך היחידה להוסיף ב"הכל" היא תמיד דרך הבורר, שכן מפיק קטגוריה מפורשת
- **דה-דופ בהוספה עובר לבדוק מול כל הקטגוריות**, לא רק היעד - עקבי עם איך שהצעות AI כבר בודקות כפילות (הכרעה קודמת, מיושמת עכשיו)

### 🐛 תיקון
- **`renderLists()`'s ההגנה על ערך לא תקין (`if (!ids.includes(activeList)) activeList = "main"`) הייתה מאפסת "all" בשקט לכל טעינה** - "all" לעולם לא רשימה אמיתית ולכן אף פעם לא ב-`ids`, כך שההגנה הקיימת הייתה תופסת אותה כלא-תקינה ומחזירה תמיד ל-"main". זה לא רק חלק מהשכתוב לצ׳יפים - זה באג ששבר את השמירה מ-commit קודם באופן שקט לחלוטין (הבחירה "הכל" הייתה נעלמת בכל רענון). תוקן: "all" מטופלת כערך תקין תמיד, לפני בניית הצ׳יפים (לא אחרי, כמו שההגנה הישנה עשתה על ה-`<select>`)
- **`$("listSelect").onchange` נשאר מקושר לאלמנט שכבר לא קיים** - נתפס תוך כדי בדיקה (לא רק בקריאת קוד): שגיאת `TypeError: Cannot set properties of null` עצרה את כל טעינת המודול, כך שאף פעם אין תלות בהנחה - כל שינוי כזה עובר בדיקת טעינה נקייה בפועל

### 🔧 בדיקה
- **נבדקו לראשונה, בתצוגה מאוחדת אמיתית, לא מדומה**: פעולות (סימון, תת-פריט) על פריטים משתי קטגוריות שונות **מוצגים יחד באותו מסך** - כל אחת נחתה בקטגוריה הנכונה (`items/catB/b1`, `items/catC/c1/subItems/s1`, אומת דרך נתיבי אזהרת Firebase). מודאל עריכה שנפתח מהתצוגה המאוחדת לכד את הקטגוריה האמיתית (`catC`), לא "all". **בחירה מרובה חוצת-קטגוריות בוצעה ישירות מתוך התצוגה המאוחדת** (לא בהדמיה של החלפת activeList כמו בשלב 4) - שני פריטים משתי קטגוריות נבחרו על אותו מסך, `selectedIds` תיעד נכון
- נבדק: מעבר "הכל" ⇄ קטגוריה בודדת - תוכן, התקדמות, מצב כפתורים מתעדכנים נכון בשתי הכיוונים. `applyLanguage()` נבדק תוך כדי מצב מושבת - הטקסט/מצב ההשבתה נשמר נכון (לא נדרס בחזרה למצב "פעיל" על ידי החלפת שפה)
- נבדק: הוספת פריט ב"הכל" עם בחירת קטגוריה מהבורר - נחת בקטגוריה שנבחרה, לא ב-`activeList`. דה-דופ חוצה-קטגוריות נבדק - פריט בשם קיים בקטגוריה אחרת נחסם גם כשהיעד הנבחר שונה
- נבדק: התמדה של "all" דרך רענון עמוד אמיתי (לא רק קריאה/כתיבה ל-localStorage) - `activeList` חוזר "all" אחרי `navigate` מלא, בדיוק התרחיש שההגנה הישנה הייתה שוברת
- נבדק חזותית בעברית/RTL ובאנגלית/LTR - כיוון ההצללה בקצה הרצועה נבדק דרך `getComputedStyle` (`to left` ב-RTL, `to right` ב-LTR), לא רק חזותית. `scrollWidth`/`clientWidth`/`.has-more` אומתו ישירות. עיצוב ה-CSS החדש (`.chip`/`.category-chips`) משתמש אך ורק בטוקנים קיימים (`--primary`, `--line`, `--surface-2`, `--glow-color`) שכבר מתחלפים נכון בין מצב כהה/בהיר וארבע ערכות סוג הטיול - לא נבדק חזותית בנפרד בכל שילוב, מוסק מהתבנית הזהה ל-`.day-tab`/`.quick-picks` שכבר הוכחו
- `node scripts/i18n-audit.js packing.html` אחרי כל השינויים - 0 ממצאים

## v4.13.13 — בחירה מרובה חוצת-קטגוריות (שלב 4 מתוך 6)

### ✨ תכונה חדשה
- **`selectedIds` הפך מ-`Set` שטוח של `itemId` ל-`Map<itemId, categoryId>`** - בחירה יכולה עכשיו לחצות קטגוריות, וכל פריט נבחר נושא את הקטגוריה האמיתית שלו, לא רק את `activeList`. נבחרה `Map` על פני `Set` של `"categoryId:itemId"` (התבנית הקיימת ב-`data-sub-check` וכו') כי כל אחת משלוש הפעולות (`bulkDelete`/`bulkToggleDone`/`confirmBulkMove`) צריכה את שני הערכים יחד לכל פריט - `Map.forEach((categoryId, id) => ...)` נותן את זה ישירות, בלי לפרק מחרוזת בכל אתר קריאה
- **`bulkDelete`/`bulkToggleDone` כותבות לקטגוריה של כל פריט בנפרד** - לא ל-`activeList` אחיד לכולם
- **`openBulkMove()` מסננת דינמית**: אם כל הפריטים שנבחרו מגיעים מקטגוריה אחת, היא מוסתרת מרשימת היעדים (בדיוק ההתנהגות הקודמת מבוססת-`activeList`). בבחירה חוצת-קטגוריות שום דבר לא מוסתר - אין "קטגוריה נוכחית" יחידה להסתיר, וכל קטגוריה אמיתית היא יעד תקף לפחות לחלק מהפריטים
- **`confirmBulkMove()` מדלגת על פריט שכבר ביעד** (אפשרי גם כשההסתרה הדינמית לא תפסה את זה - למשל 2 מתוך 3 פריטים כבר ביעד) - בלי כתיבה מיותרת. **הטוסט מציג את המספר האמיתי שהועבר** (`"3 הועברו"`), לא מספר הנבחרים - אחרת דילוג שקט על חלק מהפריטים ייראה כהצלחה מלאה. אם כל הנבחרים כבר ביעד, טוסט נפרד ("הפריטים כבר ברשימה הזו") בלי לגעת ב-RTDB בכלל
- **בכוונה: `activeList` לא קופץ ליעד אחרי העברה מרובה**, בניגוד ל-`saveEdit()`'s ההעברה הבודדת שכן קופצת. תועד בקוד כדי שלא "יתוקן" בעתיד: פריט בודד "לקפוץ אחרי" הגיוני; קבוצה יכולה להגיע ממספר קטגוריות מקור בבת אחת, ומשתמש שרק סיים בחירה מרובה מאבד את ההקשר של מה שנשאר אם התצוגה קופצת
- **כלל 1 (אין fallback) מיושם גם כאן**: `requireCategory` פוצל ל-`assertCategory(categoryId, context)` (בדיקה גנרית) ו-`requireCategory(el, context)` שעוטפת אותה לאלמנטי DOM - שלוש פעולות ה-bulk קוראות ל-`assertCategory` ישירות על כל ערך ב-`selectedIds`

### 🐛 תיקון
- **`bulkToggleDone` קראה `currentItems()[id]` (מוגבל ל-`activeList`) כדי לבדוק אם כל הנבחרים כבר מסומנים** - לא רק רה-פקטור נלווה: בבחירה חוצת-קטגוריות זה היה מחזיר `undefined` בשקט לכל פריט מחוץ ל-`activeList`, וקובע "לא כולם בוצעו" גם כשכולם באמת בוצעו. תוקן לקרוא `items[categoryId][id]` - הבאג אומת בפועל: 3 פריטים ב-2 קטגוריות, כולם `isDone:true`, `activeList` מוצב על קטגוריה שלישית - הקוד המתוקן חישב `allDone:true` נכון

### 🔧 בדיקה
- נבדק בפועל (hook זמני, הוסר ואומת): בחירה חוצת-קטגוריות אמיתית (2 פריטים מקטגוריה אחת + 1 משנייה) - מונה "3 נבחרו" נכון. `openBulkMove` נבדק בשני המקרים - בחירה הומוגנית (קטגוריה משותפת מוסתרת, כמו קודם) ובחירה חוצת-קטגוריות (כלום לא מוסתר). `confirmBulkMove` נבדק עם דילוג חלקי (2 מתוך 3 כבר ביעד - `movedCount:1`, ה-`updates` בפועל הכיל רק את הפריט השלישי) ודילוג מלא (כל הנבחרים כבר ביעד - `movedCount:0`, `updates` ריק, אין קריאת RTDB). `bulkDelete`/`bulkToggleDone` נבדקו עם בחירה חוצת-קטגוריות - הנתיבים בפועל אומתו (לא רק שלא קרסו). כלל 1 נבדק ישירות - `Map` עם ערך קטגוריה חסר זורק לפני כל כתיבה

## v4.13.12 — 7 נקודות כתיבה קוראות `data-category` במקום `activeList` (שלב 3 מתוך 6, השלב המסוכן)

### ✨ תכונה חדשה
- **7 מתוך 14 נקודות הכתיבה על פריט קיים עברו מ-`activeList` ל-`data-category` של האלמנט שהפעיל אותן**: סימון בוצע (`data-check`), נראות (`data-toggle-visibility`), סימון תת-פריט (`data-sub-check`), מחיקת תת-פריט (`data-sub-delete`), הוספת תת-פריט - **שני המסלולים**, קליק על ➕ ו-Enter בשדה (`data-sub-add`/`data-sub-add-input`), ועריכה/העברה/מחיקה (`saveEdit`/`deleteItem`, דרך `data-edit`)
- **כלל מרכזי, בלי חריגים: אין fallback ל-`activeList`.** `requireCategory(el, context)` חדשה - אם `data-category` חסר על האלמנט שהפעיל את הפעולה, `console.error` + `throw` לפני שנוגעים ב-RTDB. כישלון רועש, לא כתיבה שקטה לקטגוריה הלא נכונה
- **`openEdit(id, categoryId)` לוכדת את הקטגוריה בזמן הקליק על `data-edit`**, לא קוראת מ-`activeList` כשהמודאל נפתח - נשמרת ב-`editingCategoryId` (משתנה מודול חדש, מתאפס יחד עם `editingId` בכל נקודת סגירה: שמירה, ביטול, מחיקה). `saveEdit()` מבדילה במפורש: המקור הוא `editingCategoryId`, היעד הוא מה שנבחר ב-`editListSelect`
- **תת-פריטים יורשים את קטגוריית ההורה** - אין להם קטגוריה עצמאית, מתויגים ב-`renderMine()` (שלב 2) בקטגוריה של הפריט המכיל
- **`currentItems()` נשארת ללא שינוי** - עדיין בשימוש בכל מקום שלא הוסב (5 קבוצות כתיבה מחוץ להיקף: `setListVisibility`, שלוש פעולות bulk, `importItem`, `addToSuggestedCategory`, `addItem`)
- **מחוץ להיקף בכוונה, ללא שינוי**: הפעולות שאין להן שורת-פריט קיימת לקרוא ממנה - פעולה על כל הרשימה, בחירה מרובה (שלב 4), ייבוא מקבוצה ופריט חדש (שלב 6)
- **נבדק בפועל, כל 9 נקודות הכתיבה שהשתנו, בתרחיש קבוע**: פריט בקטגוריה `catB` מרונדר בזמן ש-`activeList` על `catB`, ואז `activeList` מוחלף ל-`catA` **בלי רינדור מחדש** - מדמה בדיוק את מצב "פריט ב-B, הקשר על A" שהמודל הממוזג ייצור. כל אחת מ-9 הפעולות הופעלה על האלמנט (עדיין מתויג `catB`), והנתיב שנשלח בפועל אומת דרך אזהרות `permission_denied` של Firebase עצמה (שמראות את הנתיב המלא) - **כל התשע נחתו תחת `items/catB/...`, אף אחת לא תחת `catA`**. `editingCategoryId` אומת ישירות (`"catB"`) גם כשמעבר-בין-רשימות (multi-path `update`) לא הראה נתיבים בודדים באזהרת Firebase המקובצת
- **כלל 1 (אין fallback) אומת ישירות**: אלמנט `data-check` בלי `data-category` (מדמה באג רינדור עתידי) - `console.error` נורה, `Error` נזרק, **ואין אזהרת Firebase חדשה בכלל** - הכתיבה מעולם לא יצאה
- אין שגיאות קונסולה חדשות בטעינה רגילה (נבדק בטאב נקי)

## v4.13.11 — `renderMine` מתייגת `data-category` (שלב 2 מתוך 6, רשימת ציוד מסוננת)

### 🔧 תשתית
- **`renderMine()` צורכת `itemsForCategoryFilter(activeList)` במקום `currentItems()`** - אותה קבוצת פריטים בדיוק (המסנן הוא `activeList`, זהה למה שהיה מוצג), אבל כל item בתוצאה נושא עכשיו `categoryId`
- **כל אלמנט אינטראקטיבי בשורת פריט מתויג ב-`data-category`**: `data-select`, `data-check`, `data-expand`, `data-toggle-visibility`, `data-edit`, `data-sub-check`, `data-sub-delete`, `data-sub-add-input`, `data-sub-add` - 9 סוגי אלמנטים, `data-check` על תת-פריט מקבל את הקטגוריה **של ההורה** (תת-פריטים לא נושאים קטגוריה עצמאית)
- **אפס שינוי התנהגות**: שום קוד לא קורא ל-`data-category` עדיין - שלב 3 יקרא. כל הפעולות ממשיכות לכתוב דרך `activeList`, בדיוק כמו קודם
- **מיפוי מלא של 14 נקודות הכתיבה** מול השינוי: 9 מהן מתויגות (ישירות או דרך `data-select`/`data-edit` שמזינות אותן בשלבים הבאים). 5 לא רלוונטיות לשלב הזה כי אין להן שורת-פריט קיימת לתייג - `setListVisibility` (פעולה על כל הרשימה), `importItem` (ב-`renderGroup()`, פונקציה אחרת לגמרי), `addToSuggestedCategory` ושתי הענפים של הוספת פריט חדש (`addItem`) - כולן פועלות לפני שיש item שרונדר, הקטגוריה שלהן תיפתר אחרת (שלב 6)
- נבדק בפועל (hook זמני, הוסר ואומת): 3 פריטים (אחד עם 2 תת-פריטים, מצב בחירה מרובה + תת-פריטים פתוחים) - 20 אלמנטים אינטראקטיביים בשורות, **20** נושאים `data-category` תקין (לא ריק, לא `undefined`). צילום מסך מאמת תצוגה זהה למה ש-`currentItems()` הייתה מציגה. אין שגיאות קונסולה

## v4.13.10 — `itemsForCategoryFilter()` ב-packing.html (שלב 1 מתוך 6, רשימת ציוד מסוננת במקום בורר רשימות)

### 🔧 תשתית
- **פונקציה חדשה, לא מחוברת לשום דבר עדיין**: `itemsForCategoryFilter(filter)` - תחליף עתידי ל-`currentItems()` (שנשארת ללא שינוי ובשימוש מלא כרגיל). `filter === "all"` מאחדת פריטים מכל הקטגוריות (`items/{categoryId}`) למפה שטוחה אחת, כשכל פריט מתויג ב-`categoryId` שהוא הגיע ממנו; `filter` ספציפי מחזיר קטגוריה בודדת, באותה צורה
- **הסיבה לתיוג**: 14 נקודות הכתיבה הקיימות (סימון, מחיקה, עריכה, תת-פריטים, העברה) מניחות כרגע ש-`activeList` **הוא** הקטגוריה של הפריט - נכון היום כי כל תצוגה מציגה קטגוריה אחת. ברגע שתצוגה אחת תציג כמה קטגוריות יחד, ההנחה נשברת בכל מקום בו-זמנית - התיוג הזה הוא מה שיאפשר לכל נקודת כתיבה לדעת את הקטגוריה האמיתית של הפריט, לא לנחש מה-state הגלובלי
- **אפס שינוי התנהגות**: נבדק בפועל (hook זמני, הוסר ואומת) - מיזוג נכון על פני 3 קטגוריות, תיוג נכון, `filter` שלא קיים מחזיר `{}` בלי קריסה, ו-`currentItems()` הקיימת ממשיכה להתעלם לגמרי מהתוספת. אין שגיאות קונסולה

## v4.13.9 — תיקון: הרשימה הפעילה ב-packing.html מתאפסת ל"רשימה ראשית" בכל טעינת עמוד

### 🐛 תיקון
- **הבאג**: `activeList` היה משתנה בזיכרון בלבד (`let activeList = "main"`) - לא נשמר בשום מקום. כל טעינת עמוד (כולל יציאה וחזרה) איפסה אותו לברירת המחדל הקשיחה `"main"`, בלי קשר לאיזו רשימה המשתמש היה בה קודם
- **התיקון, אותה תבנית בדיוק כמו `lastTripId` הקיים**: `activeListKey = \`trip-${tripId||"legacy"}-activeList\`` - מפתח **לכל טיול בנפרד** (לא גלובלי), נקרא פעם אחת בהצהרת `activeList` (עם `try/catch`, כמו שאר קריאות ה-localStorage בקובץ), ונשמר בנקודה יחידה - בתוך `renderLists()`, מיד אחרי שהיא כבר קבעה את הערך הסופי
- **למה נקודת שמירה יחידה ולא 7 נקודות**: `activeList` מוקצה מחדש ב-7 מקומות שונים בקוד (מעבר ידני, יצירה/שכפול/מחיקה של רשימה, `ensureStarterLists`, ההגנה הקיימת על רשימה שנעלמה, העברת פריט לרשימה אחרת) - **כל אחד מהם מגיע בסופו של דבר ל-`renderMine()`→`renderLists()`**, ישירות או דרך הד ה-listener של RTDB. שמירה שם אחת בלבד מכסה את כל השבעה בלי לגעת בהם, ומצמצמת את השטח שהשתנה
- **ההגנה הקיימת על רשימה שנמחקה/לא קיימת - לא שונתה בכלל**: `if (!ids.includes(activeList)) activeList = "main";` נשארה מילה במילה. אומת בפועל: רשימה משוחזרת שכבר לא קיימת נופלת ל-"main" **וגם מתקנת את עצמה ב-localStorage** (השמירה שמופעלת מיד אחרי ההגנה, באותה הרצה)
- **הקשר בין `ensureStarterLists()` לערך המשוחזר - לא דורש קוד נוסף, רק אומת**: למשתמש קיים (יש `listMeta`/`items`) - ה-early-return הקיים של הפונקציה מונע ממנה לגעת ב-`activeList` בכלל, כך שהערך המשוחזר עובר בלי הפרעה. למשתמש חדש (אין כלום) - ההקצאה הקיימת `activeList = lists[0].id` דורסת בלי תנאי, כולל אם משהו הזדמן להישמר בעבר - שתי הכיוונים כבר מטופלים על ידי ההסתעפות הקיימת בקוד, לא נוספה הגנה חדשה
- נבדק בפועל (hook זמני `window.__test`, נוסף/נבדק/הוסר, אומת שהוסר): **1.** טעינה ראשונה בלי ערך שמור - `"main"`, כמו קודם. **2.** מעבר לרשימה + `renderLists()` - נשמר מיד. **3.** רענון עמוד - משוחזר נכון, **לפני** שהגיע נתון כלשהו מ-RTDB. **4.** רשימה משוחזרת שלא קיימת יותר - נופלת ל-"main" **ומתקנת** את הערך השמור. **5.** משתמש קיים (יש `listMeta`) - `ensureStarterLists()` לא נוגעת בערך המשוחזר (early-return מאומת). **6.** שני `tripId` שונים - מפתחות נפרדים לגמרי, אין דליפה בין טיולים. (המסלול של משתמש חדש-לגמרי לא נבדק חי - דורש `user` אמיתי מחובר ל-`ensureStarterLists()`, לא זמין בסביבת הבדיקה; מאומת בקריאת קוד בלבד: הפונקציה לא שונתה כלל, וההקצאה שלה ל-`activeList` תמיד ללא תנאי כשהיא מגיעה לשם)

## v4.13.8 — תיקון: places.html מציגה יום פנטום שנעלם ל-itinerary.html (commit ב מתוך 2)

### 🐛 תיקון
- **הבאג**: `places.html`'s `tripDayDates()` חישבה את היום הראשון/אחרון של הטיול לפי אזור הזמן של **המכשיר הצופה**, לא של היעד - בעוד `itinerary.html`'s הגרסה (מתוקנת מאז 2026-09-05) עושה זאת נכון דרך `zonedParts()`. הועתקה מ-itinerary.html ב-2026-09-03, יומיים **לפני** התיקון של 09-05 - והתיקון ההוא מעולם לא הועתק חזרה
- **לא רק תצוגה שגויה - תוכן שנעלם בפועל**: `openDayPicker()` כותבת דרך `addPlaceToDay(dayKey)` ל-`itinerary/days/{dayKey}/activities`, עם `dayKey` שמחושב מהיום הפנטום. `itinerary.html` לעולם לא מייצר טאב עם אותו מפתח (הרשימה שלה מתחילה יום מאוחר יותר) - הפעילות נשמרת, אבל לא מוצגת באף מקום
- **התיקון**: places.html מקבל את אותה שורה בדיוק שכבר ב-itinerary.html - `tz`/`startP`/`endP` דרך `window.zonedParts()` (מ-`datetime.js`, ר' v4.13.7) במקום `start.getFullYear()/getMonth()/getDate()` הגולמי. `tripDayDates()` בשני הקבצים זהה עכשיו מילה במילה (מלבד קידומת `window.`)
- **תיעוד מתוקן (v4.13.7) הוחלף בשימוש בפועל**: `itinerary.html` ו-`index.html` הפסיקו להשתמש בעותקים המקומיים של `zonedParts`/`todayKeyInZone`/`nowHHMMInZone`/`dateKeyInZone` וקוראים ל-`window.*` מ-`datetime.js` בכל נקודת קריאה. **העותקים המקומיים נמחקו** (לא רק "לא נקראים יותר") - כדי שאף אחד לא יערוך בטעות את הגרסה הלא נכונה. `dateKey()` נשאר מקומי בשני הקבצים בכוונה - לא נגעו בו ב-09-05 ולא ברשימת ארבע הפונקציות שרוכזו
- נבדק בפועל: **1.** השוואת שוויון - טיול סינתטי לבנגקוק (`startAt:"2026-10-06T20:00:00.000Z"`, `timezone:"Asia/Bangkok"`) הורץ בשני הקבצים (hook זמני `window.__test`, נוסף/נבדק/הוסר, אומת שהוסר). `itinerary.html` ו-`places.html` מחזירות **בדיוק** אותו מערך של 8 מפתחות יום (`2026-10-07` עד `2026-10-14`) - זו ההוכחה שהתיקון נכון, לא רק שהוא לא קורס. **2.** לשם ההשוואה בלבד (לא מהקוד עצמו) - חושב מה הגרסה הישנה הייתה מחזירה על אותו טיול: 9 מפתחות במקום 8, כולל `2026-10-06` פנטום - בדיוק התרחיש שתואר בחקירה. **3.** טיול ריק (`{}`) - `tripDayDates()` מחזירה `[]` בלי שגיאה, כמו קודם. **4.** כל 6 העמודים נטענו מחדש - אין שגיאות קונסולה חדשות; `window.todaysPlanContext()` (index.html, צרכן של `window.dateKeyInZone`) נבדק ישירות ולא זרק
- **לא בהיקף**: הנתונים היתומים שכבר קיימים ב-RTDB אמיתי (אם יש) - הסקריפט לבדיקתם עדיין ממתין לבדיקה שלך, לא הורץ

## v4.13.7 — `datetime.js`: ריכוז `zonedParts`/`todayKeyInZone`/`nowHHMMInZone`/`dateKeyInZone` (commit א מתוך 2)

### ✨ תשתית
- **קובץ חדש ברמת השורש, `datetime.js`, אותה תבנית טעינה בדיוק כמו `legs.js`** - IIFE, script קלאסי בכל 6 העמודים (`<script src="datetime.js">` מיד אחרי `legs.js`), מצמיד ל-`window` במפורש כדי שיהיה נגיש גם מ-index.html (script קלאסי) וגם מחמשת המודולים
- **הסיבה שזה לא נכנס ל-legs.js**: הפונקציות האלה לא קשורות לרגליים - הן עונות "מה השעה/היום ב-timezone X", בלי שום ידע על מבנה טיול. לערבב את השניים היה הופך את legs.js לקובץ אוסף
- **למה עכשיו, לא רק "עוד ניקוי"**: `zonedParts` היה כבר קיים כעותק זהה ביט-ביט בשני קבצים (`itinerary.html`, `index.html` - אומת ב-git log: שניהם עצמאיים, לא אחד הועתק מהשני). היה עומד להיווצר עותק שלישי בתיקון של `tripDayDates()` ב-places.html (commit ב). שלושה עותקים של פונקציה עם קצה אמיתי (`hour===24`→`0`) זה כבר לא תקדים כמו `dateKey`/`--glow-color` (חד-שורתי/CSS) - זה דפוס שיחזור. רוכז לפני שהעותק השלישי נכתב, לא אחריו
- **אומת לפני הכתיבה, לא רק אחריה**: השוואה בין שלוש המימושים (itinerary.html, index.html, והגרסה המתוכננת ל-datetime.js) על 5 תאריכים × 3 timezones (כולל שתי נקודות DST של ארה"ב, אזור עם היסט חצי-שעה - Asia/Kolkata, ומופע מדויק של קצה ה-hour===24 ב-Asia/Bangkok) - 20/20 השוואות זהות. אפס הבדל, כולל הקצה
- **אפס שינוי התנהגות**: שום קוד קיים לא קורא ל-`datetime.js` עדיין - העותקים המקומיים ב-itinerary.html/index.html עדיין קיימים ומוצללים (shadowing) על ידי הצהרות הפונקציה המקומיות שלהם, בדיוק כמו ש-legs.js לא שינה כלום ביום שנוסף. הוחלפה בפועל רק ב-commit ב
- נבדק בפועל: כל 6 העמודים נטענו מחדש, `window.zonedParts`/`window.dateKeyInZone`/`window.todayKeyInZone`/`window.nowHHMMInZone` הם `function` בכולם (גם ב-index.html הקלאסי, גם במודול כמו packing.html), קצה ה-`hour===24` נבדק חי דרך `window.zonedParts` (לא רק בבדיקת ה-Node) - `Asia/Bangkok` בחצות מקומי בדיוק מחזיר `hour:0`. אין שגיאות קונסולה חדשות באף עמוד

## v4.13.6 — תיקון: aria-label "הסר X" לא מתעדכן, ולא מתעדכן חי (commit ג2 מתוך 3, `scripts/i18n-audit.js`)

### 🐛 תיקון
- **מ-12 מועמדים דינמיים שהסורק דיווח, 11 היו חיוביים שגויים** (כבר עברו דרך `tr()`/ternary עובד - ר' תיקון הסורק למטה) ורק אחד היה באג אמיתי: `wizard.html`'s `renderParticipants()` - ה-`aria-label` "הסר {שם}" על כפתור ה-× ליד כל תג משתתף היה מחרוזת עברית קשיחה, בלי שום תלות בשפה
- **שני שינויים, לא אחד**: **א.** ה-`aria-label` עכשיו לפי `currentLanguage === 'he' ? 'הסר' : 'Remove'` - בתבנית ה-ternary של `wizard.html` עצמו (לעמוד הזה אין `tr()` בכלל). **ב.** נוספה קריאה ל-`renderParticipants()` בתוך `changeGlobalLanguage()` - **שינוי התנהגות, לא רק תרגום, מאושר במפורש**: בלעדיה, תגי משתתפים שכבר נוספו היו נשארים עם ה-aria-label הישן עד ההוספה/הסרה הבאה, לא עד החלפת השפה עצמה - כי `renderParticipants()` לא היה נקרא כלל מ-`changeGlobalLanguage()` (אומת בקריאת הפונקציה כולה, שורות 862–946 לפני התיקון)
- **אומת שהקריאה הנוספת לא מאפסת מצב**: `participants` הוא מערך שמות בלבד (`let participants = []`), ו-`renderParticipants()` בונה מחדש `innerHTML` אך ורק ממנו - אין קלט/עריכה בתוך כל תג (רק שם + כפתור הסרה), ואין מצב אחר שיכול ללכת לאיבוד בהחלפת שפה
- נבדק בפועל: **1.** הוספת 3 משתתפים, החלפה לאנגלית - השמות (Dana/Roi/Maya) לא נעלמו, כל ה-`aria-label` עדכנו ל-"Remove X" (נבדק דרך `getAttribute` בקונסול, לא בעין) - וחזרה לעברית מחזירה "הסר X" עם אותם שמות. **2.** דף טרי בלי משתתפים, שתי החלפות שפה - לא קרס, `innerHTML` ריק כצפוי, אין שגיאות קונסולה. **3.** החלפה לאנגלית ואז הוספת משתתף - "Yael" נוספה עם `aria-label="Remove Yael"` מיד, לא בעברית
- `node scripts/i18n-audit.js wizard.html` אחרי התיקון - 0 ממצאים

### 🔧 כלי פיתוח (`scripts/i18n-audit.js`, בלי version bump - קומיט נפרד)
- **תוקן חיובי שגוי שלישי בתור** (אחרי `previewBtn` ב-commit א ו-`langHeBtn`): scan 2 סימן עברית בתוך template string ב-`render*()` בלי לבדוק אם היא כבר ענף של תרגום עובד. נוספה `isTranslationBranch()` - מדלגת על עברית שהיא ארגומנט ל-`tr(` **או** ענף בתנאי `? :` על `currentLanguage`/`lang` (התבנית של `wizard.html`, שאין בו `tr()` בכלל). אומת עם קובץ סינתטי: שני הדפוסים מדולגים, עברית קשיחה אמיתית עדיין נתפסת
- לפני התיקון: 12 מועמדים (8 ב-`shopping.html`, 2 ב-`index.html`, 1 ב-`places.html`, 1 ב-`wizard.html`). אחרי: 1 בלבד (`renderParticipants`) - שתוקן למעלה. `packing.html` נשאר 0

## v4.13.5 — תיקון: 4 aria-label/title שלא תורגמו (commit ב מתוך 3, `scripts/i18n-audit.js`)

### 🐛 תיקון
- **`index.html` (3)**: `#appTitle` (`title`, "לחץ 3 פעמים לכניסת מנהל"), `#adminGearBtn` (`aria-label`, "הגדרות מנהל" - תורגם "Organizer settings", עקבי עם `adminModalTitle` הקיים ולא "Admin settings"), `#forecastStrip` (`aria-label` על הרצועה עצמה - התוכן הדינמי בפנים כבר היה מתורגם, רק התווית של הקונטיינר לא). כל השלוש נוספו כשורות ישירות ב-`applyTripLanguage()`, אותו דפוס כמו `weatherRow`/`.bottom-nav` הקיימים
- **`itinerary.html` (1)**: `#fab` (`aria-label`, "הוספת פעילות") - שורה אחת ב-`applyLanguage()`, משתמשת באותו זוג `tr()` שכבר קיים לכותרת המודאל
- **אלה תיקונים שלא נראים למשתמש** (אף אחד מהארבעה לא טקסט גלוי) - **הבדיקה בוצעה אך ורק דרך `getAttribute` בקונסול, לא בעין**: `index.html` דרך `currentLanguage`/`applyTripLanguage()` הגלובליים, `itinerary.html` דרך hook זמני (`window.__test`) שנוסף, שימש לבדיקה, הוסר, ואומת שהוסר (`grep` ל-`__test` - "No files found"). שתי הפעולות בשתי השפות: אנגלית ואז חזרה לעברית, שתיהן נכונות
- נבדק גם: `node scripts/i18n-audit.js index.html itinerary.html` אחרי התיקון - 0 ממצאים סטטיים (נשארו רק 2 המועמדים הדינמיים של `renderActionBadges()`, שייכים ל-commit ג)

## v4.13.4 — תיקון: 12 טקסטים גלויים שלא תורגמו, ב-3 עמודים (commit א מתוך 3, `scripts/i18n-audit.js`)

### 🐛 תיקון
- **`index.html` (7)**: כל שער "הוספת קישור להתחשבנות" (Spliit) - כותרת, טקסט, קישור, כפתורי שמירה/ביטול - ומצב ריק/כפתור יצירה ב"בחירת טיול" (`tripPicker`, כשלמשתמש כמה טיולים). נוספו כ-7 מפתחות חדשים ל-`dashboardText` (he/en) - הלולאה הגנרית ב-`applyTripLanguage()` שכבר קיימת מיישמת אותם, לא נדרש קוד נוסף
- **`wizard.html` (2)**: `#tripTypeHint` ו-`#checkinHint` - שני רמזי שדה בשלב 1. נוספו `typeHint`/`checkinHint` ל-`translations` (he/en) + שורה לכל אחד ב-`changeGlobalLanguage()`
- **`places.html` (3)**: כל מודאל "לאיזה יום להוסיף" (`dayPicker`) - כותרת, תת-כותרת, וכפתור ביטול. שלוש שורות ב-`applyLanguage()`, אותה תבנית כמו שאר הפונקציה
- **בכוונה לא תוקן, מתועד כדחייה**: 3 כפתורי שפת ייצוא ב-`shopping.html` (`exportHebrewBtn`/`exportEnglishBtn`/`exportGreekBtn`, למשל `"🇮🇱 עברית (Hebrew)"`) - בניגוד לביקורת הידנית הקודמת שסימנה אותם כפער אמיתי, בבדיקה חוזרת הם נראים מכוונים: כל אחד כבר כולל גלוסה דו-לשונית קבועה בסוגריים, בדיוק המוסכמה המקובלת לבורר שפות (כמו אתר שהמתג שלו ממשיך להציג "Français" גם באנגלית) - לא באג, ולכן לא תוקן. יחד עם `langHeBtn` (שכבר ב-`scripts/i18n-ignore.txt`), אלה 4 הדחיות המוצדקות שהתגלו בסבב הזה
- **נבדק בפועל, לפי commit**: **1.** אנגלית בכל 12 המחרוזות - הוחלף בפועל בדפדפן (`index.html`/`wizard.html` דרך הפונקציה הגלובלית שלהם, `places.html` דרך hook זמני שהוסר ואומת שהוסר). **2.** חזרה לעברית - כל 12 חוזרות. **3.** כל 6 העמודים נטענו מחדש ונבדקו לאיתור שגיאות קונסולה - אין חדשות (רק שגיאות הרשת הצפויות בסביבת הבדיקה, ללא אינטרנט אמיתי)
- **תיקון קטן ל-`scripts/i18n-audit.js` עצמו, לפני שהריצה הזו**: הסורק דיווח בטעות על 5 כפתורי "צפה בדוגמה" ב-`wizard.html` כפער - הם כן מתורגמים, רק לא לפי `id` בודד אלא דרך `document.querySelectorAll('.preview-btn').forEach(...)`. נוסף זיהוי כיסוי מבוסס-מחלקה (Pattern D) שתפס את זה; אומת מול `packing.html` (שוב, 0 ממצאים)

## v4.13.3 — תיקון: מודאל "הוספת פריט חדש" נשאר בעברית באנגלית (packing.html)

### 🐛 תיקון
- **הבעיה**: 6 אלמנטים ב-`applyLanguage()` לא היו מכוסים בכלל - לא רק בכותרת המודאל, גם במקומות אחרים בעמוד: 3 טקסטים גלויים (`categorySuggestBtn`, `addItemTitle`, `closeAddItemOverlay`) ו-3 `aria-label` (`addItemFab`, `homeTop`, `themeBtn`)
- **`#addItemFab` (הכפתור שפותח את המודאל) ו-`#addItemTitle` (הכותרת בתוך המודאל) חולקים את אותו טקסט** - "הוספת פריט"/"Add item" - נפתר עם קריאת `tr()` אחת יחידה, ששני האלמנטים משתמשים בתוצאה שלה, לא שני עותקים נפרדים של אותו זוג מחרוזות
- **נבדק במפורש אם `applyLanguage()` יכולה לרוץ לפני שהאלמנטים האלה קיימים בכלל** (חשש שעלה מחקירת ה-warn של `trip` בשלב מוקדם): לא רלוונטי כאן - הסקריפט הראשי הוא `type="module"`, שדוחה ריצה עד אחרי שכל ה-HTML נפרס, וארבעת האלמנטים (`homeTop`, `themeBtn`, `addItemFab`, `addItemTitle`) כולם מופיעים ב-markup **לפני** תג ה-`<script type="module">` - אומת לפי מספרי שורה, לא הונח. לכן לא נוספה הגנת `if (el)` - עקבי עם שאר הפונקציה (~50 שורות, אפס בדיקות הגנתיות קיימות)
- נבדק בפועל: **1.** אנגלית - כל 6 מתורגמים. **2.** חזרה לעברית - כל 6 חוזרים. **3.** שני ה-`aria-label` נבדקו ב-devtools (`getAttribute`), לא בעין. **4.** החלפת שפה בזמן שהמודאל כבר פתוח - הטקסט מתעדכן חי, בלי לסגור ולפתוח מחדש

### 📝 לתיעוד (ר' CLAUDE.md)
- **סקר את אותו דפוס בדיוק על 5 העמודים האחרים** (itinerary, places, wizard, shopping, index) - נמצא בכולם, בהיקפים שונים: itinerary (aria-label אחד), places (מודאל שלם - 3 טקסטים), wizard (2 רמזי שדה, ועוד 3 ממצאים דומים בלי `id`), shopping (3 כפתורי שפה ביצוא), index (8 טקסטים + 3 aria-label/title, בעיקר בשער הקישור ל-Spliit). לא תוקן - החלטה על סדר עדיפויות בהמשך

## v4.13.2 — תיקון: אין דרך לגלול את רצועת ההצעות במחשב (packing.html)

### 🐛 תיקון
- **מידע חדש מבדיקה במכשירים אמיתיים**: התיקון הקודם (v4.13.1) פתר את בעיית הנייד (גוללת אבל בלי סימן) - אבל במחשב הרצועה לא הייתה גוללת **בכלל**, לא רק חסרת סימון. `scrollLeft` שמדדתי בבדיקה הקודמת היה גלילה תכנותית, לא כזו שמשתמש יכול לבצע בפועל
- **אומת לפני התיקון**: גלגלת עכבר רגילה מעל הרצועה לא מזיזה `scrollLeft` בכלל (נבדק ישירות). גרירה עם העכבר לא נתמכת באופן טבעי בדפדפן. לקונטיינר אין `tabindex`, אז מקלדת לא מגיעה אליו. Shift+גלגלת הוא באמת מנגנון דפדפן קיים שממיר גלילה אנכית לאופקית - אבל לא ניתן היה לבדוק אותו אוטומטית בסביבת הבדיקה (כלי האוטומציה לא תומך במקש modifier על גלילה), וגם אם הוא עובד - זו אינטראקציה נדירה שכמעט אף משתמש לא יגלה בלי שיגידו לו
- **`.forecast-strip` (index.html) נבדקה - סובלת מאותה בעיה בדיוק**, לא רק מפער הריפוד שכבר תועד. אין שם שום מנגנון חלופי. תועד ב-CLAUDE.md, לא תוקן - מחוץ להיקף
- **הפתרון שנבחר: סרגל גלילה דק, במחשב בלבד, בלי JS** - `@media (hover:hover) and (pointer:fine)` ולא breakpoint לפי רוחב מסך, כי הסיגנל הנכון הוא סוג המצביע (יש עכבר מדויק?) לא גודל החלון - טאבלט מגע ברוחב גדול לא אמור לקבל את זה, חלון עכברי צר כן. `scrollbar-width:thin` (Firefox) + `::-webkit-scrollbar` מעוצב (Chrome/Safari/Edge), בצבע `var(--line)` - טוקן קיים שכבר משתנה לפי ערכת סוג הטיול ומצב כהה/בהיר, כך שאין צורך בהתאמות נפרדות
- **נשקלה ונדחתה בכוונה: המרת גלילה אנכית לאופקית ב-JS.** חוטפת התנהגות דפדפן מוכרת - משתמש שמגלגל כדי לגלול את העמוד והעכבר נמצא מעל הרצועה, העמוד נעצר במקום. מרגיש שבור גם כשזה מכוון. הבעיה היחידה שדרשה פתרון הייתה "אי אפשר להגיע" - הסרגל פותר אותה במלואה בלי הסיכון הזה
- **קפיצת פריסה שנבדקה במפורש**: השטח ששמור בפועל לסרגל התברר כ-10px (לא 5px כמו שהוגדר מפורש ב-`::-webkit-scrollbar{height:5px}` - הדפדפן הזה מעדיף כנראה את הגודל המובנה של `scrollbar-width:thin` על פני הגובה המפורש). כדי לצמצם את ההבדל, `padding-block-end` יורד ל-0 במחשב (היה 2px). התוצאה: `.quick-picks` גבוה בכ-8px יותר במחשב מאשר בנייד - אין חפיפה או קפיצה, כפתור ה-AI ממשיך לשבת ב-`margin-top:12px` הרגיל שלו מתחת לרצועה, בלי קשר לגובה שלה
- נבדק בפועל: **1.** הגרירה בפועל עובדת - `scrollLeft` השתנה מ-0 ל-(-291) בעקבות גרירת הסרגל עם העכבר, לא רק גלילה תכנותית. **2.** נבדק שהסרגל **נעלם לגמרי** במצב נייד/מגע (`matchMedia` על `hover:hover`/`pointer:fine` מחזיר `false`, `scrollbar-width` חוזר ל-`none`, אפס שטח שמור) - אפס השפעה על חוויית הנייד. **3.** מראה סביר נבדק חזותית במצב כהה (ברירת המחדל) ובשתי ערכות נוספות (`local`, `camping`, מצב בהיר) - הסרגל דק, בצבע התואם לערכה, לא בולט מדי. **4.** קפיצת פריסה - נמדד לפני ואחרי, כפתור ה-AI לא זז ממיקומו היחסי

## v4.13.1 — תיקון: רצועת "מה עוד חסר לך?" נגללת אבל לא מציגה את זה (packing.html)

### 🐛 תיקון
- **הבעיה**: הרצועה כבר הייתה `display:flex; overflow:auto` - גוללת בפועל, לא נחתכת - אבל `padding:10px 1px 2px` השאיר כ-1px ריפוד אופקי בלבד, כך שבסוף הגלילה הפריט האחרון נגע ממש בגבול הקונטיינר. יחד עם סרגל גלילה מוסתר בכוונה (`scrollbar-width:none`), למשתמש לא היה שום רמז שיש עוד לגלול - זה נראה כמו "נחתך", לא כמו "אפשר לגלול עוד קצת". החמיר עם `tripCharacter` כי הרשימות התארכו והחיתוך נהיה שכיח יותר
- **אומת לפני התיקון, לא רק נקרא מה-CSS**: מדידת `scrollWidth`/`clientWidth`/`scrollLeft` בפועל בדפדפן הראתה שהרצועה כן נגללת, כולל כיוון RTL נכון (הפריט הראשון מוצג בימין ב-`scrollLeft:0`, בדיוק כמו שצריך). מנוע הדפדפן הזה משתמש ב-`scrollLeft` **שלילי** לכיוון הסוף ב-RTL - לא כל המנועים זהים בזה, זו בדיוק הסיבה ל-`Math.abs()` בלוגיקת ההצללה למטה
- **שלושה תיקונים**: **א.** ריפוד אמיתי בסוף הרצועה - גם `padding-inline` לוגי (מתהפך אוטומטית RTL/LTR) וגם spacer אמיתי כ-flex child (לא ריפוד CSS בלבד - ריפוד בקצה קונטיינר גלילה לא תמיד מכובד עד הסוף בכל מנוע). **ב.** `flex-shrink:0` מפורש על `.pick`. **ג.** הצללה עדינה בקצה ה"רחוק" (`mask-image`, עם `-webkit-mask-image` ל-Safari) שמוצגת רק כשיש עוד לגלול (`.has-more`, מחושב ב-JS) ונעלמת בפועל בהגעה לסוף האמיתי - לא הצללה קבועה. כיוון ההצללה **הפוך** בין השפות בכוונה, לא זהה-עם-flip: הקצה ה"רחוק" הוא שמאל בעברית וימין באנגלית, אז `[dir="rtl"]`/`[dir="ltr"]` מגדירים כל אחד את כיוון ה-gradient שלו בנפרד
- **`-webkit-overflow-scrolling:touch` נבדק ולא נוסף בכוונה** - תכונה מיושנת שממילא לא משפיעה על שום דבר החל מ-iOS 13 (momentum scrolling אוטומטי כבר שנים). מתועד בקוד כדי שלא "יתוקן" בחזרה
- **באג אמיתי שנתפס תוך כדי בדיקה, לא רק תוכנן**: מעבר בין קטגוריות (`gear`→`clothing` וכו') לא איפס את מיקום הגלילה - רשימה קצרה שנפתחה אחרי רשימה ארוכה יכלה לרשת מיקום גלילה שגוי, וכתוצאה מכך גם מצב הצללה שגוי. `renderQuickPicks()` מאפס עכשיו `scrollLeft` בכל רינדור מחדש
- **בכוונה לא נגעו**: `.forecast-strip` (index.html) - סובלת מאותו פער ריפוד-בסוף בדיוק, מתועד ב-CLAUDE.md, לא בהיקף המשימה הזו
- נבדק בפועל: **1.** גלילה במגע - **לא נבדק על מכשיר אמיתי בפועל** (לא זמין בסביבת הבדיקה) - נבדק בקירוב הכי טוב הזמין: viewport נייד מדומה (375px) + מדידת scroll מציאותית, לא רק קריאת CSS; מומלץ לוודא במכשיר אמיתי בנוסף. **2.** ההצללה מופיעה עם רשימה גולשת (`abroad`+`ski`, 9 פריטים) ונעלמת בהגעה לסוף האמיתי - נבדק ונמדד בשתי השפות, כולל אימות כיוון ה-gradient המחושב בפועל (`to left` ב-RTL, `to right` ב-LTR). **3.** רשימה קצרה שנכנסת במלואה (`reserve`/`uniform`, 3 פריטים, `scrollWidth===clientWidth` בדיוק) - אין `has-more` בכלל. **4.** צילומי מסך בהקשר עמוד מלא, בעברית ובאנגלית, בשני מצבים (עם הצללה / בסוף גלילה בלי הצללה) - הפריט האחרון מלא ונגיש עם רווח אמיתי (32-33px) בשתי הבדיקות

## v4.13.0 — tripCharacter משפיע על קטגוריות הציוד (שלב 3ב מתוך docs/schema-legs.md, packing.html בלבד)

### ✨ תכונה חדשה
- **`starterLists()` מקבל ממד שלישי**: אחרי `tripType` (קטגוריות) ו-`vibes` (פריטים ל-`clothing`), עכשיו גם `tripCharacter` מוסיף פריטים לקטגוריות **קיימות** - לא יוצר קטגוריות חדשות. שבעה אופי, כל הפריטים ל-`gear` חוץ מ"תלבושות למסיבות" (`pride`/`bachelor`) שנכנס ל-`clothing`
- **`friends`/`couple` ריקים בכוונה, מתועד בקוד** - טיול חברים הוא ברירת המחדל של האפליקציה, וטיול זוגי לא שונה בציוד. הערה בקוד מפורשת נגד "תיקון" עתידי
- **חל על כל 4 סוגי הטיול**, לא רק `abroad`/`local` כמו `vibes` - נבדק במפורש שההגבלה של `vibes` לא הועתקה
- **מיזוג, לא דריסה** - בניגוד ל-`vibes` (ר' "לתיעוד" למטה), `tripCharacter` קורא את הפריטים הקיימים בקטגוריה ומוסיף אליהם, לא מחליף. רץ **אחרי** בלוק ה-`vibes` הקיים בכוונה, כדי ששני המנגנונים באמת יפעלו במקביל: תרומת `tripCharacter` שורדת גם כשל-`vibes` יש מה להגיד על אותה קטגוריה
- **Dedup בין-קטגורי, לא רק מול קטגוריית היעד**: פריט שכבר קיים בכל קטגוריה אחרת של אותו `tripType` (למשל "שק שינה" שכבר ב-`shelter` של קמפינג) לא נוסף שוב ל-`gear` רק כי שם הטבלה שמה אותו
- **טבלת שמות נרדפים קטנה, ל-dedup בלבד** (`DEDUP_SYNONYMS`, 5 ערכים) - "פאוור בנק"/"מטען"/"מטען נייד"/"power bank"/"charger" מזוהים כאותו פריט אמיתי, בלי לשנות את השם המוצג. **חריג מכוון**: "מטען נייד נוסף"/"Extra power bank" (`family`) לא בטבלה - עם ילדים באמת רוצים שניים, אז הוא תמיד נוסף גם כשכבר יש מטען. זוגות נוספים שנשקלו ונדחו (מתועד בקוד, לא בטבלה): "מטען לטלפון"/"Phone charger" (כבר מופיע יחד עם "פאוור בנק" כשני פריטים נפרדים ברשימת `abroad` הקיימת - הוכחה שהם לא נחשבים לאותו דבר), "תרופות אישיות" ו"כדורי כאב ראש" מול "תרופות" הגנרי (נקראים כתוספות מכוונות, לא כשכפול, אותה צורה בדיוק כמו חריג המטען)
- **`reserve` בלי `clothing`** - אומת שזה אכן קורה בפועל (היחיד מבין 4 סוגי הטיול). טיול `reserve` עם `pride`/`bachelor` מדלג בשקט על "תלבושות למסיבות"; כל שאר הפריטים ל-`gear` נוספים כרגיל
- **התנגשות אמיתית שנתגלתה בבדיקות, נפתרה לפי הכרעה מפורשת**: `ski` (יש לו "קרם הגנה" ב-`gear`) + vibe `beach` (מכניס "קרם הגנה" ל-`clothing`) - ה-dedup הבין-קטגורי מזהה שהפריט כבר קיים ומוותר על ההוספה החוזרת ל-`gear`. **זו התנהגות נכונה, לא כשל**: המשתמש רואה רשימת ציוד אחת, לא ארבע נפרדות - פריט שמופיע פעם אחת בכל קטגוריה השלים את המשימה; אותו פריט פעמיים היא בדיוק הכפילות שתוקנה בהצעות ה-AI (v4.11.7-v4.11.9)
- **בכוונה לא נגע**: `payload` ל-Cloud Function של הצעות ה-AI - `tripCharacter` יגיע לשם בשלב נפרד

### 📝 לתיעוד (ר' CLAUDE.md)
- סיווג שגוי בטבלת `vibeClothing`: "קרם הגנה"/`Sunscreen` (beach) ו-"תרמיל גב"/`Backpack` (nature) הם ציוד, לא ביגוד - זה השורש להתנגשות שתוארה למעלה. לא תוקן

### נבדק בפועל, לא רק נקרא
כל הבדיקות הורצו גם כפונקציות טהורות (Node, כולל snapshot מלא של כל 8 צירופי סוג-טיול/שפה לפני השינוי) וגם מול `starterLists()` האמיתית בדף (`window.__test` זמני - הוסר ואומת שהוסר): **1.** `abroad`+`ski` - 5 פריטים ב-`gear`, בלי כפילות. **2.** `abroad`+`pride` - 5 ב-`gear`, "תלבושות למסיבות" ב-`clothing` בלבד. **3.** `friends` - זהה בייט-לבייט לברירת המחדל. **4.** בלי `tripCharacter` בכלל - זהה לברירת המחדל בכל 8 הצירופים (סוג טיול × שפה) - **הבדיקה הקריטית**, מכסה כל טיול קיים. **5.** אנגלית - כל הפריטים מתורגמים, כולל dedup נכון ("Power bank" מופיע פעם אחת). **6.** `ski`+vibe `beach` יחד - 4 פריטי ski שלא מתנגשים ב-`gear`, "קרם הגנה" ב-`clothing` בלבד דרך ה-vibe (ר' ההכרעה למעלה). **7.** `camping`+`festival` - עובד גם שם; "שק שינה" ו"פנס" לא מוכפלים ל-`gear` (כבר ב-`shelter`)

### ✨ תכונה חדשה
- **שדה חדש `tripCharacter`** בוויזרד - בחירה יחידה מתוך 7 (`family`/`friends`/`couple`/`ski`/`pride`/`bachelor`/`festival`), `<select>` מתחת לשדה סוג הטיול, עם אופציה ריקה ראשונה "לא צוין". `<select>` נבחר בכוונה, לא צ׳יפים - הוויזרד עומד לעבור עבודה גדולה בשלב 10 (קלט רגליים), ואין טעם לבנות רכיב חדש שישונה בקרוב; `<select>` מטפל ב-7 אופציות כמו שהוא
- **המפתח באנגלית נשמר, התצוגה תמיד דרך תרגום** - בדיוק כמו `tripType`. אין מחרוזת עברית בערך שנכתב
- **השדה אופציונלי, ובאמת אופציונלי**: כשלא נבחר - `tripCharacter` לא נכתב בכלל לאובייקט שנשלח ל-Firebase, לא כמחרוזת ריקה. מימוש: `...(tripCharacter ? { tripCharacter } : {})` בתוך אובייקט היצירה
- **`resetWizard()` מאפס גם אותו** - נבדק במפורש, כי בדיוק בפונקציה הזו כבר היה באג בעבר (v4.10.x לערך): לחיצה על "טיול חדש" מהצלחה בלי רענון השאירה מצב ישן (`selectedVibes` לא מתנקה וכו')
- **`saveWizardDraft()`/`restoreWizardDraft()` (שמירת מצב הטופס לפני/אחרי redirect של Google Sign-In) כוללות גם אותן, באותה תבנית בדיוק כמו `tripType`** - זוהה תוך כדי העבודה, לא כבאג נפרד אלא כחלק מהחיים המלאים של השדה: שדה שנשמר ומתאפס אבל לא שורד redirect הוא מיושם חלקית. משתמש שבוחר אופי ואז מתחבר עם Google היה מאבד אותו בשקט אלמלא זה
- **ביקורת מלאה**: כל שדה אחר בטופס שנבדק מול `saveWizardDraft()` כבר נשמר נכון (`tripDestination`, `tripType`, `vibes`, `tripStart`/`tripEnd`, `checkinUrl`, חמשת ה-`feat*`). נמצא פער אחד נוסף, **שלא תוקן** (מחוץ להיקף): `participantName` - תיבת הטקסט של שם חבר קבוצה *לפני* לחיצה על "הוסף" - לא נשמרת; רק שמות שכבר אושרו ל-`participants` שורדים redirect. זה פער מבודד (הדפוס היחיד בטופס של "הקלדה ואז אישור בכפתור נפרד"), לא דפוס רחב - כל שאר השדות הם ערכים ישירים שכבר נשמרים
- **בכוונה מחוץ להיקף**: `starterLists()`, `packing.html`, וה-payload ל-Cloud Function של הצעות ה-AI - לא נגעו. זה שלב נפרד (3ב), אחרי שהשפעת `tripCharacter` על קטגוריות ציוד תיקבע בנפרד
- נבדק בפועל: **1.** מבנה השדה - 8 אופציות (ריקה + 7), ברירת מחדל ריקה. **2.** מעבר שפה - התווית ושמונת האופציות מתורגמות נכון בשני הכיוונים. **3.** `resetWizard()` - נבדק ישירות: `tripCharacter` נקבע ל-`'ski'`, `resetWizard()` נקרא, הערך חוזר ל-`''`. **4.** בניית האובייקט - הביטוי המדויק מ-`generateTripAndSave()` (`...(tripCharacter ? {tripCharacter} : {})`) הורץ נגד קריאת ה-DOM האמיתית: עם בחירה - המפתח קיים עם הערך האנגלי (`'ski'`); בלי בחירה - המפתח נעדר לגמרי מהאובייקט. **5.** טיוטה: `tripCharacter` נקבע ל-`'pride'`, `saveWizardDraft()` נקרא (נבדק גם שהטיוטה השמורה בפועל מכילה `tripCharacter:'pride'`), השדה נוקה ידנית ל-`''`, `restoreWizardDraft()` נקרא - הערך חוזר ל-`'pride'`. **לא נבדק קצה-לקצה מול Firebase בפועל** - `generateTripAndSave()` דורש `requireOrganizer()` (Google Sign-In אמיתי), לא זמין בסביבת הבדיקה, כמו בכל משימה קודמת הפעם

### 📝 לתיעוד (ר' CLAUDE.md)
`--glow-color` משוכפל ב-4 מקומות עצמאיים (shared.css + שלושה בלוקי `<style>` inline נפרדים) ולא מוגדר במקום אחד - שכבת המבטא העתידית (`--accent-gradient`, §3.4) תצטרך את אותו שכפול

## v4.11.9 — פרומפט הצעות AI: אין תוארי קישוט מיותרים (functions/index.js)

### 🐛 תיקון
- **סבב שלישי בבדיקת פרודקשן**: אחרי v4.11.7+v4.11.8, 0 כפילויות זוהו על ידי הסינון - אבל נשארו זוגות כמו "מטרייה מתקפלת קלה"/"מטרייה מתקפלת קומפקטית" ו"מעיל מעבר עמיד למים"/"מעיל מעבר עמיד לגשם". הפיצול במילה האחרונה - שני הפריטים חולקים בסיס ונבדלים בתואר, ואף אחד אינו תחילית של השני, כך שאין כלל טקסטואלי שיכול לתפוס את זה. השורש: המודל ניצל את התקרה של "2-4 מילים" במקום לשאוף לרצפה - "קלה"/"קומפקטית" הם קישוט, לא מידע
- **שני שינויים בפרומפט בלבד, שום דבר אחר**: **א.** "2-4 מילים" → **"2-3 מילים, עדיפות ל-2"**. **ב.** פסקה חדשה: אל תוסיף תואר אלא אם הוא באמת מבחין בין הפריט לפריט אחר ברשימה - אם השם החשוף כבר מספיק כדי לדעת מה לארוז, לעצור שם. עם דוגמאות טובות/רעות משלה (לא אותה דוגמה כמו פסקת האורך - זו ממוקדת בתוארי קישוט ספציפית, לא באורך כללי)
- **הדוגמאות "רעות" נבחרו כך שיהיו חד-משמעיות**: הניסוח הראשון ("waterproof rain jacket") היה בעצמו בתוך טווח 2-3 המילים, מה שהפך אותו למבלבל כדוגמת "רע" - הוחלף ב"lightweight compact umbrella"/"waterproof windproof jacket" (שני תוארים מוערמים, חריגה ברורה). הדוגמאות בעברית ("מטרייה מתקפלת קלה"/"מעיל מעבר עמיד למים") נשארו כמו שהוצעו
- **`generationConfig` לא נגע** - `temperature: 0.7`, `maxOutputTokens: 2000`, `responseMimeType: "application/json"` זהים. `packing.html` לא נגע בכלל - זה תיקון פרומפט בלבד
- נבדק: הפרומפט המלא הורץ ב-Node עבור עברית ואנגלית, אומת שהדוגמאות תואמות שפה בשתי הפסקאות (אורך ותוארים) ושה-`generationConfig` לא השתנה. **לא נבדק מול Gemini עצמו עד לפריסה** - חוזה חיצוני; `firebase deploy --only functions` באישור נפרד

## v4.11.8 — רשת ביטחון: סינון כפילויות-תחילית בהצעות AI (packing.html)

### 🐛 תיקון
- **commit ב' משניים** (אחרי v4.11.7). בדיקה בפרודקשן של תיקון הפרומפט הראתה: הפריטים קצרים עכשיו, אבל נשארו כפילויות מסוג חדש - תחילית מדויקת ברמת מילים: `"צעיף קל"` / `"צעיף קל לסתיו"`, `"מטרייה מתקפלת"` / `"מטרייה מתקפלת קלה"`. בשונה מהמקרה הקודם (`"מעיל מעבר"` מול `"מעיל סתווי"`) - זה כן ניתן לתפיסה טקסטואלית
- **כלל שלישי, ב-`renderAiSuggestions`**: פריט נחשב כפילות אם אחרי נירמול המילים שלו הן תחילית מדויקת של פריט אחר - ברמת **מילים שלמות**, לא תווים. `"מעיל"` לא תופס את `"מעילים"` או `"מעילון"` (מחרוזות שונות לגמרי במערך המילים, לא substring). כש-2 פריטים מזוהים ככפילות - **נשמר הקצר**, ללא תלות בסדר ההגעה מה-API (אם הארוך מגיע ראשון במערך, הוא עדיין מוחלף בקצר כשהקצר מגיע)
- **החלטה מפורשת על המקרה שביקשת שאשקול**: פריט של מילה אחת לא נכנס לבדיקת התחילית בכלל - רק להתאמה מדויקת. הנימוק: מילה בודדת גנרית (`"מעיל"`) היא סיגנל חלש מדי מול פריט ספציפי יותר (`"מעיל גשם"`) - הם עלולים להיות שני דברים שהמשתמש הוסיף בכוונה, לא אותו פריט. הכלל דורש שהצד הקצר יהיה **לפחות 2 מילים**
- **הרחבת סינון מול הרשימה הקיימת**: היה בודק רק `currentItems()` (הקטגוריה הפעילה), עכשיו בודק את **כל** הרשימות של המשתמש (`Object.values(items)` על פני כל ה-lists, לא רק `activeList`) - הצעה שכבר קיימת ברשימה *אחרת* מסוננת גם היא
- **נירמול, בסדר הנכון**: הסרת סוגריים ותוכנם → צמצום רווחים כפולים → trim → lowercase (בניגוד לניסוח הקודם שהציע סדר שגוי - `"מטען (נייד) חזק"` היה משאיר רווח כפול)
- **אבחון בקונסול, לא חסימה**: `console.log` כשמשהו סונן בפועל - כמה מול הרשימה הקיימת וכמה ככפילות בתוך המנה עצמה (מתוך כמה שהוצעו) - כדי לדעת אם הפרומפט לבד מספיק או שהרשת עושה את העבודה. `console.warn` (לא חוסם) אם אחרי הסינון נשארו פחות מ-3 הצעות - סימן שמשהו למעלה בשרשרת עלול להיות לא תקין
- נבדק בפועל, לא רק נקרא: **הזוגות האמיתיים מהפרודקשן** - `"צעיף קל"`/`"צעיף קל לסתיו"` ו-`"מטרייה מתקפלת"`/`"מטרייה מתקפלת קלה"` - מזוהים נכון ככפילות. **הזוגות שאסור שייתפסו** - `"מטען נייד"`/`"מטען חשמלי"` ו-`"צעיף קל"`/`"כפפות קלות"` - לא מזוהים ככפילות. בדיקות נוספות: `"מעיל"` מול `"מעילים"`/`"מעילון"` (לא נתפס, ברמת מילים לא תווים), סדר הגעה הפוך (הארוך קודם) - עדיין נשמר הקצר, נירמול סוגריים תקין. כל הבדיקות הורצו גם כפונקציות טהורות ב-Node וגם מול הפונקציה האמיתית בדף עצמו (`window.__test` זמני - הוסר ואומת שהוסר), כולל תרחיש מלא: מנת AI עם כפילויות + פריט שכבר קיים ברשימה לא-פעילה → הצ'יפים שנוצרו, ה-`console.log`, וה-`console.warn` כולם תואמים למצופה

## v4.11.7 — פרומפט הצעות AI לציוד: שמות קצרים, בלי כפילויות סמנטיות (functions/index.js)

### 🐛 תיקון
- **הבעיה**: הצעות ה-AI לרשימת הציוד ייצרו כפילויות סמנטיות - אותו פריט אמיתי בכמה ניסוחים שונים באותה תשובה (למשל "מעיל מעבר עמיד למים ולרוח" + "מעיל סתווי עמיד לרוח ולגשם", או שלוש גרסאות של "מטען נייד"). מדוגמה אמיתית: 9 פריטים, 8 מהם ארבעה זוגות/שלישיות של אותו פריט. סינון טקסטואלי לא יכול לתפוס את זה - אלה מחרוזות שונות לגמרי, לא שגיאות כתיב
- **השורש היה בפרומפט**, לא בסינון: הוא ביקש "specific items" עם דוגמה אחת שהיא כבר משפט ("power adapter for [region] outlets"), ולא הגביל אורך ולא אמר במפורש לא לחזור על אותו פריט בניסוח שונה
- **שלושה תיקונים לפרומפט**: **א.** אילוץ אורך מפורש - כל פריט הוא שם של 2-4 מילים, לא תיאור, עם דוגמאות טובות/רעות (כולל הדוגמה האמיתית "מטען נייד חזק ליום שלם מחוץ למלון" כדוגמה ל"רע"). **ב.** הדוגמאות תלויות-שפה - כש-`language` הוא `en` כל הדוגמאות באנגלית בלבד, וכש-`he`/ברירת מחדל כולן בעברית בלבד. **לא** דוגמאות דו-לשוניות מעורבות באותה הוראה - המודל מתבקש לענות בשפה אחת (`Respond in ${lang}`), ודוגמאות מעורבות עלולות לגרום לו לערבב שפות בפועל. **ג.** הוראה מפורשת: כל פריט הוא צורך נפרד, אל תציע את אותו פריט בניסוחים שונים
- **`generationConfig` לא נגע** - `maxOutputTokens: 2000`, `responseMimeType: "application/json"` ו-`temperature: 0.7` נשארו בדיוק כמו שהיו. פריטים קצרים יורידו את הצריכה, לא יעלו אותה
- **מכוון**: זהו commit א' משניים. commit ב' (רשת ביטחון בסינון ב-`packing.html`) מגיע אחריו בכוונה, כדי שאפשר יהיה לבדוק אם התיקון בפרומפט לבד פותר את הבעיה לפני שהרשת מסתירה את התוצאה
- נבדק: תבנית הפרומפט הורצה ב-Node עם קלט עברית ואנגלית, אומת ידנית שהדוגמאות תואמות שפה בכל מקרה ושה-`generationConfig` לא השתנה. **לא נבדק מול Gemini עצמו** - זה חוזה חיצוני; דורש `firebase deploy --only functions` שלא בוצע, ממתין לאישור נפרד

## v4.11.6 — legs.js: האזהרה על lat/lon חסר שותקת בטיול שעוד לא נטען

### 🐛 תיקון
- **הבעיה**: ה-`console.warn` שנוסף ב-legs.js (v4.11.2) ירה על **כל** טעינת עמוד ב-`itinerary.html`, `places.html` ו-`packing.html` - לא רק על טיול אמיתי עם נתון חסר. הסיבה: שלושת העמודים האלה מריצים "תיקון הבזק" קיים - אם יש שפה שמורה ב-localStorage, הם קוראים `applyLanguage()` (וב-`places`/`itinerary` גם פונקציות נוספות בשרשרת) **באופן סינכרוני, מיד בטעינה, לפני שה-Firebase עונה** - על אובייקט `trip` כמעט ריק (`{language:"he"}` בלבד). זה דפוס קיים משכבר, לא רגרסיה - `trip.destination` היה `undefined` שם גם לפני שהיה `legs.js` בכלל, פשוט בשקט. האזהרה החדשה נתנה לזה קול, ונמדד בפועל: 3 פעמים ב-`places.html`, 2 ב-`packing.html`, ו-**6** ב-`itinerary.html` (יותר מהצפוי - `applyLanguage()` שם משרשרת גם ל-`renderDayTabs()`→`tripDayDates()` וגם ל-`renderActivities()`→`computeLiveActivityId()`, וכל אחת קוראת ל-`getLastLeg()` בנפרד). אזהרה שיורה בכל טעינת עמוד היא אזהרה שמתעלמים ממנה - בדיוק ההפך ממה שהיא נועדה לעשות
- **התיקון, ב-`getLegs()` בלבד** - שום דבר לא השתנה בארבעת העמודים עצמם: האזהרה יורה עכשיו רק כש-`trip.destination` **קיים** וגם `lat`/`lon` **חסרים** - טיול אמיתי עם נתון חסר, בדיוק המקרה שרצינו לתפוס. אובייקט בלי `destination` (בין אם ריק לגמרי `{}` ובין אם `{language:"he"}` כמו במקרה שתואר למעלה) מתפרש כ"עוד לא נטען" ולא מייצר אזהרה. בכוונה **לא** נבדק לפי `trip.language` או שדה אחר - אובייקט לפני-טעינה יכול להיראות שונה בעמוד אחר; `destination` הוא המשותף לכולם
- **ערך ההחזרה של `getLegs()` לא השתנה בכלל** - רק מתי מודפסת האזהרה. טיול בלי `lat`/`lon` עדיין מחזיר רגל עם `lat`/`lon` בתור `undefined`, בדיוק כמו היום
- נבדק בפועל, לא רק נקרא: **1.** שלושת העמודים (`places`, `packing`, `itinerary`) נטענו בפועל בדפדפן עם `langKey` שמור ב-localStorage - אומת ש"תיקון ההבזק" אכן רץ (הכותרת הציגה placeholder ריק, "•", בדיוק כמו לפני התיקון) - ואפס אזהרות בקונסול, בטאב נקי לגמרי כדי לשלול שאריות מבדיקות קודמות. **2.** טיול אמיתי טעון (כל שבעת השדות) - אפס אזהרות. **3.** אובייקט מזויף עם `destination` בלי `lat`/`lon` - האזהרה יורה, פעם אחת - זה המקרה שהאזהרה קיימת בשבילו. **4.** אובייקט ריק לגמרי `{}` - אפס אזהרות, ומחזירה רגל אחת עם שדות `undefined`, זהה למה שהיה לפני התיקון. כל הבדיקות הורצו זהות בשלושת העמודים

## v4.11.5 — קריאות destination/endAt דרך getLegs ב-packing.html (אפס שינוי התנהגות)

### ✨ תכונה חדשה
- **שלב 2ב-ב מתוך docs/schema-legs.md**: `packing.html` בלבד, מבודד מהקומיט הקודם (itinerary/places/shopping) כי שני מתוך ארבעת אתרי הקריאה כאן הם ה-payload ל-Cloud Function של הצעות ה-AI (`requestAiSuggestions()`) - חוזה חיצוני, לא תצוגה פנימית. אם משהו שם נשבר, רוצים שזה יהיה מבודד לקומיט משלו
- **אותו מיפוי כמו בקומיטים הקודמים**: `destination` ← `getPrimaryLeg()`; `endAt`/`endDate` ← `getLastLeg()`; `startAt`/`startDate` נשאר נקרא ישירות מ-`trip.startAt` - כולל בתוך ה-payload ל-AI (`startDate: trip.startAt`, ללא שינוי)
- **ארבע נקודות עדכון**: `applyTheme()` (מטמון localStorage), `applyLanguage()` (`document.title` + `#destination`), `requestAiSuggestions()` (ה-payload שנשלח ל-Cloud Function), וכתיבת הסיכום ל-`users/{uid}/trips/{tripId}` (אותו דפוס כמו בשלושת העמודים הקודמים)
- נבדק בפועל, לא רק נקרא, עם אותו דפוס `window.__test` זמני (הוסר ואומת שהוסר לפני ה-commit): **א.** טיול אמיתי (ברלין) - `document.title`, `#destination`, וה-**payload המלא** ל-AI הודפסו לפני ואחרי והושוו מול השדות הגולמיים, זהים בייט-לבייט. **ב.** טיול מזויף עם 2 רגליים ו-timezone/`endAt` שונים (רומא/ניו יורק) - הכותרת הציגה את הרגל הראשונה (רומא), ה-payload ל-AI הכיל `destination:"רומא"`, `startDate` זהה ל-`trip.startAt` הגולמי (ללא שינוי), ו-`endDate` מהרגל **האחרונה** (20/10, לא 9/10 של הרגל הראשונה) - אותה בדיקה שתפסה את הטעות הפוטנציאלית ב-2א. **ג.** אין שגיאות קונסול חדשות מעבר לכשלי הרשת הצפויים בסביבת הבדיקה (ושתי שגיאות `console.error` צפויות מיירוט ה-`fetch` המכוון בבדיקה עצמה, כדי לתפוס את ה-payload בלי לפגוע ברשת אמיתית)

## v4.11.4 — קריאות destination/lat/lon/timezone/endAt דרך getLegs (itinerary/places/shopping, אפס שינוי התנהגות)

### ✨ תכונה חדשה
- **שלב 2ב-א מתוך docs/schema-legs.md**, שלושה עמודים מודולריים: `itinerary.html`, `places.html`, `shopping.html`. `packing.html` בכוונה **לא** בקומיט הזה - ה-payload שלו ל-Cloud Function מבודד בקומיט נפרד (חוזה חיצוני, לא תצוגה פנימית)
- **המיפוי, זהה לשלב 2א**: `destination`/`lat`/`lon` ← `getPrimaryLeg()`; `timezone`/`endAt` ← `getLastLeg()`; `startAt` נשאר נקרא ישירות מ-`trip.startAt`/`tripData.startAt` - לא עובר דרך `getLegs`, בדיוק כמו ב-index.html, ומאותה סיבה (§4 במסמך)
- **מימוש: קריאה מפורשת בכל נקודת שימוש** (`window.getPrimaryLeg(trip)`/`window.getLastLeg(trip)`), לא מוטציה על אובייקט ה-`trip`/`tripData` המשותף. שלוש האפשרויות שנשקלו ולמה זו נבחרה: מוטציה (אפשרות ג) הייתה מייצרת אובייקט שלא משקף את ה-snapshot מ-RTDB - סותר ישירות את הכלל ב-§8ב "הקוד הקיים הוא מקור האמת על מבנה הנתונים" - ובנוסף `onValue` יורה בכל שינוי בתת-עץ הטיול, כך שהדריסה הייתה חוזרת שוב ושוב ויוצרת invariant סמוי. משתנה מודול חדש (אפשרות א) היה דורש את אותו מספר שורות בדיוק בלי היתרון של הצהרה מקומית בכל אתר שימוש
- **כל שלושת העמודים התבררו כבעלי נקודת חנק יחידה** לטעינת ה-snapshot: `itinerary.html:115` ו-`places.html:176` (`trip=s.val()` בתוך `listen()`), ו-`shopping.html:2766` (`tripData = data || {}` בתוך `syncTripUI()`, הנקראת מ-`onValue` ב-2930) - אבל בניגוד ל-`index.html`, אין שכבת ביניים של משתנים גלובליים מנורמלים, אז ההחלפה עדיין נגעה בכל אתר קריאה בנפרד, לא רק בנקודת החנק
- **גילוי אמיתי, לא רק בדיקה**: `places.html:113` (`GEO_RULES`, `t=>!isNearCoast(t.lat,t.lon)`) מתברר להיות קריאת `trip.lat`/`trip.lon` אמיתית - `t` שם הוא אובייקט הטיול עצמו (`forTrip`), לא נקודת מקום. עודכנה בהתאם, בשתי נקודות הקריאה ב-`categories()`
- **אין fallback ל-`flightOut`/`tripEnd`/`flightBack` בשלושת העמודים האלה** (בניגוד ל-`index.html`) - כל שלושתם קוראים רק `startAt`/`endAt` ישירות, ללא שכבת תאימות לשמות ישנים. אומת בחיפוש מקיף לפני הכתיבה
- **תשומת לב מיוחדת ל-`tripDayDates()`** ב-`itinerary.html` (שורה 99) וב-`places.html` (שורה 168): `endAt` עובר ל-`getLastLeg`, `startAt` נשאר. בטיול רגל אחת אין הבדל בערך בין הישן לחדש - **בדיוק לכן קל לכתוב את זה לא נכון בלי לשים לב** - אומת במפורש בבדיקה עם טיול מזויף בעל שתי רגליים ו-timezone/endAt שונים ביניהן
- **הבאג הידוע ב-`tripDayDates()` של `places.html`** (בלי נרמול timezone, בניגוד לגרסה של `itinerary.html`) - לא נגע, מחוץ להיקף כפי שהוחלט קודם
- **`wizard.html` לא נגע בכלל** - נבדק ואומת שאין בו שום קריאה לשדות אלה מטיול קיים/טעון; כל השימושים שם הם כתיבה של טיול חדש (מהטופס + geocoding) או קריאת תצוגה מאינדקס-סיכום שאף פעם לא שמר lat/lon/timezone מלכתחילה. שייך במלואו לשלב 10
- נבדק בפועל על שלושת העמודים, לא רק נקרא: לכל עמוד הוצמד זמנית `window.__test` שחשף את `trip`/`tripData` ואת הפונקציות הרלוונטיות (הוסר ואומת שהוסר לפני ה-commit): **א.** טיול אמיתי (ברלין) - כל ערך שהוחלף (`document.title`, `#destination`/`#destinationKicker`, `tripDayDates()`) הודפס והושווה מול השדה הגולמי המקורי, זהה בייט-לבייט; ב-`places.html` גם `GEO_RULES` אומת ישירות (טיול פנים-יבשתי מסתיר "חוף", טיול חופי לא). **ב.** טיול מזויף עם 2 רגליים ו-timezone/`endAt`/קואורדינטות **שונים** בכל רגל (רומא/ניו יורק) - הכותרת ואלמנט היעד הציגו את הרגל הראשונה (רומא) ולא האחרונה, `tripDayDates()`/מספר הימים שיקפו נכון את ה-`endAt` של הרגל **האחרונה** (15 ימים, לא 4 שהיו מתקבלים אילו נשאר על הרגל הראשונה), וב-`places.html` הקואורדינטות של הרגל הראשונה (ברלין) קבעו את הסתרת "חוף" למרות שהרגל השנייה (תל אביב) חופית. **ג.** ב-`shopping.html` הודפס גם ה-payload המלא שנכתב ל-`users/{uid}/trips/{tripId}` (`destination`/`startAt`/`endAt`) לפני ואחרי - התאמה מלאה למצופה. **ד.** אין שגיאות קונסול חדשות מעבר לכשלי הרשת הצפויים בסביבת הבדיקה בכל שלושת העמודים

## v4.11.3 — hydrateTripData ניזונה מ-getLegs (index.html, אפס שינוי התנהגות)

### ✨ תכונה חדשה
- **שלב 2א מתוך docs/schema-legs.md**: `hydrateTripData()` (index.html) - נקודת החנק היחידה שמזינה 30+ פונקציות דרך משתני מודול גלובליים - עכשיו שואבת שבעה מהם (`destinationName`, `latCoord`, `lonCoord`, `hasRealCoords`, `tripTimezone`, `tripEnd`) מ-`getPrimaryLeg()`/`getLastLeg()` במקום ישירות מ-`data`. **אף צרכן לא נגע** - כל 30+ הפונקציות ממשיכות לקרוא את אותם משתנים בדיוק כמו קודם
- **מיפוי, לפי §4 במסמך הסכמה**: `destinationName`/`latCoord`/`lonCoord`/`hasRealCoords` מהרגל **הראשונה**; `tripTimezone`/`tripEnd` מהרגל **האחרונה**. `flightOut` נשאר קורא `trip.startAt` **ישירות, לא דרך `getLegs`** - זה שעון המכשיר של יוצר הטיול, לא שעון היעד, שני דברים שונים סמנטית גם אם הערך זהה בטיול רגל אחת
- **פער תאימות אמיתי שנתפס לפני כתיבת קוד, לא אחריה**: `getLegs()` מכירה רק `trip.startAt`/`trip.endAt` - לא את השמות הישנים `flightOut`/`tripEnd`/`flightBack` שה-`hydrateTripData` הקודמת נפלה אליהם. תוקן בנרמול על **עותק רדוד** לפני הקריאה ל-`getLegs` (`data` המקורי לעולם לא משתנה): אם חסר `startAt` יש נפילה ל-`flightOut`, אם חסר `endAt` יש נפילה ל-`tripEnd`/`flightBack`. כשהנפילה מופעלת בפועל - `console.warn` עם ה-`tripId`, כדי לדעת אם יש טיול אמיתי שעדיין תלוי בשמות הישנים
- **`hydrateTripData` רצה על כל שינוי בתת-עץ הטיול**, לא פעם אחת (מאזין חי על `tripRoot`) - אומת בקוד, לא רק בהנחה. `getLegs()` זולה מספיק שזה בסדר, ובכוונה **לא** נוספה memoization - זה שיפור אגב, וזה commit של אפס שינוי התנהגות
- **`renderActionBadges`/`updateExpensesCard` לא נגעו** - הן מקבלות את ה-`data` הגולמי כפרמטר משלהן, לא דרך המשתנים הגלובליים, ולא נוגעות באף אחד משבעת השדות
- נבדק בפועל, לא רק נקרא: **א.** טיול אמיתי (ברלין, בלי `legs`) - הלוגיקה הישנה והחדשה הושוו ישירות באותו קלט, תוצאה זהה בייט-לבייט על כל 7 המשתנים; אומת גם בדף עצמו (`http://localhost:8777/index.html`, קריאה ל-`hydrateTripData()` בפועל בקונסול) - `appTitle` הציג נכון "ברלין 🇩🇪". **ב.** טיול מזויף עם 2 רגליים ו-timezone **שונה** בכל רגל (רומא/ניו יורק, לא אותו ערך במקרה) - `destinationName`/קואורדינטות מהרגל הראשונה, `tripTimezone`/`tripEnd` מהרגל **האחרונה** (`America/New_York`, לא `Europe/Rome`) - מוכיח שיוך נכון, לא צירוף מקרים. **ג.** טיול מזויף עם `flightOut`/`tripEnd` בלבד, בלי `startAt`/`endAt` - התאריכים נטענו נכון, ה-`console.warn` ירה עם ה-`tripId`. **ד.** אין שגיאות קונסול חדשות מעבר לכשלי הרשת הצפויים בסביבת הבדיקה (אין אינטרנט אמיתי), והעמוד נראה זהה - נבדק דרך שער ההתחברות (Google sign-in לא זמין בסביבת הבדיקה, כמו בכל משימה קודמת הפעם)

## v4.11.2 — legs.js: פונקציות עזר ליעדים מרובים (תשתית בלבד, לא בשימוש עדיין)

### ✨ תכונה חדשה
- **קובץ חדש `legs.js`**, טעון ב-6 קבצי ה-HTML (אחרי `toast.js`). זו התשתית הראשונה למסע רב-יעדים (מסמך סכמה נפרד, `legs`) - **שום קובץ אחר לא השתנה מעבר לתג ה-`<script>`, ואף קוד קיים לא קורא לפונקציות האלה**
- חמש פונקציות, כולן על `window`: `getLegs(trip, tripId?)` (הליבה - מנרמלת כל טיול למערך רגליים; טיול בלי `trip.legs` מקבל רגל יחידה מסונתזת מהשדות הקיימים, כולל `vibes`; `tripId` אופציונלי, רק לזיהוי בלוג האזהרה - ר' סעיף הבא), `getPrimaryLeg`, `getLastLeg`, `isMultiLeg`, `getLegForDate(trip, date)`
- **תוקן לפני שנעשה בו שימוש בפועל: `lon` ולא `lng`**. מסמך הסכמה שהמימוש הזה מבוסס עליו קרא לשדה `lng`, אבל בדיקה מול קוד אמיתי (`wizard.html` כותב, `index.html`/`places.html` קוראים) מצאה שהשדה בפועל ב-RTDB הוא `lon`. הנתונים מנצחים - `legs.js` תוקן ל-`lon`, וגם מבנה הרגל שמוחזר וגם ה-JSDoc-style header מתעדים את זה במפורש (כולל למה זה מבלבל: Open-Meteo מחזיר `longitude`, ורוב קוד המיפוי בעולם משתמש ב-`lng`, אז קוד עתידי שיכתב כאן ינחש `lng` ויקבל `undefined` בשקט). **נבדק בקוד**: `lng` מופיע פעם אחת בלבד בכל הריפו - השורה שתוקנה כאן; אין ערבוב `lat/lng` מול `lat/lon` בשום מקום אחר באפליקציה
- **אזהרה מוקדמת**: אם ל-`getLegs` חסר `lat` או `lon` (בענף הסינתזה של טיול בלי `trip.legs`) - `console.warn` עם ה-`tripId` (אם נמסר) או שם היעד כגיבוי, ואובייקט הטיול המלא ללוג. לא זורק, לא שובר שום זרימה - רק מתריע. טיול עם `trip.legs` תקין לא בודק את זה (הרגליים כבר קיימות, אין סינתזה)
- **script קלאסי בכוונה** (לא `type="module"`) - כמו `toast.js` - כדי שהפונקציות יהיו נגישות דרך `window.*` גם מ-`index.html` (script קלאסי) וגם משאר 5 העמודים (`type="module"`). נבדק בפועל בשני ההקשרים
- **החלטת עיצוב ל-`getLegForDate` על תאריך מחוץ לטווח הטיול**: מחזיר `null`. זה **לא** fallback לנתון חסר (כמו timezone: רגל ← טיול ← מכשיר) - תאריך מחוץ לטווח הוא נתון קיים שבאמת לא שייך לאף רגל, וclamp שקט היה גורם ליום שלפני הטיול להיראות כאילו הוא ברגל הראשונה. קורא שרוצה בכל זאת "הרגל הקרובה ביותר" יקבל את זה מ-wrapper נפרד בעתיד (`getLegForDateOrNearest()`), לא כברירת מחדל של הפונקציה הזו
- בגבול בין שתי רגליים (`legs[n].endAt === legs[n+1].startAt`) - הרגל **היוצאת** מנצחת, כנדרש בסכמה §5.2: טווח סגור-סגור על כל רגל, והתאמה ראשונה במערך (שממילא כרונולוגי) זוכה
- נבדק בפועל, לא רק נקרא, על אובייקטי טיול מציאותיים (אין אינטרנט אמיתי בסביבת הבדיקה, אז נבנו אובייקטים תואמי-סכמה ידנית ולא נשלפו מ-Firebase בפועל): **א.** טיול קיים בלי `legs` - רגל יחידה, כל השדות וה-`vibes` נכונים, גם משתי דרכי גישה (`getLegs` ישיר ו-`window.getLegs`). **ב.** `isMultiLeg` - `false` על טיול כזה. **ג.** `getLegForDate` בתוך הטווח ובדיוק על `startAt`/`endAt` - הרגל הנכונה; לפני/אחרי הטווח - `null`. **ד.** טיול מזויף עם שתי רגליים (רומא→פירנצה, גבול משותף) - `getLegs` מחזירה את המערך המקורי (לא מסונתז), `isMultiLeg` → `true`, `getPrimaryLeg`/`getLastLeg` מצביעות על הרגל הנכונה, `getLegForDate` בגבול המדויק מחזירה את הרגל היוצאת (רומא), ולפני/אחרי הטיול כולו - `null`. נבדק גם ש-`window.*` נגיש (ללא שגיאות קונסול) בכל 6 העמודים

## v4.11.1 — undo וreset רשימה לא מכריזים הצלחה לפני שהכתיבה אושרה (shopping.html)

### 🐛 תיקון
- **undoBtn**: `lastStateBackup` היה מתאפס ל-`null` באופן סינכרוני, בלי קשר לתוצאה של כתיבת השחזור - אם הכתיבה נכשלה, הגיבוי היחיד נמחק והמצב לא ניתן היה לשחזור. עכשיו הגיבוי מתאפס **רק** אחרי שהכתיבה אכן נפתרת בהצלחה
- **clearCurrentListBtn**: שתי כתיבות נפרדות (`remove(listRef)` + `set(listStatusRef,...)`) אוחדו ל-`update()` רב-נתיבי אחד, אטומי - מחיקה חלקית (פריטים נמחקו אבל הסטטוס נתקע, או להפך) גרועה יותר מכשלון מלא
- **הטוסט המיידי לא נדחה** - בשני המקרים הוא נשאר בדיוק כמו היום, מוצג מיד. מה שבאמת השתנה זה מה שקורה **ברקע**, אחרי הטוסט:
  - **הצלחה אמיתית** → אין שינוי גלוי, רק עכשיו `lastStateBackup` באמת מתאפס רק כשמובטח שהשחזור הצליח
  - **rejection מפורש** (permission-denied וכו') → `showToast` שני עם הודעת כשלון, דרך ה-`showToast` המקומי הקיים - לא `window.toast` (איחוד הטוסטים באפליקציה הוא commit נפרד). ב-undo, הטוסט השני מציג מחדש את כפתור ה-undo (`showUndo:true`) כדי שאותו גיבוי יהיה ניתן לניסיון חוזר
  - **timeout (4 שניות, לא 8)** - המקרה של כתיבת RTDB שנכנסת לתור בלי חיבור ולעולם לא נדחית ולא מתאשרת עד שהחיבור חוזר: **בלי שום הודעה למשתמש** - הוא כבר ראה טוסט הצלחה והמשיך הלאה; טוסט שני שמדבר על בעיית חיבור בלי שיש מה לעשות רק ידאיג. רק `console.warn` + `window.__gtWriteTimeout = { op, at: Date.now() }` (`op` הוא `'undo'` או `'clearList'`) - הדרך היחידה לדעת בעתיד אם/כמה פעמים זה קורה. ב-undo, `lastStateBackup` **לא** מתאפס במצב הזה - אין ודאות שהכתיבה תצליח
  - **הסף 4 שניות, לא 8**: כפתור ה-undo חי בערך 3.5 שניות בלבד - הוא יושב בתוך הטוסט שמתפוגג (`.toast.show` מסתיים ב-3.7 שניות). סף של 8 שניות היה פג הרבה אחרי שהמשתמש כבר איבד את ההקשר
- נבדק בפועל, לא רק בקוד, עם כתיבה מדומה בת-שליטה: **א.** undo מצליח - הגיבוי מתאפס, אין שינוי מהתנהגות היום. **ב.** undo נכשל (rejection מדומה) - הגיבוי נשאר, טוסט כשלון מוצג, כפתור ה-undo חוזר להיות גלוי - ולחיצה שנייה עליו, עם כתיבה שמצליחה הפעם, אכן עובדת ומאפסת את הגיבוי. **ג.** undo בפסק זמן - ב-12+ שניות אחרי הקליק, הטוסט עדיין "בוטל בהצלחה" (אין טוסט שני), הגיבוי עדיין שלם, `window.__gtWriteTimeout` נכתב. **ד.** reset רשימה - אימות שנעשית קריאת כתיבה אחת בלבד (לא שתיים), טוסט הצלחה עם כתיבה שמצליחה, טוסט כשלון עם כתיבה שנכשלת, ושתיקה מלאה + `console.warn` בפסק זמן

### 📝 לתיעוד (לא תוקן עכשיו, ר' CLAUDE.md)
`lastStateBackup` משותף ל-5 נקודות כתיבה שונות בעמוד (לא רק undo/clear) - ההגנה שנוספה כאן חלקית מטבעה, כי כל אחת מהן דורסת את הגיבוי היחיד. ר' "lastStateBackup - הגנה חלקית" ב-CLAUDE.md

## v4.11.0 — timeout על loadProfile: אין תקיעה בלי מוצא (packing/places/itinerary)

### ✨ תכונה חדשה
- **הבעיה**: ה-`get()` שטוען את הפרופיל של המשתמש (`loadProfile`, בעיה מספר 1 מהתחקיר) יכול להיתקע לנצח. עד עכשיו זה אומר שגיליון "מי את/ה בקבוצה?" (`#memberOverlay`) לעולם לא נחשף - המשתמש תקוע בלי שום מוצא, בלי הודעה
- **התיקון**, ב-3 עמודים בלבד (`packing.html`, `places.html`, `itinerary.html` - `shopping.html` ו-`index.html` מחוץ להיקף בכוונה): `get()` עטוף בסף של 8 שניות. אם הוא לא נפתר עד אז - מתנהג **בדיוק** כמו המסלול הקיים של "אין profile": חושף את `#memberOverlay`, קורא ל-`profileOptions()`/`syncIdentity()` בהתאם לעמוד. אין UI חדש, אין הודעה, אין כפתור "נסה שוב" - המשתמש בוחר שם ידנית וממשיך, בדיוק כמו כל משתמש חדש היום
- **דגל resolved** מבטיח שהתשובה הראשונה מנצחת, בשני הכיוונים: אם ה-`get()` המקורי נפתר אחרי שהסף כבר פג, הוא מתעלם ממנו לחלוטין (לא דורס `profile`, לא נוגע ב-`#memberOverlay` שכבר נחשף וייתכן שהמשתמש כבר מקליד/בוחר בו) - וגם ההפך, אם ה-`get()` מצליח לפני הסף, ה-timeout מבוטל (`clearTimeout`) ולא עושה כלום. `catch` על שגיאה מפורשת מתנהג באותה צורה בדיוק כמו timeout
- **אבחון**: כשהסף יורה (או כשיש שגיאה), נכתב `console.warn` עם שם העמוד, ונשמר `window.__gtProfileTimeout = { page, at: Date.now() }` - הדרך היחידה כרגע לדעת בעתיד אם/כמה פעמים זה קורה בפועל
- **למה לא helper משותף**: שלושת העמודים דומים מבנית אבל השימוש הוא נקודתי (מקום אחד לכל עמוד) - helper גנרי לא היה מקצר כלום, רק מוסיף שכבת הפשטה. כל עמוד מקבל עטיפה מקומית, בדיוק בגודל שצריך
- **מרוץ שנתגלה תוך כדי בדיקה עצמית, לפני שהיה commit**: `loadProfile()` לא נקראת פעם אחת - היא נקראת בכל ירייה של מאזין הטיול (`onValue` על `tripRoot`), וזה מאזין חי שיורה מחדש על **כל** שינוי בתת-העץ של הטיול, כולל פריטי ציוד. בלי הגנה, קריאה ישנה שעדיין תקועה יכולה למחוק profile שקריאה חדשה וטרייה כבר טענה בהצלחה - ולפתוח מחדש גיליון שכבר נסגר, כולל איפוס בחירה שהמשתמש כבר התחיל להקליד
- **התיקון למרוץ**: מונה `profileLoadGen` ברמת המודול. כל כניסה ל-`loadProfile()` עושה `++profileLoadGen` ולוקחת עותק מקומי (`gen`); מסלול ההצלחה, ה-timeout וה-catch כולם בודקים `if (gen !== profileLoadGen) return` לפני שהם כותבים ל-`profile` או קוראים ל-`syncIdentity`/`profileOptions`. דגל `resolved` המקומי נשאר בדיוק כמו שהיה - הוא פותר מרוץ **בתוך** אותה קריאה (timeout מול get שנפתר בו-זמנית); המונה פותר מרוץ **בין** קריאות שונות. שניהם נחוצים, לא חלופיים זה לזה. בנוסף, `profileLoadTimer` ברמת המודול - כל כניסה חדשה מנקה (`clearTimeout`) את הטיימר של הקריאה הקודמת כניקיון best-effort, אבל ההגנה האמיתית היא בדיקת המונה, לא הניקוי
- **למה לא `if(!profile)` בנקודת הקריאה**: זה היה מונע מ-`syncIdentity()`/`profileOptions()` לרוץ מחדש כשמשתתף *אחר* תובע שם - הדרופדאון היה מתיישן. רוצים שהקריאה תמשיך לרוץ בכל פעם; רק שקריאה ישנה לא תדרוס תוצאה חדשה יותר
- נבדק בפועל (לא רק בקוד), בכל 3 העמודים, דרך קריאה ישירה לפונקציה עם `get()` מדומה בר-שליטה (אין אינטרנט אמיתי בסביבת הבדיקה): **א.** פתרון מהיר עם profile - אין שינוי, הגיליון לא נחשף. **ב.** תקיעה אמיתית - אחרי 8+ שניות הגיליון נחשף, ה-`console.warn` וה-`window.__gtProfileTimeout` נכתבים, מאומת בצילום מסך בהקשר עמוד מלא (packing.html). **ג.** תקיעה שנפתרת בשנייה 12 עם profile אמיתי, אחרי שהגיליון כבר נחשף ומשתמש מדומה כבר "בחר" שם בתפריט - ב-30+ שניות אחרי ההתחלה, `profile` נשאר `null`, הגיליון נשאר גלוי, הבחירה לא התאפסה, וחותמת הזמן לא השתנתה. **ד.** קריאה ראשונה תקועה, קריאה שנייה (דור עדכני יותר) נפתרת מהר עם profile אמיתי - אחרי שהסף של הראשונה פג: `profile` נשאר הערך האמיתי מהקריאה השנייה, הגיליון לא נפתח, `window.__gtProfileTimeout` לא נכתב כלל. **ה. הכיוון ההפוך**: קריאה ראשונה נפתרת מהר, קריאה שנייה (הדור העדכני) נתקעת - הסף שלה כן יורה כרגיל, הגיליון נחשף, `profile` חוזר ל-`null`

## v4.10.1 — רשימת הציוד לא תלויה בטעינת הזהות (packing.html)

### 🐛 תיקון
- **הבעיה**: `listenPacking()` - המאזין שטוען את כל רשימת הציוד מ-Firebase - נקרא רק משני מקומות: מזרימת ההצטרפות, ומתוך `loadProfile()` **ורק אם `profile` קיים**. כלומר למשתמש חוזר, כל הרשימה תלויה בכך ש-`get()` של הפרופיל שלו יצליח. אם היא נתקעת (הבעיה מספר 1 מהתחקיר הקודם) - הרשימה נשארת `0/0` ריקה לצמיתות, בלי שום שגיאה, וניסיון להוסיף פריט נכשל בשקט לגמרי (`addItem` בודק `!profile` ועושה `return` בלי אפילו טוסט)
- **התיקון**: `listenPacking()` נקרא עכשיו **גם** באופן עצמאי, ישירות ממאזין טעינת הטיול (אותו מקום שבו `applyTheme`/`applyLanguage`/`configureLinks`/`participantOptions` כבר רצים ללא תלות בזהות) - לא רק מתוך `loadProfile()`. `loadProfile()` עצמו נשאר אחראי רק על טעינת שם המשתמש והצגת/הסתרת גיליון "מי את/ה בקבוצה" - שום קוד לא הניח שהוא רץ אחרי הקריאה שהוסרה ממנו (היא הייתה המשפט האחרון בפונקציה)
- **בטיחות שנבדקה, לא רק נקראה**: `listenPacking()` וכל שרשרת ה-render שהוא מפעיל (`render`→`renderMine`/`renderGroup`, `ensureStarterLists`) אינם מפנים בשום מקום ל-`profile` - רק ל-`user.uid` ולנתוני הטיול/פריטים עצמם. `packingListening` (דגל קיים, `packing.html:1056`) כבר מונע קריאה כפולה - הקריאה השנייה (מזרימת ההצטרפות, אחרי שהמשתמש בוחר שם) פשוט לא עושה כלום, ו-`syncIdentity()` הצמוד אליה ממשיך לעדכן את התצוגה כרגיל
- נבדק בפועל (לא רק בקוד): שני התרחישים דרך קריאה ישירה לפונקציות הפנימיות (הזרקת מצב מדומה, כי אין אינטרנט אמיתי בסביבת הבדיקה) - **א.** משתמש עם `profile` - הרשימה נטענת, גיליון הבחירה נשאר סמוי (ללא שינוי מההתנהגות הקודמת). **ב.** משתמש בלי `profile` - הרשימה נטענת **וגם** גיליון "מי את/ה בקבוצה?" מוצג, בו-זמנית - זה החידוש, מאומת בצילום מסך בהקשר עמוד מלא. גם `listenPacking()` הופעל פעמיים ברצף ישירות - הדגל עולה פעם אחת, הקריאה השנייה לא זורקת שגיאה ולא עושה כלום, כצפוי

## v4.10.0 — תשתית משותפת: טוסט גלובלי + רג'יסטרי onForeground

### ✨ תשתית (לא משנה שום דבר שהמשתמש רואה היום)

הכנה לניטור חיבור ל-Firebase RTDB (`.info/connected`) ו-reconnect
כפוי ב-visibilitychange, בעקבות תחקיר קריאה-בלבד. שלושה חלקים,
אף אחד מהם עדיין לא מחובר להתנהגות קיימת:

- **`window.toast(message, type)`** (`toast.js`, חדש, נטען בכל 6
  העמודים אחרי version.js) - טוסט גלובלי לא-חוסם, נעלם לבד,
  `type` אופציונלי `'info'`/`'error'`. מוגדר ב-CSS חדש ב-shared.css
  (`.gt-toast`, סוף הקובץ):
  - **צבע הרקע הבסיסי** קבוע בכוונה (לא theme-token) - כדי לקרוא
    נכון על פני כל 4 ערכות הטיול ומצב כהה בלי override לכל אחת.
    **צבע ה-error** לעומת זאת משתמש ב-`var(--danger, #D14343)` -
    אותו טוקן סמנטי שכפתור המחיקה בדיאלוג הגלובלי כבר משתמש בו -
    כי צבע סמנטי (שגיאה/סכנה) צריך להישאר עקבי בכל האפליקציה,
    בניגוד לצבע הבסיס שאין לו משמעות כזו לשמר
  - **z-index 200** - זהה לטוסט הישן, בכוונה: טוסט לא אמור לצוף
    מעל דיאלוג אישור (השתמשנו קודם ב-3000 בטעות, לפני שהתבקשנו
    להשוות ל-200)
  - **מיקום**: לא קרוב ל-88-90px שבו יושבים שלושת מימושי הטוסט
    הקיימים (`.toast` הליבה, `.app-toast` של index.html) - אלא
    `calc(160px + safe-area-inset-bottom)`, כדי ששני הטוסטים לא
    ייפלו זה על זה אם שניהם מוצגים בו-זמנית, **וגם** לא ייגע
    ב-`.fab` (שהקצה העליון שלו יושב ב-148px מתחתית המסך: `bottom:
    90px` + גובה `58px`). ניסינו קודם 146px וזה השאיר מרווח של
    2px בלבד מה-FAB - לא מספיק, במיוחד עם הודעה ארוכה שמתרחבת
    כמעט לרוחב המסך. נבדק בפועל ב-viewport של 375×667 (iPhone SE)
    עם `getBoundingClientRect`: מרווח אמיתי של 12px מה-FAB, ושני
    הטוסטים בו-זמנית מוצגים בלי לחפוף זה את זה, את ה-FAB, או את
    התפריט התחתון - כולל צילום מסך
  - קריאות רצופות ל-`window.toast` משתמשות באלמנט DOM אחד
    ובטיימר אחד שמתאפס (לא נערם) - נבדק.
- **המימושים המקומיים הקיימים לא נגעו** (`toast()`/`showToast()`
  בכל עמוד) - עדיין משרתים את כל הקריאות הקיימות באפליקציה.
  ראו CLAUDE.md, "שתי מערכות טוסט במקביל" - האיחוד (מחיקת
  המימושים הישנים) הוא commit נפרד, עדיין לא קיים
- **תגלית תוך כדי בדיקה, לא כפי שתועדה בהתחלה**: `window.toast`
  נדרס בפועל (על ידי המימוש המקומי של אותו עמוד) **רק** ב-
  index.html - לא ב-4 מתוך 6 כפי שהונח לפני שנבדק בדפדפן אמיתי.
  הסיבה: index.html הוא היחיד עם script קלאסי; שאר 5 העמודים
  מריצים את ה-toast המקומי שלהם בתוך `type="module"`, שההצהרות
  שלו לעולם לא נדבקות ל-window. אותה חלוקה בדיוק כמו ה-SDK
  (compat v8 מול מודולרי v9) - ראו CLAUDE.md, "מבנה סקריפטים
  בין 6 הדפים"
- **`window.onForeground(fn)`** (ב-version.js) - רג'יסטרי מינימלי:
  רושם callback שרץ כשהעמוד חוזר להיות גלוי, דרך מאזין
  visibilitychange **יחיד** לכל הצרכנים (לא אחד לכל צרכן).
  `checkForUpdate()` הקיים (בדיקת גרסה + באנר "גרסה חדשה
  זמינה") נרשם עכשיו דרך אותו רג'יסטרי במקום מאזין נפרד משלו -
  ההתנהגות שלו זהה לחלוטין: אותו DOM, אותו z-index, אותם
  guard-ים (`shown`/`checking`), אותה הודעה. נבדק ישירות (לא
  רק נקרא): mismatch אמיתי מציג באנר, mismatch שני לא מציג
  כפול, dismiss מסיר, ה-fetch ל-version.js אכן קורה על
  visibilitychange
- נבדק: 6 העמודים טוענים בלי שגיאות קונסול חדשות; `typeof
  window.toast`/`window.onForeground` הן function בכל עמוד;
  ה-diff בכל אחד מ-6 קבצי ה-HTML הוא שורת `<script>` אחת בלבד

## v4.9.1 — כפתור הבית המוקדם ב-shopping.html

### 🐛 תיקון
- **תיקון לטעות מ-v4.9.0**: הערך שם ("ל-shopping.html אין בכלל configureLinks, קישורי הניווט שלו לא מתעדכנים עם tripId בשום שלב") היה שגוי - נבדק שוב לעומק. לעמוד יש חיווט מקביל מלא, רק בשם אחר: `scopedUrl()` וקוד שמעדכן את `.bottom-nav a` + `#homeBtn` (כפתור בית נפרד בכותרת, בנוסף ל-`#navHome` בסרגל התחתון), חי בתוך `syncTripUI()`. כלומר זה בדיוק אותו מרוץ תזמון שהיה בשלושת העמודים האחרים (packing/places/itinerary) לפני v4.9.0 - לא חיווט חסר לגמרי כפי שנכתב
- **התיקון**: הוצא הקוד שהיה חבוי בתוך `syncTripUI()` (עדכון קישורי `.bottom-nav a`, `#homeBtn`, והחלפת קישור ההוצאות ל-`tripData.spliitUrl`) לפונקציה עצמאית בשם `configureLinks()` - אותו שם בדיוק כמו בשאר העמודים. `syncTripUI()` קורא לה כרגיל אחרי טעינת הטיול, אבל היא רצה **גם** מיד עם טעינת הדף, סינכרונית מה-tripId שבכתובת - אותה תבנית כמו בשלושת העמודים האחרים
- **בטיחות לקריאה מוקדמת**: הקריאה ל-`tripData.spliitUrl` הפכה ל-`tripData?.spliitUrl` (אופציונל-צ'יינינג) כי `tripData` הוא `null` עד שהטיול נטען - קריאה מוקדמת בלי זה הייתה זורקת שגיאה
- נבדק: `#homeBtn` ו-`#navHome` נושאים tripId מיד בטעינה (גם עם וגם בלי tripId בכתובת), אין שגיאות קונסול חדשות

## v4.9.0 — מסך בחירת טיול ב-index.html כשאין tripId (שלב 2/2)

### ✨ תכונה חדשה
- **הבעיה**: משתמש מחובר שנוחת על index.html בלי tripId בכתובת (לחיצה על כפתור הבית לפני שהתעדכן, פתיחה מאייקון PWA, או tripId שכבר לא קיים) ראה עד היום דשבורד **מזויף** - כותרת, טיימר, מזג אוויר וכרטיסי פעולה עם נתוני ברירת מחדל קשיחים בקוד, בלי שום סימן שזה לא הטיול האמיתי שלו. זה כנראה בדיוק מה שדווח בעבר כ"האפליקציה החליפה לי טיול" - לא טיול מחוק שצץ מחדש, אלא placeholder שנראה כמו טיול אמיתי
- **התיקון**: כשמשתמש מחובר מגיע בלי tripId תקין, index.html טוען את `users/{uid}/trips` (אותו נתיב שנכתב גם ביצירת טיול ב-wizard.html וגם בהצטרפות בכל 4 העמודים האחרים, v4.8.0) ומציג מסך בחירה במקום הדשבורד:
  - **טיול אחד** ברשימה → ניווט אוטומטי אליו, בלי מסך ביניים
  - **כמה טיולים** → רשימה לחיצה (יעד, סוג, תאריך התחלה), אותו מסך למארגן ולמשתתף - רק הרשימה שמאחוריו שונה
  - **אין טיולים** → הודעה + קישור ליצירת טיול חדש (wizard.html)
  - **tripId שגוי/טיול שנמחק** → אותו מסך בחירה, עם הודעת "הטיול לא נמצא" מעליו
- **lastTripId**: מפתח localStorage חדש (נפרד מהמטמון הקיים `trip-{id}-*`), נכתב בכל אחד מ-5 עמודי הטיול (index/packing/places/itinerary/shopping) **רק** אחרי טעינה מוצלחת של טיול אמיתי - לא סתם כשיש tripId בכתובת, כדי לא לשמור מזהה של טיול מחוק וליצור לולאת הפניות. index.html בודק אותו לפני שהוא פונה בכלל ל-users/trips: אם קיים, מפנה ישר לטיול האחרון שנטען בהצלחה (המקרה הנפוץ - כפתור בית / אייקון PWA); אם הטיול הזה מתברר כלא תקין, המפתח נמחק ונופלים חזרה לרשימה מ-users/trips
- **מניעת חזרה של הדשבורד המזויף**: `updateClockAndPhases()` (הפונקציה שמצירת את הטיימר/מזג האוויר/הבאנרים ורצה כל שנייה ב-setInterval) הייתה מציגה מחדש את הבאנרים עם נתוני ברירת מחדל גם אחרי שהוסתרו, כי היא לא הייתה תלויה בכלל בכך שנמצא טיול אמיתי. נוסף דגל `tripPickerActive` שחוסם אותה כליל כשמסך הבחירה פעיל - אומת שהדשבורד המזויף לא חוזר גם אחרי כמה סבבי טיימר
- **כפתור הבית המוקדם**: `configureLinks()` (שמעדכן את קישורי הניווט, כולל כפתור הבית, עם tripId) עכשיו רץ גם מיד עם טעינת העמוד (סינכרונית, מה-tripId שבכתובת) - לא רק אחרי שהטיול נטען מ-Firebase - ב-packing.html, places.html, itinerary.html. **shopping.html לא כלול בסבב הזה** - תוקן בנפרד ב-v4.9.1 (התיאור כאן שהופיע במקור, שלעמוד אין בכלל חיווט מהסוג הזה, היה שגוי - תוקן שם)
- נבדק: כל 4 המצבים (0/1/2+ טיולים, טיול לא נמצא) ב-index.html, כולל שהדשבורד המזויף באמת נעלם ולא חוזר; lastTripId נכתב ונקרא נכון (כולל מחיקה כשהוא כבר לא תקף); כפתור הבית נושא tripId מיד בטעינה ב-3 העמודים; דסקטופ ומובייל; אין שגיאות קונסול חדשות
- **מגבלת בדיקה**: בסביבת האוטומציה אין חיבור Firebase אמיתי, אז מסלול ה-`users/{uid}/trips` לא נבדק מול נתונים אמיתיים - רק עם נתונים מדומים שהוזרקו ישירות לפונקציית הרינדור. מומלץ לוודא בזרימה אמיתית (משתמש עם כמה טיולים) לפני שממשיכים הלאה

## v4.8.1 — datetime-local לא נפתח בלחיצה בדסקטופ (רגרסיה)

### 🐛 תיקון
- **הרגרסיה**: בתיקון הקודם (v4.8.0 ומטה, שני הסבבים על date-only) הרחבנו את הסלקטור `.date-picker-shell input[type="date"]` והוספנו מאזין `click` שמפעיל `showPicker()` **רק** על `type="date"` - מתוך הנחה ש-`datetime-local` "כבר נפתח בלחיצה" מעצמו. בדיקה בפועל (דסקטופ, כל 4 השדות) הראתה שההנחה הזו שגויה: כמו בשדות date-only, לחיצה על datetime-local בדסקטופ פותחת את הפיקר רק אם היא נופלת בדיוק על אזור האייקון הפנימי של הדפדפן - לא בכל מקום בשדה המורחב ב-CSS. במובייל זה עבד כי שם הפיקר הטבעי נפתח כבר מ-focus, לא רק מהאייקון - אותה אסימטריה מובייל/דסקטופ שראינו בסבב הקודם על date-only
- **התיקון**: אותו מאזין `click` -> `showPicker()` שכבר קיים ל-`type="date"` הורחב גם ל-`datetime-local`, בשני הקבצים (`wizard.html`: tripStart/tripEnd, `index.html`: adminFlightInput/adminEndInput). הוסר התנאי `input.type === 'date'` - הקריאה יוצאת לשני הסוגים
- **החשש שנבדק ונמצא לא מוצדק**: בסבב הקודם נמנענו מלהחיל את זה גם על datetime-local מחשש להבהוב (סגירה-פתיחה) אם קוראים ל-`showPicker()` כשהפיקר כבר פתוח. נבדק ישירות: קריאות חוזרות ל-`showPicker()` על שדה עם פיקר פתוח לא גרמו ל-blur/focus מחדש ולא זרקו שגיאה - אותה התנהגות בדיוק כמו ב-date-only שכבר בפרודקשן. אין הבהוב בפועל
- **מגבלת בדיקה**: לא ניתן לראות חזותית את הפופאפ הנפתח בסביבת האוטומציה (חלונות פיקר טבעיים של הדפדפן לא נלכדים בצילום מסך, לא ב-date ולא ב-datetime-local) - האימות נעשה בעקיפין: `showPicker()` אכן נקרא בפועל בלחיצה אמיתית (patch על הפונקציה + מעקב אחרי הקריאות), בלי שגיאות קונסול חדשות, בכל 4 השדות ובשני סוגי הקלט
- נבדק: tripStart/tripEnd (wizard.html) ו-adminFlightInput/adminEndInput (index.html), דסקטופ, בטיול לחו"ל (datetime-local) ובטיול בארץ/קמפינג (date) - showPicker() נקרא כצפוי בשני המצבים, ללא רגרסיה בהתנהגות ה-date-only הקיימת

## v4.8.0 — הצטרפות לטיול רושמת אותו תחת users/{uid}/trips (שלב 1/2)

### ✨ שינוי סכמה
- **השורש**: `users/{uid}/trips/{tripId}` נכתב עד היום **רק** כשמישהו יוצר טיול חדש (wizard.html). הצטרפות לטיול קיים דרך קישור (saveProfile/claimMember בכל 4 העמודים) כתבה רק ל-`{tripRoot}/memberProfiles/{uid}` ו-`participantClaims` - בתוך הטיול, לא מתחת ל-uid של המצטרף. וידאנו: אין שום מנגנון אחר (לא בשרת, לא ב-localStorage) שמאפשר למשתתף רגיל לגלות מחדש אילו טיולים הוא חבר בהם. כללי Firebase לא מאפשרים סריקה גורפת של `trips/*` לחיפוש memberProfiles
- **התיקון**: הצטרפות לטיול (בכל 4 העמודים: packing/places/itinerary/shopping) כותבת עכשיו **גם** ל-`users/{uid}/trips/{tripId}` - אותה צורת נתיב וצורת ערך בדיוק כמו שה-wizard כבר כותב למארגן (`destination`,`tripType`,`language`,`startAt`,`endAt`,`createdAt`), כדי שמסך בחירת הטיול (שלב 2, נפרד) יוכל להציג טיולים של מארגן ומשתתף במאוחד בלי קוד מיוחד. כל השדות כבר טעונים ב-`trip`/`tripData`, אין קריאת Firebase נוספת
- **בכוונה לא אטומי**: הכתיבה ל-`memberProfiles` (עם ה-`.validate` המורכב שלה מול `participantClaims`) נשארה `set()` נפרד, **בדיוק כמו לפני השינוי**, ללא שום שינוי בהתנהגות שלה. הכתיבה ל-`users/{uid}/trips/{tripId}` היא קריאה נפרדת ל-`update()`, **אחרי** שההצטרפות כבר הצליחה במלואה - לא מבוטלת (`update()` רב-נתיבי) ביחד איתה. הסיבה: `update()` רב-נתיבי ב-Firebase הוא אטומי (כל-או-כלום) - אם הנתיב החדש, הפחות-מוכח, היה נכשל מול הכללים, זה היה מפיל גם את ה-`memberProfiles` שכבר עובד היום, רגרסיה גרועה יותר מהבאג שאנחנו מתקנים. אם הכתיבה השנייה נכשלת - `.catch()` שכותב ל-console בלבד, **לא** מוצג למשתמש: מבחינתו ההצטרפות הצליחה, וזו האמת
- **וידוא הרשאות**: אין קובץ rules בריפו (מנוהל רק ב-Firebase Console) - אושר ישירות מול הכלל בקונסולה: `"users": { "$uid": { ".write": "auth != null && auth.uid === $uid" } }` - אותה צורת נתיב בדיוק (`users/{auth.uid}/...`) שה-wizard כבר כותב בהצלחה בפרודקשן ליצירת טיול, מתיר את אותו דבר למשתתף
- **לא רטרואקטיבי בכוונה**: מי שכבר הצטרף לפני השינוי הזה לא ייכתב לו כלום - הכתיבה קורית רק בזרימת ההצטרפות עצמה. מילוי רטרואקטיבי (Cloud Function עם הרשאות אדמין) נשקל ונדחה כרגע - אין עדיין משתמשים אמיתיים מלבד המפתח
- נבדק: כל 4 העמודים נטענים בלי שגיאות קונסול חדשות (בדיקת syntax/scope בלבד - **לא** נבדקה הצלחת הכתיבה בפועל מול כללי Firebase האמיתיים, כי אין אינטרנט אמיתי בסביבת הבדיקה; מומלץ לוודא בהצטרפות אמיתית לפני בניית שלב 2 עליה)

## v4.7.3 — הסרת נפילת-לאחור גלובלית שהחזירה טיולים ישנים (index.html)

### 🐛 תיקון
- **השורש**: index.html שמר נתיב Firebase גלובלי ישן (`trip_settings`/`group_members`/`personal_locker` - שרד מלפני התמיכה בכמה טיולים), שהופעל בכל פעם ש-`tripId` חסר מהכתובת. הקריאה (`hydrateTripData`) הציגה את הנתונים משם **כאילו** היו הטיול הנוכחי, והכתיבה (בשמירת הגדרות ניהול / לוקר אישי / רשימת משתתפים) כתבה **לאותו נתיב גלובלי משותף לכל מי שנכנס בלי tripId** - לא קשור בכלל למערכת ה-trips/{tripId} החדשה. מחיקת טיול (`deleteRecentTrip` ב-wizard.html) אף פעם לא נגעה בנתיב הזה, כך שנתונים ישנים ששכבו שם יכלו "לצוץ מחדש" בכל כניסה בלי tripId - בלי שום קשר למה שנמחק או לא נמחק
- **איך מגיעים לשם בלי tripId**: כפתור הבית (⌂/"בית") בכל 6 העמודים הוא `href="index.html"` **גולמי בלי tripId** ב-HTML - ה-tripId מתווסף רק אחר-כך, א-סינכרונית, ברגע שנתוני הטיול נטענים (`configureLinks()`). לחיצה על הכפתור לפני שזה קרה (טעינה איטית, רגע אחרי חזרה מרקע) נוחתת בלי tripId. גם `manifest.json`'s `start_url` הוא `"index.html"` בלי tripId - פתיחה מאייקון מסך הבית (אם הותקן) נופלת לאותו מצב תמיד. **שני המסלולים האלה עדיין קיימים** - לא טופלו בסבב הזה, לפי בקשה מפורשת. עכשיו זה משנה פחות (אין יותר נתונים גלובליים לחשוף/לשבש), אבל עדיין חוויה שבורה - עמוד ריק/לא פונקציונלי בלי הודעה
- **התיקון**: הוסרו לחלוטין - בלוק ההאזנה (`db.ref("trip_settings").on(...)` וכו') וכל שלוש נקודות הכתיבה שנפלו לאחור לנתיבים הגלובליים. `saveTripCollection()`/שמירת הגדרות ניהול/לוקר אישי עכשיו פשוט **לא שומרות שום דבר** כש-`tripRoot` חסר, במקום לכתוב לנתיב משותף. אומת: המסלול הרגיל (עם tripId) לא נגוע כלל - יש populator נפרד ומתוזמן נכון ל-`groupMembers`/`personalLockerCards` דרך `trips/{tripId}/...` שלא נגעתי בו
- **הערה מהמשתמש**: שלושת הנתיבים הגלובליים האלה כבר חסומים בכללי Firebase מלפני יומיים - הקוד שהוסר ממילא לא יכול היה לעבוד, רק לייצר מצב שבור/תקוע
- נבדק: טעינה עם ובלי tripId - אין שגיאות קונסול חדשות; grep מוודא שלא נשארה שום קריאה ל-`trip_settings`/`group_members`/`personal_locker` כנתיב גולמי

## v4.7.2 — timeout על שער ה-auth (סעיף 1 מתוך התחקיר: אפליקציה נתקעת אחרי חוסר פעילות)

### 🐛 תיקון
- **הבעיה**: כל עמוד (מלבד wizard.html) חוסם את כל המסך מאחורי `.auth-loading` (spinner מלא-מסך) ומסיר אותו **רק** בתוך ה-callback הראשון של `onAuthStateChanged` - בלי שום timeout. אם ה-callback הזה נתקע (חשד: חיבור IndexedDB/WebSocket שנתקע ב-WebKit אחרי שהטאב הוקפא ברקע ב-iOS - ראו את דוח התחקיר), המשתמש נשאר תקוע על ה-spinner לנצח, בכל עמוד, בלי שום דרך החוצה חוץ מסגירת הדפדפן
- **התיקון**: קובץ JS משותף חדש (`auth-gate.js`, נטען אחרי version.js בכל 5 העמודים הרלוונטיים - **לא** wizard.html, שאין לו שער auth כזה). כל עמוד קורא ל-`window.armAuthGate(retryFn)` פעם אחת, ליד ה-listener של `onAuthStateChanged`. אם `.auth-loading` לא נעלם תוך 12 שניות, מוצגת שכבה (z-index מעל ה-spinner, לא נוגעת ב-class שלו) עם הודעה וכפתור "נסה שוב" - לא רענון דף
- **הכפתור מנסה מחדש באמת**: כל עמוד עבר ריפקטור קטן (ה-callback האנונימי של `onAuthStateChanged` הפך לפונקציה בשם `handleAuthStateChanged`), כך שהכפתור יכול לקרוא לה ישירות עם `auth.currentUser` (המצב הסינכרוני שכבר שמור בזיכרון ה-SDK) - עוקף אפשרי את הבדיקה האסינכרונית התקועה, בלי טעינה מחדש של כל הדף. אם `auth.currentUser` עדיין ריק, שום דבר לא נכפה - ה-listener המקורי (עדיין מחובר) ממשיך לחכות, וטיימר חדש מתחיל
- **התנהגות זהה כשהחיבור תקין**: `armAuthGate` בודק אם `.auth-loading` כבר מוסתר לפני שהוא בכלל מפעיל טיימר - במקרה הרגיל (resolve מהיר) הוא לא עושה כלום. `MutationObserver` על ה-class של `.auth-loading` הוא מה שמזהה הצלחה בכל מצב - גם דרך הכפתור וגם אם ה-listener המקורי נפתר לבד תוך כדי שההודעה מוצגת - ומנקה הכל אוטומטית
- **באג אמיתי שנתפס תוך כדי בדיקה עצמית**: בגרסה ראשונה, הצגת ההודעה כללה הסתרת `.auth-loading` בעצמה (כדי "להחליף" את ה-spinner) - וזה גרם ל-MutationObserver לפרש את זה כאילו ה-listener האמיתי הצליח, ומיד הסתיר את ההודעה שהוא בעצמו הרגע הציג. תוקן: ההודעה יושבת מעל ה-spinner (z-index) בלי לגעת ב-class שלו בכלל - כך שה-observer לעולם לא מבלבל בין "אנחנו הסתרנו" ל"הדף האמיתי הסתיר"
- **מה לא נכלל בסעיף הזה** (בכוונה, לפי בקשה): מאזין `.info/connected`, `goOffline`/`goOnline` בחזרה מרקע, timeout על קריאות get/update בודדות - אלה שלבים נפרדים לאחר אימות שהתיקון הזה עובד
- נבדק: סימולציה מלאה של תקיעה (spinner מוסתר אחרי timeout, הודעה מופיעה, כפתור מנסה שוב, מצב שבו הפתרון קורה עצמאי תוך כדי שההודעה מוצגת) על packing.html במלואו; שאר 4 העמודים נבדקו לפרסור/קונסול תקין וללא רגרסיה בזרימה הרגילה. tr(he/en), בהיר/כהה, RTL/LTR

## v4.7.1 — הידוק המרווחים באזור העליון של עמוד הציוד (packing.html)

### 🐛 תיקון
- **שני מרווחים אנכיים גדולים מדי בין שורות** (לא גובה השורות, לא מספרן): בין שורת המשתמש לטאבים, ובין הטאבים לבורר הרשימה. השורש - כל השורות הן ילדי flex ישירים של `.shell` (`display:flex;flex-direction:column`), ומרווחי flex-items **לא מתמזגים** (margin collapse) כמו קופסאות block רגילות - כך ש`margin-bottom` של שורה אחת ו-`margin-top` של הבאה **מצטברים** (14+16=30px, 12+16=28px) במקום להתמזג לערך המקסימלי ביניהם
- **הייחוס** (shopping.html, לפי בקשה): שם המרווחים בין שורות דומות הם רק 6px - כי שם רק הצד ה**קודם** של כל שורה מגדיר `margin-bottom`, והבא לא מגדיר `margin-top` בכלל, כך שאין הצטברות. יושר `packing.html` לאותה קונבנציה: הוסר ה-`margin-top` מ-`.tabs` ומ-`.list-head`, וצומצמו הערכים הנותרים
- נמדד (מכשיר וירטואלי, לא הערכה): מרווח 1 (משתמש→טאבים) 30px→8px, מרווח 2 (טאבים→בורר רשימה) 28px→8px. גובה האזור העליון הכולל מעל הרשימה: 296px→254px נוסף (בנוסף לצמצום הקודם מ-318px)
- נבדק: מספר השורות וגובהן לא השתנו; פעולות (☑️⚙️➕, בורר רשימה) נגישות ולוחצות; בהיר/כהה; RTL/LTR; shopping.html - ללא שינוי בפועל

## v4.7.0 — הידוק האזור העליון ברשימת קניות (shopping.html)

### ✨ צפיפות
- **איחוד סטטוס+שם לשורה אחת**: "סטטוס רשימה: בהכנה" ו"שם: יניב" עברו משתי שורות נפרדות (עם סרגל ההתקדמות ביניהן) לשורה אחת - סטטוס בצד אחד, שם בצד השני. תווית הסטטוס קוצרה ל"סטטוס:" (מ"סטטוס רשימה:") כדי להתאים לשורה הצפופה. שם ארוך נחתך בשלוש נקודות (`overflow:hidden;text-overflow:ellipsis` על ה-select) ולעולם לא דוחק את הסטטוס החוצה - הסטטוס קיבל `flex:0 0 auto` (מוגן, לא נחתך), השם `flex:1 1 auto;min-width:0` (סופג את הלחץ). מצב ה-highlight (מסגרת אדומה כשלא נבחר שם) נשאר מוגבל לתיבת השם בלבד, לא לכל השורה
- **הידוק ערכים בהשוואה ל-packing.html**: `.sticky-header-container` padding-bottom 8px→6px, margin-bottom 10px→8px; `header` margin-bottom 8px→6px; `.progress-section` padding 11px 14px→9px 12px, margin-bottom 12px→10px; `.whoami` padding 8px 12px→7px 10px; `.main-actions-row` margin-bottom 12px→10px
- נמדד (מסך מובייל 812px): האזור מעל הרשימה 379px→309px (46.7%→38%), לעומת packing.html 296px (36.5%) - קרוב משמעותית, לא זהה: shopping עדיין כולל שורת main-actions-row (הוסף פריט/הסתר שהושלמו/עוד) שאין ל-packing כשורה נפרדת, ולא הוסרו שורות מעבר לאיחוד המבוקש
- נבדק: שם ארוך (40+ תווים) לא דוחק את הסטטוס ולא חורג מרוחב המסך; מצב highlight; בהיר/כהה; RTL/LTR; פעולות קיימות (בורר סטטוס לחיץ, שדה שם ניתן לעריכה) נשארו נגישות

## v4.6.1 — תיקון חלקי 1/N: קטימת ראש העמוד חוזרת ב-wizard.html

### 🐛 תיקון באג (בבדיקה - סבב ראשון מתוך כמה)
- **הבאג חזר אחרי תיקון קודם (fd0a9da)**: אותו סבב תיקן רק את `.wizard-container` (הוחלף `calc(100dvh-58px)` ב-`flex:1`), אבל `body` עצמו נשאר עם `min-height:100dvh` בלי נגיעה - אותה מחלה בדיוק, אלמנט אחר. אומת: זה לא קשור לשינויי הצפיפות של אתמול/היום (הושוותה הצהרת ה-body ב-git diff מול `eb5b6d7` - זהה לחלוטין), והתבנית קדמה לסבב הזה בכמה ימים
- **סקר renderMine בכל 6 העמודים**: אותה תבנית בדיוק (body ו/או מכולה ראשית נעולים ישירות ל-`100dvh`, בלי `overflow:hidden`) חוזרת גם ב-index.html (`body`+`.app-container`) וגם ב-packing/places/itinerary/shopping (`body`+`.shell`, עם `overflow:hidden`). זה תיקון **חלקי, ראשון מתוך כמה** - wizard.html בלבד בסבב הזה, שאר העמודים בהמתנה לאישור אחרי בדיקה במכשיר אמיתי
- **התיקון**: משתנה CSS מחושב ב-JS (`--vh`, מ-`window.innerHeight`, מתעדכן ב-resize/orientationchange בלבד - לא בגלילה) במקום `100dvh` ב-`body`. זו הטכניקה הסטנדרטית נגד הבאג המתועד ב-WebKit שבו הערך המחושב של dvh נתקע "ישן" אחרי מחזור הצגה/הסתרה של סרגל הכתובת שמופעל מגלילה

### ⚠️ באג פתוח - לא פתור
- **התיקון לעיל לא פתר את הבאג בפועל במכשיר אמיתי**. נוסף badge אבחון חי מוסתר מאחורי `?vhdebug=1` (ב-wizard.html, לא הוסר) שמראה innerHeight/visualViewport.height/offsetTop/גובה body/גובה `.wizard-container` בזמן אמת - כדי לתפוס את המספרים ברגע שהבאג קורה, במקום לנחש תיקון נוסף. **הבאג לא ניתן לשחזור בעקביות** - ממתין לצילום מסך של ה-badge ברגע שהתוכן נחתך בפועל לפני שממשיכים
- היקף: וידאנו (v4.6.1) שהתבנית הבעייתית (body/מכולה ראשית נעולים ל-`100dvh`) קיימת בכל 6 העמודים, לא רק wizard - אבל בפועל, נכון לרגע זה, wizard.html הוא היחיד שבו הבאג נצפה. שאר העמודים לא נגעו בהם

## v4.6.0 — כיווץ האזור העליון בעמוד הציוד (packing.html)

### ✨ צפיפות
- **שורת בורר הרשימה + סרגל ההתקדמות אוחדו לשורה אחת**: הכותרת הסטטית "הרשימה שלי" הוסרה (מיותרת - הטאב "הציוד שלי" ממש מעליה כבר אומר את זה), ובמקומה בקצה השני של אותה שורה יושב המונה "1/8 · 13%". סרגל ההתקדמות עצמו נשאר כפס דק תחת השורה. תווית "התקדמות" הוסרה מהתצוגה (מוסקת מההקשר - בצמוד לפס התקדמות) אך נשמרה כ-aria-label דינמי לנגישות
- **תגית שם המשתמש הפסיקה לחזור על השם**: הייתה מציגה "דניאל · Daniel Cohen" כששם המשתתף כבר מוצג (נעול) בשדה הבחירה ממש לידה - השם הכפול הוסר, נשאר רק חשבון ה-Google המקושר ("Daniel Cohen") שהוא המידע החדש היחיד שהתגית נושאת
- **באג חפיפה שהתגלה תוך כדי**: שורת הבורר-המאוחדת חרגה מרוחב המסך ב-LTR (חרגה גם לפני השינוי, בכיוון ההפוך ב-RTL - לא נגרם על ידי השינוי הזה אבל תוקן באותה הזדמנות כי נגעתי באותה שורה) - `.list-select` לא היה `min-width:0`, כך שברירת המחדל שלו לא התכווצה מתחת לתוכן, ודחפה את שאר השורה החוצה. תוקן עם `flex:1 1 auto;min-width:0` + `text-overflow:ellipsis` על ה-select, כך שהוא זה שנחתך (עם שלוש נקודות) כשיש הרבה רשימות/שם רשימה ארוך, לא הכפתורים ולא המונה
- נמדד: גובה האזור מעל הרשימה 318px→296px (39%→36% ממסך מובייל בגובה 812px) - שיפור בעקבות איחוד השורות בלבד; המרחקים בין השורות (identity-row/tabs) לא נגעו בהם
- נבדק: תפריט ⚙️/☑️/➕ נגישים ולוחצים, בהיר/כהה, RTL/LTR, שם רשימה ארוך לא שובר את השורה

## v4.5.0 — כיווץ שורת ציוד ומקומות/מוזיקה למבנה שתי-שורות (כמו כרטיס הפעילות)

### ✨ צפיפות (packing.html item, places.html שני הטאבים)
- **packing.html**: שם הפריט ו"פרטי"/"משותף" עברו משתי שורות נפרדות לשורה אחת - שם בולט (נחתך ראשון אם ארוך מאוד, עם רצפת 30% רוחב), מטא-דאטה עמומה קטנה נשארת שלמה ברוב המקרים (נחתכת רק כשהשילוב ארוך במיוחד). "גלוי לקבוצה" קוצר ל"משותף" (מ-11 לכ-5 תווים) - הביטוי הישן נגרם לחיתוך מיותר גם בשמות פריט קצרים. גם כפתורי הפעולה (▸/🔒/•••) קיבלו bleed בשוליים שליליים כדי שלא הם יכתיבו את גובה השורה. נמדד: 60px→50px לפריט רגיל (ירידה ~17%), 7→8 פריטים במסך מובייל
- **places.html (מקומות + מוזיקה, אותו CSS לשני הטאבים)**: שם/הערה/תגית (מקומות) או שם/מוסיף/תגית (מוזיקה) עברו משלוש שורות נפרדות לשתיים - שורה 1 שם בלבד (נחתך אם ארוך), שורה 2 מידע משני בשורה אחת (הערה או שם-מוסיף · תגית). התגית לעולם לא נחתכת (`flex:0 0 auto`); שם-המוסיף (מוזיקה) מוגן ברצפת 50% רוחב - נחתך רק במקרי-קיצון, לא נבלע לגמרי ולא דוחק את התגית מחוץ לכרטיס (בדוק עם שם בדוי בן 48 תווים). כפתור "ניווט" הפך מכפתור-טקסט בשורה נפרדת (52px+) לאייקון מצפן 🧭 קבוע 40×40 ליד ⋮. תוקן גם `.cards{grid-template-columns:repeat(2,1fr)}`→`minmax(0,1fr)` בדסקטופ, כדי שכרטיס אחד לא ימתח את האחרים. נמדד: כרטיס מקום עם הערה+ניווט 173px→76px, כרטיס מוזיקה (שם ארוך) 123px→76px - במסך מובייל: מקומות 2→5-7 פריטים, מוזיקה 3-4→5-6 פריטים
- נבדק: תגית/שם-מוסיף במגוון אורכים כולל קיצון, בהיר/כהה, RTL/LTR, דסקטופ שתי-עמודות, פעולות קיימות (עריכה/מחיקה/הוספה ליום) נשארו נגישות

## v4.4.3 — באג: תת-רשימת ציוד נבלעת מאחורי הפריט הבא (packing.html)

### 🐛 תיקון באג
- **התת-רשימה נחתכת ונעלמת חלקית מאחורי הפריטים שבאים אחריה**: השורש - `.items` (מיכל הרשימה, `display:grid`) הסתמך על ברירת המחדל `grid-auto-rows:auto`. כשפריט התרחב (`.sub-panel` נוסף כילד עם `grid-column:1/-1`), שורת ה-grid החיצונית של אותו פריט לא גדלה בהתאם לתוכן הפנימי שלו - נמדד בפועל: גובה הפריט נשאר נעול על 58px (ה-min-height) גם כשהתת-רשימה בפנים דרשה כ-109px, כך שהתת-רשימה חרגה מחוץ לתיבת הפריט וטואטה מתחת לפריט הבא (שם רקע אטום צייר מעליה). התיקון: `grid-auto-rows:max-content` על `.items` - כל שורה מתכווצת/מתרחבת לפי התוכן בפועל שלה, כולל תת-רשימה שנפתחת
- **חץ ההרחבה כמעט בלתי-נראה**: ה-▸/▾ עצמו היה בפונט 0.72rem בצבע מעומעם (`--muted`) - כמעט לא ניתן להבחין בו ליד סמל המנעול וכפתור ה-•••, למרות שאזור הלחיצה עצמו כבר היה 40×40px (תקין). הוגדל ל-1.05rem, מודגש, בצבע `--text`. נוסף מצב "פתוח" ברור: רקע מגוון בצבע הטיול (`color-mix`) כשהתת-רשימה מורחבת, כדי שהמצב פתוח/סגור יהיה חד-משמעי ויזואלית
- נבדק: תת-רשימה עם פריט אחד ועם כמה; הרחבת הפריט האחרון ברשימה (לא נחתך מאחורי ה-FAB); כמה תתי-רשימות פתוחות בו-זמנית; מצב בחירה מרובה (☑️) יחד עם הרחבה - אין התנגשות; בהיר/כהה, RTL/LTR

## v4.4.2 — כיווץ נוסף של כרטיס הפעילות (מדוד: 91px→75px)

### ✨/🐛 itinerary.html
- **הצ׳יפ/כפתור התפריט הכתיבו את גובה שורה 1**: `.action-menu-btn` המשותף (shared.css) גובה 40px, ו-`.join-chip` היה 38px - הרבה יותר מגובה הטקסט (שעה+כותרת, כ-22px). שורה 1 נמדדה בפועל ב-40px במקום ~22px. הפתרון: `margin-top/bottom:-8px` על `.activity-row1-end` - הצ׳יפ ותפריט 3-הנקודות ממשיכים להיראות בגודלם המלא (36-40px, נוח ללחיצה), בלי לנפח את גובה השורה שסביבם. גם `.join-chip` צומצם קלות (38px→36px, פונט .78rem→.74rem)
- **רווח פנימי מיותר**: היה תופעת לוואי ישירה של הבעיה הקודמת - עם שורה 1 בגובה 40px אך הטקסט בה רק ~22px, נשאר שטח ריק בתוך השורה מתחת לטקסט, לפני ה-gap המפורש (4px) לשורה 2. לא נדרש שינוי נפרד - נעלם עם התיקון הראשון
- **באג: שם היוצר נעלם בהערה ארוכה** - שורה 2 הייתה טקסט משורשר אחד (`הערה · שם`) עם חיתוך יחיד, כך שחיתוך ה-ellipsis יכול היה לבלוע את השם. פוצל לשני span נפרדים: `.activity-note` (`flex:0 1 auto`, מתכווץ ונחתך) ו-`.activity-owner-name` (`flex:0 0 auto`, קבוע, לעולם לא נחתך) - השם תמיד גלוי, צמוד ישירות אחרי סוף ההערה (בין השניים 0px רווח, נמדד)
- מדידה בפועל (כרטיס עם הערה קצרה): גובה כרטיס 91px→75px, שורה 1 40px→24px, גובה צ׳יפ 38px→36px
- הרחבה/קיפול בלחיצה על שורה 2 ממשיכה לעבוד עם המבנה החדש - במצב מורחב `.activity-note` עובר ל-`white-space:normal`, השם ממשיך לזרום מיד אחריו

### 🐛 תיקוני באגים (itinerary.html)
- כרטיס עם הערה ארוכה נדחק מחוץ לרוחב המסך, והצ׳יפ נחתך יחד איתו. השורש: `.cards` הוא `display:grid`, ופריט grid (`.activity-card`) מקבל כברירת מחדל `min-width:auto` - כלומר לא יכול להתכווץ מתחת לרוחב התוכן הפנימי שלו. טקסט `white-space:nowrap` בשורה 2 דחף את הכרטיס לרוחב תוכנו המלא, וה-grid track נאלץ לגדול בהתאם - ומתח איתו **כל** כרטיס אחר באותה עמודה, כולל כרטיס קצר לגמרי ("לילה טוב")
- התיקון: `min-width:0` על `.activity-card` (ועל `.activity-row1`/`.activity-row2`), ו-`minmax(0,1fr)` במקום `1fr` בעמודות ה-grid בדסקטופ - כך שפריט grid יכול להתכווץ לרוחב המיכל בפועל, וטקסט ה-nowrap+ellipsis בשורה 2 נחתך במקום לדחוף את הכרטיס. הרווח הגדול בין השורות היה תופעת לוואי של אותו overflow ונעלם עם התיקון
- **הרחבה/קיפול הערה בלחיצה**: זו הייתה חסרה לגמרי בפועל - שורה 2 הייתה CSS-truncation סטטי בלבד, בלי שום מנגנון להרחבה. נוסף עכשיו: לחיצה על שורה 2 מוסיפה/מסירה מחלקת `expanded` שמחליפה `white-space:nowrap` ל-`normal` (עטיפה טבעית לכמה שורות שצריך), לחיצה נוספת מחזירה לשורה אחת חתוכה. מצב ההרחבה מתאפס ברינדור מחדש של הרשימה, כמו שאר מצבי ה-UI הזמניים בעמוד הזה (למשל תפריט 3-הנקודות)

### ✨ כרטיס פעילות בשתי שורות במקום שש (itinerary.html)
- כרטיס פעילות תפס שש שורות (שעה, כותרת, הערה, "נוסף ע״י", מונה מגיעים נפרד, וכפתור הצטרפות נפרד) - נכנסו רק 2-3 כרטיסים למסך. עכשיו שתי שורות: שורה 1 - שעה + כותרת, ובקצה הנגדי צ׳יפ ההצטרפות + תפריט 3 הנקודות; שורה 2 - הערה · שם היוצר (בלי מפריד כשאין הערה)
- **צ׳יפ אחד מחליף שני אלמנטים**: המונה (עם ערימת האווטארים) וכפתור "אני בא/ה" הנפרד אוחדו לכפתור-צ׳יפ יחיד. לא מסומן: "אני בא/ה" (+ "· N" אם כבר יש אחרים, בלי מספר כשאין). מסומן: "N מגיעים ✓" ברקע צבע הטיול. לחיצה מחליפה מצב כמו קודם
- כותרת ארוכה גולשת לשורה נוספת בלי לדחוף את הצ׳יפ החוצה (`flex:0 0 auto` על קבוצת הצ׳יפ+תפריט); הערה ארוכה נחתכת בשלוש נקודות בשורה 2 כדי לשמור על הגובה הקומפקטי
- "קורה עכשיו" ותפריט 3 הנקודות לא נגעו בהם

### 🐛 תיקוני באגים
- **itinerary.html**: הכותרת וטאבי הימים נגללו יחד עם רשימת הפעילויות (בניגוד ל-packing/places/shopping) - כי הקובץ מעולם לא אימץ את תבנית "מעטפת קבועה + גלילה פנימית" שלהם, אלא גלל את כל המסמך. עכשיו `.shell` הוא `height:100dvh;overflow:hidden`, `.page-head`/`.user-chip`/`.day-tabs` קבועים (`flex-shrink:0`), ורק רשימת הפעילויות (`.cards`) גוללת (`flex:1;min-height:0;overflow-y:auto`, עם `align-content:start`). החלפת יום בטאבים מאפסת עכשיו את גלילת הרשימה לראש, במקום להשאיר אותה באמצע מהיום הקודם
- **חפיפת FAB**: בכל עמוד עם FAB (packing.html, places.html, itinerary.html) הפריט האחרון ברשימה חפף לכפתור הצף - הריפוד התחתון (86px) לא כיסה את מלוא גובה ה-FAB (148px). הועלה ל-`calc(160px + env(safe-area-inset-bottom))` בשלושתם

### ✨ הבהרה למצב בחירה מרובה (packing.html)
- לחיצה על כפתור ה-☑️ הראתה checkbox נוסף ליד כל פריט בלי שום הסבר - ה-`title`/`aria-label` הקיימים לא מוצגים במגע (אין hover במובייל). נוסף טוסט קצר בכניסה למצב: "מצב בחירה מרובה: סמנו פריטים ובחרו פעולה למטה"

## v4.3.2 — ניסיון תיקון ממוקד: לא ניתן לגלול חזרה לראש הוויזרד

### 🐛 תיקון (ממוקד, טרם מאושר במכשיר אמיתי)
- **wizard.html**: אחרי גלילה למטה וחזרה למעלה ב-iOS Safari, כותרת הוויזרד נחתכת מאחורי שורת הכתובת של כרום והעמוד מסרב לגלול עוד למעלה. נבדק ונשלל: `justify-content:center` על ה-body לא הבעיה - כבר קיים override ל-`flex-start` במובייל
- הבדל מבני אמיתי מול שאר העמודים: בעמודים שעובדים (packing/places/itinerary) שום דבר שמשתתף בחישוב גובה אזור הגלילה בפועל לא תלוי ב-dvh - הגלילה קורית באלמנט פנימי נפרד עם `flex:1;min-height:0` בלבד. בוויזרד אין אלמנט גלילה פנימי - הגלילה היא גלילת המסמך (body), ול-`.wizard-container` (הילד שקובע כמה body גבוה) היה `min-height:calc(100dvh - 58px)` - כלומר גם מסגרת הגלילה וגם התוכן שבתוכה תלויים ב-dvh בו-זמנית. זו קטגוריית באג מתועדת ב-WebKit (scrollHeight לא תמיד מתעדכן נכון אחרי שתוכן הנגזר מ-dvh גדל וחוזר ומתכווץ)
- התיקון: הוחלף `min-height:calc(100dvh - 58px)` ב-`flex:1` - הכרטיס ממלא את שארית המסך מתחת ל-top-bar דרך flexbox רגיל, בלי לגזור שום דבר מ-dvh מלבד ה-body הבסיסי (משותף לכל העמודים). תוקן רק במדיה קוורי מובייל - דסקטופ לא נגע
- לא ניתן לשחזר את הבאג המקורי (תלוי ב-toolbar דינמי אמיתי של iOS Safari) ולכן גם לא לאמת ישירות שהתיקון סוגר אותו - נדרש אימות ידני במכשיר. אם זה לא מספיק, תוכנית ב' היא מעבר למבנה גלילה פנימית כמו packing.html (משימה נפרדת)

## v4.3.1 — תפריט תחתון שצף גבוה מדי במובייל עם safe-area

### 🐛 תיקוני באגים
- **index.html / shopping.html**: `.bottom-nav` חישב את `env(safe-area-inset-bottom)` פעמיים - גם כריפוד פנימי וגם דרך היסט קבוע (`bottom:8px`) שלא כלל אותו. במכשיר עם פס תחתון (iPhone עם Face ID) זה הצטבר לכ-51px של שטח מת מתחת לאייקונים, והרצועה נראתה צפה גבוה מדי. שאר שלושת העמודים (itinerary/places/packing) לא נפגעו - הם משתמשים ב-`.bottom-nav` המשותף מ-shared.css שלא סבל מהבעיה. התיקון: ה-safe-area מחושב עכשיו רק במיקום (`bottom`), לא גם בריפוד
- **index.html**: ל-`.bottom-sheet` (המודל של פאנל האדמין ועוד) היה `max-height:85vh` בלי fallback ל-`85dvh`, בניגוד לכל שאר השימושים ב-vh באפליקציה

## v4.3.0 — שעון היעד, שדה תאריך חכם לפי סוג טיול, ובדיקת גרסה אוטומטית

### 🌍 אזור הזמן של היעד
- טיולים לחו״ל ומקומיים שומרים את אזור הזמן (IANA) של היעד בזמן היצירה, כולל עדכון כששינוי היעד בפאנל האדמין מגיאוקד מחדש
- `endAt` (מועד סיום/חזרה) מתפרש עכשיו לפי שעון היעד ולא שעון המכשיר של המארגן. `startAt` (המראה מישראל) נשאר במכוון בשעון המקומי של המארגן - זו לא אותה סמנטיקה, בכוונה
- כל חישוב "עכשיו"/"היום" באפליקציה (באדג׳ "מתרחש עכשיו" וטאבי הימים ב-itinerary, הבאנר החכם ורצועת מזג האוויר ב-index) מחושב לפי שעון היעד ולא שעון הצופה - חבר בישראל שמסתכל על טיול ברומא או בבנגקוק רואה עכשיו את השלב והיום הנכונים
- טיולים ללא אזור זמן שמור (reserve, טיולים ישנים מלפני התכונה) ממשיכים ליפול חזרה לשעון המכשיר בדיוק כמו קודם - אין מיגרציה

### ☁️ רצועת תחזית מזג אוויר ב-index
- נוספה רצועת תחזית לאורך ימי הטיול (Open-Meteo, ללא מפתח או ספרייה חיצונית), עם סימון היום הנוכחי וטשטוש הדרגתי לימים הרחוקים יותר
- כווצה בהמשך לכ-50% מהגובה המקורי לפי סעיף הצפיפות הוויזואלית

### 🕐 שדה שעה רק בטיולים לחו״ל
- טיולים מסוג local/camping/reserve מציגים שדה תאריך בלבד (`type="date"`) בוויזרד ובפאנל האדמין - שעת המראה רלוונטית רק לחו״ל
- `startAt`/`endAt` נשמרים כתחילת/סוף היום (00:00/23:59) כשאין שדה שעה, כך שהיום הראשון והאחרון בטיול נכללים במלואם
- החלפת סוג טיול באמצע מילוי הטופס משמרת את התאריך שכבר הוזן, בשני הכיוונים
- תוקנה רגרסיה שנחשפה בעקבות זה: שדה מסוג `date` פתח את הבורר הילידי רק בקליק מדויק על אזור האייקון שלו, לא בכל שטח השדה (בניגוד ל-datetime-local) - נפתר עם קריאה מפורשת ל-`showPicker()` בקליק על שדות תאריך בלבד

### 🔄 בדיקת גרסה אוטומטית
- GitHub Pages שומר כל קובץ (כולל HTML) במטמון עד 10 דקות ואין שליטה על כותרות ה-caching מהריפו - טאב פתוח לאורך זמן, או PWA שחוזר מהרקע, לא מרעננים שום דבר מעצמם ועלולים לשבת על קוד ישן בלי שום סימן
- בחזרה לטאב (`visibilitychange`) - לא polling תקופתי - version.js נבדק מחדש מול השרת ללא מטמון; אם הגרסה שונה מהטעונה, מוצגת רצועה קטנה וניתנת לסגירה בראש העמוד, בכל 6 העמודים (קוד יחיד ב-version.js, בלי לגעת בעמודים בנפרד)

### 🐛 תיקוני באגים
- **index.html**: `direction:rtl` קשיח שהיה מוגדר על ה-body דרס את מצב אנגלית (LTR) - הוסר
- **wizard.html**: לחיצה על "טיול חדש" ממסך ההצלחה בלי רענון עמוד השאירה מצב ישן מהטיול הקודם (באדג׳ יעד קודם, `selectedVibes` לא מנוקה, כפתור "הבא" תקוע במצב `loading`) - `resetWizard()` מנקה עכשיו הכל
- הוסרה קריאת `navigator.serviceWorker.register("sw.js")` מ-index.html - הקובץ לא קיים בריפו, הקריאה נכשלה בשקט בכל טעינה. service worker אמיתי, אם וכשיתווסף, יתווסף בכוונה עם אסטרטגיית invalidation מתוכננת

## v4.2.0 — תפריטי 3-נקודות בכל מקום, תיקוני מובייל, צבעי טיול מעודכנים

מספר הגרסה לא עודכן ברצף בזמן שהעבודות הבאות בוצעו - הרשומה הזו מרכזת את כולן יחד בדיעבד.

### 🎵 מוזיקה: תמיכה ב-SoundCloud + כרטיסים קומפקטיים
- נוסף זיהוי קישורי SoundCloud (`musicSourceFor`/`detectMusicTitle`/`musicSourceMeta`) דרך ה-oembed הרשמי שלהם
- כרטיסי מוזיקה כווצו: שם השיר עצמו הוא הקישור הלחיץ, כפתור "פתח" נפרד הוסר, עריכה/מחיקה עברו לתפריט 3-נקודות

### 📱 תיקון באג מובייל אמיתי ב-wizard.html
- מדיה קוורי במובייל הסתירה לגמרי את כפתורי "פתח"/"העתק קישור" בכרטיסי "הטיולים שלי" - נשאר רק מחיקה. הכפתורים צומצמו לאייקונים במקום שיוסתרו, כדי שהפונקציונליות תישאר נגישה

### 🃏 תפריטי 3-נקודות בכרטיסי מקום, פעילות ותגובה
- places.html: כרטיסי מקום - "ניווט" נשאר גלוי, "הוסף ליום"/עריכה/מחיקה עברו לתפריט
- itinerary.html: כרטיסי פעילות - "אני בא/ה" נשאר גלוי, עריכה/מחיקה עברו לתפריט
- places.html (טאב תמונות): תגובות על תמונה - עריכה/מחיקה עברו לתפריט 3-נקודות

### 📏 צמצום צפיפות ב-places.html (לפי מדידות getComputedStyle בפועל)
- רווח פנימי, גובה כפתורים וגדלי גופן צומצמו במספר מקומות בשלושת הטאבים (מקומות/תמונות/מוזיקה) - הכל אומת מול יעדי נגישות סבירים למובייל (כפתורים לא מעל 44-48px, ריפוד פנימי 12-16px)
- כפתורי "•••" ברשימת הציוד (packing.html) הוגדלו מ-38×38px ל-40×40px

### 🐛 תפריט התגובה שנפתח "מוסתר" + מקלדת שמכסה את שדה התגובה
- שורש הבעיה: תפריט ה-3-נקודות של תגובה היה `position:absolute` בתוך רשימת תגובות שגוללת (`overflow:auto`), ומעל טור המסך שגם הוא גולל - כשהתגובה קרובה לתחתית הרשימה, התפריט נחתך ונעלם
- התיקון: התפריט ממוקם עכשיו לפי `position:fixed` עם קואורדינטות שמחושבות מהכפתור עצמו בזמן פתיחה, נצמד לגבולות המסך, ונפתח כלפי מעלה כשאין מקום למטה. נסגר גם בגלילה
- בנוסף: פתיחת "תגובה" הייתה מפעילה `focus()` פרוגרמטית שפותחת את המקלדת ב-iOS Safari בלי לגלול את השדה לתצוגה - נוסף גלילה מפורשת לשדה כשהמקלדת נפתחת בפועל

### 🎨 עדכון צבעי קמפינג ומילואים
- קמפינג: מירוק-מנטה (#6EE7A0/#4F772D) לירוק "אמיתי" יותר (#52B788/#2C7755)
- מילואים: מכחול-פלדה (#7FB3E8/#55637A) לסגול (#A78BFA/#6C57A7)
- חו״ל ומקומי נשארו ללא שינוי. גרסת מצב בהיר נגזרה מגרסת מצב כהה לפי אותו יחס OKLCH שקיים בין הגרסאות הבהירות/כהות של חו״ל ומקומי, ואומתה ניגודיות WCAG AA

### 🤖 subagent חדש: ux-reviewer
- נוסף `.claude/agents/ux-reviewer.md` - סוכן ביקורת UX שרק בודק ומדווח, לא מתקן קוד בעצמו

## v4.1.0 — עוד סבב הבהוב שם + מספור גרסה אחיד

### 🐛 "הטיול שלנו" עדיין הבהב - נמצא ותוקן
- הבאג האמיתי: ב-places / packing / itinerary יש טריק ותיק למניעת הבהוב כיוון (RTL/LTR) - קריאה מוקדמת ל-`applyLanguage()` עם השפה השמורה, עוד לפני שפיירבייס ענה. הבעיה: `applyLanguage()` היא פונקציית "צייר הכל מחדש" מונוליטית שגם כותבת את שם הטיול - ועם `trip.destination` עדיין ריק באותו רגע, היא כתבה את "הטיול שלנו" **מעל** השם שכבר צויר מהמטמון. אותו דפוס בדיוק כמו הבאג הקודם ב-index (`applyTripPresentation`) - קריאה מוקדמת שדורסת ערך אחר שכבר תקין
- התיקון: דגל `tripDataLoaded`, בדיוק כמו ב-index - הקריאה המוקדמת כבר לא נוגעת בשם הטיול עד שהגיעה רשומת טיול אמיתית
- אומת בדפדפן אמיתי: שוחזר הבאג במדויק (מטמון עם שם ושפה, טעינה חדשה → "הטיול שלנו"), ואומת שהתיקון סוגר אותו - בשלושת העמודים

### 🔍 סריקה שיטתית לבאגים מאותה משפחה
נבדקו כל 6 העמודים בכמה זוויות:
- **ID כפולים** - אין
- **תקינות תחביר JS** בכל בלוק `<script>` (כולל מודולים) - הכל תקין
- **איזון סוגריים CSS** בכל עמוד + shared.css - תקין
- **מחלקות CSS שה-JS מפעיל אך לא מוגדרות בשום מקום** (בדיוק הדפוס שגרם לבאג של כפתור ההגדרות) - נמצא אחד: **מפת ההושבה במטוס** (`.seat`) הוסיפה מחלקת `reveal` בהדרגה (30ms על כל מושב) ליצירת אפקט "קופצים פנימה" - אבל לא הייתה שום הגדרת CSS ל-`.reveal`, אז זו הייתה קריאה סמויה לגמרי. נוסף מצב התחלתי שקוף+מוקטן ומעבר חלק, בכיבוד להגדרת "הפחת תנועה"
- **קריאות ל-ID שלא קיים** - אין

### 🔢 מספר גרסה אחיד לכל האפליקציה
- עד עכשיו מספר הגרסה היה כתוב ידנית בכל עמוד בנפרד: תגית `<!-- GitTrip vX.X.X -->` ב-4 עמודים (שנשארה תקועה על v4.0.0), טקסט גלוי בפוטר ב-2 עמודים אחרים (גם תקוע על v4.0.0) - בזמן ש-CHANGELOG.md כבר היה ב-v4.0.3. בדיוק אותה משפחת באגים: דבר שאמור להתעדכן בכמה מקומות, ומתישהו מפסיק
- נוצר `version.js` - מקור אמת יחיד: `window.APP_VERSION`. כל 6 העמודים טוענים אותו, ושני העמודים שמציגים גרסה בפוטר (index, shopping) קוראים ממנו בזמן ריצה במקום מחרוזת קשיחה
- מכאן והלאה: לעדכן מספר אחד ב-`version.js`, ולהוסיף רשומה תואמת ב-CHANGELOG.md - שני המקומות היחידים שצריך לגעת בהם

### 🤖 subagent חדש: coder
- נוסף `.claude/agents/coder.md` - subagent ייעודי לפיתוח על GitTrip, עם העובדות הקריטיות על הפרויקט (6 עמודים ללא build, shared.css, Firebase SDK ישן מול מודולרי, tr() לתרגום, דיאלוגים מותאמים, מצב כהה) ורשימת בדיקות לפני שמסמנים משימה כגמורה

---

## v4.0.3 — סוף להבהוב שם הטיול במעבר בין עמודים

### ⚡ שם הטיול נצבע מיד, בלי "הטיול שלנו" באמצע
- כל עמוד הגיע עם שם ממלא-מקום קשיח ב-HTML ("הטיול שלנו" / "הטיול הבא"), והשם האמיתי הוחלף רק אחרי שפיירבייס ענה - כלומר בכל מעבר בין עמודים
- אותו פתרון שכבר היה בשימוש לנושא ולמצב כהה: שם הטיול נשמר ב-`localStorage` לפי `tripId`, וסקריפט זעיר מיד אחרי הכותרת צובע אותו בזמן שהפרסר עוד בתוך `<body>` - לפני הציור הראשון
- ‏index שומר את הכותרת המורכבת ("שם + דגל") ואת `countryCode`, כדי שגם הדגל לא יקפוץ

### 🎨 שני באגים שהיו מאחורי חלק מה"רפרוש"
- **shopping לא החיל את ערכת הטיול בכלל בטעינה** - הסקריפט המוקדם שלו שיחזר רק בהיר/כהה, ולא את `theme-<סוג>`. התוצאה: כל טעינה התחילה בטורקיז ברירת המחדל ורק אז עברה לצבע של הטיול. עכשיו הוא זהה לשאר העמודים
- **index דרס את סוג הטיול השמור** - `applyTripPresentation()` רץ פעם אחת באתחול, לפני שהמידע הגיע, וכתב את ברירת המחדל `"abroad"` לתוך `trip-<id>-type`. כלומר ביקור בדף הבית הרס את המטמון, וכל שאר העמודים נטענו אחריו עם הפלטה הלא נכונה. עכשיו נכתב רק אחרי שהגיעה רשומת טיול אמיתית
- מאותה סיבה index גם התחיל מברירות המחדל שלו במקום מהמטמון - עכשיו הוא מאתחל את סוג הטיול, השם והמדינה מהמטמון

### ✅ אימות
- נמדד עם `PerformanceObserver`: הסקריפט שצובע את השם רץ ב-12-22ms, ו-first-contentful-paint ב-284-388ms. השם האמיתי נמצא ב-DOM כ-270-370ms לפני שהדפדפן מצייר משהו, כך שהממלא-מקום לא נצבע כלל
- הושוו `getComputedStyle` של כל אלמנט בכל 6 העמודים מול הגרסה הקודמת, עם מטמון ריק, על פני 4 ערכות × בהיר/כהה - כולם זהים לחלוטין

### ℹ️ מה עדיין יבהב
- בכניסה ראשונה לטיול חדש במכשיר, לפני שיש מטמון. מהביקור השני והלאה - לא
- אם המארגן משנה את שם היעד, מכשיר אחר יראה לרגע את השם הישן מהמטמון עד שפיירבייס עונה, ואז יתוקן

---

## v4.0.2 — כפתור "טיול חדש" באדמין + תיקון מחלקת hidden

### ✈️ יצירת טיול נוסף מתוך טיול קיים
- נוסף בתחתית פאנל האדמין סעיף "טיול נוסף" עם כפתור "יצירת טיול חדש" שמוביל לאשף. הטיול הנוכחי לא מושפע - נפתח טיול חדש ונפרד שהלוחץ הוא המארגן שלו
- הכפתור בתוך המודאל, שכבר חסום ל-`requireOrganizerAccess()`, כך שהוא ממילא זמין רק למארגן. עיצוב מתאר (outline) בצבע הנושא, כדי להבדיל אותו מכפתורי השמירה המלאים
- מתורגם לעברית ולאנגלית

### 🐛 מחלקת `hidden` לא הייתה מוגדרת באף עמוד
- באג ותיק: כל 6 העמודים משתמשים ב-`classList.toggle("hidden", ...)` מ-JavaScript, אבל אף אחד מהם לא הגדיר את הכלל `.hidden`. כל ההסתרות האלה פשוט לא עשו כלום
- **התוצאה שדווחה:** כפתור ההגדרות (⚙️) הופיע לכל אחד, לא רק למארגן. הבדיקה עצמה הייתה תקינה כל הזמן - `isOrganizer()` ו-`requireOrganizerAccess()` פעלו נכון, ולכן לחיצה החזירה "רק למארגן יש הרשאות". רק ההסתרה החזותית לא עבדה
- אותו באג הסתיר-לכאורה גם את כפתור "יציאה מהחשבון" (הופיע גם למי שלא מחובר) ואת 5 תגי הסטטוס על כרטיסי הפעולה בדף הבית (הופיעו ריקים), וב-wizard את דגל היעד שנשאר גלוי בזמן הקלדה מחדש
- הכלל `.hidden { display: none !important; }` הוגדר עכשיו פעם אחת ב-`shared.css`, לכל העמודים

### ✅ אימות
- הושוו `getComputedStyle` של כל אלמנט בכל 6 העמודים לפני ואחרי, על פני 4 ערכות טיול × בהיר/כהה
- כל ההבדלים הם אלמנטים שנושאים `hidden` (עברו ל-`display:none`) והמכולות שהתכווצו סביבם. המאפיינים שהשתנו הם גיאומטריה, `display`, וצל הפעימה של גלגל השיניים - **אף לא שינוי צבע, גופן, רקע או גבול**
- שימו לב: index ו-wizard שמים `data-theme` על `<body>`, ושאר העמודים על `<html>`. האימות הורץ בשני המסלולים

---

## v4.0.1 — shared.css: איחוד ה-CSS המשותף

ריפקטור בלבד. אין שינוי בהתנהגות או במראה של אף עמוד.

### 📦 קובץ CSS משותף
- נוצר `shared.css` שאליו הועבר כל ה-CSS שהיה משוכפל בין 6 העמודים. כל 6 הקבצים טוענים אותו לפני ה-`<style>` שלהם
- **683 שורות נמחקו מהעמודים, 42 נוספו.** ה-CSS המוטבע ירד מ-128KB ל-104KB
- מה עבר: ה-`@keyframes` המשותפים (`dialogFadeIn`, `dialogPopIn`, `overlayFadeIn`, `sheetSlideUp`, `auth-spin`), מערכת החלונות הקופצים `.custom-dialog-*` על האנימציות והזוהר שלה, ומסך טעינת האימות
- ‏places / packing / itinerary העבירו גם את מערכת ה-design tokens ומצב כהה במלואה (4 ערכות × בהיר/כהה), את התפריט התחתון, את ה-bottom sheet, ה-toast וה-FAB

### 🎛️ איך זה מוגדר
- שלושת העמודים שחולקים אוצר מילים אחד של טוקנים (`--surface`/`--text`/`--line`) נרשמים עם `<html data-tokens="core">`. index (`--card-bg`/`--text-main`), shopping (`--sea`/`--tab-bg`) ו-wizard (`--background`/`--border`) שומרים על הפלטה שלהם
- הסגר כתוב ב-`:where()` שתורם 0 לספציפיות, כך שכל סלקטור שומר בדיוק על אותו משקל שהיה לו בתוך העמוד
- החלונות הקופצים קוראים צבעים דרך שרשראות `var()` (למשל `var(--surface, var(--card-bg))`), כך שכל עמוד ממשיך להשתמש בטוקנים שלו
- ההבדלים שנשארו הוצאו ל-knobs שכל עמוד מגדיר ב-`:root` שלו: `--nav-max-width`, `--sheet-max-width`, `--fab-inset`, `--nav-label-size`, `--field-label-weight`, `--auth-loading-z`, `--auth-spinner-size`, `--dlg-danger-glow`

### ✅ אימות
- הושוו `getComputedStyle` של כל אלמנט בכל עמוד (כולל `::before`/`::after`) לפני ואחרי, על פני 4 ערכות טיול × בהיר/כהה, בשני רוחבי מסך (420px ו-1200px, כדי לכסות גם את ה-`@media (min-width:680/700px)`)
- כל 6 העמודים יצאו זהים לחלוטין. ההפרש היחיד: הזוהר של כפתור המחיקה ב-shopping במצב כהה נגזר עכשיו מ-`color-mix()` במקום `rgba()` כתוב ידנית - אותו צבע בדיוק, רק ייצוג טקסטואלי אחר

### 🚫 מה לא אוחד
- התפריט התחתון של index ו-shopping הוא רכיב אחר ("גלולה" צפה עם `.nav-item`, לא גריד עם `.bottom-nav a`) ונשאר בעמוד. איחוד היה משנה את המראה

---

## v4.0.0 — Dark Mode, AI, and Cross-Page Overhaul

זהו שדרוג מג'ורי - נגע בכל אחד מ-6 העמודים באפליקציה. מתועד כאן לפי נושא, לא לפי סדר כרונולוגי.

### 🎨 עיצוב ומצב כהה
- מצב כהה מלא בכל 6 העמודים, עם פלטת צבעים ייחודית לכל סוג טיול (חו״ל=טורקיז, מקומי=כתום, קמפינג=ירוק, מילואים=כחול-פלדה)
- שפת "glow" סלקטיבית - זוהר עדין רק על אלמנטים פעילים/ראשיים, לא בכל מקום
- איורים תלת-ממדיים מ-Fluent Emoji (Microsoft, MIT license) לכרטיסי הפעולה בדף הבית, עם נפילה חזרה (fallback) לאייקוני קו אם הטעינה נכשלת
- תפריט תחתון אוחד - אותם 6 אייקוני SVG בדיוק בכל עמוד (במקום 3 מערכות שונות שהיו קיימות)
- ברירת מחדל חכמה למצב כהה/בהיר לפי הגדרת המכשיר, עם זיכרון בחירה ידנית
- תיקון הבהוב מעבר-בין-עמודים (סקריפט מוקדם שקורא את הנושא לפני הציור הראשון של הדף) בכל 6 העמודים
- דף הבית דינמי לפי שלב הטיול (לפני/במהלך/אחרי) - כרטיסים מתחלפים ומוסתרים אוטומטית
- אייקון גישה למנהל (⚙️) גלוי עם פעימת תשומת-לב, במקום לחיצה משולשת נסתרת

### 🤖 בינה מלאכותית
- פונקציית ענן (Cloud Function) עם Gemini API להצעות ציוד חכמות, נפרסה בהצלחה
- תוקנו: כתובת שגויה, הרשאות גישה, שם מודל מיושן
- ⏳ פתוח: תקלת סנכרון תשלום בצד גוגל (לא תלוי בנו, דווח בפורום הרשמי שלהם)

### 💬 חלונות אישור מותאמים
- הוחלפו 36 שימושים ב-`alert()`/`confirm()` המובנים של הדפדפן בעיצוב ממותג ותואם-נושא, בכל 6 הקבצים
- כל חלון בכל עמוד נסגר גם בלחיצה מחוצה לו, לא רק בכפתור ביטול
- כל חלון נפתח באנימציית החלקה עדינה מלמטה (לא הייתה קיימת קודם באף אחד מהחלונות)
- כיבוד להגדרת "הפחת תנועה" (Reduce Motion) לנגישות

### 🗓️ תוכנית יומית
- זיהוי אוטומטי של "קורה עכשיו" לפי שעת הפעילות, עם טיפול חזותי שונה למצב בהיר (טבעת פועמת) וכהה (זוהר יציב)
- קישור דו-כיווני עם עמוד מקומות: כפתור "הוסף ליום" על כל מקום שמור, ו-autocomplete הפוך בשדה הוספת פעילות

### 🎒 רשימת ציוד
- קיבוץ חכם - זיהוי מילות מפתח (תרופות/תיק רחצה/אלקטרוניקה/מסמכים) עם הצעה אוטומטית להוספה כתת-פריט
- מבנה חדש: כותרת קבועה למעלה + רשימה גוללת בנפרד, שדה ההוספה עבר לכפתור צף

### 🛒 רשימת קניות
- אותו מבנה של כותרת קבועה + רשימה גוללת בנפרד

### 🔧 תיקוני יציבות ו-UX
- מחיקת טיולים מהאשף - תוקן כלל הרשאות ב-Firebase Security Rules
- שדה הוספת חבר בפאנל אדמין - ניגודיות טקסט במצב כהה, גודל, גלילה מעל המקלדת במובייל
- כמה מקרי RTL/LTR עם טקסט מעורב עברית-אנגלית (למשל "X days · Y hours to go")
- באג עדיפויות CSS שגרם לכרטיסי דף הבית "לקפוץ בחזרה" לגלוי אחרי הסתרה לפי שלב טיול
- באג מתיחת CSS Grid שגרם לריבועים ענקיים ריקים ברשימת ציוד קצרה
- הודעת "התנתקת בהצלחה" בעת יציאה מהחשבון

### ⏳ פתוח / נדחה לסבב נפרד
- הוספה/הסרה של מודולים (ציוד/קניות/וכו') דרך פאנל האדמין, אחרי שהטיול כבר נוצר
- שדרוג Node.js מגרסה 20 ל-22 בפונקציית הענן - הקובץ מוכן, ממתין לפריסה מחדש (עד 30/10/2026)
- תקלת AI בצד גוגל (סנכרון תשלום) - ממתינה לתיקון מצידם

---

## v3.1.0 ואילך (לפני שיחה זו)
ראו היסטוריית שיחות קודמת - כלל את בניית עמוד התוכנית היומית, אינטגרציית Spliit, שדרוג פונט, ותיקוני iOS.
